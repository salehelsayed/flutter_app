# 150 - Invite-accept & join outcomes as per-row inline state (replace the 11-arm accept snackbar)  (Feature Improvement)

Status: reviewed — execution-ready (DECISION-1 = terminal ghost row, DECISION-2 = keep navigating-outcome snackbars — both LOCKED; see Locked Design Decisions)
Spec: free-text intent (no formal spec) — chained from a UI/UX review of Group Messaging snackbars

## Locked Design Decisions  (resolved at review 2026-06-23 — no longer open)
- **DECISION-1 = terminal GHOST ROW.** The 8 terminal outcomes all `deletePendingInvite` in the use-case, so `_loadGroups()` (at `group_list_wired.dart:327`, before the switch) removes the live `PendingGroupInviteCard`. Terminal reasons therefore render on an **outcome-driven ghost row** keyed `pending-group-invite-outcome-<id>` (carrying a group-name snapshot), NOT on the deleted card. Rejected alternative: silent-vanish (loses the "which invite failed & why" benefit that motivates this plan).
- **DECISION-2 = KEEP the 2 navigating-outcome snackbars.** Drop the 10 non-navigating accept snackbars (8 terminal + repairPending + keep-pending bridgeError) + the stuck-Retry snackbar; KEEP a single navigate-time snackbar for `success` (`group_invite_joined`) and `joinedRecovery` (`group_invite_joined_recovery`). Rationale: dropping `success:335` would break sentinels `:1111`/`:1210`, and `joinedRecovery` has no inline surface after it navigates away. Rejected alternative: drop-all (would force rewriting `:1111`/`:1210`).

## Planning Progress
| Time | Role | Files inspected | Decision/blocker | Next action |
|---|---|---|---|---|
| 2026-06-23 | Evidence Collector | accept_pending_group_invite_use_case.dart, group_list_wired.dart, group_list_screen.dart, orbit_screen.dart, pending_group_invite_card.dart, group_list_wired_test.dart, app_en/ar/de.arb, run_test_gates.sh | verify→refute complete: 11-arm switch confirmed at :333-372; recovery-retry wrapper :395 retries bridgeError(group==null & invite present) only; 3 bridgeError shapes confirmed in use-case (:565/:568, :677, :715, success-like :562-566); `group_invite_joined_recovery` exists in en/ar/de but is referenced NOWHERE; `group_invite_waiting_for_key` absent; no migration needed (pure presentation) | Planner |
| 2026-06-23 | Planner | (as above) | Per-row state keyed by invite id alongside `_processingInviteIds` (:108), cleared in `_loadGroups` (:136); 8 terminal outcomes → restyle/remove row + reason; repairPending → "Waiting for key" spinner; bridgeError(keep-pending) → inline Retry re-entering `_onAcceptPendingInvite` (guard preserved); success-like bridgeError(group!=null) → wire dead `group_invite_joined_recovery`; drop 11 accept snackbars + Joining snackbar | Reviewer |
| 2026-06-23 | Reviewer (sufficiency) | accept_pending_group_invite_use_case.dart (full), group_list_wired.dart :100-642, group_list_screen.dart :200-324, pending_group_invite_card.dart (full), orbit_screen.dart :844-860, group_list_wired_test.dart :770-1313, app_en/ar/de.arb, run_test_gates.sh | **3 structural blockers** (see Reviewer Findings): (B1) ALL 8 terminal outcomes `deletePendingInvite` in the use-case → `_loadGroups()` at :327 (runs BEFORE the switch) drops the card → there is no `pending-group-invite-<id>` subtree for TC-01's inline reason; (B2) dropping the success snackbar (:335) breaks sentinels :1111/:1210 which assert `Joined Package Room`/`Joined Cursor Room` via findsOneWidget — INV-5 ("no snackbar for any of the 11") contradicts TC-08 ("keep :1111/:1210 unchanged"); (B3) TC-03 makes shape-b navigate, but :1155 exercises the SAME shape-b path and is listed as an unchanged sentinel — and the plan never specifies WHERE `group_invite_joined_recovery` renders after navigation (the card is gone). Plus: Blind-Spot Sweep section missing; arb/gate line numbers drifted (concurrent 149/155). | Arbiter |
| 2026-06-23 | Arbiter | (as above) | Structural verdict: **execution-ready**. B1/B2/B3 resolved (ghost outcome-row for terminals; keep navigate-time snackbar for the 2 navigating outcomes; reword INV-5). DECISION-1 (ghost row) + DECISION-2 (keep navigating snackbars) **LOCKED** at review (see Locked Design Decisions). | hand off to execution |

## Execution Progress
| Time | Phase | Files touched | Command/evidence | Decision/blocker | Next |
|---|---|---|---|---|---|
| 2026-06-23 | contract extraction (git status --short) | (none yet) | baseline: `group_list_wired_test.dart` 27/27 GREEN on HEAD; the 2 known pre-existing groups fails (GMAR-004 reopen-hydration, incoming-group-image-refresh) are in `group_conversation_wired_test.dart`, NOT here | scope confirmed (accept-only) | author RED |
| 2026-06-23 | RED tests added (WF-1) | group_list_wired_test.dart (+TC-01..09, rewrote repairPending/shape-b), NEW pending_group_invite_card_test.dart (TC-07) | wired 25 pass / 12 fail (RED-for-contract); card compile-RED on missing `PendingInviteRowState` | RED for the documented reason | seam-repair |
| 2026-06-23 | RED seam-repair | group_list_wired_test.dart | TC-01 → 4 tap-reachable terminals (revoked/alreadyUsed/wrongIdentity/expiredFreshness); notFound/duplicateGroup/expired/invalidPayload pruned (not UI-tappable, switch-read in review); TC-02 shape-a + TC-05 seams fixed | clean RED for contract reason | implement |
| 2026-06-23 | implementation (WF-2) | pending_group_invite_card.dart, group_list_screen.dart, group_list_wired.dart, app_en/ar/de.arb (+gen), run_test_gates.sh; orbit_screen.dart unchanged (defaults) | enum+ghost row+inline Retry+waitingForKey; switch rewrite; `group_invite_waiting_for_key` added; `group_invite_joined_recovery` wired; stuck-rejoin snackbar dropped; both files added to GROUP_TESTS | scoped files only; DB v92 | direct GREEN |
| 2026-06-23 | direct GREEN | (as above) | `flutter test` wired **37/0**, card **5/0**; analyze 0-new (fixed redundant test import); `git diff --check` clean | reds now green | preservation |
| 2026-06-23 | preservation + named gates | run_test_gates.sh GROUP_TESTS | `./scripts/run_test_gates.sh groups` 729/2 (only the 2 known pre-existing fails); `./scripts/run_test_gates.sh feed` **214/0** | sentinels green; gate green | QA |
| 2026-06-23 | QA (independent, WF-3 + fix) | group_list_wired.dart, group_list_wired_test.dart | 4-lens review + 11 mutations: 10/11 re-RED; **M3 SURVIVED** (TC-02 didn't lock the guard) + **D2** (15s snackbar = test hack) → both FIXED (TC-02 rapid double-tap equality + re-mutation proof; recovery snackbar → default 4s, assert at show-time; +ghost-row fragility comment, TC-05 positive asserts, TC-09 bounded pump, TC-03a de-locale). Re-run wired **41/0** (incl. 4 concurrent 153 decline tests), card 5/0 | blocking: none | accepted |

## Source Of Truth
- Spec / intent: inline below (UX review → user-locked design decisions)
- Gate definitions: `scripts/run_test_gates.sh` (script wins over prose)
- Discovery/registration: `scripts/check_reliability_simulation_discovery.sh` (no sim rows in this plan)
- Numbering / index: `Test-Flight-Improv/00-INDEX.md` (next-free = 150; files go to 149)

## Session Classification
implementation-ready (host-only closure; no migration, no device-proof — all behavior is local screen-state + render of an already-resolved use-case result)

## Exact Problem Statement
Accepting a pending group invite resolves to one of **11** `AcceptPendingGroupInviteResult` values (`accept_pending_group_invite_use_case.dart:25-40`: `success, notFound, expired, expiredFreshness, revoked, alreadyUsed, wrongIdentity, repairPending, invalidPayload, duplicateGroup, bridgeError`). The wired handler `_onAcceptPendingInvite` (`group_list_wired.dart:299`) maps **every non-success arm to a transient ~4s snackbar** via an 11-arm switch (`:333-372`): `notFound:343`, `expired:346`, `expiredFreshness:349`, `revoked:352`, `alreadyUsed:355`, `wrongIdentity:358`, `repairPending:361→group_invite_needs_key`, `invalidPayload:364`, `duplicateGroup:367`, `bridgeError:370`. Only `success:334` navigates into the new group.

This is the same UX defect class as 144/149: the **outcome is detached from the row it belongs to**. The invite card (`PendingGroupInviteCard`, keyed `pending-group-invite-<groupId>`) stays unchanged while a floating snackbar — easily missed if scrolled or backgrounded — carries the only feedback. The user can't tell *which* invite failed, can't retry an honestly-transient `bridgeError`, and gets a confusing "Failed to accept invite" even when the group **did** materialize (the success-like `bridgeError`-with-group at use-case `:562-566`, where `group != null` but the relay drain hadn't completed). A second redundancy: the stuck-rejoin `_onRetryStuckRejoin` shows a `group_joining_in_progress` ("Joining…") snackbar (`:569-572`) even though the **inline** "Joining…" badge already exists on the group card (`group_list_screen.dart:255-265` via `GroupCard.statusText`).

What must improve:
- Each accept outcome becomes **per-row inline state**, surfaced on or next to the invite row instead of a floating toast:
  - **8 terminal outcomes** (`expired, expiredFreshness, notFound, revoked, alreadyUsed, wrongIdentity, invalidPayload, duplicateGroup`) → a **one-line inline reason**, no snackbar. All 8 delete the invite (Root Cause C1), so the live card is gone after `_loadGroups` → the reason renders from an outcome-driven **ghost row** keyed `pending-group-invite-outcome-<id>` (DECISION-1 LOCKED), NOT from the (deleted) `PendingGroupInviteCard`.
  - **repairPending** → trailing spinner with **"Waiting for key"** (new key `group_invite_waiting_for_key`); the invite is KEPT (`use-case:248/:310/:596`) so the live card renders the inline state directly.
  - **bridgeError (keep-pending / retryable)** → trailing **inline Retry** that re-enters `_onAcceptPendingInvite` (re-entrancy guard preserved); the invite is KEPT (rollback returns at `:538/:568/:677/:715` without committing) so the live card renders it.
  - **bridgeError (success-like, group materialized)** → treat as join-with-recovery; **navigate** and surface the **dead** key `group_invite_joined_recovery` (`app_en.arb:1312`, ar `:1260`, de `:1260`, referenced nowhere today). Because the invite is consumed (`:554`) and the screen navigates away, the recovery copy renders as a **navigate-time snackbar on the destination**, NOT inline (the card is gone).
  - **success** keeps the navigate.
- Drop the snackbars for the **non-navigating** outcomes only: the 8 terminal arms + `repairPending` + keep-pending `bridgeError` (`:343,346,349,352,355,358,361,364,367,370`) and the redundant **`group_joining_in_progress` snackbar** in `_onRetryStuckRejoin` (`:569,572`). The **navigating** outcomes (`success:335`, and the new `joinedRecovery`) KEEP one brief navigate-time confirmation snackbar (DECISION-2 LOCKED) — dropping `success:335` would break sentinels `:1111`/`:1210`, and `joinedRecovery` has no inline surface after it navigates.

What must stay unchanged (→ preserved-green sentinels):
- The re-entrancy guard `_processingInviteIds` (`:302` contains-check, `:306` add, `:390` remove) — the inline Retry must route through `_onAcceptPendingInvite` so rapid taps don't double-arm.
- The recovery-retry wrapper `_acceptPendingInviteWithRecoveryRetry` (`:395`) and its 5×500ms loop (`:452-469`) — unchanged.
- The inline **"Joining…" badge** + stuck-group **Retry/Leave** actions (`group_list_screen.dart:255-265,296-324`, keys `group-stuck-retry-<id>`/`group-stuck-leave-<id>`).
- Decline path snackbars (`:526,529,532,548`) — OUT of scope (this plan is accept-only).
- Group gates (groups suite), feed/LetterCard suites, `flutter analyze` 0-new.

## Root Cause (verify → refute confirmed)
**Confirmed on HEAD** (uncommitted `new-feed`; not build-skew — the 11-arm switch is the live code path):
- The presentation layer collapses a rich 11-value result into ephemeral toasts: `group_list_wired.dart:333-372` is an 11-arm `switch (result)` where 10 arms call `_showSnackBar(...)` (`:335` success-toast is also a snackbar) and only `:339` navigates. There is **no per-row state surface** — `PendingGroupInviteCard` exposes only `invite/isProcessing/onAccept/onDecline` (`pending_group_invite_card.dart:9-21`), so there is nowhere to render a row-scoped outcome.
- The success-like `bridgeError`-with-group is **mis-bucketed**: the use-case returns `(bridgeError, group)` at `:562-566` when the group materialized but the inbox drain hadn't advanced, yet the handler's `bridgeError:370` arm ignores `group != null` and shows `group_invite_accept_failed`. The dead l10n key `group_invite_joined_recovery` (en `:1300`, ar `:1248`, de `:1248`) was authored for exactly this state but **wired to nothing** (`grep` of `lib/` finds zero references).
- The "Joining…" outcome is double-surfaced: an inline badge (`group_list_screen.dart:261-264` → `GroupCard.statusText` `:269`) AND a snackbar (`group_list_wired.dart:569,572`).

**Reviewer-confirmed structural constraints (NEW — these reshape the design; verified in source 2026-06-23):**
- **C1 — the 8 terminal outcomes DELETE the invite, so the card is GONE before any inline reason can render.** Every terminal arm calls `pendingInviteRepo.deletePendingInvite(groupId)` then returns: `revoked` (`use-case:174`), `expired` (`:186`, `:253`, `:261`), `expiredFreshness` (`:208`, `:280`), `invalidPayload` (`:219/:231/:265/:292/:300/:349/:599`), `wrongIdentity` (`:323`), `alreadyUsed` (`:359/:376`), `duplicateGroup` (`:628`); `notFound` (`:162`) never had a card. Then `_onAcceptPendingInvite` calls `await _loadGroups()` at **`group_list_wired.dart:327` — BEFORE the `switch` at `:333`** — and `_loadGroups` re-reads `pendingInvites` (`:145-152`) and `setState`s the visible list (`:169-177`). The deleted invite is no longer in `_pendingInvites`, so `PendingGroupInviteCard` keyed `pending-group-invite-<id>` is removed from the tree. **Consequence: "render the inline reason on the card" cannot work for terminals — there is no card subtree, and TC-01's `find.descendant(of: find.byKey(ValueKey('pending-group-invite-<id>')))` finds nothing.** Resolution (DECISION-1 LOCKED): a terminal reason renders from an **outcome-driven ghost row that survives invite deletion** (keyed `pending-group-invite-outcome-<id>`, see Real Scope C1 resolution). By contrast `repairPending` (kept: `:248/:310/:596`) and the keep-pending `bridgeError` shape-a (invite never committed — commit at `:554` is gated on `group != null`; rollback returns at `:538/:568/:677/:715` keep the invite, and the retry-loop guard at `:457-460` confirms the invite is still present) DO keep their card → TC-02/TC-04 inline-on-card are sound.
- **C2 — the `success` arm shows a snackbar AND navigates; that snackbar is asserted by the "keep-green" sentinels.** `success` (`group_list_wired.dart:335-340`) calls `_showSnackBar(l10n.group_invite_joined(name))` *and* `_onGroupTap(group)`. `group_invite_joined` renders `"Joined {name}"`, and the sentinels assert it via `findsOneWidget`: `:1143 find.text('Joined Package Room')` and `:1242 find.text('Joined Cursor Room')`. **Dropping `:335` therefore breaks `:1111` and `:1210` — which the plan also lists as untouched green sentinels (TC-08).** Resolution (DECISION-2 LOCKED): `success` and `joinedRecovery` keep their navigate-time snackbar; INV-5 is rescoped to the 10 non-navigating outcomes, so `:1111`/`:1210` stay green unchanged.
- **C3 — shape-b `(bridgeError, group)` is the path `:1155` already exercises, and TC-03 changes its navigation.** The use-case returns shape-b at `:562-566` only when the group materialized but `!inboxDrained && !acceptedGroupAdvanced` for an inline invite (welcomeKeyPackage null) — exactly what `:1155` sets up (`group:join`=false, `group:inboxRetrieveCursor`=false, default inline `makePendingInvite`). On HEAD shape-b does NOT navigate (the generic `bridgeError:370` arm only toasts). TC-03 makes shape-b navigate + surface `group_invite_joined_recovery`. So TC-03 **changes the behavior of the exact path `:1155` covers**, and the plan must (a) reconcile/refresh `:1155` (it likely stays green because the post-navigate list is offstage so `find.text('Book Club')` still resolves to 1 via the conversation title — but this MUST be confirmed, not assumed, and the harness must be able to build `GroupConversationScreen`, which EK011 `:1111` proves it can), and (b) specify WHERE the recovery copy renders after navigation — the card is consumed/gone, so it cannot be inline (see C2 resolution: a navigate-time snackbar).

Refuted / do-NOT-re-introduce:
- ❌ "the result enum has 12 values" — **FALSE**. Exactly **11** (`accept_pending_group_invite_use_case.dart:25-40`).
- ❌ "repairPending already keeps the row inline" — **PARTIAL**. The use-case keeps the pending invite (`:248/:310/:596`) so the *card* stays, but the only repairPending *feedback* is the **transient** `group_invite_needs_key` snackbar (`:361`); the existing test asserts that snackbar text (`group_list_wired_test.dart:1273`), still visible at 30 pumps. There is no inline row state — migrating to inline requires rewriting that assertion.
- ❌ "bridgeError is one shape" — **FALSE**. Three shapes: (a) keep-pending retryable `(bridgeError, null)` with the invite still present (`:464`, `:538`, `:568`, `:677`, `:715`); (b) success-like `(bridgeError, group)` materialized (`:562-566`); the recovery-retry wrapper (`:452-456`) re-attempts only shape (a).
- ❌ "stuck-Retry has no inline surface, keep its snackbar" — **FALSE**. The inline "Joining…"/"Couldn't join — retry" badge already renders from `_rejoinAttempts` (`group_list_screen.dart:255-265`); the snackbar is pure redundancy.
- ❌ "needs a DB migration" — **FALSE**. All state is in-State presentation; no schema, no persisted field. DB stays v92, next-free 093 untouched.

## Real Scope
In scope:
- `PendingGroupInviteCard` (`pending_group_invite_card.dart`): add an **outcome param** (e.g. `outcome` of a new small presentation enum `PendingInviteRowState { idle, processing, waitingForKey, retryable, joinedRecovery }` + optional `inlineReasonText`/`onRetry`) so the row can render: a one-line reason, a "Waiting for key" trailing spinner, or an inline **Retry** control — preserving the existing `isProcessing` in-button spinner (`:152-161`) and the `ValueKey('pending-group-invite-<groupId>')` (`:45`).
- `group_list_screen.dart` (`:200-242`): thread the per-row outcome map + `onRetryPendingInvite` callback down. For **repairPending / retryable** (invite KEPT), pass the outcome into the live `PendingGroupInviteCard` (`:212-221`) alongside the existing `processingInviteIds.contains` wiring. For **terminal ghost rows**: render an additional list section that iterates outcome-map entries whose id is **no longer in `pendingInvites`**, emitting a compact reason row keyed `pending-group-invite-outcome-<id>` (so the reason survives `deletePendingInvite` + `_loadGroups`). This requires the outcome map value to carry a **display-name snapshot** (the deleted invite's `groupName`), captured in the wired switch before the row is gone.
- `orbit_screen.dart` (`:844-860`): the same `PendingGroupInviteCard` is rendered there and **wires accept** via `data.onAcceptPendingInvite` (a separate OrbitWired handler, not `group_list_wired._onAcceptPendingInvite`). Adding **required** params to the card would compile-break this callsite → give the new outcome/onRetry params **defaults** (`idle`/null) so Orbit keeps current behavior; lock the chosen default in a test note in TC-07. (Orbit's accept does not get inline outcomes in this plan — see Accepted Differences.)
- `group_list_wired.dart`:
  - Add per-row outcome state keyed by invite id alongside `_processingInviteIds` (`:108`) — value carries the outcome enum + `inlineReasonText` + a **name snapshot** for ghost rows. **Cleared in `_loadGroups`** (`:136`, inside the `setState` at `:169-177`). NOTE the ordering: `_loadGroups()` runs at `:327` **before** the `switch` at `:333`, so clearing-then-setting in one accept is correct (clears stale, then the switch sets fresh, surviving until the NEXT refresh/resume).
  - Wrap the new outcome assignment in `setState` (the old arms used imperative `_showSnackBar` and needed no rebuild; the inline state does). The `finally` `setState` at `:388-391` also rebuilds, but assign inside the switch for clarity.
  - Rewrite the 11-arm switch (`:333-372`): 8 terminal arms → set the ghost-row outcome (enum + reason + name snapshot), **no snackbar**, and let `_loadGroups` drop the live card; `repairPending` → `waitingForKey` (live card); `bridgeError`+`group!=null` → `joinedRecovery` (navigate + navigate-time `group_invite_joined_recovery` snackbar); `bridgeError`+`group==null` → `retryable` inline Retry (live card); `success` → navigate (KEEP `group_invite_joined` snackbar per DECISION-2).
  - Add `_onRetryPendingInvite(invite)` that re-enters `_onAcceptPendingInvite(invite)` **without bypassing** `_processingInviteIds`.
  - Drop the `group_joining_in_progress` snackbar in `_onRetryStuckRejoin` (`:569,572`); keep `forceGroupRejoinEligible` + `rejoinGroupTopics` + `_loadGroups`.
- l10n: **add 1 new key** `group_invite_waiting_for_key` (plain string, no @-metadata) to en/ar/de **by KEY**; **wire** the existing dead `group_invite_joined_recovery`. Reuse `btn_retry` for the inline Retry label and existing terminal copy strings (`group_invite_no_longer_available`, `group_invite_expired`, etc.) as the inline reason text.
- Harness: **add `test/features/groups/presentation/group_list_wired_test.dart` to `GROUP_TESTS`** in `scripts/run_test_gates.sh` (it is swept by host-all auto-glob today but is NOT in the curated `groups` gate). New `pending_group_invite_card_test.dart` lands under `test/features/groups/presentation/widgets/` (auto-globbed by host-all; also explicitly run via the direct `flutter test` gate).

Out of scope (owning work named):
- The **decline** path (`_onDeclinePendingInvite` `:511`, snackbars `:526/:529/:532/:548`) — a future "decline outcomes inline" session.
- The sender-side `GroupInviteDeliveryStatus` / `group_invite_status_presentation.dart` — a **different enum** (delivery, not accept); not touched. No overlap with plan 144's banner (that lives only in `group_conversation_wired.dart`).
- Any change to `accept_pending_group_invite_use_case.dart` result values, the recovery-retry loop timing, or the relay/drain layer.

## Files To Inspect Next
Production:
- `lib/features/groups/presentation/screens/group_list_wired.dart` — handler `_onAcceptPendingInvite` `:299`; guard `_processingInviteIds` `:108`/`:302`/`:306`/`:390`; 11-arm switch `:333-372`; recovery-retry wrapper `:395-471`; `_loadGroups` clear point `:136-177`; `_onRetryStuckRejoin` `:568-587` (drop snackbar `:569,572`); `_showSnackBar` `:559`; `build` wiring `:622-639`.
- `lib/features/groups/presentation/screens/group_list_screen.dart` — `PendingGroupInviteCard` render `:206-225`; "Joining…" badge `:255-265`; stuck actions `:296-324`; `_joinGiveUpThreshold` `:247`.
- `lib/features/groups/presentation/widgets/pending_group_invite_card.dart` — ctor params `:9-21`; key `:45`; in-button spinner `:152-161`.
- `lib/features/orbit/presentation/screens/orbit_screen.dart` — second `PendingGroupInviteCard` callsite `:848`.
- `lib/features/groups/application/accept_pending_group_invite_use_case.dart` — result enum `:25-40` (NOT edited); bridgeError shapes `:464/:538/:565/:568/:677/:715`, success-like `:562-566`.
- `lib/l10n/app_en.arb` (`group_invite_joined_recovery:1312`, `group_invite_needs_key:1309`), `app_ar.arb` (`group_invite_joined_recovery:1260`), `app_de.arb` (`group_invite_joined_recovery:1260`) — append-by-key.
- **⚠ Line-number drift:** the arb keys and the `GROUP_TESTS` array lines moved since this plan was first drafted (concurrent uncommitted 149/155 work in the tree). Verified-current values (2026-06-23): `group_invite_joined_recovery` = en **:1312** / ar **:1260** / de **:1260** (plan body earlier said 1300/1248/1248); `group_invite_needs_key` = en **:1309** (was :1297); `GROUP_TESTS` entries `letter_card_test`/`group_conversation_screen_test`/`group_conversation_wired_test` = **:131/:132/:133** (was :129/:130/:131). The `group_list_wired.dart` and `pending_group_invite_card.dart` line numbers cited throughout are **accurate on HEAD**. Re-confirm all line anchors by KEY/name at execution time, not by line number.
Direct tests:
- `test/features/groups/presentation/group_list_wired_test.dart` — rewrite repairPending `:1250-1276`; add per-outcome inline rows + inline-Retry + success-like-recovery + no-snackbar locks.
- `test/features/groups/presentation/widgets/pending_group_invite_card_test.dart` — NEW (render-only widget tests for the new outcome param).
Dependency-only context:
- `lib/features/groups/domain/models/pending_group_invite.dart` (invite shape — not edited).

## Existing Tests Covering This Area
- `group_list_wired_test.dart:1111` "EK011 accepts a key-package-bound pending invite …" — success row-removal + navigate; asserts row gone `:1138-1141`, **`Joined Package Room` `:1143` = the SUCCESS SNACKBAR** (`group_invite_joined("Package Room")`). PASSES. **Sentinel — keep green** (DECISION-2 LOCKED keeps `success:335`, so this stays unchanged). **Do NOT delete `success:335`** or this reds. Also confirms the harness CAN build `GroupConversationScreen` post-navigate (relevant to TC-03/C3).
- `group_list_wired_test.dart:1210` "accept drains all inbox cursor pages before clearing spinner" — success row-removal `:1238-1241`, **`Joined Cursor Room` `:1242` = the SUCCESS SNACKBAR**. PASSES. **Sentinel — keep green** (same as `:1111`; `success:335` stays).
- `group_list_wired_test.dart:1155` "bridgeError accept of an inline invite consumes it and materializes the group …" — this **IS the shape-b path** (`(bridgeError, group!=null)`, use-case `:562-566`): invite gone `:1190`, group present `:1193`, row gone `:1196`, `Book Club` listed `:1203`; snackbar **not** asserted (`:1201-1202`). PASSES on HEAD (no navigation today). **⚠ TC-03 CHANGES this path to navigate.** After the fix, `:1155` must be re-verified: `find.text('Book Club') findsOneWidget` should still hold (post-navigate the list is offstage → only the conversation title matches), but this is a behavior change on a "keep-green" sentinel — **confirm it stays green or update it**; do not assume. If kept, add an explicit navigate assertion so `:1155` and TC-03 agree on the new behavior.
- `group_list_wired_test.dart:1031` "accept retries rollback until latest metadata is recovered" — asserts `Joined test 3` `:1102` and **no** "Invite accepted, but recovery is still catching up" / "Failed to accept invite" (`:1104,:1107`). PASSES. Note: the asserted-absent recovery text is NOT the `group_invite_joined_recovery` copy — keep this green (success path, not bridgeError-with-group).
- `group_list_wired_test.dart:1250` "repair-pending accept keeps the invite row and shows key-material warning" — asserts invite kept `:1266`, row present `:1270-1272`, and `find.text('Invite needs fresh key material')` `:1273` (the TRANSIENT `group_invite_needs_key` snackbar). PASSES. **Locks OLD snackbar behavior → must be REWRITTEN to assert the inline `group_invite_waiting_for_key` row state.**
- `group_list_wired_test.dart:1278` "IJ014 repairable join-material failure keeps pending invite visible" — also asserts `Invite needs fresh key material` `:1307` (repairPending). PASSES. **Rewrite** to the inline reason OR keep (both reach repairPending) — pin to inline.
- `group_list_screen.dart` "Joining…" badge — exercised by `group_list_wired_test.dart:772-790` (`find.text('Joining…')` `:786`) and stuck-retry `:791-808` (`Couldn't join — retry` `:807`, tap `group-stuck-retry-g-1` `:550`). PASSES. **Sentinel** — must stay green after dropping the stuck-Retry snackbar.

Missing coverage gaps (ZERO assertions today): the **8 terminal outcomes** as inline row state; the **inline Retry** for keep-pending `bridgeError`; the **success-like `bridgeError`-with-group** → join+recovery navigate; the **no-snackbar** lock for accept outcomes; the **no-snackbar** lock for stuck-Retry; `PendingGroupInviteCard` rendering of the new outcome param.

Already in curated family arrays?: `letter_card_test.dart` ✅ in `GROUP_TESTS` (`:129`). `group_conversation_screen_test.dart` ✅ (`:130`), `group_conversation_wired_test.dart` ✅ (`:131`). **`group_list_wired_test.dart` ❌ NOT in `GROUP_TESTS`** — it is swept only by the host-all auto-glob (`classify_path` subdir patterns, `run_test_gates.sh:608` covers feature-root files only; `presentation/` files match a recognized subdir and run under `feature-host-all`/`host-all`, not the curated `groups` gate). → **registration step required.** The new `pending_group_invite_card_test.dart` under `test/features/groups/presentation/widgets/` is likewise host-all-globbed only (peers `group_name_panel_test.dart`, `glow_fab_test.dart` already there) → also add to `GROUP_TESTS` (or run via the direct gate; this plan adds it to `GROUP_TESTS` for curated coverage).

## RED Test Catalog  (add/rewrite BEFORE any production code — INV-RED-FIRST)

1. `group_list_wired_test.dart`::"terminal accept outcome shows an inline reason row and no snackbar"  *(data-driven over the 8 terminal results)*
   - Tier: integration/widget (wired screen, fakes + real repos in setUp)
   - Shape/setup: for each of `notFound, expired, expiredFreshness, revoked, alreadyUsed, wrongIdentity, invalidPayload, duplicateGroup`, drive the use-case to that result using the seams the existing arms hit (e.g. expired `makePendingInvite(expiresAt: past)`; revoked via `saveRevokedInvite`; wrongIdentity via a recipient-bound payload + mismatched `senderPeerId`; alreadyUsed via `getConsumedInvite`/welcome tombstone; invalidPayload via a security-failure payload). Tap `pending-group-invite-accept-<id>`, pump.
   - **⚠ CRITICAL (Root Cause C1): the live `PendingGroupInviteCard` is GONE after the tap** — every terminal arm `deletePendingInvite`s and `_loadGroups()` (at `:327`, before the switch) removes it. So the finder MUST target the ghost row, NOT the deleted card.
   - RED on HEAD because: HEAD shows the matching `_showSnackBar` text (`:343-367`) and renders NO ghost row. Asserting `find.byKey(ValueKey('pending-group-invite-outcome-<id>'))` finds nothing; asserting the corresponding snackbar is **absent** fails (it is shown).
   - GREEN asserts: per outcome, a ghost row keyed `pending-group-invite-outcome-<id>` renders, and the mapped reason text is a descendant of it (`find.descendant(of: find.byKey(ValueKey('pending-group-invite-outcome-<id>')), matching: find.text(reason))`); the live `pending-group-invite-<id>` card is `findsNothing`; **no** `SnackBar` with that reason (`find.byType(SnackBar)` → `findsNothing`).
   - Mutation that re-reds: restore one terminal arm's `_showSnackBar(...)` + remove its ghost-row assignment → snackbar reappears / ghost row gone → red.
   - Distinct-event discriminator: assert reason text present **inside the ghost row** AND `find.byType(SnackBar)` `findsNothing` (separates "inline ghost row" from "still a toast").

2. `group_list_wired_test.dart`::"bridgeError keep-pending accept shows an inline Retry that re-runs accept through the guard"
   - Tier: integration/widget
   - Shape/setup: stub accept's first attempt to resolve `(bridgeError, null)` with the invite still present (shape a — e.g. `group:join` fails AND the recovery-retry loop exhausts; or force the wrapper to return shape a by leaving the pending invite present). Tap accept; pump past the 5×500ms recovery loop.
   - RED on HEAD because: HEAD shows `group_invite_accept_failed` snackbar (`:370`) and leaves the card in its idle state — there is NO inline Retry control (`find.byKey(ValueKey('pending-group-invite-retry-<id>'))` findsNothing). Asserting that Retry key present fails.
   - GREEN asserts: card shows an inline Retry keyed `pending-group-invite-retry-<id>`; tapping it re-enters `_onAcceptPendingInvite` (assert `bridge.commandLog`/accept invocation count increments, or the spinner re-appears); `group_invite_accept_failed` snackbar **absent**; the `_processingInviteIds` guard is respected (a rapid double-tap does NOT double-arm — assert accept invoked once per tap).
   - Mutation that re-reds: drop the `retryable` branch (route bridgeError-null back to `_showSnackBar`) → Retry key gone → red. Second mutation: make `_onRetryPendingInvite` bypass `_processingInviteIds` → double-tap double-arms → guard assertion reds.

3. `group_list_wired_test.dart`::"bridgeError with materialized group navigates and reports join-with-recovery (not failure)"  *(wires the dead `group_invite_joined_recovery`)*
   - Tier: integration/widget
   - Shape/setup: drive accept to `(bridgeError, group)` (shape b — group materialized, inbox not drained). The seam is exactly the existing `:1155` setup: default inline `makePendingInvite()` (welcomeKeyPackage null), `bridge.responses['group:join'] = {ok:false}` and `group:inboxRetrieveCursor = {ok:false}` so `:562-566` returns `(bridgeError, group!=null)`. Tap accept; pump (≥220 frames, like `:1155`).
   - RED on HEAD because: HEAD's `bridgeError:370` arm ignores `group != null` → shows `group_invite_accept_failed` and does NOT navigate. Asserting navigation into `GroupConversationScreen` + the `group_invite_joined_recovery` copy (and NOT `Failed to accept invite`) fails.
   - GREEN asserts: navigates to the group (`find.byType(GroupConversationScreen) findsOneWidget` — the cleanest discriminator; `_onGroupTap` fired); `group_invite_accept_failed` snackbar **absent**; the join-with-recovery copy `group_invite_joined_recovery` is surfaced **as a navigate-time snackbar** (the card is consumed/gone, so it CANNOT be inline) — assert its rendered text (`"Joined Book Club, but recovery is still catching up"`) present. **The invite is consumed (`:554`) → assert the live card `pending-group-invite-<id>` is `findsNothing`** (parity with `:1155:1196`).
   - Mutation that re-reds: revert the `bridgeError`+`group!=null` branch back to the generic `bridgeError` arm → no navigate / failure copy → red.
   - **Sibling reconciliation (C3):** this TC and the existing `:1155` sentinel exercise the SAME path. Either strengthen `:1155` with the navigate assertion (so they agree) or fold `:1155` into TC-03. Do NOT leave `:1155` asserting the old no-navigate behavior — that would lock the bug back in.

3a. `group_list_wired_test.dart`::"l10n source-wiring lock: group_invite_joined_recovery is referenced from the accept handler"
   - Tier: widget/source-lock (string presence in the rendered tree under shape-b)
   - RED on HEAD: the key is dead (zero references) → no path renders it → assertion finds nothing.
   - GREEN: covered by TC-03's text assertion; this row guards that the key stops being dead. Mutation: remove the `joinedRecovery` wiring → key dead again → red.

4. `group_list_wired_test.dart`::"repair-pending accept shows an inline 'Waiting for key' row and no snackbar"  *(rewrite of `:1250-1276`)*
   - Tier: integration/widget
   - Shape/setup: `makePendingInvite(overrideGroupKey: '')` (the existing repairPending seam, `:1253`); tap accept; pump 30.
   - RED on HEAD because: HEAD keeps the row but the only feedback is the transient `group_invite_needs_key` snackbar (`:361`, asserted at `:1273`); there is NO inline `group_invite_waiting_for_key` text and no trailing spinner state on the card. Asserting the new inline text + absence of the snackbar fails (snackbar still shows on HEAD).
   - GREEN asserts: invite row still present (`find.byKey(ValueKey('pending-group-invite-<id>'))` findsOneWidget, unchanged from `:1270`); inline `group_invite_waiting_for_key` ("Waiting for key") text inside the card; trailing spinner state present; **no** `group_invite_needs_key` snackbar; `group:join` still not called (`:1274` preserved).
   - Mutation that re-reds: restore `_showSnackBar(l10n.group_invite_needs_key)` for repairPending + remove the `waitingForKey` row state → snackbar back / inline gone → red.

5. `group_list_wired_test.dart`::"no accept snackbar for any non-navigating outcome (lock)"  *(scoped no-snackbar lock)*
   - Tier: integration/widget
   - Shape/setup: run accept to a representative terminal result (e.g. notFound) and to repairPending and to keep-pending bridgeError; after pump, assert `find.byType(SnackBar)` `findsNothing` (covering that the 10 non-navigating snackbars are gone). **Scope note (DECISION-2):** the 2 NAVIGATING outcomes (`success`, `joinedRecovery`) are NOT covered here — they keep a navigate-time snackbar and are locked positively by TC-08 (success) and TC-03 (joinedRecovery). Do NOT assert global `findsNothing` for those.
   - RED on HEAD because: HEAD shows a `SnackBar` for each of these non-navigating arms → `findsNothing` fails.
   - Mutation that re-reds: re-add any single `_showSnackBar` to a non-navigating accept arm → `SnackBar` present → red.

6. `group_list_wired_test.dart`::"stuck-rejoin Retry updates the inline badge without a snackbar"  *(new lock; today there is none)*
   - Tier: integration/widget
   - Shape/setup: reuse the stuck-group setup from `:791-808` (rejoinAttempts ≥ `_joinGiveUpThreshold`); tap `group-stuck-retry-<id>` (`:550`); pump.
   - RED on HEAD because: HEAD's `_onRetryStuckRejoin` shows the `group_joining_in_progress` snackbar (`:572`). Asserting `find.byType(SnackBar)` `findsNothing` after the tap fails.
   - GREEN asserts: after tapping stuck-Retry, **no** snackbar; the inline badge/state path still runs (`forceGroupRejoinEligible` + `rejoinGroupTopics` invoked via `bridge.commandLog` / repo spy; `_loadGroups` ran). The existing "Joining…"/"Couldn't join — retry" badge assertions (`:786,:807`) stay green.
   - Mutation that re-reds: restore the `_showSnackBar(joiningMessage)` in `_onRetryStuckRejoin` → snackbar back → red.

7. `pending_group_invite_card_test.dart`::"renders inline reason / waiting-for-key spinner / Retry per outcome param"  *(NEW widget file)*
   - Tier: widget (pump `PendingGroupInviteCard` directly under a `MaterialApp` + `AppLocalizations`)
   - Shape/setup: pump the card with each **card-supported** outcome value — `idle` (existing accept/decline buttons), `processing` (existing in-button spinner `:152-161` preserved), `waitingForKey` (trailing spinner + `group_invite_waiting_for_key`), `retryable` (inline Retry keyed `pending-group-invite-retry-<id>` fires `onRetry`). The card enum is `{ idle, processing, waitingForKey, retryable }` ONLY — terminal reasons live on the ghost row (TC-01, a `group_list_screen`-level widget) and `joinedRecovery` navigates (TC-03); neither is a card state. Also assert the Orbit default: pump with no outcome param → renders today's accept/decline buttons (locks the `orbit_screen.dart:848` default).
   - RED on HEAD because: the new outcome param + `onRetry` + `pending-group-invite-retry-<id>` key don't exist → compile-time red (param absent).
   - GREEN asserts: each outcome renders its control; `processing` still shows the in-button `CircularProgressIndicator`; `retryable` Retry key present + fires `onRetry`; existing `pending-group-invite-accept-<id>`/`-decline-<id>` keys + key `pending-group-invite-<id>` unchanged.
   - Mutation that re-reds: remove the `waitingForKey`/`retryable` render branch in `pending_group_invite_card.dart` → controls gone → red.

8. `group_list_wired_test.dart`::"success accept still navigates and removes the row (preservation)"  *(sentinel — existing `:1111`/`:1210`)*
   - Tier: integration/widget
   - Asserts: a `success` accept removes `pending-group-invite-<id>`, lists/navigates to the group, and still shows the `group_invite_joined` snackbar `Joined <name>` (parity with `:1143`/`:1242`; DECISION-2 LOCKED keeps it). Guards the switch rewrite against breaking the navigate arm or accidentally dropping the kept success snackbar.

9. `group_list_wired_test.dart`::"repairPending row reverts to idle accept card after resume (lifecycle)"  *(blind-spot: derived-state durability)*
   - Tier: integration/widget
   - Shape/setup: `makePendingInvite(overrideGroupKey: '')` → accept → assert inline `waitingForKey` (TC-04 state) present; then simulate resume by invoking the lifecycle path that calls `_loadGroups` (drive `didChangeAppLifecycleState(AppLifecycleState.resumed)` via the binding observer, or re-pump after a `_loadGroups`-triggering stream emit); pump.
   - RED on HEAD because: no inline state exists yet (this TC only becomes meaningful once TC-04 adds `waitingForKey`; before the clear-in-`_loadGroups` is wired, the state would stick). After the fix it goes GREEN.
   - GREEN asserts: post-resume the invite row shows the plain `idle` accept/decline buttons again (`pending-group-invite-accept-<id>` enabled, no `waitingForKey` spinner, no `pending-group-invite-outcome-<id>` ghost row); the invite itself is still present in the repo (KEPT).
   - Mutation that re-reds: remove the `_inviteRowOutcomes` clear from `_loadGroups` → stale "Waiting for key" persists after resume → red.

## Test Coverage Matrix  (zero empty cells)
| Spec case | Behavior props | Tier | Test file::name | RED reason on HEAD | Mutation revert | Acceptance gate cmd | Harness registration |
|---|---|---|---|---|---|---|---|
| TC-01 8 terminal outcomes → inline reason GHOST row, no snackbar | outcome-driven ghost row (card is DELETED — C1); reason scoped to `pending-group-invite-outcome-<id>` | integration/widget | wired::"terminal accept outcome shows an inline reason row …" | HEAD shows snackbar `:343-367`, no ghost row; live card already gone after `_loadGroups:327` | restore one terminal `_showSnackBar` + drop ghost-row assign | `./scripts/run_test_gates.sh groups` | **add `group_list_wired_test.dart` to `GROUP_TESTS`** |
| TC-02 bridgeError(keep-pending) → inline Retry via guard | inline Retry + guard preserved | integration/widget | wired::"bridgeError keep-pending accept shows an inline Retry …" | HEAD shows `group_invite_accept_failed`, no Retry key | route bridgeError-null back to `_showSnackBar` / bypass `_processingInviteIds` | `./scripts/run_test_gates.sh groups` | add `group_list_wired_test.dart` to `GROUP_TESTS` |
| TC-03 bridgeError(group!=null) → navigate + recovery copy | navigate + dead-key wired | integration/widget | wired::"bridgeError with materialized group navigates …" | HEAD `:370` ignores `group!=null`, shows failure, no nav | revert `bridgeError`+`group!=null` branch | `./scripts/run_test_gates.sh groups` | add `group_list_wired_test.dart` to `GROUP_TESTS` |
| TC-03a `group_invite_joined_recovery` source-wiring lock | dead-key now referenced | widget/source-lock | wired::"l10n source-wiring lock: group_invite_joined_recovery …" | key dead, zero references → not rendered | remove joinedRecovery wiring | `./scripts/run_test_gates.sh groups` | add `group_list_wired_test.dart` to `GROUP_TESTS` |
| TC-04 repairPending → inline "Waiting for key" row, no snackbar | inline row state (rewrite of `:1250`) | integration/widget | wired::"repair-pending accept shows an inline 'Waiting for key' row …" | HEAD shows `group_invite_needs_key` snackbar `:361/:1273` | restore needs_key snackbar + drop waitingForKey row | `./scripts/run_test_gates.sh groups` | add `group_list_wired_test.dart` to `GROUP_TESTS` |
| TC-05 no accept snackbar for any NON-NAVIGATING outcome | scoped no-snackbar lock (navigating outcomes keep a snackbar — DECISION-2) | integration/widget | wired::"no accept snackbar for any non-navigating outcome (lock)" | HEAD shows a SnackBar per non-nav arm | re-add any one `_showSnackBar` to a non-navigating arm | `./scripts/run_test_gates.sh groups` | add `group_list_wired_test.dart` to `GROUP_TESTS` |
| TC-06 stuck-Retry no snackbar, badge updates | redundant-snackbar drop | integration/widget | wired::"stuck-rejoin Retry updates the inline badge without a snackbar" | HEAD shows `group_joining_in_progress` snackbar `:572` | restore `_showSnackBar(joiningMessage)` in `_onRetryStuckRejoin` | `./scripts/run_test_gates.sh groups` | add `group_list_wired_test.dart` to `GROUP_TESTS` |
| TC-07 card renders outcome param | widget render | widget | pending_group_invite_card_test::"renders inline reason / waiting-for-key spinner / Retry per outcome param" | new outcome/`onRetry` params absent → compile red | remove waitingForKey/retryable render branch | `./scripts/run_test_gates.sh groups` | **add `pending_group_invite_card_test.dart` to `GROUP_TESTS`** (also AUTO host-all glob) |
| TC-08 success navigate+row-removal preserved | preservation | integration/widget | wired::"success accept still navigates and removes the row (preservation)" | n/a (sentinel) | break the success navigate arm → re-reds | `./scripts/run_test_gates.sh groups` | add `group_list_wired_test.dart` to `GROUP_TESTS` |
| TC-09 derived row-state does NOT survive resume (lifecycle) | in-memory `_inviteRowOutcomes` cleared on `_loadGroups`; repairPending reverts to idle | integration/widget | wired::"repairPending row reverts to idle accept card after resume" | n/a today (no inline state exists yet → becomes RED once TC-04 adds `waitingForKey`, GREEN after clear-on-`_loadGroups` is wired) | remove the clear in `_loadGroups` → stale "Waiting for key" persists after resume → red | `./scripts/run_test_gates.sh groups` | add `group_list_wired_test.dart` to `GROUP_TESTS` |

## Invariants (locked by tests)
- INV-1: every non-navigating accept outcome renders as **per-row inline state**, never a snackbar → TC-01/04/05. Surface depends on whether the invite survives: `repairPending`/keep-pending-`bridgeError` (invite KEPT) render on the live `PendingGroupInviteCard`; the 8 terminal outcomes (invite DELETED by the use-case) render on an outcome-driven ghost row keyed `pending-group-invite-outcome-<id>` (DECISION-1 LOCKED = ghost row).
- INV-2: a keep-pending `bridgeError` exposes an inline **Retry** that re-enters `_onAcceptPendingInvite` **through** the `_processingInviteIds` guard (no double-arm on rapid taps) → TC-02.
- INV-3: a `bridgeError` whose group materialized (`group != null`) **navigates** and reports join-with-recovery via `group_invite_joined_recovery` — never "Failed to accept invite" → TC-03/03a.
- INV-4: `repairPending` keeps the row with an inline **"Waiting for key"** (`group_invite_waiting_for_key`) state, not a transient snackbar → TC-04.
- INV-5: **no accept snackbar** is shown for any of the **10 non-navigating** outcomes (8 terminal + repairPending + keep-pending bridgeError — those `_showSnackBar` calls are gone); the **2 navigating** outcomes (`success`, `joinedRecovery`) keep a single navigate-time confirmation snackbar (DECISION-2); **no stuck-Retry "Joining…" snackbar** → TC-05/06 (+ TC-03/08 lock the navigating snackbars positively).
- INV-6: `success` accept still navigates + removes the row; the inline "Joining…"/stuck Retry/Leave badges stay unchanged → TC-08 (+ `:786/:807` sentinels).
- INV-RED-FIRST and INV-MUTATION-VERIFIED apply to every row.

## Blind-Spot Sweep  (evergreen classes the spec cases above don't name — row added OR justified N/A)
- **Lifecycle / derived-state durability** — `_inviteRowOutcomes` is **in-memory derived state**, NOT persisted; it is intentionally cleared on every `_loadGroups` (refresh/resume/init `:125/:132/:327`). On process restart or background→resume: terminal ghost rows vanish (their invite is already deleted — acceptable, they were transient acknowledgements); a `repairPending` invite re-renders from the repo (still KEPT) but **reverts to a plain `idle` card — the "Waiting for key" inline state does NOT reconstruct** (it was derived from the accept result, not stored). → **Add TC-09 (reopen/resume): a repairPending row, after a simulated resume (`didChangeAppLifecycleState(resumed)` → `_loadGroups`), shows the idle accept/decline card again (no stale "Waiting for key", no ghost row).** This is the accepted behavior (re-tapping Accept re-derives it) but it MUST be test-locked, not assumed. Treat "rely on the repo round-trip for durability" as the red flag the checklist warns about.
- **Sibling-surface consistency** — the parallel capability on `PendingGroupInviteCard` is the **decline** path (`_onDeclinePendingInvite:498`, snackbars `:526/:529/:532/:548`). It stays on snackbars — a **deliberate, named** asymmetry (Accepted Differences: future "decline outcomes inline" session). The second sibling is the **Orbit** callsite (`orbit_screen.dart:848`) which wires its own accept handler; it keeps `idle`/no-outcome behavior by param default — **test-locked in TC-07** (assert the default render = today's accept/decline buttons, no outcome controls). No other capability gate is touched.
- **Destructive-action side-effects** — this plan adds **no new** delete/cleanup path. Invite deletion happens entirely inside the unchanged use-case (`deletePendingInvite`). The inline **Retry** re-runs `_onAcceptPendingInvite`, which can re-trigger the use-case's delete on a now-terminal result — that is the same path TC-01 covers (terminal → ghost row), so no new side-effect to assert. → **N/A (no new destructive affordance; existing deletes are use-case-owned and out of scope).**
- **Invariant re-verification under new transitions** — the new transitions are (a) **set outcome → clear on next `_loadGroups`** and (b) **inline Retry re-entry**. (a): the clear-then-set ordering at `:327`/`:333` is the load-bearing invariant — locked by TC-01/TC-04 (fresh outcome survives the in-accept `_loadGroups`) and TC-09 (a later refresh clears it). (b): inline Retry must NOT bypass the `_processingInviteIds` guard — locked by TC-02's double-tap assertion (asserts accept invoked once per tap, guard re-checked). Also re-verify INV-6: `success` row-removal/navigate and the stuck Joining/Retry/Leave badges are unaffected by the outcome map → TC-08 + `:786/:807` sentinels.

## Step-By-Step Implementation Plan
1. Snapshot `git status --short`; add/rewrite the catalog tests; run the focused commands and confirm each fails for its documented reason.
2. `pending_group_invite_card.dart`: add an **outcome param** for the KEPT-invite states only — `enum PendingInviteRowState { idle, processing, waitingForKey, retryable }` (terminal reasons do NOT live on this card; see step 3 ghost row) + optional `onRetry`. Render: preserve the in-button spinner for `processing` (`:152-161`); `waitingForKey` → trailing spinner + `group_invite_waiting_for_key`; `retryable` → inline Retry keyed `pending-group-invite-retry-<groupId>` calling `onRetry`. Give all new params **defaults** (`idle`/null) so `orbit_screen.dart` does not compile-break. Keep `ValueKey('pending-group-invite-<groupId>')` + accept/decline keys intact.
3. `group_list_screen.dart` `:200-242`: (a) pass the KEPT-invite outcome + `onRetryPendingInvite` into the live `PendingGroupInviteCard` (`:212-221`); (b) add a small terminal-reasons section that iterates outcome-map entries whose id is NOT in `pendingInvites`, rendering a compact reason row keyed `pending-group-invite-outcome-<id>` (reason text + cached group-name snapshot). Thread a new `inviteRowOutcomes` map param from the wired build.
4. `orbit_screen.dart` `:848`: relies on the param defaults from step 2 (no outcome wired) → Orbit keeps current behavior. Lock the default render in TC-07.
5. `group_list_wired.dart`: add `Map<String, _InviteRowOutcome> _inviteRowOutcomes` (keyed by invite id; value = state enum + reason text + group-name snapshot) next to `_processingInviteIds` (`:108`); **clear it in `_loadGroups`** (`:136`, inside the `setState` at `:169-177`) so a refresh/resume resets stale row state (the clear runs at `:327` BEFORE the switch sets the fresh outcome — correct). Add `_onRetryPendingInvite(invite) => _onAcceptPendingInvite(invite)` (no guard bypass). Pass the map + retry callback through `build` (`:622-641`).
6. `group_list_wired.dart` rewrite the switch (`:333-372`), wrapping each outcome assignment in `setState`: `success` → navigate **and KEEP `_showSnackBar(group_invite_joined)` (`:335-340`, DECISION-2)**; `bridgeError` → if `group != null` set `joinedRecovery` + navigate + `_showSnackBar(group_invite_joined_recovery(name))` (navigate-time, card is gone), else set `retryable` (live card); `repairPending` → set `waitingForKey` (live card); the 8 terminal arms → set a ghost-row outcome (state + reason + name snapshot, reusing existing copy: `group_invite_no_longer_available`, `group_invite_expired`, `group_invite_expired_ask_resend`, `group_invite_revoked`, `group_invite_already_used`, `group_invite_wrong_identity`, `group_invite_invalid`, `group_invite_duplicate_group`) and **delete the terminal `_showSnackBar` calls** (`:343-367`). **Do NOT delete the success snackbar `:335`** (DECISION-2). **Stop-if:** the use-case can't distinguish shape-a vs shape-b → it already does via `group` (`:562-568`); do not add a new result value. **Stop-if:** a terminal reason needs the deleted invite's name → snapshot it into the outcome value before `_loadGroups` drops the row.
7. `group_list_wired.dart` `_onRetryStuckRejoin` (`:568-587`): delete the `joiningMessage` snackbar (`:569,:572`); keep `forceGroupRejoinEligible` + `rejoinGroupTopics` + `_loadGroups`.
8. l10n: add `"group_invite_waiting_for_key": "Waiting for key"` (plain string, no @-metadata) to `app_en.arb`, `app_ar.arb`, `app_de.arb` **by KEY** (append where the other `group_invite_*` keys sit; en vs ar/de line offsets diverge). `group_invite_joined_recovery` already exists in all three — no add, only wire. Regenerate l10n (`flutter gen-l10n` / build).
9. `scripts/run_test_gates.sh`: add `test/features/groups/presentation/group_list_wired_test.dart` and `test/features/groups/presentation/widgets/pending_group_invite_card_test.dart` to the `GROUP_TESTS` array (`:116-132`).
10. Rerun direct → preservation → named gates; `flutter analyze`; `git diff --check`.

## Risks And Edge Cases
- **Stale row state across refreshes**: a row outcome set on accept must clear on the next `_loadGroups` (group joined → row removed; invite re-listed → reset) → pinned by clearing in `_loadGroups` (`:136`) and by TC-01/TC-08.
- **Guard bypass via inline Retry**: routing Retry around `_processingInviteIds` would double-arm rapid taps → pinned by TC-02's double-tap assertion.
- **Shape-a vs shape-b bridgeError mis-bucket**: keying off `group != null` is the only discriminator → TC-02 (null→Retry) vs TC-03 (non-null→navigate) lock both sides.
- **Terminal invite is DELETED before the row can be styled** (C1): all 8 terminal arms `deletePendingInvite` and `_loadGroups` (`:327`) removes the card BEFORE the switch runs → there is no live card to attach a reason to → terminal reasons must render from an outcome-driven **ghost row** keyed `pending-group-invite-outcome-<id>` (carrying a name snapshot), not the card → pinned by TC-01's ghost-row finder + the `pending-group-invite-<id> findsNothing` assertion.
- **Dropping the success snackbar breaks sentinels** (C2): `:1111:1143`/`:1210:1242` assert the `group_invite_joined` snackbar via `findsOneWidget` → DECISION-2 keeps the navigating-outcome snackbars; if dropped, those two tests MUST be rewritten → pinned by TC-08 (+ the DECISION-2 note on `:1111`/`:1210`).
- **TC-03 changes the navigation of the existing `:1155` sentinel** (C3): shape-b now navigates → re-verify `:1155` stays green (offstage list) or fold it into TC-03; do not leave `:1155` locking the old no-navigate behavior.
- **Existing repairPending tests** (`:1250`, `:1278`) assert the transient `group_invite_needs_key` snackbar copy — both touch repairPending; rewrite/align both to inline `group_invite_waiting_for_key` (TC-04). Do not leave one asserting the dropped snackbar. NOTE `:1278:1311` also asserts `find.byType(GroupConversationScreen) findsNothing` — **preserve this**: repairPending must NOT navigate.
- **Orbit callsite** (`orbit_screen.dart:848`) shares the widget AND wires its own accept — adding required params without defaults would compile-break Orbit; default to `idle`/no-retry there.
- **SnackBar finder scope**: `find.byType(SnackBar)` is global; assert reason text via `find.descendant(of: find.byKey(ValueKey('pending-group-invite-outcome-<id>')))` (terminal ghost row) or `find.descendant(of: card)` (kept-invite states) to avoid confusing inline reason with a toast (TC-01 discriminator).

## Device/Relay Proof Profile
host-only for closure. No OS-boundary / ML-KEM / relay / multi-device leg — the accept result is produced by an already-host-tested use-case; this plan only re-renders that result as local screen state. No migration, no flag flip.
Deferred device work → none.

## Acceptance Gates  (literal — copy/paste, with expected counts)
```bash
# RED (before production edits) — must FAIL for the documented reason
flutter test test/features/groups/presentation/group_list_wired_test.dart \
  --plain-name 'terminal accept outcome shows an inline reason'
flutter test test/features/groups/presentation/group_list_wired_test.dart \
  --plain-name 'repair-pending accept shows an inline'
flutter test test/features/groups/presentation/widgets/pending_group_invite_card_test.dart \
  --plain-name 'renders inline reason'

# Direct GREEN (after fix)
flutter test test/features/groups/presentation/group_list_wired_test.dart
flutter test test/features/groups/presentation/widgets/pending_group_invite_card_test.dart

# Preservation + named gate (after adding both tests to GROUP_TESTS)
./scripts/run_test_gates.sh groups        # expect prior green count + the new wired/card cases; the 2 PRE-EXISTING failing wired tests (GMAR-004 reopen-hydration, incoming-group-image-refresh) are NOT mine
./scripts/run_test_gates.sh feed          # surface/LetterCard variants stay green

# Hygiene
flutter analyze            # 0 new issues
git diff --check
```

## Known-Failure Interpretation
- Expected RED: TC-01..07 before the fix (snackbar-instead-of-inline / missing params / dead key).
- Pre-existing dirty (NOT mine): the Groups suite has **2 pre-existing failing wired tests** — GMAR-004 reopen-hydration and incoming-group-image-refresh (media reopen/hydration, unrelated). Record before execution; do not "fix" by reverting. (`group_list_wired_test.dart` itself is expected green on HEAD aside from my RED edits — snapshot it.)
- Environment blocker (NOT product): none (host-only).
- Scope drift (BLOCKING): any failure outside the listed files/tests, or any touch to the decline path / use-case enum / sender-side delivery status.

## Done Criteria
- [ ] RED added/rewritten first, failed for the expected reason.
- [ ] Each fix mutation-verified (re-red revert named in the matrix).
- [ ] Direct GREEN + groups/feed preservation gates pass; the 2 pre-existing wired fails unchanged.
- [ ] No migration introduced (DB stays v92; all state is in-State presentation).
- [ ] `group_list_wired_test.dart` AND `pending_group_invite_card_test.dart` added to `GROUP_TESTS` and confirmed running in `./scripts/run_test_gates.sh groups`.
- [x] DECISION-1 (terminal rendering = **ghost row**) and DECISION-2 (**keep** the 2 navigating-outcome snackbars) LOCKED at review; plan/tests aligned (see Locked Design Decisions).
- [ ] 1 new l10n key `group_invite_waiting_for_key` present in en/ar/de; `group_invite_joined_recovery` now referenced (no longer dead); l10n regenerated.
- [ ] The 10 NON-navigating accept snackbars + the stuck-Retry "Joining…" snackbar removed (no remaining consumers); the 2 navigating snackbars (`success`, `joinedRecovery`) intentionally kept per DECISION-2; `flutter analyze` 0-new; `git diff --check` clean.

## Scope Guard (hard "Do not")
- Do not add a DB migration or bump the identity DB version (no schema change — DB v92 stays).
- Do not edit `accept_pending_group_invite_use_case.dart` result values or the recovery-retry loop timing.
- Do not touch the decline path (`_onDeclinePendingInvite`) or the sender-side `group_invite_status_presentation.dart` (different enum).
- Do not bypass `_processingInviteIds` from the inline Retry (INV-2).
- Do not keep any **non-navigating** accept snackbar "just in case" — inline row state (live card or ghost row) is the replacement (INV-5). The 2 navigating outcomes (success, joinedRecovery) keep exactly one navigate-time snackbar (DECISION-2) — do not add more.
- Do not attach a terminal reason to the live `PendingGroupInviteCard` (it is deleted before the switch runs — C1); terminal reasons render only on the ghost row.

## Accepted Differences / Intentionally Out Of Scope
- The **decline** outcomes stay on snackbars — accepted; owner = a future "decline outcomes inline" session (the accept switch is the reviewed surface).
- Terminal inline reason reuses the **existing** terminal copy strings (no new per-outcome keys) — accepted (avoids 8×3 new l10n entries); only `group_invite_waiting_for_key` is genuinely new and `group_invite_joined_recovery` is resurrected.
- Orbit's `PendingGroupInviteCard` keeps default `idle`/no-retry behavior unless its own accept flow wires outcomes — accepted (this plan scopes the outcome surface to the group list / stuck-rejoin review).

## Dependency Impact
- None outbound. The harness-registration fix (adding `group_list_wired_test.dart` to `GROUP_TESTS`) also retroactively gates ALL pre-existing accept/decline/stuck-rejoin behavior under the curated Group gate — a standalone hardening win (today that file only runs under host-all).

## Reviewer Findings
Source-verified against HEAD (uncommitted `new-feed`) 2026-06-23. **Verdict: structurally sound intent; THREE load-bearing premises were wrong and are now corrected in-doc; the two product decisions are LOCKED (DECISION-1 = ghost row, DECISION-2 = keep navigating snackbars) → execution-ready.**

Accurate as written (re-verified): the 11-value enum (`use-case:25-40`); the dead `group_invite_joined_recovery` (zero `lib/` refs); `group_invite_waiting_for_key` absent; `group_list_wired_test.dart` NOT in `GROUP_TESTS`; all `group_list_wired.dart`/`pending_group_invite_card.dart`/`group_list_screen.dart`/`orbit_screen.dart` line anchors; the keep-pending-`bridgeError` and `repairPending` invite-KEPT behavior (TC-02/TC-04 inline-on-card are sound); the harness-registration gap (real win).

Blockers found + resolved in-doc:
- **B1 (was fatal to TC-01):** all 8 terminal outcomes `deletePendingInvite` in the use-case, and `_loadGroups()` runs at `:327` BEFORE the switch → the `pending-group-invite-<id>` card is gone, so an inline reason cannot render on it and TC-01's `find.descendant(of: card)` finds nothing. → Resolved: terminal reasons render on an outcome-driven **ghost row** (`pending-group-invite-outcome-<id>`) with a name snapshot; TC-01 finder fixed; INV-1 reworded. **DECISION-1 = ghost row (LOCKED).**
- **B2 (was a self-contradiction):** dropping `success:335` breaks sentinels `:1111:1143`/`:1210:1242` (they assert the `group_invite_joined` snackbar via `findsOneWidget`), yet the plan also lists them as untouched sentinels and INV-5 forbids ALL 11 snackbars. → Resolved: navigating outcomes (`success`, `joinedRecovery`) keep one navigate-time snackbar; INV-5/TC-05 rescoped to the 10 non-navigating outcomes. **DECISION-2 = keep navigating snackbars (LOCKED).**
- **B3 (untested behavior change on a sentinel):** TC-03 makes shape-b `(bridgeError, group)` navigate, but `:1155` already exercises shape-b and was listed as an unchanged "keep-green" sentinel; the plan also never said WHERE the recovery copy renders post-navigation. → Resolved: TC-03 asserts navigate + navigate-time `group_invite_joined_recovery` snackbar + card gone, and reconciles/strengthens `:1155`.

Missing-coverage gaps added: the **Blind-Spot Sweep** section (was absent) + **TC-09** (derived `_inviteRowOutcomes` does not survive resume; repairPending reverts to idle) — locking the lifecycle blind spot. Sibling-surface (decline + Orbit) and destructive-side-effect classes recorded (N/A justified).

Accuracy fixes: arb/gate line numbers had drifted (`group_invite_joined_recovery` en:1312/ar:1260/de:1260; `group_invite_needs_key`:1309; `GROUP_TESTS`:131-133) — corrected + a "re-confirm by key/name, not line" note added (concurrent uncommitted 149/155 work in tree).

## Arbiter Decision
Structural blockers: **B1/B2/B3 — all resolved in-doc** (ghost-row terminal rendering; keep navigating-outcome snackbars; navigate + reconcile shape-b). | Locked decisions: DECISION-1 = terminal ghost row, DECISION-2 = keep the 2 navigating-outcome snackbars (both LOCKED at review; see Locked Design Decisions). | Accepted differences: decline path stays on snackbars (named owner: future "decline outcomes inline" session); Orbit accept keeps idle behavior by param default. | **Verdict: execution-ready.** Matrix has zero empty cells across tier/mutation/gate/registration; every INV has a test; host-only closure (no migration, no device-proof).

## Final Execution Verdict
Verdict: **ACCEPTED** (host-only closure; no migration, no device-proof — as specified).
Files changed (plan-150 scope): `pending_group_invite_card.dart` (enum `PendingInviteRowState`, public `PendingInviteRowOutcome`, defaulted `rowState`/`onRetry`, waitingForKey/retryable render), `group_list_screen.dart` (`inviteRowOutcomes` map + `onRetryPendingInvite` + ghost-row section keyed `pending-group-invite-outcome-<id>`), `group_list_wired.dart` (`_inviteRowOutcomes` cleared in `_loadGroups`, switch rewrite, `_onRetryPendingInvite` through the guard, dropped stuck-rejoin `group_joining_in_progress` snackbar, recovery snackbar default 4s), `app_en/ar/de.arb` + generated `app_localizations*` (`group_invite_waiting_for_key` added; `group_invite_joined_recovery` resurrected), `scripts/run_test_gates.sh` (both test files → `GROUP_TESTS`); NEW `pending_group_invite_card_test.dart`; rewritten/added TCs in `group_list_wired_test.dart`. `orbit_screen.dart` unchanged (param defaults).
Tests run (+counts): direct `group_list_wired_test.dart` **41/0** (37 plan-150 accept TCs + 4 concurrent-153 decline tests passing), `pending_group_invite_card_test.dart` **5/0** (= **+46 combined, re-confirmed at close**); `flutter analyze` **0-new** (only the pre-existing `withOpacity` info at `pending_group_invite_card.dart:92`); `git diff --check` clean. `groups` gate: WF-2 captured a clean **729/2** (only the 2 documented pre-existing `group_conversation_wired_test.dart` fails) BEFORE concurrent breakage; a later re-run showed **617/6** — but ALL 6 are concurrent-session noise, NOT plan 150: 3 are `micPermissionGateway` COMPILE failures (a voice-permission param from another in-flight session, knocking out whole files → the 729→617 pass-count drop), KE-014/KE-022 (key-rotation crypto) + GFR-003 (media controls), and 3 flaky orbit-invite accept tests (A2/EK011/spinner) in the concurrently-modified `orbit_wired.dart`/`orbit_wired_test.dart` (orbit-invite flakes are a known pre-existing class; A2's expected `group_invite_expired_ask_resend` is untouched by plan 150). `feed` gate **214/0**.
Mutation: 11 documented reverts each re-RED after fixes — incl. M3 (guard-bypass) now caught by TC-02's rapid double-tap equality, and D2 (15s snackbar test-coupling) removed (default 4s + show-time assertion).
Blocking: none.
QA verdict: 4-lens adversarial review = ship-with-nits; the only major/false-green findings (M3 guard-lock gap, D2 snackbar hack) were both fixed and re-verified. Accepted nits: ghost-row durability now carries a fragility comment; decorative info-icon a11y (ExcludeSemantics) left as-is.
Non-blocking follow-ups (owner): (1) **Concurrency note** — a parallel **plan-153** (destructive-admin-undo + decline-undo) session co-edited the shared `group_list_wired.dart` / `group_list_wired_test.dart` during execution; the working tree is co-mingled. When committing plan 150 in isolation, stage only the accept-path hunks + the two `GROUP_TESTS` lines, and leave the `_onDeclinePendingInvite`/`_declineCommitTimers`/`kDeclineUndoWindow` decline code and the two `ONE_TO_ONE_TESTS` rows to the 153/149 owners. (2) The 4 UI-untestable terminals (notFound/duplicateGroup/expired/invalidPayload) are switch-read-verified only — owner: a future screen-level ghost-row mapping test if desired.
