The relay and operator now accept the native Android admission result without conflating it with WorkManager completion, mailbox custody or media success.

The canonical schema adds stage `admission`, the closed dispositions `admitted`, `terminal`, `permanent_reject`, `empty_or_already_acked`, `deferred` and `unknown`, and the boolean flags `databaseClosed`, `leaseReleased` and `requiredPersistenceComplete`. The relay embedded JSON and operator schema match canonical SHA256 `6ea59e2bf718345f110a3fffc03b8ecbc18bcea7bd3b7e48d6d4ced559f247db`. No call-authority, WorkManager retry, node/bridge API or mobile-binding behavior changed in this relay batch.

Exact regression tests rejected the new shape before synchronization. After synchronization, all six dispositions are accepted and raw strings in disposition/custody fields are rejected. Focused relay schema/privacy tests passed13.273s. The operator suite passed9/9, including a native-only deferred admission with completed database/lease cleanup and incomplete required persistence. That case exposes its failed admission stage and reason while retaining zero mailbox admission and zero media success. A missing admission stage is placed before presentation for an observed Android callee push; it is not required for an iOS-only path.

The full relay suite passed66.274s; the stamped Linux candidate SHA256 is `d19ec7efeccf0a3624ec07b8312c737895fc2d5b79763328d8df20a89c5f73b3`. Its event build/source digest is `7c70c5616c335ee1a3435730a153946466b73b636a138a1fef3ae78732900865`; the matching operator SHA256 is `e9fb9371ec2d8c0dabc25de8484b9ecf0d5c026aa4e3895d287844a46cd636cc`. Complete provenance is saved in `relay-admission-schema-build.json`. There are22 diagnostic tests across the two relay test files. Scoped diff and affected checks passed, and source is frozen after the build. Deploy that server/operator before enabling the new native producer, because older strict schemas reject unknown diagnostic fields. A fresh configure after restart is also required for effective push-trace capability. All earlier quota, consent, privacy and binding behavior is preserved.

These are schema and projection proofs. Actual new native admission events on a physical device require their own subsequent live evidence; this document does not claim that worker-result logging proves a call connected.

Deployment was completed by the coordinating agent, who confirmed the exact relay/operator hashes above and the16:19:09 UTC backup reference. Services were active, storage-ready was1 and post-restart diagnostic counters initially0. The matching mobile packages and final physical admission probe remain pending. This subagent made no production deployment or device changes.
