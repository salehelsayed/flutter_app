A normal joined call exceeded the relay's original single-endpoint event budget. The first lab audio call produced132 Android records totaling56,852 JSON bytes and117 iOS records totaling50,772 bytes, before relay phase records. Both device spools fit their own64KiB limits, while their joined relay trace could not. The relay retained145 records but its historical counter reached742 because each retried overflow incremented the same counter. Those742 refusals do not establish742 unique lost events.

The repair gives a joined trace768 ordinary events and192KiB event payload: two bounded endpoint spools plus an equal relay contribution. Each authenticated endpoint's first verified-media record and terminal summaries also have reserved bounded slots. The serialized-record ceiling is320KiB, including wrappers and private ledger metadata. Global64MiB, per-owner5MiB,14-day server retention and all local-device limits remain unchanged.

Permanent ordinary trace-cap refusal now returns a rejected event ID with `quota_exceeded`, so the client can advance its queue. Disk/global/owner quota failures remain retryable and unacknowledged. A private256-ID ledger counts distinct permanent refusals and survives restart; saturation reports a lower bound. Ledger and reserved records follow consent erasure and expiry. Historical counters migrate to `legacyDropAttempts`, which the operator reports separately with unknown unique loss. No ledger IDs or authority maps are exported.

An additional restart regression initializes omitted empty summary maps before the first post-restart media/terminal write. This matters for already-retained traces that had not yet received a summary.

Focused causal tests passed47.992s; the diagnostics race batch passed72.826s before the additional empty-map restart guard. The final full relay suite passed84.471s; the final focused restart/capacity race repeat passed57.993s. There are21 focused diagnostic tests across the two relay test files. Operator tests passed8/8, covering the preflight-only shape, larger bounded files, modern versus legacy loss counts, endpoint completeness and private-ledger exclusion. Scoped diff checks and affected checks passed. Native bindings did not change.

The exact candidate and operator hashes, build input digest and limits are recorded in `relay-quota-repair-build.json`. The Linux candidate SHA256 is `9a771c4bdbc8adf02f9864d7e5d619b8ed368fd693901b42bfb6fb9b70a659eb`; the matching operator SHA256 is `bb980a57015e559b369d0de5b0cd4e41568835ff0ddd6c8e8c14c53cb7e6d1fe`. Source is frozen after this build. The coordinating agent deployed them at15:57:15 UTC. Subsequent read-only checks confirmed ready storage,0 new quota/sink/rejection errors and partial recovery of the old successful call's callee media/terminal records. Both-endpoint recovery and fresh final-package call completeness remain separate live assertions. Earlier snapshots are preserved to show the original failure rather than replaced with repaired results.
