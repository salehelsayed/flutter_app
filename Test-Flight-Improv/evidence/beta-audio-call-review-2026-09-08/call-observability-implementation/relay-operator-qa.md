Relay operator QA — 2026-09-08

Status: runtime ingestion and automatic offline-preflight upload are verified. The first successful lab call exposed and led to repair of the joined relay trace budget. After deployment, its callee media/terminal evidence recovered; caller media/terminal evidence remains absent in the latest saved lookup. Fresh final-package call completeness is still pending.

Latest deployment reference: the coordinating agent confirmed the admission-capable relay SHA256 `d19ec7efeccf0a3624ec07b8312c737895fc2d5b79763328d8df20a89c5f73b3` and operator SHA256 `e9fb9371ec2d8c0dabc25de8484b9ecf0d5c026aa4e3895d287844a46cd636cc`, with the16:19:09 UTC backup reference, active services and storage-ready1. This preserves the earlier quota repair and adds only the bounded admission diagnostic vocabulary/operator projection described in `relay-admission-schema-validation.md`. Physical admission evidence from the matching mobile build is pending; no additional poll was performed for this deployment note.

At 15:06:41 UTC, the protected state contained zero consent entries and zero authority entries. The diagnostic storage-ready gauge was 1. Accepted, rejected, sink-unavailable, queue-full and quota-exceeded event counters were all 0. The initial operator overview reported zero retained traces, zero invalid/unreadable records and no alerts. Only these aggregate values were exported from the private state. Effective push-parser capability is process-memory state; it was necessarily absent with zero consent entries, but was not otherwise inspected.

The application investigation identified a bridge request-shape error before native dispatch: the diagnostic producer used top-level action/op where the client required cmd/payload. This explains the missing consent registration; it is not a relay storage failure. Live polling paused while corrected packages were prepared.

After the corrected Android package launched at approximately 15:17:04 UTC, consent persisted at 15:17:23.549 UTC. At 15:17:40 UTC the server held one enabled consent with desired push tracing enabled; the accepted-event counter was 14, all rejection/storage/queue/quota counters remained zero, and storage was ready. The 15:18:57 operator projection confirmed 14 retained runtime records and zero dropped or invalid records. It reported four authority phase failures, one cleanup phase failure, and two registration withdrawals with origin `native_lifecycle_failed`. These are uploaded runtime/phase reports, not classified failed calls or evidence of relay rejection. No trace existed at the 15:20:32 snapshot. Saved projections contained none of the checked private field names; this is an export-shape check, not proof of complete call coverage.

The offline preflight trace subsequently arrived automatically; its two event IDs match the offline, restored-before-upload and acknowledged device archives. Details are in `offline-preflight-upload-proof.md` and its JSON projection. A terminal-only preflight classification defect was fixed and verified in the production operator.

The Android-to-iOS call identified by trace `9e891762-4b44-4e1a-99a1-9419d5ff748d` has device-controller evidence of bidirectional media and UI termination. At 15:36:39, however, the relay held145 records without either endpoint's final media/terminal records, and its old quota counter was742. This counter counted repeated refusal attempts, not unique missing events. `relay-quota-repair-validation.md` documents the reproduced cause, bounded repair, tests and candidate provenance.

After the quota repair deployment at 15:57:15, the 16:02:25 sanitized lookup held220 records for that trace, including callee Flutter bidirectional-RTP progress over `turn_udp` and `completed_after_media`. It still lacked caller media and terminal records. Historical refusal attempts had reached1062 before migration; the operator now exposes them separately with unknown unique loss. Modern distinct discards were0 and no records were invalid/unreadable. The separate historical presentation trace `442d53a5-343f-4905-a8a1-120d7b0598db` is a known provisional-identity artifact, not another call attempt.

The fresh trace `f47d37a1-eb4a-45f0-8158-cbe2ae6ac403` had79 records at 16:02:25, a caller `canceled` terminal, and no reported media or callee terminal. Across all retained diagnostic partitions there were898 records, including runtime records. Trace counts are not automatically call-attempt counts. At 16:03:48, post-deployment metrics showed363 accepted events,0 rejected,0 quota-exceeded,0 sink-unavailable and0 queue-full, with storage ready. These counters support healthy ingestion; they cannot prove that every device event has arrived.

The bounded live check will use `/usr/local/bin/call_diagnostics.py` on the relay, exporting only its validated projection. Canonical random trace filenames may be enumerated to select lookups. The temporary capture helper is `/tmp/beta-call-review-20260908/relay_diagnostic_qa_capture.py`; snapshots are written beside this report. Private author maps, peer identities, protocol handles, tokens, credentials, IP addresses and raw device logs are excluded.

Interpretation boundaries already enforced by the reviewed operator and focused tests:

- Relay mailbox commit, actual provider invocation/result and TURN mint/write are separate server stages. Signaling acknowledgement, push submission and credential issuance do not prove audio.
- Client media remains client-reported evidence. Caller and callee media/terminal presence are counted separately; one endpoint does not satisfy both-endpoint completeness.
- Related authority operation IDs are joined only through bounded references. Missing ancestry remains visible rather than inferred.
- Direct transport can succeed without successful mailbox custody. Such endpoints can retain separate or provisional traces because shared relay binding requires successful Store.
- Disabled consent, old clients, storage failure, expiry and quotas can leave partial evidence. Delayed upload receipt order is not a globally synchronized device event order.
- Ordinary declines, cancellations, busy and no-answer results are excluded from the technical-failure denominator. Rate alerts require at least five observations.

Verified live assertions include schema acceptance, native/Flutter/relay provenance, joined caller/callee signaling records, bounded authority-operation references, automatic offline upload and correct separation of reported media from signaling. Pending assertions are both-endpoint media/terminal completeness on a fresh final-package call and recovery of the historical caller tail. Unresolved operation references remain visible. The server build remains frozen; production changes were performed by the coordinating agent, not this QA helper.
