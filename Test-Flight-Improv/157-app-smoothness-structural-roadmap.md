# 157 - App Smoothness STRUCTURAL Findings — Master Roadmap & Index  (Modification — release-build performance)

Status: roadmap (umbrella) — the focused TDD plans **158–164** chain off this doc.
Spec: derived from `Test-Flight-Improv/app-smoothness-performance-audit.md` (Findings by Impact: **High** + **Medium**, the "Structural" Phase-2/3 roadmap) + `app-smoothness-performance-audit.findings.json`. The **Quick Wins (Phase 1)** already shipped as **156** (`156-app-smoothness-quick-wins-tdd-plan.md`, IMPLEMENTED host-green on branch `performance`). This roadmap owns **everything in the audit that is NOT a quick win**.

This is an INDEX + sequencing + shared-infrastructure doc, **not** an implementation plan. Each epic gets its own source-verified, tier-complete, mutation-aware `/tdd-plan` (158–164). Implement them in the dependency order below.

---

## Relationship to 156 (do NOT re-plan these)

156 shipped 13 quick wins. The following finding ids are **DONE on HEAD** and are explicitly OUT of every 158–164 plan: `critic-1` (per-bubble blur removed), `navigation-hangs-1`/`lists-scrolling-2`/`rebuild-storms-1`/`animations-repaint-1` (ambient `RepaintBoundary` — the audit bundles all four ids into one finding, audit line 73), `animations-repaint-4` (OS reduce-motion gate on all ambient surfaces), `images-media-1`/`images-media-2`/`main-isolate-blocking-4`/`lists-scrolling-5` (avatar/profile `cacheWidth` — `main-isolate-blocking-4` + `lists-scrolling-5` folded into `images-media-1`, audit Appendix; all avatar decode surfaces dpr-gated, `user_avatar.dart:131`/`group_avatar.dart:115`/`profile_avatar_widget.dart:84`), `db-persistence-2` (migration **093** `idx_messages_contact_ts`), `db-persistence-5`-**INDEX** (migration **094** `idx_group_messages_group_ts`, 3-col), `main-isolate-blocking-3` (ring memo + `==`), `cold-start-2` (scrub sentinel), `reactive-streams-2` (1:1 media-resolve gate + already-shown dedup — **media/dedup half only**; the un-coalesced double-listener double-`setState` residual is still live and is collapsed by **159**'s per-frame coalescer, see below), `lists-scrolling-1`/`navigation-hangs-4` (quote map), `lists-scrolling-4` (entrance-anim isNew, **1:1 only — the group conversation has zero entrance-animation infrastructure, so there is no group half to gate** `[re-verify 2026-06-24]`), `lists-scrolling-3` (DateFormat hoist), `navigation-hangs-5`/`animations-repaint-3`/`rebuild-storms-4` (orbit prototype `kDebugMode` — the same `kOrbit2PrototypeEnabled = kDebugMode` gate neutralizes `rebuild-storms-4`'s Orbit2 `onPanUpdate` per-pointer `setState` in release).

**DB floor is now v94.** Any migration in 158–164 takes **095+** and rebases its `onUpgrade` arm ABOVE the 156 (`<93`, `<94`) arms.

**156 follow-ups that remain genuine gaps (not separate audit findings):**
- **GROUP incoming-media resolve gate** `[re-verify 2026-06-24]` — 156 QW-9 (`reactive-streams-2`) shipped the **1:1** gate only (`conversation_wired.dart:1546` `shouldResolveMedia = !alreadyShown || hasUnresolvedMedia`). The group path is still unconditional: `group_conversation_wired.dart:1417` subscribes `groupMessageStream` → `_applyMessageUpdate` (`:3069`), which calls `_loadResolvedAttachmentsForMessage` (`:3075` → `getAttachmentsForMessage` `:2671`) for **every** event (text/status/reaction/sys-), with no media-presence / already-shown gate. (NB: the group stream is `groupMessageStream`, not the 1:1 `messageChanges` — the symptom and fix location are right; only the stream name was borrowed from 1:1 vocabulary.) Folded into **159** (which already co-edits the group screen) as a small additive obligation, OR a stand-alone micro-fix. Tracked here so it is not lost.
- **`images-media-3` GIF `cacheWidth`** — REFUTED by 156 (the `isGifImage ? null` guard protects animated-GIF first-frame survival). NOT re-planned.

---

## Grounding provenance

All confirmations below come from a **verify→refute** Workflow (11 read-only `graphify-explorer` agents, 2026-06-24): one per structural epic + a Phase-3 bundle + a test/harness inventory. Each agent grounded graphify-first, verified the symptom reproduces **on HEAD** with `file:line`, ran four adversarial kill paths (already-fixed-by-156? wrong-RC? build-skew? other-path?), and derived per-tier test obligations. Raw results: workflow `wf_85c86584-4e1`. **Every epic below survived its refute pass.**

**Second source re-verification (2026-06-24, workflow `wf_87524e2e-aa9`, 10 `graphify-explorer` agents).** A follow-up `/tdd-plan` review re-grounded every cited `file:line` anchor, every disposition, and the full 47-finding coverage against the live working tree. Result: the epics and their root causes all hold, but the pass corrected/added the items flagged inline below as **[re-verify 2026-06-24]** and added the **Complete audit-finding reconciliation (all 47 ids)** section. Headline corrections: (a) `reactive-streams-2` is only PARTIALLY shipped by 156 — the double-listener double-`setState` residual is owned by **159**; (b) the off-screen orbit subscription count is **8** (not 6) + a periodic Timer; (c) `cold-start-5` is **2** serial probes (not 3); (d) `_buildDisplayItems` lives in `conversation_screen.dart`, not `conversation_wired`; (e) six audit ids were unlisted aliases now reconciled; (f) the `reactive-streams-4` DROP rationale and the `reactive-streams-5` refutation were over-stated and are now scoped.

---

## Refuted / do-NOT-re-introduce ledger (from the 156 verifier, re-confirmed)

These were investigated and deliberately **not** planned — listed so no 158–164 plan silently re-adds them:
- **`reactive-streams-1`** ("Feed reloads whole conversation on every status flip") — REFUTED: status flips route to the in-memory `_applyIncomingContactMessageToFeed` merge path (`refreshUnreadCount:false`), not a reload. The genuine reload triggers (send-from-feed, delete/hide) are owned by **160**.
- **`db-persistence-4`** ("no WAL → reader/writer hang") — REFUTED: `sqflite` serializes on one native connection + a Dart FIFO mutex; WAL would not fix the claimed hang.
- **`critic-3`** ("first-open blur shader-compile jank") — REFUTED: Impeller default-on precompiles pipelines.
- **`lists-scrolling-6`** ("group list eager `ListView`") — REFUTED/off-hot-path: `GroupListWired`/`GroupListScreen` are not mounted in the live `new-feed` app.
- **`reactive-streams-5`** ("ambient loop not paused on background") — REFUTED **on the background/opaque-route axis only** `[re-verify 2026-06-24]`: the framework `TickerMode`-mutes the ambient loop when the app is backgrounded or an opaque route covers the surface (`ambient_background.dart` uses `SingleTickerProviderStateMixin`; `_shouldAnimate:146-151` has no background/route term and correctly relies on the framework for those axes). **NOT a blanket refutation** — the Feed/Orbit shell keeps *both* panes mounted in one `Stack` (`feed_wired.dart:2407-2426`) with the off-screen pane only `Transform.translate`d (no opaque route between them, no `TickerMode`), so the off-screen tab's 8s ambient loop keeps ticking. That orthogonal **active-tab** pause is still owned by **163** (`animations-repaint-2`). Do not read this entry as "no explicit pause is ever needed."

---

## Complete audit-finding reconciliation (all 47 ids)  `[added 2026-06-24]`

The audit (`app-smoothness-performance-audit.findings.json`) carries **47** finding ids; the audit MD consolidates several into single findings. To make "this roadmap owns everything in the audit that is NOT a quick win" *provable* (the prior draft silently dropped six aliases), every id is accounted for here. **★ = surfaced by the 2026-06-24 re-verification** (was unlisted/mis-scoped in the prior draft).

- **156-DONE (19):** `critic-1`, `cold-start-2`, `navigation-hangs-1`, `navigation-hangs-4`, `navigation-hangs-5`, `main-isolate-blocking-3`, `main-isolate-blocking-4`★, `rebuild-storms-1`★, `lists-scrolling-1`, `lists-scrolling-2`, `lists-scrolling-3`, `lists-scrolling-4`, `lists-scrolling-5`★, `images-media-1`, `images-media-2`, `animations-repaint-3`, `animations-repaint-4`, `db-persistence-2`, `db-persistence-5`-INDEX.
- **158:** `critic-2`.
- **159:** `main-isolate-blocking-1`, `rebuild-storms-2`, `rebuild-storms-3`, `navigation-hangs-3`★ (DateTime alias of `rebuild-storms-3`), `reactive-streams-2`-residual★ (double-listener double-`setState`).
- **160:** `db-persistence-1`, `db-persistence-3`.
- **161:** `db-persistence-5`-QUERY (roadmap-escalated from the audit's Phase-1 index-only placement).
- **162:** `db-persistence-7`.
- **163:** `navigation-hangs-2`, `reactive-streams-3`, `animations-repaint-2`.
- **164:** `cold-start-1`, `main-isolate-blocking-2`, `cold-start-3`, `cold-start-5`.
- **Spans multiple plans:** `animations-repaint-1` (156 RepaintBoundary half + 158 chat-idle + 163 off-screen); `reactive-streams-2` (156 media/dedup + 159 coalesce + 159 group-gate fold-in); `db-persistence-5` (156 index 094 + 161 query).
- **REFUTED — do not re-introduce (6):** `reactive-streams-1` (in-memory merge; residual → 160), `db-persistence-4` (single-connection + FIFO mutex), `critic-3` (Impeller default-on), `lists-scrolling-6` (GroupList not mounted), `reactive-streams-5` (background/opaque-route axis **only** — off-screen-tab pause → 163), `images-media-3` (animated-GIF first-frame guard).
- **DROP (3):** `cold-start-4` (alloc sub-ms; the real cost is the awaited I/O that 164 defers), `animations-repaint-5` (conflicts with green sentinel TC-25), `reactive-streams-4` (subscribers rely on non-distinct re-emits).
- **DROP / neutralized (1):** `rebuild-storms-4`★ (Orbit2 `onPanUpdate` per-pointer `setState`, dev-lab `kDebugMode`-gated, absent in release).
- **DEFER (1):** `db-persistence-6` (contacts/groups zero-index full-scan; social-graph-bounded — note: no hard `LIMIT` backs the ~100-row assumption).

Tally (each id counted once, at its terminal home — `db-persistence-5` counted under 156-DONE for its index half, `animations-repaint-1` + `reactive-streams-2` counted under 158–164): **156-DONE 19 + owned-by-158–164 17 + REFUTED 6 + DROP/neutralized 4 + DEFER 1 = all 47 unique ids accounted for.**

---

## Confirmed-live structural epics → focused-plan mapping

| Plan | Title | Audit finding ids | Severity | Tier floor | Migration | Device-proof | Shared hot file(s) |
|---|---|---|---|---|---|---|---|
| **158** | Chat/group ambient **idle-glow suppression** | `critic-2` | High | host widget | none | optional perf | `ambient_background.dart` |
| **159** | Conversation/group **list memoize + per-frame coalesce + window cap + cached timestamp** | `main-isolate-blocking-1`, `rebuild-storms-2`, `rebuild-storms-3`, `navigation-hangs-3` (DateTime alias), `reactive-streams-2`-residual (double-listener coalesce) | High | host widget/unit | none | none | — (1:1 + group screens/wired) |
| **160** | 1:1 Feed **N+1 → thread-summary + lazy-on-focus + per-event refresh bound** | `db-persistence-1`, `db-persistence-3` | High/Med | host unit/widget | optional 095 (EQP-contingent) | perf-win only | `feed_wired.dart`, `load_feed_use_case.dart` |
| **161** | **Group feed batched thread-summary** preview | `db-persistence-5` (query half) | Med | host real-DB (sqflite_ffi) | none (094 shipped) | optional perf | `feed_wired.dart`, `load_feed_use_case.dart` |
| **162** | Feed/conversation **media-resolve sync swap + reload debounce** | `db-persistence-7` | Med | host unit/widget | none | none | `feed_wired.dart`, `load_feed_use_case.dart` |
| **163** | Feed/Orbit **shell rebuild minimization + off-screen ticker/subscription pause** | `navigation-hangs-2`, `reactive-streams-3`, `animations-repaint-2` | Med | host widget | none | optional perf | `feed_wired.dart`, `ambient_background.dart` |
| **164** | **Cold-start pre-runApp deferral** + iOS keychain mirror incremental + parallel launch probes | `cold-start-1`/`main-isolate-blocking-2`, `cold-start-3`, `cold-start-5` | Med | host wiring-lock | **device-proof (closure gate)** | yes | `main.dart` |

---

## Dependency order & shared-file collisions (READ before scheduling)

**`feed_wired.dart` is the single biggest collision risk** — it is touched by **160, 161, 162, 163** (and is 134/135-owned: pending-reply inbox, focus/commit INV-3, swipe-dismiss). Do **NOT** run those four in parallel against the same tree. Either:
- run them **sequentially** in the order `160 → 161 → 162 → 163`, rebasing each on the prior, **or**
- give each `isolation: 'worktree'` and reconcile `feed_wired.dart` by hand at merge.

**`ambient_background.dart`** is touched by **158** (adds a chat-surface term to `_shouldAnimate`) and **163** (adds an active-tab term + shell `TickerMode`). Land **158 before 163** so 163 extends a known `_shouldAnimate` shape.

**`main.dart`** is touched only by **164** — no collision with the others.

**Recommended build sequence** (leverage × risk, collision-aware):
1. **158** — standalone, host-only, RED scaffold already exists, zero reliability intersection → ship first, fast.
2. **159** — independent of the feed surface; highest single-symptom leverage ("interactions stutter inside chats"). Reliability-sensitive (land the 145/131 preservation lock BEFORE the coalesce edit).
3. **160 → 161 → 162 → 163** — the `feed_wired` cluster, sequential. 160 first (the N+1 restructure others build on), then 161 (group twin), 162 (media-resolve + debounce), 163 (shell rebuild/offscreen pause).
4. **164** — independent file (`main.dart`); OS-boundary, gated on a device-proof. Can run in parallel with the `feed_wired` cluster.

158, 159, and 164 can each run concurrently with the others (disjoint files); only the `feed_wired` cluster must serialize.

---

## Shared infrastructure (build once, reuse across plans)

1. **Per-frame stream-coalescing.** 159 (collapse a drain burst of M `messageChanges`/`incomingMessageStream` events into ONE rebuild) and 162 (debounce feed-reload triggers) both need a trailing per-frame flush (`SchedulerBinding.addPostFrameCallback` / 16ms microtask). If it factors cleanly, extract one small `FrameCoalescer` helper under `lib/core/util/`; otherwise keep per-surface but use the identical shape. **Hard constraint (159):** the coalescer must sit ONLY on the per-event message-apply `setState` — it must NOT touch 145's `_drainAndReloadOnce` banner `setState` (`conversation_wired.dart:1185/1242`) / `NOTIFICATION_TAP_TO_LIVE_MESSAGE_TIMING` milestone (`:1217`, already coalesced) or it regresses 145/131. **It must also dedup ACROSS the two distinct 1:1 subscriptions** (`_repoChangeSubscription` @1509 + `_incomingSubscription` @1481) — a single incoming message fires both (the `reactive-streams-2` residual), so a within-one-stream coalescer leaves the double rebuild intact.
2. **Windowed thread-summary query pattern.** 160 (1:1) reuses the EXISTING `getConversationThreadSummaries` → `dbLoadConversationThreadSummaries` (`messages_db_helpers.dart:243`, already used by orbit at `load_orbit_data_use_case.dart:191`). 161 (group): **extend, don't rebuild** — `dbLoadGroupThreadSummaries` (`group_messages_db_helpers.dart:306`) ALREADY batches via `WHERE group_id IN (…)` + unread aggregation but returns latest-only (`:344-353`); add a bounded recent-message window to it (or a sibling `dbLoadGroupThreadPreviews`) for the unread-window + exchange context. Use a consistent shape across both: `{unreadCount, hasSent, latest, totalMessageCount, bounded tail window K≈8}`. Both also need a new `totalMessageCount` on the thread feed-item model so the cap-surviving getters work (today they read `messages.length`/`indexOf`): not just `hasEarlierHistory` (`feed_item.dart:164`) but `hasEarlierInteractionHistory:199`, `recentInteractionMessages:186`, `previewMessages:157`, `exchangePreview:143`, `unreadMessages:152`, `hasReply:149`, `additionalCount:140`. The summary query already yields `message_count` (`ConversationThreadSummary.messageCount`) — thread it through.
3. **Existing pagination primitives (reuse, do NOT rebuild).** `loadConversationPage` (`load_conversation_use_case.dart:43`) / `getMessagesPage` (`message_repository_impl.dart:276`) / `dbLoadMessagesPage` (`messages_db_helpers.dart:94`, `DESC`+`limit`+`beforeTimestamp` cursor) already exist (commit `dc4731c6`, wired into the 1:1 conversation screen at `conversation_wired.dart:1112/1256/1297`). 160 (delete/hide paged snapshot) wires them into the feed path — **covering BOTH `_refreshContactFeedItem` (`feed_wired.dart:630`) and the all-contacts sibling `_refreshAllContactsSection` (`:702`)**. **Cap newest-first (`DESC`+limit)**, never oldest — a notif-drained message must stay visible as `latestMessage` (131/141/145).
4. **`resolveStoredPathSync` sync twin** (`media_file_manager.dart:56`, startup-seeded at `main.dart:401`) — already the production render-boundary resolver; 162 swaps the awaited loader loops to it.
5. **`PERF_TARGET` benchmark harness** (`integration_test/performance_harness.dart` dispatch `:33-58`; keys FEED/FEED_INIT/CONVERSATION/CONVERSATION_SUB/ORBIT/IDENTITY_PROGRESS — exactly 6, default arm `fail`s an unknown key, so a new key needs a new case arm + `register*Perf`). Each plan may add a key (e.g. `SHELL_SWITCH`, `FEED_ORBIT_OFFSCREEN`, `FEED_MEDIA`, `feed_mount_deep_history`) for an **optional, advisory** device/sim perf-budget confirmation. These are `@Tags(['device'])`, classified "ignored" by the reliability-sim discovery (`check_reliability_simulation_discovery.sh:142-154`), and are **NOT host gates** — host tests are the correctness floor. New perf entrypoints MUST be named `*_performance_test.dart` / `*_performance_harness.dart` **AND live directly under `integration_test/`** (the `classify_path` glob is `integration_test/`-prefix-anchored — a perf file in a subdir or under `test/` falls through), or get an explicit `classify_path` case, or they break sim discovery.
6. **Migration numbering.** Next-free is **095** (093/094 = 156). Only an OPTIONAL covering index in 160 (contingent on `EXPLAIN QUERY PLAN` showing a temp-b-tree) or `db-persistence-6` (deferred) would consume it.

---

## Per-plan charters (one paragraph each — full plan in the linked file)

- **158 — chat/group ambient idle-glow suppression (`critic-2`).** `_shouldAnimate` (`ambient_background.dart:146-151`) has no chat-surface term, so the 8s glow loop `repeat()`s while a chat/group screen is foregrounded for default (motion-on) users, making the always-mounted header/composer/panel `BackdropFilter`s un-cacheable → idle battery/heat. Add a distinct `isChatSurface`/`pauseGlowAtRest` flag (a 4th `_shouldAnimate` term, OR-of-disable, additive to the 156 reduce-motion clauses), pass it from `conversation_screen.dart:293` + `group_conversation_screen.dart:194`, optionally lower chrome sigmas. Host-tier; RED scaffold = `ambient_background_test.dart` TC-04/05 + `ambientControllers` helper. **No reliability intersection. Best first plan.**

- **159 — conversation/group list memoize + coalesce + cap + cached timestamp (`main-isolate-blocking-1`/`rebuild-storms-2,3`/`navigation-hangs-3`/`reactive-streams-2`-residual).** `[re-verify 2026-06-24]` `_buildDisplayItems` (**`conversation_screen.dart:704`**, called from `_buildMessageList:447` — the *presentational* widget, **NOT** `conversation_wired`) recomputes a full O(N) two-pass grouping on every `setState`; the per-message message-apply `setState`s are in the wired layer (`conversation_wired.dart:1555` durable-repo + `:1590` live-router; group `group_conversation_wired.dart:3083`) → a relay drain is O(M×N). **`reactive-streams-2` residual:** a *single* incoming message fires BOTH wired listeners (`_repoChangeSubscription` @1509 + `_incomingSubscription` @1481) → two full-list rebuilds; 156 gated only the media-resolve, NOT these two `setState`s. Four sub-changes, RED-first in order: (1) cache parsed `DateTime` on the model — `conversation_message.dart:21` is `String timestamp` today; there are **three** uncached parse sites (`conversation_screen.dart:737` per-message `tryParse`, `:957` per-message `_formatDateLabel` parse, `:463` **per-visible-row** `formatTime` parse), all eliminated by one model field; (2) memoize the display-items list (in `conversation_screen.dart`'s State) keyed on length+first-id+last-id+status/transport flags; (3) per-frame coalesce the message-apply `setState` — **must dedup ACROSS both subscriptions** (not within one) and must NOT touch 145's distinct `_drainAndReloadOnce` banner `setState` (`conversation_wired.dart:1185/1242`) / `NOTIFICATION_TAP_TO_LIVE_MESSAGE_TIMING` milestone (`:1217`); land the 145/131 preservation lock first; (4) in-memory window cap (~300) with older-page eviction. Covers 1:1 + group (group `_buildDisplayItems` mirror at `group_conversation_screen.dart:782`). Also folds in the **group media-resolve gate** (gate `group_conversation_wired.dart:3075`). Host-tier; no migration; no device-proof. Optional `CONVERSATION_SUB` burst perf lane.

- **160 — 1:1 feed N+1 → summary + lazy-on-focus + per-event refresh bound (`db-persistence-1` + `db-persistence-3`).** `[re-verify 2026-06-24]` `loadContactFeedItems` (`load_feed_use_case.dart:35`, fn def `:17`) loops `getMessagesForContact` per contact UNBOUNDED (`dbLoadMessagesForContact`, no `LIMIT`), SQLCipher-decrypting whole histories on the root isolate; `_loadReactionsForFeed` (`feed_wired.dart:1383`) then fans reactions over every message id. And every send-from-feed / delete-hide re-reads the full history via `_refreshContactFeedItem` (`feed_wired.dart:630`) — **plus the sibling `_refreshAllContactsSection` (`:702`), which re-runs the unbounded `loadContactFeedItems` for ALL contacts on a full refresh; both must be bounded.** Phase A: swap the preview loop to `getConversationThreadSummaries` (`message_repository_impl.dart:455` → `dbLoadConversationThreadSummaries`, `messages_db_helpers.dart:243`, already used by orbit) + bounded reaction fan-out. Phase B: lazy-load the full thread on FOCUS (preserving 134/135 commit/INV-3) + send-success in-memory merge + paged delete/hide snapshot via the existing `loadConversationPage`. **Summary-swap blast radius:** the in-memory `messages` list shrinks to a preview, so EVERY `CardThreadFeedItem` getter that infers state from the full list — `hasEarlierHistory` (`feed_item.dart:164`), `hasEarlierInteractionHistory:199`, `recentInteractionMessages:186`, `previewMessages:157`, `exchangePreview:143`, `unreadMessages:152`, `hasReply:149`, `additionalCount:140` — must be re-derived from a new `totalMessageCount` field (the summary query already returns `message_count` → `ConversationThreadSummary.messageCount`; the field is absent on the feed model today, repo-wide). Host unit/widget floor; optional `feed_mount_deep_history` PERF for the real decrypt-cost win. **Cap newest-first.**

- **161 — group feed batched thread-summary (`db-persistence-5` query half — roadmap-escalated from the audit's Phase-1 index-only placement).** `[re-verify 2026-06-24]` Index 094 is shipped; the per-group loop (`load_feed_use_case.dart:96-100`, `getMessagesPage(group.id, limit:200)`) remains — note the non-sargable `id NOT LIKE` + secondary `id DESC` sort live one level down in `dbLoadGroupMessagesPage` (`group_messages_db_helpers.dart:170-172`), **not** at `:96`, so the fix must touch/replace that DB query too. **Extend, don't rebuild:** `dbLoadGroupThreadSummaries` (`group_messages_db_helpers.dart:306`) ALREADY batches via `WHERE group_id IN (…)` + unread aggregation but returns latest-message-only (`:344-353`); add a bounded per-group recent-message window to it (or a sibling `dbLoadGroupThreadPreviews`) preserving `unreadIncomingCount`, `ConversationState`, preview window, sys-cutoff exclusion; the 094 3-col `(group_id, timestamp, id)` index is what makes the per-group window subquery performant (coupled). Lazy full-load on focus. Host real-DB tier (sqflite_ffi); reuse `group_messages_db_helpers_test.dart` (`dbLoadGroupThreadSummaries` group `:246`) + `load_feed_use_case_test.dart`. **Don't drop to limit-1** (breaks unread counts + preview).

- **162 — media-resolve sync swap + reload debounce (`db-persistence-7`).** `[re-verify 2026-06-24]` Loaders await `resolveStoredPath` (a `path_provider` `getApplicationDocumentsDirectory` channel hop, `media_file_manager.dart:225-256`) per attachment, serializing the loop. Phase A: swap the 3 awaited loops (`load_conversation_use_case.dart:99`, `load_feed_use_case.dart:52`, `group_feed_media_verification.dart:32`) to the existing `resolveStoredPathSync` (`media_file_manager.dart:56`, startup-seeded `main.dart:401`); keep the group `existsSync/lengthSync` integrity verify intact (`group_feed_media_verification.dart:64/66`, size-mismatch gate `:133-142`). **Two source-verified obligations:** (i) the group resolver `resolveGroupFeedMediaForDisplay` has **THREE** callers — `load_feed_use_case.dart:109`, `load_group_feed_snapshot_use_case.dart:34`, `feed_wired.dart:796` — so do the swap *inside* `group_feed_media_verification.dart` (all inherit) or cover all three; (ii) the sync resolver returns the relative path UNCHANGED when `_cachedDocumentsDir` is unseeded (`MEDIA_RESOLVE_SYNC_NO_CACHE`), so confirm these LOAD loops run **after** the `main.dart:401` seed, else paths mangle — calling `resolveStoredPathSync` directly inherits the 127/128 null-cache passthrough for free. Phase B: debounce the `feed_wired` stream-driven reload triggers. Host unit/widget; no migration.

- **163 — feed/orbit shell rebuild minimization + off-screen pause (`navigation-hangs-2` + `reactive-streams-3`/`animations-repaint-2`).** `[re-verify 2026-06-24]` Merged because both live in `feed_wired.dart`. `_onShellChanged` (`:2182`) `setState`s unconditionally (even background-only — `AppShellController` shares ONE `notifyListeners` for `switchTo` + `setBackgroundPreference`, `app_shell_controller.dart:22/30`), build() (`:2326`) re-allocates both panes (`FeedScreen` ~28 args `:2356`, `OrbitWired` ~40 args `:2205`), the `AnimatedBuilder` (`:2400`) supplies **no** `child:` and never references its `child` param (dead), 0 RepaintBoundaries; the off-screen orbit tab is only `Transform.translate`d (`:2410/:2419`, no `TickerMode`/`Offstage`) so 2 ambient loops + **8** orbit stream subscriptions (`orbit_wired.dart:235-242`) **+ a periodic `Timer` (`orbit_wired.dart:1381`)** stay live off-screen. Split `AppShellController` notify (tab vs background), **pass the RepaintBoundary-wrapped panes via `AnimatedBuilder.child`** (so the heavy subtree is excluded from per-tick rebuilds, not merely wrapped), `TickerMode(enabled: active||midSwipe)` the inactive side, gate the **8 subscriptions + the timer** on `activeTab` (dirty-flag + coalesced re-activation reload, **pause not cancel** — preserve 131-class live updates). Host widget; preserve 134-P6 swipe arena (`feed_wired.dart:1977-1984`, both panes co-rendered mid-swipe `:2407-2428`) + 156 reduce-motion gate.

- **164 — cold-start pre-runApp deferral + keychain mirror + parallel probes (`cold-start-1`/`main-isolate-blocking-2` + `cold-start-3` + `cold-start-5`).** `[re-verify 2026-06-24]` Normal (non-share) launch awaits `ensureFirebaseReady` (`main.dart:391`, `!isShareLaunch`) + `startLiveServices` (`:3096`) before `runApp` (`:3105`); iOS mirrors all group keys/mutes every launch with **no presence/idempotence guard** — `mirrorAllKeysToSecureStore` (`:1312`) + `mirrorAllMutedGroups` (`:1315`) write UNCONDITIONALLY (`_mirrorGroupKeyForPush:668` `store.write`; the only early-return `:663` is a format check, not a presence-diff) so it is a genuine serial **write** blast (the audit's "dominated by reads/skips" under-stated it). **`cold-start-5` is TWO independent serial probes, not three:** `captureInitialIntent` (`main.dart:350`, before Firebase) + `getApplicationDocumentsDirectory` (`:395`, which already runs AFTER Firebase); only the share-intent probe precedes Firebase, and only these two are safely overlapped (a 3rd top-level await, `wipeKeychainOnce:415`, is TEMP dev-only). Defer `startLiveServices` (share-launch already proves it via `deferredRuntimeStartup:3186`) + move Firebase off the critical path; make the keychain mirror incremental (presence-diff via `SecureKeyStore.containsKey`, NOT a one-shot global sentinel — preserve NSE group-preview decrypt completeness); overlap the share-probe + docs-dir futures (keep Firebase gated behind `!isShareLaunch`). **Load-bearing regressions (first-class RED):** (1) deferring Firebase past `MyApp.initState` makes `_setupPushListeners` (`main.dart:4431`, guard `:4432` `Firebase.apps.isEmpty`) early-return → silent loss of foreground push (`onMessage`) + push-tap routing (`onMessageOpenedApp`) → it MUST be re-armed after `_ensureRuntimeServicesReady`, **behind a new `_pushListenersArmed` latch** (the listeners have NO unregister today, so a warm-restart re-arm would double-register → duplicate `PUSH_FOREGROUND_MESSAGE_RECEIVED`); (2) `ensureFirebaseReady` also wires the iOS NSE recent-remote gate (`configureRecentRemoteNotificationGateForIos:384` + `persistAppGroupContainerPathForGate:385`) — the deferral must keep this reached before the first incoming push, or NSE dedupe / app-group seeding is skipped. **Device-proof is the closure gate:** cold local-notif tap, cold FCM tap, foreground push received, + `StartupTiming` first-frame-precedes-`firebase_ready`. Heavy reliability surface (131/133/141/145 notif + 134/135 feed).

---

## Phase-3 disposition (NOT given their own plans)

| Finding | Verdict | Rationale |
|---|---|---|
| `cold-start-3` (iOS keychain mirror every launch) | **SHIP** — folded into **164** | Source-verified: `_mirrorGroupKeyForPush` (`group_repository_impl.dart:668`) writes UNCONDITIONALLY (only early-return `:663` is a format check, not a presence read) — a real serial **write** blast pre-runApp (the audit's "dominated by reads/skips" under-stated it). Incremental presence-diff (`SecureKeyStore.containsKey`) + post-frame deferral. MUST preserve eventual mirror completeness (NSE group-preview decrypt + mute-honoring) — gate on presence-diff, not a global sentinel. |
| `cold-start-5` (**2** independent serial launch probes) | **low-ship** — folded into **164** | Source-verified TWO probes, not three: `captureInitialIntent` (`main.dart:350`, pre-Firebase) + `getApplicationDocumentsDirectory` (`:395`, already post-Firebase). Overlap share-probe + docs-dir only; Firebase MUST stay `!isShareLaunch`-gated. Modest win. |
| `cold-start-4` (synchronous DI before `runApp`) | **DROP** | Causal-jank claim refuted (AOT alloc is sub-ms); the real pre-frame cost is the awaited I/O that 164 defers. Lazy-DI = huge blast radius for negligible gain. Documented no-op. |
| `db-persistence-6` (contacts/groups indices) | **DEFER** | Social-graph-bounded (~100 rows) → sub-ms gain; not worth a v95 migration + upgrade-path test surface. Revisit only if a power-user profile shows a real list stall; then it is migration 095, a standalone real-SQLCipher DB plan. |
| `animations-repaint-5` (EmptyCircleState reduce-motion gate) | **DROP** | Directly conflicts with the green 156 preservation sentinel TC-25 (`empty_circle_state_test.dart:54-90`, which deliberately keeps it animating under reduce-motion). Offscreen ticking/repaint already handled by 156's RepaintBoundary+TickerMode wraps. Leave the sentinel green. |
| `reactive-streams-4` (NodeState stream no `.distinct()`) | **DROP** | Off-hot-path: 6 subscribers (`p2p_service_impl.dart` stateStream) — 5 retriers edge-trigger via `if (nowOnline && !_wasOnline)`, and `connection_status_indicator._onState` (`:104-106`) RELIES on non-distinct re-emits to update the live connection count, which a top-level `.distinct()` (NodeState `==`) would swallow. **Rationale corrected `[re-verify 2026-06-24]`:** the 141 fire-once latch is producer-side inside `_emitState` (`:2789-2794`), upstream of where a consumer-side `.distinct()` sits, so it would NOT break the latch — the real reason is the connection-count re-emits. No user-visible win; a test would be vacuous. |
| `rebuild-storms-4` (Orbit2 `onPanUpdate` per-pointer `setState`) | **DROP / neutralized** `[added 2026-06-24]` | Source-verified dev-lab gated: `orbit2_floating_canvas.dart:333` full-canvas `setState` per pan delta is reachable only under `kOrbit2PrototypeEnabled = kDebugMode` (`orbit2_prototype.dart:14`) → absent from release. Same gate as `navigation-hangs-5`/`animations-repaint-3` (156 QW-13). Listed so it is not mistaken for an unowned high-frequency `setState`. |

---

## Acceptance-gate strategy (across the set)

- **Host floors (mandatory):** `./scripts/run_test_gates.sh 1to1` (1:1 + conversation), `feed`, `groups`; `./scripts/run_host_test_gates.sh feature-host-all` / `core-host-all`; `flutter analyze` (0 new); `git diff --check`. New tests under `test/features/**` / `test/core/**` AUTO-glob into `*-host-all`; **curated-family** runs (`1to1`/`feed`/`groups`) need the new path added to the matching `*_TESTS` array in `scripts/run_test_gates.sh` (it is an explicit list, not a glob).
- **Real-DB tier (161, + any 095 migration):** `flutter test test/core/database/...` with `sqflite_common_ffi` + `EXPLAIN QUERY PLAN` no-temp-b-tree, mirroring 093/094.
- **Device-proof (164 only, closure gate):** cold local-notif tap, cold FCM tap, foreground push received, `StartupTiming` first-frame-before-`firebase_ready`. The other six plans are **host-only for closure**; their device/sim PERF_TARGET runs are advisory perf lanes, not gates.

---

## Reviewer / Arbiter note

This roadmap is an index; the sufficiency bar (spec-case totality, mutation-verifiable RED, literal gates, harness registration, blind-spot sweep) is enforced **per focused plan (158–164)**, each emitted via `/tdd-plan` from the grounding in `wf_85c86584-4e1`. Structural blockers at the program level: the `feed_wired.dart` collision (sequence 160→161→162→163) and the `ambient_background.dart` ordering (158 before 163). Both are scheduling constraints, recorded above; no plan is blocked on another's *design*, only on merge order.
