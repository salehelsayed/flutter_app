# 252 Review — Fix-List (apply against `252-intro-accept-notification-copy-chat-routing-tdd-plan.md`)

Source: 8-agent adversarial audit (2 verifiers → 5 dimension assessors → completeness critic) + lead source-verification, 2026-07-10. Plan is **ready-with-tightening** (not yet execution-ready): the core technical bet is **verified sound**, but one material integration-seam gap and an untested fallback branch must close first.

Dimension scorecard: D1 Goal clarity **80 (strong)** · D2 Compartmentalization **66** · D3 Anti-drift **72** · D4 Define-good **69** · D5 Tests-verify-goal **64** (all adequate). Critic: **yes-with-tightening**.

Decisions locked with the user (2026-07-10):
- **Copy wording LOCKED as-is** (all four strings are the pinned test assertions): acceptance `Introduction accepted` / `Someone accepted an introduction involving you.`; legacy fallback `Introduction update` / `Open Mknoon to see the latest update.`
- **Wiring gate: ADD a host source-wiring lock** (repo precedent exists) so a forgot-to-wire no-op re-reds at the always-run host tier, independent of device availability.
- **iOS-as-introducer render+tap: record as EXPLICIT "N/A by choice"** (Android-only device closure; do not fold silently into "device closure").
- Next action: **written fix-list** (this file). Plan file left untouched.

Glossary for terms this plan assumes: **A/B/C** = introducer / recipient (`introduction.recipientId`) / introduced friend. **Canonical message_id** = the transport envelope id `<introductionId>::<action>::<senderPeerId>` built by `IntroductionPayload.buildEnvelopeMessageId`. **The coordinator** = the new shared open-flow (plan Step 7) that replaces the intros branch's unconditional `_openIntroOrbitRoute`. **Wiring lock** = a host test that reads `lib/main.dart` as source text and asserts a specific branch calls a specific function (established repo pattern).

Verified facts this list relies on (checked against current source by the lead auditor):
- The intros branch calls `await _openIntroOrbitRoute(navigator: navigator)` **unconditionally** — `lib/main.dart:3846`, inside the private `_handleNotificationRouteTarget` (`:3805`). ✅
- **No host test reaches that branch**: the widget test imports/calls the *exported* twin `openIntroNotificationOrbitRoute` (`lib/main.dart:3123`) directly — `test/features/push/application/intro_notification_orbit_route_test.dart:14,212`; the smoke test wires a *fake* `onRouteTarget` recorder; TC-09 tests the coordinator in isolation. ✅
- **Wiring-lock precedent exists**: `test/core/notifications/app_root_conversation_route_dedup_wiring_test.dart` + the 10-file `test/core/lifecycle/main_*_wiring_test.dart` family read `lib/main.dart` and assert wiring. ✅
- Cold-start + warm + background taps all funnel through the **same** `_handleNotificationRouteTarget` (`main.dart:4665` wires `onNotificationRouteTarget: _handleNotificationRouteTarget`; cold-start reaches it via `startup_router.dart:826 → widget.onNotificationRouteTarget`). So a coordinator placed at the single intros branch covers all three; wrapping individual warm call-sites would NOT. ✅
- Core bet SOUND: `ensureEnvelopeMessageId` writes the canonical id at `json['messageId']` (`introduction_payload.dart:262`) for v1 (`payload.toJson()` normalized) and v2 (`buildEncryptedEnvelope messageId:`); relay `extractMessageId` (`inbox.go:739`) reads it before the opaque block; `::` split unambiguous. ✅
- Device tier is **waivable**: "an unavailable leg is recorded N/A" (plan:453-454, 389-391). ✅
- Old-build downgrade hazard: `fromPayload` exact-matches `'intros'` (`notification_route_target.dart:76`) with catch-all `return NotificationRouteTarget.conversation(payload)` (`:126`) → an old build maps any anchored payload to a garbage conversation, not Intros. ✅
- Existing relay fixture is non-canonical: `inbox_test.go:733` uses `messageId:"intro-1"` (no `::`). ✅
- Introducer local mutual-accept notification is a **single** `showNotification` shared by introducer + non-introducer, body differs via `isIntroducer` ternary — `introduction_listener.dart:425-437`; it fires **only at mutual acceptance** (2nd accept) while A is alive. ✅

---

## §A — Ship-model integrity: the main.dart wiring seam (BLOCKER #1, B-4/B-8)

- **A1.** Add a new host-tier **source-wiring lock** test, e.g. `test/core/notifications/intro_accept_open_coordinator_wiring_test.dart`, modeled exactly on `test/core/notifications/app_root_conversation_route_dedup_wiring_test.dart`: read `lib/main.dart` as a string, scope to the `case NotificationRouteTargetKind.intros:` body inside `_handleNotificationRouteTarget`, and assert it **invokes the new coordinator** AND **no longer contains an unconditional `_openIntroOrbitRoute(` call** in that branch. *Why: the one seam that makes routing true (`main.dart:3846`) has zero always-run coverage today; a no-op that builds coordinator+resolver+copy but forgets to rewire main.dart passes TC-01…TC-11 host-green and is caught only by the waivable device tier.*
- **A2.** Add A1's file to `INTRO_TESTS` (`scripts/run_test_gates.sh:221`) and mirror it in `Test-Flight-Improv/test-gate-definitions.md`. Add it as a new **TC row** (TC-09b) in the Test Contract with HEAD→GREEN = "HEAD intros branch calls `_openIntroOrbitRoute` unconditionally → branch calls coordinator" and mutation "revert the branch to `_openIntroOrbitRoute` → TC-09b re-reds." *Why: makes the forgot-to-wire no-op re-red at the tier that always runs.*
- **A3.** In Step 7, add one sentence: "The coordinator MUST live at the single `_handleNotificationRouteTarget` intros case (`main.dart:3846`) — do NOT wrap individual warm call-sites — because the cold-start entry (`startup_router.dart:826 → onNotificationRouteTarget`, wired at `main.dart:4665`), the background handler, and the warm tap all funnel through it." *Why: prevents the executor from wrapping warm sites and leaving the cold path (the exact D1/D2 scenario) unrouted with green host tests.*

## §B — Step 5's untested host-fallback accept-copy branch (BLOCKER #2, D4/B-9)

- **B1. (recommended) Delete Step 5's accept-ID branch.** Change Step 5 so the Dart background fallback for intros **always** emits the generic `Introduction update` / `Open Mknoon to see the latest update.` when provider title/body are absent, and let the relay (TC-02) solely own acceptance wording. *Why: the current relay always attaches a notification block for intros, so `shouldShowBackgroundPushFallbackNotification` returns false for relay-built intros pushes (`background_push_notification_fallback.dart:43`) — the accept-copy fallback branch is near-unreachable in production, and leaving it untested creates a Scope-vs-Steps inconsistency (Scope plan:128-129 lists only the generic fallback).*
- **B2. (only if B1 is rejected)** Keep the branch but add **TC-01b**: "data-only intros push with a `::accept::` message_id and no provider title/body → `Introduction accepted` / `Someone accepted an introduction involving you.`", mutation "fall through to generic/global copy → re-reds", registered to the same file. *Why: never ship a user-facing copy path with no named test or mutation guard.*
- **B3.** Whichever of B1/B2 is chosen, name the two existing preservation tests `background_push_notification_fallback_test.dart:525` ("preserves provided copy for a new-introduction intros fallback") and `:541` ("...introducer mutual-accept intros fallback") as explicit **must-stay-green sentinels** in the Test Contract — the route-aware fallback change must not regress provider-copy preservation.

## §C — Cross-language producer/consumer fixture (BLOCKER #3, D5)

- **C1.** Change TC-02's Go table to use the **exact canonical** `<introId>::accept::<senderPeerId>` string that `IntroductionPayload.buildEnvelopeMessageId` emits (as the v2 top-level `messageId`) plus a distinct v1 row carrying `payload.action:"accept"`. Do **not** reuse the `intro-1` shape from `inbox_test.go:733`. *Why: TC-06 (Dart parser) and TC-02 (Go relay) currently use independent hand fixtures; if the Go shape drifts from real Dart output, both stay green while production envelopes fail to parse action — exercised only at the waivable device tier.*
- **C2.** Add a shared/golden cross-language pin: a Dart test asserting `buildEnvelopeMessageId(introductionId:'X', action:'accept', senderPeerId:'Y')` equals the literal string the Go TC-02 consumes (e.g. document the golden literal in one place both tests reference). *Why: pins the two sides to one canonical shape so the seam can't silently break.*

## §D — Anchored-intros wire-format precision, sibling enumeration, downgrade honesty (BLOCKER #4, B-1/B-2/D3)

- **D1.** In Step 3, pin the **exact anchored payload wire string**: reuse the existing group marker convention — `intros|message:<canonicalMessageId>` (mirrors `notification_route_target.dart:63/91`). *Why: "backward-compatible local payload encoding" (plan:241) leaves the literal to the executor; two executors pick two formats.*
- **D2.** In Step 3, explicitly name the **symmetric `fromPayload` edit**: `fromPayload` must prefix-parse `intros|message:<id>` into an anchored intros target **while still exact-matching bare `'intros'`** for backward compatibility (`notification_route_target.dart:76`). List `toPayload` (`:64`) and `fromPayload` as a lockstep pair. *Why: an executor could edit `toPayload` without the symmetric parse; TC-04 catches asymmetry but the plan should preclude drift, not rely on tests to catch an unstated contract.*
- **D3.** Correct the "backward-compatible" claim (Step 3 line 241 / Dependency line 188): state plainly that an **old build** parsing a new anchored local payload hits `fromPayload`'s catch-all (`notification_route_target.dart:126`) and routes to a **dead conversation, not Intros** — a soft/ephemeral downgrade (recoverable by back-nav), acceptable but **not** strict backward-compat. Note the remote path *is* backward-compatible (old client drops `message_id`, routes to generic Intros). *Why: honest labeling; the remote and local compat stories differ.*
- **D4.** Add `handle_foreground_remote_message_use_case.dart:16` and `notification_open_dedupe_gate.dart:120` to the **Do-not / leave-unchanged sentinel** list, and state that **foreground** intros-accept intentionally keeps the Intros route (app already open, no tap-to-open) — so the foreground-vs-tap divergence is a recorded decision, not drift. *Why: verified both are safe today (dedupe keys on `message_id` from data at `:61`, not the route target), but a global "thread messageId everywhere" pass has no plan signal to stop.*

## §E — iOS closure honesty (BLOCKER #5, B-10) — decision: explicit N/A-by-choice

- **E1.** In the Device/Relay Proof Profile closure section, add an explicit line: "**N/A by choice:** iOS-as-introducer render+tap is NOT proven. iPhone is used only as party C. The APNs acceptance-copy render and the iOS local-notif tap seam (`didReceiveResponse`, distinct from Android `onNewIntent`) are asserted only at relay-build time (TC-02), never rendered/tapped on real iOS. Cross-platform closure covers Android introducer only." *Why: do not fold an unproven platform leg silently into "device closure"; the user chose to record it as an intentional gap.*

## §F — Smaller tightenings

- **F1. Warm/local leg is mutual-only AND device-unproven (D2 missing-dim).** Add a Scope sentence: the introducer LOCAL notification exists only at **mutual** acceptance (2nd accept, while A is alive), while the REMOTE relay push fires per-acceptance; both share the one canonical-accept decision seam. Then add a **host-integration assertion** (extend TC-09) that a **local** anchored payload flows `fromPayload → resolver → conversation:peer-B` — because D1/D2 terminate A and therefore never exercise the local leg; TC-10 only asserts the payload string, not that a local tap routes to B.
- **F2. TC-10 / Step 8 precision.** State that `introduction_listener.dart:425-437` is a **single** `showNotification` shared by introducer and non-introducer; the anchored payload must be gated on `isIntroducer && responderId != null`, else it re-anchors the participant notification and violates Must-preserve line 143. *Why: easy to miss; a blanket payload change breaks participant behavior.*
- **F3. Device harness feasibility gate (D2/D5 missing-dim).** Add a Step-9 pre-check that `adb shell dumpsys notification` actually exposes the literal title/body text of a relay-built FCM notification (Android may format/truncate/localize extras) BEFORE relying on it for copy assertion; if not, switch to a UiAutomator content-text read. *Why: the whole copy-assertion half of D1/D2 is unimplementable if dumpsys doesn't surface the text.*
- **F4. Pin the UIAutomator tap selector.** Replace "a bounded UIAutomator selector" (plan:226-227,412) with the literal expression, e.g. `UiSelector().textContains("Introduction accepted")` or the app notification content `android:id`. *Why: the anti-coordinate-tap guard (plan:298) is stated but the positive selector that satisfies it is not — the most fragile step is left to improvisation.*
- **F5. Machine-checkable status-context marker.** Give the secondary device assertions ("B/C connection status is visible" plan:414; "rendered B-acceptance progress text" plan:412) a named test-only marker (e.g. `statusContext=bc_connected`, `finalPeer=peer-B`), not rendered-text/visibility. *Why: the plan forbids visual judgment (plan:227) but part of the closure evidence rides on "is visible".*
- **F6. Pin relay `pass` copy.** Add a `pass` row to TC-02/TC-03's Go table asserting `pass` keeps generic copy, OR tighten TC-03's mutation to "only `accept` yields acceptance copy." *Why: a broad `action != 'send'` match could leak acceptance copy to `pass` uncaught; low blast radius but technically untested.*
- **F7. Step-4 relay Stop-if.** Add a Stop-if to Step 4 (parallel to Step 2): "stop and fix the envelope contract if a real recorded v2 opaque envelope's `extractMessageId` does not return `<id>::accept::<sender>`." *Why: the relay-parse bet is the only load-bearing bet without a host Stop-if.*
- **F8. Consolidate the "done" line (D1 nit).** Add one invariant at the top of the Scope Contract: "Acceptance notification shows the committed action-aware copy, and an introducer tap deterministically lands in the recipient (B) conversation (`finalPeer=peer-B`) for either responder." *Why: goal legibility currently requires assembling three sections.*
- **F9. Re-sequence (D2, optional).** Consider splitting into Milestone A (copy: verify the canonical-ID bet, then TC-01/02/05 → relay + fallback gates → shippable) before Milestone B (routing: TC-04/06/07/09/10 + Steps 3,6,7,8), and move the Step-2 bet check ahead of Step-1's authoring of TC-07/TC-09. *Why: the plan itself says copy can deploy independently (plan:190); today the low-risk copy slice is un-isolated behind the higher-risk routing work. Optional — accept if the batch RED authoring is preferred.*

---

## Priority order to apply
1. **§A (all)** — the wiring lock. Without it the feature can ship unwired and host-green; this is the one material blocker that gates the whole ship model. *(Locked: add the host source-wiring lock.)*
2. **§B1 + §C** — remove/guard the untested fallback branch, and pin the cross-language relay fixture to the real canonical shape. Makes the copy win verifiable and the relay bet non-vacuous.
3. **§D + §F2 + §F7** — pin the wire-format + symmetric parse, enumerate siblings + single-sink location, correct the compat claim, and add the relay Stop-if. Precludes the drift that passes host and fails device.
4. **§E + §F1 + §F3–F5** — closure honesty (iOS N/A + warm-leg proof) and device-harness executability/machine-checkability.
5. **§F6, §F8, §F9, §B3** — residual copy pin, done-line, optional re-sequence, preservation sentinels.
