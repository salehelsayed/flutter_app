# 270 Review — Fix-List (apply against `270-protected-photo-tap-tile-tdd-plan.md`)

Source: 8-agent adversarial `/tdd-review` (`wf_b363931d-e03`) + lead-auditor source verification, 2026-07-24. Plan is **ready-with-tightening** (critic: yes-with-tightening). Core bet **verified sound** (the optimistic-`sending`-row forgery premise is real — the row persists at `conversation_wired.dart:3550/:3553` before the send at `:3609`), but that soundness justifies only the retry-lane's *internal* matrix, not the guard's *existence*.

Dimension scores: Goal 62 · Compartmentalization 61 · **Anti-drift 77** · Define-good 60 · **Goal-verification 79**.

Decisions locked with the user (2026-07-24):
- **Retry-lane fate = DROP.** Remove the in-`sendChatMessage` `allowsNewPrivateMedia` guard and the entire `LegacyPrivateGifRetryLane` apparatus. The composer-layer split alone satisfies the user's "new GIF = Keep in chat" decision; legacy retries keep passing the unchanged compatibility validator. This choice **eliminates blockers B-6 (front-guard lock hazard), B-8 (host-only transport proof), and most of B-2/B-9 (send-caller census)** because `sendChatMessage` and its 8 callers are no longer modified.

Verified facts this list relies on (checked against source by the lead auditor):
- **Composer split fully contains fresh GIF.** The only UI send calls `widget.sendChatMessageFn` (`conversation_wired.dart:3886`) with `_privateMediaPolicy`; every private-capable writer of `_privateMediaPolicy` is gated by `_setPrivateMediaPolicy` (`:5508`) and `normalizePrivateMediaComposerPolicy` (`:5570` → `private_media_policy.dart:279`), and the selector visibility is gated at `compose_area.dart:435`. ✅
- **No fresh non-composer path carries a private policy.** `feed_wired:2125`, `share_batch_delivery_coordinator:1306`, `send_voice_message_use_case:189` pass **no** `privateMediaPolicy` (→ the `mode != ordinary` guard block at `send_chat_message_use_case.dart:381` is skipped). ✅
- **The edit-resend wrapper is inert on HEAD.** `editChatMessage` (`:1525`) passes `originalMessage.privateMediaPolicy` with `action: actionEdit`; `_privateMediaEligibilityForSend` sets `isEdit: action == actionEdit` (`:257`), and `allowsPrivateMedia` has `!isEdit` (`:80`) → `validatedFor` → `unsupported` → rejected at `:389-391` today, **for any kind**. So there is **no live edit regression** (this corrects a D4/D3 false-positive). ✅
- **Legacy retries would pass unchanged without the guard.** `validatedFor` (`private_media_policy.dart:241`) uses the GIF-inclusive getter; `retry_incomplete_uploads:643` and `retry_failed_messages:639` pass `row.privateMediaPolicy`. Only the plan's *new* guard breaks them. ✅
- **The authority the plan re-derives already exists and is locked.** `_commitOutgoingDirectPrivateEnvelopeForTransport` (`send_chat_message_use_case.dart:2211-2287`) already implements the case-a/case-b attachment authority under `coordinator.lifecycleLock.synchronized` (`:2255`). The plan's proposed *second* fingerprint read never mentions that lock (plan grep for `lifecycleLock`/`synchronized` = 0 hits) → strand hazard (moot under DROP). ✅
- **Line citations are otherwise accurate**; only cosmetic range drift on two retry-send citations (both deleted under DROP).

Glossary (plan-assumed terms): **"composer split"** = add an image/video-only `allowsNewPrivateMedia` for new-selection while keeping the GIF-inclusive validator for compatibility/decode. **"layer-2"** = the existing `_commitOutgoingDirectPrivateEnvelopeForTransport` authority. **"front-guard"** = the new pre-encryption GIF check the plan proposed to add to `sendChatMessage` (deleted under DROP).

---

## §A — Drop the self-inflicted transport-authorization subsystem (locked: DROP)

- **A1. Do NOT add the in-`sendChatMessage` `allowsNewPrivateMedia` guard.** Leave `send_chat_message_use_case.dart:381-399` unchanged — `validatedFor` (GIF-inclusive) stays the sole private-media validation there. *Why:* the composer split already forces every production fresh path to ordinary-GIF; the guard's only real-world effect is to break the two legacy retries, which the rest of the subsystem then exists to un-break.
- **A2. Delete the entire `LegacyPrivateGifRetryLane` apparatus from the plan.** Remove Scope-item-3 paragraphs at plan `:103-105` (the enum, the lane-and-parent authorization, the completion-fingerprint/coordinator-owned-pending front-guard authority, the lane wiring into `retryIncompleteUploads`/`retryFailedMessages`). `retry_incomplete_uploads_use_case.dart`, `retry_failed_messages_use_case.dart`, and `retry_unacked_messages_use_case.dart` become **untouched** — strike them from "Affected files" (`:54`) and "Files To Inspect" (`:150-154`, `:162-163`). *Why:* with the guard gone, these paths keep working via the unchanged compatibility validator; there is nothing to grandfather.
- **A3. Re-scope TC-18 to composer-selection + unchanged-legacy sentinels only.** Strike every guard/lane/authority leaf from TC-18 (`:201`), Test Notes (`:214`), Risks (`:219`), `INV-7` (`:236`), and Scope-drift (`:324`): null-lane, wrong-lane/status, parent-metadata mismatch, attachment-authority (completed / coordinator-owned-pending), optimistic-row self-authorization, arbitrary-pending, the active-viewer coordinator race, the three retry-path proofs, and the `retry_incomplete_uploads_pending_open_race_test.dart` variant (`:163`). **Keep** the composer/new-selection proofs (see §B5) and the legacy-preservation **sentinels** (`message_payload` decode stays private; ordinary-GIF wire; the iOS `full_screen_typed_media_viewer` native surface) — these are GREEN on HEAD and stay green **with zero production change**, so they remain as cheap regression guards proving the drop touched nothing. *Why:* the send/retry code is unmodified, so legacy private-GIF decode/retry/native protection are preserved by construction.
- **A4. Record that DROP closes the send-path evergreen hits.** Add a one-line note to the plan's Risks section: "Because `sendChatMessage` and its 8 callers are untouched, the front-guard lock hazard (B-6), the host-only transport-authorization proof (B-8), and the send-caller census gap (B-2/B-9) do not apply." *Why:* keeps the audit trail honest about which risks the decision retired.

## §B — Composer-layer split: the remaining necessary backend work (pin it)

- **B1. Pin `allowsNewPrivateMedia`'s full predicate.** Specify it as `(kind == image || kind == video)` **intersected with** the existing shape guards `attachmentCount == 1 && !hasTextOrCaption && !isEdit && !isForward` — i.e. identical to `allowsPrivateMedia` (`private_media_policy.dart:72-82`) except the kind set. Update plan `:101` and `:146`, which currently describe it **kind-only**. *Why:* a literal implementer could author just the kind check and silently drop the shape guards.
- **B2. Keep the compatibility validator and decode GIF-inclusive and UNCHANGED.** `validatedFor` (`private_media_policy.dart:241`) and the decode path (`message_payload.dart:213-221`) must continue to use the image/gif/video getter. The rename to `allowsPrivateMediaPayload` is an **optional clarity nicety** under DROP (not load-bearing); if kept, update only `:241`/`:279` and introduce **no** `sendChatMessage` consumer of either getter. *Why:* older-peer and durable private-GIF payloads must stay private/validated.
- **B3. Point exactly the four composer/reset sites at `allowsNewPrivateMedia`.** `compose_area.dart:435` (selector visibility), `conversation_wired.dart:3310-3311` (send-guard), `:5508` (`_setPrivateMediaPolicy`), and the `normalizePrivateMediaComposerPolicy` call at `:5570` (flip the getter inside `private_media_policy.dart:279` — it has exactly one caller, direct-only; groups use the separate `GroupPrivateMediaEligibility` at `group_private_media_policy.dart:57`). *Why:* this is the whole mechanism that delivers "new GIF = Keep in chat."
- **B4. Add an execution note for the extra `_privateMediaPolicy` writers (B-5).** The plan's inspect list names only `:3306-3316/:5506-5511/:5569-5574`, but snapshot-restore (`conversation_wired.dart:4409/:4449`) and the E2E outbox (`:6522`) also write `_privateMediaPolicy`. Both are safe today (they re-run `_updateComposerState`'s normalize; the outbox is image/jpeg-only), but pin the invariant: *every private-capable write of `_privateMediaPolicy` must funnel through `normalizePrivateMediaComposerPolicy`; a future refactor of `_updateComposerState`/`_restoreComposerSnapshot` must not reopen a bypass.* *Why:* the split's safety silently depends on normalize being the sole funnel.
- **B5. Keep the composer/new-selection RED proofs as the real TC-18 residue.** Retain: `private_media_policy_test` "new selection permits image/video while compatibility preserves GIF"; the composer "selector appears only for one eligible image/video and GIF stays keep-in-chat" + "stale/ineligible shape resets private policy"; and `conversation_wired_test` "replacing a private image draft with GIF resets to keep-in-chat before optimistic persistence." *Why:* these guard the only backend behavior the goal requires, and remain causal RED→GREEN after the drop.

## §C — Test-contract precision (applies regardless of §A)

- **C1. TC-11 ar/de must assert LITERAL strings, not getter output.** The existing `direct_private_media_placeholder_body_test.dart:106-109` asserts `find.text(ar.private_media_protected_body_received(name))` — a getter-vs-getter tautology that cannot catch a mistranslation. Author TC-11's new-compact-key assertions against **hard-coded** expected ar/de remainders, plus a negative "no old lead-in" assertion and a real `de` render (plan `:194`/`:210` say "exact" — make "exact = literal" explicit). *Why:* otherwise the new key inherits the tautology the plan is trying to fix.
- **C2. Split TC-18's blanket registration cell per file.** Replace "all except composer are existing/new `ONE_TO_ONE_TESTS` owners…" (`:201`) with an explicit per-file list stating which surviving paths are in `ONE_TO_ONE_TESTS` vs run explicitly (`l10n` tests, the `full_screen_typed_media_viewer` sentinel, `message_payload` if unregistered), mirroring the granularity TC-11/15/17 already use. *Why:* per-file registration can't be audited from a blanket cell — the exact "unregistered feature-glob test passes silently" hazard the plan warns about elsewhere.
- **C3. Anchor the "byte identical" / parity claims (B-10).** Add a one-line "the wire envelope format is invariant under this plan" note so the ordinary-GIF byte-equality claim rests on unchanged-transport rather than on the fake bridge; and either add an Android native-protection sentinel or state Android native protection is explicitly out-of-scope/unchanged (only the iOS surface is sentinel-locked today, plan `:172`). Under DROP the cached-envelope path is untouched, so this is documentation only.

## §D — Goal framing & hygiene (low priority)

- **D1. Promote the composer kind-boundary into the goal statement.** The "Behavior to improve" statement (`:47`) is UX-only; the image/video-only new-selection boundary survives the drop and is co-equal work. Rewrite `:47` (and the title) to name both objectives, each with its measurable outcome, so the goal encompasses the plan's real scope. *Why:* a goal statement that omits half the change can't be used to scope or challenge blast radius.
- **D2. Bulletize the Do-not list.** The ~250-word run-on at `:124` should become discrete lines so each prohibition is individually checkable at each Stop-if — and prune the now-obsolete GIF-lane prohibitions after §A. *Why:* a run-on is unscannable exactly when a Do-not list must be scanned.
- **D3. Correct cosmetic citation drift** *(only if any retry citation survives §A2, else moot):* retry_incomplete send is `:643-661` (plan says `:643-659`); retry_failed is `:639-660` (plan says `:639-658`).

## Priority order to apply
1. **§A** — drop the transport-authorization subsystem. Collapses the plan's risk surface and retires B-6/B-8/B-2/B-9; must precede test re-scoping.
2. **§B** — pin the composer split (the only backend work the goal needs) and its `_privateMediaPolicy`-funnel invariant.
3. **§C** — make the surviving test contract auditable and non-tautological (TC-11 literals, TC-18 per-file registration, parity notes).
4. **§D** — goal statement, Do-not hygiene, cosmetic citations.
