# 221 Review — Fix-List  (apply against `221-signal-light-background-reskin-tdd-plan.md`)

Source: 8-agent source-verified audit (2 verifiers + 5 dimension assessors + completeness critic) + orchestrator linchpin re-checks, 2026-07-07.
Verdict: **core bet VERIFIED SOUND; NOT ship-ready as written → ready-with-tightening.** All fixes below are plan-level edits; none re-architect. Critic: yes-with-tightening.

Dimension scores: D1 Goal 68 · D2 Compartmentalization 62 · **D3 Anti-drift 54 (weak)** · D4 Define-good 58 · D5 Goal-verification 60.

Glossary for terms the plan assumes: **tone seam** = `context.backgroundReadableColors` / `context.feedTokens` (a `ThemeExtension` injected per-surface by `AmbientBackground`); **Signal** = the new light palette replacing "Daylight Lagoon" (storage key `daylight_lagoon` kept, no migration); **preservation sentinel** = a widget test proving the 3 dark presets render pixel-identically after the re-skin.

## Decisions locked with the user
- **Scope:** FIX EVERYTHING in 221 — the 5 missed/broken surfaces AND the structural node-glow fix are now in-scope (not de-scoped to a 221b).
- **Fidelity gate:** PIN EXACT Signal hex values as unit assertions (not WCAG-floor + manual-only).
- **No release-risk question needed:** pure presentation re-skin, storage key unchanged → no rollback/brick vector (B-1/B-5 clear).

## Verified facts this list relies on (checked against source this session — build on these)
- Core bet SOUND: `FeedTokens` consumed only via `context.feedTokens`; **no static `FeedTokens.dark` bypass** (only `app_theme.dart:35` registration + the `feed_tokens.dart:174` fallback). Feed AND chat wrap their own `AmbientBackground` (`feed_screen.dart:181`, `conversation_screen.dart:325`, `group_conversation_screen.dart`). ✅
- `AmbientBackground` re-injects extensions at a single `build()` (`ambient_background.dart:135-146`); ThemeData keys extensions by runtime type (**last-wins**) → **one** tone-keyed `FeedTokens` append reaches all ~17 mounts. ✅
- Ring painter: `orbital_ring_painter.dart:42-44` consts + no color params; `orbital_visualization.dart:114` already reads `context.backgroundReadableColors` and builds the painter at `:172` → tone threading trivial; unread indicator is a **separate** widget → TC-194-30 not threatened. ✅
- Dark path returns exact old const because `toneForPreference→dark` for the 3 dark presets; value-correctness is pinned ONLY by the `dark==const` assertions (they carry all of INV-1). ✅
- Every cited widget `file:line` is EXACT **except** `ambient_background_test.dart:159` — the white-base assertion is at **L408**. ✅

---

## §A — Correctness: the node-glow fix is mislocated (spec failure #3 "dead node glows")  [BLOCKER]

- **A1. The friend-node glow is structurally CLIPPED on every ground — recoloring cannot fix it.** `user_avatar.dart:186-196` `_wrapWithGlow` returns a `size×size` Container whose `boxShadow` (`blurRadius: size*0.35`, `spreadRadius: size*0.05`) paints **outside** its box; `orbital_avatar.dart:130` wraps the avatar in `ClipOval(child: UserAvatar(...))`, which clips that halo away. `orbital_visualization` emits no node glow at all. **Why:** the plan's step 7 ("user_avatar glow visibility") + RED #9/#16 (assert glow inside `orbital_visualization_signal_test`) target a seam that renders no visible glow — TC-221-16 is **un-satisfiable as written**, and spec failure #3 would NOT be fixed.
- **A2. Specify the real fix seam.** Pick ONE and write it into Real Scope + the step plan: **(a)** paint the node halo in `OrbitalVisualization` itself (a glow layer drawn *behind* the `OrbitalAvatar`, outside the ClipOval, using new tokens) — cleanest and testable; OR **(b)** move `_wrapWithGlow` outside `orbital_avatar.dart:130`'s `ClipOval` (wrap the ClipOval, not its child) and let the shadow extend. **Recommend (a)** — it gives `orbital_visualization_signal_test` a real rendered glow to assert and keeps `UserAvatar` unchanged for non-orbit uses.
- **A3. Rewrite RED #9/#16 + TC-221-16** to assert the RENDERED halo (a `BoxShadow`/paint captured off the built orbit tree) equals the new `nodeSelfGlow`/`nodeContactGlow` token on light and the captured per-peer/current value on dark — NOT a token-in-isolation assert on a clipped halo (a pinned assert on an invisible glow is vacuous — see D5 vs verifier-1 contradiction).

## §B — Missed hardcoded-dark surfaces still broken on Signal (B-2 cluster)  [BLOCKER — now in-scope]

Add each to Real Scope (plan L51), the RED catalog, and the matrix. Each is a `build(context)` site → readable via the tone seam.

- **B1. ExpandableFab menu-item pills — white-on-white.** `expandable_fab.dart:172-192`: Container `Colors.white.withOpacity(0.1)` fill / `0.15` border, `Icon(Colors.white)`, `Text(Colors.white)`. Mounted at `orbit_screen.dart:858` ("New group" / "New announcement") under `AmbientBackground` → invisible on porcelain. **Also** `expandable_fab.dart:88` builds the const-colored `GlowFab` (`glow_fab.dart:25-32` `0xFF1A1A2E`/`0xFF64B5F6`) → `ExpandableFab` must gain + thread a color param to tone that FAB (GlowFab is used ONLY here). Add a RED asserting menu-pill fill/text tone-aware + legible on light, dark unchanged.
- **B2. Mic — 3 green literals beyond the accent.** `voice_record_button.dart:26` `_accentColor 0xFF1DB954` is cited, but `:73` un-pressed fill `rgba(29,185,84,.15)`, `:76` border `rgba(29,185,84,.3)`, `:111` pressed shadow `rgba(29,185,84,.24)` are NOT. Toning only the accent leaves a green-tinted mic on light. Extend the mic token set to all four; TC-221-20/21 must assert fill+border+shadow, not just accent.
- **B3. Empty-conversation state — date, divider, avatar glow untested.** `empty_conversation_state.dart:77` date `rgba(255,255,255,.35)` (ghost on porcelain — same class as failure #1), `:182` dashed divider `white .12`, `:146-155` avatar glow `rgba(78,205,196,.3)` + black `.4`. Plan tests only hint (`:91`, TC-11) + heading (`:62`, TC-12). Add TCs for date + divider + avatar glow.
- **B4. user_avatar photo-frame / fallback — dark chip default-on.** `user_avatar.dart:216-217` (photo) + `:244-245` (fallback) fill `rgba(22,24,30,.7)` + border `white .35`; `showPhotoFrame` defaults true (`:111`). Visible on the Orbit center self-avatar (`orbital_visualization.dart:189/204`) and empty-state avatar (`:158`). Frame fill hides behind a real photo (lower impact) but border + fallback blob are tone-blind. Tokenize frame border/fill.
- **B5. Send button == mic green.** `compose_area.dart` send button is `0xFF1DB954` (same as mic) with no dedicated TC — the accent-asymmetry the plan's own blind-spot sweep flags. Add it to the composer tone edit + a TC so send and mic move together on light.

## §C — Test modify-surface is under-enumerated + one off-target line  [BLOCKER — breaks the plan's OWN gate]

- **C1. Fix the off-target line: L159 → L408.** The white-base `expect(decoration.color, Colors.white)` is at `ambient_background_test.dart:408`, NOT L159 (which is the chat-surface opt-in count test). Correct **every** cite: RED #19 (plan L118), Existing-Tests (plan L64), TC-221-01's regression note, AND spec §3.6 (spec L114). Note the siblings to PRESERVE: mapping `==representativeLight` at `:396`, `statusBarIconBrightness==Brightness.dark` at `:402` — edit only the color line.
- **C2. Enumerate the full rename-breakage surface.** The plan lists only `background_choice_control_test.dart:54`. The l10n rename ALSO breaks: `background_choice_control_test.dart:56` (description literal `'A bright lagoon sky with soft pastel blooms.'`), `:248/:312/:319/:320/:335` (label/value literals **including the composed accessibility value** `'Daylight Lagoon selected'` → must become `'Signal selected'` — the plan never states the new value), AND **`settings_screen_test.dart:210`** `expect(find.text('Daylight Lagoon'), findsOneWidget)` which runs under `feature-host-all`. **Why:** §Acceptance claims `feature-host-all` "0 new failures" — false until all sites are updated. List every site in RED #18 / TC-221-28 and state the new semantics value.
- **C3. Supply the actual de/ar strings.** Step 9 gives only EN copy and says "edit app_de/ar.arb" with no targets. Current values: `app_de.arb:365-367` (`'Tageslicht-Lagune'` + old desc + `'…selected'`), `app_ar.arb:365-367`. TC-221-29 only checks "no raw-key fallback" → a stale `Tageslicht-Lagune` or English copy ships GREEN. Write the real German + Arabic label ("Signal") + description into the plan, and strengthen TC-221-29 to assert the NEW translated strings, not just non-fallback.

## §D — Dark-preservation guard is under-specified / can be vacuous (INV-1 hollow)  [BLOCKER]

- **D1. Multi-stop gradients — assert the full gradient, not a single color.** The composer bar is a **2-stop** `LinearGradient([Colors.transparent, Color.fromRGBO(10,10,15,.95)])` stops `[0,0.2]` (`compose_area.dart:317-323`), not a solid. Replacing it with a solid token DROPS the transparent-top fade on the DARK presets, yet TC-221-24 (`composer == rgba(10,10,15,.95)`) still passes → vacuous. Same class for nav: `barGradientColors` `[white .18, white .10]`, `activePillGradient` `[white .25, white .12]`, `badgeGradientColors` `[0xFFFF3B30, 0xFFE0342A]` (`nav_bar_theme.dart:15-63`) — single-color tokens can't reproduce these lists. **Fix:** keep the composer a 2-stop `[transparent, tokenColor]` gradient on BOTH tones; TC-221-24 must assert the full `colors` list + `stops`; TC-221-21 must assert the nav gradient LISTS the consumers produce.
- **D2. Assert RENDERED colors under a real dark pump — enumerate ALL surfaces.** `dark_preset_preservation_test` is one line (plan L122-123) with no per-surface pump list. It must PUMP each of {glow_fab, expandable_fab menu, compose_area bar, voice_record_button fill+border+shadow, the nav host + active pill, orbital ring painter, letter_card_system, user_avatar glow+frame} under each of the 3 dark presets' resolved `BackgroundReadableColors` + `FeedTokens.dark`, and assert the **rendered** color (DecoratedBox/Container decoration, `Text.style.color`, painter param captured off the built `CustomPaint`) == a **transcribed HEAD literal** — NOT `BackgroundReadableColors.dark.<token>` (self-referential: passes even if the dark value drifts). Enumerate all ~18 tokens, not a sample.
- **D3. Legibility tests must read the rendered element too.** TC-221-08/09/10/11/27 must extract fg/bg off the pumped widget tree (find the Container decoration color + the Text's resolved `style.color`) and feed THOSE to `contrastRatio` — not recompute from `FeedTokens.light`/BRC constants in isolation. **Why:** a token-only assert proves the palette is legible but NOT that the tone-blind widget was rewired to consume it — the exact bug being fixed still passes.

## §E — Evaluation gate is easier than reality + fidelity un-pinned  [BLOCKER for legibility claim]

- **E1. The WCAG "absolute gate" measures the WRONG background.** The plan LOWERS `representativeLight` textSecondary `0xFF293241→#4A4E5C`, textMuted `0xFF4B5563→#656A79` (spec says 4.66:1 on `#EDEEF3`, near-floor), placeholder `0xFF566173→#6A6F7E`. But `background_readable_colors_test.dart:97-100` checks these against `surfaceBase 0xF7FFFFFF` / `inputFill 0xF2FFFFFF` (**near-white, ~5.2:1**), NOT the Signal ground `#EDEEF3`. The gate is therefore easier than reality and can pass while muted/hint text on the real porcelain is at/below 4.5:1. **Fix:** either (a) re-skin `surfaceBase`/`inputFill` to the Signal porcelain family and pin them (they're absent from scope L51 today), OR (b) add an explicit assertion that textMuted/placeholder/hint have ≥4.5:1 **against the `#EDEEF3` ground**, and reconcile gate-bg vs ground-bg in the plan.
- **E2. Extend the gate's field list to the NEW tokens.** `_expectTextContrast`/`_expectComponentContrast` (`background_readable_colors_test.dart:96-110`) enumerate a **fixed** field list — adding a token to `BackgroundReadableColors` does NOT enroll it. TC-221-07's "no token regresses" is **false as written**. In step 2, add every new text/fill pairing (composerHint vs composerInputFill, connectedHeading vs ground, accent-icon vs accent-fill, placeholder vs new inputFill) to the helper, and reword TC-221-07 to assert the gate LOOPS the new fields.
- **E3. Exact-pin the Signal fidelity values (locked user decision).** Add unit assertions mirroring the existing `FeedTokens.dark`/BRC.dark exact-pins:
  - `FeedTokens.light`: pill bg `0xFFDFF3E9`, pill text `0xFF0A5D34`, `green500 0xFF0C7C46` (spec §3.5 L105-106) — in `feed_tokens_tone_test`.
  - Ambient washes: the two remaining bloom inputs == violet `0x0D5A24E0` / blue `~0x0A2E5AC8`, teal/pink removed (extend TC-221-02 which already introspects these params).
  - Ground base: `#EDEEF3` + radial stops `#F5F6FA→#EEEFF4→#E7E9F0` (tighten TC-221-01 from "!= Colors.white / in family" to exact).
  - Node glow tokens: self `rgba(90,36,224,.50)`, contact `rgba(229,72,77,.45)` (spec §3.5 L103-104) — tie to §A3.
  - Accent-on-icon legibility: add `expectComponentContrast(accentIcon, accentFill) ≥ 3:1` to `glow_fab_tone_test`/`nav_bar_theme_tone_test` so accent legibility carries a number, not just a value-match.
- **E4. Fix test registration so the aggregate actually runs the new tests.** (a) Move TC-221-29's l10n test to `test/core/l10n/app_localizations_signal_test.dart` (currently `test/l10n/` is swept by NEITHER `feature-host-all` NOR `core-host-all`) and add its literal `flutter test` line to Acceptance Gates. (b) The `feed` gate is a hand-list (`FEED_TESTS`, `run_test_gates.sh:168`), NOT a glob — either add the two new feed tests to it as an explicit step-5 action, or drop the `feed`-gate claim and fix the matrix cells from "AUTO + FEED_TESTS" to "AUTO (feature-host-all glob)".
- **E5. Reconcile the "done" contrast number.** Spec S2 targets pill ≥6.9:1 / placeholder ≥4.7:1; TC-221-08/10 gate ≥4.5:1. Add one sentence: "pass gate = WCAG floor (4.5 body / 3 large); the 6.9/4.7 figures are design outcomes produced by the exact Signal values pinned in E3."

## §F — RED-first & compartmentalization hygiene (moderate)

- **F1. Kill the "author all RED 1-21 up front" waterfall.** Step 1 (L185) + Done Criteria (L259) say add catalog 1-21 RED-first, but Known-Failure (L253) says only 1-17 fail on HEAD, and several REDs reference symbols that don't exist yet (new BRC tokens #5/#6, painter params #7, `FeedTokens.light` #15/#16) → they compile-fail, not "fail for the documented reason". Author each new-symbol RED at the HEAD of its own green step, immediately before the symbol is introduced. Reconcile the 1-17 vs 1-21 ranges and label #5/#8/#14/#16-dark/#21 as green-from-start preservation-pins.
- **F2. Make the preservation sentinel a green-throughout gate, not a step-11 afterthought.** Author `dark_preset_preservation_test` right after step 2 (once the tokens compile) and re-run it as a mandatory green gate at the END of each of steps 4/5/6/7 — not only step 11. Rewrite step 11 to "run (not author)" it and remove catalog #21 from step 1's RED batch.
- **F3. Split step 7's three shared-global conversions.** 7a glow_fab/expandable_fab (CTA + menu, §B1), 7b nav_bar as its own slice with a stop-if ("dark nav gradient must stay exact `white .18/.10` + border `.30` + the pill/badge lists → preservation sentinel green, else replan the threading"), 7c user_avatar glow (§A) + frame (§B4). Mirror step 4's stop-if on 7b.

## §G — Mechanism precision notes (correct the plan's prose)

- **G1. Correct the FeedTokens injection prose — a bare append works; filtering is OPTIONAL.** `ambient_background.dart:138` already spreads `theme.extensions.values` (includes the global `FeedTokens.dark`); appending the tone-chosen `FeedTokens` after `readableColors` **overrides by type (last-wins)**. Do NOT tell the executor the `.where()` FeedTokens-exclusion is mandatory (it isn't). State step 5 as: "append `tone==representativeLight ? FeedTokens.light : FeedTokens.dark` after `readableColors`; the append wins by runtime type." (Corrects an over-statement in the audit's own seed facts.)
- **G2. Name the real nav edit site.** `NavBarTheme` is a static-const class — it can't be "made tone-aware" in place. The tone-blind consumer is **`nav_bar_button.dart`** (active pill/icon/text: `:30-36/:68/:120/:123`); `feed_navigation_bar.dart:42-58` ALREADY flips bar chrome by `isLightSurface`. The edit is: add color params to `NavBarButton` and thread an accent from `feed_navigation_bar` (which builds `NavBarButton` at `:70-84` with no colors). Rename the test to target `nav_bar_button`. Also add nav-CONTAINER visibility: the bar pill (`white .18/.10`) goes ghost on porcelain — tokenize the container gradient/border and assert ≥3:1 vs the `#EDEEF3` ground (not just the active-tab state).
- **G3. Thread the ring GLOW sublayer, not just the two dashes.** `orbital_ring_painter.dart:83` always paints `_tealGlow`. If `orbital_visualization` passes ring1/ring2 but omits the glow color, the ring glow stays teal `0x1481E6D9` (α .08) on light. Low visual impact but an uncovered wiring path — assert the threaded glow in TC-221-13. (Arcs at `:65` already covered by TC-221-15, but note the RED must construct `OrbitalVisualization` in an **overflow-expanded** state — `orbital_visualization.dart:147` — or the arcs aren't painted.)
- **G4. Record the shared-surface facts (why single-file edits suffice).** `compose_area`/`empty_conversation_state` serve BOTH 1:1 AND group chat (`group_conversation_screen.dart` wraps `AmbientBackground`, same widgets) → the composer/empty-state fix covers group too; note it so the reviewer confirms group is intended-covered. Corollary to record: because all ~17 mounts re-inject at the single `ambient_background.dart:135` `build()`, ONE FeedTokens swap reaches every surface — the reason a single-file swap is sufficient. Also verify the chat fix relies on chat being UNDER `AmbientBackground` (it is: `conversation_screen.dart:325`) — the getter fallback is `BackgroundReadableColors.dark`, so a chat route outside AmbientBackground would render DARK in prod while `compose_area_tone_test` (which injects representativeLight directly) passes green.
- **G5. Fix TC-221-04 "repaints within one frame".** If the theme lerps (`BackgroundReadableColors.lerp` / `AnimatedTheme`), a live preset switch spans multiple frames, contradicting "within one frame"; and the matrix maps TC-04 to `dark_preset_preservation_test` which never exercises a live preference CHANGE. Rewrite TC-04 as a real light-repaint test: pump a dark preset, switch the preference to `daylightLagoon`, pump-and-settle, assert the ground is Signal with teal/pink bloom inputs gone (mutation: leave a stale bloom → red).
- **G6. Give durability a real restart.** TC-221-30/31 re-read the same in-memory store in one process; TC-221-35 is a same-derived rebuild. Boot a FRESH `MaterialApp`/`AmbientBackground` from the persisted `daylight_lagoon` key (or force State recreation via `UniqueKey`) so a broken restore path actually reds.

---

## Priority order to apply
1. **§A (node-glow structural fix) + §B (missed broken surfaces)** — now in-scope; these are the user-visible goals the plan currently misses. Author their REDs first.
2. **§C (rename undercount + L159→L408) + §D (non-vacuous preservation) + §E1/E2 (honest legibility gate)** — make the plan's own acceptance gate real (feature-host-all green; dark-identical actually guarded; WCAG measured on the true ground).
3. **§E3 (exact-hex fidelity pins) + §E4 (registration) + §E5 + §C3 (de/ar strings)** — close the fidelity/coverage gaps per the locked decisions.
4. **§F (RED-first hygiene, sentinel timing, step-7 split) + §G (mechanism prose corrections)** — tighten execution discipline; no new test surface, just accuracy.

## Keep these (do not "fix")
- The root-cause diagnosis + pre-refutation of the two tempting-wrong framings (redirects REDs onto the tone-blind globals — verified correct).
- Both mechanism bets (FeedTokens swap, ring parametrization, dark-token read-back) — verified sound; do not re-architect.
- Dark-preservation as a first-class INV with a BLOCKING stop-if — right instinct; §D only adds rigor.
- The WCAG-floor numbers + honest disclosure of the manual TC-41 leg + `(guard)`-labeled matrix.
