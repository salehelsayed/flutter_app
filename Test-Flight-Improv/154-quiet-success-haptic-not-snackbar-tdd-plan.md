# 154 - Quiet success (mute/details/copy) -> haptic + control state, snackbar channel reserved for errors  (Feature Improvement)

Status: IMPLEMENTED host-green (2026-06-23) — see Final Execution Verdict. Prior: accepted (sufficiency review complete; 2 blocking findings B1/B2 fixed inline, 5 non-blocking N1–N5 incorporated)
Spec: free-text intent (no formal spec) — chained from a UI/UX review of Group Messaging snackbars

## Planning Progress
| Time | Role | Files inspected | Decision/blocker | Next action |
|---|---|---|---|---|
| 2026-06-23 | Evidence Collector | group_info_wired.dart (mute :478-537, details :1938-2025), group_conversation_screen.dart (copy :1031-1044, overlay pop :1011), conversation_screen.dart (1:1 copy :879-893), orbit2_screen.dart (:109 haptic precedent), group_list_wired.dart (decline :335-367), app_en/ar/de.arb, run_test_gates.sh, group_conversation_screen_test.dart, group_info_wired_test.dart, conversation_screen_test.dart | verify→refute complete: success snackbars redundant w/ control/field state (mute switch+bell flips, details fields re-render); COPY has NO control state (overlay popped first) → must keep a visible cue; group_info_wired.dart MISSING services.dart import; both conversation screens ALREADY import it; `conversation_context_copied` has exactly 2 lib call sites | Planner |
| 2026-06-23 | Planner | (as above) | mute/details: drop success snackbar + `HapticFeedback.selectionClick()`, lean on control/field. copy: replace floating snackbar with haptic + a tiny non-error quiet-confirm cue (keep `conversation_context_copied`). KEEP all error snackbars (mute_update_failed, details error :2012-2022, decline_failed). NO migration. | Reviewer |
| 2026-06-23 | Reviewer (sufficiency) | REAL source via verify→refute workflow: group_info_wired.dart, conversation_screen.dart, group_conversation_screen.dart, group_info_wired_test.dart, group_conversation_screen_test.dart, conversation_screen_test.dart, run_test_gates.sh, app_en/ar/de.arb, orbit2_screen.dart, AND flutter SDK (services/haptic_feedback.dart, material/scaffold.dart, material/snack_bar.dart, widgets/framework.dart, flutter_test/test_default_binary_messenger.dart) | verdict **needs-fixes** → **B1** registration premise FALSE (`group_info_wired_test.dart` already in `GROUP_TESTS:135`); **B2** haptic assert under-specified (all variants share method `HapticFeedback.vibrate`; must also assert arg `HapticFeedbackType.selectionClick`; one handler per channel). Non-blocking N1 `maybeOf` throws on dead context (not null), N2 sibling success snackbars unnamed, N3 prefer in-place EK004 retarget, N4 ~+96-line drift in group_info_wired_test, N5 two distinct `_copyMessageText` signatures. | Arbiter |
| 2026-06-23 | Arbiter | (as above) | **accepted** — B1/B2 fixed inline (registration → "already curated"; haptic dual-assert + single consolidated handler), N1–N5 incorporated, Blind-Spot Sweep section added. Matrix has zero empty cells; host-only closure, no migration, no device-proof. | hand off to execution |

## Execution Progress
| Time | Phase | Files touched | Command/evidence | Decision/blocker | Next |
|---|---|---|---|---|---|
| 2026-06-23 | contract extraction (git status --short) | — | tree dirty on `new-feed` (149/150/151/153/155 concurrent work co-edits the same conversation/group screens); located every anchor by SYMBOL not line (drift confirmed) | scope confirmed; my 154 files = group_info_wired.dart, group_conversation_screen.dart, conversation_screen.dart, NEW quiet_confirm.dart + 3 test files | RED |
| 2026-06-23 | RED tests added | group_info_wired_test (TC-01/02), group_conversation_screen_test (TC-03), conversation_screen_test (TC-04/05/06) | `flutter test --name …` → EK004 RED "Group details updated found but not expected"; mute RED on haptic-any leg; copy RED `Found 0 widgets key 'quiet-confirm'` | RED for documented reason (snackbar present / no haptic / no key) | implement |
| 2026-06-23 | implementation | NEW lib/core/widgets/quiet_confirm.dart; group_info_wired.dart (+services import, mute+details haptic, drop 2 success snackbars); both copy screens → showQuietConfirm | — | scoped files only; KEPT error snackbars + 1:1 `!mounted` + group `cardContext` | GREEN |
| 2026-06-23 | direct GREEN | 3 test files | full files: group_info_wired +68, group_conversation_screen +62, conversation_screen +71 | reds now green | gates |
| 2026-06-23 | preservation + named gates | — | `run_test_gates.sh`: 1to1 **+1194/0**, feed **+214/0**, groups **+871/-7** | groups -7 = 100% concurrent noise (149 `attachment-total-overflow`, 150 orbit invite, 151 "You were removed" snackbar + 2 plan-named pre-existing media); ZERO `quiet-confirm`/copy in any failure | mutation |
| 2026-06-23 | hygiene | group_info_wired_test.dart (dropped now-redundant `dart:typed_data`) | `flutter analyze` touched files: 0-new (services.dart made test's typed_data import redundant → removed); `git diff --check` clean | 0-new | mutation |
| 2026-06-23 | mutation-verify (self, main loop) | — | M1 re-add mute snackbar→TC-01 red; M2 drop details haptic→TC-02 red; **M3 selectionClick→heavyImpact→TC-03 red on ARG leg (B2 proven)**; M5 drop 1:1 `!mounted`→TC-06 throws/red; M6 drop key→TC-03 red. **M4 drop hideCurrentSnackBar→TC-04 STILL GREEN (plan claim refuted: ScaffoldMessenger QUEUES, identical keyed cues → can't stack; findsOneWidget holds either way)** | 5/6 load-bearing confirmed; M4 documented as plan correction (hideCurrentSnackBar kept for snappy UX, not mutation-locked) | QA |
| 2026-06-23 | QA (independent) | — | post-revert: all TCs green; full 3 files re-run +68/+62/+71; graphify full+arch refreshed | blocking: none | ACCEPTED |

## Source Of Truth
- Spec / intent: inline below (UX review → user-locked design decisions)
- Gate definitions: `scripts/run_test_gates.sh` (script wins over prose)
- Discovery/registration: `scripts/check_reliability_simulation_discovery.sh` (no sim rows in this plan)
- Numbering / index: `Test-Flight-Improv/00-INDEX.md` (this file = 154)

## Session Classification
implementation-ready (host-only closure; no migration, no device-proof; pure presentation)

## Exact Problem Statement
Three **success** actions in Group Messaging (and the parity 1:1 copy) confirm themselves with a transient ~4s **snackbar** even though the surface already shows the result, OR the snackbar is the only cue when it doesn't need to be a full error-styled banner. This dilutes the snackbar channel: when a real **error** snackbar (mute failed, details broadcast failed, decline failed) appears, the user has been trained to ignore it because "snackbars just mean it worked."

Specifically:
- **MUTE / RESTORE** (`group_info_wired.dart:478-509`): `setGroupMuted` (`:485`) returns, then `setState(_group = updatedGroup)` (`:495-498`) **flips the Switch + bell icon + description** immediately, and only THEN a snackbar `group_info_notifications_muted` / `group_info_notifications_restored` (`:503/:506`) appears. The control already reflects the new state — the snackbar is redundant.
- **DETAILS updated** (`group_info_wired.dart:1938-1947`): `_loadGroupInfo()` (`:1939`) **re-renders the name/description fields** to the new values, then a snackbar `group_info_details_updated` (`:1944`) appears. The field re-render is the surviving confirmation; the snackbar is redundant.
- **COPY (group)** (`_copyMessageText` `group_conversation_screen.dart:1055-1068`): `Clipboard.setData` (`:1056`) + a floating snackbar `conversation_context_copied` (key `:1063`, `SnackBarBehavior.floating` `:1065`) via `ScaffoldMessenger.maybeOf(context)` (`:1057`). The long-press overlay is **popped (`Navigator.of(dialogContext).pop()` `:1035`) BEFORE `_copyMessageText`** runs (called with the **live `cardContext`** at `:1036`, NOT the popped `dialogContext`), so by the time copy completes there is **no persistent visible control** — the snackbar is the only cue. **1:1 copy is the same pattern** (`conversation_screen.dart:915-929`, key `:924`, `maybeOf(context)` `:918`) **except** the 1:1 `_copyMessageText(String text)` takes no `BuildContext` (uses the State's `this.context` behind an `if (!mounted) return;` guard at `:917`); the group `_copyMessageText(BuildContext context, String text)` is passed `cardContext`. HAZARD: dropping the cue entirely would make copy **invisible**; it must keep a small, visually-distinct (non-error) confirmation. (Both call sites already resolve a **live** messenger today — the null-messenger trap only materializes if a careless refactor passes the popped `dialogContext` or drops the 1:1 `!mounted` guard; see N1/N5 below.)

What must improve:
- Mute/restore and details-updated: **drop the success snackbar**; add a tactile `HapticFeedback.selectionClick()` and rely on the already-updated control / field state. (`group_info_wired.dart` must **add** `import 'package:flutter/services.dart';` — it does not currently import it.)
- Copy (group + 1:1): replace the full floating snackbar with `HapticFeedback.selectionClick()` **plus** a minimal, visually-distinct **quiet-confirm** cue (a short success-styled pill / brief auto-dismiss confirmation, NOT the standard error snackbar). Keep `conversation_context_copied` for the cue text.

What must stay unchanged (→ preserved-green sentinels):
- **Error snackbars stay**: `group_info_mute_update_failed` (`:527`), the details failure/rollback path (`:2012-2022`, `detailsUpdateFailedMessage` / `detailsUpdateQueuedMessage` / recovery-wait), and `group_invite_decline_failed` (`group_list_wired.dart`, key `:1311`).
- The **DECLINE success** snackbar (`group_list_wired.dart:525-526`) is **owned by plan 153 (Undo)** — do NOT touch it here.
- Clipboard text fidelity (multiline / mixed-script / Arabic) and the overlay-dismiss-once behavior of the long-press menu.
- LetterCard / feed / 1:1 / group render suites; `flutter analyze` 0-new.

## Root Cause (verify → refute confirmed)
The redundancy is **committed on HEAD** (tree dirty on `new-feed` for unrelated Feed/Orbit work; these specific lines are not part of that churn):
- Mute success snackbar fires AFTER the control already flipped: `group_info_wired.dart:495-498` flips state (`setState` only — mute success does **not** call `_loadGroupInfo()`; it relies on the inline `setState(_group = updatedGroup)`), `:499-509` shows the snackbar (`group_info_notifications_muted` `:503` / `_restored` `:506` ternary). Post-await, guarded by `if (!mounted) return;` at `:491-493`.
- Details success snackbar fires AFTER `_loadGroupInfo()` re-renders fields: `group_info_wired.dart:1939` re-renders, `:1940` `if (!mounted) return;`, `:1941-1947` shows the snackbar (`group_info_details_updated` `:1944`). (Asymmetry vs mute: details success reloads via `_loadGroupInfo()` BEFORE the snackbar; mute success uses only the inline `setState`. Both are confirmation-bearing — the haptic must sit at each guarded post-await point.)
- Copy snackbar is the SOLE persistent cue because the overlay is popped first: `group_conversation_screen.dart:1035` pops `dialogContext`, then `_copyMessageText(cardContext, …)` `:1055-1068` copies + shows the snackbar via the live `cardContext` (1:1: `conversation_screen.dart:915-929`, via `this.context` behind `!mounted` `:917`).
- `group_info_wired.dart` does **not** import `package:flutter/services.dart` (confirmed: grep returns no match) → `HapticFeedback` is unavailable until the import is added.
- HapticFeedback precedent exists in-tree: `lib/features/orbit2/presentation/screens/orbit2_screen.dart:109` `HapticFeedback.selectionClick(); // tactile confirmation the toggle fired`.

Refuted / do-NOT-re-introduce:
- ❌ "copy can be haptic-only like mute/details" — **FALSE**; copy has **no persistent control** (overlay popped at `:1035` before `_copyMessageText`). Haptic-only would make copy silent → MUST keep a small visible cue.
- ❌ "drop `conversation_context_copied` because the snackbar goes away" — **FALSE**; the key is reused by the new quiet-confirm cue. It has **exactly two** lib call sites (`group_conversation_screen.dart:1063`, `conversation_screen.dart:924`); only remove the key if BOTH sites stop using it (they don't).
- ❌ "both conversation screens need the services.dart import added" — **FALSE**; both already import it (`conversation_screen.dart:5`, `group_conversation_screen.dart:5`). Only `group_info_wired.dart` needs the import.
- ❌ "the decline success snackbar is in scope" — **FALSE**; it is owned by plan 153 (Undo). Out of scope here.
- ❌ "remove the mute/details error snackbars too" — **FALSE**; errors are exactly what the reserved channel is FOR. Keep `group_info_mute_update_failed`, the details error/rollback path, `group_invite_decline_failed`.
- ❌ "`group_info_wired_test.dart` must be ADDED to `GROUP_TESTS`" — **FALSE** (corrected by review B1); it is **already curated** at `scripts/run_test_gates.sh:135` (next to `group_list_wired_test.dart:134`). Adding it again creates a duplicate array entry. No registration step needed.
- ❌ "`ScaffoldMessenger.maybeOf(context)` returns `null` (silent no-op) on an unmounted/popped context" — **FALSE** (corrected by review N1); on a *deactivated* element `maybeOf` **throws** in debug (the framework asserts `_debugCheckStateIsActiveForAncestorLookup`, `framework.dart:5046-5089`). `null` is returned **only** when the lookup runs on an *active* element that has no `ScaffoldMessenger` ancestor. `showQuietConfirm` must guard the **lifecycle** (`mounted`/`context.mounted`) BEFORE the lookup, not rely on a `null` return.
- ❌ "asserting `call.method == 'HapticFeedback.vibrate'` alone pins `selectionClick()`" — **FALSE** (corrected by review B2); **every** `HapticFeedback` variant (lightImpact/mediumImpact/heavyImpact/selectionClick/vibrate/successNotification…) sends the same method string `HapticFeedback.vibrate` (`services/haptic_feedback.dart:90-95`). `selectionClick()` is distinguished **only** by its positional argument `'HapticFeedbackType.selectionClick'`. Tests must assert BOTH `call.method` AND `call.arguments`.
- ❌ HapticFeedback precedent only at `friends_list_header.dart` — **wrong/old path**; cite `orbit2_screen.dart:109` `HapticFeedback.selectionClick(); // tactile confirmation the toggle fired` (canonical, non-awaited). NOTE: `contact_profile_screen.dart:92` is ALSO a real precedent (awaited variant), and `orbit3_screen.dart` uses it 8×; the orbit2 line is the closest match (fire-and-forget selectionClick).

## Real Scope
In scope:
- `group_info_wired.dart`: **add** `import 'package:flutter/services.dart';` (after `package:flutter/material.dart` `:7`). In `_onMuteChanged` (`:478-537`): delete the success snackbar (`:499-509`); after the `setState` flip (`:495-498`, already behind the `!mounted` guard `:491-493`) call `HapticFeedback.selectionClick()`. In the details-save success branch: delete the success snackbar (`:1941-1947`); after `await _loadGroupInfo()` (`:1939`) and the `!mounted` guard (`:1940`) call `HapticFeedback.selectionClick()`. **Keep** the catch/error snackbars (mute `:524-530`, details `:2012-2023`).
- COPY shared quiet-confirm: build a **tiny shared helper** `showQuietConfirm(BuildContext context, String message)` (new file `lib/core/widgets/quiet_confirm.dart`) that (a) **first** checks `context.mounted` (lifecycle guard — see N1: `ScaffoldMessenger.maybeOf` THROWS on a deactivated context, it does NOT return null), then (b) calls `HapticFeedback.selectionClick()`, then (c) resolves `ScaffoldMessenger.maybeOf(context)` (null-check the *result* for "active element, no messenger ancestor") and shows a small, success-styled, auto-dismiss confirmation **visually distinct** from an error snackbar — a short SnackBar with `key: const ValueKey('quiet-confirm')` + reduced `duration` (~1.2s) + `behavior: SnackBarBehavior.floating` + non-error styling (so tests assert the quiet-confirm key, NOT a generic error SnackBar). The user-supplied `key` is preserved on the inserted SnackBar (`snack_bar.dart:516-518` only uses a fallback key when `key == null`), so `find.byKey(const ValueKey('quiet-confirm'))` locates it after one `pump`. (No reusable pill/toast widget exists — `Orbit*Pill` are buttons/search inputs, not toasts.)
- `group_conversation_screen.dart` `_copyMessageText(BuildContext context, String text)` (`:1055-1068`): replace the `ScaffoldMessenger.maybeOf(context)..showSnackBar(...)` body (`:1057-1066`) with `showQuietConfirm(context, AppLocalizations.of(context)!.conversation_context_copied)` — **keep passing the live `cardContext`** the call site already uses (`:1036`), never `dialogContext`. `Clipboard.setData` (`:1056`) stays first. services.dart already imported (`:5`).
- `conversation_screen.dart` `_copyMessageText(String text)` (`:915-929`): same replacement, **keeping the `if (!mounted) return;` guard (`:917`)** and using the State's `this.context`. services.dart already imported (`:5`).
- Tests: rewrite the snackbar-locking assertions (see RED catalog). **CONSOLIDATE** the `SystemChannels.platform` mock into a SINGLE handler that `switch`es on `call.method` and records a **list** of calls (a second `setMockMethodCallHandler` on the same channel OVERWRITES the first — there is one handler slot per channel). The handler must capture `HapticFeedback.vibrate` (returning `null` is safe — `selectionClick` awaits `invokeMethod<void>`) AND keep returning `null` for `Clipboard.setData`. Assert the haptic with BOTH `call.method == 'HapticFeedback.vibrate'` AND `call.arguments == 'HapticFeedbackType.selectionClick'` (the arg is the raw String, not a Map) — method-string-only would let a wrong-haptic mutation escape (B2).
- Harness: **NO registration step** — `group_info_wired_test.dart` is **already curated** in `GROUP_TESTS` at `scripts/run_test_gates.sh:135` (verified; the earlier "not curated" claim was FALSE — adding it again duplicates the entry). `group_conversation_screen_test.dart` (`GROUP_TESTS:132`) and `conversation_screen_test.dart` (`ONE_TO_ONE_TESTS:62`) are also already curated. All three rewritten tests run under existing gates with zero array edits.
- l10n: NO new keys. Keep `conversation_context_copied` (still used by the pill). Note that `group_info_notifications_muted` / `_restored` / `group_info_details_updated` become **unused** by production — leave them in the arb files for now (removal is a no-op cleanup that risks unrelated l10n churn; flag, don't delete).

Out of scope (owning work named):
- DECLINE success snackbar / Undo affordance — **plan 153**. NOTE the 153 decline machinery spans BOTH `group_list_wired.dart` (optimistic+Undo `:624`, success `:723`, both key `group_invite_declined`) AND `orbit_wired.dart:1447` (`group_invite_decline_failed` mirror); leave all of it untouched.
- **Other `group_info_wired.dart` SUCCESS snackbars that match the same "surface already re-rendered" pattern are intentionally DEFERRED, not converted here** (named so the channel-reservation goal isn't silently half-applied): `group_dissolved` (`:617`), role-change `admin_added`/`admin_removed` (`~:2083`, the member list re-renders the role), resend-invite success (`~:2109`), revoke-invite success (`~:2240`). They follow the identical `await _loadGroupInfo()` → success-snackbar shape and are candidates for the same haptic+control treatment in a follow-up; converting them is out of THIS plan's locked scope (mute/details/copy only). Their ERROR snackbars (rotate-key-failed, role-update-failed, dissolve-recovery) stay regardless — errors are what the channel is FOR.
- Removing the now-unused `group_info_notifications_*` / `group_info_details_updated` arb keys (and their generated getters) — a separate l10n-cleanup pass; deleting them would touch `app_localizations*.dart` generated files unnecessarily.
- Any redesign of the long-press context menu, the mute use-case, or the details-broadcast pipeline.
- A bespoke animated toast/pill widget — the minimal distinct SnackBar is the agreed cue.

## Files To Inspect Next
> **Line numbers below are approximate on the dirty `new-feed` tree** (verified 2026-06-23; several drifted — see N4). **Execution MUST locate by test NAME / symbol, not by line.** The worst trap: plan-era mute line `:2854` lands inside an UNRELATED dissolve test — a blind line-anchored edit there corrupts the B4-dissolve-audit test. All anchors here are the *current verified* values.

Production:
- `lib/features/groups/presentation/screens/group_info_wired.dart` — imports `:1-7` (flutter `package:flutter/material.dart` `:7`; **ADD `package:flutter/services.dart` after `:7`** — currently NO services import, grep-confirmed), `_onMuteChanged` `:478-537` (success snackbar `:499-509`, `!mounted` guard `:491-493`, error snackbar `:524-530`), details success `:1939` (`_loadGroupInfo`) → `:1940` (`!mounted`) → `:1941-1947` (success snackbar), details error path `:2012-2023`. All snackbars use INLINE `ScaffoldMessenger.of(context)` (no captured messenger).
- `lib/features/groups/presentation/screens/group_conversation_screen.dart` — overlay pop `:1035` (`Navigator.of(dialogContext).pop()`), call site passes `cardContext` `:1036`, `_copyMessageText(BuildContext context, String text)` `:1055-1068` (Clipboard `:1056`, `maybeOf` `:1057`, key `:1063`, floating `:1065`), services import `:5`.
- `lib/features/conversation/presentation/screens/conversation_screen.dart` — `_copyMessageText(String text)` `:915-929` (`!mounted` guard `:917`, Clipboard `:916`, `maybeOf(this.context)` `:918`, key `:924`, floating `:926`), services import `:5`.
- `lib/core/widgets/quiet_confirm.dart` — NEW shared helper (lifecycle-guarded haptic + distinct quiet-confirm cue).
- `lib/features/orbit2/presentation/screens/orbit2_screen.dart:109` — HapticFeedback precedent (reference only); also `contact_profile_screen.dart:92` (awaited) and `orbit3_screen.dart` (8×).
Direct tests (locate by NAME — lines drifted):
- `test/features/groups/presentation/group_info_wired_test.dart` — mute test `'toggles mute state and persists it to the repository'` `:2950` (switch isFalse `:2972-2977` → isTrue `:2982-2987`, repo `isMuted` `:2988`, snackbar assert `:2989`); EK004 test `'EK004 PREREQ-SIGNED-COMMIT-AUDIT admin metadata edit …'` `:3723` (field re-render `:3812-3813`, success snackbar assert `:3814`, **load-bearing commandLog/sign-order asserts `:3816-3823`, signed-audit asserts `:3836-3873` — must stay verbatim**); error/rollback `'Group details updated' findsNothing` at `:3322/:3363/:3412/:3466`.
- `test/features/groups/presentation/group_conversation_screen_test.dart` — copy test `'long-press copy action copies exact text and dismisses once'` `:540` (mock `:546-558` filters only `Clipboard.setData`, returns `null` otherwise; snackbar asserts `:576-577`).
- `test/features/conversation/presentation/screens/conversation_screen_test.dart` — copy test `:1331` (mock `:1337-1339` via `TestDefaultBinaryMessengerBinding.instance.defaultBinaryMessenger`, asserts `:1375-1389`), Arabic copy `:1393` (mock `:1398-1400`, asserts `:1429-1431`), findsNothing sentinels `:872` (backdrop-dismiss — NO custom mock) & `:1508` (dispose-during-await, `takeException isNull` `:1509`).
Dependency-only context:
- `lib/features/groups/application/set_group_muted_use_case.dart` (`setGroupMuted` — not edited).
- `lib/l10n/app_en.arb` / `app_ar.arb` / `app_de.arb` — `conversation_context_copied` at `:223` each (EN "Message copied to clipboard", AR "تم نسخ الرسالة إلى الحافظة", DE "Nachricht in die Zwischenablage kopiert" — reused, not edited); the orphaned-after-fix keys: `group_info_details_updated` (EN `:576`), `group_info_notifications_muted` (EN `:784`), `_restored` (EN `:785`) — left in place (deferred cleanup).

## Existing Tests Covering This Area
- `group_info_wired_test.dart:2950` "toggles mute state and persists it to the repository" — asserts switch isFalse `:2972-2977` → isTrue `:2982-2987`, repo persists `:2988` (`isMuted isTrue`), AND snackbar `:2989` `expect(find.text('Notifications muted for this group'), findsOneWidget)`. PASSES on HEAD. **`:2989` locks OLD behavior → must be rewritten** (drop snackbar assert; assert haptic fired w/ method+arg; keep switch+repo asserts).
- `group_info_wired_test.dart:3723` "EK004 PREREQ-SIGNED-COMMIT-AUDIT admin metadata edit …" — asserts fields re-render (`:3812-3813`) AND snackbar `:3814` `expect(find.text('Group details updated'), findsOneWidget)`. PASSES on HEAD. **`:3814` ALONE locks OLD UI behavior → retarget IN PLACE** (drop/flip the snackbar assert; assert haptic; keep field-render asserts). **Do NOT touch the load-bearing crypto/audit asserts `:3816-3823` (commandLog `group:publish`/`group:inboxStore` + `sign`-index ordering) and `:3836-3873` (signed sys-text decode, `signedGroupTransitionAuditField`, privacy "no private key" assert) — they are separable from `:3814`.** NOTE: `:3322/:3363/:3412/:3466` already assert `'Group details updated' findsNothing` (error/rollback paths) — those stay green unchanged.
- `group_conversation_screen_test.dart:540` "long-press copy action copies exact text and dismisses once" — mock filters only `Clipboard.setData` (`:549`); asserts clipboard fired `:573-574`, overlay gone `:575`, AND `find.byType(SnackBar) findsOneWidget` `:576` + `find.text('Message copied to clipboard') findsOneWidget` `:577`. PASSES. **`:576-577` lock OLD behavior → rewrite** (assert quiet-confirm key + haptic; assert NO generic error SnackBar).
- `conversation_screen_test.dart:1331` "copy action copies exact multiline text, replaces the prior snackbar, and dismisses the overlay" — asserts `find.byType(SnackBar) findsOneWidget` `:1376/:1388` + `'Message copied to clipboard'` `:1377/:1389`. PASSES. **Rewrite** to the quiet-confirm cue + haptic.
- `conversation_screen_test.dart:1393` "copy action localizes the snackbar in Arabic …" — asserts `find.byType(SnackBar)` `:1430` + Arabic text `:1431`. PASSES. **Rewrite** to assert the quiet-confirm cue carries the localized text (keep clipboard-fidelity assert `:1429`).
- `conversation_screen_test.dart:872`/`:1508` "…findsNothing" cases (copy unavailable / disposed) assert `'Message copied to clipboard' findsNothing` — those **stay green** (no quiet-confirm should appear there either).

Missing coverage gaps: NO test asserts a haptic on mute/details/copy (the SystemChannels.platform mock currently filters only `Clipboard.setData`); NO test asserts the **absence** of the success snackbar for mute/details; NO test asserts copy uses a **distinct** cue (not a generic error SnackBar).

Already in curated family arrays? (corrected by review B1 — ALL THREE already curated; `GROUP_TESTS` spans `run_test_gates.sh:118-138`):
- `group_info_wired_test.dart` ✅ **already** `GROUP_TESTS` (`:135`, next to `group_list_wired_test.dart:134`). **No registration step** — the earlier "NOT in GROUP_TESTS" claim was FALSE; adding it would duplicate the entry.
- `group_conversation_screen_test.dart` ✅ `GROUP_TESTS` (`:132`).
- `conversation_screen_test.dart` ✅ `ONE_TO_ONE_TESTS` (`:62`).
- `letter_card_test.dart` ✅ `GROUP_TESTS` (`:131`) AND `ONE_TO_ONE_TESTS` (`:61`) — shared sentinel, unaffected here.

## RED Test Catalog  (add/rewrite BEFORE any production code — INV-RED-FIRST)

1. `group_info_wired_test.dart`::"toggles mute state and persists it to the repository"  *(rewrite of the test at `:2950`, snackbar assert `:2989` — locate by NAME, not line)*
   - Tier: integration/widget (wired screen, in-memory fakes)
   - Shape/setup: pump GroupInfoWired with an unmuted group; install a SINGLE `SystemChannels.platform` mock handler (via `TestDefaultBinaryMessengerBinding.instance.defaultBinaryMessenger.setMockMethodCallHandler`, the in-file pattern) that `switch`es on `call.method` and appends every call to a `List<MethodCall>` (one handler per channel — a 2nd install overwrites it); tap `ValueKey('group-mute-switch')`; `pumpFrames`/`pump`.
   - RED on HEAD because: HEAD shows the `group_info_notifications_muted` snackbar (`:503`) and fires **no** haptic. New asserts (switch `isTrue` `:2987` + repo muted `:2988` + **haptic captured** + **no** `find.text('Notifications muted for this group')`) fail on both the snackbar-present and haptic-absent legs.
   - GREEN after fix asserts: switch flips `isTrue`; `groupRepo.getGroup().isMuted == true`; the captured calls contain one with `call.method == 'HapticFeedback.vibrate'` **AND** `call.arguments == 'HapticFeedbackType.selectionClick'` (B2 — method-string alone is shared by every variant); `find.text('Notifications muted for this group') findsNothing`.
   - Mutation that re-reds: (a) re-add the mute success snackbar (`showSnackBar(… group_info_notifications_muted …)`) → snackbar-absent leg reds; (b) delete the `HapticFeedback.selectionClick()` call → haptic-captured leg reds; (c) **wrong-haptic** swap `selectionClick()` → `heavyImpact()` → the `arguments == 'HapticFeedbackType.selectionClick'` leg reds (this is what the method-only assert would miss).
   - Discriminator: assert haptic captured (method+arg) AND success-snackbar text absent (each leg independently catches a half-fix; the arg leg catches a wrong-haptic).

2. `group_info_wired_test.dart`::"EK004 PREREQ-SIGNED-COMMIT-AUDIT admin metadata edit …"  *(retarget the snackbar assert at `:3814` IN PLACE inside EK004 `:3723` — N3: do NOT split into a new focused case; splitting duplicates the signed-commit/audit setup. Keep the field-render + commandLog + signed-audit asserts verbatim)*
   - Tier: integration/widget
   - Shape/setup: the existing EK004 drives the edit-details save (rename + description); EXTEND its existing platform mock to also capture haptics into the call list; `pumpFrames`.
   - RED on HEAD because: HEAD shows `group_info_details_updated` snackbar (`:1944`) and fires no haptic; new asserts (fields show new values `:3812-3813` + **haptic captured (method+arg)** + **no** `find.text('Group details updated')` flipping `:3814` from `findsOneWidget`→`findsNothing`) fail.
   - GREEN asserts: `find.text('Renamed Group') findsWidgets` + `find.text('Fresh description') findsOneWidget`; captured calls contain `method == 'HapticFeedback.vibrate'` AND `arguments == 'HapticFeedbackType.selectionClick'`; `find.text('Group details updated') findsNothing`; **untouched/still-green**: commandLog `group:publish`/`group:inboxStore` + `sign`-index ordering (`:3816-3823`) and the signed sys-text/audit/privacy asserts (`:3836-3873`).
   - Mutation that re-reds: (a) re-add the details success snackbar → snackbar-absent leg reds; (b) remove the haptic call → haptic leg reds; (c) wrong-haptic swap → the `arguments` leg reds.
   - Discriminator: keep the error-path `findsNothing` asserts (`:3322/:3363/:3412/:3466`) green — proves we only removed the SUCCESS snackbar, not the error ones; and the crypto/audit asserts staying green prove the retarget did not weaken the signed-commit invariant.

3. `group_conversation_screen_test.dart`::"long-press copy action copies exact text and dismisses once"  *(rewrite of `:540`, asserts `:576-577`)*
   - Tier: widget
   - Shape/setup: EXTEND the EXISTING single platform mock (`:546-558`, currently gates on `Clipboard.setData` and `return null` otherwise) to ALSO record `HapticFeedback.vibrate` into a call list — keep it ONE handler switching on `call.method` (a 2nd `setMockMethodCallHandler` overwrites it); long-press, tap copy; `pump` (NOT `pumpAndSettle` — the floating SnackBar's ~timeout never settles).
   - RED on HEAD because: HEAD shows the floating snackbar via `maybeOf(context)..showSnackBar(...)` (`group_conversation_screen.dart:1057-1066`) and fires no haptic; new asserts (clipboard fidelity kept `:573-574` + overlay gone `:575` + **quiet-confirm** `find.byKey(const ValueKey('quiet-confirm')) findsOneWidget` carrying `'Message copied to clipboard'` + **haptic captured (method+arg)**) fail.
   - GREEN asserts: `clipboardCalls == 1`; `copiedText == copiedMessage`; overlay `findsNothing`; `find.byKey(const ValueKey('quiet-confirm')) findsOneWidget`; the quiet-confirm text resolves `conversation_context_copied`; captured calls contain `method == 'HapticFeedback.vibrate'` AND `arguments == 'HapticFeedbackType.selectionClick'`.
   - Mutation that re-reds: (a) revert `_copyMessageText` to the plain `showSnackBar(...)` (drop `showQuietConfirm`) → quiet-confirm key absent + no haptic → red; (b) wrong-haptic swap inside `showQuietConfirm` → the `arguments` leg reds.
   - Distinct-event discriminator: assert the quiet-confirm KEY (`ValueKey('quiet-confirm')`) is present — distinguishes the reserved error-snackbar channel from the success cue (a half-fix that left a keyless error-styled SnackBar would fail).

4. `conversation_screen_test.dart`::"copy action copies exact multiline text, replaces the prior snackbar, and dismisses the overlay"  *(rewrite of `:1331`, asserts `:1375-1389`)*
   - Tier: widget
   - Shape/setup: extend the existing single platform mock (`:1337-1347`) to also record `HapticFeedback.vibrate` into a call list (switch on `call.method`, one handler); two-copy sequence (`copy-1`, then `copy-2`); `pump` not `pumpAndSettle`.
   - RED on HEAD because: HEAD asserts a generic keyless floating SnackBar with the copied text (`:1376/:1388` + `:1377/:1389`); new asserts (clipboard fidelity `:1385-1386` + overlay gone `:1387` + quiet-confirm key present + prior replaced + haptic captured method+arg) fail because HEAD fires no haptic and uses a keyless snackbar.
   - GREEN asserts: `clipboardCalls == 2`; `copiedText == copiedMessage`; overlay `findsNothing`; `find.byKey(const ValueKey('quiet-confirm')) findsOneWidget` (single — the 2nd copy's `hideCurrentSnackBar()` inside `showQuietConfirm` replaces the 1st, preserving the "replaces the prior" assertion); captured calls contain `method == 'HapticFeedback.vibrate'` AND `arguments == 'HapticFeedbackType.selectionClick'`.
   - Mutation that re-reds: (a) revert 1:1 `_copyMessageText` to the plain snackbar → quiet-confirm key absent + no haptic → red; (b) drop `hideCurrentSnackBar()` from `showQuietConfirm` → two quiet-confirms stack → `findsOneWidget` reds; (c) wrong-haptic swap → `arguments` leg reds.

5. `conversation_screen_test.dart`::"copy action localizes the quiet confirm in Arabic while preserving mixed-script clipboard text"  *(rewrite of `:1393`, asserts `:1430-1431`)*
   - Tier: widget
   - Shape/setup: locale `ar`; single platform-mock handler (`~:1398-1400`) captures clipboard + haptic into a call list (switch on `call.method`); long-press + copy; `pump`.
   - RED on HEAD because: HEAD asserts a generic SnackBar `:1430` with the Arabic text `:1431`; new asserts (clipboard fidelity kept `:1429` + quiet-confirm key carrying `'تم نسخ الرسالة إلى الحافظة'` + haptic captured method+arg) fail (no haptic, keyless snackbar).
   - GREEN asserts: `copiedText == copiedMessage`; `find.byKey(const ValueKey('quiet-confirm')) findsOneWidget`; the quiet-confirm shows `'تم نسخ الرسالة إلى الحافظة'`; captured calls contain `method == 'HapticFeedback.vibrate'` AND `arguments == 'HapticFeedbackType.selectionClick'`.
   - Mutation that re-reds: (a) revert 1:1 copy cue → red; (b) wrong-haptic swap → `arguments` leg reds.

6. `conversation_screen_test.dart`::"copy unavailable / disposed shows neither snackbar nor quiet-confirm"  *(preservation sentinel — existing `:872` / `:1508` `findsNothing` cases extended)*
   - Tier: widget
   - Asserts: in the copy-unavailable (`:872`, backdrop-dismiss, no copy fires — NOTE this test installs NO custom platform mock; that's fine since no haptic/clipboard call is made) and disposed-during-await (`:1508`, with `takeException isNull` `:1509`) cases, `find.text('Message copied to clipboard') findsNothing` AND `find.byKey(const ValueKey('quiet-confirm')) findsNothing`. No change expected; guards against the quiet-confirm leaking into the no-op/disposed paths.
   - Mutation that re-reds: drop the `mounted`/`context.mounted` lifecycle guard from `showQuietConfirm` → on the disposed path the screen is unmounted, so `ScaffoldMessenger.maybeOf(deactivated context)` **throws** (N1: `maybeOf` asserts on a deactivated element, it does NOT return null) → the existing `takeException isNull` assert (`:1509`) flips RED. (The mutation works; the failure mode is a throw, not a leaked cue — which is exactly why the guard must check lifecycle, not the `maybeOf` result.)

## Test Coverage Matrix  (zero empty cells)
| Spec case | Behavior props | Tier | Test file::name | RED reason on HEAD | Mutation revert | Acceptance gate cmd | Harness registration |
|---|---|---|---|---|---|---|---|
| TC-01 mute → haptic + control, no snackbar | screen state + haptic + snackbar-absent | integration/widget | group_info_wired_test::"toggles mute state and persists it to the repository" | HEAD shows `group_info_notifications_muted` snackbar + no haptic | (a) re-add mute success snackbar; (b) drop `HapticFeedback.selectionClick()`; (c) **wrong-haptic** `selectionClick→heavyImpact` (caught by `arguments` assert) | `./scripts/run_test_gates.sh groups` | **AUTO — already in `GROUP_TESTS:135`** (no add) |
| TC-02 details → haptic + field render, no snackbar | screen render + haptic + snackbar-absent | integration/widget | group_info_wired_test::"EK004 PREREQ-SIGNED-COMMIT-AUDIT admin metadata edit …" (retarget `:3814` in place) | HEAD shows `group_info_details_updated` snackbar + no haptic | (a) re-add details success snackbar; (b) drop haptic; (c) wrong-haptic | `./scripts/run_test_gates.sh groups` | **AUTO — already in `GROUP_TESTS:135`** (no add) |
| TC-03 group copy → haptic + quiet-confirm, no error snackbar | widget render + haptic + distinct cue | widget | group_conversation_screen_test::"long-press copy action copies exact text and dismisses once" | HEAD shows floating `conversation_context_copied` snackbar (keyless) + no haptic | (a) revert `_copyMessageText` to plain `showSnackBar`; (b) wrong-haptic | `./scripts/run_test_gates.sh groups` | AUTO (already in `GROUP_TESTS:132`) |
| TC-04 1:1 copy → haptic + quiet-confirm | widget render + haptic + replace-prior | widget | conversation_screen_test::"copy action copies exact multiline text, replaces the prior snackbar, and dismisses the overlay" | HEAD shows keyless floating snackbar + no haptic | (a) revert 1:1 `_copyMessageText` to plain snackbar; (b) drop `hideCurrentSnackBar()`; (c) wrong-haptic | `./scripts/run_test_gates.sh 1to1` | AUTO (already in `ONE_TO_ONE_TESTS:62`) |
| TC-05 1:1 copy Arabic localized cue | l10n + cue + clipboard fidelity | widget | conversation_screen_test::"copy action localizes the quiet confirm in Arabic while preserving mixed-script clipboard text" | HEAD shows keyless snackbar w/ Arabic text + no haptic | (a) revert 1:1 copy cue; (b) wrong-haptic | `./scripts/run_test_gates.sh 1to1` | AUTO (`ONE_TO_ONE_TESTS:62`) |
| TC-06 copy no-op/disposed shows no cue | guard / negative | widget | conversation_screen_test::"copy unavailable / disposed shows neither snackbar nor quiet-confirm" | n/a pre-feature; mutation-verifiable | drop the `mounted`/`context.mounted` lifecycle guard in `showQuietConfirm` → `maybeOf` THROWS on disposed path → `takeException isNull` (`:1509`) reds | `./scripts/run_test_gates.sh 1to1` | AUTO (`ONE_TO_ONE_TESTS:62`) |

## Blind-Spot Sweep  (evergreen classes the spec cases above don't name — row added OR justified N/A)
- **Lifecycle / derived-state durability** — **JUSTIFIED N/A.** All three cues are transient by design (haptic = fire-and-forget method-channel call; quiet-confirm = auto-dismiss SnackBar). No new in-memory state is derived from an event. The durable results persist independently and are already asserted: mute writes through `setGroupMuted` to the repo (TC-01 asserts `groupRepo.getGroup().isMuted` — reopen-survival), details persist via broadcast/`_loadGroupInfo` (TC-02 + the EK004 signed-commit asserts), copy writes to the OS clipboard (TC-03/04/05 clipboard-fidelity). A reopen simply does not re-show the transient cue — which is correct. (Confirmed asymmetry: mute success uses `setState` only, details success calls `_loadGroupInfo()` first — no durability risk because switch state and repo are independently asserted.)
- **Sibling-surface consistency** — **ROW NOT NEEDED for the locked scope; deferral NAMED (non-blocking).** The plan's principle ("surface already re-rendered → success snackbar redundant") applies identically to OTHER `group_info_wired.dart` success snackbars that are deliberately NOT converted here: `group_dissolved` (`:617`), role-change `admin_added`/`admin_removed` (`~:2083`), resend-invite (`~:2109`), revoke-invite (`~:2240`). These are named as **intentionally deferred** in *Out of scope* so the channel-reservation goal isn't silently half-applied. The asymmetry the plan DOES introduce (mute/details = haptic-only; copy keeps a visible cue) is deliberate (copy has no persistent control because the overlay is popped) AND test-locked (TC-01/02 assert snackbar-absent; TC-03/04/05 assert the quiet-confirm cue present).
- **Destructive-action side-effects** — **JUSTIFIED N/A.** No delete / file-cleanup / key-rotation / DB mutation. The diff removes two `showSnackBar` calls, adds two guarded `HapticFeedback.selectionClick()` calls + one `services.dart` import, creates `quiet_confirm.dart`, and swaps two copy-snackbar bodies. No migration (no schema touch). The three orphaned l10n keys are intentionally LEFT in the arb files (no consumer ≠ analyze failure) — no generated-getter churn.
- **Invariant re-verification under new transitions** — **JUSTIFIED N/A.** No new self-heal / reset / latch / state-transition — pure presentation. The one pre-existing invariant at risk (channel-reservation: error snackbars stay) is **preserved, not transitioned**, and is held green by the untouched error-path asserts (`group_info_wired_test.dart:3322/:3363/:3412/:3466` + `mute_update_failed` / `decline_failed` left in place). The EK004/TC-02 in-place retarget does NOT weaken the signed-commit/audit invariant — the `:3814` UI assert is separable from the load-bearing `:3816-3823` (commandLog/sign-order) and `:3836-3873` (signed-audit/privacy) asserts, which stay verbatim → re-verified green by TC-02's discriminator.

## Invariants (locked by tests)
- INV-1: muting/unmuting fires `HapticFeedback.selectionClick()` (asserted by method `HapticFeedback.vibrate` **AND** arg `HapticFeedbackType.selectionClick`) and shows **no** `group_info_notifications_muted/_restored` snackbar; the switch+repo state still flips → TC-01.
- INV-2: a successful details edit fires the haptic (method `HapticFeedback.vibrate` **AND** arg `HapticFeedbackType.selectionClick`) and shows **no** `group_info_details_updated` snackbar; the name/description fields still re-render AND the signed-commit/audit asserts stay green → TC-02.
- INV-3: copy (group + 1:1) fires the haptic (method **AND** arg) and shows a **distinct quiet-confirm cue** (`ValueKey('quiet-confirm')`) carrying `conversation_context_copied`, **not** a generic error-style SnackBar → TC-03/04/05.
- INV-4: clipboard text fidelity (multiline / mixed-script / Arabic) is unchanged → TC-03/04/05.
- INV-5: the quiet-confirm cue does **not** appear on the copy-unavailable / disposed paths; `showQuietConfirm` guards lifecycle (`mounted`/`context.mounted`) BEFORE `ScaffoldMessenger.maybeOf` (which throws on a deactivated context, not returns null) → TC-06.
- INV-6 (channel reservation, preserved): error snackbars stay — `group_info_mute_update_failed`, the details error/rollback path, `group_invite_decline_failed` — proven by the still-green error-path asserts (`group_info_wired_test.dart:3322/:3363/:3412/:3466`) → TC-02 discriminator + preservation gate.
- INV-7 (haptic specificity): every haptic-capture assert checks BOTH `call.method == 'HapticFeedback.vibrate'` AND `call.arguments == 'HapticFeedbackType.selectionClick'` — because all `HapticFeedback` variants share the method string, a method-only assert would let a wrong-haptic mutation pass → TC-01/02/03/04/05 mutation (c).
- INV-RED-FIRST and INV-MUTATION-VERIFIED apply to every row above.

## Step-By-Step Implementation Plan
1. Snapshot `git status --short`. Add/rewrite the 6 catalog tests (locate by test NAME, not the drifted line numbers); for each test consolidate the `SystemChannels.platform` mock into ONE handler that switches on `call.method` and records a `List<MethodCall>` (clipboard + haptic). Run the focused cmds; confirm each fails for its documented reason (snackbar present / no haptic-with-arg / no quiet-confirm key).
2. `lib/core/widgets/quiet_confirm.dart` (NEW): `void showQuietConfirm(BuildContext context, String message)` that (a) **FIRST** guards lifecycle — `if (!context.mounted) return;` — because `ScaffoldMessenger.maybeOf(context)` **throws** (debug assert) on a deactivated/popped context and does NOT return null (N1); (b) calls `HapticFeedback.selectionClick()`; (c) `final m = ScaffoldMessenger.maybeOf(context); if (m == null) return;` (null only when the active element has no messenger ancestor) then `m..hideCurrentSnackBar()..showSnackBar(SnackBar(key: const ValueKey('quiet-confirm'), duration: ~1.2s, behavior: SnackBarBehavior.floating, non-error styling, content: Text(message)))`. Import `package:flutter/material.dart` + `package:flutter/services.dart`. Stop-if: a SnackBar can't carry a stable findable key in this Flutter version → fall back to a minimal `OverlayEntry` pill with the same `ValueKey('quiet-confirm')` (still no new l10n). [Verified: the user `key` IS preserved on the inserted SnackBar — `snack_bar.dart:516-518` — so the keyed-SnackBar path is expected to hold; the OverlayEntry fallback is contingency only.]
3. `group_info_wired.dart`: add `import 'package:flutter/services.dart';` (after `package:flutter/material.dart` `:7`). In `_onMuteChanged`: after the `setState` flip (`:495-498`, already behind `!mounted` `:491-493`) call `HapticFeedback.selectionClick();` and DELETE the success `showSnackBar` block (`:499-509`). Keep the catch error snackbar (`:524-530`) untouched. In the details success branch: after `await _loadGroupInfo();` (`:1939`) and the `!mounted` guard (`:1940`) call `HapticFeedback.selectionClick();` and DELETE the success `showSnackBar` block (`:1941-1947`). Keep the details error/rollback snackbar (`:2012-2023`) untouched.
4. `group_conversation_screen.dart` `_copyMessageText(BuildContext context, String text)` (`:1055-1068`): replace the `ScaffoldMessenger.maybeOf(context)..showSnackBar(...)` body (`:1057-1066`) with `showQuietConfirm(context, AppLocalizations.of(context)!.conversation_context_copied);` (keep `Clipboard.setData` `:1056` first). The call site already passes the LIVE `cardContext` (`:1036`) — keep it, never `dialogContext`. services.dart already imported (`:5`).
5. `conversation_screen.dart` `_copyMessageText(String text)` (`:915-929`): same replacement, **keeping the `if (!mounted) return;` guard (`:917`)** and passing the State's `this.context`. services.dart already imported (`:5`).
6. Do NOT touch the DECLINE snackbar (`group_list_wired.dart:624/:723` + `orbit_wired.dart:1447`) — owned by 153.
7. **No harness change.** `group_info_wired_test.dart` is ALREADY in `GROUP_TESTS` (`scripts/run_test_gates.sh:135`); `group_conversation_screen_test.dart` (`:132`) and `conversation_screen_test.dart` (`ONE_TO_ONE_TESTS:62`) are also curated. Do NOT add any array entry (the earlier "add to GROUP_TESTS" step was based on a false premise — would duplicate `:135`).
8. Rerun direct → preservation → named gates; `flutter analyze` (0-new); `git diff --check`. Note (do NOT delete) the now-unused arb keys `group_info_notifications_muted` (en:784) / `_restored` (en:785) / `group_info_details_updated` (en:576).

## Risks And Edge Cases
- **Copy invisibility** if the cue is dropped: copy has no persistent control (overlay popped at `:1035`) → the quiet-confirm cue is mandatory → pinned by TC-03/04/05.
- **Wrong context passed to `showQuietConfirm`** (N5): the two `_copyMessageText` have DIFFERENT signatures — group is `(BuildContext context, String text)` called with the live `cardContext` (`:1036`); 1:1 is `(String text)` using `this.context` behind `!mounted` (`:917`). The silent-null-cue trap materializes ONLY if a refactor passes the popped `dialogContext` (group) or drops the 1:1 `!mounted` guard. KEEP both as-is → the cue resolves a live messenger (it already does today).
- **`maybeOf` is NOT null-safe on a dead context** (N1): on a deactivated/unmounted element `ScaffoldMessenger.maybeOf` THROWS (debug assert at `framework.dart:5046-5089`); it returns null only on an active element with no messenger ancestor. `showQuietConfirm` MUST guard `context.mounted` BEFORE the lookup → pinned by TC-06 (drop-guard mutation throws on the disposed path, caught by `takeException isNull` `:1509`).
- **Cue indistinguishable from an error snackbar**: would re-pollute the reserved channel → the `ValueKey('quiet-confirm')` discriminator + non-error styling pin it (TC-03).
- **Haptic mock channel (B2)**: `HapticFeedback.selectionClick()` routes through `SystemChannels.platform` as method `HapticFeedback.vibrate` with positional arg `'HapticFeedbackType.selectionClick'` — but EVERY haptic variant shares that method string, so tests MUST assert `call.method == 'HapticFeedback.vibrate'` **AND** `call.arguments == 'HapticFeedbackType.selectionClick'` (else a wrong-haptic mutation escapes). There is exactly ONE mock handler per channel — a 2nd `setMockMethodCallHandler` OVERWRITES the 1st, so consolidate clipboard + haptic into ONE handler that switches on `call.method` and records a `List<MethodCall>`; returning `null` for `HapticFeedback.vibrate` is safe (it awaits `invokeMethod<void>`), and keep returning `null` for `Clipboard.setData`. Use `pump` not `pumpAndSettle` (the floating SnackBar timeout never settles).
- **services.dart import only in group_info_wired.dart**: adding it to the conversation screens would be a redundant duplicate-import analyze warning — they already import it (`:5` each); pinned by `flutter analyze` 0-new.
- **Line-number drift (execution hazard, N4)**: the dirty `new-feed` tree drifted test anchors ~+96 lines (mute `:2854`→`:2950`, EK004 `:3627`→`:3723`) and copy-source lines too — plan-era `:2854` lands in an UNRELATED dissolve test. Locate every edit by test NAME / symbol, never by the plan's line.
- **Unused l10n keys**: leaving `group_info_notifications_*` (en:784/785) / `group_info_details_updated` (en:576) unreferenced is harmless (arb keys without consumers don't fail analyze); deleting them would churn generated `app_localizations*.dart` — explicitly deferred.

## Device/Relay Proof Profile
host-only for closure. Pure presentation (snackbar removal, haptic call, a local cue) — no OS-boundary / ML-KEM / relay / multi-device / migration leg. `HapticFeedback` is mocked at the `SystemChannels.platform` boundary in widget tests; no device evidence required. (A render-proof is not warranted — there is no new persisted surface, only a transient cue + a method-channel call already exercised via the platform mock.)

## Acceptance Gates  (literal — copy/paste, with expected counts)
```bash
# RED (before production edits) — must FAIL for the documented reason
flutter test test/features/groups/presentation/group_info_wired_test.dart \
  --plain-name 'toggles mute state and persists it to the repository'
flutter test test/features/groups/presentation/group_info_wired_test.dart \
  --plain-name 'EK004 PREREQ-SIGNED-COMMIT-AUDIT admin metadata edit'
flutter test test/features/groups/presentation/group_conversation_screen_test.dart \
  --plain-name 'long-press copy action copies exact text and dismisses once'
flutter test test/features/conversation/presentation/screens/conversation_screen_test.dart \
  --plain-name 'copies exact multiline text'

# Direct GREEN (after fix)
flutter test test/features/groups/presentation/group_info_wired_test.dart
flutter test test/features/groups/presentation/group_conversation_screen_test.dart
flutter test test/features/conversation/presentation/screens/conversation_screen_test.dart

# Preservation + named gates (NO array edit needed — group_info_wired_test already curated at run_test_gates.sh:135)
./scripts/run_test_gates.sh groups        # group_info_wired ALREADY curated (:135); expect prior green + rewritten cases; the 2 PRE-EXISTING wired fails (GMAR-004 reopen-hydration, incoming-group-image-refresh) are NOT mine
./scripts/run_test_gates.sh 1to1          # conversation_screen_test copy cases green
./scripts/run_test_gates.sh feed          # LetterCard / feed unaffected, stay green

# Hygiene
flutter analyze            # 0 new issues (esp. no duplicate services.dart import)
git diff --check
```

## Known-Failure Interpretation
- Expected RED: the 6 catalog tests before the fix (success-snackbar present / no haptic captured / no quiet-confirm key).
- Pre-existing dirty (NOT mine): the Groups suite has **2 pre-existing failing wired tests** — `GMAR-004` reopen-hydration and incoming-group-image-refresh (in `group_conversation_wired_test.dart`, unrelated to this plan). Record before execution; do not "fix" by reverting.
- The wider tree is already dirty on `new-feed` (uncommitted Feed/Orbit work per `git status`) — snapshot `git status --short` first; touch only the Real-Scope files.
- Environment blocker (NOT product): none (host-only, no sim/device).
- Scope drift (BLOCKING): any failure outside the listed files/tests, or any change to the DECLINE snackbar (153) / error snackbars.

## Done Criteria
- [x] RED added/rewritten first, failed for the expected reason (snackbar-present + haptic-absent legs).
- [x] Each fix mutation-verified (re-red revert named in the matrix) — 5/6 confirmed; M4 refuted as a plan error (documented), not a defect.
- [x] Direct GREEN + groups/1to1/feed preservation gates pass; the groups -7 are concurrent-uncommitted-work noise + the 2 plan-named pre-existing media (zero copy/quiet-confirm involvement).
- [x] No migration introduced (pure presentation).
- [x] `group_info_wired_test.dart` confirmed running under `./scripts/run_test_gates.sh groups` — **already curated at `:135`, NOT newly added** (no duplicate array entry).
- [x] Every haptic assert checks BOTH `call.method == 'HapticFeedback.vibrate'` AND `call.arguments == 'HapticFeedbackType.selectionClick'`; mocks consolidated into ONE per-channel handler (clipboard + haptic).
- [x] `showQuietConfirm` guards `context.mounted` BEFORE `ScaffoldMessenger.maybeOf` (maybeOf throws on a dead context); group copy keeps the `cardContext` arg, 1:1 keeps its `!mounted` guard.
- [x] `group_info_wired.dart` imports `package:flutter/services.dart`; both conversation screens left with their single existing import (no duplicate). (Test-only: dropped the now-redundant `dart:typed_data` from `group_info_wired_test.dart` — services.dart provides it — to keep analyze 0-new.)
- [x] Mute + details success snackbars removed; copy uses haptic + distinct `quiet-confirm` cue; error snackbars (mute_update_failed, details error path, decline_failed) untouched.
- [x] `flutter analyze` 0-new; `git diff --check` clean.

## Scope Guard (hard "Do not")
- Do not add a DB migration or bump `currentIdentityDatabaseVersion` (no schema change).
- Do not touch the DECLINE success snackbar / Undo affordance (`group_list_wired.dart`) — owned by plan 153.
- Do not remove or rename `conversation_context_copied` (reused by the quiet-confirm cue) — and only ever remove a copy-cue key after BOTH call sites (group `:1063`, 1:1 `:924`) stop using it.
- Do not remove the error snackbars: `group_info_mute_update_failed`, the details error/rollback path (`:2012-2023`), `group_invite_decline_failed`.
- Do not ADD `group_info_wired_test.dart` to `GROUP_TESTS` — it is already at `:135`; a duplicate entry would run it twice / risk a script error.
- Do not pass the popped `dialogContext` to `showQuietConfirm`, and do not drop the 1:1 `_copyMessageText` `!mounted` guard — either makes the cue silently never show (or throw).
- Do not convert the deferred sibling success snackbars (dissolve/role/resend/revoke) in this plan — scope is mute/details/copy only.
- Do not delete the now-unused `group_info_notifications_*` / `group_info_details_updated` arb keys or regenerate `app_localizations*.dart` in this plan (deferred l10n cleanup).
- Do not add `services.dart` to the conversation screens (already imported → duplicate-import warning).
- Do not build a bespoke animated toast widget — the minimal keyed SnackBar (or OverlayEntry fallback) is the cue.

## Accepted Differences / Intentionally Out Of Scope
- The unused success-l10n keys remain in the arb files (no consumer) — accepted; a follow-up l10n-cleanup pass owns their removal (it touches generated getters).
- The quiet-confirm cue is a short keyed SnackBar (or OverlayEntry fallback), not a new design-system pill component — accepted; no reusable toast/pill exists and building one is out of scope.
- Mute/details rely on the already-updated control/field as the confirmation; only copy keeps a visible cue (because it has no persistent control — the overlay is popped first) — accepted asymmetry per root cause, test-locked (TC-01/02 snackbar-absent vs TC-03/04/05 cue-present).
- Other `group_info_wired.dart` success snackbars (dissolve/role/resend/revoke) keep their snackbars for now — accepted; converting them is a follow-up that can reuse `quiet_confirm.dart`. Named in *Out of scope* so the channel-reservation goal is explicitly only-partially applied this pass (mute/details/copy).

## Dependency Impact
- None outbound. Plan 153 (Undo) owns the DECLINE snackbar independently (machinery spans `group_list_wired.dart:624/:723` + `orbit_wired.dart:1447`); this plan deliberately does not touch it.
- ~~The harness-registration fix retroactively gates group-info behavior — a standalone hardening win.~~ **REMOVED (B1):** `group_info_wired_test.dart` is ALREADY in `GROUP_TESTS:135`, so there is no registration change and no new hardening win — group-info behavior is already gated. This plan adds zero array entries.
- Inbound: shares `letter_card.dart`/test churn space with concurrent 149/155 work and `group_list_wired.dart` with 150/152/153 — but this plan touches NONE of those files (only `group_info_wired.dart`, the two conversation screens, the new `quiet_confirm.dart`, and the three test files), so no edit-collision. The new `lib/core/widgets/quiet_confirm.dart` is a fresh shared helper any future "quiet success" conversion (the deferred sibling snackbars) can reuse.

## Reviewer Findings
Verdict: **needs-fixes → all fixed inline (this revision).** Source-grounded via a 6-agent verify→refute workflow against real `lib/`, real tests, `run_test_gates.sh`, the arb files, AND the Flutter SDK (`services/haptic_feedback.dart`, `material/scaffold.dart`, `material/snack_bar.dart`, `widgets/framework.dart`, `flutter_test/test_default_binary_messenger.dart`).

**BLOCKING (both fixed):**
- **B1 — registration premise FALSE.** `group_info_wired_test.dart` is ALREADY curated in `GROUP_TESTS` at `scripts/run_test_gates.sh:135`. The plan's Real Scope / Step 7 / Done Criteria / TC-01+02 registration cells / Dependency-Impact all said to ADD it — executing that creates a duplicate entry. Fixed: every "add to GROUP_TESTS" → "already at :135, no array edit"; the "standalone hardening win" claim struck. (`GROUP_TESTS` actually spans `:118-138`; `group_conversation_screen_test`=`:132`, `letter_card_test`=`:131`.)
- **B2 — haptic assert under-specified (mutation escape).** Verified in SDK: `HapticFeedback.selectionClick()` → `SystemChannels.platform.invokeMethod('HapticFeedback.vibrate', 'HapticFeedbackType.selectionClick')`. EVERY variant shares the method string; only the positional arg differs. A method-only assert reds for "drop the haptic" but NOT for "wrong haptic". Fixed: all haptic asserts (TC-01/02/03/04/05) now require `call.method == 'HapticFeedback.vibrate'` AND `call.arguments == 'HapticFeedbackType.selectionClick'`, plus mutation (c) wrong-haptic in the matrix. Also: ONE mock handler per channel (a 2nd `setMockMethodCallHandler` OVERWRITES the 1st) → consolidate clipboard + haptic into ONE handler that records a call list.

**NON-BLOCKING (all incorporated):**
- **N1** — `ScaffoldMessenger.maybeOf` THROWS (debug assert) on a deactivated context; it does NOT return null. `showQuietConfirm` must guard `context.mounted` BEFORE the lookup. (The original "null-guarded; no-op if unmounted" wording was wrong.) Current copy code is already safe — see N5.
- **N2** — sibling success snackbars (`group_dissolved:617`, role-change `~:2083`, resend `~:2109`, revoke `~:2240`) now NAMED as intentionally deferred (Blind-Spot Sweep + Out of scope + Accepted Differences).
- **N3** — EK004/TC-02: retarget the single snackbar assert (`:3814`) IN PLACE; do NOT split into a new case (would duplicate the signed-commit setup). The crypto/audit asserts (`:3816-3823`, `:3836-3873`) are separable and stay verbatim — confirmed in source.
- **N4** — systematic line drift on the dirty tree (group_info_wired_test ~+96 lines: mute `:2854`→`:2950`, EK004 `:3627`→`:3723`; error-paths →`:3322/:3363/:3412/:3466`; copy source group `:1055-1068`, 1:1 `:915-929`, pops `:1035`/`:841`). All anchors corrected; a prominent "locate by test NAME, not line" warning added (plan-era `:2854` lands in an unrelated dissolve test).
- **N5** — the two `_copyMessageText` have DIFFERENT signatures (group `(BuildContext, String)` w/ live `cardContext`; 1:1 `(String)` w/ `this.context`+`!mounted`). Refactor must respect both; never pass `dialogContext`. Documented in Real Scope / Steps 4-5 / Risks / Scope Guard.

Confirmed-correct (no change needed): keyed-SnackBar findability (user `key` preserved at `snack_bar.dart:516-518`; gotcha: `pump` not `pumpAndSettle`); `conversation_context_copied` reused, exactly 2 call sites (group `:1063`, 1:1 `:924`), snake_case getter; orbit2 haptic precedent real (`:109`); both conversation screens already import `services.dart` (`:5`); the three orphaned l10n keys' only lib consumers are the two removed snackbars; no migration.

## Arbiter Decision
Structural blockers: **none remaining** — B1 and B2 fixed inline; N1–N5 incorporated; the required **Blind-Spot Sweep** section (previously missing) added with a row-or-justified-N/A for all four classes. Matrix has **zero empty cells** across tier / mutation / gate / registration, and the mutation column now carries the wrong-haptic discriminator. Closure is **host-only** (pure presentation: snackbar removal + guarded haptic + a keyed cue) — no migration, no DB version bump, no OS-boundary/ML-KEM/relay/multi-device leg, so no simulator or device-proof gate. **No harness-registration change** (all three test files already curated). Deferred details: the sibling success snackbars (named) and the orphaned-l10n-key cleanup (named) — both owned by explicit follow-ups. Accepted differences: the mute/details-haptic-only vs copy-keeps-a-cue asymmetry (test-locked) and the keyed-SnackBar-not-a-design-system-pill cue. **Verdict: accepted — hand off to execution.**

## Final Execution Verdict
**ACCEPTED — host-green, implementation complete (2026-06-23).** All 6 TCs RED-first then GREEN; mute/details success snackbars dropped for `HapticFeedback.selectionClick()` (the switch+bell / re-rendered fields are the surviving confirmation); copy (group + 1:1) now fires the haptic + a distinct, keyed `ValueKey('quiet-confirm')` floating cue (1.2s, secondaryContainer-tinted) via the new shared `lib/core/widgets/quiet_confirm.dart`. Error snackbars (mute_update_failed, details error/rollback, decline_failed) untouched; DECLINE snackbar (153) untouched.

Evidence: direct GREEN group_info_wired +68 / group_conversation_screen +62 / conversation_screen +71; gates 1to1 +1194, feed +214 clean; groups +871/-7 where the 7 are entirely concurrent uncommitted 149/150/151 work + the 2 plan-named pre-existing media (proven: zero `quiet-confirm`/`_copyMessageText` in any failure, and my own group test files pass fully). `flutter analyze` 0-new; `git diff --check` clean. No migration, no DB version bump, host-only (HapticFeedback mocked at `SystemChannels.platform`).

Mutation verification (self, main loop — NOT subagents, per the tree-mutation hazard): **5 of 6 confirmed load-bearing** — M1 re-add mute snackbar (TC-01 reds `findsNothing`), M2 drop details haptic (TC-02 reds haptic leg), **M3 selectionClick→heavyImpact (TC-03 reds the `arguments` leg — confirms the B2 fix: method-only would have wrongly passed since every variant shares `HapticFeedback.vibrate`)**, M5 drop 1:1 `!mounted` (TC-06 throws on deactivated-context lookup), M6 drop `ValueKey('quiet-confirm')` (TC-03 reds key finder).

**Plan correction (M4):** the matrix's TC-04 mutation (b) — "drop `hideCurrentSnackBar()` → two quiet-confirms stack → `findsOneWidget` reds" — is FALSE. `ScaffoldMessenger` QUEUES SnackBars (only one in-tree at a time) and both cues share the same key + identical text, so dropping `hideCurrentSnackBar()` leaves TC-04 GREEN. `hideCurrentSnackBar()` is retained for snappy immediate replacement (without it the 2nd cue waits out the 1st's 1.2s) but is NOT independently mutation-locked; the real invariant (exactly one cue visible) holds via queue semantics regardless. No behavioral defect.

Deferred-as-planned: the orphaned `group_info_notifications_*` / `group_info_details_updated` arb keys (left in place; l10n-cleanup follow-up owns removal), and the sibling success snackbars (dissolve/role/resend/revoke) which can reuse `quiet_confirm.dart`. Uncommitted on `new-feed`.
