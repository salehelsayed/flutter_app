# 260 - Private Media Proposed-UX Adoption: Never-Blank Cards, Picker Sheet, Offline Media Outbox, Feedback System (Bug + Feature Improvement)

Status: execution-ready (v4 — second adversarial /tdd-review applied 2026-07-19, decisions locked with the user: automatic upload attempts are issuance-gated on OS connectivity AND connectivity-disposition failures never burn the retry ceiling; TC-37 PASS keys on causality/exactly-once/delivery with latency recorded informationally; MEDIA_ERROR split by permanence and transport code lists corrected to the real media path; upload-consumer topology corrected (TWO group-composer sites, group manual media retry is NEW wiring on the incomplete-retry seam, three bridge-bypass lanes named out of scope); S3a/S4 split into micro-checkpoints; five over-builds trimmed (one shared consumer-boundary wrapper, claim-scoped trigger source, offline-banner state reuse, TC-38 device-unique trim, E14 fallback retired). v3 — source-backed readiness audit applied 2026-07-19: stage-typed upload failures replace relay-state guessing; direct retry single-flight and atomic terminal projection added; owner-lane and no-bridge voice bypasses closed; LetterCard/overlay tests made geometric and causal; foreground-generation and typed-settle contracts added; sender-consumed cards intentionally become kind-neutral after cleanup; invite ghost identity retained; TC-37/TC-38 replaced with manifest-owned, production-driving, availability-bounded device proofs; literal gates corrected)
Spec: free-text intent (no formal spec). Source design: `Test-Flight-Improv/private-media-feedback-ux-mockup-codex.html` — the ENTIRE **proposed** side of every section (§01 protection picker, §02 chat-state cards, §03 offline delivery, §04 feedback system, §05 copy map, §06 revised copy), plus the user's screenshot showing the headline bug: protected/view-once messages render an EMPTY bubble on sender and receiver. User constraints: (a) the thumbnail/blank-bubble issue MUST be fixed with regression coverage; (b) **every button this plan introduces must actually do what it says** (no dead affordances).

## Planning Progress
| Time | Role | Files inspected | Decision/blocker | Next action |
|---|---|---|---|---|
| 2026-07-18 | Evidence Collector | Workflow `wf_ef4bf167-17d` (103 agents, 0 errors: 6 area grounders w/ embedded digest, 45×2 verify+refute pairs, 1 xhigh completeness critic): 48 claims → 45 trusted, 3 refuted; 8 gaps | Blank-bubble mechanism triple-verified; every §01-§04 seam grounded with file:line; feasibility verdicts on sender-reopen, ask-new-invite, rollback, per-bubble progress | hand to Planner |
| 2026-07-18 | Planner | conversation_screen.dart, letter_card.dart, compose_area.dart, direct_private_media_viewer(+_controller).dart, private_media_policy.dart, private_media_lifecycle_engine.dart, messages_db_helpers.dart, conversation_wired.dart, pending_message_retrier.dart, retry_incomplete_uploads/retry_failed_messages use cases, upload_media_use_case.dart, orbit_wired.dart, group_list_wired.dart, pending_group_invite_card.dart, quiet_confirm.dart, mic_permission_prompt.dart, run_test_gates.sh, ARBs | 6 slices S1-S6; 10 explicit mockup-vs-truth decisions (Accepted Differences); NO DB migration needed (sender lane reuses existing columns) | hand to Reviewer |
| 2026-07-18 | Reviewer (/tdd-review) | Audit workflow `wf_dd620bde-9d7` (8 agents: factual + domain verifiers, 5 dimension assessors, blind-spot critic) + orchestrator linchpin self-checks (stuck-sending SQL, block-0 grep, CAS mode predicate, media_attachments schema) | Verdict "no — replan S3": E10's keep-at-'sending' destroyed by 3 sweepers (pause/no-age-gate, 30s stuck recovery, retrier pass); voice/share/thrown-exception lanes uncovered; TC-37 mutation-vacuous; E20 3 scope holes; 4 unguarded ADs; step-ledger defects | apply v2 revision |
| 2026-07-18 | Arbiter | v2 of this plan | v2 handoff recorded; superseded by the 2026-07-19 counterexample audit below | re-audit before execution |
| 2026-07-19 | Planner/Arbiter | v3 source verification: upload/retry seams, lifecycle/controller directionality, LetterCard overlay construction, invite ghost projection, SIMS manifest/CLI, device-local harness, l10n/analyzer gates, live device matrix | All new blockers incorporated in-place. Product choice locked: sender-consumed cleanup retains no kind metadata and renders a generic private-media terminal title. No production files changed while patching this plan | re-audited (v4) |
| 2026-07-19 | Reviewer (/tdd-review, 2nd pass) | Audit workflow `wf_43c68f97-753` (10 agents: factual + domain + 2 over-engineering/duplication verifiers, 5 dimension assessors, blind-spot critic) + orchestrator linchpin self-checks (resume Step 8b un-gated, kMaxUploadRetries=3, bridge.go:1470 MEDIA_ERROR wrapping, bypass callers, group :2733, report.dart devices.assignments) | Verdict "yes-with-tightening": TC-37 phase-2 counters would false-RED (un-gated offline resume retry); offline ceiling-burn re-created the §03 lie test-green; MEDIA_ERROR mixed permanence; consumer enumeration decomposed wrong; S3a/S4 monoliths; 5 concentrated over-builds. S4 continuity bet CONFIRMED sound; alleged devices.assignments gate break REFUTED (report.dart:201-202 emits it) | apply v4 revision (this doc) |

## Execution Progress
| Time | Phase | Files touched | Command/evidence | Decision/blocker | Next |
|---|---|---|---|---|---|
| 2026-07-19 | contract extraction / Step 0 | working-tree inventory; `/tmp/plan-260-analyze-before.txt` | Flutter 3.41.4; stale-literal sweep PASS; `run_test_gates.sh 1to1` `+2105 All tests passed`; serial groups rerun `+2204 -3 Some tests failed`; analyzer snapshot contains 1,956 pre-existing diagnostics | baseline HEAD is `2be07626e` (the prose baseline hash is stale); the three exact groups failures remain a forward-fix/closure obligation and are not re-baselined | identify/fix the three groups failures before named-gate closure |
| 2026-07-19 | S6a l10n enabler | en/ar/de ARBs, generated localization, exact l10n contract test | exact private-media UX copy contract + l10n integrity: 5/5 PASS; checkpoint `6c5fdc940` | all plan copy keys and placeholder metadata are present; E16 promise strings use l10n | keep literal/parity sentinels green at final closure |
| 2026-07-19 | S1 cards (interim) | direct/group conversation card composition, LetterCard private slot, focused tests | direct private card 4/4; direct conversation 79/79; group screen 70/70; direct viewer placeholder 21/21 | sender/receiver/terminal private state is inside the decorated keyed slot with no private pixels; screenshot-vs-mockup evidence still pending live capture | capture and record §02 visual comparison, then checkpoint |
| 2026-07-19 | S2 picker sheets (interim) | shared picker sheet, direct ComposeArea, group conversation picker adapters | direct composer 12/12; group screen 73/73; group wired action 1/1 | summary chip + CTA-only modal selection is wired on both policy types; screenshot-vs-mockup evidence still pending live capture | capture and record §01 visual comparison, then checkpoint |
| 2026-07-19 | S3a1 typed upload boundary | typed outcome/wrapper and ten production consumers | upload/direct/group background focused set 57/57; retry/failed-media/voice set 81/81; upload suite 32/32; targeted analyze has no errors | E9 is green; `MEDIA_UPLOAD_START` is emitted only at transport issuance after encryption and carries no raw identifiers | finish S3a2 projection/gating and rerun offline-text sentinel |
| 2026-07-19 | S5 feedback checkpoint | shared UndoBar; Orbit/GroupList durable invite outcomes; quiet confirm; microphone permission sheet; contact-profile copy feedback | UndoBar 3/3; GroupList 46/46; Orbit 101/101; quiet + microphone + contact-profile 13/13; §04 artifact `Test-Flight-Improv/evidence/260/s5-permission-sheet.png` captured at 390×844 | the stale queued-SnackBar/controller race is fixed by replacing existing feedback before mounting UndoBar; the permission prompt now matches §04's scrim, rounded sheet, icon, title/body, and two live actions; at the narrow capture width its action bar wraps vertically as an intentional responsive difference | preserve these focused greens in final family gates |
| 2026-07-19 | S7 host contracts (interim) | SIMS manifest/validator/protocol/controller/dispatch; TC-38 harness/schema/runner | protocol/controller/dispatch/main focused 7/7; manifest/criteria 18/18; TC-38 criteria 87/87; Android integration APK compile PASS | TC-37 production endpoint/host runner/device proof remain; TC-38 strict production-card artifact still needs the pinned live run | finish production endpoint and SIMS runner, then resolve/pin live Android pair |
| | final preservation / named gates / QA | | pending | no final verdict yet | focused suites, justified family sweeps, wave-level `host-all`, device proofs, incremental Graphify refresh |

## Source Of Truth
- Design: the mockup's proposed side; **this plan's contracts win wherever the mockup contradicts platform truth or shipped 259 copy** — every delta is listed in Accepted Differences.
- Baseline: execution resolved the CURRENT WORKING TREE at HEAD (`2be07626e`) before Plan 260 changes; the earlier planning snapshot `5ae501f9c` is superseded. "RED on HEAD" below means RED on that recorded execution baseline. Step 0 re-runs 259's stale-literal sweep as baseline sanity.
- Gate definitions: `scripts/run_test_gates.sh` (script wins over prose)
- Discovery/registration: `scripts/check_reliability_simulation_discovery.sh`
- Numbering / index: `Test-Flight-Improv/00-INDEX.md`
- Toolchain: run from this repository root with the host Flutter on PATH. At planning time `command -v flutter` resolves to `/Users/I560101/development/flutter/bin/flutter` and `flutter --version` reports 3.41.4. Step 0 re-resolves both facts and stops on a version mismatch; do not use stale `/workspace` or `/claude-host-bin/flutter` literals.

## Session Classification
implementation-ready (large; execute slice-by-slice with the per-slice RED/GREEN checkpoints and the availability-bounded device matrix below)

## Exact Problem Statement
Private media is the app's flagship privacy feature and it currently *looks broken*. (1) **Blank bubbles** (the screenshot): a protected/view-once message renders an empty outlined bubble containing only a timestamp, with the mode/reassurance panel visually detached below it — on BOTH sender and receiver. (2) The **chooser** is a popup menu that commits instantly with no consequence summary, no per-mode disclosure, and no persistent selection summary. (3) **Offline media lies**: a media send with no connectivity is stamped terminal `failed`, restores the composer (inviting a duplicate), and shows a red "Failed to upload media. Try again." — while the durable row is still `upload_pending` and background retry will send it; text in the identical situation calmly says "Will send when you're back online". (4) **First open can appear dead**: the first tap on undownloaded private media only downloads (no visible progress) and the user must tap again; open failures are silently swallowed — and for view-once, a post-lease failure permanently consumes the view before showing a single pixel. (5) **Feedback** for invite outcomes, undo, permission blocks, and quiet successes still routes through one global snackbar channel in several places.

What must improve: all six mockup sections' proposed treatments, adapted only where the mockup contradicts platform truth (each adaptation recorded). The blank-bubble fix gets dedicated regression coverage. Every new button is wired to real behavior.

What must stay unchanged (→ preserved-green sentinels): the offline TEXT promise UX (mockup keeps it; snackbar shape pinned by `conversation_wired_offline_send_ux_test.dart` — S1 shape, TC-185-01 retriable-'sent', schedule_send glyph :1008); the hard-delete removed-from-group snackbar+pop branch; receiver view-once single-view security (no double view, ever); upload-cancel lane (`upload_cancelled` rows stay excluded from retriers); settings inline-only save errors, removed-member banner, chooser labels/details, placeholder reassurance bodies (all landed by 259); `ValueKey` test hooks `private-media-selector`, `private-media-option-*`, `group-private-open-*`, `mic-perm-not-now`, `mic-perm-open-settings`.

## Root Cause (verify → refute confirmed, workflow `wf_ef4bf167-17d`)
- **Blank bubble (the screenshot)**: `conversation_screen.dart:759-761` empties LetterCard's media for private presentation (text is already empty), then `:942-947` composes `Column([letterCard, privatePlaceholder])`. The hollowed LetterCard still paints its full decorated shell (`letter_card.dart:738-747`) because the footer meta row renders unconditionally (`:610-639`) — that shell with only a timestamp IS the empty bubble; the detached dark panel is the placeholder's own container (`direct_private_media_viewer.dart:516-522`). Sender mounts `DirectPrivateMediaOutgoingPlaceholder` at `:905-908`, receiver `DirectPrivateMediaOpenPlaceholder` at `:919-940`, terminal `:839`, unsupported `:876`. Bonus defect: the detached placeholder sits OUTSIDE every gesture surface — long-pressing it does not open the context overlay, and the overlay snapshot shows only the empty bubble (thumb/C4).
- **Chooser**: `PopupMenuButton<PrivateMediaPolicy>` (`compose_area.dart:361-433`), 6 flat items, instant commit; group sibling `_GroupPrivateMediaSelector` over the DIFFERENT `GroupPrivateMediaPolicy` type (`group_conversation_screen.dart:1318-1383`). The domain model already matches the mockup exactly: 4 modes + `allowedDurationsSeconds = {3600, 86400, 604800}` (`private_media_policy.dart:141`) — §01 is a pure presentation retrofit (picker-1).
- **Offline media**: upload failure returns an untyped `null` (`upload_media_use_case.dart:200-215`; nulls at `:312,:338,:407,:560`) so `conversation_wired.dart:3086-3096` cannot distinguish offline from terminal → `_restoreComposerSnapshot` (`:3589-3628`) stamps message `failed`, restores composer, red snack — while attachment rows remain `upload_pending`. The old v2 classifier was itself impossible: `UploadMediaFn` has no `P2PService`/relay probe, its outer catch collapses file I/O, encryption, durable-copy, and transport exceptions, and `relayReady` can be stale. Classification must therefore happen at the failing stage inside `uploadMedia`, never from ambient relay state. The ~1s network-restored and 5s/full retry passes have independent booleans; the foreground-only upload tracker does not prevent those two retry passes from encrypting/uploading the same stable blob concurrently. The attachment layer already has the queued/terminal split + retry budget, but terminal attachment and parent-message projection are separate writes and can strand `sending + upload_failed` after a crash. Direct/group parent IDs are independent, so every sweeper subquery must filter `owner_lane`. Direct no-bridge voice currently returns before the send use case creates an `upload_pending` durable row, so merely preserving its parent at `sending` would strand it. Manual Retry still misreads relative paths, and the background retry still needs promote-before-cleanup.
- **Viewer**: first tap on undownloaded media downloads and stops; `prepare()` collapses every failure to null; pre-first-frame failures consume view-once. Existing rollback authority remains double-view-safe. Two v2 assumptions were false: `mounted && ModalRoute.isCurrent` remains true while the app is backgrounded, and `settle(): Future<void>` discards whether rollback succeeded, so neither the no-background-auto-open rule nor truthful rollback copy is implementable without new state/result wiring. Sender one-more-look also needs an explicitly direction-aware target: incoming protected media must stay repeat-openable, while outgoing protected and view-once each get one sender-only lease. Lifecycle columns already exist on outgoing rows, so this still needs no migration. Media kind does **not** survive terminal cleanup: it lives on the transient/attachment projection, and cleanup deletes the attachment. v3 deliberately renders a kind-neutral sender-consumed terminal card after cleanup rather than retaining sensitive metadata.
- **Feedback**: orbit accept failures fire only snackbars while group-list already renders outcomes on the row/ghost row. The live invite has the inviter peer/key, but `PendingInviteRowOutcome` snapshots only state/reason/group name; after deletion, the ghost row cannot execute "Ask for a new invite" unless it also snapshots a qualified conversation target. Decline+Undo remains duplicated, `showQuietConfirm` remains full-width, and the mic prompt is still a dialog with working Open Settings.

Refuted / do-NOT-re-introduce (this plan's refute pass + carried from 259):
1. **"The composer tests pin the PopupMenu structure — a sheet breaks all 6"** — FALSE. They select by ValueKey + copy only; test :284 never opens the picker. A sheet keeping the keys/copy breaks ≈0; at most :172's counts need attention (tests/C1 refutation).
2. **"The capture-limit ARB strings are unused"** — FALSE. Both viewers render them (`direct_private_media_viewer.dart:282-283`, `group_private_media_viewer.dart:227-233`). Reuse them; do not re-add.
3. **"The QR 'copied' snackbar is a production surface"** — FALSE. `kDebugMode`-gated (`qr_code_section.dart:46-49`); production copy-snackbar targets are contact-profile only (GAP-3).
4. Carried from 259: no blanket screenshot-blocking claims (iOS is detection-only, `PrivateMediaProtectionCoordinator.swift:13`); "Sent to inbox" doesn't exist; settings-save "check your connection" is a lie (keystore write); removed-from-group §05 row is ALREADY DONE (GAP-8 — the exact revised string is live; only the hard-delete snackbar branch remains, deliberately).
5. **"Group private rows blank-bubble too"** — they don't; group substitutes placeholder TEXT inside the card (thumb/C7). The group work is card-parity, not a blank fix.

## Real Scope (7 slices)
**S1 — §02 unified private card + blank-bubble regression (the headline fix)**
- E1: add optional **`Widget? privateContentSlot`** and `Key? decoratedBodyKey` inputs to LetterCard. Render the slot from `_buildBodyChildren` immediately before the ordinary media-grid position, inside the clipped/decorated bubble `Container`; suppress ordinary media/audio rendering and the media-only spacer whenever populated. `ConversationScreen` computes a **slot factory** before `buildLetterCard`, then calls it from every `buildLetterCard()` invocation: live row and both `selectedMessage` context-overlay snapshots. Delete the sibling `Column([letterCard, placeholder])`. The slot receives policy, enum kind, copy, and callbacks only—never a path, bytes, `ImageProvider`, or attachment object. Stable decorated-body and per-message slot keys make geometry/ancestry testable; merely being somewhere below `LetterCard` is insufficient. Footer, reactions, long-press, semantics, and width cap remain on the one shell.
- E2: card visual redesign per mockup §02 inside the placeholders: visual header (gradient + large mode icon; **explicitly NOT a real pixel thumbnail — privacy-by-design**), media-kind title, existing reassurance body (259 keys), action button, meta stays LetterCard's footer. New `PrivateMediaAttachmentKind`-ish **kind param** on both placeholders (mount site already has `visual.single.mediaType`, V6; gif → photo strings, Accepted Difference; outgoing multi/zero-visual fallback: photo).
- E3: group receiver card parity uses the same keyed decorated-body/slot seam with a `GroupPrivateMediaPolicy`-typed variant deriving lifecycle from `mediaConsumedAt`/`mediaExpiredAt`; the private panel must be non-zero inside the decorated LetterCard body. Preserve whole-bubble Semantics and every `group-private-open-*` key while adding the card button.
- E4: terminal/unsupported placeholders move into the slot keeping their reply/info/delete action rows; their self-imposed width constraints yield to the bubble cap (thumb/C8).

**S2 — §01 protection picker bottom sheet + summary chip**
- E5: replace the 1:1 PopupMenu with a modal bottom sheet (reuse the `DirectMediaSaveDestinationSheet` blurred-card convention + `StatefulBuilder` for in-sheet state, picker-6): title "How should {name} see this photo/video/GIF?", 4 radio modes (existing label+detail keys), duration chips (1 hour/1 day/7 days — NEW short keys) revealed only for expiry, per-mode disclosure, CTA "Use <mode>" — **provisional selection commits only on CTA** (new UI state; menu committed instantly, picker-8), result feeds the SAME `ValueChanged<PrivateMediaPolicy>` callback. Keys `private-media-selector` / `private-media-option-*` kept.
- E6: composer summary chip (mode icon + title + consequence detail + "Change") replacing the trigger Chip at the same mount (`compose_area.dart` selector row — already correctly mount-gated by `allowsPrivateMedia`, picker-3/4); label DERIVED from `widget.privateMediaPolicy` so external resets (typing → ordinary) self-heal. **The GROUP composer gets the same summary chip** (mockup parity — the audit flagged the group chip as unspecified; the group selector chip at `group_conversation_screen.dart:1318-1383` is replaced by the same summary component over `GroupPrivateMediaPolicy`).
- E7: disclosure honesty: base copy makes no screenshot-blocking claim; append the existing `private_media_{ios,android}_capture_limit` note through an injected target-platform value that defaults to the production platform (rather than an untestable raw branch). The "You can reopen it once here after sending." sentence renders only for protected/viewOnce after S4 sender reopen is merged.
- E8: group selector gets the same sheet, parameterized over `GroupPrivateMediaPolicy`; name-free title ("How should members see this photo?" — group can't name one recipient).

**S3 — §03 offline media outbox** (REDESIGNED per audit `wf_dd620bde-9d7` — the original "keep `sending`" spec collided with three untouched sending→failed sweepers; sub-sliced S3a/S3b/S3c)
- E9 (S3a): replace nullable `UploadMediaFn` with exhaustive **`UploadMediaOutcome`**: `UploadMediaSucceeded(attachment)` or `UploadMediaFailed(stage, disposition, errorCode)`. Stages are validation, localSource, encryption, transport, durableCopy, and consumerBoundary; dispositions are **connectivityRetryable** (waits on connectivity, NEVER increments the retry budget), **boundedRetryable** (increments toward `kMaxUploadRetries`), or **terminal**. Classify inside `uploadMedia`, where the stage is known: invalid input/MIME and missing/unreadable source are terminal; keygen/encrypt/hash failures are boundedRetryable (encryption includes TRANSIENT Dart timeouts, `bridge.dart:699/:732` — v4 correction from terminal); durable-copy/canonical verification failures are boundedRetryable (they run AFTER a successful relay upload, `upload_media_use_case.dart:465-487` — terminalizing them discarded a completed upload). Transport is split by REAL permanence, not by code alone: the Go bridge wraps EVERY `MediaUpload` error into the single `MEDIA_ERROR` code (`bridge.go:1470`), so `MEDIA_ERROR` is sub-classified by errorMessage — relay-unreachable/dial/stream shapes are connectivityRetryable; `size … exceeds max` (the relay's permanent cap, `go-relay-server/media.go:438-439`; 1:1 uploads have NO Dart size gate) and local `open file`/`stat file` shapes are terminal; unrecognized `MEDIA_ERROR` messages are boundedRetryable. `NOT_INITIALIZED`, `NULL_RESPONSE`, and the issued-request watchdog `TimeoutException` (the media path throws TimeoutException and never yields a `BRIDGE_TIMEOUT` code — `p2p_bridge_client.dart:999-1031`, v4 correction) are connectivityRetryable; `INVALID_INPUT`, `UNKNOWN_COMMAND`, `MISSING_PLUGIN`, `MALFORMED_RESPONSE` are terminal; `PLATFORM_ERROR`/`INTERNAL_ERROR` and otherwise-unknown failure after the upload request boundary are boundedRetryable; unknown before that boundary is terminal. `relayReady` is never consulted for classification. Consumer-boundary throws normalize in ONE place: a single shared **`runUploadMedia`** wrapper — the only way consumers invoke the seam — catches any unexpected injected/call-boundary throw (including the pre-`try` window at `upload_media_use_case.dart:253-290`) and returns `UploadMediaFailed(consumerBoundary, terminal, UNEXPECTED_CONSUMER_THROW)`; consumers carry NO per-site catch (v4 trim: ten per-consumer catches duplicated uploadMedia's own whole-body catch at `:536`) and feed the wrapper result to the same projection/result switch—never a separate sequential parent-failure catch. Adapt ALL TEN wrapper call sites individually — direct composer (`conversation_wired.dart:3061`), **BOTH group-composer sites** (`_uploadPreparedGroupMediaUploads` `group_conversation_wired.dart:2279` AND the inline `_onSend` loop `:2733` — previously undercounted as one), direct voice (`send_voice_message_use_case.dart:115`), group voice (`:4866`), direct/group incomplete retry (`:226`/`:487`), direct failed-media retry (`retry_failed_messages_use_case.dart:753`), both share sites (`:1219`/`:1374`); automatic failed retry is deliberately zero-upload under E12, and GROUP manual failed-media retry is **NEW wiring on the incomplete-retry seam** (it owns zero upload sites today — see E12). Three production callers bypass the wrapper via `callP2PMediaUpload` and are NAMED OUT OF SCOPE (posts attach `attach_post_media_use_case.dart:288`, pass-post-along `pass_post_along_use_case.dart:595`, group avatar `group_avatar_storage.dart:129` — distinct blob domains, no conversation attachment rows; E9/E10/E11 do not apply). Share still maps every failure disposition to its existing terminal batch state; share queued-outbox UX remains named out of scope.
- E10 (S3a): a retryable outcome puts a direct parent in `sending` (group uses existing `queued_offline`), keeps unfinished attachment `upload_pending`, staging copy durable, the composer cleared, and the active quote **not restored**; the optimistic parent retains its original `quotedMessageId`. This normalization also applies to a legacy `failed + upload_pending` input, so red disappears before the later failed-retry pass. Apply owner-qualified exclusions before destructive transitions: `handleAppPaused` gets an exact bulk query for supplied direct sending IDs (batched, no 50-row retry-page limit; reuse `getAttachmentsForMessages` `media_attachments_db_helpers.dart:816` where it fits — v4 trim), then excludes matches from pause-flush and `sending→failed`; direct stuck recovery uses correlated `NOT EXISTS owner_lane='direct'`; both group cutoff forms use `owner_lane='group'`. Same-ID opposite-lane media never exempts a parent. Add crash-atomic **Direct and Group upload-retry projection repositories**, implemented by `MessageRepositoryImpl`/`GroupMessageRepositoryImpl` with one DB transaction per parent. Inside the transaction, first qualify an outgoing parent in allowed states (direct: `sending|failed`; group: `sending|queued_offline|failed`); a missing/incoming/delivered/settled parent is a no-op before attachment mutation. Then update only exact owner-matched attachment IDs/retry counts: **connectivityRetryable failures keep `upload_pending` and NEVER increment the count** (user-locked v4 decision — an offline stretch, however many app-opens it spans, cannot burn the budget); boundedRetryable failures increment the existing count and use `upload_pending` below `kMaxUploadRetries`/`upload_failed` at the ceiling; typed terminal failures—including normalized `consumerBoundary` throws—set `upload_failed` **without forging the ceiling count**. **Issuance gate (user-locked v4 decision)**: automatic retry passes (resume, periodic, full) consult an injected OS-connectivity probe (default `connectivity_plus checkConnectivity` — precedent `media_auto_download_decider.dart:20`) BEFORE issuing any upload attempt; when the OS reports no network they perform zero claim/encrypt/upload and leave every row untouched. The restored-edge leg (fires only on restore) and explicit manual Retry bypass the probe. Attempt-issuance gating is DISTINCT from the banned relayReady failure-classification: failures are still classified only by stage inside `uploadMedia`. When no owner attachment is terminal, normalize the parent to direct `sending`/group `queued_offline`; when any owner attachment terminalizes, set/keep the parent `failed`. This existing count is the no-migration discriminator for a later user-authorized budget rearm — a ceiling count now proves **bounded-retryable exhaustion** (connectivity failures never increment, so they can never reach it); validation/localSource/transport-terminal/consumer-boundary rows remain non-rearmable, while encryption/durableCopy rows are boundedRetryable per E9 and rearm like any at-ceiling bounded row. Emit changes after commit; no sequential fallback. Every upload-capable production consumer—direct/group composer, direct/group voice, direct/group incomplete retry, and direct/group manual failed-media retry—receives this projection capability and invokes it exactly once for every typed or normalized failure; it may not repeat attachment/parent writes itself. Automatic failed retry performs no upload and therefore makes zero projection calls. Extend group incomplete retry qualification to `queued_offline`. No schema/status migration. Queued LetterCard text remains in `_buildBodyChildren`; view-once adds "Not viewed yet."
- E10v (S3a): direct voice creates its stable IDs, atomically claims upload ownership, copies the recording to `pending_uploads/<message>/<attachment>`, and persists a direct-owned `upload_pending` row **before** checking/using the bridge. If bridge is absent after staging, release foreground ownership and keep queued/no-red; if staging fails, terminalize. `sendVoiceMessage` and group voice switch on the typed outcome: transport-retryable queues; validation/local/encryption/durable-copy stays terminal. Preserve local playback from the resolved durable path while DB storage remains container-move-safe. Reconnect must deliver both upload-failed and no-bridge cases without user action. v4 note: the queued-no-bridge branch is DEFENSIVE-ONLY — `widget.bridge == null` is production-unreachable (`main.dart:2043` always passes a GoBridgeClient); the load-bearing halves are the durable pre-bridge staging (today the optimistic attachment persists a `done` row pointing at the recorder TEMP path, `conversation_wired.dart:4046-4059` — a real defect) and the typed-outcome switch.
- E11 (S3a): upgrade process-wide **`MediaUploadInFlightTracker`** from check-then-act to atomic token ownership: `tryClaimAll(blobIds) -> lease?`, `isInFlight(blobId)`, owner-token `release(lease)`; multi-blob is all-or-none and foreign release cannot clear it. Introduce **`MediaUploadTriggerSource`** values (`foreground`, `networkRestored`, `full`, `periodic`, `resume`, `manual`) carried ONLY at the claim API and its telemetry event — each call site passes its own literal at `tryClaimAll`; the source is NOT threaded through entry-point signatures (v4 trim: no production branch consumes it — it exists for claim telemetry and TC-37 causality). Foreground image/video/GIF/voice, incomplete retry, and manual Retry retain the same registry lease through attachment persistence and envelope settlement; automatic/bulk failed retry remains envelope-only for `done` attachments and takes no media-upload claim or projection. The pre-existing group process guard remains only a coalescer, never the ownership authority. Emit `MEDIA_UPLOAD_LEASE_CLAIMED` only after a successful claim with safe hashed/run correlation plus the typed source (wire value `network_restored`; never raw IDs). The 1s restored callback invokes text/unacked, direct incomplete, and group incomplete as independently guarded legs; one leg's throw cannot skip either later media leg; the coalescing flag ALREADY releases in `finally` on HEAD (`pending_message_retrier.dart:250-251` — v4 correction: this clause is a preservation assertion, not a RED). Token ownership prevents overlap with full/node, periodic, resume, foreground, and manual upload-capable paths. Tests hold direct and group foreground leases separately, fire restored/full/manual competitors, prove the winner's source, one encryption/upload/envelope, and later re-claim after release; restored-leg tests inject unacked and direct exceptions and prove later legs plus a subsequent signal still run.
- E12 (S3b): add `MediaFileManager?` through `retryFailedMessages`, `retryFailedMessage`, `_retryFailedMessagesInternal`, `_retryFailedMessageCandidate`, `_resolveAttachmentsForRetry`, and `_reuploadAttachments`, plus the group failed-media manual entry and its wired callsite (**NEW wiring, v4 correction**: today `_onRetryFailedMedia` `group_conversation_wired.dart:3085` routes to `retryIncompleteGroupUploads` `:3121` and `retry_failed_group_messages_use_case.dart` owns ZERO upload sites — the group rearm capability is net-new on the incomplete-retry seam, not an adaptation of an existing upload path). Resolve every stored path before existence checks, pass the absolute path and manager into upload, and thread the manager through `PendingMessageRetrier`, both `main.dart` retry wirings, `ConversationWired`, and `GroupConversationWired` manual Retry. Only absence after resolution is localFileMissing. Partition retry authority explicitly: automatic/bulk failed retry reuses `done` attachments for envelope retry but never uploads `upload_pending` (owned by incomplete retry), terminal `upload_failed`, or preserved `upload_cancelled`; it makes zero upload claims/projection calls. A user-initiated manual Retry first resolves and qualifies **all** exact owner-matched unfinished attachments. It may continue `upload_pending`; it may rearm `upload_failed` only when `uploadRetryCount >= kMaxUploadRetries` proves bounded-retryable exhaustion (connectivity-disposition failures never increment, so a ceiling count can only come from counted bounded attempts — v4 re-key), every required source resolves, and the outgoing failed parent still qualifies. It then calls `tryClaimAll` over the complete unfinished blob set **before mutation**. Only while holding that manual lease may one parent+attachments transaction revalidate the same set, reset rearmable rows to `upload_pending`/retry count 0, and normalize the parent to direct `sending`/group `queued_offline`; failed claim/CAS leaves every terminal row unchanged. The lease remains held through upload persistence/envelope settlement. Missing/unresolvable media, mixed-in cancelled/typed-terminal rows, or any multi-attachment claim/CAS failure has no partial rearm and no Retry action. Single/manual, bulk/automatic, same-pass sequencing, held-competitor, mixed multi-attachment all-or-none, and concurrency-with-incomplete tests share this path; after attempt 3, the remainder of the same full pass and the next full pass perform zero claim/encrypt/upload/send work until an explicit qualified manual rearm.
- E13 (S3b): pass `mediaFileManager` into incomplete-retry upload; persist the returned canonical `media/<peer>/<blob>` path before envelope send. Delete `pending_uploads/<message>` only after every attachment is `done` **and** `sendChatMessage` succeeds. Never persist `done` while `localPath` still points into staging.
- E14 (S3c): uploading state on the bubble: "Sending automatically…" + "Uploading photo · {percent}%" driven by generalizing the screen's single-upload tracking (`conversation_wired.dart:774-812`) to a per-message map and engaging it from the retry lane. GAP-5 is CLOSED at plan time (v4): the upload id ↔ blobId ↔ attachment identity is stable by the same-blob-ID contract verified in source across all lanes, so percent SHIPS; the "Sending automatically…" no-percent copy survives only as a one-line contingency if execution disproves the contract (decision logged, not improvised).
- E15 (S3c): persistent offline banner ("You're offline / Messages and media will send when you're back online.") mounted beside the upload banner in 1:1 + group. Reuse the exact seed-then-subscribe derivation `ConnectionStatusIndicator` already implements (`connection_status_indicator.dart:101-108`) — extract or mirror it; only the banner PRESENTATION and its two ARB keys are new (v4 trim: do not re-derive offline-state logic). Seed synchronously from `p2pService.currentState`, then update from `stateStream`; do not wait for a replay that the broadcast stream does not provide. Widget tests cover initially offline, online→offline, and offline→online. `ConnectivitySignal` remains a restore trigger only and does not drive banner truth.
- E16 (S6a, executed in step 1): promise-string l10n unification (GAP-4): migrate the hardcoded consts — `conversation_wired.dart:3301-3302` (`senderOfflineCopy`, `queuedRetryCopy`), `group_conversation_wired.dart:3423-3424` (BOTH consts — senderOfflineCopy AND queuedRetryCopy, v4 citation fix), `share_batch_delivery_coordinator.dart:1455` — to new ARB keys; the queued-bubble line reuses the same key so the promise can never fork.

**S4 — viewer flows (make §02/§05 buttons true)**
- E17: single-tap open runs download → auto-continue with card-owned progress. `ConversationWired` already owns `WidgetsBindingObserver`; add current `appLifecycleState` plus a monotonically increasing `appLifecycleGeneration`, update them on every transition, and pass both into `ConversationScreen`. Create one stable production `RouteObserver<ModalRoute<void>>`; register that exact instance in `MaterialApp.navigatorObservers` and expose the same instance through a `MaterialApp.builder` scope so every conversation entry path subscribes without optional per-route wiring. `ConversationScreen` becomes `RouteAware` and increments `routeGeneration` on `didPushNext`/`didPopNext`. Before download, the screen claims its existing per-identity `_privateOpenInFlight` single-flight and captures both generations only while `resumed` and current; after every **pre-viewer-push** await, auto-continue requires `mounted`, current route, still-resumed state, and unchanged lifecycle **and route** generations. Pass a required **`DirectPrivateMediaContinuityGuard`** (captured generations plus a current classifier) into `prepare`; the controller checks it before qualification, immediately before lifecycle CAS/protection entry, and after every internal await. If invalid before acquisition, return typed failure with no lease. If invalid races with acquisition, no grant escapes: app-lifecycle invalidation terminalizes fail-closed, while route-only invalidation before first frame rolls back; app lifecycle wins if both changed. The intentional private-viewer push then consumes the continuity token, so its own push/pop does not invalidate handling of that viewer's operation-bound typed result. An invalidated download/prepare may cache bytes but must not reveal or push; it clears UI in-flight state and the next explicit tap requalifies. A production wiring test locks the single registered/scoped observer and every current direct-conversation construction path, not only an injected widget harness.
- E18: `prepare(identity, continuityGuard)` returns explicit **`DirectPrivateMediaPrepareResult.granted(grant)`** or `.failed(reason, settleResult?)`; expected failures never collapse to null. `settle()` returns/caches **`DirectPrivateMediaSettleResult {disposition, exitReason, firstFrameRecorded}`** with `rolledBackAvailable`, `terminalized`, `lostRollbackRace`, `noLease`, or `indeterminateFailClosed`. Persistence-exception reread is **operation-aware**: after an attempted rollback, the exact row proven `available` maps to `rolledBackAvailable`; after attempted terminalization, only a durably terminal/cleaned exact row maps to `terminalized`, never to rollback success. Move terminalization, exception reread, quarantine, and retry into one engine-owned settlement operation under a single attachment lifecycle lock; the controller must not compose separately locked calls. An exact `available` reread must not be handed to the existing generic reconciler, which intentionally skips available rows. While the original protection and lifecycle lock are still held, run a new direction/mode/message/attachment/path-qualified **`quarantineIndeterminateAvailable(identity)`** CAS from that exact `available` row back to persisted `opening`, then retry terminalization/cleanup. If durable terminal state is proved, return `terminalized`; if the retry remains uncertain, return `indeterminateFailClosed` with no View/Try Again and retain the persisted `opening` quarantine so the existing startup/resume reconciler can finish fail-closed cleanup. A failed quarantine CAS performs an authoritative reread and may accept only the same exact row already terminal or already quarantined; it never claims a changed identity or reports safe availability. Unreadable/otherwise ambiguous state is also `indeterminateFailClosed`. Repeated calls return the same lifecycle result and perform no second lifecycle transition, but result caching is independent from protection ownership: a first `settle(releaseProtection: false)` followed by a releasing call still exits protection exactly once. Settlement disposition alone does not mean failure: `_openDirectPrivateMedia` returns **`DirectPrivateMediaOpenResult.displayed(settleResult)`** after a real frame/normal close, or `.failed(reason, settleResult?, canRetry)` before display. Incoming protected may succeed with `noLease`; view-once may succeed with `terminalized` after its frame—neither renders failure. GIF maps to `MediaViewerKind.gif` and must satisfy exact image/GIF MIME qualification rather than falling through the image/video switch. For `.failed`, re-qualify fresh parent, direction/mode/identity, attachment status, integrity, path, and retry budget. `canRetry` is true only for (a) a fresh exact `done`, integrity/path-valid, lifecycle-open-eligible row after an allowed pre-frame failure/rollback, or (b) transient download `failed` below `kMaxDownloadRetries`; `download_failed`, `integrity_failed`, missing/mismatched attachment, lost authority, terminalized/lost-race/indeterminate settlement, or a non-retryable failure reason has neither View nor Try Again. Show the kind-correct failure title, receiver safe-view copy only for incoming `rolledBackAvailable`, and sender one-more-look-safe copy only for outgoing `rolledBackAvailable`. Both paths clear in-flight state.
- E19: rollback is an explicit pre-frame allowlist: protection-enter/revalidation failures during prepare, route-only continuity loss, `preFrameDecodeFailure`, and `routePushFailure` while `!firstFrameRecorded`. App lifecycle loss during held prepare, capture/background/dispose, and every post-first-frame exit remain fail-closed terminalization. Both route-push exits await and propagate the settle result, exit reason, and first-frame latch into truthful card state; this is new end-to-end wiring. Tests hold internal prepare awaits and inject (a) pause/resume, proving a raced lease terminalizes/no grant or push, and (b) covering-route push/pop, proving route-only rollback/no grant or push and a later explicit open. They also cover rollback success, lost CAS race, and rollback versus terminalization persistence throws with operation-aware reread. The terminalize-throws/read-available case must prove the exact identity-qualified quarantine CAS persists `opening`; one variant retries to durable terminal+cleanup immediately, and another leaves the quarantined row, restarts the engine, and proves the existing reconciler then terminalizes and cleans it. Removing the quarantine step must leave `available` and fail the test. Repeated settle returns the same lifecycle result/no second lifecycle transition while a later protection release still occurs exactly once; normal post-frame completion, post-frame exception, capture/background failure, and copy for each disposition remain covered.
- E20: make lifecycle targets explicitly directional (`incoming`/`outgoing`) instead of overloading `requireIncoming`. Incoming `view_once` keeps its existing one-view lease; incoming `protected` remains repeat-openable and never enters the sender lease. Outgoing `protected` and `view_once` each enter the new sender-only one-more-look CAS lane (`is_incoming=0 AND private_media_mode IN ('protected','view_once')`). `PrivateMediaOpeningLease` records the qualified direction, mode contract, attachment identity, and path; every revalidation/first-frame/terminal transition must match them. Carry direction through qualification, engine target, recovery candidates, and cleanup. Mirrored stale-opening recovery consumes an abandoned sender lease; receiver behavior stays unchanged. Once sender consumption begins, render existing kind-neutral `private_media_notification_body` ("Private media") plus `private_media_sender_consumed`; do not infer kind from a deleted attachment or add a column/tombstone. Cleanup deletes canonical file/key/attachment, and DB-reopen tests assert the generic card. Test outgoing protected/view-once separately, deny their second sender opens, preserve incoming protected repeat-open and incoming view-once exactly-once, and open/settle incoming and outgoing GIFs through `MediaViewerKind.gif`. Validate a dangling path before lease/CAS: an outgoing already-sent row uses the sender-local-missing copy and has no active View/Try Again; the unsent outbox keeps its separate “Choose it again to send” body. Disappearing outgoing has no reopen button.

**S5 — §04 feedback system**
- E21: orbit invite outcomes ON the row: port group-list's `_inviteRowOutcomes` map + ghost rendering into orbit, replacing all three families—accept result switches, accept exceptions (retryable live row), and decline-commit success/not-found/expired/error (live or ghost outcome)—instead of only the main snackbar switch. Install outcomes **after** any awaited reload that clears derived maps. Table-driven tests assert each localized row/ghost state and no SnackBar.
- E22: "Ask for a new invite" on live-expired and ghost rows (orbit + group list): snapshot inviter peer ID/name before deletion, but qualify only when `contactRepo.getContact(peerId)` returns active, unblocked, **non-archived** `ContactModel`; ML-KEM alone is insufficient. Re-resolve on tap. Orbit extends its opener with `initialText`; GroupList gets a narrow injected launcher and hides the button when that seam is absent. If contact authority disappears, retain the row, replace action with localized unavailable state, and do not navigate. ML-KEM-only non-contacts remain follow-up. Tests cover live-expired, deleted ghost, missing launcher, and contact-loss race.
- E23: standardized undo bar: extract a shared `core/widgets` undo bar (message, window, onCommit, onUndo) replacing BOTH decline-undo snackbar+timer implementations; **visible duration aligned to `kDeclineUndoWindow`** (mockup: bar matches commit timer; the current +1s grace and its already-committed no-op guard are retired together).
- E24: quiet pill: restyle `showQuietConfirm` (`quiet_confirm.dart:18-38`) into the centered pill; route the contact-profile copy snackbar (`contact_profile_screen.dart:90-113`) through it. The third callsite (group media-saved confirm) inherits the restyle — enumerated, intended.
- E25: mic permission bottom sheet: rewrite `showMicPermissionDeniedPrompt`'s builder (dialog → sheet), preserving `mic-perm-not-now`/`mic-perm-open-settings` keys and the `MicPermissionGateway` deep-link.

**S6 — l10n + copy contract**
- E26: in S6a/step 1, land **all** new keys in the Copy Contract below—not only the early S1/S3 subset—across en+ar+de with @-placeholder metadata, then run `flutter gen-l10n`, the exact all-key contract, and the parity gate. RTL locks are extended with their owning UI slices.

**S7 — executable device/relay proof**
- E27: add manifest-owned capability **`android.connectivity_restore_media_outbox`** with a dedicated production-driving adapter, not a legacy facade scenario. Add debug protocol/controller, SIMS adapter, strict validator/mutations, adapter contract, discovery, manifest, and proof binding. `intro_e2e_runner.dart` dispatches before generic health/drain; `main.dart` injects into mounted `ConversationWired`; Wired exposes only an E2E-gated run/message/attachment-ID seam and returns `PRIVATE_MEDIA_OUTBOX_E2E_ARMED` before host network mutation. Host IDs select an in-app tiny JPEG and production send/retry; ADB JSON control is allowed, but host media-payload/evidence transfer, fake repos/network, or raw DB insertion is forbidden. Correlate hashed IDs across `PENDING_RETRIER_NETWORK_RESTORED_TRIGGER`, `MEDIA_UPLOAD_LEASE_CLAIMED`, new safe `MEDIA_ENCRYPTION_PREPARED` (emitted after real artifact creation, no crypto material), `MEDIA_UPLOAD_START`, `CHAT_MSG_SEND_SUCCESS`, and receiver `PRIVATE_MEDIA_OUTBOX_E2E_RECEIVED`; post-restore window begins at restored trigger. The successful claim for the run-bound attachment must carry enum `MediaUploadTriggerSource.networkRestored`, serialized as exact wire field `source=network_restored`; mere ordering after the restored event cannot be satisfied by full/periodic/resume/manual competitors. Phase 1 PASS keys on **CAUSALITY, not a stopwatch** (user-locked v4 decision): the source-qualified claim sits between the restored trigger and retry `MEDIA_UPLOAD_START`; the trigger→start latency is RECORDED in the artifact as an informational trend field and can never fail the gate (claim→start alone remains insufficient as the causal witness because it could hide dispatch delay). Phase 2 dwells ≥31s, pauses/resumes, proves queued/no-red, restores, and delivers with zero UI action. Counter derivation (v4 — the counters are now the issuance-gate's own discriminator): the initial offline send legitimately encrypts/issues one failed upload; phase 2's offline pause/resume must contribute ZERO attempts BECAUSE E10's issuance gate skips automatic attempts while the OS reports no network (without the gate, un-gated resume Step 8b — `handle_app_resumed.dart:683`, no online precheck anywhere in the retriers — fires a third pair and these counters false-RED). Each phase therefore expects **2 encryption-prepared + 2 upload requests + 1 envelope + 1 receive**, exactly **1 `network_restored` token claim, encryption, and retry upload post-restore**, and phase 2 additionally asserts **zero attempts issued between pause and restore** — a third encrypt/upload pair IS a regression of the issuance gate. Why a NEW capability rather than extending the existing text campaign (v4 defense): the existing validators are text-protocol-hardcoded (`android_connectivity_restore_campaign.dart:56-97`) and cannot express media attachment/encryption/lease events by catalog registration alone; `relay-mutation:staging` is precedented in `critical_features.json`. Proof chain: the device proves persisted queued STATUS + wire delivery; host catalog-11 owns status→UI rendering — together they close the honest-offline claim. Artifacts contain hashes/safe counters/events/durations/booleans only—never paths, keys, ciphertext, peer IDs, or bytes.
- E28: extend `direct_private_media_device_local_journey_harness.dart` so sender and recipient roles mount localized production `ConversationScreen`. Device assertions are trimmed to the DEVICE-UNIQUE contract (v4 trim: the per-fixture geometry battery and per-field mutation matrix re-proved host-deterministic composition that catalog-1 already gates on every host run): per keyed fixture the harness asserts production `ConversationScreen`/LetterCard actually mounted, exactly one keyed private slot inside its decorated LetterCard body with non-zero size, no `Image`/`RawImage`/`MediaGrid` **within that slot**, and the expected action visible — against REAL-device-SQLCipher-produced fixture states (outgoing, incoming, and attachment-deleted terminal after re-pump). Fine-grained decoration introspection, width-cap geometry, and exhaustive per-field mutation coverage remain HOST-owned by catalog-1/3 (do not duplicate them on-device; do not use global screen-level negatives that can fail on unrelated avatars/backgrounds). Add the trimmed exact fields to both role artifacts, bump schema, enforce exact types/values, and mutation-test the schema (revert E1 → slot ancestry fails; inject a visual `DecorationImage` into a fixture → the slot-scoped no-pixel assertion fails). The pinned runner validates the combined device artifact; host criteria test only validates that strict validator.

Out of scope (deferred, named owner):
- **SHARE-lane queued media** (audit finding): `share_batch_delivery_coordinator.dart:1234-1240` maps upload-null to terminal `ShareBatchTargetStatus.failed` (second upload site `:1374`) — the §03 lie survives on the share surface. E9 types the share caller (so the classifier is AVAILABLE there), but the share batch UI has its own status model; a share queued-lane is follow-up "share offline outbox" (owner: share coordinator). E16 still migrates its promise string.
- Replace-attachment-on-failed-message ("Choose photo") → conflicts with the same-blob-ID contract; follow-up "terminal media re-pick" owns it (GAP-7). This plan ships Retry (fixed) + Remove.
- Expiry-mode sender reopen → needs a sender-side expiry decision that doesn't exist (GAP-2).
- Structured resend-request signal to unreachable non-contact inviters → decline-ack-shaped follow-up.
- OS-level connectivity loss-edge stream (`connectivity_signal.dart` extension) → banner v1 uses `p2pService.stateStream`; airplane-mode fidelity follow-up.
- QR copy surface (debug-only), removed-from-group row (done by 259/GAP-8), mockup's duplicate top-of-chat removal banner (single-surface rule).
- **Bridge-bypass upload lanes** (v4): posts attach (`attach_post_media_use_case.dart:288`), pass-post-along (`pass_post_along_use_case.dart:595`), and group avatar (`group_avatar_storage.dart:129`) call `callP2PMediaUpload` directly — distinct blob domains, no conversation attachment rows, no queued-outbox semantics. E9's typed outcome, E10 projection, and E11 leases deliberately do NOT apply; INV-260-10 is scoped to conversation/share lanes.
- **Background outbox delivery** (v4, user-requested named follow-up): delivering the queued outbox while the app is BACKGROUNDED when connectivity returns. Foreground-only today by platform reality — the Dart isolate is suspended in background, so the restored-edge stream cannot fire. Android is feasible-with-effort (WorkManager NETWORK_CONNECTED wake + running the Go node in a background process); iOS is architecturally constrained (no connectivity-triggered background execution for a custom P2P transport; background URLSession covers only plain HTTP). Owner: dedicated follow-up plan. 260 guarantees honest queued state + zero-tap delivery on next foreground.

## Files To Inspect Next
Production: `lib/features/conversation/presentation/widgets/letter_card.dart`; `lib/features/conversation/presentation/screens/conversation_screen.dart`, `conversation_wired.dart`, `direct_private_media_viewer.dart`; `lib/features/conversation/application/direct_private_media_viewer_controller.dart`; NEW `lib/features/conversation/presentation/navigation/direct_private_media_route_observer.dart` stable observer/scope; `lib/core/lifecycle/handle_app_paused.dart`, `handle_app_resumed.dart`; `lib/features/conversation/application/recover_stuck_sending_messages_use_case.dart`; `lib/core/database/helpers/media_attachments_db_helpers.dart`, `group_messages_db_helpers.dart`, `messages_db_helpers.dart`; `lib/features/conversation/presentation/widgets/compose_area.dart`; `lib/features/conversation/application/upload_media_use_case.dart`, `retry_incomplete_uploads_use_case.dart`, `retry_failed_messages_use_case.dart`; `lib/features/groups/application/retry_incomplete_group_uploads_use_case.dart`, `retry_failed_group_messages_use_case.dart`; `lib/core/media/media_upload_in_flight_tracker.dart`; NEW direct/group upload-retry projection capabilities + DB transactions; `lib/core/services/pending_message_retrier.dart`; `lib/core/media/private_media_policy.dart`, `private_media_lifecycle_engine.dart`; `lib/features/groups/presentation/screens/group_conversation_screen.dart`, `group_conversation_wired.dart`, `group_list_wired.dart`, `group_list_screen.dart`; invite/orbit/feedback surfaces including `lib/features/contact_profile/presentation/screens/contact_profile_screen.dart`; `lib/core/debug/intro_e2e_runner.dart`; `lib/main.dart` production route-observer + E2E wiring; NEW private-media outbox E2E protocol/controller; `tool/sims/manifest.dart` target-lock validation; share coordinator; ARBs + generated l10n.
Direct tests (extended in place): `conversation_wired_test.dart`, `conversation_wired_bg_task_test.dart`, `conversation_wired_offline_send_ux_test.dart`, `direct_private_media_viewer_test.dart`, `direct_private_media_placeholder_body_test.dart`, `conversation_private_media_composer_test.dart`, `group_conversation_wired_test.dart`, `group_conversation_wired_bg_task_test.dart`, `group_list_wired_test.dart`, `orbit_wired_test.dart`, `mic_permission_prompt_test.dart`, `upload_progress_banner_test.dart`, `test/features/contact_profile/presentation/screens/contact_profile_screen_test.dart`, `test/features/conversation/application/retry_failed_messages_media_reupload_test.dart`, `test/features/conversation/application/send_voice_message_durable_copy_test.dart`, `test/core/database/helpers/messages_db_helpers_stuck_sending_test.dart`, `test/features/conversation/application/consume_private_media_use_case_test.dart` (fake-lane engine tests — extended for rollback ordering).
NEW test files: `test/features/conversation/presentation/screens/direct_private_media_card_test.dart`; `test/features/conversation/presentation/screens/direct_private_media_route_observer_wiring_test.dart`; `test/features/conversation/application/media_outbox_queued_lane_test.dart`; `test/core/database/helpers/direct_media_upload_retry_projection_test.dart`; `test/core/database/helpers/group_media_upload_retry_projection_test.dart`; `test/core/database/helpers/private_media_lifecycle_cas_test.dart`; `test/core/debug/private_media_outbox_e2e_test.dart`; `test/core/debug/intro_e2e_runner_private_media_outbox_test.dart`; `test/core/lifecycle/main_private_media_outbox_e2e_wiring_test.dart`; `test/l10n/private_media_ux_strings_test.dart`; `test/core/widgets/undo_bar_test.dart`; `test/core/widgets/quiet_confirm_test.dart`. Existing wiring coverage extended in `test/core/lifecycle/main_resume_group_upload_wiring_test.dart`.
Device tier: NEW manifest-owned `integration_test/scripts/run_connectivity_restore_media_outbox_sims.dart` plus its production debug protocol/driver and artifact criteria; `tool/sims/critical_features.json`; `tool/sims/device_criteria.dart`; SIMS contract tests/discovery; `integration_test/direct_private_media_device_local_journey_harness.dart`, runner, validator, and criteria test. `run_1to1_device_real.dart` remains support infrastructure and is not registered as a selectable scenario runner.

## Existing Tests Covering This Area (workflow-inventoried)
- `conversation_wired_test.dart` — upload-failure red-lane pins (:1229/:1320 'Failed to upload media. Try again.', :8382 quote-restore, :8646/:8652 cancel-path negatives), schedule_send queued glyph (`conversation_wired_offline_send_ux_test.dart:1008` — v4 citation fix, previously misattributed to this file), harness `pumpScreen` (:958-1042) with injectable `uploadMediaFn` + `FakeP2PService.emitState` (fake reconnect signal EXISTS — `fake_p2p_service.dart:121`).
- `conversation_wired_offline_send_ux_test.dart` — S1 offline TEXT snackbar shape + TC-185-01 retriable-'sent' (KEPT sentinels; text UX unchanged).
- `direct_private_media_viewer_test.dart` — placeholder shapes (:906,:930,:952,:816) + the two view-once lease tests (:322,:367) this plan deliberately flips.
- `direct_private_media_placeholder_body_test.dart` (untracked, curated at run_test_gates.sh:222) — 259 reassurance bodies; bodies are REUSED by the card → extended, not flipped.
- `conversation_private_media_composer_test.dart` — ValueKey/copy-driven (NOT PopupMenu-structural); :284 typing-reset never opens the picker.
- `group_conversation_wired_test.dart` — group selector labels (:3585), upload-failure quote restore (:10169), relay progress (:10429).
- `orbit_wired_test.dart` — accept-no-snackbar (:3646, stays green), decline-undo snackbar pins (:3771,:3829,:3971-3972,:3976-4022 — flip to undo bar), ar accept-notFound l10n (259 TC-10).
- `mic_permission_prompt_test.dart` — AlertDialog-shape pins (:45,:63-71,:92,:106 — flip to sheet, keys survive).
- `upload_progress_banner_test.dart` — percent+title (extends).
- `retry_failed_messages_media_reupload_test.dart:388` currently pins relative paths as unresolvable (P7 flip); `send_voice_message_durable_copy_test.dart:109` and the wired durable-voice assertion around `conversation_wired_test.dart:7706` pin an absolute canonical failure copy rather than pre-bridge container-move-safe staging (P8 flip).
- `conversation_wired_bg_task_test.dart` and `group_conversation_wired_bg_task_test.dart` preserve balanced background-task begin/end across typed retryable/terminal outcomes and unexpected throws; both are affected preservation suites, not optional smoke.
- Real-SQLCipher CAS precedent: `test/core/database/helpers/messages_db_helpers_stuck_sending_test.dart` (sqfliteFfi fixture) — the model for the NEW `private_media_lifecycle_cas_test.dart`; the lifecycle ENGINE is fake-lane tested in `consume_private_media_use_case_test.dart` (no real-DB CAS test for the private-media helpers exists on HEAD — a gap this plan closes). The device-local journey CRITERIA test `test/integration/direct_private_media_device_local_journey_criteria_test.dart` is curated at run_test_gates.sh:226.
- E2E: no automated real-bridge media-delivery proof exists. The manifest-owned text connectivity campaign demonstrates the physical-Android+emulator resource/build pattern, but its text-only protocol/validator cannot be extended by catalog registration alone; TC-37 adds a dedicated protected-media campaign.
Already in curated arrays: ONE_TO_ONE_TESTS (:21) holds conversation_wired (:83), offline_send_ux (:93), share_target_picker_wired (:194), viewer (:220), placeholder_body (:222); GROUP_TESTS (:320) holds group_conversation_wired (:336), orbit_wired (:339). NOT in arrays (auto-glob): composer test, banner test, share_target_picker_screen; mic test + new core-widget tests run via core-host-all (core/** is NOT auto-globbed into curated gates).
Missing coverage gaps closed by this plan: no geometric blank-bubble/overlay regression; no stage classifier, owner-lane collision, atomic projection, token-race, no-bridge voice, or reconnect proof; no production-rendering device artifact; no lifecycle-generation/typed-settle/direction-mode tests; no deleted-ghost action identity; no undo-bar component tests.

## Existing Pins To Update (shipped WITH their production slice — expected reds otherwise)
| # | File:line | Old pinned behavior | New assertion | Slice |
|---|---|---|---|---|
| P1 | `conversation_wired_test.dart:1229,:1320,:8382` | red + composer/quote restore on upload failure | retryable lane: parent sending + upload_pending, queued copy, no snackbar, composer/active quote remain cleared while parent `quotedMessageId` survives; terminal variant keeps red | S3 |
| P2 | `direct_private_media_viewer_test.dart:322,:367` | view-once terminalizes on EVERY non-decode exit; only pre-frame decode rolls back | pre-frame failures (incl. protection-enter, revalidation, route-push) roll back to available; post-first-frame still consumes. `consume_private_media_use_case_test.dart:259` ("route exit before first frame consumes") FLIPS with this slice — it encoded the pre-E19 policy; its replacement asserts pre-frame route exits roll back at the engine tier (v4 disposition) | S4 |
| P3 | `orbit_wired_test.dart:3771,:3829,:3971-3972,:3976-4022` | decline-undo SnackBar + action + de label | shared undo bar (key `undo-bar`), visible == commit window, same undo/commit semantics | S5 |
| P4 | `mic_permission_prompt_test.dart:45,:63-71,:92,:106` | AlertDialog shape | bottom sheet shape; same keys, copy, gateway spy | S5 |
| P5 | `group_conversation_wired_test.dart:3585` | single-line group selector popup labels | same labels inside the group sheet (counts re-checked) | S2 |
| P6 | 259's `conversation_private_media_composer_test.dart` catalog-1 counts (chip 2×) | trigger-chip double-render arithmetic | sheet scrims the composer: 'Keep in chat' counts re-derived (chip + sheet option) | S2 |
| P7 | `retry_failed_messages_media_reupload_test.dart:388` | bulk retry treats a relative `upload_pending` path as inherently unresolvable and skips it | rewrite this pin through the single-message/manual entry: `MediaFileManager.resolveStoredPath` finds and uploads it; add a separate bulk assertion that skips the same pending row because incomplete retry owns it | S3b |
| P8 | `send_voice_message_durable_copy_test.dart:109` + `conversation_wired_test.dart:7706` | failed direct voice upload writes an absolute canonical `/media/` path after the failed request | voice is durably staged under container-move-safe `pending_uploads/<message>/<attachment>` before bridge use; queued rendering resolves that stored path, reconnect promotes it, and no raw absolute-path assumption remains | S3a |

## Copy Contract (authoritative; ar/de Claude-curated; ALL with @-metadata where placeholders appear)
§01 sheet: `private_media_sheet_title_photo/_video/_gif` ({name}) "How should {name} see this photo?"/…video/…GIF; `group_private_media_sheet_title` "How should members see this photo?"; `private_media_set_expiry` "Set an expiry"; `private_media_expiry_choose_detail` ({name}) "Disappears from {name}'s phone after a time you choose."; `private_media_delete_after` "Delete after"; `private_media_duration_1h/1d/7d` "1 hour"/"1 day"/"7 days"; `private_media_use_mode_cta` ({mode}) "Use {mode}"; `private_media_summary_change` "Change"; `private_media_summary_{ordinary,protected}_detail` "Normal photo · can be saved or shared"/"Viewable again · no saving or sharing"; `private_media_summary_view_once_detail` ({name}) "One view for {name}"; `private_media_summary_expiry_detail` ({duration}) "No saving or sharing · deleted after {duration}"; `private_media_disclosure_protected/_view_once/_expiry` ({name}) — screenshot-free bodies per Accepted Differences; `private_media_disclosure_reopen` "You can reopen it once here after sending." (S4-gated).
§02 cards: `private_media_card_title_protected_photo/_video`, `_view_once_photo/_video`, `_expiry_photo/_video` ({duration}); `private_media_open_photo/_video` "Open photo"/"Open video"; `private_media_view_photo/_video` "View photo"/"View video"; consumed sender cards reuse existing `private_media_notification_body` ("Private media", deliberately kind-neutral after cleanup) plus `private_media_sender_consumed` "You've used your one more look"; gif reuses photo strings before terminal cleanup.
§03: `offline_send_promise` "Will send when you're back online" (byte-identical only for the offline text snackbar and queued direct/group bubble); `offline_retry_delayed` "Delivery delayed — retrying automatically"; `share_stored_offline_promise` "Stored — will send when you're back online."; `offline_banner_title` "You're offline"; `offline_banner_body` "Messages and media will send when you're back online."; `media_sending_automatically` "Sending automatically…"; `media_uploading_percent` ({percent}) "Uploading photo · {percent}%" (+ `media_uploading` fallback "Uploading photo…"); `media_view_once_not_viewed` "Not viewed yet."; `media_missing_terminal_title_photo/_video` "Photo is no longer on this phone"/"Video is no longer on this phone"; `media_missing_terminal_body` "Choose it again to send."; `media_remove` "Remove". Banner and share copy are intentionally distinct keys/strings.
§04/§05: `private_media_open_failed_title_photo/_video` "Couldn't open this photo"/"Couldn't open this video"; `private_media_open_failed_view_safe` "Your one view is still available." (incoming rollback only); `private_media_open_failed_reopen_safe` "Your one more look is still available." (outgoing rollback only); `private_media_sender_local_missing_body` "Your sent media can't be reopened on this phone."; `private_media_try_again` "Try again"; `group_invite_ask_new` "Ask for a new invite"; `group_invite_request_new_draft` ({groupName}) localized prefilled request for a new invite; `group_invite_contact_unavailable` "This contact is no longer available." for the render→tap authority-loss race.
Arabic/German values are authored in the same S6a change using the existing private-media glossary and the exact placeholder schema above; there is no external scratchpad dependency. The focused l10n test locks every plan key, exact English, locale presence, and placeholder names/types; `l10n_integrity_test.dart` locks global parity.
Placeholder types are authoritative in every ARB: `{name}`, `{mode}`, `{duration}`, and `{groupName}` are `String`; `{percent}` is `int`. No locale may omit or change a type.

## RED Test Catalog (add BEFORE production code per slice — INV-RED-FIRST)
1. NEW `direct_private_media_card_test.dart`::`private media renders one cohesive in-bubble card, never an empty bubble` **(THE regression, TC-01/02)**
   - Tier: widget. Pump conversation screen (existing harness) with an outgoing protected image message AND an incoming one.
   - RED on HEAD: placeholders are Column SIBLINGS of LetterCard — `find.descendant(of: find.byType(LetterCard), matching: find.byKey(ValueKey('private-media-outgoing')))` findsNothing (same for the open placeholder).
   - GREEN: for each keyed message, exactly one shell and one slot exist (two shells/two slots total in this fixture); each slot is below its own keyed decorated body, has width/height >0, and its rectangle is contained by that shell. Within each keyed slot assert no `Image`, `RawImage`, or `MediaGrid` descendant, and inspect that message's keyed visual `BoxDecoration.image == null` directly (`DecorationImage` is not a Widget). This locks the blank-shell regression and AD-1 without a vacuous or global finder.
   - Mutation: revert E1 slot mount → red.
2. same file::`card shows visual header and media-kind title` — RED: no visual header/kind param exists. GREEN: mode-icon header + 'Protected photo' vs 'Protected video' by mediaType; a GIF-kind message asserts the PHOTO strings render (locks AD-10); the meta row asserts NO sender-name text (locks AD-2). Mutation: revert E2 kind param.
3. same file::`card preserves reactions and context overlay` — RED: long-press on placeholder region opens nothing. GREEN: reaction chips render; the lifted overlay root contains exactly one newly-built private slot with non-zero rect geometrically contained by its selected LetterCard decorated body, plus the same slot-scoped no-pixel assertions as catalog-1. A test that merely finds the overlay/reactions or descendant ancestry without geometry is insufficient. Mutation: omit/misplace the slot in either selected-message build → red.
4. same file::`terminal and unsupported cards keep action rows in-bubble` (TC-05). RED: terminal placeholder is a sibling. GREEN: descendant + reply/info/delete rows work; the terminal-missing media card asserts NO 'Choose photo' affordance exists (locks AD-4). Mutation: revert E4.
5. `direct_private_media_placeholder_body_test.dart`::extended `card bodies render localized and RTL-safe under ar` (TC-07) — RED: new card layout absent. GREEN: ar bodies + header inside card under RTL without overflow. Mutation: drop ar entries.
6. `group_conversation_wired_test.dart`::`group private slot is non-zero inside the decorated card and preserves tap target` (TC-06) — RED: text substitute/no slot. GREEN: keyed group slot is a non-zero descendant geometrically contained by the decorated LetterCard body, then `group-private-open-*` remains tappable. Mutation: move the panel to a sibling or revert E3.
7. `conversation_private_media_composer_test.dart`::`summary chip opens sheet; CTA commits; scrim cancel keeps prior policy` (TC-08/09) — RED: PopupMenu commits instantly on option tap; no CTA exists. GREEN: option tap only marks provisional (policy callback NOT yet fired — discriminator), CTA fires callback once; scrim-dismiss fires nothing. Mutation: revert E5 commit gating.
8. same file::`expiry mode reveals duration chips only when selected` — RED: 6 flat items, no panel. GREEN: 'Delete after' + 3 chips visible only under expiry; emits `PrivateMediaPolicy.disappearing(86400)` etc. Mutation: revert E5 nesting.
9. same file::`disclosure is per-mode, base copy makes no blocking claim, exact platform note is present` (TC-11) — RED: no disclosure in menu. GREEN: scope the negative assertion to the base disclosure key/body only; separately assert the exact existing iOS and Android capture-limit strings via a testable platform override. Do not globally search for “screenshot,” because the truthful platform notes contain that word. Mutation: add a blocking claim to the base disclosure or omit either platform note.
10. `group_conversation_wired_test.dart`::`group sheet parity with name-free title` (TC-12) — RED: group popup. GREEN: sheet + group title key. Mutation: revert E8.
11. NEW `media_outbox_queued_lane_test.dart` + real helper tests::`typed transport failure stays queued and survives every owner-qualified sweeper` (TC-13) — direct parent stays sending (group queued_offline), attachment upload_pending, composer remains cleared, quote is not restored, persisted `quotedMessageId` is unchanged, no red, queued copy reconstructs. Invoke pause before pause-flush, aged direct recovery, and both group cutoff forms. Add opposite-lane same-ID cases and 51-parent pause batch. Mutations: restore composer/quote, move exclusion after flush, reuse limited loader, or remove owner predicates → red.
11g. same file + retriers::`offline app-open cycles never burn the ceiling; automatic issuance is gated on OS connectivity` (TC-13g, v4 — locks the user-decided mechanism) — with the OS probe reporting no network, N≥`kMaxUploadRetries` resume/periodic passes issue ZERO claim/encrypt/upload and the parent stays direct `sending`/group `queued_offline` + `upload_pending` + queued copy (never red); a connectivityRetryable failure from a user-initiated attempt leaves `uploadRetryCount` unchanged; a boundedRetryable failure still increments; manual Retry bypasses the probe and may attempt while offline without burning the budget. Mutations: count a connectivity failure toward the ceiling, remove/invert the probe gate, gate the manual path, or classify a FAILURE from the probe → red.
12. same file + existing `upload_media_use_case` and direct/group background-task tests::`stage-typed failures cannot be reclassified by relay state` (TC-14) — real use case: known/unknown bridge or transport throw **after upload request issuance** while ambient relay looks ready → retryable; missing/unreadable, encryption, durable-copy, or pre-request unknown while relay is down → terminal. Separately, an unexpected throw from an injected `UploadMediaFn` at the consumer boundary is normalized to terminal `consumerBoundary/UNEXPECTED_CONSUMER_THROW`, then invokes the same projection exactly once. Caller spies cover every upload-capable direct/group consumer — including BOTH group-composer sites (`:2279` AND `:2733`) individually (v4); representative direct and group consumers use real abort-trigger repositories to prove the normalized catch rolls back attachment+parent together rather than stranding pending media. Wired assertions prove only typed retryable outcomes queue; `conversation_wired_bg_task_test.dart` and `group_conversation_wired_bg_task_test.dart` prove background ownership ends exactly once for retryable, terminal, and thrown outcomes. Mutation: collapse stages, consult relayReady, conflate/bypass the consumer-boundary outcome, or release only on one result variant → red.
12v. same file + `group_conversation_wired_test.dart`::`offline voice upload joins the queued lane only after durable pending ownership` (TC-13v) — cover direct no-bridge, direct typed transport failure, and group transport failure. Each retryable case persists a durable `upload_pending` row before returning queued, then a reconnect delivers without UI action. Force durable-copy failure and assert terminal red. Mutation: skip no-bridge promotion or E10v → red.
13. same file + tracker/retrier tests::`all direct and group upload paths share one token-owned, source-typed lease and restored legs isolate failure` (TC-15) — first prove the 1s restored trigger invokes direct and group media with `MediaUploadTriggerSource.networkRestored`. Hold a direct foreground upload, then a separate group foreground upload, while firing restored/full/manual competitors. GREEN: all-or-none claim; foreign release fails; the winning claim records the correct source; each case performs one encryption/upload/envelope and later reclaims after release. Restored callback variants prove (a) unacked throws but direct+group run, (b) direct throws but group runs, and (c) a later restored signal runs after either failure. A full/periodic/resume/manual claim after the restored marker cannot satisfy the causal assertion. Mutation: omit group foreground ownership, restore check-only tracker, drop the source/per-leg guards, or rely on retrier-local booleans → red.
14. failed-retry use-case, wired, retrier, and main-wiring tests::`every failed-message entry resolves relative paths and respects automatic/manual ownership` (TC-17) — rewrite P7 as a single/manual retry with a real relative stored path and manager, proving resolution/upload; a separate bulk/automatic case proves `upload_pending`, `upload_failed`, and `upload_cancelled` produce zero claim/upload/projection while `done` may retry only the envelope. A `ConversationWired` manual Retry proves manager propagation; hold a competing lease and prove failed manual `tryClaimAll` leaves parent/terminal rows byte-identical. Direct/group multi-attachment cases mix pending, rearmable terminal, done, cancelled/typed-terminal, and missing-source rows: only the wholly qualified unfinished set can claim and rearm atomically, with no subset mutation. A `PendingMessageRetrier` default-path test proves automatic propagation/partition; source/wiring assertions cover both `main.dart` construction paths and the group manual sibling. Mutation: mutate before claim, omit any manager/tracker caller, let bulk steal a pending/terminal row, or partially rearm → red.
15. same file::`background retry promotes before envelope send and cleans staging only after success` (TC-18) — hold `sendChatMessage` after upload returns; before release, assert DB `done.localPath` resolves to an existing canonical file outside `pending_uploads`. Release successful send, then assert staging deleted and canonical retained. A send failure leaves staging available for retry. Mutation: copy after send or clean before success → red.
16a. same file + NEW direct/group projection tests::`legacy failed normalization and terminal projection are atomic in the correct lane` (TC-23a) — attempts 1-2 retain their queued parent + `upload_pending`; inputs starting `failed + upload_pending` atomically normalize to direct `sending`/group `queued_offline` and render non-red. Attempt 3 commits parent failed + attachment `upload_failed`. For each repository, install a trigger aborting the parent update and prove attachment rollback. Same-ID opposite lane stays untouched. Move parent to delivered between load/projection and prove a full no-op; test incoming/missing too. Group covers sending, queued_offline, failed eligibility. Projection spies in every **upload-capable** direct/group composer, voice, incomplete-retry, and manual failed-media caller assert exactly one projection call for typed and normalized consumer-boundary failures plus zero legacy sequential writes; representative direct/group throw cases traverse the real abort trigger. Automatic failed retry is explicitly excluded here because E12 makes it zero-upload. Mutation: split/bypass projection/catch normalization, omit parent CAS/owner, leave legacy failed red, or omit an upload-capable caller/group twin → red.
16b. failed/incomplete retrier + wired tests::`terminal upload budget cannot be bypassed by the later failed-retry leg` (TC-23b) — after attempt 3, the remainder of that same full pass and the next full pass perform zero claim/projection/encrypt/upload/send for the `upload_failed` attachment. A missing source or typed-terminal row whose count is below `kMaxUploadRetries` has no Retry. With a wholly qualified multi-attachment set, an explicit manual action wins `tryClaimAll` first, then atomically rearms direct/group parent+attachments while holding the lease and starts a new bounded cycle; a held competitor, failed parent CAS, or one ineligible sibling leaves the whole terminal projection byte-identical. Automatic execution alone never resets the count. Mutations: mutate before claim, let bulk consume pending/terminal, forge/reset automatically, partially rearm, or split writes → red.
17. `conversation_wired_test.dart`::`offline banner seeds current state and follows both relay edges` (TC-20) — initially-offline pump shows the banner without a stream event; online→offline shows it; offline→online clears it. Group twin. Mutation: rely on the non-replay stream alone or omit an edge → red.
18. `conversation_wired_test.dart`::`uploading bubble state during retry lane` (TC-16) — RED: no per-message uploading projection. GREEN: `media_sending_automatically` (+percent when mapping verified) then sent status. Mutation: revert E14.
19. l10n sweep + focused rendered-copy test::`offline promises use their authoritative keys` (TC-21) — RED: hardcoded English remains. GREEN: grep finds no hardcoded promise literals outside generated l10n; the text snackbar and queued bubble both resolve `offline_send_promise` byte-for-byte, while banner and share surfaces resolve their intentionally distinct keys. Mutation: hardcode a literal or point either shared surface at another key.
20. `direct_private_media_viewer_test.dart` + `conversation_wired_test.dart` + NEW route-observer wiring test::`single tap auto-opens only under uninterrupted lifecycle/route continuity, including held prepare` (TC-24) — hold download, assert opening/progress is visible in-card, double-tap, and prove one download/one eventual launch. Download-held variants (a) pause→resume and (b) push→pop a covering route assert no prepare/lease/reveal/push. Prepare-held variants pause after lease start (terminalized fail-closed, no grant/push) and push→pop during an internal await (rolled back, no grant/push); later explicit tap requalifies and opens once. The wiring test proves one stable observer instance is both registered and scoped by production `MaterialApp`, and enumerates every current direct-conversation construction path as scope-backed—not optional injection. Mutation: omit progress/single-flight, either generation, internal guard, invalidation precedence, or production observer registration → red.
21. same files::`typed open success is distinct from settle disposition; failures gate truthful Retry` (TC-25) — successful incoming protected closes with `noLease` and successful post-frame view-once closes `terminalized`, with no failure card; incoming and outgoing GIF fixtures qualify `image/gif`, open as `MediaViewerKind.gif`, and settle normally while using photo copy. Incoming pre-frame rollback shows `private_media_open_failed_view_safe`; outgoing protected/view-once rollback shows only `private_media_open_failed_reopen_safe`. Try Again has two positive fresh-row branches: (a) an allowed pre-frame failure with exact `done`/integrity/path-valid/open-eligible media, including a successful rollback or protected `noLease`, and (b) transient download `failed` with `downloadRetryCount < kMaxDownloadRetries`. Pre-render and post-failure table rows for non-allowed reasons, `download_failed`, `integrity_failed`, missing/mismatched attachment, mode/direction/authority loss, lost race, terminalized, and `indeterminateFailClosed` show neither View nor Try Again and make no safety claim. Exception tables distinguish rollback intent from terminalization intent: terminalize-throws followed by readable `available` never reports rollback success; it exact-CAS quarantines that unchanged identity to persisted `opening`, then either proves immediate terminal+cleanup or remains indeterminate until a restarted engine reconciles it to durable terminal+cleanup. A changed identity is never claimed. Repeated settle performs no second lifecycle transition, while `settle(releaseProtection:false)` followed by release exits protection exactly once. Photo/video titles remain exact. Both route-push exits propagate results. Mutation: infer failure from disposition, skip reason/status/path/budget/authority qualification, make reread operation-blind, omit/loosen quarantine identity or restart recovery, collapse GIF, suppress later protection release, or always render Retry/safe copy → red.
22. same file (P2 flip)::`view-once pre-frame route failures roll back while app-lifecycle loss fails closed` (TC-26) — RED (as rewritten): current behavior consumes every non-decode exit. GREEN: protection-enter/revalidation/route-only continuity/route-push pre-frame failures roll back to `available`, reopenable with direction-correct safety copy; a pause/background race during held prepare terminalizes and never claims availability. Mutation: revert E19 or treat lifecycle and route invalidation identically.
23. NEW `test/core/database/helpers/private_media_lifecycle_cas_test.dart`::`direction-aware CAS refuses after reveal; sender modes are one-shot; incoming protected stays repeatable` (TC-28/30) — sender CAS touches only `is_incoming=0` and claims both protected/view_once; each denies a second sender open; incoming protected opens repeatedly without a lease; incoming view-once security remains unchanged; stale sender-opening recovery consumes only sender targets. After cleanup and DB reopen, attachments are absent and the generic consumed-private-media card contract remains renderable without kind metadata. Mutations: drop direction, narrow sender mode, or lease incoming protected → red.
24. `conversation_wired_test.dart`::`unmounted route push no longer consumes the view` (TC-27) — RED: routePushFailure settle terminalizes. GREEN: rolls back pre-frame. Mutation: revert E19's wired fix.
25. NEW in card/wired tests::`sender mode matrix, generic cleanup, GIF parity, and honest missing legacy path` (TC-29) — outgoing protected/view-once each open once, deny second open, and after cleanup/restart render existing generic title+caption; incoming protected repeats, incoming view-once remains one-shot, and incoming/outgoing GIF opens use `MediaViewerKind.gif` with photo copy. Disappearing outgoing has no action. A dangling outgoing canonical path returns typed local-missing presentation with `private_media_sender_local_missing_body`, no unsent “Choose it again to send” copy, no lease/CAS/throw, and no active View/Try Again button. Mutation: wire one mode, collapse/reject GIF, depend on terminal kind, use unsent copy, or claim before path validation → red.
26. `orbit_wired_test.dart` table::`every migrated invite outcome survives reload as row/ghost/navigation without snackbar` (TC-31) — kept live rows: repairPending, retryable bridgeError, thrown accept/decline errors while invite exists. Terminal ghosts: notFound, expired, expiredFreshness, revoked, alreadyUsed, wrongIdentity, invalidPayload, duplicateGroup, and successful/expired decline. Navigating outcomes: success/materialized-group recovery with no row feedback. Force the awaited reload/late pending-invite refresh after every outcome and assert intended state remains; SnackBar absent in every row. Mutation: leave any accept/exception/decline site on snackbar or install outcome before reload → red.
27. `orbit_wired_test.dart` + `group_list_wired_test.dart`::`live-expired and deleted-ghost Ask actions resolve the exact active contact` (TC-32) — test both row forms, two simultaneous ghosts, exact peer resolution, and localized group-named draft. Negatives: blocked, archived, ML-KEM-only/non-contact, missing GroupList launcher hide the button; contact removed between render/tap yields unavailable copy, removes action, and never navigates. Mutation: implement ghost only or omit inviter/contact/launcher qualification → red.
28. NEW `undo_bar_test.dart` + orbit/group pins (P3)::`undo bar visible duration equals commit window; undo restores; commit deletes` (TC-33) — RED: component absent. GREEN: 4s alignment, both callsites migrated, undo/commit semantics preserved. Mutation: revert E23.
29. NEW `quiet_confirm_test.dart` + existing `contact_profile_screen_test.dart`::`quiet confirm is a centered pill; contact-profile copy routes through it` (TC-34) — RED: full-width snackbar + contact-profile bespoke snackbar. GREEN: helper pill shape assertions; tapping both peer-ID and safety-number copy surfaces still writes the exact clipboard bytes, renders the quiet pill through the shared helper, and leaves no bespoke `SnackBar`. Mutation: revert the E24 restyle or either callsite migration.
30. `mic_permission_prompt_test.dart` (P4 rewrite)::`denied mic prompt is a bottom sheet with working Open Settings` (TC-35) — RED (rewritten): dialog shape. GREEN: sheet + keys + gateway spy invoked. Mutation: revert E25.
31. NEW `test/l10n/private_media_ux_strings_test.dart` + existing `l10n_integrity_test.dart` (TC-36) — the new test lists every plan-260 key, exact English contract, all locale presence, and `@key.placeholders` names/types; it REDs before keys land. The existing integrity test proves global key/placeholder-set parity. Mutation: drop an Arabic key or one placeholder metadata entry.
32. **PROD-CRITICAL** manifest capability `android.connectivity_restore_media_outbox` (TC-37) — physical Android sender under the explicit control lock + emulator recipient, real bridge/relay, production endpoint with armed ack. Phase 1 PASS = causal chain (run-bound `source=network_restored` claim between restored trigger and retry `MEDIA_UPLOAD_START`) + exact counters + delivery; trigger→start latency is recorded informationally, never gating (v4 user-locked). Phase 2 ≥31s + pause/resume remains queued/no-red with **zero attempts issued between pause and restore** (the E10 issuance-gate discriminator — un-gated resume Step 8b would fire a third encrypt/upload pair), then exact delivery with zero UI action. Per phase totals: 2 encryption-prepared events, 2 upload requests, 1 envelope, 1 receive; post-restore: exactly 1 wire `source=network_restored` token claim + 1 encryption + 1 retry upload. RED: protocol/dispatch/wiring/capability and the physical-control manifest validator support are absent. Mutations: relabel a full/periodic claim as the causal winner, issue an attempt during the offline resume, remove E11/sweep/token ownership, or reject `device-control:android-physical` in target-lock validation → red. Registration: stable manifest, dispatch/wiring tests, discovery, validator, adapter, proof binding.
33. Device-local production card journey (TC-38): extend the actual integration harness to mount `ConversationScreen`/LetterCard and emit run-bound measurements for outgoing, incoming, and attachment-deleted terminal fixtures. The pinned physical+emulator runner creates and validates the device artifact; host criteria mutation-tests its schema. RED: current harness imports only `Locale` and never pumps production UI. Mutation: revert E1 → slot ancestry/geometry fails.

## Test Coverage Matrix (ZERO empty cells)
| Spec case | Behavior props | Tier | Test file::name | RED reason on HEAD | Mutation revert | Acceptance gate cmd | Harness registration |
|---|---|---|---|---|---|---|---|
| TC-01/02 blank-bubble regression | UI composition | widget | direct_private_media_card_test::catalog-1 | placeholders are Column siblings; empty shell renders | revert E1 | `flutter test test/features/conversation/presentation/screens/direct_private_media_card_test.dart` | AUTO (glob) + **add to ONE_TO_ONE_TESTS** |
| TC-03 kind titles/visual header | UI + param plumb | widget | card_test::catalog-2 | no kind param | revert E2 | same | same file |
| TC-04 affordances preserved | UI gesture parity | widget | card_test::catalog-3 | placeholder outside gesture surface | revert E1 | same | same file |
| TC-05 terminal/unsupported in-bubble | UI | widget | card_test::catalog-4 | siblings today | revert E4 | same | same file |
| TC-06 group in-card slot + tap key | geometry/gesture parity | widget | group_conversation_wired_test::catalog-6 | text substitute/no keyed slot | move slot to sibling/revert E3 | focused group wired + groups gate | GROUP_TESTS |
| TC-07 ar/RTL card | l10n/RTL | widget | placeholder_body_test::catalog-5 | new layout absent | drop ar entries | `flutter test …/direct_private_media_placeholder_body_test.dart` | already in ONE_TO_ONE_TESTS (:222) |
| TC-08/09 sheet + CTA commit | UI state machine | widget | composer_test::catalog-7 | popup commits instantly | revert E5 | `flutter test …/conversation_private_media_composer_test.dart` | AUTO (glob) |
| TC-10 chip re-derivation | UI derived state | widget | composer_test (:284 stays + chip asserts) | n/a green sentinel + new asserts | revert E6 | same | AUTO |
| TC-11 disclosure honesty | copy + platform branch | widget | composer_test::catalog-9 | no disclosure | revert E7 | same | AUTO |
| TC-12 group sheet | UI sibling | widget | group_conversation_wired_test::catalog-10 | group popup | revert E8 | `./scripts/run_test_gates.sh groups` | GROUP_TESTS |
| TC-13 queued lane + owner-qualified sweeps | typed outcome + persisted projection | use-case/widget/SQL | queued-lane + pause/stuck/group helper tests | untyped null → red; sweepers reap or cross-lane-exempt | E9/E10 or owner predicate | focused S3 commands | add affected direct tests to ONE_TO_ONE_TESTS; group helper to GROUP_TESTS |
| TC-13v voice queued lane | pre-bridge durable ownership | use-case + widget | queued-lane + voice + group tests | no-bridge returns failed before pending row | E10v/staging | focused voice + queued + groups commands | direct array + GROUP_TESTS |
| TC-13g offline ceiling/issuance gate (v4) | budget exemption + OS-probe attempt gating | use-case/unit | queued-lane + retriers catalog-11g | resume passes attempt while offline; connectivity failures increment count | remove probe gate or count a connectivity failure | focused queued/retrier commands | same suites as TC-13/TC-15 (already in arrays) |
| TC-14 stage classifier | real outcome + background-release discriminator | unit/use-case | upload_media_use_case + queued-lane + direct/group bg-task catalog-12 | stages collapse to null/relay guess | consult relay/collapse stages/leak task | focused upload + queued + both bg-task commands | upload already listed; add queued/direct bg to ONE_TO_ONE_TESTS and group bg to GROUP_TESTS |
| TC-15 reconnect/token ownership | direct/group foreground cross-trigger concurrency + causal source/leg isolation | unit/integration | tracker + retrier + queued/group catalog-13 | fast leg absent; tracker check-only/source-free | E11 token/source/per-leg fast leg | focused tracker/retrier/queued/group commands | direct tests in ONE_TO_ONE_TESTS; group twins in GROUP_TESTS; tracker under core-host-all |
| TC-16 uploading bubble state | progress projection | widget | conversation_wired_test::catalog-18 | no per-message tracking | revert E14 | `./scripts/run_test_gates.sh 1to1` | ONE_TO_ONE_TESTS |
| TC-17 relative-path + retry-ownership fix | all automatic/manual caller paths | unit | retry_failed_messages + media_reupload + group/wired catalog-14 | verbatim existsSync; bulk steals pending/terminal; missing wiring | omit manager/partition/rearm guard | focused failed-retry/media-reupload/group/queued commands | add affected direct tests to ONE_TO_ONE_TESTS; group failed retry to GROUP_TESTS |
| TC-18 promote-before-cleanup | file lifecycle | integration (real files) | same::catalog-15 | done-row points at deleted dir | revert E13 | same | same |
| TC-19 honest terminal missing-file | terminal cause + Remove reuse | widget | conversation_wired_test (extend) | generic failure copy; Remove path asserts rows+files deleted via existing `_onDeleteFailedMedia` | revert E12 detection | 1to1 gate | ONE_TO_ONE_TESTS |
| TC-20 offline banner | signal→UI | widget | conversation_wired_test::catalog-17 (+group twin) | no banner | revert E15 | 1to1 + groups gates | arrays |
| TC-21 promise authority | l10n/render + source sweep | widget/script | catalog-19 | hardcoded/forked keys | re-add literal or wrong key | rendered-copy test + corrected `rg` gate | direct test array + literal gate |
| TC-22 not-viewed-yet line | UI copy | widget | conversation_wired_test (extend catalog-11) | line absent | revert E10 view-once branch | 1to1 gate | ONE_TO_ONE_TESTS |
| TC-23a/b retry budget/terminal projection | crash atomicity + lane/parent CAS + same-pass terminal partition/manual rearm | real-SQL + caller spies/use-case | direct + group projection tests + catalog-16a/16b | sequential writes/red early/bulk bypass | split/bypass transaction/remove owner/parent/partition guard | both projection + queued/direct/group retry commands | core-host-all; caller suites in ONE_TO_ONE/GROUP_TESTS |
| TC-24 single-tap open | visible progress + single-flight + internal lifecycle/route continuity + production observer | widget/wiring | viewer + wired + route-observer catalog-20 | invisible two-tap; no uninterrupted-continuity guard | omit progress/single-flight/internal guard/registration | three focused UI/wiring commands | ONE_TO_ONE_TESTS |
| TC-25 open-result truth + conditional Try again | typed prepare/open/settle, GIF, two retry branches, intent-aware exception/copy authority, durable indeterminate quarantine | widget + real-SQLCipher | viewer + wired + lifecycle-CAS catalog-21 | null/void outcomes; disposition conflated with failure; GIF rejected; bare available reread cannot reconcile | collapse result/skip exact qualification/quarantine/restart recovery | same + lifecycle-CAS command | ONE_TO_ONE_TESTS + core-host-all |
| TC-26 pre-frame rollback (P2 flip) | route rollback vs lifecycle fail-closed semantics | widget + real-SQLCipher | viewer_test::catalog-22 + private_media_lifecycle_cas_test | consumes every non-decode exit without typed distinction | conflate route/lifecycle or revert E19 | same + `flutter test test/core/database/helpers/private_media_lifecycle_cas_test.dart` | ONE_TO_ONE_TESTS + AUTO (core/** glob → core-host-all) |
| TC-27 route-push/pre-frame truth | wired result propagation | widget | conversation_wired_test::catalog-24 | push failure consumes/result discarded | revert E19 wiring | 1to1 gate | ONE_TO_ONE_TESTS |
| TC-28/30 CAS locks | double-view + lane isolation | real-SQLCipher helper tier | private_media_lifecycle_cas_test::catalog-23 | sender helpers absent; CAS guards unlocked | drop `revealed_at IS NULL` | `flutter test test/core/database/helpers/private_media_lifecycle_cas_test.dart` | AUTO (core/** glob → core-host-all) |
| TC-29 sender one-more-look | direction×mode/kind + cleanup + sender-local missing copy | widget + real-SQL | card/wired catalog-25 + lifecycle CAS | outgoing denied; direction/GIF absent | one mode/kind/copy dependency/path claim | card/wired + CAS commands | ONE_TO_ONE_TESTS + core-host-all |
| TC-31 all invite outcomes | table-driven row/ghost/navigation after reload | widget | orbit_wired_test::catalog-26 | snackbar-only/derived map cleared | leave any branch or pre-reload write | focused orbit + groups gate | GROUP_TESTS |
| TC-32 ask-new-invite action | live+ghost identity, contact and launcher authority | widget | orbit+group_list catalog-27 | outcome loses inviter; no launcher | ghost-only/omit authority | focused orbit/group-list + groups gate | GROUP_TESTS |
| TC-33 undo bar | shared component + timing | widget | undo_bar_test + P3 flips | duplicated snackbar impls; +1s drift | revert E23 | `flutter test test/core/widgets/undo_bar_test.dart` + groups gate | AUTO core (core-host-all) + GROUP_TESTS pins |
| TC-34 quiet pill | restyle + callsite port | widget | quiet_confirm + contact_profile_screen catalog-29 | full-width snackbar; bespoke contact-profile | revert E24/helper call | both focused tests | core test under core-host-all; profile test under feature-host-all |
| TC-35 permission sheet | presentation swap | widget | mic_permission_prompt_test (P4) | dialog shape | revert E25 | `flutter test test/core/permissions/mic_permission_prompt_test.dart` | AUTO core (core-host-all) |
| TC-36 l10n contract | exact all-plan keys/English/placeholders + parity | host test | private_media_ux_strings + l10n_integrity | plan keys absent | drop locale/@placeholder | exact l10n tests in S6a | register focused test; later host-all |
| TC-37 **PROD-CRITICAL** reconnect outbox | real OS network + bridge/relay + restored-source causality | SIMS device proof | stable campaign + dispatch/arming/manifest-target-lock tests | protocol/control seam and physical-control validation absent | E11/sweeper/token/source/dispatch | stable-ID SIMS command | core dispatch/main wiring + manifest/discovery/validator/adapter/proof binding |
| TC-38 device-local production card | production per-fixture render + slot-visual no-image truth | Android device-local | pinned `run_direct_private_media_device_local_journey.dart` + produced artifact criteria | current harness never mounts UI | revert E1/insert visual DecorationImage/globalize assertions | literal pinned runner, then host criteria test | existing runner; extend harness/schema/validator |

## Blind-Spot Sweep
- **Lifecycle / derived-state durability**: queued UI derives from owner-qualified persisted attachment status and survives pause/stuck/group sweeps, including cross-lane same-ID counterexamples. Terminal attachment+parent projection is transactionally atomic. TC-37 proves ≥31s + background/resume on-device. Sender-consumed state persists on the message row, but terminal media kind intentionally does not; DB-reopen renders generic private-media copy. Offline banner seeds currentState then follows live stream (no persistence claim).
- **Sibling-surface consistency**: enumerated everywhere — group fork (E3/E8/E10 twins, C8), share coordinator (E16 string only; share banner untouched this plan — named), upload-banner 3 mounts (percent already landed by 259), both viewers' capture-limit copy reused (E7). Reactions asymmetry (1:1 keeps, group redacts) is pre-existing and NOT changed — locked by catalog-3 (1:1) and group tests' existing redaction pins.
- **Destructive-action side-effects**: TC-19 asserts Remove reuses `_onDeleteFailedMedia` (rows + files + message deleted, nothing else); E23's undo-commit asserts the decline actually deletes ONLY after the window; catalog-15 asserts cleanup deletes the pending dir but PRESERVES the canonical copy.
- **Invariant re-verification under new transitions**: E19 verifies typed/cached settle results, rollback CAS, and fail-closed post-frame/capture/background exits; E20 verifies all direction×mode combinations and generic post-cleanup UI; E11 verifies atomic token ownership against competing restore/full/resume/manual triggers. TC-38 proves production render composition rather than accepting booleans from a backend-only harness.

## Invariants (locked by tests)
- INV-260-1: a private-media message never renders an empty shell: one non-zero private slot lives inside one keyed decorated LetterCard body in the live row and lifted overlay, with no private pixel renderer → catalog-1/3 + TC-38.
- INV-260-2: `offline_send_promise` is byte-identical across the offline text snackbar and queued direct/group bubble. Banner body and share custody copy are intentionally distinct authoritative keys → catalog-19 rendered-copy assertions + literal sweep.
- INV-260-3: receiver view-once can never be viewed twice → catalog-23 CAS lock (revealed_at guard) + kept post-frame-consume pins.
- INV-260-4: outgoing protected/view-once reopen consumes exactly once in the sender lane; incoming protected remains repeatable; post-cleanup sender UI is kind-neutral and receiver rows stay untouched → catalog-23/25.
- INV-260-5: no dead buttons — every introduced action has a behavior assertion; viewer Try Again requires either fresh exact open-eligible `done` media after an allowed pre-frame failure or transient under-budget download failure; outbox Retry requires pending ownership or an all-or-none ceiling-count/fresh-source-qualified connectivity rearm (never a typed-terminal row); Ask-for-new requires fresh contact/launcher authority. Each disappears or transitions truthfully when authority is lost.
- INV-260-6: an owner-matched `upload_pending` row is never presented red or reaped by pause/stuck/group sweeps; an opposite-lane same-ID row grants no exemption; connectivity-disposition failures never increment the retry budget and automatic attempts are never issued while the OS reports no network, so NO number of offline app-opens can terminalize a queued row (v4) → catalog-11/11g/16 + TC-37 phase-2 zero-offline-attempt assertion.
- INV-260-9: direct/group retry projection first CAS-qualifies the outgoing parent, then commits owner-matched attachment/retry-count and parent-failed state in one transaction or not at all; a concurrently delivered/incoming/missing parent causes zero attachment mutation → catalog-16.
- INV-260-10: every **conversation/share-lane** direct/group foreground/retry upload owns an atomic token through persistence and envelope settlement; competing restore/full/periodic/resume/manual triggers cannot re-encrypt the same stable blob, and TC-37 accepts only the `networkRestored` claim as its causal winner (the posts/group-avatar `callP2PMediaUpload` bypass lanes are named out of scope — v4 scoping) → catalog-13 + TC-37 counters/source.
- INV-260-11: download **and internal prepare** can yield an auto-open grant only under the same uninterrupted resumed lifecycle and route continuity; a raced app-lifecycle loss terminalizes while a route-only pre-frame loss rolls back, and route-current/mounted at completion alone are insufficient → catalog-20/22.
- INV-260-7: no screenshot-blocking claim on any surface → catalog-9 discriminator (+ 259's catalog-5 stays green).
- INV-260-8: ValueKey continuity (`private-media-selector`, `private-media-option-*`, `group-private-open-*`, mic keys) → existing tests stay green unflagged.
- INV-RED-FIRST and INV-MUTATION-VERIFIED apply to every row.

## Step-By-Step Implementation Plan (slice order; per-slice RED → GREEN checkpoints)
0. Contract extraction: verify repo root + Flutter 3.41.4, snapshot `git status --short` (259's diff is in-tree—never revert), resolve the live device matrix, run the corrected zero-match sweep and focused baseline greens, capture fresh 1to1/groups counts, and write machine analyzer output to `/tmp/plan-260-analyze-before.txt`. **Baseline decision rule (v4)**: 259's gates are recorded UNPROVEN — a Step-0 red attributable to 259's in-tree diff is fixed forward inside the owning 260 slice (P6 already anticipates composer-count churn) or triggers a named STOP recorded in Execution Progress; never silently re-baseline. **Checkpoint commits (v4)**: record a commit on the working branch after Step 0 and after every slice checkpoint — a 7-slice multi-session execution on a heavily dirty tree must not be losable to one careless checkout.
1. **S6a (enabler)**: author catalog-31 RED, then land **every key in the complete Copy Contract** with en/ar/de placeholder metadata plus E16's promise-key migration → gen-l10n → exact all-key and parity tests GREEN. Checkpoint: catalog-19 rendered/sweep gate green, offline_send_ux sentinels still green (same rendered bytes). Catalog-19 and catalog-31 are authored and satisfied here—they are NOT re-redded in later slices (INV-RED-FIRST applies to catalog-11..18 only in S3).
2. **S1**: catalog-1..6 RED → E1-E4 → GREEN. Stop-if: LetterCard slot forces touching reaction/gesture internals beyond mount order → replan (do not fork a second bubble). Record one screenshot-vs-mockup (§02) comparison in Execution Progress at the checkpoint (v4 — the plan adopts a visual mockup; no gate otherwise ever looks at it).
3. **S2**: catalog-7..10 RED (+P5/P6) → E5-E8 → GREEN. E7's reopen sentence stays OFF until step 5 lands. Record one screenshot-vs-mockup (§01) comparison at the checkpoint (v4).
4. **S3 (three sub-slices — audit split; the offline_send_ux sentinel runs green at EVERY sub-checkpoint since S3 edits sit closest to the frozen text lane)**:
   - **S3a** (v4 split: FOUR micro-checkpoints, each RED→GREEN→pause with its focused suites + the offline_send_ux sentinel green — the prior single checkpoint bundled the exact failure family that killed v1 with a concurrency rewrite and a new transaction layer):
     - **S3a1**: catalog-12 RED → E9 (stage-typed outcome + shared `runUploadMedia` wrapper) → GREEN.
     - **S3a2**: catalog-11, 11g, 16a RED (+P1) → E10 (owner-qualified sweeps + issuance gate + atomic terminal projection/caller seam) → GREEN. Stop-if (v4): the sweeper exclusions cannot be expressed attachment-predicated (i.e. would change any non-queued-media row's semantics) → STOP, replan.
     - **S3a3**: catalog-12v RED (+P8) → E10v (pre-bridge durable voice staging) → GREEN.
     - **S3a4**: catalog-13 RED → E11 (token-owned upload registry + isolated reconnect media legs) → GREEN.
   - **S3b**: catalog-14, 15, 16b RED (+P7) → E12 (resolved paths + automatic/manual partition/rearm), E13. Catalog-16a remains green from S3a and is not re-assigned. Checkpoint + sentinel.
   - **S3c**: catalog-17, 18 RED → E14 (id↔blob mapping verified BEFORE its GREEN — fallback copy if unstable, decision logged), E15. Checkpoint + sentinel.
5. **S4** (v4 split: THREE micro-checkpoints so the security-critical CAS lane greens in isolation): **S4a** catalog-20 RED → E17 (observer/continuity) → GREEN; **S4b** catalog-21/22/24 RED (+P2 flips) → E18/E19 (typed settle + rollback truth) → GREEN; **S4c** catalog-23/25 RED → E20 (direction-aware CAS security lane, reviewed alone) → GREEN. Register and scope the one stable production route observer, add GIF qualification, exact status/budget authority, direction-specific copy, and persistence-exception settlement truth before flipping on E7's reopen disclosure line. Stop-if: sender-lane CAS requires ANY schema change → STOP, replan with DB v102 (grounding says columns suffice; treat contradiction as a blocker, not a detour).
6. **S5**: catalog-26..30 RED (+P3/P4) → E21-E25 → GREEN. Record one screenshot-vs-mockup (§04) comparison at the checkpoint (v4).
7. **S7 device tier**: author TC-37 protocol/adapter/strict validator/manifest contracts before the production endpoint, including the narrow `tool/sims/manifest.dart` change that lets `device-control:android-physical` satisfy `android.physical` and a mutation test that rejects the row when that support is removed. Discovery/list must show stable ID `android.connectivity_restore_media_outbox`, then run it through SIMS resolution/preparation on the pinned live physical-Android+emulator pair. Extend TC-38 harness/schema/validator, take its IDs from the successful SIMS report, run the pinned journey, and validate its produced artifact. Record N/A only from the policy-valid SIMS target-unavailable verdict.
8. Preservation + named gates + hygiene (Acceptance block), fill Execution Progress.

## Risks And Edge Cases
- LetterCard slot vs text+media private messages: private eligibility forbids captions (`allowsPrivateMedia` requires no text) — the slot never coexists with body text on HEAD; catalog-1 pins single-card rendering anyway.
- Sheet commit-on-CTA changes selection timing: `normalizePrivateMediaComposerPolicy` still normalizes on attachment change/typing — chip re-derivation (TC-10) covers the race.
- Reconnect flush double-upload: the atomic token tracker—not `_isRetrying`, `_isNetworkRestoredFlushing`, or a check-only probe—owns every direct/group foreground/retry blob through persistence and envelope settlement. Catalog-13 holds competing triggers, locks their typed sources, and mutation-tests foreign release/all-or-none claim.
- Route/lifecycle interruption during download or prepare: the one production observer increments continuity on both cover and uncover; returning to the same current route does not resurrect an old token. The controller's internal guard prevents a grant escaping a raced prepare: route-only loss rolls back, app-lifecycle loss terminalizes. Catalog-20/22 hold both boundaries.
- Settle persistence exception: authoritative reread is intent-aware. Only rollback-attempt+available proves `rolledBackAvailable`; terminalize-attempt+available exact-CAS quarantines the unchanged direction/mode/message/attachment/path identity to persisted `opening` before returning. Immediate retry may prove `terminalized`; otherwise the result stays `indeterminateFailClosed`, with no safe copy/Retry, and restart reconciliation must durably terminalize and clean the quarantined row. The generic reconciler is never asked to recover a bare `available` row. Unreadable/ambiguous state is likewise never safe/retryable. Lifecycle-result caching remains separate from exactly-once protection release.
- Rollback abuse: only pre-first-frame; post-frame consume pins stay; RECEIVER crash-recovery still fails closed (engine reconcile consumes stale 'opening'); the SENDER lane gets its own mirrored recovery-candidate leg (without it a crashed sender view strands a dead button — audit-corrected; the original "fails closed" claim was receiver-only) — catalog-23.
- Rollback-vs-reconcile race: a concurrent reconcile can consume the stale lease before rollback lands; rollback then returns false and the card must NOT claim "still available" (E18 truthfulness rule; catalog-21 variant). Rare, availability-shaped, accepted.
- Sweeper-exclusion subquery cost: use owner-qualified correlated `NOT EXISTS`, backed by `message_id`; both direct/group collision tests lock semantics. Profile only if needed—never drop owner correlation for speed.
- Sender reopen of a message the receiver already consumed: lanes are independent by design (sender's local copy; no wire effect) — catalog-23 lane isolation makes this explicit.
- Ask-new-invite requires a real active, unblocked contact; ML-KEM-only non-contacts hide the action. Re-resolve on tap and remove the action if contact authority disappeared — catalog-27.
- Undo-bar timing change (5s→4s visible): the retired +1s grace previously allowed a no-op undo tap; aligning removes that dead tap — P3 asserts alignment.
- Percent mapping (GAP-5): closed at plan time (stable id↔blob↔attachment contract verified in source — v4); fallback copy retained as contingency only.
- Connectivity-probe staleness (v4): a captive portal / stale "online" reading lets issuance proceed; the attempt fails connectivityRetryable and — because connectivity failures never increment — the budget is untouched. Honesty holds without ever consulting relay state for classification.
- Group `GroupPrivateMediaPolicy` type split: sheet is parameterized, no shared-type refactor (Scope Guard).

## Device/Relay Proof Profile
TC-37 is the required real wire boundary when the live topology exists; host tests cannot substitute for it. Resolve targets immediately before execution with `flutter devices --machine`, `adb devices -l`, `xcrun simctl list devices available`, and SIMS live-device resolution/preparation, then pin every command from the SIMS report. Project-default topology is one USB physical Android sender plus one Android emulator recipient; the physical sender owns the network-control lock so taking it offline creates the sender upload outbox while the emulator receiver remains reachable. Discovery on 2026-07-19 found `21071FDF600CSC` and emulators `emulator-5554`/`emulator-5556`; those IDs are current evidence, not permanent requirements, and a stopped-but-launchable AVD is not “absent.” No iPhone leg is required because neither claim is iOS-specific. If SIMS records the capability's strict target-unavailable verdict, record **`N/A (target unavailable by project policy)`** for both device legs; do not park the plan or call it a gap. Missing automation, invalid artifacts, present-but-unusable targets, missing relay configuration, or any non-target-unavailable SIMS verdict are BLOCKED/FAIL, not N/A. TC-38 uses the same SIMS-resolved Android pair. No DB migration is consumed. Scheduling note (v4): the existing text campaign locks the same physical phone as `device:android-physical` while TC-37 locks it as `device-control:android-physical`; name-based lock merging (`device_binding.dart:171-179`) does NOT mutually exclude two lock NAMES over one resolved target — a `sims major` run containing both campaigns must treat same-resolved-target-under-different-lock-names as a scheduling conflict and serialize them.

The TC-37 manifest record is locked to: `owner=conversation`, `lane=reliability`, `required=true`, proof boundary `android.os-network.private-media-outbox.relay-delivery`; assertions `network_restored_claim_is_causal`, `restore_retry_latency_recorded` (informational field, artifact-required, never gating — v4), `pause_resume_remained_queued`, `zero_offline_resume_attempts` (v4 — the issuance-gate discriminator), `one_post_restore_retry`, `expected_attempt_counts`, `zero_post_restore_ui_actions`, `receiver_exact_media_delivered`; modes `major/full`; families `1to1/media/transport`; command `dart run integration_test/scripts/run_connectivity_restore_media_outbox_sims.dart`; `buildProfile=android.e2e.main`; exact dependency `build.android.e2e.main`; resources `build:android.e2e.main/read`, `device-control:android-physical/exclusive` (sender network mutation), `device:android-emulator/exclusive`, `relay-mutation:staging/exclusive`, `artifact:private-media-outbox/write`; target capabilities `android.physical`, `android.emulator`, `adb.network-control`; `allowedNaReason=target_unavailable_by_project_policy`; `artifactRequired=true`; `artifactValidator=validatePrivateMediaOutboxRestoreArtifact`; `automationReady=true`; `active=true`; `declaredBuildException=false`. The same slice extends `_targetResourceErrors` so `android.physical` accepts either `device:android-physical` or `device-control:android-physical`; manifest/target-lock tests accept this exact mutation topology and mutation-fail when physical control support is removed.

## Acceptance Gates (literal — run from this repository root)
```bash
set -euo pipefail

# 0) Resolve toolchain, baseline, and live matrix
command -v flutter
command -v jq
flutter --version | head -1 | rg -F 'Flutter 3.41.4'
git status --short
flutter devices --machine > /tmp/plan-260-flutter-devices.json
adb devices -l > /tmp/plan-260-adb-devices.txt
xcrun simctl list devices available > /tmp/plan-260-ios-devices.txt

if rg -n -e 'not an active' -e "text\('Ordinary'\)" -e '25 B / 100 B' test \
  | rg -v 'reason:|findsNothing'; then
  echo 'stale literals remain' >&2
  exit 1
fi

./scripts/run_test_gates.sh 1to1
./scripts/run_test_gates.sh groups
flutter analyze --machine > /tmp/plan-260-analyze-before.txt || true

# 1) Focused RED/GREEN commands — each new/rewritten test first fails for its catalog reason
flutter test test/features/conversation/presentation/screens/direct_private_media_card_test.dart
flutter test test/features/conversation/presentation/screens/conversation_private_media_composer_test.dart
flutter test test/features/conversation/application/upload_media_use_case_test.dart
flutter test test/features/conversation/application/retry_incomplete_uploads_use_case_test.dart
flutter test test/features/conversation/application/retry_failed_messages_use_case_test.dart
flutter test test/features/conversation/application/retry_failed_messages_media_reupload_test.dart
flutter test test/features/conversation/application/send_voice_message_use_case_test.dart
flutter test test/features/conversation/application/send_voice_message_durable_copy_test.dart
flutter test test/features/conversation/application/media_outbox_queued_lane_test.dart
flutter test test/core/media/media_upload_in_flight_tracker_test.dart
flutter test test/core/services/pending_message_retrier_test.dart
flutter test test/core/lifecycle/handle_app_paused_test.dart
flutter test test/core/lifecycle/handle_app_paused_pause_flush_test.dart
flutter test test/core/database/helpers/messages_db_helpers_stuck_sending_query_test.dart
flutter test test/core/database/helpers/messages_db_helpers_stuck_sending_test.dart
flutter test test/core/database/helpers/group_messages_db_helpers_sending_test.dart
flutter test test/core/database/helpers/direct_media_upload_retry_projection_test.dart
flutter test test/core/database/helpers/group_media_upload_retry_projection_test.dart
flutter test test/core/lifecycle/main_resume_group_upload_wiring_test.dart
flutter test test/core/debug/private_media_outbox_e2e_test.dart
flutter test test/core/debug/intro_e2e_runner_private_media_outbox_test.dart
flutter test test/core/lifecycle/main_private_media_outbox_e2e_wiring_test.dart
flutter test test/features/groups/application/retry_incomplete_group_uploads_use_case_test.dart
flutter test test/features/groups/application/retry_failed_group_messages_use_case_test.dart
flutter test test/features/groups/presentation/group_conversation_wired_test.dart
flutter test test/features/conversation/presentation/screens/conversation_wired_bg_task_test.dart
flutter test test/features/groups/presentation/screens/group_conversation_wired_bg_task_test.dart
flutter test test/features/share/application/share_batch_delivery_coordinator_test.dart
flutter test test/features/conversation/presentation/screens/direct_private_media_viewer_test.dart
flutter test test/features/conversation/presentation/screens/conversation_wired_test.dart
flutter test test/features/conversation/presentation/screens/direct_private_media_route_observer_wiring_test.dart
flutter test test/core/database/helpers/private_media_lifecycle_cas_test.dart
flutter test test/features/orbit/presentation/screens/orbit_wired_test.dart
flutter test test/features/groups/presentation/group_list_wired_test.dart
flutter test test/core/widgets/undo_bar_test.dart
flutter test test/core/widgets/quiet_confirm_test.dart
flutter test test/features/contact_profile/presentation/screens/contact_profile_screen_test.dart
flutter test test/core/permissions/mic_permission_prompt_test.dart

# 2) l10n contract and literal ownership (after S6a and at final closure)
flutter gen-l10n
flutter test test/l10n/private_media_ux_strings_test.dart test/l10n/l10n_integrity_test.dart
if rg -n -e "Will send when you're back online" -e 'Stored — will send' \
  -e 'Delivery delayed — retrying' lib --glob '*.dart' \
  --glob '!app_localizations*.dart'; then
  echo 'hardcoded offline-promise literal remains' >&2
  exit 1
fi

# 3) Exact preservation sentinel + affected curated/family gates
flutter test test/features/conversation/presentation/screens/conversation_wired_offline_send_ux_test.dart
./scripts/run_test_gates.sh 1to1
./scripts/run_test_gates.sh groups
./scripts/run_host_test_gates.sh feature-host-all
./scripts/run_host_test_gates.sh core-host-all

# 4) Manifest/discovery contracts
./scripts/check_reliability_simulation_discovery.sh
./scripts/run_test_gates.sh sims major --list --format tsv \
  | rg '^android\.connectivity_restore_media_outbox\t'
flutter test test/integration/android_1to1_reliability_device_criteria_test.dart \
  test/tool/sims/sims_manifest_test.dart \
  test/tool/sims/sims_proof_binding_registry_test.dart
bash scripts/test/private_media_outbox_restore_sims_adapter_contract_test.sh
./scripts/run_test_gates.sh sims-contracts

# 5) Availability-bounded device proof. SIMS owns discovery/preparation, so a
# stopped-but-launchable AVD is prepared before any N/A decision. Any BLOCKED or
# FAIL exits here because the whole block is fail-closed.
PLAN260_TC37_ID=android.connectivity_restore_media_outbox
PLAN260_TC37_REPORT=/tmp/plan-260-tc37-report.json
SIMS_REPORT_PATH="$PLAN260_TC37_REPORT" \
  ./scripts/run_test_gates.sh sims major --only "$PLAN260_TC37_ID"
PLAN260_TC37_STATUS="$(
  jq -er --arg id "$PLAN260_TC37_ID" \
    '[.verdicts[] | select(.capabilityId == $id)][0].status' \
    "$PLAN260_TC37_REPORT"
)"

case "$PLAN260_TC37_STATUS" in
  PASS)
    PLAN260_PHYSICAL_ANDROID_ID="$(
      jq -er '.devices.assignments["device-control:android-physical"]' \
        "$PLAN260_TC37_REPORT"
    )"
    PLAN260_ANDROID_EMULATOR_ID="$(
      jq -er '.devices.assignments["device:android-emulator"]' \
        "$PLAN260_TC37_REPORT"
    )"
    adb -s "$PLAN260_PHYSICAL_ANDROID_ID" get-state | rg -x 'device'
    adb -s "$PLAN260_ANDROID_EMULATOR_ID" get-state | rg -x 'device'

    PLAN260_DEVICE_ARTIFACT_DIR="$(
      mktemp -d "${TMPDIR:-/tmp}/plan260-private-media.XXXXXX"
    )"
    export PLAN260_DEVICE_ARTIFACT_DIR
    dart run integration_test/scripts/run_direct_private_media_device_local_journey.dart \
      --sender "$PLAN260_PHYSICAL_ANDROID_ID" \
      --recipient "$PLAN260_ANDROID_EMULATOR_ID" \
      --artifact-dir "$PLAN260_DEVICE_ARTIFACT_DIR"
    ;;
  N/A)
    jq -e --arg id "$PLAN260_TC37_ID" '
      [.verdicts[] | select(.capabilityId == $id)][0]
      | .reason == "target_unavailable_by_project_policy"
        and .blocker == "targetUnavailable"
        and .targetCapabilityAvailable == false
    ' "$PLAN260_TC37_REPORT" >/dev/null
    printf '%s\n' \
      'TC-37=N/A (target unavailable by project policy)' \
      'TC-38=N/A (target unavailable by project policy)' \
      > /tmp/plan-260-device-disposition.txt
    ;;
  *)
    echo "BLOCKED: unexpected TC-37 verdict $PLAN260_TC37_STATUS" >&2
    exit 1
    ;;
esac
flutter test test/integration/direct_private_media_device_local_journey_criteria_test.dart

# 6) Normalized analyzer delta + hygiene
flutter analyze --machine > /tmp/plan-260-analyze-after.txt || true
dart tool/sims/sims.dart verify-analyzer-delta \
  /tmp/plan-260-analyze-before.txt \
  /tmp/plan-260-analyze-after.txt
git diff --check
```

## Known-Failure Interpretation
- Expected RED: every **new or deliberately rewritten** assertion fails before its production slice for the catalog reason. Existing preservation assertions (for example TC-10 re-derivation and text-send sentinels) stay green and are logged as preservation, not falsely manufactured REDs.
- Expected planned flips: P1-P8 — shipped with their slices, NOT drift. The two viewer lease tests (P2) flip DELIBERATELY (rollback is the feature).
- Pre-existing dirty: the ENTIRE 259 diff + light-theme edits + docker-ws artifacts — never revert.
- Device availability: absent required topology records `N/A (target unavailable by project policy)`. A present-but-locked/unusable target, missing driver/protocol, invalid artifact, or missing relay setup is BLOCKED/FAIL. Mac SDK lock contention is waited out, not killed.
- Scope drift (BLOCKING): any diff in receiver post-first-frame consume semantics, `upload_cancelled` lane, text-send paths, `connectivity_signal.dart` restored-edge contract, group membership/roster logic, relay/Go code.

## Done Criteria
- [ ] RED-first per slice for every new/rewritten behavior assertion; preservation sentinels remained green.
- [ ] Mutation-verified: every E# names its re-red row (matrix column complete).
- [ ] Blank-bubble regression (catalog-1) green; TC-38 pinned Android device-local artifact green, or N/A only when the required live topology is absent by project policy.
- [ ] TC-37 stable SIMS capability green: causal PASS (run-bound `source=network_restored` claim between restored trigger and retry start) + exact per-phase counters including phase-2 zero-offline-resume-attempts + receiver delivery, with trigger→start latency recorded informationally (never gating — v4 user decision); or N/A only for an actually absent policy-bounded topology. No host substitution for a present topology.
- [ ] All P1-P8 pins updated in place; preservation sentinels + 1to1/groups/feature-host-all/core-host-all at expected counts.
- [ ] en/ar/de parity + promise-literal sweep zero; analyze 0 new; git diff --check clean.
- [ ] Every new button demonstrably does what it says (INV-260-5 rows all green).
- [ ] Terminalization-throw/read-available proof green: exact identity quarantines durably to `opening`, no safe copy/Retry appears, and immediate or restarted reconciliation reaches terminal state plus cleanup.
- [ ] Registrations complete without duplicates: new card/queued/route-observer wiring, direct voice/durable-copy, retry/media-reupload, both stuck-sending causal suites, and direct wired background-task suite in ONE_TO_ONE_TESTS (existing upload test single-listed); group incomplete/failed retry, DB/wired/background-task suites in GROUP_TESTS; tracker/projection/main-wiring/lifecycle/debug-dispatch tests under core-host-all; the contact-profile callsite remains exact + feature-host-all; TC-37 manifest/proof-binding/discovery/validator/adapter contracts list the stable ID.

## Scope Guard (hard "Do not")
- Do not render real pixel thumbnails for private media anywhere (privacy-by-design; the card's icon-visual is the fix).
- Do not pass attachment objects, paths, bytes, or image providers into the private card slot.
- Do not exclude rows WITHOUT an `upload_pending` attachment from any sweep — text/stuck-send recovery semantics stay byte-identical for every non-queued-media row (the exclusions are attachment-predicated, never blanket).
- Do not omit `owner_lane` from a message-correlated media query, classify upload failures from `relayReady` OR from the OS connectivity probe (the probe gates attempt ISSUANCE only — v4), compose terminal attachment/parent writes sequentially, or use check-then-upload ownership.
- Do not let automatic/bulk failed retry upload `upload_pending`, `upload_failed`, or `upload_cancelled`; pending rows belong only to incomplete retry or an explicit fully qualified manual Retry. Manual terminal rearm must resolve/qualify the whole unfinished set, win `tryClaimAll`, and only then mutate parent+attachments atomically—never mutate before ownership or partially rearm.
- Do not introduce a new 1:1 message-status vocabulary value (rollback brick — audit B-1); the queued lane rides existing `sending` + attachment status.
- Do not weaken receiver view-once security: no rollback after first frame; do not touch the crash-recovery consume; do not alter `revealed_at` exactly-once semantics beyond adding the sender lane.
- Do not lease incoming protected media, retain a sender-consumed attachment tombstone, add a media-kind parent column, or render a kind-specific title after cleanup.
- Do not build "Choose photo" replace-attachment; do not add sender reopen for disappearing mode.
- Do not touch text-send paths, the offline TEXT snackbar bytes, or `upload_cancelled` exclusion.
- Do not merge `PrivateMediaPolicy` and `GroupPrivateMediaPolicy` types.
- Do not add screenshot-blocking claims; capture notes only via the two existing platform keys.
- Do not remove the whole-bubble group tap target or any listed ValueKey.
- Do not add a second undo mechanism or leave either old snackbar-undo implementation behind.
- Do not touch relay/Go/bridge code (the reconnect leg is Dart-side wiring of existing signals).
- Do not edit generated l10n by hand; do not run with any Flutter version other than the Step-0-verified 3.41.4 toolchain.

## Accepted Differences / Intentionally Out Of Scope (mockup ≠ shipped, each deliberate)
1. **No real thumbnail** in private bubbles — the mockup itself uses an icon-visual; "the thumbnail issue" closes as never-blank-card. (User-visible framing: private media deliberately never previews its pixels in the chat scroll.)
2. 1:1 card meta shows NO sender name (app-wide 1:1 rule; mockup's "iPhone-11 · 9:13" meta adapted to time+status only).
3. "Protected photo sent" title → title stays kind/mode; sent-ness lives in the status glyph (no duplicate narration).
4. "Choose photo" terminal action not shipped (ID-stability conflict; GAP-7) — Retry (fixed) + Remove are the honest recoveries; follow-up named.
5. Expiry-mode sender reopen + its disclosure sentence omitted (GAP-2).
6. Screenshot claims replaced by platform-true capture notes (existing keys) (GAP-1).
7. Removed-from-group §05 row: already live via 259 (GAP-8); mockup's extra top banner not added (single-surface rule); hard-delete snackbar branch kept.
8. QR copy stays debug-only; quiet-pill production targets are contact-profile + `showQuietConfirm` callsites (GAP-3).
9. Bubble percent contingent on upload-id mapping verification; pre-decided fallback "Sending automatically…" without percent (GAP-5).
10. Group sheet title is name-free; gif reuses photo strings.
11. After sender one-more-look consumption and sensitive attachment cleanup, the terminal card is deliberately kind-neutral (existing “Private media” title); exact photo/video/GIF identity is not retained.

## Dependency Impact
- Builds directly on 259's applied-in-tree keys/params (mode names, details, bodies, contactDisplayName); 260 must not fork a second mode-name set.
- The typed `UploadMediaFn` outcome (E9) is the shared seam future share/group outbox UX will consume (C8).
- Follow-ups named here: terminal media re-pick; non-contact resend-request signal; loss-edge connectivity stream; sender expiry semantics.

## Readiness Corrections Applied (2026-07-19 v3)
1. Replaced ambient relay-state guessing with stage-typed upload outcomes and exhaustive callers.
2. Added owner-qualified pause/direct/group sweep predicates, real cross-lane collision tests, and one transactional attachment+parent terminal projection.
3. Replaced check-only retry guards with token-owned upload leases across foreground, restored/full/resume, bulk, and manual paths; direct no-bridge voice stages durable pending media before queuing.
4. Made LetterCard proof geometric and required the same private slot in the lifted overlay; TC-38 now mounts production UI.
5. Added lifecycle-generation handoff, typed prepare/open/settle propagation, cached settlement truth, and direction×mode lifecycle tests.
6. Locked the no-migration product choice: sensitive cleanup deletes attachment metadata and sender-consumed UI is kind-neutral.
7. Added deleted-invite identity plus active-contact qualification; ML-KEM-only reachability is not claimed.
8. Replaced numeric/catalog SIMS prose with stable manifest capability `android.connectivity_restore_media_outbox`, a dedicated production driver, strict artifacts, literal pinned commands, and availability-bounded N/A semantics.
9. Corrected screenshot-copy scope, offline copy-key invariant, l10n placeholder proof, zero-match shell semantics, normalized analyzer delta, toolchain paths, and device policy.
10. Closed the final retry counterexamples: manual multi-attachment rearm claims the whole set before mutation, automatic retry is zero-upload/zero-projection, and unexpected consumer throws enter the same typed atomic projection as ordinary failures.
11. Made viewer continuity race-safe inside `prepare`, made persistence-exception rereads operation-aware, added an exact persisted `available→opening` quarantine plus restart recovery for indeterminate terminalization, widened truthful pre-frame Retry only to freshly requalified rows, and required one production-scoped route observer across every conversation entry path.
12. Bound TC-37 latency to restored-trigger→retry-start with a source-qualified claim in between, and made TC-38 inspect every decorated descendant inside each keyed private slot rather than a non-causal ancestor or global aggregate.

Verified foundations retained: the original blank-bubble root cause; no new message-status vocabulary; existing receiver first-frame/CAS authority; promote-before-cleanup requirement; group/direct policy type split; text-send and upload-cancel preservation; per-plan gate cadence without full host-all.

## Readiness Corrections Applied (2026-07-19 v4 — second /tdd-review `wf_43c68f97-753`, decisions locked with user)
1. **Offline honesty mechanics (user-locked)**: connectivityRetryable failures never increment the retry budget AND automatic attempt issuance is gated on an OS-connectivity probe (manual + restored-edge legs bypass); new catalog-11g locks ≥kMax offline app-open cycles staying queued. Closes the ceiling-burn hole where offline send + two resumes terminalized to red while still offline with every previously-planned test green.
2. **TC-37 re-derived (user-locked)**: per-phase counters are now the issuance-gate discriminator (2+2 with phase-2 zero-offline-resume-attempts asserted — un-gated resume Step 8b `handle_app_resumed.dart:683` would fire a third pair and false-RED); PASS keys on causality + exactly-once + delivery; trigger→start latency demoted to a recorded informational artifact field.
3. **Classification soundness**: MEDIA_ERROR split by errorMessage permanence (`bridge.go:1470` wraps everything; relay 5GB rejection + local open/stat → terminal, relay-unreachable → connectivityRetryable, unknown → boundedRetryable); encryption/durableCopy reclassified boundedRetryable (transient timeouts `bridge.dart:699/:732`; durable-copy runs after a successful upload); BRIDGE_TIMEOUT removed (the media path throws TimeoutException); PLATFORM_ERROR/INTERNAL_ERROR → boundedRetryable; rearm discriminator re-keyed to bounded-retryable exhaustion.
4. **Consumer topology corrected**: TEN wrapper sites enumerated individually (TWO group-composer sites `:2279`/`:2733`); group manual failed-media retry re-described as NEW wiring on the incomplete-retry seam (zero upload sites today); three `callP2PMediaUpload` bypass lanes (posts ×2, group avatar) named out of scope; INV-260-10 scoped to conversation/share lanes.
5. **Over-build trims (the user's audit lens)**: one shared `runUploadMedia` wrapper replaces ten per-consumer catches; `MediaUploadTriggerSource` carried only at the claim API/telemetry (not through entry-point signatures) and the finally-release clause reclassified as a preservation assertion (already true on HEAD); E15 reuses `ConnectionStatusIndicator`'s seed-then-subscribe derivation; TC-38 trimmed to the device-unique contract (host catalog-1 keeps the geometry/mutation battery); E14's mapping fallback retired (stable-ID contract verified); E10v's queued-no-bridge branch labeled defensive-only.
6. **Process hardening**: S3a split into four micro-checkpoints with a sweeper stop-if; S4 into three with the E20 CAS security lane isolated; Step-0 baseline decision rule for 259-attributable reds; checkpoint commit per slice; screenshot-vs-mockup checkpoint per UI slice (§01/§02/§04); SIMS same-target/different-lock-name serialization note; two citation fixes (offline_send_ux `:1008` attribution, group consts `:3423-3424`); `consume_private_media_use_case_test.dart:259` disposition pinned in P2; background outbox delivery recorded as a named follow-up (Android feasible with WorkManager, iOS architecturally constrained).
Refuted by the v4 audit (do not re-introduce): the claimed `.devices.assignments` gate/report schema mismatch is FALSE — `tool/sims/sims.dart:405-412` + `tool/sims/report.dart:190-206` emit exactly the field the acceptance gate reads; no S7 schema work is needed.

## Arbiter Decision
Structural blockers from BOTH 2026-07-19 reviews are closed in the plan. The second review's user-locked decisions (issuance gate + no offline ceiling burn; causality-keyed TC-37 with informational latency) are incorporated, its five over-build trims are applied, and its refuted finding (report schema) is recorded so it is not re-litigated. E14's mapping question is closed at plan time. Accepted differences: 11. The plan is ready to execute slice-by-slice, subject only to the stop conditions and live availability policy stated above.

## Final Execution Verdict
execution-ready plan; implementation and evidence pending
