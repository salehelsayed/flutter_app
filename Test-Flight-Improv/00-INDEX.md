# Test-Flight-1 Improvement Reports

Generated: 2026-04-04

**Closure note:** Sessions `1` through `29` are closed execution artifacts.
`session-30-plan.md` is a plan-only narrow reopen artifact and should not be
read as landed work. `session-31-plan.md`, `session-32-plan.md`,
`session-34-plan.md`, `session-35-plan.md`, `session-36-plan.md`, and
`session-37-plan.md` are completed narrow closure sessions, including the
intro-to-Orbit / intro-to-Feed follow-up closure from Session `35`. Sessions
`44` through `47` are completed Report `24` execution/closure artifacts, with
`24-cancel-media-upload-session-breakdown.md` and the stable messaging closure
references carrying the maintenance-time meaning. Sessions `54` and `55` are
completed Report `27` execution/closure artifacts, with
`27-persistent-nav-bar-orbit-session-breakdown.md` carrying the
maintenance-time meaning. Session `56` is the completed Report `28`
execution/closure artifact, with
`28-orbit-intro-badge-session-breakdown.md` carrying the maintenance-time
meaning. Sessions `58` and `59` are the completed Report `29`
execution/closure artifacts, with
`29-batch-parallel-intro-sending-session-breakdown.md` carrying the
maintenance-time meaning. The doc-scoped Sessions `59` and `60` for Report
`30` are the completed Report `30` execution/closure artifacts, with
`30-swipe-nav-feed-orbit-session-breakdown.md` carrying the maintenance-time
meaning. The doc-scoped Session `1` for Report `34` is the completed Report
`34` execution/closure artifact, with
`34-orbit-intros-swipe-delete-missing-session-breakdown.md` carrying the
maintenance-time meaning. The doc-scoped Session `1` for Report `35` is the
completed narrow reopen/closure artifact for the late-boundary 1:1 video
cancel seam, with
`35-cancelled-video-upload-still-sends-session-breakdown.md` carrying the
maintenance-time meaning. The doc-scoped Sessions `1` through `3` for Report
`41` are the completed inbox-recovery and notification-open trust closure
artifacts, with
`41-notification-open-missing-incoming-messages-session-breakdown.md` plus the
refreshed 1:1 closure reference carrying the maintenance-time meaning. The
doc-scoped Session `1` for Report `44` is the completed Feed/Orbit handled
notification sync closure artifact, with
`44-feed-orbit-notification-desync-session-breakdown.md` carrying the
maintenance-time meaning. The doc-scoped Session `1` for Report `45` is the
completed Feed inline-reply viewport reorientation closure artifact, with
`45-feed-stack-card-does-not-reorient-after-inline-reply-session-breakdown.md`
carrying the maintenance-time meaning. The doc-scoped Session `1` for Report
`48` is the
completed automatic 1:1 inbox-drain fallback removal closure artifact, with
`48-gap3-remove-destructive-inbox-fallback-plan-session-breakdown.md` plus the
refreshed 1:1 closure reference carrying the maintenance-time meaning. The
doc-scoped Sessions `1` through `10` for Report `50` are the completed
two-simulator coverage-refresh artifacts, with
`50-two-simulator-user-journey-tests-todo-session-breakdown.md` plus the
refreshed audit/journey docs carrying the maintenance-time meaning. The
doc-scoped Sessions `1` and `2` for Report `55` are the completed Report `55`
execution/closure artifacts, with
`55-external-share-skip-post-and-multi-recipient-plan-session-breakdown.md`
carrying the maintenance-time meaning. The doc-scoped Session `1` for Report
`76` is the completed outbound 1:1 v2-only privacy closure artifact, with
`76-remove-legacy-1to1-plaintext-path-plan-session-breakdown.md` plus the
refreshed 1:1 closure reference carrying the maintenance-time meaning.
The doc-scoped Sessions `1` through `8` for Report `87` are the completed
app-wide light-theme readability closure artifacts, with
`87-app-wide-light-theme-readability-session-breakdown.md` carrying the
maintenance-time verdict `accepted_with_explicit_follow_up` for remaining
device/simulator visual evidence.
Roadmaps `15` and `16` plus the residual reliability/measurement sessions `24`
through `29` remain historical execution artifacts. Reports `01` through `14`
remain the rationale archive. The current closure state lives in Sections `8`,
`9`, and `10` below.

**Project-wide mobile target policy (2026-07-10):** Mobile device proof is
availability-bounded. Run only on USB-connected Android/iPhone targets and
available Android emulators/iOS simulators discovered at execution time. An
absent model, OS version, or API band is N/A and non-blocking; retain causal
host/native coverage for unavailable version branches. This rule governs Plan
227 and all future specs, plans, QA verdicts, and closure docs. Canonical
instructions live in `AGENTS.md` and `test-gate-definitions.md`.

---

## 1. Test Coverage

| Report | Focus |
|--------|-------|
| [01-unit-test-coverage.md](01-unit-test-coverage.md) | Corrected module-by-module unit test picture — broad coverage, narrower remaining gaps |
| [02-integration-test-coverage.md](02-integration-test-coverage.md) | Cross-feature flows, integration quality, and remaining boundary gaps |
| [50-two-simulator-user-journey-tests-coverage-audit.md](50-two-simulator-user-journey-tests-coverage-audit.md) | Refreshed manual-journey coverage matrix plus accepted notification-open contract for the current app |
| [_current-test-map.md](_current-test-map.md) | Compact operator runbook for which tests exist, what they protect, and how to run them now |

**Top finding:** Test coverage is materially stronger than the first pass
suggested. Report `50` is now the folder-local matrix for manual
two/three-simulator journey confidence, `67` is the compact current runbook
for day-to-day test selection, and the old stale Feed-expanded-card
notification-open assumption is retired in favor of the current
conversation/group/intros routing contract.

---

## 2. Smoke Tests

| Report | Focus |
|--------|-------|
| [03-smoke-test-strategy.md](03-smoke-test-strategy.md) | Lean smoke strategy plus change-based regression gates for high-blast-radius subsystems |

**Top finding:** The smallest effective smoke suite is already mostly present in the repo. Reusing existing startup, loading, QR, inbox, posts, and group-smoke tests is better than building a fake-only parallel test app, but smoke alone is not enough for risky shared pipelines such as 1:1 reliability.

---

## 3. Performance

| Report | Focus |
|--------|-------|
| [04-ui-performance.md](04-ui-performance.md) | Profile-gated UI/perf candidates; stale false positives removed |
| [05-database-storage-performance.md](05-database-storage-performance.md) | High-confidence DB/storage cleanups; broad indexing/caching recommendations narrowed |
| [app-smoothness-performance-audit.md](app-smoothness-performance-audit.md) | Release-build (TestFlight/Play) "feels slow/buggy" audit — multi-agent, adversarially verified: per-bubble blur + always-animating ambient bg never let the raster thread rest; chats/feed redo O(N) work per interaction. Quick Wins + High/Medium roadmap |
| [156-app-smoothness-quick-wins-tdd-plan.md](156-app-smoothness-quick-wins-tdd-plan.md) | TDD plan for the 13 Quick Wins (blur removal, RepaintBoundary + reduce-motion, avatar cacheWidth, 2 targeted indices DB v93/v94, ring memo+==, scrub sentinel, media-resolve gate, quote-map O(1), entrance-anim isNew, orbit flags). Host-only closure. IMPLEMENTED host-green |
| [157-app-smoothness-structural-roadmap.md](157-app-smoothness-structural-roadmap.md) | Master roadmap/index for the **structural** (non-quick-win) audit findings: refuted ledger, epic→plan map (158–164), feed_wired collision order, shared infra (coalescing layer, thread-summary pattern, PERF_TARGET harness), Phase-3 ship/defer/drop. Grounded by verify→refute workflow wf_85c86584-4e1 |
| [158-chat-ambient-idle-glow-suppression-tdd-plan.md](158-chat-ambient-idle-glow-suppression-tdd-plan.md) | TDD plan E-A (`critic-2`): stop the 8s ambient glow animating on foregrounded chat/group surfaces for default users (distinct `isChatSurface` _shouldAnimate term) so chrome BackdropFilters become cacheable at rest. Host-only; RED scaffold exists. **First in the 157 build sequence** |
| [159-conversation-list-memoize-coalesce-cap-tdd-plan.md](159-conversation-list-memoize-coalesce-cap-tdd-plan.md) | TDD plan E-B (`main-isolate-blocking-1`/`rebuild-storms-2,3`): memoize `_buildDisplayItems` (1:1 + group), per-frame coalesce the drain setState-storm, cache parsed DateTime, cap the in-memory window. Land the 145/131 preservation lock BEFORE the coalesce. Host-only |
| [160-feed-contact-n1-summary-and-per-event-bound-tdd-plan.md](160-feed-contact-n1-summary-and-per-event-bound-tdd-plan.md) | TDD plan E-C+E-D (`db-persistence-1`/`db-persistence-3`): 1:1 feed N+1 → `getConversationThreadSummaries` + `totalMessageCount` + lazy-on-focus + bounded reaction fan-out; send-merge + paged delete/hide (cap NEWEST-first). feed_wired cluster #1 |
| [161-group-feed-batched-thread-summary-tdd-plan.md](161-group-feed-batched-thread-summary-tdd-plan.md) | TDD plan E-E (`db-persistence-5` query half; index 094 shipped): replace per-group `limit:200` loop with a batched windowed `dbLoadGroupThreadPreviews` (preserve unread/state/preview; no limit-1). Real-DB host tier. feed_wired cluster #2 |
| [162-feed-conversation-media-resolve-sync-and-debounce-tdd-plan.md](162-feed-conversation-media-resolve-sync-and-debounce-tdd-plan.md) | TDD plan E-F (`db-persistence-7`): swap 3 awaited `resolveStoredPath` loops to the existing sync twin + debounce feed reloads. Preserve 127/128 null-cache passthrough. feed_wired cluster #3 |
| [163-feed-orbit-shell-rebuild-and-offscreen-pause-tdd-plan.md](163-feed-orbit-shell-rebuild-and-offscreen-pause-tdd-plan.md) | TDD plan E-G+E-H (`navigation-hangs-2`/`reactive-streams-3`/`animations-repaint-2`): AppShellController notify-kind split (tab vs background side-effects) + per-pane RepaintBoundary + off-screen TickerMode mute + per-handler orbit subscription pause (dirty-flag, not cancel). **REVIEWED 2026-06-24 → minimal scope: active-tab ambient term + AnimatedBuilder child-hoist CUT; anchors corrected for 158 in-tree.** feed_wired cluster #4 |
| [164-cold-start-deferral-and-keychain-mirror-tdd-plan.md](164-cold-start-deferral-and-keychain-mirror-tdd-plan.md) | TDD plan E-I + cold-start-3/5 (`cold-start-1`/`main-isolate-blocking-2`): defer startLiveServices/Firebase off the pre-runApp path, incremental iOS keychain mirror, parallel launch probes. `_setupPushListeners` re-arm = first-class RED; **device-proof is the closure gate** |
| [165-orbit3-arch-overflow-and-avatar-size-tdd-plan.md](165-orbit3-arch-overflow-and-avatar-size-tdd-plan.md) | NEW FEATURE (Orbit3 prototype, visuals-only — NOT performance): replace messy >2-orbit ring-growth with an **arch** above the One Circle holding `+N 👥`; tap → scrollable continuous semi-circle arc panel ("MORE"); + `+/-` avatar-size stepper scaling circle AND panel. Additive `showOverflowNode`/`avatarScale` keep InnerSky/Fisheye byte-identical. Host-only closure; AUTO-glob `feature-host-all` |
| [166-orbit3-arch-anchor-cap-collapse-resize-tdd-plan.md](166-orbit3-arch-anchor-cap-collapse-resize-tdd-plan.md) | MODIFICATION (refines 165): fix arcs OVERLAPPING the inner circle (band bottom ignored the top member's `15·scale` radius) → raise band above the ring; bottom-anchored cap+scroll (reverse ListView, comfortable size, no compression); explicit `orbit3-arch-collapse` button; shrink the oversized `+N` `Orbit3ArchBar` (52→≤38, chip-sized). No-overlap is a geometric INV locked under population AND avatarScale. Host-only; AUTO-glob |
| [167-orbit3-arch-unified-scroll-record.md](167-orbit3-arch-unified-scroll-record.md) | MODIFICATION (refines 166, **workflow-designed** wf_73085ae0-7d0): make the inner circle + ALL arches ONE continuously-scrollable surface ("scroll the entire screen") — single `SingleChildScrollView`+`Column` (arches above, circle last), pinned collapse/stepper/search overlays, `jumpTo(maxScrollExtent)` opens anchored on the circle. Load-bearing INV test: circle avatar + arch avatar resolve to the IDENTICAL `ScrollableState` (RED on HEAD). 51/51 host-green (review-nit fixes); AUTO-glob |
| [168-orbit3-spacing-groups-entrance-100users-tdd-plan.md](168-orbit3-spacing-groups-entrance-100users-tdd-plan.md) | FEATURE BATCH (5 changes, refines 167): C1 second ＋/－ **spacing** stepper (scales orbit-ring radii + arch pitch, orthogonal to avatar size); C2 expanded **group avatars** render with `OrbitalAvatar` parity + stay adjacent; C3 **rise-up fast** entrance (additive `riseUp`/`entranceDelayMs` on shared OrbitalAvatar — bottom-up, not left-right); C4 **no inner-circle re-entrance** on expand (`animateEntrance:false`); C5 **100-user** population. ALL additive (InnerSky/Fisheye/Orbit/Orbit2/feed defaults preserved). Host-only; AUTO-glob |
| [169-orbit3-dimension-persistence-tdd-plan.md](169-orbit3-dimension-persistence-tdd-plan.md) | NEW FEATURE (Orbit3 prototype, **workflow-grounded** wf_2ad5022a): persist the 4 dimension steppers (avatar/spacing/curve/per-arch) across launches + a **Reset-to-default** pill (`orbit3-reset-dimensions`). Mirrors the SecureKeyStore preference pattern (NOT shared_preferences — absent): NEW `Orbit3DimensionPreferences` codec (first NUMERIC pref; clamp-on-decode) + load/save/clear use-cases; optional null-default `SecureKeyStore?` injected at the sole prod mount `feed_wired.dart:2645`. Host-only; AUTO-glob `feature-host-all`. 8 TCs |
| [170-1to1-send-button-frozen-snackbar-overlap-tdd-plan.md](170-1to1-send-button-frozen-snackbar-overlap-tdd-plan.md) | BUG (surfaced during FDC-S4 device testing; **6-agent verify→refute** root-caused): under degraded network the 1:1 composer is unusable for seconds and a red failure **SnackBar covers the Send button**. ONE root cause in `conversation_wired.dart`: the global `_isSending` (set `:2058`, reset only in the `finally` `:2511` after the multi-second awaited send) doubles as the Send-button enable gate (`compose_area.dart:486`) — **S2** frozen button; and floating failure SnackBars (`:2481`/`:3883`, no `margin`) land on the bottom-anchored composer (Scaffold `:4268` has no `bottomNavigationBar`) — **S1**. Fix = optimistic `_isSending` release after the optimistic insert + detached send future; bottom `margin` on failure bars. Host-only; widget-tier; **add to `ONE_TO_ONE_TESTS`**. 5 TCs. Edit/retry/media S2 + inline-banner redesign deferred (siblings). |
| [174-fdc11-lan-dial-advert-libp2p-ports-tdd-plan.md](174-fdc11-lan-dial-advert-libp2p-ports-tdd-plan.md) | BUG (device-verified at CV-08 D1, Pixel↔iPhone11; **7-agent verify→refute** grounded): FDC-11 libp2p LAN-direct never fires on device — the bonsoir advert carries only the wsPort, so discovered peers have empty `libp2pAddresses` and the forwarder skips (`p2p_service_impl.dart:914` — a SYMPTOM, not the cause). Real cause: the FDC-07 cold-start early seam derives **null** libp2p ports (listenAddresses not yet populated), caches them in `local_p2p_service`, and nothing re-derives — `_handleAddressesUpdated:4052` updates state but never re-advertises; the null is **permanent** for the process. Go is CORRECT (resolves `udp/0`→concrete LAN `quic-v1` into `listenAddresses`). Fix = **Dart-only**: new `LocalP2PService.updateLibp2pPorts` (re-advertise on change, no-op unchanged) + `_handleAddressesUpdated` re-derive + numeric `FDC_LAN_ADVERT_PORTS` diagnostic (multiaddrs are redacted in [FLOW]). Host TDD (6 TCs, AUTO `core/**`); CV-08 manual two-phone gate = closure. Do NOT flip the flag (CV-09 owns). |
| [175-bonsoir-broadcast-off-main-thread-tdd-plan.md](175-bonsoir-broadcast-off-main-thread-tdd-plan.md) | BUG (follow-on from 174 CV-08 session-2; **8-agent verify→refute** grounded — CORRECTED the originating premise): iPhone Dart freezes ~1s post-launch → `0x8BADF00D` scene-update watchdog SIGKILL, blocking CV-08 PASS #4 (`MSG_RECEIVED_TRANSPORT:"direct"`) + CV-09. Real cause = the **advertise/broadcast** leg's synchronous `DNSServiceProcessResult(sdRef)` on the iOS **main thread** (`bonsoir_darwin-5.1.3 BonsoirServiceBroadcast.swift:35`, via `bonsoir_discovery_service.dart:166`); the first `startAdvertising` is ungated and a Dart `.timeout()` can't unblock the native run loop. **REFUTED & do-not-re-introduce:** "browse blocks main" (NWBrowser is async), "bump bonsoir 7.x" (broadcast byte-identical), "native pre-probe" (wrong API surface + false-skip regression). Fix = move the broadcast processing **off-main** via `DispatchSourceRead` (mirror the discovery-resolve path) — Approach A vendor+patch `bonsoir_darwin` (recommended) or Approach B app-side `NWListener` advertise. Native XCTest + **device-proof closure** (no host-Dart RED — accepted, OS-watchdog fix). Step-0: symbolicate the `.ips` to confirm the blocking frame. Do NOT flip the flag (CV-09 owns). |
| [176-cv09-libp2p-lan-dial-default-on-tdd-plan.md](176-cv09-libp2p-lan-dial-default-on-tdd-plan.md) | MODIFICATION (flag graduation; **4-agent verify→refute** grounded): graduate `EnableLibp2pLANDial` dark→default-ON now that CV-08 closed (commit `121f0551`). KEY FINDING: the Dart feature-flag map is an **always-sent full override** (`p2p_bridge_client.dart:125` → Go `config.go:207-208` returns `*c.FeatureFlags` wholesale; `json.Unmarshal` never seeds Go defaults), so the LOAD-BEARING flip is the **Dart** `defaultValue:false→true` at `p2p_bridge_client.dart:64` — flipping ONLY the Go default (`feature_flags.go:83`) is a **production no-op** (the trap, locked out by TC-09-02 asserting the SENT `node:start` payload). Go default flipped too, for consistency + guard-test inversion (`feature_flags_runtime_test.go:46`). **REFUTED & do-not-re-introduce:** "Go flip breaks the ~36 nil-flag Go integration tests" (no discovery trigger — only the Dart-fed bridge calls `HandleLANPeerFound`, node runs no libp2p mDNS → flag inert there); "flip-only-Go suffices". NEW Dart guard `test/core/services/p2p_service_lan_dial_flag_test.dart` (mirrors the DCUtR sibling; the Dart default was UNGUARDED — GAP #1). DcutrUpgrade/LANMedia stay dark (CV-13/CV-34). Host RED→GREEN gating (3 host TCs, AUTO-glob + `go test`); two-phone device-proof with NO dart-define = closure. |
| [177-lan-classifier-dns4-local-private-tdd-plan.md](177-lan-classifier-dns4-local-private-tdd-plan.md) | BUG (FDC-S6 instrument; found during the plan-176 CV-09 pilot, **device-confirmed**): `lan_address_classifier.dart` `multiaddrIsPrivateIp` returns FALSE for `/dns4/<host>.local` — it only credits `/ip4`/`/ip6` literals, but **Fix C (plan 175)** builds `/dns4/<host>.local` for iOS-resolved bonsoir peers (`bonsoir_discovery_service.dart:136`; iOS resolves to a `.local` hostname, not an IP). So the `lanPrivateIp` boolean emitted at `p2p_bridge_client.dart:629` is `false` for EVERY iOS-discovered peer → FDC-S6's "clean LAN win" (bonsoir-fed AND lanPrivateIp:true) can never be met when iOS is the discoverer → the soak **under-counts** real LAN wins (cross-platform + iOS↔iOS). Device proof: iPhone 11 `P2P_LAN_PEER_FOUND_REQUEST{addrCount:2, lanPrivateIp:false}` for a genuine `transport:"direct"` LAN conn (same /24, DCUtR off). FIX = `.local`-gated: classify `/dns4|/dns6|/dnsaddr/<host>.local` (RFC 6762 link-local) as private; **non-`.local` `/dns4` (e.g. `example.com`) STAYS false** (over-broaden lock — the existing `:72` test already pins it). Host-only TDD (3 RED in `lan_address_classifier_test.dart`, AUTO `core/**`); device FDC-S6 pilot re-run = closure. Do NOT change the win-criterion / Fix C / Go. |
| [178-bonsoir-suspected-denied-gate-overlatch-tdd-plan.md](178-bonsoir-suspected-denied-gate-overlatch-tdd-plan.md) | BUG (recurring device blocker; found during CV-34 / FDC-S6, **device-confirmed**): the pure-Dart suspected-Local-Network-denied gate in `bonsoir_discovery_service.dart` OVER-LATCHES + self-reinforces. `startAdvertising:166-174` returns early on latch **before BOTH** the broadcast (`:198-200`) AND the browse (`:209-213`), but the only un-latch path (peer-resolve `:311`) NEEDS the browse → terminal until the 5-min backoff. And `_armSuspectedDenialProbe:254` latches **platform-blind** though it's the "iOS watchdog" gate — on Android there's no `0x8BADF00D` vector + gating the broadcast breaks the working iPhone→Pixel direction. Premise largely OBSOLETED by 175 (off-main broadcast) + upstream 5.1.3 (off-main discovery). Symptom: Pixel repeatedly `LOCAL_MDNS_SUSPECTED_DENIED_GATE_LATCHED{300000}` → never discovers the iPhone (Mac `dns-sd` sees BOTH advertising; iPhone finds Pixel fine; perms granted). Blocks FDC-11 discoverer-side, FDC-15 CV-34 Pixel→iPhone, FDC-S6 i→A. FIX (1) platform-gate the latch to iOS (`defaultTargetPlatform`, not `dart:io`); (2) decouple — gate ONLY the broadcast, KEEP the browse running so it self-clears. **PRESERVE** the iOS broadcast gate (watchdog) — TC-78-04 mutation-locks it; do NOT revert 175 / add a native pre-probe. Host TDD (4 RED, AUTO `core/**`); device = Pixel discovers iPhone + iPhone 0×`0x8BADF00D`. |
| [193-orbit-inner-circle-default-view-split-spec.md](193-orbit-inner-circle-default-view-split-spec.md) + [tdd-plan](193-orbit-inner-circle-default-view-split-tdd-plan.md) | FEATURE IMPROVEMENT (product-confirmed; **4-sweep evidence workflow + 10-claim verify→refute, 0 refuted**): split the SHIPPED Orbit screen (`lib/features/orbit/` — orbit2/3 are kDebugMode mock prototypes with NO all-chats surface) into a default **Inner-Circle-only** view vs an alternative classic **all-chats** view, toggled by a NEW top-left button (`ValueKey('orbit-view-toggle')`; plain physical `Positioned(left:16)` — the FAB is physical right and does NOT mirror in RTL). EVERY entry resets to Inner-Circle (no persistence): initState default + rising-edge reset in `_onAppShellChanged` (orbit_wired.dart:466 — the embedded pane is latched-alive so initState alone cannot satisfy re-entry) WITHOUT starving `_replayDirtyOrbitWork` (order-locked). DESIGN LOCK: `initialFilterTab != null` ⇒ all-chats view (carries the intro-notification route, ~24 initialFilterTab tests, and INVITE_ACCEPT_SPINNER). Supersedes 2 locks (feed_wired search-survival :926-989, dual-'Close Friends' color-set :291-346); cold-start sim `_runOrbitSessionAndOpenThreads` gets a pre-assert + toggle preamble (PROD-CRITICAL closure leg, dual-suite 1to1+group); OrbitScreen gains OPTIONAL `viewMode`(default allChats)/`onToggleView` params (4 bare-pump ctor sites must not compile-break; perf harness passes innerCircle explicitly). 3 new l10n keys en/ar/de. 42 TCs, widget-floor everywhere, no DB migration, no device-proof; NEW `orbit_view_split_test.dart` + `test/l10n/orbit_strings_parity_test.dart` → **append both to GROUP_TESTS** (test/l10n is outside the feature-host-all glob) |
| [194-orbit-node-unread-messenger-orbit-spec.md](194-orbit-node-unread-messenger-orbit-spec.md) + [tdd-plan](194-orbit-node-unread-messenger-orbit-tdd-plan.md) + [mockup](194-orbit-unread-indicator-mockups.html) | NEW FEATURE (product-confirmed: alternative **B** of the interactive mockup; **3-ground + 5-refute workflow wf_e0aee5cf-e5f, 0 refuted**): per-node **unread "messenger orbit"** on the 193-default Inner-Circle view — thin green (`#1DB954`) ring + min(unreadCount,3) halo'd satellites (donor = contact-profile `_OrbitRingsPainter` + 28s `Transform.rotate`, sped to ~9s), NO numerals. Rendering is presentation-only (`OrbitFriend.unreadCount` reaches `OrbitalVisualization` unstripped — C2 verified) BUT clearing needs ONE new seam: read-marking is a silent DB write and `externalRouteChangesListenable` is production-DEAD (zero writers, feed_wired.dart:260/2532/2617) → notif-tap (C4) AND feed-side (C5) reads stale-lit the node; plan = `markConversationAsRead` emits (rows>0 guard) + orbit subscribes w/ active/dirty handling (`ORBIT_FL_READ_REFRESH` discriminator; PROD-CRITICAL row TC-194-16). KEY HAZARDS locked: pumpAndSettle hangs on lit nodes (absence-when-0 = INV-1, bounded pumps only); donor test TC-24 LOCKS keeps-rotating-under-reduce-motion (do NOT copy donor motion — cosmic_background `_syncMotionPreference` pattern instead); perf harness has NO budget asserts (evidence-only). 36 TCs → 38 matrix rows, host-only closure, no DB migration, no device-proof; NEW `unread_orbit_indicator_test.dart` + `orbit_unread_indicator_wired_test.dart` (**append wired file to GROUP_TESTS**), l10n key `orbit_node_unread_open_chat` en/ar/de via already-registered parity test |
| [196-orbit-qr-scan-entry-migration-spec.md](196-orbit-qr-scan-entry-migration-spec.md) + [tdd-plan](196-orbit-qr-scan-entry-migration-tdd-plan.md) + [mockup](196-orbit-qr-scan-entry-mockups.html) | FEATURE IMPROVEMENT (product-confirmed: alternative **B "Quiet Chrome"** of the 4-option mockup; **2-agent ground wf_3bf64e03-e20 + 3-agent verify→refute wf_e3934c6d-a77, 1 REFUTED**): migrate **My QR / Scan** off the all-chats `FriendsListHeader` pills (hidden in search, zero Semantics, only on the non-default surface post-193) onto twin **40px top-center chrome circles** (toggle species: `surfaceSubtle`/0.5-hairline/20px glyph, `top=safeTop+8`), mounted on **BOTH surfaces** (Layer 1c, **must precede `ExpandableFab`** — open-scrim precedence), physical non-mirroring centered pair, `Semantics(button,label)` from the EXISTING `orbit_my_qr`/`orbit_scan` keys (l10n literal-scan catches `label:'…'` hardcodes). **REFUTED A4 = the load-bearing correction:** the list header row top is EXACTLY `safeTop+8` (same band as chrome — a centered pair collides with the 'Close Friends' title at EVERY supported width) → design = header **leaves the strip** (first-sliver padding 8→56, 48px RTL inset deleted) which also kills a pre-existing LTR bug (Scan pill's right ~36px under the FAB tap footprint) + the 320dp header overflow. Handlers/destinations UNCHANGED (`_onMyQR:2079`/`_onScanQR:2130`, B6 feedClearedRepository lock stays); double-push (no guard anywhere) + `ownPeerId ''` window inherited knowingly (follow-ups named). Supersedes 2 locks: orbit_wired_test:859 pill-text assert (find.text can't see Semantics labels) + view_split:388 RTL left-edge lock (flips ONLY when the 48 inset goes — rewritten to vertical strip-clearance). 26 TCs → 26 matrix rows, host-only closure, no DB migration, no device-proof; NEW `orbit_qr_chrome_buttons_test.dart` (AUTO) + `orbit_qr_entry_migration_test.dart` (**append to GROUP_TESTS**); parity test gains the 2 keys (keeps them live once the pills — their only consumers — die). **197 QUARANTINE:** concurrent session left the ORBIT perf harness + `inner_circle_items_test` non-compiling (missing `inner_circle_items.dart`) — perf gate + full feature-host-all deferred until 197 lands. |
| [197-orbit-groups-on-inner-circle-rings-tdd-plan.md](197-orbit-groups-on-inner-circle-rings-tdd-plan.md) | NEW FEATURE (product-confirmed placement = **interleave by recency**; **5-investigator debug workflow wf_97cf9a55-ac5, 0 refuted** — REVERSES the deliberate friend-only scope of 193/194): group chats appear as satellite nodes on the SHIPPED Inner-Circle rings, merged with 1:1 friends into one recency-desc order (both compete for the 13 seats + overflow), each group node rendering its real `GroupAvatar` (`GroupModel.avatarPath`, initials fallback) + 194 `UnreadOrbitIndicator` (`OrbitGroup.unreadCount`) + tap→`onGroupTap`→group conversation. ROOT CAUSE = inner circle is structurally friend-only (`List<OrbitFriend>` end-to-end): `OrbitalVisualization` (widget), `OrbitHeaderProjection` (no groups field, orbit_screen.dart:75-85), `_buildHeaderProjection` (allFriends only, orbit_wired.dart:284-290) — groups flow ONLY to `OrbitViewProjection.mergedItems` (list surface). Friend-only from inception `c90b9fcf`; NOT a dropped wire. PARTS ALREADY EXIST (gap = data-plumbing): reuse production `OrbitItem.sortKey` union (list+rings share one ordering; friends already recency-sorted `load_orbit_data_use_case.dart:204`), `OrbitalAvatar.child` "group glyph (168 C2)" + node-agnostic `unreadCount`, and `_onGroupTap` (orbit_wired.dart:2460) — just not threaded into the rings. FIX = new host-pure `mergeInnerCircleItems` helper (filter blocked/archived, sort desc) + `OrbitHeaderProjection.innerItems` + widget per-item fork + surface passes `onGroupTap`. 11 TCs, widget/host closure ONLY (pure presentation — NO DB migration, NO device-proof, NO /sims), all AUTO-globbed under `feature-host-all` (perf harness scenario extension = verify discovery). Do NOT import orbit2/orbit3 prototype types. Accepted diff: search "inner circle badge" (orbit_screen.dart:1023) stays friend-only (follow-up owns) |
| [198-orbit-sculpt-and-summon-spec.md](198-orbit-sculpt-and-summon-spec.md) + [mockup](198-orbit-arch-overflow-edit-find-mockups.html) | NEW FEATURE (product-confirmed 2026-07-03: the featured combined build **"Sculpt & Summon"** = Concentric Arcs (Q1-B) + Orbit Handles (Q2-B) + Result Chips (Q3-C) on one surface; **5-explorer evidence workflow wf_afb8aa1c-a93 + 3-critic verify wf_0cb85bd0-971**): the production Orbit's dead 28px +N badge (no tap/Semantics, slot ≈9.4px from seat 13 — `overflow_badge.dart`, `orbital_visualization.dart:157-172`) becomes the toggle (ring-2 re-spread 8→9 slots, ≥44pt, chevron, localized Semantics) revealing overflow on **concentric arcs** rₖ=(108+46·og+46k)·sp sharing the rings' dashed teal/purple vocabulary — arch nodes are first-class (tap-to-chat, 194 unread, 197 groups, ≥44pt); **long-press 500ms on empty space** → edit mode (dim 22%, banner, Reset, tap-away-done, release-click suppressed) with **5 geometry handles** (av 0.6-1.4 / sp 0.7-1.5 / wrap cv 0.5-2.5 w/ 1.6 pinch-release→332° shell / max-per-arc pr 4-9 def **9** / orbit-gap og 0.5-2.5) — tap-to-arm → value bubble + −/+ 48px steppers flanking the nav pill; persisted via versioned SecureKeyStore key + **Move-registry entry** (orbit3's `orbit3_dimension_preferences_v1` is UNREGISTERED = silent drop precedent); **find pill** on the inner circle (reverses 193's all-chats-only-search asymmetry): lights matches in place (dim .28), ≤4 chips w/ ring/arc provenance, **groups included** (list search drops them — an overflow group is unsearchable anywhere today), no auto-expand, zero-match = no dim; edit×find compose; double-tap = labels (NO tap-latency arena). Supersedes the badge-inertness lock (`orbital_visualization_test.dart:268`); chrome/193/194/196/197 untouched + regression-locked; **73 TCs in 9 groups** (incl. pan-release-guard, badge-toggle-under-find, handles-track-scroll, circle-planted-while-sculpting hardening); reduce-motion for all new animation (today only the 194 ring honors it); tdd-plan: [198-orbit-sculpt-and-summon-tdd-plan.md](198-orbit-sculpt-and-summon-tdd-plan.md) |
| [198-orbit-sculpt-and-summon-tdd-plan.md](198-orbit-sculpt-and-summon-tdd-plan.md) | TDD PLAN for the 198 spec (grounding workflow wf_e1760c56-da6: 5 ground explorers + 8 verify→refute claim pairs; 2 independent post-write reviewers, 1 BLOCKING + 13 MINOR findings ALL applied in place): 73 spec TCs + 3 plan-added obligations — **YIELD-1/2**: the refutation-discovered feed↔orbit host-swipe raw `Listener` (`feed_wired.dart:2722`, bypasses the gesture arena) gets an `_orbitEditSessionActive` yield gate via a new `OrbitWired.onEditSessionActiveChanged` param (precedent `onRowActionOpenChanged`), else a rightward handle drag slides the tab mid-sculpt; **SIM-PC** PROD-CRITICAL real-boot leg folded into the already-registered cold-start sim (0 new builds) — → 76 matrix rows, zero empty cells, unit/widget/wired/migration-host/sim/perf tiers; 6 NEW test files (arc-layout, geometry-prefs, use-cases, find-matches pure; orbital_arcs widget; orbit_sculpt_summon_wired → GROUP_TESTS pin = the only manual registration) + 8 extended; knobs = NEW `orbit_geometry_prefs_v1` arity-5 key + Move registry entry (new closed-enum member `orbitGeometryPreferences`, policy migrate / criticality **optional** — forced: critical breaks never-sculpted Moves; old-importer enum-skew recorded as product flag, `_enumByName` throws); Move round-trip clones the proven bundle_transfer host harness (:1016-1212; NO preference key round-trips anywhere today); supersedes the badge-inertness lock via the TC-198-67 rewrite (INV-1 badge-never-opens-chat preserved); l10n +16 keys two-step (newOrbitKeys list-RED before gen-l10n, generated-API getters after — else compile break); perf ORBIT 5th scenario requires an `interaction` hook (`_OrbitScenario` is data-only — bare append unexecutable); literal-scan pinned exactly-3-orbit3 via a machine-checked grep gate. Closure = host-green + one iOS-sim run of the extended cold-start case; no device/relay proof needed (no OS/crypto/transport boundary) |
| [198-orbit-sculpt-handles-fidelity-tdd-plan.md](198-orbit-sculpt-handles-fidelity-tdd-plan.md) | MODIFICATION follow-up slice to the 198 plan (found 2026-07-03: shipped edit mode ≠ mockup despite host-green; **17-agent mismatch workflow wf_b3a2d45a-f09 → 12 confirmed (M1 critical: bottom text-chip Wrap instead of geometry-anchored glowing handles), jiggle refuted (mockup CSS bug); 7-agent ground+refute wf_f2875564-e7f → 4 mechanisms feasible-with-changes**): escape RC = presence-by-key-only F6 tests + TC-198-71/72 wired tests never written. Rework: five 30px glowing icon handles seated ON geometry (sp ring2-6h / av ring1-12h / cv arc-tip / pr twin / og apex; measured canvas origin via new canvasKey — analytic-y REFUTED — + per-build analytic anchors from live `_geometry`, hybrid frame-sync, Offstage band-hide both axes, never-unmount-mid-drag), bubble floats above armed handle, −/+ to bottom corners **with armed-state bottom re-flow** (find pill lifts +40→+88; right-corner stepper without the re-flow REFUTED — collides with the pill; all bottom elements gain the missing `bottomInset+` term), cv atan2 angle-follow + og ÷sp pure helpers, 650ms dim-flash Timer, `editEmphasis` ring-brighten prop, pulse gated on reduce-motion (`_syncMotionPreference` donor) with lifecycle at the `_setEditing` choke point (F13 pops the route mid-edit → undisposed ticker reds), green terminal banner/Reset, TC-198-56 semantics sweep (orig matrix claimed it falsely). ARBs UNTOUCHED (glyphs = scan-exempt letter-free Dart literals; Title-case-vs-mockup-lowercase flagged to product). 25 RED rows / 30 matrix rows, zero empty cells; 1 NEW auto-glob widget file, all else already pinned (GROUP_TESTS :254/:251); closure = host-green (`groups` gate 403→+~27), optional F13 sim leg; M12 slop stays Accepted Difference |
| [191-ios-foreground-push-forwarding-spec.md](191-ios-foreground-push-forwarding-spec.md) + [tdd-plan](191-ios-foreground-push-forwarding-tdd-plan.md) | BUG (RCA root #3; device-proven 2026-07-02 via 2 timed testpeer inbox injections → 0 push-triggered drains, acks only on 189's 30s grid; **3-agent verify→refute CORRECTED the mechanism**): iOS foreground push never reaches Dart onMessage — NOT delegate-stomping (4 installs = self-for-self; FLN never contends): the FCM plugin defers ALL launch wiring into a `UIApplicationDidFinishLaunchingNotification` observer that NEVER fires under this app's UIScene adoption (plugins register at scene-connect, after the notification; engine scene-fallback replays lifecycle calls not NSNotifications) → plugin never in the engine UN-callback chain; latent co-bug: nobody invokes willPresent completion for FCM payloads. FIX = **Option C**: AppDelegate willPresent forwards FCM-shaped (`gcm.message_id`, non-FLN) notifications to the PUBLISHED plugin instance (`valuePublished(byPlugin:"FLTFirebaseMessagingPlugin")`) with the original completion handler — zero Dart core-path changes, plugin-canonical shape, iOS-18 dedupe built-in, options-0 preserved; + Dart hardening: `FirebaseReadiness` (latch-on-success retry) + `PushListenerArmer` (`PUSH_LISTENERS_ARMED` + readiness-driven third arm point). KILLED: delegate-removal (B), proxy re-enable (D), `addApplicationLifeCycleDelegate` (E — double tap-routing, 139 reborn); do NOT forward didReceive. Host wiring-lock REDs + 2 new units (**append to `ONE_TO_ONE_TESTS` + `ONE_TO_ONE_HOST_TESTS`**) + native XCTest (manual xcodebuild); closure = device runsheet (testpeer injections ≤5s off-grid ×3, **--profile only** — dev_keychain_wipe hazard). |
| [190-android-netlink-selinux-addr-visibility-spec.md](190-android-netlink-selinux-addr-visibility-spec.md) + [tdd-plan](190-android-netlink-selinux-addr-visibility-tdd-plan.md) | BUG (root-cause #4 of the 2026-07-02 delivery RCA; live-proven on Pixel 6/Android 16; **9-agent ground+refute workflow** — RC survived all 5 alternatives, already-fixed/upgrade/D2/D3 refuted): SELinux denies the Go runtime's netlink route-socket **bind** (b/155595000, targetSdk≥30; stdlib `NetlinkRIB` binds — `syscall/netlink_linux.go:67`) → basichost ERROR every 5s + `h.Addrs()` empty → identify/signed-peer-records/rendezvous carry zero addrs, DCUtR input starved, **observed-addr recording ALSO netlink-dead** (obsaddr→InterfaceListenAddresses), LAN-direct hangs solely on the FDC-11 mDNS lane. FIX (plan) = **fork-and-replace `go-multiaddr` v0.14.0** (`third_party/`, pubsub-fork pattern) routing `InterfaceMultiaddrs` through a race-safe provider seam: `wlynxg/anet` under `//go:build android` (no bind, RTM_GETADDR-only; already linked via pion; Makefile already ships `-checklinkname=0`), stdlib under `!android` — kills the 5s spam AT SOURCE, no log filtering; + mini-fork `go-netroute` (host denial-fixture seam only). 3 spec corrections recorded (node:status NOT empty today — FDC-11 0.0.0.0 shape; TC-190-20 LAN-candidates impossible — upstream public-only filter, re-scoped to input term; 5-min spam window device-only). 10 Go RED + 2 Go sentinels (fork unit + `addr_visibility_denial_test.go` w/ marker-addr discriminators) + 1 Dart port-parity sentinel; new pinned Go target in `run_host_test_gates.sh host-all`; PROD-CRITICAL leg = TC-190-41 mDNS-disabled identify-dial (2-phone); closure = Pixel/iPhone runsheet (**--profile only**). Preserve FDC-11 fallback + filterAddresses policy; 189/188/relay untouched. |
| [189-degraded-relay-drain-starvation-restart-loop-spec.md](189-degraded-relay-drain-starvation-restart-loop-spec.md) + [tdd-plan](189-degraded-relay-drain-starvation-restart-loop-tdd-plan.md) | BUG (live two-phone debug 2026-07-02, Pixel-cellular→iPhone11-WiFi 86–368s+ store→ack, >10-min undelivered repro; **5-agent RCA workflow + 3-agent verify→refute** — all claims survived): (A) `_performHealthCheck` recovery branch `return;` at `p2p_service_impl.dart:3871` precedes the ONLY periodic drain `:3888` → chronically-degraded device never pulls its relay inbox (drains only when its own SEND flips relayState online); (B) permanent 30s full-restart loop — manual `Reserve` succeeds but only autorelay's relayFinder publishes `/p2p-circuit` (v0.39.1) and the 3s/10s waits kill it; bridge NEVER serializes `RecoveryResult.Success` (`bridge.go:821-862`) → false `phase=recovered` + counter reset every cycle; `refreshFailureThreshold:352` dead. FIX = drain-on-every-tick + Go reservation-truth verdict + bridge/Dart truthful bookkeeping + failed-recovery backoff. Host TDD (8 Dart RED in NEW `test/core/services/p2p_service_impl_health_drain_test.dart` — **add to `ONE_TO_ONE_TESTS`**; 3 Go integration RED in `no_circuit_recovery_test.go`, deterministic via `SetForcePublicReachabilityForTests`); PROD-CRITICAL leg = peer-dials-reservation Go test; closure = 2-phone device runsheet (≤35s p95, no 30s relay flap). Siblings out of scope: iOS push→drain, netlink/anet. |
| [199-graphify-token-discipline-tdd-plan.md](199-graphify-token-discipline-tdd-plan.md) | MODIFICATION (tooling/process, not app code; derived from a token-usage audit this session, workflow `wf_02c2e3c6-ea2`, no formal spec doc; amended 2026-07-03 after round-2 independent adversarial verification, workflow `wf_c1876316-b3f`): closes 7 graphify token/wall-clock discipline gaps. (1) `spec-doc/SKILL.md` Step 2's evidence-gathering `Explore` agent had zero graphify integration (blind 5-directory sweep) — now graphify-first, mirroring `tdd-plan/SKILL.md` Step 1. (2) No reusable "scout once, fan out many" pattern existed across the 14 saved Workflow scripts (measured ~37.7K tokens/agent avg on redundant re-querying this session) — adds `.claude/workflows/_template-scout-then-fanout.mjs` + makes `graphify-explorer.md`'s `--budget 1500` mandatory instead of an example. (3) `graphify-out/graph.json` (115,798,170 bytes) had no guardrail against a raw dump into context — extends `.claude/hooks/graphify_grep_gate.py` to deny both `Read` of graph.json and Bash `cat`/`less`/`more`/`bat` of it (blind-spot sweep found the Bash sibling vector), consolidated into the existing single `"Read|Glob"` hook entry (REFUTED: a second `PreToolUse` entry with an overlapping `Read` matcher — undefined decision precedence per official docs) + a `permissions.deny` backstop. (4) `graphify-out/cost.json` was blind (0 tokens logged, silent since 2026-06-11) — traced end-to-end into the installed `graphifyy` package source (confirmed the package itself never writes cost.json; it's entirely a `.claude/skills/graphify/SKILL.md`-orchestrated bash block, lines 505-533) → root cause = (a) `CLAUDE.md:14`'s own recommended bare-CLI `graphify update .` maintenance path structurally never reaches that block, and (b) `SKILL.md:275`'s manual token-backfill instruction (copying each Agent-tool subagent's real `usage` into the chunk JSON) is unenforced prose, evidently skipped both times it logged; fix = MANDATORY-callout the backfill step, add a zero-token warning to the append block, and document cost.json's actual coverage in `CLAUDE.md` (REFUTED: patching the installed package — it has no cost.json logic at all; REFUTED: retroactively fixing the 2 existing $0 entries — forward-fix only). (5) none of the 14 saved workflows use `pipeline()` for multi-stage-per-item work (wall-clock, not token, concern) — adds a second reference template `.claude/workflows/_template-pipeline-multistage.mjs` (forward-looking only, no retrofit of existing/closed workflows). ROUND 2 (2026-07-03) added: hook-enforced `--budget` on `graphify query` + budgeted examples in `CLAUDE.md`/`tdd-plan/SKILL.md`/graph-selection bullets (prose→mechanical enforcement); GRAPH_REPORT.md (~283K/61K tok) advisory steering nudge on Read; dump-gate hardening (dated-backup `graphify-out/YYYY-MM-DD/graph.json` copies covered, tool list widened to `jq`/`head`/`tail`/`awk`/`sed`/`git show|diff`, `GRAPH_OK` scoped so it cannot unlock the dump gate); template wiring into `tdd-plan`/`spec-doc` + `effort:` tiering demonstrated; 2 factual corrections (`.claude/settings.local.json` HAS a `permissions.allow` key; the Read/Glob nudge matches extensions by SUBSTRING so `.json` paths DO nudge today — behavior consciously preserved); item-5 premise corrected (`pipeline()` precedent already exists in `Test-Flight-Improv/Move-Feature/.move-*-workflow.js` — the gap is a canonical discoverable template, not absence of precedent); severity reframed (harness caps bound a raw dump to ~8-25K tokens, not 110MB — guard is steering/defense-in-depth); REFUTED with measured data: per-TDD-plan graphify graphs (full graph already ingests 1,697/1,699 TFI `.md`; markdown extraction is LLM-subagent-based so a per-plan build costs more than the ~4.1K-tok median raw read and is re-paid on every edit; execution needs literal row/gate fidelity; 537 extra fat-artifact sets). 17 matrix rows + 11 preservation sentinels, zero empty cells; first-ever test coverage for `.claude/hooks/` (new stdlib-`unittest` file, no pytest dependency). Host-only closure, no DB migration, no device-proof, zero `lib/`/`go-mknoon/` touched. EXECUTED + CLOSED 2026-07-03: 29-test standalone gate green (`python3 .claude/hooks/tests/test_graphify_token_discipline.py -v`), 24/24 mutations re-red, 3-agent adversarial QA pass (`wf_0810fe69-df2`) with same-day fixes (hook-wiring test TC-199-18; whole-command `--budget` check — quote-blind split false positive; double-slash regex evasion closed; graphify-explorer.md examples budgeted + added to the TC-199-17 scan). |
| [200-avatar-aspect-ratio-stretch-tdd-plan.md](200-avatar-aspect-ratio-stretch-tdd-plan.md) | BUG (user-reported 2026-07-03 "group picture stretched"; 7-bug debug workflow `wf_0396f589-131` B1 verdict CONFIRMED + grounding `wf_0e48db0b-199` + 2-reviewer pass `wf_a8091cae-3b0`, 3 BLOCKING findings fixed in place): **every avatar in the app renders squashed-square for non-square sources** — regression from `9e17f597` (156 QW-4): `cacheWidth`+`cacheHeight` both set ⇒ `ResizeImage` default `ResizeImagePolicy.exact` ("similar to BoxFit.fill", squash AT DECODE) in GroupAvatar/UserAvatar/ProfileAvatarWidget/share-thumb; compounded by `processAvatar` never cropping (":64 always 512x512" doc is FALSE — minW/H scale proportionally) and the receive-side NORMALIZATION_FALLBACK committing raw bytes (render fix therefore MANDATORY). FIX = shared `avatarResizedProvider` (`ResizeImage(..., policy: fit)`, GIF-exempt, carries key/errorBuilder/filterQuality) in all 4 widgets + best-effort square center-crop (package:image, NEW dep) after compress in processAvatar (pass-through on missing/undecodable compress output — else 3 fake-compressor suites red). LOAD-BEARING: existing TC-06/07/08/08b locks stay green under fit — they MUST be extended to assert `resize.policy` or the fix is mutation-invisible; decode-aspect proof fixture MUST exceed the target box in both dims (288×144 — `allowUpscaling:false` clamps to intrinsic, small fixtures vacuous-green). 15 TCs, host-only closure, no migration/device leg. Land BEFORE 201 (chip avatars inherit). |
| [201-orbit-find-ux-element-identity-tdd-plan.md](201-orbit-find-ux-element-identity-tdd-plan.md) | BUG batch (user-reported #4+#6 2026-07-03; debug `wf_0396f589-131` B4+B6 both CONFIRMED + grounding + 2-reviewer pass, minor fixes applied): ONE mechanism, two symptoms — **unkeyed conditional `Positioned` slot-shifts in the surface/viz Stacks**: (B4) find pill = corner `width:200` no-height; first MATCHING keystroke inserts the unkeyed chip-strip Positioned before the unkeyed pill Positioned → TextField REMOUNTED (focus/IME drop; re-fires on chips non-empty→empty and edit enter/exit; inner `'orbit-find-pill'` key can't save it — sibling matching is per parent Element); `_FindChip` text-only though `OrbitFindMatch.item` carries the full OrbitItem; (B6) label double-tap interleaves unkeyed label Positioneds → every node i≥1 re-inflated → 500ms staggered entrance REPLAYS = the blink; badge invisible 1000+500ms (already replays on arc expand/collapse); ring entrance hard-codes motion ON (`orbital_visualization.dart:207` reduce-motion gap). FIX = ValueKeys on ALL node/label/badge + find/edit-overlay Positioned wrappers; pill left/right:16 + minHeight 48 + host-supplied `bottomClearance` (nav-band overlap at zero-safe-area); UserAvatar/GroupAvatar in chips + Flexible/ellipsis; NO label fade (user wants instant). Identity oracle = `tester.state` + `identical()` (TC-194-11 precedent); enterText can NOT prove focus; node-0 false-green trap (assert i≥1). Churns TC-198F-17/18/20S geometry pins in-slice. 13 TCs, host-only closure. Sequence: after 200, before 202. |
| [203-orbit-groups-republish-handle-scale-text-removal-tdd-plan.md](203-orbit-groups-republish-handle-scale-text-removal-tdd-plan.md) | BUG ×2 + MODIFICATION triad — the REMAINING three fixes from the 2026-07-03 seven-bug debug (B1/B4/B6/B7 → plans 200/201/202; debug `wf_0396f589-131` all confirmed + grounding `wf_c0efa6c4-5fa` with 3 anchor corrections + 2-reviewer pass `wf_ddc43d40-779` ZERO blocking): **(B2)** group avatars/unread/rename/removal never reach the Orbit rings on group events — 197 IS in-tree but `_loadGroupData`/`_refreshOrbitGroup` publish LIST-only (orbit_wired.dart:652/676/680/699/703/850) while friend/identity paths publish-all; header notifier written ONLY at :373; funnel airtight (`_activeGroups` mutated only :668/:697/:848/:849); "worked earlier" = favorable race. FIX = six-site flip to `_publishAllProjections()` (body :380-383); behavior-bearing locks = :676 (Completer-gated `getActiveGroups` test — must seed via `gated.saveGroup(...)` DIRECTLY, the seedGroup helper closes over the file-level repo) + :850 (add/remove/unread/rename rows, groupMessageStream driver — the ONLY group-stream fixture lives in orbit_wired_test); :652/:699/:703 = header-inert uniformity flips (mutation-exempt). Do NOT flip the search/intro/invite list-only sites (:1710 typing lock). **(B3)** edit-handle discs don't scale with avatarScale — OrbitEditHandle all compile-time constants, host ctor :761-770 passes nothing, bubble clamp hard-codes discSize/2 at :794. FIX = raw-scale param + ONE shared `effectiveDiscSize(scale)=(30s).clamp(24,42)`, hitTarget STAYS 44 (all anchor/position locks key off it), glow/icon ×factor, LIVE (same-pump). Clamp ceiling DEAD (30×1.4=42 exactly) — clamp mutation must use the 0.6→24 floor row; mid-range scales via store seeding `'1.4|1.0|1.0|9|1.0'` BEFORE pump (initState-once restore; drag has ~18px slop, only clamp-saturating drags deterministic); bubble row asserts center−27 (NOT the old 15-based shape — boundary-vacuous at 1.4). **(B5)** remove "Close Friends"+"YOUR INNER CIRCLE" — 2 ARB keys / 3 sites (viz :234-245 title, surface :547-556 caption, FriendsListHeader — UNCONDITIONAL, outside the searchActive guard); delete friends_list_header.dart + 3-locale-ATOMIC ARB removal (badge key adjacency!) + gen-l10n ORDERED lib-sites-first; complete 16-item test-edit churn list incl. the :4615 negative obligation + 196 chrome-clearance re-anchor (FriendsListHeader→FriendsFilterToggle) + parity-test ARB-absence re-introduction guard; l10n_integrity NOT in any curated gate — run directly. Ring-view-only scope fork recorded. 19 TCs / 19 rows, all host-only, ZERO new manual registrations (all target files GROUP_TESTS-pinned or auto-glob). Recommended global order: 203-B2 → 200 → 201 → 203-B3 → 203-B5 → 202 (B3 before 202's pulse redesign; 202's coalescer rides B2's publish-all). Parks with owners: tap-away 300ms + decode quantization (202's Accepted Differences). **EXECUTED + CLOSED 2026-07-04** (all three slices; d1687eca B2 / 8f6b8502 B3 / 50e07f7d B5 / eeaff05f QA): groups gate 1022, orbit cluster 463, feed 285 green; 12/12 mutations re-red; group-node semantics asserts use the 198-F12 prefix idiom (merged initials suffix); INV-203-1 narrowed — a :676-alone revert is masked by the archived leg's publish-all, the enforced lock is the whole-funnel revert; TC-198F-04 re-anchored to a 340px band (title removal lifts the top-anchored canvas ~41px); l10n_integrity literal-scan red = PRE-EXISTING orbit3 debt (2026-06-25 commits), its 3-locale parity leg green. |
| [202-orbit-interaction-perf-slice-tdd-plan.md](202-orbit-interaction-perf-slice-tdd-plan.md) | FEATURE IMPROVEMENT (user: "make orbit faster WITHOUT removing features"; 2-lens perf audit `wf_0396f589-131` B7 verdict partial→verifier-CORRECTED + grounding + 2-reviewer pass, 2 BLOCKING findings fixed): corrected repaint model = **TWO independent per-frame streams** (scroll viewport + AmbientBackground ARE boundaries — pane-wide-repaint story REFUTED): canvas layer (9s unread rotation ⇒ blurred rings + avatar glows + dim saveLayers) and chrome layer (1.6s handle pulses — band-hidden handles keep ticking: Offstage ≠ TickerMode). SIX edits: keyed RepaintBoundaries per node (INSIDE the dim Opacity)/handle/ring (`orbit-handle-boundary-<knob>`/`orbit-ring-boundary` — bare ancestor finders are vacuous, `_ModalScope` route boundaries); silent `_CanvasMeasurement` update (kills the 2nd rebuild per drag frame; do NOT drop `_geometry` from the signature — overhang); constant-cost pulse (opacity over pre-rasterized halo + freeze while band-hidden; re-baselines 3 blur-reading tests); 162-style 32ms trailing-flush coalescer for friend+group refreshes (preserves 194 read-clear + cat.22 order + the :4578 single-event pin); CurvedAnimation hoist (per-build ctor leaks status listeners; identity test MUST use a real feed→orbit `switchTo` — same-tab early-returns, vacuous); friend_row stagger clamp + reduce-motion. REFUTED/do-not-reintroduce: dim-flash rebuild scoping (unattainable — `_dimFor` feeds every node), tap-away 300ms fix (spec-pinned, follow-up), decode quantization (follow-up after 200). Perf lane PERF_TARGET=ORBIT is report-only evidence (harness bypasses OrbitWired — host rows carry edits 4/5). 14 TCs; `debugOnSurfaceBuild` hook + `transientCallbackCount` = new-but-grounded patterns. Sequence AFTER 201 (+ B2 publish-all fix first). |
| [204-composer-attach-cancel-and-plus-size-tdd-plan.md](204-composer-attach-cancel-and-plus-size-tdd-plan.md) | BUG ×2 (UI, user-reported 2026-07-04; 4-agent verify→refute + test/l10n/blind-spot workflow `wf_f4b82c58-585`, all 4 claims CONFIRMED): two chat-composer defects present in BOTH 1:1 and group. **BUG 2** — the `+` attach button is `36×36` (`compose_area.dart:344-346`) while the voice-note and send buttons are `48×48` (`voice_record_button.dart:102-103` / `compose_area.dart:495-496`); `ComposeArea` is the SHARED composer (`conversation_screen.dart:427` + `group_conversation_screen.dart:265`, `GroupComposeArea` is DEAD code) so one edit `36→48` fixes both, and the resize is height-neutral (row stays 51 via `crossAxisAlignment.end` + identical `Padding(bottom:3)` — do NOT touch padding/`Icon size:20`). **BUG 1** — the attach source sheet (`_onAttach` `showModalBottomSheet`, byte-identical DUPLICATE at `conversation_wired.dart:2922` + `group_conversation_wired.dart:3228`) has only a drag handle + 3 picker rows, NO Cancel/X → add a keyed Cancel `ListTile` (`Icons.close_rounded`, reuse generic `btn_cancel` l10n — zero ARB/gen-l10n work, present en/ar/de `app_en.arb:427`; `onTap: Navigator.pop(ctx)` ONLY — must not pick nor clear staged media) to BOTH sheets (distinct `conversation-`/`group-attach-cancel-action` keys). REFUTED/do-not-re-introduce: "chat missing back" (header `chevron_left` exists `conversation_header.dart:60-67`), "can't remove picked image" (per-thumb `×` exists `attachment_preview_strip.dart:277-294`), "needs new `picker_cancel` key", "resize needs padding tweak". 5 TCs / 5 rows, all widget-tier, host-only closure, no DB migration / no device-proof; `conversation_wired_test.dart` ∈ ONE_TO_ONE_TESTS:75 + `group_conversation_wired_test.dart` ∈ GROUP_TESTS:224 (already curated), `compose_area_test.dart` AUTO-globs `feature-host-all` — ZERO new registration. Anchor Cancel tests on ValueKey (`Icons.close_rounded`/`find.text('Cancel')` ambiguous with quote-dismiss + RecordingOverlay). |
| [206-orbit-center-avatar-settings-entry-tdd-plan.md](206-orbit-center-avatar-settings-entry-tdd-plan.md) | TDD PLAN for the 206 spec (grounding+verify→refute workflow `wf_c22619f4-4c5`: 1 scout + 4 ground + 6 verify + 6 refute, 3 claims CORRECTED): **hit-opacity, not gesture-arena** — the whole 320px canvas is hit-opaque (`OrbitalRingPainter` no hitTest override → `RenderCustomPaint.hitTestSelf` true; `UserAvatar` DecoratedBox), so center taps NEVER reach the background detector; center tap/long-press/double-tap are ALL dead today → modal-dismiss + long-press-sculpt-entry on the center are NEW deliberate behaviors (node tap-away TC-198-19 precedent), and with a tap-only detector a long hold fires onTap on release + double-tap fires onTap TWICE → explicit `onLongPressStart` + single-flight `_settingsRouteActive` latch REQUIRED. **Background pref propagation REFUTED as .then-dependent** — shared `AppShellController` path (settings_wired:230 → app_shell_controller:40-47 → feed_wired:387/2508) already live; identity/avatar + image/video quality have NO passive channel → design = new `AppShellChangeKind.identity`/`.mediaQuality` kinds fired by SettingsWired on saves, consumed by FeedWired._onShellChanged AND OrbitWired._onAppShellChanged (SINGLE mechanism, NO `.then(_loadIdentity)` duplicate — INV-203-1 masking lesson; enum growth compile-safe, zero switches). **Breaking-test set corrected in BOTH directions**: only `feed_wired_test.dart:408-441` is a guaranteed breaker (rewritten in place as the PROD-CRITICAL full-chain FeedWired→embedded-OrbitWired→SettingsWired row); TC-198-67 + TC-194-01/34 SURVIVE under conditional mounting; refute-discovered **TC-194-26 (:464-486, unscoped single-GestureDetector getSize)** breaks if the center detector mounts unconditionally → mount ONLY when callback non-null (orbital_avatar:155-165 precedent, INV-206-1); geometry locks (sculpt:809-824, arcs:123) pin the unchanged 48px Positioned box. 10 edits E1-E10 (viz wrap keyed `orbit-center-self-avatar` + Semantics `orbit_open_settings` l10n key ORDERED-FIRST two-step; surface gated wrapper mirroring `_onNodeFriendTap:330-336`; 4-hop OrbitScreen threading; OrbitWired push+latch+identity-branch+`nearbyLocationService` field; controller kinds; settings notifies at :414/:453-467/:257/:274; feed listener branches; header/plumbing removal incl. `_avatarBytes`+`IdentityAvatarResolver` trim; threading at feed_wired:2542+main.dart:4257 w/ source-guard test). 30 spec TCs → 33 matrix rows, zero empty cells; NEW `orbit_settings_entry_test.dart` (14 tests, **add to GROUP_TESTS**) + `feed_header_test.dart` (3, **add to FEED_TESTS**) + 5 extended files; host-only closure, NO migration, NO device-proof, NO /sims; gates: feed 285→290, groups 1041→1055, orbit cluster 472→~491; l10n_integrity literal-scan pre-existing orbit3 red documented (205 owns). Scope guard: no onDoubleTap on center (TC-54 latency trap), no dual refresh mechanisms, no geometry re-baselines. |
| [206-orbit-center-avatar-settings-entry.md](206-orbit-center-avatar-settings-entry.md) | FEATURE IMPROVEMENT SPEC (user request 2026-07-04; Explore evidence + source-verified file:line): migrate the Settings entry point — the Orbit center self-avatar (48×48 static `UserAvatar`, NO gesture/Semantics, `orbital_visualization.dart:171-180`; taps fall through to the opaque background detector `inner_circle_interactive_surface.dart:573-581` → idle no-op) must open Settings, and the Feed header top-right avatar (`GestureDetector(onTap: onAvatarTap)` 42px, `feed_header.dart:51-58` → `feed_wired._onAvatarTap:1869-1898` = `buildSettingsSlideUpRoute`+`SettingsWired` 13 deps + `.then` pref-reload) is REMOVED entirely. Pure Flutter UI/nav — no Go/bridge. Pinned hazards: (a) Feed's `.then` continuation reloads identity/background/quality prefs — must survive the entry-point move (Group D TCs); (b) Settings functional parity from Orbit: `OrbitWired` has NO `NearbyLocationService` field and nullable `appShellController`/`postsPrivacySettingsRepository` (:137/:148) vs `SettingsWired` required non-null (settings_wired.dart:67-68) — posts-nearby refresh must not go silently inert (TC-206-22); both `OrbitWired` ctor sites (`main.dart:4257` + `feed_wired.dart:2542`) covered (TC-206-24). Spec decision: modal-state precedence — center tap during edit/find dismisses the mode (background-tap parity), Settings opens ONLY from idle surface. Preserved: long-press-to-edit, double-tap-labels, satellite taps, `ConnectionStatusIndicator` + username editing in Feed header, Posts-screen Settings entry, doc-202 INV-202-1 repaint isolation. Known breaking tests: `feed_wired_test.dart:409-441` (taps the doomed Feed avatar), `orbital_visualization_test.dart:319-347` TC-198-67 (center-inert framing; center-never-opens-chat intent survives), :405-424 TC-194 wording. 30 TCs in 7 groups (A happy-path incl. no-photo + pre-identity null-guard + double-push; B gesture-arena coexistence; C header removal incl. 320px layout; D settings-return refresh; E parity/entry-points; F a11y ≥48px + button semantics + reduce-motion; G lifecycle + INV-202-1 guard). Spec only — no solution; tdd-plan chains next. |
| [205-orbit-find-ux-visibility-and-prototype-cleanup-tdd-plan.md](205-orbit-find-ux-visibility-and-prototype-cleanup-tdd-plan.md) | BUG + FEATURE IMPROVEMENT + MODIFICATION (7-item user request 2026-07-04 on the SHIPPED Orbit inner-circle view — user calls it "Orbit3"; grounding + verify→refute `wf_6a5af21e-829`, 1 scout + 7 ground + 7 refute + test-inventory, 0 refuted-away): **(1)** the inner-circle **find pill** has NO close X (only the undiscoverable `_onBackgroundTap` `inner_circle_interactive_surface.dart:318`) → add a trailing X → `_closeFind` (`:500`, collapse `_findOpen=false` + `clear()` + `unfocus()`), new `orbit_find_close` l10n. **(2)** pill floats ~104px above the keyboard (`_bandLift` added unconditionally at `:915` + `:237`) and is too small (minHeight48/hpad-only/fontSize15) → anchor ≤12px above the kb (drop base 40→10, zero `_bandLift` while kb up) + enlarge (56/vpad12/font16/icon22). **(3)** FOUR faint `iconMuted 0xAFC9CED6` search lenses (`:927`,`:961`, `orbit_search_dock.dart:58`, `orbit_screen.dart:1102`) → `iconPrimary 0xFFF8FAFC` (refute upgraded 2→4). **(4)** canvas top-anchored (`Stack(:552)` no `alignment` + unbounded `SingleChildScrollView` height ⇒ `Center(:564)` shrink-wraps to 320) → `Stack(alignment: Alignment.center)`. **(5)** double-tap labels `textMuted 0xB8C9CED6` translucent, no shadow (`orbital_visualization.dart:360`) → `textPrimary` + drop-shadow + w600. **(6)** edit-mode Reset (`inner_circle_interactive_surface.dart:664`) occluded by the higher-z view-toggle (screen Layer 1b `orbit_screen.dart:402`, NOT edit-gated) → **user chose "hide top chrome in edit"**: OrbitScreen Stateless→Stateful, intercept the existing 198 `onInnerCircleEditSessionChanged` passthrough (`:570`) into local `_innerEditing`, gate Layer 1b (toggle) + Layer 1c (QR `:414`) on `!editing`, **preserve** the upward swipe-yield forward (INV, TC-205-07 re-reds if dropped); also fixes the banner/QR overlap. **(7)** **delete orbit2 + orbit3** `kDebugMode` prototypes (`kOrbit3PrototypeEnabled = kDebugMode`): 25 lib files + 3 shipped coupling sites (`app_shell_tab.dart:7/9`, `feed_navigation_bar.dart:5/87-95`, `feed_wired.dart:82-83/2675-2690`) + delete `test/features/orbit3/**` + `orbit2_models_test` + **`orbit3_prototype_source_guard_test.dart`** (File-reads the deleted file) + edit `feed_navigation_bar_test.dart` (2-tab) + purge `nav_orbit3`/12×`orbit2_*`/13×`orbit3_*` ARB keys via `gen-l10n`; **KEEP** live `orbital_avatar` `riseUp`/`entranceDelayMs`/`animateEntrance` (seeded `orbit_arc_layout.dart:346`); orbit2 already unmounted; `AppShellController` degrades a stale `orbit3` tab→feed. REFUTED/do-not-re-introduce: "item-1 already implemented" (TRUE only for the all-chats `OrbitSearchDock`, NOT the find pill), "only 2 faint lenses" (four), "orbital_avatar params are orbit3-dead" (LIVE), "source-guard test optional to delete" (throws FileSystemException). 12 TCs / 13 rows, all widget/host-tier, **host-only closure** (no DB migration, no device-proof — pure UI + dead-code deletion); NEW `orbit_find_pill_ux_test.dart` + `orbit_prototypes_removed_guard_test.dart` (AUTO-glob `feature-host-all`); items 4/6 fold into GROUP_TESTS-pinned `orbit_sculpt_summon_wired_test.dart:254` (**re-baseline TC-198F-04** 340px band on re-center); `orbit_find_close` appended to `orbit_strings_parity` `newOrbitKeys:18`. NOTE: 204 was claimed by the concurrent composer plan → renumbered 204→205. |
| [208-remove-post-accept-group-invite-snackbar-tdd-plan.md](208-remove-post-accept-group-invite-snackbar-tdd-plan.md) | MODIFICATION (user request 2026-07-04: "accepting a group invite moves me to the group chat (correct) but a snackbar slides up from the bottom right after — remove it, keep the navigation"; grounding + verify→refute `wf_45df4905-604`: 4 agents entry-census + test-inventory + verify + refute). Reverses Plan 150 "DECISION-2". **TWO live accept→navigate→snackbar surfaces** (census `onlySeamIsGroupList=false`): **group list** `group_list_wired.dart:375-377` (success, `l10n.group_invite_joined`) + `:390-392` (bridgeError shape-b join-with-recovery, `l10n.group_invite_joined_recovery`), each `_showSnackBar` THEN `_onGroupTap`; **Orbit/Intros** `orbit_wired.dart:1422` (success, HARDCODED `'Joined $name'` — why the l10n-key grep missed it) THEN `_openGroupConversationFromModel` — **Orbit is the PRIMARY live accept UI on `new-orbit`** (embedded feed_wired:2516, main.dart:4257), so group-list-only removal = "correct but INCOMPLETE" (refute verdict, refuted=true). Snackbar rides the app-root `ScaffoldMessenger` (main.dart:4843, above the Navigator) so it re-presents OVER the pushed group chat = the user's "as soon as moved to the chat". FIX = delete the 3 `_showSnackBar` calls (keep every navigation call; `_showSnackBar` KEPT — 7 other callers). Tests: invert 11 "snackbar appears" asserts to `find.byType(SnackBar) findsNothing` KEEPING every `GroupConversation{Screen,Wired}` nav assert (group_list ×6 incl. TC-08 nav-preservation lock; orbit ×5 — stale-drain S9/S10 keep the `group.name=='test 3'`/title discriminator so latest-vs-stale survives) + **DELETE TC-03a** (its sole premise — the recovery-key wiring — is reversed). Optional Part C: prune now-dead `group_invite_joined`/`_recovery` from en/de/ar `.arb` + regen (parity-guarded by `l10n_integrity_test`). REFUTED/do-not-re-introduce: group-list-only scope; `group_removed_snackbar`/read-only-banner/`catching_up` (not join flows). 13 spec cases / 13 rows, all widget-tier, **host-only closure** (no DB/crypto/relay/OS-boundary, no `/sims`, no device-proof); both test files already in `GROUP_TESTS` (run_test_gates.sh :227/:229) + auto-glob → ZERO new registration. |
| [209-settings-qr-scan-migration-mockups.html](209-settings-qr-scan-migration-mockups.html) | MOCKUP EXHIBIT (user request 2026-07-04: migrate the Orbit "My QR"/"Scan" chrome pair into Settings under the profile picture + nickname, and improve the Settings page; recon workflow `wf_32ecdb30-15d`, 3 graphify-explorer agents, all values source-verified). Four full Settings-page designs at increasing improvement amplitude: **A "Twin Pills"** (labeled green pills under the nickname, page otherwise untouched, diff ≈ 1 Row); **B "Identity Card"** (avatar+nickname+peer-ID+44px QR thumb+My QR/Scan buttons fused into one glass card + named sections, controls unchanged — exhibit's design pick); **C "One Screen"** (QR/Scan as two large tiles + every card compressed to grouped rows with values opening focused sub-sheets; whole page fits 390×844 with zero scroll — biggest cleanup; **USER CHOSE C**); **D "Personal Orbit"** (dashed ring r78 around the avatar with QR/Scan as 196-C-vocabulary satellites — considered-and-declined: ceremony taxes a utility page). Shared notes pin the migration constraints: QRScannerWired's ~30-dep FeedWired-rebuild contract (prefer `switchTo(feed)`), orbit post-scan refresh must move off `.then(_applyRouteChanges)` to `externalRouteChangesListenable`, Settings already owns the migration-QR deps, `OrbitQrChromeButtons`+dead `qr_action_cards.dart` retire. Headless-Chrome screenshot-verified; honors prefers-reduced-motion. |
| [209-settings-qr-scan-migration-spec.md](209-settings-qr-scan-migration-spec.md) | FEATURE IMPROVEMENT SPEC for the chosen Option C (Explore evidence, source-verified file:line): Orbit's icon-only 40×40 QR/Scan chrome (`orbit_qr_chrome_buttons.dart`, 196 Option B) is invisible from Settings — the identity surface since 206 — and Settings is ~2.5 screens of ungrouped always-expanded cards. Target behavior: profile header → two large labeled My QR/Scan tiles (RTL-mirroring, unlike the chrome's forced LTR) → IDENTITY group (peer-ID row w/ full-ID copy, recovery-phrase row → blur/reveal sub-surface) + PREFERENCES group (background/photo/video rows with at-rest values opening focused sub-surfaces, inline nearby switch, move-account row keeping its 206-gated key) — zero scroll at 390×844, graceful scroll at SE-class/1.5× text. Pinned invariants: INV-196-7/8 survive at the new host (destination contracts: noIdentity, self-scan/expiry rejection, ScanFriendCard loop, en/ar/de strings), INV-206-1..6 untouched (center-avatar entry, single-flight latch, single-mechanism change-kind propagation), FTE scanner + posts_wired `showNavigationBar:false` entry unaffected; post-scan outcome-level contract (functioning Feed, no `_missing*` StateError, orbit shows new contact without restart). Factual breaking-test inventory: `orbit_qr_chrome_buttons_test` (whole file), `orbit_qr_entry_migration_test` (GROUP_TESTS :254), `settings_screen_test` layout asserts, `settings_wired_test` inline-control finders; TC-206-02/23 content asserts. 48 TCs in 7 groups (A tiles/entries incl. ar-RTL + posts entry; B chrome retirement + no dead hit-regions; C one-screen layout/values/text-scale/daylight; D sub-surfaces incl. save-failure revert + mid-save back-nav; E peer-ID copy/nearby switch/move single-flight; F Settings-hosted scan flows incl. migration-QR + mutual-add side effects; G propagation/l10n-integrity/latch/cleanup). Spec only — no solution; tdd-plan chains next. AMENDED same day post verify→refute (§7): posts entry = dead code → tiles conditional-mount; avatar 72 per chosen mockup; no-scroll TC qualified textScale-1.0/debug-cards-excluded. |
| [209-settings-qr-scan-migration-tdd-plan.md](209-settings-qr-scan-migration-tdd-plan.md) | TDD PLAN for the 209 spec (grounding+refute workflow `wf_6454f379-fa7`: 3 ground + 4 refute; 3 claims REFUTED, 1 survived). **Architecture = host-built callbacks, NOT dep threading**: `SettingsWired` gains optional `onMyQrRequested`/`onScanQrRequested` (`Future<void> Function()`) + `_qrRouteActive` latch; tiles conditional-mount when both non-null (INV-206-1 pattern); OrbitWired passes closures wrapping its UNCHANGED `_onMyQR`/`_onScanQR` (:2220-2342) at the Settings push (:624-638) — INV-196-7 contracts, `.then(_applyRouteChanges)`, and ScanFriendCard cross-link survive verbatim; `qr_scanner_wired.dart`/`qr_display_wired.dart` byte-untouched (INV-209-2 grep gate). REFUTED/do-not-re-introduce: (1) posts_wired:539 is DEAD (PostsWired never constructed in lib/; structurally cannot host scanner — contactRequest repo/listener absent from its ctor); (2) 100px-avatar One-Screen (−58px over budget; fits ONLY pinned: avatar 72, custom 44px rows — ListTile fatal —, Switch shrinkWrap, tiles ≤72 → +2..5px slack; no-scroll test = maxScrollExtent==0, debugSection null, textScale 1.0); (3) switchTo(feed)+pop post-scan rewrite (kills BASELINE qr_scanner_wired_test :316/:354-355 — the FeedWired-rebuild contract stays; success destroys the Settings route by design, latch self-releases). Scanner dep truth: 37 params, only 8 needed by scan/parse/migration — 29 exist for the FeedWired rebuild. externalRouteChangesListenable has ZERO publishers (dormant plumbing — not used). Edits E1-E8: SettingsQrTiles + SettingsGroupCard/SettingsListRow (NEW widgets), settings_screen One-Screen restructure (row keys settings-row-*, move row KEEPS settings-move-account-action), settings_wired sheet launchers via showModalBottomSheet (204 attach-sheet style, FriendPicker Theme-extension tone) REUSING BackgroundChoiceControl/ImageQualityToggle/SettingsRecoveryPhraseCard + existing handlers (INV-206-6 single mechanism), orbit closure wiring + chrome retirement (2-layer required-param sweep, KEEP 56px list padding), delete orbit_qr_chrome_buttons+qr_action_cards+tests, 2 new l10n keys (settings_section_identity/preferences) en/ar/de + gen-l10n (orbit_my_qr/orbit_scan KEPT — parity getters + tiles consume). 48 spec TCs → 48 matrix rows, zero empty cells, ALL widget-tier, **host-only closure** (no migration/sim/device — 205/206/208 class); 4 new test files (~25 tests, AUTO-glob) + orbit_qr_entry_migration_test REWRITTEN IN PLACE (stays GROUP_TESTS :254 — never delete/rename) + settings_screen/settings_wired rewrites keeping 206 kind-locks :907/:930/:970 verbatim; PROD-CRITICAL = full chain center-avatar→Settings→Scan tile→valid QR→fresh FeedWired w/ forwarded runner. Sentinels: BASELINE 7/7 untouched, orbit_settings_entry_test 14/14, posts_nearby 3/3, l10n 7/7. First textScale test in repo (no precedent — platformDispatcher.textScaleFactorTestValue). **EXECUTED 2026-07-05**: RED-first (compile-RED documented) → E1-E8 → direct 222 green; gates: baseline PASS (scanner 7/7 untouched, INV-209-2 grep clean), feed 292, groups 1110, l10n 7/7, feature-host-all green EXCEPT pre-existing `group_conversation_wired_bg_task_test` red (fails at clean HEAD too — NOT 209); 3 key mutations re-red (latch, pair-gating, avatar-72 fit); 4 out-of-inventory chrome retargets (feed_wired_test 206-chain via row→sheet, orbit_wired_test inversion, TC-203-18 pair leg → resurface-lock, TC-205-06 toggle-only); integration settings smoke migrated to row→sheet. |
| [207-orbit-intro-dock-top-row-tdd-plan.md](207-orbit-intro-dock-top-row-tdd-plan.md) + [mockup](207-orbit-intros-under-orbit-mockups.html) | TDD PLAN (FEATURE IMPROVEMENT, user-ratified Option A of the 207 mockup as REVISED 2026-07-05: intro dock in the TOP chrome row beside the view toggle — NOT under the rings — folding friend intros + pending group invites; grounding workflow `wf_7e1b0fcc-6cc`: scout + 2 ground + 7 verify + 7 refute + obligations, **7/7 claims CONFIRMED, 0 refuted**; independent sufficiency review applied 2 blocking + 3 minor fixes). **Design:** `OrbitIntroDock` (dock/remnant states, facepile = UserAvatar×≤2 + group GLYPH — invites have NO avatar pre-join) mounted as keyed Layer 4c `Positioned(top: safeTop+8, left: 64, right: kIntroDockRightReserve=168)` BEFORE the FAB (211 trailing-scan trap), gated `innerCircle && !innerEditing && reviewCount>0`, reading the ALREADY-published list projection (nav badge precedent :598); tap = net-new `_onIntroDockTap` (**C2: `_onFilterChanged('intros')` alone never flips `_viewMode`** — must setState viewMode + tab together; discriminator flow event `ORBIT_INTRO_DOCK_TAP`, initialFilterTab route must NOT emit); destination COMPLETE (C6: Intros tab already renders PendingGroupInviteCard + IntroRow); dismissal → 32px dashed remnant + **UNSEEN badge semantics** via net-new seen-set store (**DB v95** `intro_review_seen`, item_key `intro:<targetPeerId>`/`invite:<groupId>`; migration 020's contacts-column precedent NOT reusable — per-contact keying can't cover invites; registry = app_database_version.dart 94→95 + main.dart onUpgrade, version pinned by TC-207-23), `computeUnseenReviewKeys` pure use-case, feed_wired `_refreshOrbitBadgeCount` consumes the same store. **INV-4 empty-seen ⇒ unseen==raw** keeps all 12+ raw-count badge locks green unmodified; Intros-tab count + list banner stay RAW (deliberate, locked TC-207-20). 24 TCs / 23 matrix rows zero empty cells, widget+wired+unit+repo(real SQLCipher)+migration tiers, **host-only closure** (no OS/crypto/relay; PROD-CRITICAL = wired tap chain TC-207-10). SUPERSEDES orbit_view_split_test:262-294 (the only inner-view intro-absence lock); sentinels UNMODIFIED: TC-211-11/14/21/22 pill geometry/identity/scrim, 209 T3 top-center tap, 193 initialFilterTab locks. NEW screens/orbit_intro_dock_test (14, AUTO + GROUP_TESTS pin), widgets/orbit_intro_dock_test (2, AUTO), 095 migration test (2), seen-repo test (2), unseen unit test (4); 3 l10n keys en/de/ar + parity list (l10n_integrity run directly — in no curated gate). Out of scope: long-press total-hide, Undo snackbar, Option D tray (dock tap = its future hook), seen-set GC. SEQUENCING: after 211 commits — orbit_screen/orbit_wired under live concurrent edit, re-anchor at step 0. |
| [210-group-offline-send-snackbar-clock-tick-tdd-plan.md](210-group-offline-send-snackbar-clock-tick-tdd-plan.md) | BUG (user request 2026-07-04: "group chat, send with no internet — message saved+sent later (correct), but NO 'no internet' snackbar like 1:1 AND a single tick shows immediately though nothing sent; want a clock while queued, snackbar like 1:1, tick only once it actually sends"). Debug via 2 workflows (`wf_6085d748-059` ground+synthesize, `wf_a706cfb8-f89` verify→refute — both gaps SURVIVE, nothing already-fixed). **Two independent gaps:** (1) group send path reads `p2pService.currentState` only for `.peerId` (`group_conversation_wired.dart:319`), **zero** `relayReady` — no offline snackbar (failure helpers `_restoreComposerSnapshot`/`_markFailedTextMessageWithoutComposerRestore` default `showSnackBar:false`, never passed true); (2) optimistic row hardcodes `status:'sending'` (:1972 text, :3845 voice) shown before the awaited send, and shared `LetterCard` legacy `_statusIcon` (:1078) maps `'sending'/'pending'/'sent'`→single `done_rounded` (group never sets `transportStatusGlyph`→legacy path :1132). **Product decisions (asked):** clock-not-tick while queued (stricter than 1:1, which intentionally shows a tick offline — LEFT alone); **group-only**. **Design:** NEW `GroupMessage.statusQueuedOffline='queued_offline'` (resolves the `'pending'` overload — `'pending'` STAYS a tick for online in-doubt) → `Icons.schedule_rounded` clock in legacy `_statusIcon` only; snackbar mirrors 1:1's HARDCODED const "Will send when you're back online" + `wifi_off_rounded` + blueGrey[700] floating (NOT l10n — parity w/ `conversation_wired.dart:2585`); optimistic insert uses `queued_offline` when `!relayReady` (text+voice), post-await offline branch (after terminal `SendGroupMessageResult` checks) persists `queued_offline` + snackbar + NO composer-restore; **recovery predicate extended** — `dbRecoverStuckSendingGroupMessagesFn` (`main.dart:1498`) `WHERE status IN ('sending','queued_offline')` so re-send is NOT stranded (group re-send is app-resume-only; no foreground retrier — out of scope). **Preservation landmine:** shared group test `FakeP2PService()` defaults `relayReady==false` → flip setUp to online `NodeState` so 168 existing sends stay ticks. 10 TCs / 10 matrix rows (widget + 1 real-SQLCipher repo), zero empty cells; **host-only closure** (no transport/OS/crypto/relay leg changed — nearest-critical = recovery predicate, anchored by real-DB TC #9). letter_card_test/group_conversation_{wired,screen}_test already in curated arrays (ONE_TO_ONE:73/GROUP:224-226) → new methods auto-run; verify/append repo+use-case tests to GROUP_TESTS. |
| [211-orbit-chrome-toggle-glyph-online-indicator-spec.md](211-orbit-chrome-toggle-glyph-online-indicator-spec.md) | FEATURE IMPROVEMENT SPEC (user request 2026-07-05, two Orbit chrome changes; Explore evidence source-verified file:line). (1) Restyle the top-left `OrbitViewToggleButton` to the 207-mockup chrome vocabulary: bullet-list glyph (`IC.list`) on inner-circle view / dashed-orbit glyph (`IC.orbit`) on all-chats view, dark `rgba(16,18,24,0.75)` circle + 0.5px light hairline — **visual only**: behavior, `ValueKey('orbit-view-toggle')`, `orbit_view_toggle_to_*` semantics, physical `Positioned(left:16)` RTL stance, and sculpt-edit hide all unchanged; 207's intros affordances stay out of scope (207 = unratified mockup, its QR/Scan center pair is stale post-209). (2) MIGRATE (move, not copy) `ConnectionStatusIndicator` from the Feed header (`feed_header.dart:38-41`) to Orbit top-right next to the `ExpandableFab` "+" (`orbit_screen.dart:698-715`) — same widget contract (5 badge states, passive non-tappable, `TIME_TO_ONLINE_BADGE_WIDGET` once), signal already in `OrbitWired.p2pService` (:113) but `OrbitScreen` needs new plumbing; deliberately revises 206's "preserve indicator in Feed header" invariant (`feed_header_test.dart:41` breaks by design; username editing must survive). 33 TCs in 7 groups (A toggle glyphs/dark-chrome/RTL/bg-extremes; B toggle behavior regression; C Orbit placement incl. FAB-scrim precedence + 1.5× text-scale; D behavior at new host incl. seed-from-currentState + no-dup flow event; E Feed removal; F cross-screen transitions; G regression locks). Spec only — glyph asset choice (material vs custom painter) + theming integration are tdd-plan decisions. AMENDED same day post verify→refute (§8, workflow `wf_080d7669-809`): (1) container restyle is a NO-OP on dark tone — `surfaceSubtle`/`border` dark tokens (`background_readable_colors.dart:65/:68` = 0xBF101218 / 0x80FFFFFF) EXACTLY equal the mockup rgba values → change 1 is glyph-only, TC-211-03/04 become token locks (hardcoding would break light tone undetected); (2) Feed is NOT the only host — FirstTimeExperienceScreen:143-149 also mounts the indicator (untouched), and node_readiness.dart + device-real harness import the widget FILE for healthFromState → file must stay put; (3) only the toggle hides during sculpt-edit (FAB unconditional) — pill hides because the edit banner spans the full top width; (4) flow event has NO latch — instance-level transition check, remount-while-ready doesn't re-emit, sole external consumer is a non-asserting headless print loop. |
| [211-orbit-chrome-toggle-glyph-online-indicator-tdd-plan.md](211-orbit-chrome-toggle-glyph-online-indicator-tdd-plan.md) | TDD PLAN for the 211 spec (grounding workflow `wf_080d7669-809`: 4 verify + 4 refute graphify-explorer agents; 3 claims corrected → spec §8). **Design:** E1 glyph-only ternary swap at `orbit_view_toggle_button.dart:36` → `Icons.format_list_bulleted` (inner) / `Icons.motion_photos_on` (all-chats; closest stock dot-in-dashed-ring; CustomPainter fallback named via `_DashedBorderPainter` precedent) — container tokens UNTOUCHED (dark tokens already = mockup; hardcode = refuted, light-tone lock TC-211-03/04); E2 OrbitScreen gains OPTIONAL `P2PService? p2pService` (secureKeyStore threading precedent; zero test-constructor breakage — all 4 direct-pump files funnel through 1 helper each) + **Layer 4b** `Positioned(top: safeTop+8, right: 64, height: 40, Center(pill))` with `ValueKey('orbit-connection-indicator')`, gated `p2pService != null && !innerEditing` (edit banner spans full top width), mounted BEFORE the ExpandableFab so its `Positioned.fill` scrim covers the pill (FAB stays LAST child — trailing-scan keeps `_ExpandableFabState` alive) and OUTSIDE the viewMode conditional (no remount across toggles ⇒ no flow-event re-emit vector); E3 one-arg pass `orbit_wired.dart:2437`; E4 FeedHeader loses param+block (feed_screen.dart:210 stops forwarding; FeedScreen.p2pService KEPT dead — churn avoidance, follow-up owns removal); E5 pin new test into GROUP_TESTS + fix stale run_test_gates.sh:255-257 comment. **Do-not-re-introduce:** hardcoded rgba chrome; deleting/moving connection_status_indicator.dart (FTE mount + node_readiness/device-real harness importers); "all chrome hides during edit". 33 spec TCs + PROD-CRITICAL (orbit_wired_test::TC-211-34 wired chain) → zero empty matrix cells, ALL widget-tier, **host-only closure** (no DB/migration/sim/device; nearest transport leg = unchanged stateStream contract). New: orbit_view_toggle_button_test (AUTO), orbit_connection_indicator_test (AUTO + GROUP_TESTS pin; whole file compile-RED on HEAD — no p2pService param), feed_header_test REWRITTEN IN PLACE (stays FEED_TESTS:184), TC-211-34 appended to orbit_wired_test (GROUP_TESTS:229). Sentinels ZERO-EDIT: connection_status_indicator_test (analyzer-baseline TSV pins its lint fingerprints), orbit_view_split, orbit_sculpt_summon, l10n parity 7/7; INV-211-3 grep gate = no diff in connection_status_indicator.dart/first_time_experience_*. Scrim-precedence test = tap pill while menu open → menu closes (mutation: mount after FAB → red). Baselines: feed 292 / groups 1110 (+new); pre-existing red group_conversation_wired_bg_task_test ≠ 211. |
| [212-orbit-search-placement-mockups.html](212-orbit-search-placement-mockups.html) | MOCKUP EXHIBIT (user request 2026-07-05: the Orbit Search icon's position + size feel wrong — explore better placements; all geometry source-verified). Today-exhibit: search entry = TWO 44px icon-only glass twins — rings-view find pill floating right:16/~104up (borderless, `inner_circle_interactive_surface.dart:973-1048`) + all-chats trigger crammed inline in the persistent nav Row's right slot (`orbit_screen.dart:656-696`). Four options: **A "Find" capsule bottom-center** (48px labeled glass capsule above nav, morphs into the existing full-width bar; interactive expand demo w/ keyboard+chips+matched-node glow; exhibit design pick), **B fused into the nav pill** (Feed·Orbit·|·search action slot; runner-up), **C top chrome beside create** (conventional, worst reach), **D persistent top search bar** (WhatsApp-style; declined — permanent vertical tax vs the cosmos identity). Post-209 chrome (no QR/Scan pair). Headless-Chrome screenshot-verified; #find-open deep-link; honors prefers-reduced-motion. **USER CHOSE none-of-the-above: keep the simple circle, improve visuals, bottom-right float → 212 tdd-plan.** |
| [212-orbit-search-trigger-visuals-bottom-right-tdd-plan.md](212-orbit-search-trigger-visuals-bottom-right-tdd-plan.md) | TDD PLAN (MODIFICATION, user-chosen "simple" variant of the 212 exhibit; grounding+refute workflow `wf_111025fb-150`: 2 ground + 2 refute graphify-explorer agents, 9/9 seam claims CONFIRMED, 2 REFUTED). **Design:** E2 `OrbitSearchTrigger` 44→52/icon 24 + Semantics(button, NEW l10n `orbit_search_trigger_semantics` en/ar/de) + opaque hit + shadow OUTSIDE the ClipOval (clipped shadow = invisible), blur 12 kept; E3 orbit_screen persistent band: nav Row loses the Spacer/Expanded trigger slot → `Center(_buildNavigationBar())`, trigger becomes floated Layer 3b `Positioned(right:16, bottom:_persistentNavBottomOffset+84)` — the EXACT rings-pill band (104 @ notched; formula `= max(16,sb−14)+84` via bottomClearance→_bandLift chain, C6) — declared BEFORE the dock layer, keeping the searchTriggerAnimation fade/scale/IgnorePointer wrapper (dock fully covers the band when open — pre-solved choreography; scroll-hide `orbit_wired.dart:1963-1979` untouched); E4 rings pill restyled IN PLACE 52/24 + glassBorder + shadow. **REFUTED/do-not-re-introduce:** type-reusing OrbitSearchTrigger on the rings surface (flips 4 findsNothing locks; deferToChild-in-ClipOval hit-shrink; permanent BackdropFilter over the animating canvas = 202-class perf hazard → INV-212-3 pill-has-NO-blur lock) + "Semantics is free" (drags full l10n pipeline — planned as E5, 198 two-step list-RED) + chip-strip bump (collapsed pill and chips NEVER coexist — dead configuration, band constants 96/144 untouched) + standalone 74-vs-88 mismatch (pre-existing, zero production hosts). 14 TCs/14 rows zero empty cells, ALL widget/host tier, **host-only closure**; PROD-CRITICAL = feed_wired shell-path search flow (:1035-1087, FEED_TESTS:182). NEW orbit_search_trigger_placement_test (band parity INV-212-1, RTL physical-right, standalone freeze, nav re-centering; AUTO + GROUP_TESTS pin — coordinate with concurrent session 211's same-array edit) + 2 wired TCs (dock-open tap-inert, scroll-hide — first-ever scroll-hide lock); REWRITES: loading:519-561 inline-lock (asserts the opposite of the plan), trigger test 44-pin, find_pill TC-205-01. Baselines: groups 1110 / feed 292 / l10n 7/7; pre-existing bg_task red ≠ 212. |
| [215-accept-friend-request-opens-chat-tdd-plan.md](215-accept-friend-request-opens-chat-tdd-plan.md) | TDD PLAN (MODIFICATION, user request 2026-07-05: accepting a **contact/friend request** should immediately open the 1:1 chat of the newly-accepted user; grounding+refute workflow `wf_d419ce11-c6b`: 4/5 agents confirmed, gap SURVIVES adversarial refute). **Root cause = 3 in-app `_acceptRequest` handlers stop short of opening the chat** (orbit_wired.dart:1930 → refresh only; feed_wired.dart:1351 → ConnectionFeedItem upsert only; first_time_experience_wired.dart:217 → pushReplacement(FeedWired), the app shell, not the chat), while the **notification-tap** materializer (`contact_request_notification_materializer.dart:96-102`) ALREADY does `getContact ?? request.toContactModel()` → openConversation — the contract to mirror. Intro (3-party) accept correctly OUT of scope (needs mutual accept + has its own post-mutual "Send message"). **User chose all 3 surfaces.** **Design:** extract `_openConversationForContact(ContactModel)` in orbit (reuse `_onFriendTap` push + `_openingFriendPeerIds` guard) and feed (reuse `_onSendMessage` push), call from `_acceptRequest` on success/notPending; FTE = keep `pushReplacement(FeedWired)` + `settleShareIntentFlow` and **push ConversationWired on TOP** (back-stack [FeedWired, ConversationWired]) — NOT a shell-replace (refute: would orphan the chat + skip share-settle). **3 test pitfalls locked:** PRE-SEED `contactRequestRepo.seed([request])` (else notFound→false-negative RED), DRAIN the 5s profile-download `Future.delayed` timer (`pump(seconds:6)` or teardown "Timer still pending"), orbit_wired_test lacks the `ConversationWired` import (add it or assert via `_RecordingNavigatorObserver`). 8 TCs / 9 matrix rows zero empty cells, ALL widget-tier, **host-only closure** (pure in-app nav — no DB/migration/OS/crypto/relay leg; accept/reciprocate/key-exchange unchanged & sentinel-locked). Registration: feed_wired_test in FEED_TESTS:182 (→`feed`), orbit_wired_test in GROUP_TESTS:230 (→`groups`), FTE test AUTO-glob feature-host-all. Sentinels: materializer test (contract), accept_and_reciprocate/flow tests, feed upsert preserved (TC-04), FTE Feed-shell+share-replay preserved (TC-06/07, `activeTab==orbit` — consistent with 214). |
| [214-orbit-home-post-onboarding-landing-tdd-plan.md](214-orbit-home-post-onboarding-landing-tdd-plan.md) | TDD PLAN (MODIFICATION, user request 2026-07-05: post-onboarding QR-scan/first-accept landings + cold start must show **Orbit, not Feed — Orbit becomes the main screen**; grounding workflow `wf_b93c257f-4ef`: 1 scout + seam + test-inventory + 8 verify→refute pairs, **8/8 claims CONFIRMED, 0 refuted**). **Root cause = designed default, one seam:** all three landings (scan `qr_scanner_wired.dart:382-434` pushAndRemoveUntil, accept `first_time_experience_wired.dart:243-291` pushReplacement, cold-start `startup_router.dart:351-459`) push the SAME `FeedWired` shell (hosts BOTH panes) and none calls `switchTo` — the surface is decided solely by `AppShellController.activeTab`, in-memory, default `feed` (`app_shell_controller.dart:21-27`), bare-constructed exactly once in prod (`main.dart:2403`), never persisted. **Fix = flip class default + invalid-coercion fallback to `AppShellTab.orbit`** (zero nav-site edits; `feed_wired.dart:373-381` initState already honors orbit-initial — mounts orbit host, seeds swipe 1.0). Rejected main.dart-injection variant (makes flipped widget tests fixture-vacuous). **Do-not-touch (confirmed self-adapting/deliberate):** `openIntroNotificationOrbitRoute` returnTab restore (doubly-guarded no-op under orbit-home), posts-notif forced `switchTo(feed)` (TestFlight posts-hidden), orbit exits→feed, six `popUntil(isFirst)` sites (land on current tab), `ID_STARTUP_ROUTE_FEED` event name kept+test-locked. 8 TCs/8 matrix rows zero empty cells, unit+widget+integration(fake-net journey) tiers, **host-only closure** + manual two-sim screenshot smoke (camera QR not sim-automatable; intro sims assert DB not UI by design); no DB migration (tab deliberately unpersisted — restart ALWAYS lands Orbit, locked). FLIPS: posts/phase1 `'defaults to feed…'`, 5q (qr_scanner, BASELINE:10), 5o (FTE) + buffered-intent sibling audits; NEW startup_router_home_surface_test (first-ever normal-path root landing lock) + home/integration/onboarding_landing_surface_test (**PROD-CRITICAL** FTE→accept→Orbit journey) — both + FTE test **pinned into BASELINE_TESTS**; TC-214-07 re-verifies 163 off-screen pause + swipe-back from orbit-INITIAL mount (RED-on-HEAD TBD; if red → in-scope wiring fix). Execution hazard: ~28 test files construct the controller bare — pin `initialTab: feed` ONLY in feed-first-arranging suites (feed_wired/feed_swipe/feed_focus/settings round-trips), orbit_wired_test's 22 sites expected unaffected; baselines feed 292 / groups 1110; pre-existing bg_task red ≠ 214. |
| [216-cold-start-online-inbox-proof-kick-tdd-plan.md](216-cold-start-online-inbox-proof-kick-tdd-plan.md) | TDD PLAN (BUG, from the device-measured RCA `project_time_to_online_30s_inbox_proof_healthcheck_gate` + 8-agent verify→refute workflow `wf_805b0b0b-92e`, all 4 dimensions survived). **Symptom:** cold-start badge sits "connecting" ~30s before "online" though the relay is reachable in ~1.5s (device-measured Pixel 6). **Root cause = send/inbox readiness asymmetry:** badge is `connecting` until `usabilityReady = isStarted && sendCapabilityReady && inboxCapabilityReady` (`node_state.dart:158`); the relay-became-healthy branches (`p2p_service_impl.dart:4340`/`:4508`) kick `_retryProactiveSendProofIfNeeded` (SEND) but have **no inbox counterpart**, so `inboxCapabilityReady` falls through to the first `Timer.periodic(30s)` health-check tick (`:768`/`:2746`, interval `:368`). The mirror already exists one-way (`_recordSuccessfulInboxProof`→`_retryProactiveSendProofIfNeeded` at `:3066`); the send→inbox half is missing. **Fix (verified over `performImmediateHealthCheck`, which is heavyweight + guard-NO-OP-able via `_isHealthChecking`/`_recoveryInProgress`):** in the private send-proof-store branch (`:3175`, the `if (stored)` arm — NOT the public `recordSuccessfulSendProof` @override), add `if (!_currentState.inboxCapabilityReady) unawaited(_drainOfflineInbox());` — a targeted, deterministic drain that records the inbox proof, guarded/re-armed per `_beginReadinessProofWindow`. Collapses ~30s→~1.5s, shared Dart (iPhone+Pixel). **Key tiering finding:** the sim/`benchmark_time_to_online` (sendable<6s) does NOT reproduce — fast localhost relay lets the startup drain win the race the device loses; so host floor reproduces deterministically via a controlled `_FakeBridge` ordering, and the closure is a **device-proof** (PROD-CRITICAL, `Reservation opened`→first `Retrieved pending` gap). 3 RED host TCs (kick-on-store, once-per-window guard, new-window re-arm) + 3 preservation sentinels (189 drain-per-tick 11/11, benchmark<6s, cold_start_sendable) + device closure; zero-empty-cell matrix, **no DB migration**. NEW `test/core/services/p2p_service_impl_inbox_proof_kick_test.dart` (AUTO core-host glob + append to `ONE_TO_ONE_TESTS`:149 beside the 189 drain test). Refuted/do-not-re-introduce: "189 already fixed it" (189 = degraded-drain starvation, zero cold-start hunks), `performImmediateHealthCheck` seam, "reservation-open gives an earlier Dart hook". |
| [217-wake-token-cv14-activation-and-flag-seam-guard-tdd-plan.md](217-wake-token-cv14-activation-and-flag-seam-guard-tdd-plan.md) | TDD PLAN (FEATURE IMPROVEMENT + MODIFICATION, from the go-libp2p 1:1-connectivity survival audit 2026-07-06; design workflow `wf_3aa586b1-383` = 5 design + 5 adversarial agents, source-verified). **Two audit-surfaced slices.** **(A) Activate the DORMANT CV-14 wake-token end-to-end** (FDC-09 §12 access-token wake gate — "only a contact presenting a valid token can wake your push"). Go/bridge/relay/**native** are complete + host-tested + dormant-safe; ONLY Dart wiring + an unbuilt distribution transport are missing (`IssueWakeTokensUseCase`/`WakeTokenStoreImpl` constructed nowhere; `callP2PInboxStore` sends no `wakeToken`; no sender-side received-token store). **Carrier = the v2 (encrypted, Ed25519-signed) contact_request envelope** (only per-contact-private + authenticated + already-reaches-existing-contacts channel; reuses the ML-KEM re-announce backfill). **Adversarial CAUGHT a break:** the receive-side signature reconstruction allowlist (`handle_incoming_message_use_case.dart:277-284` = `{mlkem?,ns,pk,rv,ts,un?}`) omits `wt` → signing `wt` on send without adding it to reconstruction drops EVERY contact_request post-backfill (breaks contact-add + key rotation) → **TC-A02 anti-break** + receiver-first phased rollout (mirrors `mlkem`/`un` precedent). Ships **DARK**: sender `wt` emission gated behind `MKNOON_EMIT_WAKE_TOKEN` (default OFF); relay `wakeTokenGateEnforced` env-flip is a SEPARATE later relay-ops slice (INV-2/CV-19 — flipping before sender saturation hard-silences pushes). Single funnel `storeInInboxDetailed:4642`; store keyed `fdc09_received_wake_tokens` + in-memory cache (hot-path); own anti-rollback ts (NOT the ML-KEM `:342-344` guard); reconcile-down prune on archive/block; register set once/cycle (avoid N²). PROD-CRITICAL = cross-device mint→distribute→attach→authorized-wake sim (`wake_token_authorized_wake_proof_test.dart`; directionality only catchable at sim tier). **(B) Close flag-seam landmine #1** (partial Dart feature-flags map ⇒ Go zero-value FALSE, silently darkening e.g. `enableLibp2pLANDial`): Go merge-at-decode (`bridge.go:572` field → `map[string]bool` + `MergeFeatureFlagsOverDefaults` starting from `DefaultFeatureFlags()`) — the only release-safe layer — + a switch-completeness reflection guard + a Dart 9-key polarity pin (the existing `p2p_bridge_client_test.dart:162-165` assert is tautological). 16 RED tests / 20 matrix rows (unit + widget + Go node/bridge/relay + sim), zero empty cells; **no DB migration** (SecureKeyStore). Refuted/do-not-re-introduce: native dispatch is NOT deferred (already wired both platforms, stale `go_bridge_client.dart:156-159` comment); `issueForContacts` returns bool not token; wiring-alone changes NO behaviour (inert foundation). Out of scope: relay enforcement env seam, emission flip, testpeer `boolFromMap` 5/9-key darkening (`cmd/testpeer/commands.go:257`), relay set durability. **↳ AUDIT (`/tdd-review` 2026-07-06, 9-agent source-verified — fix-list `217-review-fixlist.md`): READY-WITH-TIGHTENING (D1 80·D2 76·D3 71·D4 67·D5 59). Core bet SOUND, every file:line zero-drift. 4 material fixes before execution: (M1) Step-6 per-send resolver `issueForContacts([peerId])` fires the N² relay-register it forbids (`issue_wake_tokens_use_case.dart:60` re-registers full set when map non-empty) → hoist register once/cycle + per-send resolver READ-ONLY; (M2) TC-A13 unrunnable in-scope (needs gate flipped but Scope-Guard forbids + sim hits deployed fail-open relay) → SPLIT A13a gate-independent runnable-now / A13b gate-enforced deferred to relay-ops; (M3) INV-2 prose-only not test-locked (TC-A14 flips the gate, TC-A01 fake resolver — neither pins default-OFF) → add default-disabled-at-rest + real-resolver-env-unset tests + pin emission mechanism (`--dart-define` never-in-release); (M4) "No asymmetry" (line 221) WRONG — funnel serves group/intro/post/receipt stores → non-1:1-contact recipients suppressed post-enforcement → correct + name enforcement-slice decision. User LOCKED: split TC-A13, split Part B into own plan (219+, host-closeable now).** **↳ REVISED 2026-07-06: all fix-list items APPLIED — plan retitled "CV-14 Wake-Token Dark Landing", now WAKE-TOKEN-ONLY (18 TCs / 18 matrix rows; §A1 N² killed, §A3 asymmetry corrected + enforcement OPEN DECISION, §B1 TC-A13→A13a-now/A13b-deferred, §B4 binary-freshness A18, §C1 INV-2 locked by A16+A17, §C2 emission `--dart-define`/never-in-release, §C4 saturation trigger, §D1-D3 store-schema/marker/extract-placement pinned). Part B extracted → 219.** **↳ RE-REVIEWED 2026-07-06 (10-agent `/tdd-review`, revised in place): the revision INTRODUCED 2 must-fix defects, both now APPLIED — (1) A13a (the SOLE PROD-CRITICAL closure) was **un-runnable**: distribution is emission-gated but Scope Guard forbade defaulting `MKNOON_EMIT_WAKE_TOKEN` ON with no sim carve-out → added a **sim-local `--dart-define=MKNOON_EMIT_WAKE_TOKEN=true`** on the proof build + a Scope-Guard carve-out; (2) A16 **self-contradicted** (listed both Expected-RED and green-sentinel) and could **never go RED** (no `TestWakeTokenGate` on HEAD → `-run` matches 0 tests → exits PASS; `wake_token_store.go:34` already `false`) → reclassified as a mutation-verified green default-lock. + 5 tightenings (A03/A04 whole-file gate vs the F2 0-test hazard; A17 production-owned `shouldEmitWakeToken()` factory to avoid a fake-lock; per-slice GREEN gates; 1:1-only C4 saturation denominator; C2 release-omit CI logged as emission-flip dep). Scores D1 82/D2 72/D3 77/D4 77/D5 62. Core bet re-confirmed SOUND, all load-bearing anchors re-verified exact. **NOW EXECUTION-READY.**** |
| [219-feature-flag-decode-seam-merge-guard-tdd-plan.md](219-feature-flag-decode-seam-merge-guard-tdd-plan.md) | TDD PLAN (MODIFICATION — split from 217 per review fix-list §E1; verified orthogonal: Part A sends no featureFlags + edits no `bridge.go`). **Close flag-seam landmine #1** (go-libp2p audit): the Go bridge decodes the Dart feature-flags map into `*node.FeatureFlags` WHOLESALE (`bridge.go:572,592`; `config.go:206-210 EffectiveFlags` returns verbatim, replace-not-merge) → a PARTIAL map ⇒ omitted key unmarshals to Go zero-value FALSE (not the Go default), silently darkening e.g. `enableLibp2pLANDial` fleet-wide. Safe today ONLY because `defaultResilienceFeatureFlags()` always emits all 9 keys; no key-presence guard; `feature_flags_runtime_test.go` pins only the Go FALLBACK; `p2p_bridge_client_test.dart:162-165` is tautological. **Fix = Go merge-at-decode** (the only release-safe layer): `bridge.go:572` field `*node.FeatureFlags`→`map[string]bool` + `MergeFeatureFlagsOverDefaults(present)` from `DefaultFeatureFlags()`, overwrite only present keys; `:592` nil→nil else `&merged`; `EffectiveFlags` untouched (blast radius = single decode site). Tests: B01 merge-keeps-defaults (compile-RED, mutation=start from `FeatureFlags{}`) · B02 behavioural completeness (§E2: iterate reflect fields by json tag, drive Merge single-key, assert flips — impl-agnostic, NOT "switch-count reflection") · B03 Dart independent-literal 9-key polarity pin · B04 optional bridge-integration behavioural RED. **Go registration PINNED** (§D4: 5 edit sites via the addr-visibility template carrying `GOTOOLCHAIN=go1.25.0`, NOT the unpinned keyrotation `-run`). **Host-only closure, no sim/device/migration.** Out of scope: `cmd/testpeer/commands.go:257 boolFromMap` 5/9-key darkening (sim-harness follow-up); Dart↔Go default lockstep (manual sync). **↳ RE-REVIEWED 2026-07-06 (10-agent `/tdd-review` — 219's FIRST full adversarial audit; revised in place): 1 fix APPLIED — **B04** (the ONLY test on the real `bridge.go:572/:592` decode seam; B01/B02 test only the extracted pure helper) promoted optional→**MANDATORY** + given its OWN pinned `./bridge` synthetic-path branch (`GO_BRIDGE_FEATUREFLAGS`, a 6th edit site, NOT the `./node` template reuse). Scores D1 85/D2 83/D3 87/D4 82/D5 77 (all strong). Core bet re-verified exact in source (FeatureFlags read only at `:592`; config.go replace+nil-contract; 9 struct fields; testpeer 5/9; addr-vis pinned template). **EXECUTION-READY; land BEFORE 217** (shared `run_host_test_gates.sh` host-all block).** |
| [218-sqlcipher-raw-key-coldstart-tdd-plan.md](218-sqlcipher-raw-key-coldstart-tdd-plan.md) | TDD PLAN **v3 — EXECUTION-READY** (FEATURE IMPROVEMENT / PERF — Change 1 of 2 from the device-measured cold-start budget, memory `project_cold_start_budget_and_reducibility_2026_07_06`; **7-agent review fix-list `218-review-fixlist.md` APPLIED → v2, then v2 RE-REVIEWED (/tdd-review, 8-agent wf_2b86d6a6-5cd) → 5 NEW residuals CLOSED into v3**: INV-5 grep-gate was provably FALSE-green (multi-line `openDatabase` — `password: key` never shares a line with `identity.db`) → rewritten to a comment-marker convention (verified RED-on-HEAD); **§H2 `sqlite3`-package fallback DROPPED** (transitive-only + no cipher-lib binding → would ship a PLAINTEXT identity.db; primary `password:"x'<hex>'"` is now the SOLE mechanism, SC-8 GO/STOP the sole gate); SC-9 plaintext→raw DECIDED (direct, raw-literal ATTACH KEY); SC-5 baseline instrument-first (elapsedMs added in Step 0a, two-op `jq` extract); marker value-set reconciled to `{absent, raw}` (no `pass:`). Core bet re-CONFIRMED sound; scores D1 82/D2 88/D3 76/D4 89/D5 80. See memory `project_218_sqlcipher_raw_key_plan_review_2026_07_06`. **Now a TWO-RELEASE ship model** (§A rollback safety — rekey is one-way + identity.db holds the node private key, so a rollback would brick identities): **Phase A** = forward-compat read-tolerant opener (reads passphrase AND raw) ships FIRST as the floor; **Phase B** = the rekey. Also v2: added **5th open site** `background_message_handler.dart:298-303` (direct identity.db open, bypasses opener → SC-B + INV-5 literal shell grep-gate); **`user_version==95` must be set explicitly** on rekey (`sqlcipher_export` doesn't copy it → else re-runs ~80 migrations over live data, SC-2 asserts); §E2 explicit atomic swap protocol (NOT the unsafe `_encryptExistingDatabase` template); locked speed gate (`raw_p50 ≤ pass_p50/5` AND `≤15ms`, new `elapsedMs` field, 2nd steady launch); mechanism DOWN-RANKED (primary `password:"x'<64hex>'"` → `sqlite3_key`(67B) raw-key is SOUND, §H1; **v3: NO fallback — §H2 sqlite3-package fallback DROPPED, SC-8 GO/STOP is the sole gate**). **Kill the ~503ms cold-start PBKDF2 DB open.** Cold start launch→online is ~1.47s and APP-INIT bound (network/relay only ~249ms); the single biggest reducible cost is the ~503ms `openEncryptedDatabase` (`encrypted_db_opener.dart:82` opens identity.db `password:key` = SQLCipher **passphrase mode** → 256k-iter PBKDF2). The stored key is ALREADY 64-hex 256-bit CSPRNG (`_generateRandomKey:11-16`), so PBKDF2 adds ZERO security → open in **RAW-KEY mode** (`PRAGMA key="x'<64hex>'"`), collapsing KDF to sub-ms. Existing installs get a **one-time atomic passphrase→raw rekey** (ATTACH raw-keyed dest + `sqlcipher_export`, modeled on the legacy `_encryptExistingDatabase:132`; backup-swap so a failed rekey can't brick identity.db) gated on a durable **cipher-mode marker** — **NOT a schema/DB-v## change** (v95 untouched). **[v2 CORRECTION]** Account-migration Move does NOT need editing (refuted §B1, verified wf_b5c4c901-4e9): identity.db is opened ONCE (`main.dart:481`) and threaded into Move as an already-open `sourceDb` handle (`main.dart:2001`→`bundle_transfer.dart:207`→exporter `:92`), so Move inherits raw-key free; the exporter `:164/:253/:275` + importer `:185` key the transfer SNAPSHOT/probe (must STAY passphrase — cross-version wire format), NOT identity.db → SC-7 re-scoped to a decoupling SENTINEL, exporter/importer under Scope-Guard-do-not-touch. **Load-bearing UNVERIFIED risk (SC-8 feasibility gate, runs FIRST before any prod edit):** whether `sqflite_sqlcipher ^3.1.0`'s `password:"x'…"` actually yields raw-key or is passphrase-treated by `sqlite3_key()` — documented fallback = no-password open + `onConfigure PRAGMA key="x'…"`. **Cipher is DEVICE-ONLY** (host `test/core/database/migrations/*` use plain `sqflite_common_ffi`, no cipher) → closure is a device integration suite `integration_test/db_raw_key_migration_proof_test.dart` (model on `migration_database_sqlcipher_capability_test.dart`) + FDC_FLOW_LOG speed device-proof (ENCRYPTED_DB_OPEN ~503ms→≤~50ms on Pixel AND iPhone). 8 spec cases / 7 matrix rows: SC-6 host wiring (AUTO glob, seam-spy), SC-1/2/3/4/5/7/8 device (classify_path). Refuted/do-not-re-introduce: "PBKDF2 protects a full-entropy key" (false), "needs DB v## bump" (cipher-param, not schema), "host migration tests prove cipher" (plain FFI). Change 2 (de-serialize node:start, the other ~0.5s) = SEPARATE future plan. |
| [224-orbit-side-arc-tap-dead-zone-tdd-plan.md](224-orbit-side-arc-tap-dead-zone-tdd-plan.md) | TDD PLAN (BUG, user report 2026-07-09: side-of-screen arc avatars visible but taps don't open chats after enlarging avatars/arcs). **Root cause PROBE-PROVEN on HEAD + verify→refute workflow `wf_e2a9b4eb-f9f` (4 agents, both refute lenses CONFIRMED)**: viz canvas is a fixed `SizedBox(width: kOrbitCanvasSize=320)` (orbital_visualization.dart:326-335) while the geometry engine deliberately seats arcs out to |dx|=kOrbitPinchHalfWidth=170 (+release toward |dx|≈r at arcWrap>1.6); `Stack(Clip.none)` paints outside but RenderBox.hitTest rejects — dead side taps fall to the opaque background detector (surface:653-662). Vertical overhang grows the box (top+bottom); **no horizontal twin exists**. Dead zone exists at DEFAULT knobs with 2 arcs (|dx|=170 pinned). Fix = horizontal twin: E1 pure `orbitArcSideOverhang` (all-seats |dx|+tap/2 scan), E2 viz `boxWidth=320+2·so`, `cx=160+so`, LayoutBuilder-clamped to constraints, expansion-gated like HEAD's vertical; E3 surface drops scroll `padding: horizontal 24` (post-growth clamp hazard; hint re-padded) + `_CanvasMeasurement` stores WIDTH so all centre-X = origin+width/2 (invariant under symmetric growth even stale → INV-202-2 + same-frame handle seat preserved by construction); E4 rebase 3 sculpt helpers (:256/:1000/:1715) 160→width/2 (value-neutral on HEAD; ten TC-198F/223 dependents stay green). Anchors Python-derived+probe-matched: G0 av1.4/og1.5 n20 → so=31.97 box=383.94; defaults n30 → 34.0; cv2.5 → 58.19 (clamps to 35 at 390pt, cx=195, edge 194.19 tappable). 18 matrix rows zero empty cells (6 domain compile-RED, 5 widget, 7 surface; 3 preserved-green guards per 220 lesson + 1 assumption-lock), all AUTO-glob + sculpt file already in GROUP_TESTS:274; **host-only AUTOMATED closure scoped ≥390dp + REQUIRED manual sanity on the reporting device** (pure hit-testing; no OS/crypto/DB/relay). Baseline orbit suite 533/533 → expect 551. Refuted/do-not-reintroduce: background-tap router; padding-clamps-on-HEAD; clamping seats to 160 (forbidden — pinch containment test locks |dx|≤170.5). Accepted: release-band >viewport-half stays dead on ≤375pt (future horizontal-scroll session); collapsed sp1.5 ring-2 ≈1.4px poke pre-existing. **/tdd-review 2026-07-09 (8-agent `wf_6b468424-7f9` + lead Python repro): READY-WITH-TIGHTENING — core bet SOURCE-VERIFIED SOUND** (root cause, complete 3-site X-centre enum :516/:896/:957, all G0–G4 anchors, INV-224-5 measured-width invariance all reproduced). Scores D1 85/D2 80/D3 77/D4 86/D5 73. Fix-list `224-review-fixlist.md`. Tightening (none blocks core bet): (A) mutation column reasoned-not-run — 4 cells wrong/inert: TC-224-02 "25.57"→**31.77**; TC-224-12 "revert E3a→red" is **FALSE** (E3a isolated only by TC-224-13; at cv=1.0 all arcs pinch to \|dx\|=170 so a padding-clamped 342 box still seats them); TC-224-03/-05 `.abs()` mutations **structurally inert** (arcs symmetric ⇒ signed-max≡abs-max). (B) E3b `m.width` not in scope at the 3 centre-X sites — pin the `_canvasWidthNow` getter/param plumbing, never substitute the 320 constant. (C) `kOrbitCanvasCenter` 6× (2× in one Offset at :516) — X-only, no replace_all. (D) ≤375dp dead-band unguarded → user chose BOTH (add ≤375dp boundary fixture + scope closure ≥390dp + required device sanity). **↳ FIX-LIST APPLIED 2026-07-09 (counts in this row already reflect it)**: §A four mutation cells corrected (TC-02 note 31.77/~0.15px margin → robust floor proof = TC-06; TC-12 → revert-E1-or-E2 with E3a-no-op documented, discriminator TC-13; TC-03 → scan-arc-0-only 10.14≠34.0; TC-05 relabeled weak/assumption-lock — the synthetic-asymmetric-seat API refactor REJECTED as over-complication, RTL behavioral closure stays TC-11) + RUN-every-mutation is now a Done Criterion; §B E3b seam pinned 4-step into Step 5 (verified: `_CanvasMeasurement` body **:108-121**, writer **:300-304**; width scroll-INVARIANT, never compile-time 320); §C X-only rule in Step 5 + Scope Guard with VERIFIED count **7×** kOrbitCanvasCenter (fix-list prose said 6; actual 3 X-axis `:516`-1st/`:896`/`:957` + 4 Y-axis `:274`/`:516`-2nd/`:897`/`:958`); §D NEW **TC-224-18** 375×812/cv2.5/n30 fixture (flanks ±177.06 ALIVE at local 10.44/364.56, edge 194.19 center expected-OUTSIDE; mutation = raw-so-for-cx → right flank local 395>375 red); §E citations fixed, baseline-provenance row added (533 captured, "0 fail" governs), TC-224-17 committed **finder-RED** via `ValueKey('orbit-empty-hint-padding')`. **EXECUTION-READY.** |
| [225-notif-tap-last-message-lag-tdd-plan.md](225-notif-tap-last-message-lag-tdd-plan.md) | TDD PLAN (BUG, user report 2026-07-09: notif tap opens the right 1:1 chat but the ANNOUNCED message renders late — screen shows stale history, new message pops in after a lag). **Grounding via verify→refute workflow `wf_2dc4805b-7f5` (16 agents): 6 claims → 4 SURVIVED, 2 KILLED.** Survivors: C1 push payload carries the FULL encrypted envelope (kem+ciphertext+nonce, push_decrypt_preview.dart:52-54; iOS NSE even decrypts it for preview) but NOTHING persists it — the only ingestion is a post-tap relay drain; C3 `_retrievePendingInboxPage` awaits a relay ACK RTT (no timeoutMs → Go 15s default) BETWEEN stageEntries and replay, pure added latency on every page (ack-exception path already falls through to replay + staged-unacked entries dedupe ⇒ reorder is safe); C5 dead-QUIC warm tap: 3s foreground budget blown → drain "successfully empty", NO retry, recovery ≤30s health tick (FOLLOW-UP session); C6 >50 backlog oldest-first (PROVEN both relay backends) with page-1-only awaited (FOLLOW-UP). Killed/do-NOT-re-introduce: C2 cold-start reservation race (216 device A/B already refuted it — stale GoLog-misread comment), C4 banner-contract void (stream renders within 1 frame of commit, ~0ms). **Scope = Slice A: replay-before-ack reorder** (stage→gate→REPLAY→ack; migration-gated early-return byte-preserved) **+ Slice B: 1:1 push-envelope staging fast path** — file-based store (NO SQLCipher, NO DB migration), Android background handler stages at receipt, iOS NSE `AppGroupPushEnvelopeStore` (mirror of dedupe store; NSE must NEVER open SQLCipher), ingest at prepareNotificationOpen (awaited, local-only, swallow-on-throw; 145 fire-and-forget NETWORK-drain lock preserved) + startup/resume; replay through the EXISTING incoming pipeline (blocked-before-decrypt 147 policy, messageId dedupe, suppressNotification). No P2PService interface change (31-fakes hazard). 20 RED-catalog entries / 20 matrix rows zero empty cells (4 unit A + 1 real-SQLCipher A + 1 sim A custody; 10 host B + real-SQLCipher B + widget exactly-once + sim real-crypto B) — money test = notif_tap_payload_fast_path_test: tapped message visible on FIRST settled frame with drainOfflineInbox NEVER resolving (fake-tier proof = the construction itself; event-absence discriminator `P2P_SERVICE_INBOX_STAGED_DRAIN_SUCCESS` lives at the real-impl sim tier). **Closure gate = device-proof** notification_tap_message_visible_proof_test, THREE required scenarios `payload_fast_path_{ios_receiver,android_receiver,cold_kill}`: background → push → FULL AIRPLANE MODE (separate networks; LAN-direct false-green struck) → (cold-kill: force-stop) → tap → renders (categorical zero-relay-RTT; ≤2s = UX wrapper vs step-0 HEAD baseline). Registration: 4 new host files appended to ONE_TO_ONE_TESTS + 2 sim classify_path cases + proof scenario. Baseline 1to1 = 1518/1518 (2026-07-09). NOTE: numbered 225 because a concurrent session claimed 224 mid-planning. **/tdd-review 2026-07-09 (8-agent `wf_ea1fd793-f62` + lead source-verification): READY-WITH-TIGHTENING — BOTH core bets SOURCE-VERIFIED SOUND** (Slice A reorder is *strictly safer* than today: relay is non-destructive on retrieve, deletes only on ack, `backend_memory.go:156-186`/`backend_redis.go:378-423`; Slice B payload sufficient — push carries `sender_id`+`message_id`+`kem/ciphertext/nonce` `inbox.go:289-296`). Scores D1 70/D2 80/D3 57/D4 68/D5 62. Fix-list `225-review-fixlist.md`. Locked: ingest = **bounded await ~400ms** (not the plan's unbounded await — route awaits prepare before nav `notification_route_dispatch.dart:21-22` + 10s bridge timeout ⇒ up-to-10s screen stall = the 145 latency class it descends from). 4 MATERIAL: (1) iOS NSE *written* schema unpinned — omit `senderPeerId` ⇒ three-way sender check `handle_incoming_chat_message_use_case.dart:263-265` fails ⇒ EVERY iOS ingest silently rejected (host-green, TestFlight-broken; NSE has zero host/sim coverage); (2) 145 route-await stall (above); (3) B-2 undercount — `prepareNotificationRouteTarget` has TWO wrappers, cold remote-FCM tap `startup_router.dart:1031/:833` (the "app was killed" case) doesn't inherit a main.dart-only ingest param; (4) TC-B12 under-runs the OS boundary — single scenario/device-A not both required legs, "relay unreachable" false-green via same-WiFi LAN-direct, backgrounded≠killed. MODERATE: store key must be `nonce` not nullable `messageId`; `message.to`=local identity (step-7 Stop-if misdirects to relay=scope violation); file-write atomicity + clear-on-malformed deletes torn entries. **↳ Fix-list APPLIED IN FULL into the plan 2026-07-09** (§D6 receipt-timing note-only — contract inherited from 146's own locks): matrix 18→20 rows (TC-B13 single-flight double-ingest, TC-B14 cold-tap second-wrapper wiring), TC-B12 → 3 required scenarios (iOS leg also asserts NSE 04-P0 with staging ON), bounded ~400ms await + stall-path RED, NSE write-set pinned, nonce store key, atomic+young-retry writes, single dir resolver, cap-on-write, step-0 baseline. **EXECUTION-READY.** |
| [226-settings-navbar-orbit-glass-swipe-back-tdd-plan.md](226-settings-navbar-orbit-glass-swipe-back-tdd-plan.md) | TDD PLAN (MODIFICATION, user request 2026-07-09: Settings page — (1) remove the Feed icon from the navbar, (2) restyle the Orbit navbar icon to the transparent-glass look of the Orbit search trigger, (3) swipe right returns to Orbit). Grounding via verify→refute workflow `wf_194c8bc1-200` (9 agents: scout + seam + test-inventory + 3×verify + 3×refute) — **all 3 claims CONFIRMED, none refuted**: Settings mounts the SHARED `FeedNavigationBar` (`settings_screen.dart:297-308`) whose Feed/Orbit buttons are HARDCODED (`feed_navigation_bar.dart:70-84`, no hide/restyle params); the Orbit affordance is pill-style `NavBarButton` (zero glass); Settings is a bare `PageRouteBuilder` slide-up (`settings_route_transition.dart:7-34`, no built-in edge-swipe) with ZERO horizontal-gesture handling. **Design:** Settings-local `SettingsOrbitNavButton` (52px glass circle cloned from `orbit_search_trigger.dart:26-57`; shadow OUTSIDE ClipOval; `glassSurface`/`glassBorder` tokens both tones) replaces the bar on Settings ONLY; swipe = GestureDetector on SettingsWired body gated by `showNavigationBar`, drag-end qualifier (net-rightward AND ≥0.28×width OR velocity ≥900, feature-30 threshold parity), firing the EXISTING `_onSwitchView('orbit')` (switchTo+pop) exactly once; route type UNCHANGED (keeps 206 slide-up). **LANDMINE: shared-widget defaults are heavily test-locked** — 5 suites / 10+ assertion sites (feed_navigation_bar_test expectedButtons=2 + positional, nav_bar_button pill locks, orbit/feed_wired + intro-route + dark-preset `find.text('Feed')`) break if Feed removal or glass restyle leaks into `FeedNavigationBar`/`NavBarButton`/`NavBarTheme` — scope-guarded, sentinel-gated. Settings surface itself was fully UNTESTED (navbar button-set, `_onSwitchView`, swipe: zero coverage). 3 spec cases → 16 matrix rows zero empty cells, widget tier only, ALL AUTO-glob (D1 end-to-end row rides orbit_settings_entry_test already in GROUP_TESTS); host-only closure (own GestureDetector, no OS boundary; optional sim screenshot for glass render). Physical-direction RTL lock (TC-226-14), single-fire latch (TC-226-15), reopen-after-swipe-dismiss latch proof (TC-226-16 PROD-CRITICAL leg). 3 planned test-file updates: settings_screen_test byType(FeedNavigationBar)→new type ×3. **/tdd-review 2026-07-09 (`wf_a05c3362-ab0`, domain+5-dim+critic) → READY-WITH-TIGHTENING** (scores D1 88/D2 88/D3 74/D4 85/D5 78; core bet SOUND, `material_errors:[]`, in-repo precedent `swipeable_friend_row.dart:235-240`). Fix-list: `226-review-fixlist.md`. **2 material:** (M1) TC-226-07/08 tab-switch discriminator VACUOUS — `AppShellController` default `initialTab=orbit` + `switchTo` no-ops same-tab, so bare `Navigator.pop()` passes identically → seed File C `initialTab:feed`, assert feed→orbit, add mutation; (M2) plan `:702` "nested SettingsWired" claim is FALSE (it's the Move-Account push `AccountMigrationJourneyWired.oldPhone`) → real Settings sub-surfaces (Move/sheets/QR) unenumerated + no wrong-route-pop guard (`_onSwitchView` calls `Navigator.pop`) → add sub-route-up guard test + Stop-if. **Moderate:** TC-226-15 latch-vs-end-fire mutation self-contradiction; TC-226-04 missing shadow-outside-ClipOval mutation; two INV-5 sentinels (`intro_notification_orbit_route_test`, `dark_preset_preservation_test`) bound to NO literal gate cmd; matrix "compile RED"→"red vs stub"; TC-226-16 reopen leg shares TC-226-08 mutation. All other cited `file:line` CONFIRMED accurate. **↳ Fix-list critically APPLIED into the plan 2026-07-09** (lead re-verified both material claims in source before applying — the audit contradicted the grounding workflow's `:702` claim and the audit was RIGHT: `settings_wired.dart:700-717` pushes `AccountMigrationJourneyWired.oldPhone`): M1 → File C harness seeds `AppShellController(initialTab: AppShellTab.feed)`, TC-226-07/08 assert the feed→orbit TRANSITION + bare-pop mutation; M2 → false claim deleted, sub-flows enumerated, **designed `ModalRoute.isCurrent` guard** added to the drag-end handler, NEW TC-226-17 wrong-route-pop guard (modal-sheet occlusion leg + worst-case transparent non-occluding-route leg; audit's non-implementable "move detector above barrier" mutation REPLACED with "remove isCurrent guard → transparent leg pops the wrong route"), step-6 Stop-if(b) `_moveRouteActive`-style latch fallback; T1 → end-firing COMMITTED (no latch; TC-226-15 mutation = fire-on-update → >1 pop); T2 → shadow asserted on outer UNCLIPPED ancestor + move-inside mutation; T3 PARTIAL → push/theme INV-5 sentinels bound to a literal preservation line, but "206-enter unbound" sub-claim REJECTED (orbit_settings_entry_test runs in the direct-GREEN cmd AND the groups gate); T4 → stub-first behavioral RED cells; T5 → reopen-leg own mutation (neuter `whenComplete` orbit_wired:682-684); N1–N5 applied (`:147` rename, N+1 pin, half-red note, wrap-point = SettingsScreen inside the wired Scaffold body, 224px/MediaQuery-at-drag-end authoring note); N6 REJECTED (redundant — settings_wired_test taps `:414`/`:881` exercise the wrapped tree in preservation). Matrix 16→**17 rows**, File C 9→**10 tests**. **EXECUTION-READY pending 2 user decisions:** discrete over-threshold gesture vs finger-tracked drag; PHYSICAL vs RTL-mirrored swipe direction. |
| [222-mirror-cosmic-default-background-tdd-plan.md](222-mirror-cosmic-default-background-tdd-plan.md) | TDD PLAN (MODIFICATION, user request 2026-07-08: make **"Mirror Cosmic" the Default** background instead of the current glow, give the old default its own name **"Aurora"**, and migrate current-Default users to Mirror Cosmic while preserving explicit non-default picks; grounding via verify→refute workflow `wf_3a56ec66-e05` — 4 ground + refute + critic, one agent failed but its persistence/move coverage was delivered by the test-red-anchors agent; every claim CONFIRMED). **Design = "Option 1": ONE seam per concern.** `BackgroundPreference` (`background_preference.dart`): **retire `cosmicMirrored`, add `aurora('aurora')`**; render switch (`ambient_background.dart:124-136`, exhaustive) repoints `defaultBackground => CosmicBackgroundMirrored` + adds `aurora => _DefaultAmbientBackground(glow)`; `fromStorageString` remaps `'cosmic_mirrored' → defaultBackground` and keeps `'default'`/null/unknown → `defaultBackground` (so **migration is pure read-time remap + render repoint — NO DB bump, NO imperative migration**; preference lives in SecureKeyStore, single read seam `background_preference_use_cases.dart:10-11`). **CONFIRMED LANDMINE (mandatory, compiler will NOT flag): `_usesDefaultBackground` (`ambient_background.dart:157-159`) is a bare `== defaultBackground`** gating the shared glow `_controller` → must repoint to `== aurora` or the Aurora glow freezes at value 0 while `_controller` loops dead behind Mirror Cosmic (Mirror Cosmic self-animates via its OWN controller + honors OS reduce-motion internally, `cosmic_background_mirrored.dart:83-103`). Also: `toneForPreference` (exhaustive) → `aurora` dark; `settings_screen._backgroundValueLabel` (exhaustive) → `aurora` label; `background_choice_control` swaps the Mirrored-cosmic tile for an **Aurora tile** (`background-choice-aurora`) + repoints the Default tile's description to Mirror-Cosmic copy; l10n (3 arb + `flutter gen-l10n`) changes `settings_background_default_desc`, adds `settings_background_aurora`/`_desc`/`_selected`, retires `settings_background_cosmic_mirrored*`. **Account-MOVE safe with no change** (`migration_secure_storage_registry.dart:54-60` `policy:migrate/optional`; donor copies token byte-for-byte, no whitelist — recipient re-parses: old→new `'cosmic_mirrored'`→Mirror Cosmic ✓, new→old `'aurora'`→unknown→glow ✓; caveat: new-build Default user →old build renders glow, forward-compat only). **No raw token string is branched on outside the codec** (switch-census, locked by an INV-6 source-guard test). 13 spec cases + top acceptance leg / 14 matrix rows zero empty cells, **unit + widget tiers, host-only closure** (no OS/crypto/relay/SQLCipher → no device-proof; argued). RED anchors: `fromStorageString('cosmic_mirrored')==defaultBackground` (assertion-RED on HEAD), `default→cosmic-background-mirrored-root` (widget-RED), aurora-referencing tests compile-RED (new value). Updates the **9 test files** referencing `cosmicMirrored`/`cosmic_mirrored` (compile-break + flips carry the evidence); `ambient_background_test` (pinned `run_test_gates.sh:206`) + `settings_background_choice_smoke_test` (pinned :378) need no new registration, new host tests AUTO-glob. Accepted difference: glow-lovers on `'default'` (indistinguishable from never-chose) are migrated to Mirror Cosmic and re-pick "Aurora" — reversibility test-locked. Name "Aurora" + de/ar strings product-confirmable (durable contract = token `'aurora'`). **↳ /tdd-review 2026-07-08 (8-agent) = yes-with-tightening; core bet VERIFIED SOUND; fix-list `222-review-fixlist.md` APPLIED into plan: §A named the 6 default-preference tests (TC-04/05/158-01/02/03 + Feed-default) as assertion-RED (helper empty for CustomPaint) → repoint to `aurora` NOT weaken; §B render-vs-glow rebase split (render-assert→`defaultBackground`, glow-assert→`aurora`; FEED6 baseline→aurora) + added non-globbed `flutter test integration_test/feed_performance_test.dart` gate; §C1 INV-6 #10 = quote-adjacent token literal (bare-substring `'aurora'` would false-RED the whole gate via the l10n key), C2 dropped vacuous #6(d), C3 #3/#10 preserved-green, C4 SC-5b routed through `fromStorageString`, C5 SC-11→`migration_secure_storage_registry_test:63`; §D split into two gated slices (A additive→host-green checkpoint→B flip+retire, payoff-early + pre-gen-l10n green base); §E 4→5 exhaustive switches + Migration Contract table. **EXECUTION-READY.** |

| [248-signal-true-light-theme-comfort-closure-tdd-plan.md](248-signal-true-light-theme-comfort-closure-tdd-plan.md) | IMPLEMENTED / DEVICE-ACCEPTANCE-PENDING (production `c6d0058a1`, stale-copy test alignment `f029d131f`): real Signal root light ThemeData, warm palette, static blooms, lifecycle binding, semantic route/sheet/search/debug surfaces, and dark-preset/scanner preservation are host-complete. Formal TC-248-26/27 remains open at `0/23` states: it requires 46 fresh, redacted physical-Pixel captures with production Settings selection/cold restore, both font scales, restored device state, and human visual sign-off. |

**Top findings:**
- `OrbitWired` already disposes its controllers/subscriptions — that earlier P0 is stale
- The narrow DB/storage fixes worth doing were helper-local schema-capability caching, the small identity cache, the pinned-post single-load reduction, and the reload-after-update cleanup
- Targeted index work remains evidence-only; no broad SQL/index/caching program should be reopened without new profiling
- Blanket index additions, message-order SQL rewrites, and heavy SQL/materialized-view work are still not justified by default

## 3A. Received-media actions program (2026-07-09)

[Interactive received-media UX mockup](received-media-actions-ux-mockup.html) — standalone HTML prototype for 1:1, discussion, and announcement conversation/viewer/library/storage states, including Release 1 versus evidence-gated full-vision behavior.

These plans split shared device/storage/viewer infrastructure from the three
messaging lanes so local media UX cannot silently change 1:1 or group transport
semantics. The ordered schema spine is `v96` (plan `228` shared local media
state) -> `v97` (plan `232` 1:1 forwarded marker) -> `v98` (plan `235`
discussion-group deletion journal) -> `v99` (plan `236` discussion-group
forwarded marker). Every
private-media schema versions are allocated only by accepted owners: Plan 234
owns landed DB v100 and Plan 238 reserves sequential v101. Reporting and
multi-source Forward plans reserve no speculative version. Plan `248` belongs
to an unrelated theme track, so this program continues at `249`.

Plan `228` is the hard local-storage boundary for every later media plan:
attachments are explicitly `direct` or `group`, ambiguous/orphan legacy rows
remain `unresolved` and fail closed, announcement media reuses `group`, cursors
are bound to scope/filter, and later migrations extend one shared production
create/upgrade registry. Downstream plan fixtures must include same-message-ID
cross-lane collisions and must never serialize the local owner into wire or
provenance data.

Recommended execution order: plan `227` may proceed independently; land plan
`228` before any repository/storage/viewer consumer, then shared plans
`229`-`230`; lane core plans `231`/`235`/`239`; forwarding plans `232` -> `236`
-> `240`; then library plans `233`/`237`/`241`. Evidence-gated plans reopen only
after their recorded product, authority, native-platform, or wire decisions are
accepted and the plan is refreshed/reviewed.

#### Shared foundations

| Plan | Status | Owns / boundary |
|---|---|---|
| [227-received-media-native-egress-foundation-tdd-plan.md](227-received-media-native-egress-foundation-tdd-plan.md) | Accepted | Single and bounded-list Save to Photos/Files plus external OS Share; native Android/iOS proof; no messaging transport. |
| [228-shared-media-library-bookmark-persistence-tdd-plan.md](228-shared-media-library-bookmark-persistence-tdd-plan.md) | Closed / device-proven (`fc7dfa842`) | Owner-lane-safe library/retry/delete seams, replay- and key-safe local state, exact production registries/current encrypted fixtures, and DB `v96` physical Android/iOS SQLCipher upgrade/downgrade-floor proof. |
| [229-cross-track-media-download-storage-controls-tdd-plan.md](229-cross-track-media-download-storage-controls-tdd-plan.md) | Implemented host-green 2026-07-10 (see plan Execution Progress) | Auto-download policy, local availability, eviction, clear-local-copy, and storage controls across lanes. |
| [230-shared-typed-media-viewer-tdd-plan.md](230-shared-typed-media-viewer-tdd-plan.md) | Implemented / host-green | Typed current-item/action callback seam, safe Info metadata, and video seek/speed/mute/resume/lifecycle controls; callback-only and transport-free. |
| [243-native-media-picture-in-picture-tdd-plan.md](243-native-media-picture-in-picture-tdd-plan.md) | Accepted/closed; physical Android and user-directed segmented Wave-3/final host coverage frozen | Path 3 native video-only PiP: Android API 26+/feature-present only, with typed revocation/paging/one-owner settlement and six coherent Pixel-6/API-36 scenarios; iOS/other platforms hidden and fail-closed, with iOS device proof N/A. |

#### 1:1 chats

| Plan | Status | Owns / boundary |
|---|---|---|
| [231-1to1-received-media-core-actions-tdd-plan.md](231-1to1-received-media-core-actions-tdd-plan.md) | Implemented / host-green | Bubble/viewer parity for Save, external Share, whole-message Delete for Me, Info, and Reply; local-only. |
| [232-1to1-received-media-forwarding-tdd-plan.md](232-1to1-received-media-forwarding-tdd-plan.md) | Accepted | Single-source internal Forward, fresh per-target encryption, direct forwarded marker, and DB `v97`; no Go/libp2p change. |
| [233-1to1-shared-media-library-batch-tdd-plan.md](233-1to1-shared-media-library-batch-tdd-plan.md) | Accepted (`a28525db7`) | Conversation Shared Media, filters, Go to Message, and safe local batch actions; multi-source Forward excluded. |
| [234-1to1-private-media-lifecycle-tdd-plan.md](234-1to1-private-media-lifecycle-tdd-plan.md) | Accepted / closed | Device/install-local direct Protected, View Once, and Disappearing lifecycle with encrypted-inner policy, generic previews, fail-closed egress/library/viewer/PiP eligibility, guarded manual download, truthful Android/iOS protection, and landed DB `v100`; no relay/account-wide consume or actual PiP claim. |
| [244-1to1-received-media-reporting-tdd-plan.md](244-1to1-received-media-reporting-tdd-plan.md) | Accepted / intentionally not applicable | Direct received-media Report is an explicit product non-goal, not a provisioning gap. Existing Save, Share, Reply, Info, Delete for me, QR/contact admission, and direct/contact Block/Unblock remain independent; no reporting backend, gateway, queue, receipt, UI, or device/relay boundary exists. |
| [249-1to1-shared-media-batch-forwarding-tdd-plan.md](249-1to1-shared-media-batch-forwarding-tdd-plan.md) | Accepted / revalidated | Direct Shared Media forwards `1..10` exact eligible visual sources to active direct contacts as canonical source-major ordinary messages with independent captions/tokens, atomic current-row preflight, truthful cell outcomes, and failed-cell-only in-route retry; no schema, wire, group/announcement, native/device, or relay change. |

#### Discussion groups

| Plan | Status | Owns / boundary |
|---|---|---|
| [235-group-received-media-core-actions-tdd-plan.md](235-group-received-media-core-actions-tdd-plan.md) | Implemented / device-proven (UI and DB-v98 persistence slices complete) | Discussion bubble/viewer Save, external Share, tombstone-safe Delete for Me, Info, and Reply; announcement sentinel included. |
| [236-group-received-media-forwarding-tdd-plan.md](236-group-received-media-forwarding-tdd-plan.md) | **IMPLEMENTED 2026-07-10 (host-green + device-proven: GMF-06D SQLCipher v99, GMF-11 real Go bridge)** | Single-source internal Forward, fresh destination encryption, group forwarded marker, and DB `v99`; only a narrow optional-bool Go bridge mapping, with no node/topic/auth/fanout/retry change. |
| [237-group-shared-media-library-batch-tdd-plan.md](237-group-shared-media-library-batch-tdd-plan.md) | Implemented / accepted | Discussion Shared Media, paging/filtering, Go to Message, and bounded local batch actions; multi-source Forward excluded. |
| [238-group-private-media-lifecycle-tdd-plan.md](238-group-private-media-lifecycle-tdd-plan.md) | Accepted / closed | Direct canonical execution closed group private-media policy/DB `v101`, durable lifecycle, guarded download/cleanup/replay, capability/notification/native privacy, and available-target SQLCipher/bridge/platform proof while preserving Plan-234 DB `v100`. |
| [245-group-received-media-reporting-tdd-plan.md](245-group-received-media-reporting-tdd-plan.md) | Accepted / intentionally not applicable | Discussion-group Report is an explicit product non-goal. Existing media actions, invite Accept/Decline, notification-only Mute, and Leave remain independent; contact Block is not existing-member group-message moderation, and no reporting implementation or external provisioning gate exists. |
| [250-group-shared-media-batch-forwarding-tdd-plan.md](250-group-shared-media-batch-forwarding-tdd-plan.md) | Accepted / closed | Discussion Shared Media forwards `2..10` exact eligible sources as ordinary per-item × target messages with independent captions, current source/target authorization, opaque retry-stable provenance, truthful cell outcomes, and failed-cell-only in-route retry; no album, schema, wire, native/device, or durable-job change. |

#### Announcements

| Plan | Status | Owns / boundary |
|---|---|---|
| [239-announcement-received-media-core-actions-tdd-plan.md](239-announcement-received-media-core-actions-tdd-plan.md) | Implemented / host-green | Reader Save, external Share, tombstone-safe Delete for Me, and Info while compose/quote publishing stays absent. |
| [240-announcement-received-media-forwarding-tdd-plan.md](240-announcement-received-media-forwarding-tdd-plan.md) | Accepted | Single-source Forward adapter reusing plans `232`/`236`, with target revalidation and no source-announcement publish. |
| [241-announcement-shared-media-library-batch-tdd-plan.md](241-announcement-shared-media-library-batch-tdd-plan.md) | Accepted (`21b6b54e2`, `61cda3415`, `e3e5bfa7d`) | Announcement Shared Media and safe local batch actions while preserving reader/admin authorization; multi-source Forward excluded. |
| [242-announcement-private-media-lifecycle-tdd-plan.md](242-announcement-private-media-lifecycle-tdd-plan.md) | Accepted / closed | Announcement adapter reuses Plan 238 DB `v101` and its device-local lifecycle with current local-and-roster-admin send authority, current-roster-admin receive authority, generic notification privacy, retained terminal/integrity metadata without byte recovery or egress, and available-target SQLCipher, real-Go, protected-route, and iOS-NSE proof; no migration, new transport, convergence, relay-revocation, or three-party claim. |
| [246-announcement-received-media-reporting-tdd-plan.md](246-announcement-received-media-reporting-tdd-plan.md) | Accepted / intentionally not applicable (Track-3/Wave-3) | Announcement Report is an explicit product non-goal. Exact Save/Share/Delete for me/Info actions and invite Accept/Decline, notification-only Mute, and Leave remain independent; QR/contact Block is not existing-member announcement moderation, and no reporting implementation or external provisioning gate exists. |
| [247-announcement-media-private-reply-routing-tdd-plan.md](247-announcement-media-private-reply-routing-tdd-plan.md) | Accepted / closed | Explicit **Message sender** uses fresh fail-closed, callback-only navigation to an existing active sender contact's blank 1:1 composer; it copies no context, changes no announcement write authority, and sends nothing automatically. |
| [251-announcement-shared-media-batch-forwarding-tdd-plan.md](251-announcement-shared-media-batch-forwarding-tdd-plan.md) | Accepted / closed | Announcement Shared Media reuses the shared `2..10` ordinary-output batch route with exact current admin/roster/key authorization, non-source announcement targeting, opaque retry-stable provenance, and source-byte/state preservation; no source publish, schema, wire, native/device, or durable-job change. |

#### Wave 1 direct execution rollup (2026-07-12)

- Plans 234, 238, 249, and 247 are accepted/closed. Plan 238 owns landed DB
  `v101`; the other three retain their recorded schema/transport boundaries.
- The shared Plans 244-246 product decision is accepted: Report is intentionally
  absent, not waiting for Trust & Safety provisioning. Plans 244/245 belong to
  Track-2; Plan 246 records the same disposition for Track-3/Wave-3. Existing
  local actions and relationship/membership controls are not reporting
  substitutes.
- `./scripts/run_host_test_gates.sh host-all --continue-on-failure` executed all
  `1,130` registered commands: `1,125` passed initially, including all eight Go
  legs. Five failing Flutter files exposed three Wave-owned preservation-fixture
  gaps, one new fixture-matrix shape, and one transient concurrent native-asset
  build race. The exact five-file post-fix rerun passed `53/53`; repository
  cadence required only this affected-evidence rerun.
- Required available-target proof is complete on physical Android
  `21071FDF600CSC` and iOS simulator
  `DBE8C32E-9F19-4593-860A-B41113791D79`; no unavailable hardware/OS leg is a
  blocker. The final affected-files query and incremental architecture refresh
  also passed (`50,854` nodes, `78,421` edges, `12,714` named tests at refresh).

#### Track-2 / Wave-2 final closure (2026-07-13)

- [Track-2/Wave-2 final closure](track-2-wave-2-final-closure.md) records the
  authoritative rollup: Plans 238, 242, 249, 250, and 251 are accepted/closed;
  Plans 244 and 245 are accepted/closed as intentionally not applicable. The
  shared decision also resolves Plan 246 in its owning Track-3/Wave-3 record;
  Plan 246 is not counted in the Track-2 inventory.
- Reporting preservation passed `19/19` focused Flutter invocations (`21/21`
  cases), `2/2` named Go cases, both zero-match production-absence scans, and
  global diff hygiene. No reporting production or test fake was added.
- The single wave-level `host-all` passed `1151/1151` commands across `1143`
  Flutter files (`11502` tests passed, `1` skipped) and `8/8` Go legs. Former
  failures `#407` and `#616` passed in full-suite context. Retained log:
  `/tmp/track2_wave2_host_all_rerun2.log`.
- The final incremental Graphify refresh ran once and passed: `8` changed code,
  `2587` unchanged, `0` deleted; graph `51767` nodes / `79663` edges; TDD overlay
  `1331` files / `12862` named tests / `979` production targets.

#### Track-3 / Wave-3 final closure (2026-07-14)

- [Track-3/Wave-3 final handover](track-3-wave-3-handover.md) is the current
  rollup, frozen at SHA-256
  `46731d5c3464a269496994cff8ee50f335d28e7ad43a0c300c83a6a4d7f0c504`.
  Plan 243 implementation, focused/static/route/native/preservation
  tests, curated `1to1`/`groups`, analyzer reconciliation, diff hygiene, one
  incremental Graphify refresh, and available-device proof are closed. Plan 246
  is accepted/intentionally not applicable.
- Path 3 advertises PiP only on eligible Android API 26+ devices with the PiP
  feature. iOS and other platforms remain hidden/unsupported with no native
  owner/channel/handoff. Earlier physical-iPhone evidence is historical; iOS
  device proof is N/A and not a closure gate.
- The coherent physical root
  `build/received-video-pip-proof/final-coherent-20260714T072110Z` passes return,
  close, process recreation, completion, engine-detach shared-seam, and
  distinct-UID audio-focus interruption on physical Pixel 6/API 36. It uses
  production sources under disposable debug ID `com.mknoon.app.pipproof`; it is
  not a production-ID or release-signed APK.
- The engine-detach leg records `realCleanupCallback=false` and proves the
  shared cleanup seam; source/static/JVM/build-boundary tests prove the real
  callback wiring. The physical protected negative is composite; focused host
  tests isolate each protection state.

| Aggregate gate | Result |
|---|---|
| Wave-3 `host-all` | ACCEPTED under user-directed segmented-resume semantics: preserved items 1–416 plus exact resume 417–1,173; suffix passed 8,291 Flutter tests across 749 paths and 8/8 Go legs. Not a single uninterrupted invocation. |
| Final rollout `host-all` | ACCEPTED under user-directed segmented-resume semantics: preserved items 1–1,114 plus exact concurrency-1 resume 1,115–1,173; suffix passed 1,063 Flutter tests across 51 paths and 8/8 Go legs; full indexed coverage is 1,165 Flutter paths plus 8 Go legs. Not a single uninterrupted invocation. |

All unsuccessful histories remain non-acceptance: the 1,163-Flutter-path
completeness-wrapper run was interrupted with exit 130 before Go; the
1,164-path tone-tracker run reached `+3886`, `~1`, `-1` and was interrupted
with exit 141 before Go; the later 1,172-item run failed first at planned item
417 (`link_incoming_lan_media_test.dart`) and was interrupted; pass-3 and the
older SIGTERM/143 run are diagnostic only. The initial final-release
concurrency-4 run failed at item 1,115 when a child Dart process hit a host
native-assets rename race; it is not a pass.

Current isolation removes delayed stream scheduling from the notification
integration cases and gives `FakeMediaFileManager` process-and-isolate-specific
roots, backed by four new isolation tests. That new test path accounts for the
current 1,165 Flutter paths. Re-running the exact final suffix at concurrency 1
is an operational native-assets qualification, not a product behavior change.

Track-3/Wave-3 is complete under the user's explicit segmented-resume
authority. Neither composite may be restated as a fresh unbroken pass.

## 3B. Introduction notification follow-up (2026-07-10)

| Plan | Status | Owns / boundary |
|---|---|---|
| [252-intro-accept-notification-copy-chat-routing-tdd-plan.md](252-intro-accept-notification-copy-chat-routing-tdd-plan.md) | Implemented host+relay-green 2026-07-10; **narrow corrective amendment, D1/D2 coherent-source device acceptance, and Wave 4A integrated acceptance green under `W4A-BLOCKERS-AUTH-01` (2026-07-28)**. The retained live RED reached a cold tap and exposed stale B-only state before exact C convergence. The correction's resolver/open-flow/wiring suite passed 13/13, and the final physical-plus-two-emulator run passed both roles. Final intro report SHA `e75c34a00b01c967b4d0ef825ec6a71437a3696b7972e6613080da410c5b1cf6`; proof SHA `1cd87227a523523c9a490787b0a8ab0cad98fb1e942b9bccd2904113ebafcc5b`. [Stable Wave 4A evidence](evidence/dtr-wave4a/README.md). | Action-aware, role-neutral introduction-accept notification copy plus introducer-only notification-tap routing to the originating A-B chat. The corrective amendment arms exact-anchor status observation before reread, matches only the exact introduction/responder acceptance, preserves immediate A-to-B open and fail-open timeout/error behavior, and owns cancellation on every exit path. It remains product ownership under Plan 252, not Plan 291 harness scope. |

## 3C. External share media UX follow-up (2026-07-10)

| Plan | Status | Owns / boundary |
|---|---|---|
| [253-external-share-immediate-media-progress-tdd-plan.md](253-external-share-immediate-media-progress-tdd-plan.md) | Accepted (`c9b1ce6cb`, blocker/test isolation `f029d131f`): causal/focused tests, 1,672-test `1to1`, analyzer parity, physical image+video DP-01, independent QA, and all 731 `feature-host-all` commands are green. | External phone-share media publishes a renderable attachment on the first outgoing event, reuses the in-app byte-progress banner with upload/sending phases, and removes composer-covering share SnackBars in favor of inline actionable feedback; no native intake, DB, transport, or crypto change. |
| [254-group-media-share-liveness-first-frame-tdd-plan.md](254-group-media-share-liveness-first-frame-tdd-plan.md) | Accepted (`f243b2788`, acceptance hardening `de0aabfd5`): nine causal tests green, all 11 mutation cycles causally re-red, `groups` passed 1,926 Flutter tests plus Go, `feature-host-all` passed 731/731, analyzer improved 1,628→1,627, and independent QA accepted. | A group-targeted media share surfaces live with its thumbnail in an already-open group conversation, renders media on the first frame of a fresh mount mid-send, and a terminally-failed media send keeps its attachments and stays retryable; in-app optimistic display, status-in-place updates, queuedOffline UX, and all validation/reconciliation contracts unchanged. |

## 3D. Voice-message first-frame trust follow-up (2026-07-11)

| Plan | Status | Owns / boundary |
|---|---|---|
| [255-voice-duration-visible-before-audio-load-tdd-plan.md](255-voice-duration-visible-before-audio-load-tdd-plan.md) | Execution-ready | Available local voice/audio bubbles show a positive known duration on the first frame without claiming playback readiness; player-duration authority, pending/not-local placeholders, terminal media states, persistence, transport, and native audio remain unchanged. |

## 3E. 1:1 reaction notification follow-up (2026-07-11)

| Plan | Status | Owns / boundary |
|---|---|---|
| [256-1to1-reaction-notification-context-tdd-plan.md](256-1to1-reaction-notification-context-tdd-plan.md) | Evidence-gated | Preserve the landed live/local sender + emoji copy while adding a privacy-safe typed reaction push, actor/event context, route/thread/staging/dedupe parity, and automated Android plus physical-iOS notification proof; keep the green avatar orbit strictly message-unread (foreground message lights it, dismissal does not clear it, successful notification-open/read does), and prove reactions never inflate it; first attribute the reported generic card because current HEAD would suppress the remote reaction push rather than label it `New Message`. |

## 3F. Group and announcement reaction notification follow-up (2026-07-11)

| Plan | Status | Owns / boundary |
|---|---|---|
| [257-group-announcement-reaction-notification-context-tdd-plan.md](257-group-announcement-reaction-notification-context-tdd-plan.md) | Execution-ready | Preserve the landed live group reaction copy while adding one shared discussion/announcement typed reaction pipeline: author-only ADD notification, full-member reaction convergence, silent REMOVE/bystander paths, encrypted group/actor/emoji context, target-route/event-dedupe parity, message-only unread semantics, group read-event Orbit clearing, and automated Android plus physical-iOS proof. |

#### Wave 0 closure audit (2026-07-11)

- **Engineering closeout complete:** Plans 241, 253, and 254 are accepted; Plan 239 remains implemented with no additional work; Plan 248 is implemented and host-complete but is not visually accepted.
- **Aggregate host proof complete:** the current 1,078-item `host-all` inventory is green. Items 1–1,046 passed in the initial run; a stale relay-down call-count oracle was aligned with the already-locked concurrent-inbox contract and passed 5/5; items 1,048–1,070 passed on resume; the Go tail passed after commit `0f6ac03d5` pinned the two remaining unpinned commands to the module's declared Go 1.25.0 instead of the host's incompatible Go 1.26.4.
- **Ordinary received-media capability:** eligible, integrity-verified local image/video supports download/retry/local-copy state, Photos/Gallery and Files export, external OS Share, and Shared Media batch Save up to 10 across direct, discussion, and announcement lanes. Current-row/state/tombstone/path/owner checks fail closed.
- **Later progress / current boundary (updated 2026-07-14):** Plans 234 and 238
  are accepted and closed for device/install-local private media at DB `v100`
  and `v101`, respectively. Plan 242 is also accepted and closed as a
  migration-free announcement adapter over Plan 238's lifecycle. Plan 243 is
  accepted and physically proven for Android-only Path 3; its two segmented
  aggregate host-coverage records are frozen under explicit user resume
  semantics. No iOS/cross-platform, cross-lane,
  relay-authoritative, or account-wide-consume guarantee is implied. The
  current per-plan rows above are authoritative for all other later work.

## 3G. Private-media & feedback copy revision (2026-07-18)

| Plan | Status | Owns / boundary |
|---|---|---|
| [259-private-media-feedback-copy-tdd-plan.md](259-private-media-feedback-copy-tdd-plan.md) | Execution-ready (v2 — 8-agent /tdd-review `wf_d3da2753-fc2` applied 2026-07-18: TC-10 retargeted to accept-notFound, chip-aware assert arithmetic, pins-to-update table P1–P5, compile-RED quarantined to a new placeholder-body test file with required `contactDisplayName`, analyze/l10n-commit gates added; D1 86/D2 61/D3 85/D4 88/D5 64). **Production edits E1-E7 APPLIED in the uncommitted working tree 2026-07-18 evening; Execution Progress/QA not yet recorded — run its stale-literal sweep before building on it.** | Implements the REVISED copy from `private-media-feedback-ux-mockup-codex.html` where it is truthful against HEAD: chooser consequence-led labels/details ("Keep in chat"/"Protected view"), receiver/sender private-bubble reassurance bodies (`{name}`-attributed, no screenshot claim), mic dialog copy, settings inline-only "Couldn't save. Try again.", removed-from-group banner cause, Orbit invite-outcome l10n unification, upload-banner percent. Host-only closure; 5 new ARB keys + 9 value edits across en/ar/de. Defers (named follow-ups): offline media outbox presentation/behavior (mockup §03), sender reopen, view-once open-failure UI, picker sheet redesign, invite row-state port + ask-for-new-invite. Grounding `wf_2242d205-89e` refuted 6 mockup strings as lies-against-HEAD (screenshot claim, sender reopen, connection-cause, one-view-still-available, "Link copied", dead mic snackbar) — recorded as do-not-re-introduce. |
| [260-private-media-proposed-ux-adoption-tdd-plan.md](260-private-media-proposed-ux-adoption-tdd-plan.md) | Execution-ready (**v4** — second /tdd-review `wf_43c68f97-753` 10 agents applied 2026-07-19, verdict **yes-with-tightening → all tightenings applied in-place**: over-engineering verdict NOT broadly over-built (~24/30 items verified reuse/justified), 5 concentrated over-builds trimmed (shared runUploadMedia wrapper, claim-scoped trigger source, banner state reuse, TC-38 device-unique trim, E14 fallback retired); user-locked: offline issuance gate + connectivity failures never burn the retry ceiling, TC-37 causality-keyed PASS w/ informational latency; MEDIA_ERROR permanence split (bridge.go:1470 wraps relay 5GB rejection into the "retryable" code); consumer topology corrected (TWO group-composer sites, group manual retry = new wiring, 3 callP2PMediaUpload bypass lanes named); S3a→4 + S4→3 micro-checkpoints; refuted: the devices.assignments gate-break claim (report.dart:201-202 emits it). v2 — grounding `wf_ef4bf167-17d` 103 agents; /tdd-review audit `wf_dd620bde-9d7` 8 agents applied 2026-07-18: S3 queued lane REDESIGNED around the three sending→failed sweepers (pause/no-age-gate + 30s stuck recovery + retrier pass) via upload_pending sweep exclusions — no new status vocabulary, rollback-safe; voice lane added; E9 classifier pinned + :3383 catch routed; TC-37 two-phased (≤3s fast-flush bound + >30s pause/resume never-red); E20 sender-CAS mode predicate IN(protected,view_once) + mirrored sender recovery leg + post-consume cleanup stated; orbit :1607/:1846-1863 migrated; 4 AD guards; step-ledger fixes) | BUG + FEATURE IMPROVEMENT (user request 2026-07-18: screenshot shows protected/view-once messages as EMPTY bubbles on sender AND receiver; adopt the ENTIRE proposed side of the mockup; every introduced button must work; regression for the blank-bubble). Baseline = tree WITH 259 applied. Six slices: **S1** unified in-bubble private card via a LetterCard `privateContentSlot` (root cause: `conversation_screen.dart:759-761` hollows the card, `:942-947` appends the placeholder as a detached Column sibling — the empty shell is `letter_card.dart:738-747`+`:610-639`); THE regression = descendant assertion + device-journey checkpoint (INV-260-1: never a blank bubble; icon-visual by design, NO pixel thumbnail). **S2** picker bottom sheet + summary chip (model already matches: 4 modes + {3600,86400,604800}; commit-on-CTA; ValueKeys preserved — refuted "sheet breaks all composer tests"). **S3** offline media outbox: typed UploadMediaFn outcome, queued lane joins the keepRetriable template (upload_pending never red — INV-260-6), media leg in the 1s reconnect flush, manual-Retry relative-path fix, promote-before-cleanup fix, honest "Photo is no longer on this phone", offline banner, promise-string l10n unification (INV-260-2 one byte-identical promise). **S4** viewer: single-tap open w/ card-owned progress, typed open-failure + Try again, view-once PRE-FRAME rollback (makes "Your one view is still available" true; post-frame consume untouched; CAS-locked INV-260-3), sender one-more-look via new is_incoming=0 CAS lane (NO migration — columns exist; protected+viewOnce only). **S5** feedback: orbit invite outcomes on the row, "Ask for a new invite" deep-link (hidden when unreachable — no dead buttons INV-260-5), shared undo bar (visible==commit window), quiet pill restyle, mic sheet. **S6** en/ar/de + RTL. 38 TCs → 35 matrix rows, zero empty cells; PROD-CRITICAL device-proof `connectivity_restore_media_outbox` (2 real devices, real bridge — no automated real-bridge media proof exists on HEAD). 10 Accepted Differences (no Choose-photo v1, no expiry sender-reopen, screenshot-free copy w/ platform capture notes, 1:1 name-free meta, etc.). |

## 3H. Orbit sole-admin group exit guidance (2026-07-19)

| Plan | Status | Owns / boundary |
|---|---|---|
| [261-orbit-sole-admin-group-exit-tdd-plan.md](261-orbit-sole-admin-group-exit-tdd-plan.md) | Execution-ready · 2nd `$tdd-review` 2026-07-19: **ready-with-tightening** (all 3 domain bets verified sound; tighten before execute: TC-04 archived-exclusivity assertion, GROUP_TESTS grep gate, first-swipe sole-admin wired fixture + early dissolve-only sheet slice, declare offline-leave block, reword Done :223/:224, cut step-6 helper option) | Feature improvement over a confirmed Orbit bug: active group rows say Leave; sole admins receive joined-successor or dissolve recovery instead of a silently swallowed error; ordinary/multi-admin leave uses Group Info's signed voluntary-removal/key-rotation lifecycle; dissolve retains read-only history; later device-only delete is strict and cannot become active leave. Joined eligibility reuses durable invite/timeline evidence, Group Info gets parity, en/ar/de/a11y are host-tested, and friend/introduction Delete plus archived Unarchive remain unchanged. Host-only; no DB, wire, native, relay, or device-proof change. |

## 3I. Sender protected-media one-more-look independence (2026-07-19)

| Plan | Status | Owns / boundary |
|---|---|---|
| [262-sender-protected-open-pending-upload-tdd-plan.md](262-sender-protected-open-pending-upload-tdd-plan.md) | Awaiting-review (grounding + verify→refute workflow `wf_319c8f2a-c19`: 2 ground + 6 verify + refute; C2/C6 survived, **C1 field-conclusion REFUTED, C3/C4/C5 corrected**) | BUG + MODIFICATION (user-selected product decision from the 2026-07-19 debug session): sender "Open private media" → "Couldn't open this photo" while the recipient opens fine. TWO legs: **Leg A** — real `uploadMedia` unlinks the pending source (stale comment at conversation_wired:3283-3286 says otherwise), so `_buildLocalSuccessAttachmentFromPlan`'s missing-source fallback (:688-695) persists a dangling ABSOLUTE `pending_uploads/` path with 'done' → finalized sender rows fail the viewer authority FOREVER while the UI's canonical-file display fallback keeps the button enabled (the field shape; invisible to the suite because every wired test stubs uploadMediaFn without the unlink). **Leg B** — the product decision: outgoing one-more-look accepts the pending-upload durable copy for `upload_pending` rows across the FULL gate chain (adapter mapping + `_verifiedOpenableLocalPath` pending-convention arm, shared SQL lease predicate :1105 + inline quarantine :1245 outgoing arm only, `_requalifyRetry`, UI `localMediaAvailable` + ownerLane stamping + `localMediaMissing` pass-through); engine gates are DERIVATIVE — not fix sites. **Leg C** — the fix OPENS a latent mid-lease finalize race (retrier/foreground rewrite pending→canonical+done breaks lease + CAS, strands 'opening', deletes the rendered file): closed per review-1 redesign via POSITIVE-authority guarded save CAS (apply only when outgoing parent visible+terminal-null+state exactly 'available' — refuses mid-lease AND post-terminal reinsertion) routed from ALL reachable writers (retrier, wired finalize, _persistOutgoingMedia incl. bulk delete, failure projection, cancel, manual retry), convergence via ONE idempotent settlement/qualification-time repair CAS (pending-or-dangling row + canonical file ⇒ canonical+done; NO durable marker, NO writer retry loops — also repairs LEGACY deployed dangling-'done' rows) + upload lanes claiming directPrivateMediaTransferRegistry so terminal cleanup defers while plaintext is being read (consume-mid-upload sabotage; also live on HEAD via hide-while-retrier-uploads). 19 TCs → 19 matrix rows, zero empty cells; NO migration (predicate-only SQL); PROD-CRITICAL device closure = extended `direct_private_media_device_local_journey` p262 sender-pending criterion made CAUSAL (production-schema rows, real typed launcher tap-through, derived criterion — review verified the current harness open-callback is a no-op); array additions per-file grep-gated (unenforced-registration landmine). Incoming lanes + egress denial locked unchanged. Review-1 dispositions recorded in-plan (rejected: "/claude-host-bin/flutter missing" — false here; tempered: no unified mutation-authority abstraction, no durable deferred-mutation marker). |

## 3J. Removed-member shell exit recovery (2026-07-20)

| Plan | Status | Owns / boundary |
|---|---|---|
| [263-removed-member-shell-exit-dead-end-tdd-plan.md](263-removed-member-shell-exit-dead-end-tdd-plan.md) | Implemented and accepted; immutable handoff `refs/plan-handoffs/263` | BUG: B3 retains a removed member's read-only group, but every exit path attempts voluntary leave after self removal. Adds protected DB-v102 `self_removed_at` authority with null-only legacy backfill (pending invite/history/evidence preserved); locks native leave and one atomic marker+watermark+exact-self/work-reset commit together; makes old text/repush/upload transport non-rearmable by reusing `send_failed`/`upload_failed` with retry evidence scrubbed; then performs strict resumable authorization-projection → primary → generation/mute-mirror → key-reference cleanup through membership → per-group coordinator → projection-queue ordering. Marked shells remain UI-visible, while each bounded native/network leaf holds the same non-nested membership phase across final authority read + one action so it orders wholly before B3 or skips after it. Same-id acceptance checks the signed non-regressing watermark + newer `issuedAt` against the live marker or retained freshness floor; post-removal acceptance records compact floor + shape/floor/authorization-bound origin facts in `group_event_log`, atomically appends a phase-unique binding with a separate authorization id and accepted-state fingerprint per accepted group+self+key commit while leaving the canonical watermark event id null, and can exact-rollback marked or physically absent prior shape across repeated/same-invite cycles. The unused direct `joinGroup` remains fresh-only/native-first and rejects existing/floor/unknown-capability state before bridge. Local cleanup has an explicit delete/preserve matrix, capped media-journal reuse, and removed-specific confirmation plus complete typed-result handling on all four surfaces. 16 causal rows; host behavior plus real-SQLCipher proof on an available Android target (`N/A` only when none exists). No new column/status/table/receipt/pending-phase/cleanup journal/wire field/UI abstraction: the post-native/precommit gap remains the documented pre-existing degradation. No universal cascade, batch lock, global UI filtering, wire, relay, Go, native-handler, or two-peer change; Plans 264–267 retain ownership. |

## 3K. Pending role-broadcast exit recovery (2026-07-20)

| Plan | Status | Owns / boundary |
|---|---|---|
| [264-pending-role-broadcast-convergence-tdd-plan.md](264-pending-role-broadcast-convergence-tdd-plan.md) | Implemented and accepted on DB v103 at corrected immutable tip `a03300cdbc4d`; initial freeze `f0a5d2777dfd` is its direct parent | BUG + reliability + UX: one conditional automatic Retry, then explicit **Leave when sync completes** backed by the DB-v103 `group_exit_intents` state machine. Exact `selfPeerId + joinedAt` and revision CAS make queue/cancel/restart/native/cleanup membership-safe; preparation-time and transactional last-admin races map to one recoverable status, while the signed post-notice departure is irreversible. The notice is minted only after the converged role watermark, rotation is at-most-once, repeated native leave is idempotent, and cleanup cannot delete a same-id re-entry. Group rows show **Leaving…**; conversations stay readable/read-only; Group Info, Orbit primary/stuck, and Group List stuck share one fail-closed coordinator. Closure includes 19 contract rows, real SQLCipher on USB Android `21071FDF600CSC`, exact Go native-leave proof, focused 145, groups 2,759, core 2,723/343, feature 8,441/815 with one expected skip, clean analyzer, and refreshed Graphify fingerprint `3621afca05d0b1d3`. Dissolve commit-unknown/CAS cleanup stays deferred and Plan 265 remains a separately reviewed residual; no per-plan full `host-all`. |

## 3L. Voluntary-leave prework degradation hardening (2026-07-21)

| Plan | Status | Owns / boundary |
|---|---|---|
| [265-voluntary-leave-prework-degradation-tdd-plan.md](265-voluntary-leave-prework-degradation-tdd-plan.md) | Evidence-gated residual after accepted Plan 264; not implemented; re-review required before a first causal RED | BUG reliability hardening: Plan 264 supplies durable leave phases but does not cover this contract completely. Current per-recipient salvage conflicts with TC-265-09's aggregate-only semantics, and a later rotation outcome can overwrite notice degradation for TC-265-11; the remaining fault matrix still needs exact mapping. No PB265 file/registration, schema, wire/native/relay, device, or per-plan full `host-all` claim is authorized before reconciliation and re-review. |

## 3M. Group-exit release diagnosability (2026-07-21)

| Plan | Status | Owns / boundary |
|---|---|---|
| [266-group-exit-release-diagnosability-tdd-plan.md](266-group-exit-release-diagnosability-tdd-plan.md) | Evidence-gated v3; `$tdd-plan` + `$tdd-review` complete; three-reviewer final verdict `READY` only after immutable Plan 264 handoff and final Plan 265 disposition, not concurrent execution | BUG + release diagnosability: exact typed/allowlisted bridge outcomes map to stable `EX01`-`EX10`/`EX99` codes without free-text parsing. A disjoint identity/snapshot/action adapter captures pre-intent failures; one diagnosing processor covers post-intent foreground/background/runtime/rejoin/resume outside 264's keyed authority; terminal decorators and exit-flow scrubbing close UI/unmount/privacy bypasses. A vNext encrypted 20-row history uses atomic multi-fact batches and opaque intent scoping, survives cleanup, transfers only with encrypted Move Account, and is readable through fully wired release Settings states. Fifteen contract rows cover full migration/account compatibility, privacy, l10n/a11y, and an availability-bounded Android release SQLCipher reopen/remount proof; no Go/native/wire/relay, extra retry, verbose-log toggle, raw cause persistence, or per-plan full `host-all` claim. |

## 3N. Group invitation send latency (2026-07-21)

| Plan | Status | Owns / boundary |
|---|---|---|
| [267-group-invite-send-lag-tdd-plan.md](267-group-invite-send-lag-tdd-plan.md) | Evidence-gated v3; `$tdd-plan` + `$tdd-review` complete; verdict `NOT-READY` for production, Wave 0 harness/instrumentation only may proceed | BUG: create/add invite UI waits on the full already-parallel batch, while each recipient uses an unbounded live-first call before inbox fallback. Wave 0 now requires strict scenario parsing, correlated five-sample create/add × warm/cold/offline artifacts, host-mediated USB-Android/emulator rendezvous, and exact reliability registration. Production remains blocked on a late native inbox-custody outcome that cannot safely authorize new-ID resend, an absolute caller threshold, authenticated canonical duplicate handling, delayed-overlap proof, and receiver-first mixed-version activation. Ten contract rows; no production, schema, wire, Go/global-timeout, UI fire-and-forget, or per-plan full `host-all` authorization. |

## 3O. Group media Keep-in-chat-only composer (2026-07-21)

| Plan | Status | Owns / boundary |
|---|---|---|
| [268-group-media-keep-in-chat-only-tdd-plan.md](268-group-media-keep-in-chat-only-tdd-plan.md) | **implemented / host-gate accepted 2026-07-21** — §A–§E applied; 215/215 wired tests, 2,818/2,818 curated Flutter tests plus Go/relay legs, analyzer/scope/whitespace checks, and incremental Graphify refresh green | Fresh discussion-group and writable-announcement media authoring is ordinary Keep in chat only: the selector and hidden mutable policy are gone, all fresh sends omit private wire fields, terminal projected failures restore the complete draft and resend through the same ordinary parent, and voice/share/forward destinations stay ordinary. Existing persisted private receive/view/cleanup/retry compatibility, announcement roles, and the full 1:1-only picker remain covered. No DB, wire-vocabulary, Go, relay, native, or device change; document attachments and the separate nonterminal multi-item outbox transaction boundary remain deferred. |

## 3P. Group media end-to-end reliability (2026-07-22)

| Plan | Status | Owns / boundary |
|---|---|---|
| [269-group-media-end-to-end-reliability-tdd-plan.md](269-group-media-end-to-end-reliability-tdd-plan.md) | **`$tdd-review` run 2026-07-22 (11-agent audit) → NOT ready as written; fix-list at [269-review-fixlist.md](269-review-fixlist.md)** (causal spine and all five technical bets verified sound; blockers are proof-integrity: an unmasked full-row completion overwrite of `created_at`/`thumbnail_hash` that all 20 rows would stay green through, two false "already registered" gate facts with no registration enforcement, two omitted `GroupConversationWired` route-open sites, a fifth dedupe exit, and device rows closable without a real boundary). Implementation not yet run | BUG: fixes the reproduced Pixel sender regression where SQL-defaulted retry counters break exact completion and trigger five-minute duplicate uploads, then closes the source-confirmed unstable lease-ID, active transport ACL, empty-ACL direct downgrade, HEIF, bounded receiver authorization, duplicate-enrichment, durable policy-aware download recovery, and iOS background-receive gaps. No DB migration/status, Go authorization relaxation, account-ID relay fallback, private/direct semantic change, unbounded retry, or iPhone 13. Closure is availability-bounded: focused/curated/family/relay/native gates, required fresh-identity Pixel `21071FDF600CSC` + Android `emulator-5554` real-relay JPEG/MP4/voice proof, and a separate iOS suspension proof on the available iPhone 11. Full `host-all` remains wave/final only. |

## 3Q. Direct image/video private selection and received tap-tile redesign (2026-07-24)

| Plan | Status | Owns / boundary |
|---|---|---|
| [270-protected-photo-tap-tile-tdd-plan.md](270-protected-photo-tap-tile-tdd-plan.md) | **implemented and host-green 2026-07-24** — clean implementation commit `0813292de` after planning baseline `9922308fb`; focused/mutation evidence, 2,437-test `1to1`, 3,305-test `groups`, analyzer, diff hygiene, and Graphify refresh passed | MODIFICATION (approved option B): direct received protected/disappearing image/video now uses the no-pixel 150px open tile; protected drops title/button, disappearing retains its expiry title, both use compact en/ar/de body copy, opening lives inside one disabled semantic tile, and new GIF drafts remain ordinary while legacy private GIF stays compatible. View once remained unchanged in Plan 270 and is extended separately by Plan 271. |
| [271-1to1-view-once-tap-tile-tdd-plan.md](271-1to1-view-once-tap-tile-tdd-plan.md) | **execution-ready** — `$tdd-plan` source grounding complete; 13 canonical rows, no unresolved evidence, no migration or new registration | MODIFICATION: extend Plan 270's received tap-tile language to openable direct 1:1 View once image/video only. Keep the visible `View-once photo/video` title and one-view warning, remove the separate button, and use the existing 150px tile as the sole one-step control with unchanged typed-launcher/lifecycle safety. Null-open, legacy GIF, outgoing, failure/terminal, protected/disappearing, groups, copy, DB/native/relay stay unchanged. Host widget proof is causal; the existing Plan-234 device-local harness updates only its incoming selector and conditionally replays on a re-resolved physical-Android + Android-emulator pair (currently `21071FDF600CSC` + `emulator-5554`). |
| [301-protected-photo-thumbnail-bubbles-tdd-plan.md](301-protected-photo-thumbnail-bubbles-tdd-plan.md) | **Implemented / host+device verified (2026-07-29; real-transport wire campaign remains an explicit follow-up)** — `/tdd-plan` verify→refute grounding (9-agent workflow) then `/tdd-review` 4-worker audit `wf_36e5f8c0-c62` (verdict plan-fixes-required, core bet confirmed) with all meaningful deltas applied in place: Android-only route protection (iOS critical-event latch would block protected opens), enter-failure fail-closed, 128 KB transport frame-cap math replacing the refuted "no cap" claim, corrected wipe sites (`_deleteExactAppOwnedArtifacts` + `cleanupDeletedMessageArtifacts`), pure-Dart `package:image` thumbnail generation, gif dual-check exclusion, traversal/replay-after-delete/delete-for-everyone/one-action-tile contract cases, dispatcher `--scenario` instead of a new runner, honest rollback residue. 14-row contract; no migration | MODIFICATION: 1:1 protected photos render as a REAL restricted thumbnail in both sender and receiver bubbles via a bounded sender-generated `thumbnailInlineBase64` riding the E2E attachment JSON (receiver had zero pre-open bytes — "presentation-only" was refuted), persisted write-once to a guarded `.thumb.jpg` sibling, rendered by a new tile that keeps tap→in-app protected open only, egress matrix untouched, budget untouched, and adds conversation-route screenshot protection (Android FLAG_SECURE via existing coordinator; iOS per-pixel capture-protected view promoted from the viewer). Fail-closed to today's tiles when no thumb exists (old senders). View-once (Plan 271), disappearing, protected video, groups, relay/ACL, DB schema stay unchanged. Closure: host rows + required physical-Android/emulator secure-window proof. |

---

## 3R. Move Account cutover-window recovery (2026-07-26)

| Plan | Status | Owns / boundary |
|---|---|---|
| [285-move-account-cutover-window-recovery-tdd-plan.md](285-move-account-cutover-window-recovery-tdd-plan.md) | **Implemented / host-tier closure (2026-07-26).** The approved A-0/A-1/B/C/D scope and its v5 execution-counterexample closures are implemented; `DECISION-285-01` still defers the user-asserted abort as `DEFER-285-03`, with durable process-restart resume and the pre-existing erase/shared-scope residue gaps retained as `DEFER-285-01/02`. Final evidence: 173/173 focused tests, all 50 `move-feature` commands, startup wiring 4/4, l10n 4/4, completeness 1,349/1,349, strict analysis with no issues, registration count 1, clean scoped diff, and refreshed Graphify fingerprint `d3c748f7a3cdec3b`. Optional device-confidence rows TC-16/16b are N/A for host closure. Full `host-all` was not claimed; any later aggregate/release run must explicitly include the Plan-285 commit/range. DTR-09, Wave 3, and final-rollout status are unchanged. | Hardened the cutover window with replay-safe proof completion and bounded retry, resumable live-service startup, retained-route ownership/reset safety, authority-aware blocked-screen behavior, and destination-compatible primary-scope staging/promotion/cleanup. Duplicate completes share one settled result; proof identity and post-commit cleanup are fail-safe. One additive backward-compatible `old_block_proof_replay_safe` capability was added to transcript/complete responses; there was no protocol-version bump, persisted-format or DB-schema change, DTR-09 manifest change, or cutover-coordinator change. |

---

## 3S. Sender protected-media unlimited re-open (2026-07-29)

| Plan | Status | Owns / boundary |
|---|---|---|
| [302-sender-protected-reopen-unlimited-tdd-plan.md](302-sender-protected-reopen-unlimited-tdd-plan.md) | **Implemented / host-green (2026-07-29)** — `/tdd-plan` first-hand causal-chain grounding + verify→refute workflow `wf_568a330a-cba` (adversarial refuter, 27-case blast-radius inventory, sibling/copy census; refuter KILLED engine-predicate tightening — incoming-only `failClosedCorrupt` SQL ⇒ permanent zombie rows — ⇒ controller-only fix), then `/tdd-review` 2-worker audit `wf_03fb8780-c06` (verdict plan-fixes-required, core bet CONFIRMED; factual worker re-derived ~40 test cites + SQL census, all accurate) with all deltas applied in place: repaired zero-diff gate (adapter file is 301-dirty ⇒ diff-hash pin), repaired old-key census grep (quoted pattern + `-v l10n` matched nothing), NEW outgoing-disappearing `notEligible` negative (zero such assertions existed repo-wide — over-widened authorization passed everything), TC-07 assert-before-settle choreography + manual-retry lane twin, TC-08 secure-key-absence assertion, `private_media_notification_body` added to the l10n exact map, array-block-scoped registration greps. 10-row contract, zero new test files, no migration | BUG→MODIFICATION (user product decision 2026-07-29, superseding the protected half of 3I's one-more-look contract): the sender of a 1:1 protected photo re-opens it WITHOUT limit — `_requiresOpeningLease` outgoing arm drops `protected` (view-once keeps its budget everywhere), the no-lease authorization admits outgoing protected, and the "reopen once" copy forks to a new anytime key (en/de/ar) on the outgoing placeholder, plan-301 tile semantics, and (dead-copy) picker. Receiver flows, view-once both directions, disappearing, groups, terminal cards, egress matrix, engine recovery, and all CAS SQL stay byte-identical. Accepted+locked: legacy interrupted rows still consume (AD-1), already-latched bubbles stay terminal — bytes were wiped (AD-2), mid-view upload completion now commits immediately under an open viewer (AD-3/TC-07), active-viewer delete of failed protected media proceeds while view-once keeps the refusal (AD-4/TC-08). Host-tier closure; on-phone re-test needs a FRESH send. |

## 3T. 1:1 view-once minimal presentation (2026-07-30)

| Plan | Status | Owns / boundary |
|---|---|---|
| [303-1to1-view-once-minimal-presentation-tdd-plan.md](303-1to1-view-once-minimal-presentation-tdd-plan.md) | **Completed / host+single-Android green (2026-07-30)** — focused 247-test closure, 1to1 gate, affected shared-viewer sentinels, analyzer/hygiene, native protection, and recipient SQLCipher journey passed | MODIFICATION: new 1:1 view-once composition is image-only; sender/receiver image bubbles are minimal tap tiles; image view-once viewer bottom chrome is hidden; actual AppBar Back settles and cleans before route removal/native release. Terminal receipts remained intentionally unchanged during Plan 303 and are owned by Plan 304. |

---

## 3U. 1:1 consumed view-once Photo receipt (2026-07-30)

| Plan | Status | Owns / boundary |
|---|---|---|
| [304-1to1-view-once-consumed-photo-receipt-tdd-plan.md](304-1to1-view-once-consumed-photo-receipt-tdd-plan.md) | **Completed / host-green (2026-07-30)** — causal RED/GREEN and mutation re-red passed; viewer/card suites, Plan-303 composer/Back sentinels, 2,517-test `1to1` plus relay Go gate, analyzer, diff hygiene, and Graphify refresh passed | MODIFICATION: only direct `viewOnce + consumed` terminal bubbles become a compact eye-off + localized `Photo` receipt with no inline Reply/Info/Delete or lifecycle copy. Outer swipe/long-press Reply/Delete remain; expired/unsupported/protected terminals, lifecycle/cleanup/protection, group surfaces, and schema/wire state remain unchanged. Legacy consumed view-once videos share the fixed `Photo` receipt because secure cleanup deliberately removes exact kind. |

---

## 3V. 1:1 consumed view-once receipt content hug (2026-07-30)

| Plan | Status | Owns / boundary |
|---|---|---|
| [305-1to1-view-once-consumed-receipt-content-hug-tdd-plan.md](305-1to1-view-once-consumed-receipt-content-hug-tdd-plan.md) | **Execution-ready** — 4-row host-widget contract; no migration, device/relay proof, localization generation, or registration edit | MODIFICATION: only direct `viewOnce + consumed` eye-off/Photo receipt bubbles hug their summary plus unchanged timestamp/status footer instead of filling the 78% cap. Uses a default-off `LetterCard` intrinsic-size opt-in; expired/unsupported/protected and ordinary empty/media/text bubbles keep their existing size and behavior. |

---

## 3W. 1:1 Keep-in-chat image viewer metadata removal (2026-07-30)

| Plan | Status | Owns / boundary |
|---|---|---|
| [306-1to1-keep-in-chat-image-viewer-metadata-removal-tdd-plan.md](306-1to1-keep-in-chat-image-viewer-metadata-removal-tdd-plan.md) | **Implemented / host-green (2026-07-30)** — causal RED/GREEN plus mutation re-red passed; all three touched suites, exact preservation sentinels, clean 2,520-test `1to1` retry plus relay Go checks, analyzer, diff hygiene, and Graphify refresh passed | MODIFICATION: direct 1:1 ordinary (`Keep in chat`) static images suppress automatic sender/date/MIME/size/dimensions in the full-screen viewer for both incoming and outgoing directions, from either the conversation or direct Shared Media. Captions, actions, explicit received-media Info, videos/GIFs, group/announcement, and private/default routes retain their current behavior through an additive item-level default-visible policy. |

---

## 3X. 1:1 Keep-in-chat received-image viewer action layout (2026-07-31)

| Plan | Status | Owns / boundary |
|---|---|---|
| [307-1to1-keep-in-chat-image-viewer-action-layout-tdd-plan.md](307-1to1-keep-in-chat-image-viewer-action-layout-tdd-plan.md) | **Implemented / host-green (2026-07-31)** — causal RED/GREEN, preservation suites, three mutation re-reds, the 2,523-test `1to1` lane plus relay Go checks, analyzer, diff hygiene, and Graphify refresh passed | MODIFICATION: only incoming ordinary static images opened from a 1:1 conversation select an additive compact action presentation: circular top-end ellipsis lists Save image/Share/Info/Reply, Forward moves bottom-end, and Delete moves bottom-start. Existing per-item authority, current-item dispatch, Save/Share/Info/Reply/Forward effects, and whole-message Delete-for-Me stay unchanged. Outgoing, GIF/video, direct Shared Media, group/announcement, private/default routes, schema/wire/native/transport stay unchanged. |

---

## 3Y. 1:1 Keep-in-chat image viewer overflow action icons (2026-07-31)

| Plan | Status | Owns / boundary |
|---|---|---|
| [308-1to1-keep-in-chat-image-viewer-overflow-action-icons-tdd-plan.md](308-1to1-keep-in-chat-image-viewer-overflow-action-icons-tdd-plan.md) | **Implemented / host-green (2026-07-31)** — `$tdd-review` verdict `ready`; causal RED/GREEN, trailing-icon mutation re-red, shared/direct preservation, 2,523-test `1to1` lane plus relay Go checks, analyzer, diff hygiene, and Graphify refresh passed | MODIFICATION: add the canonical Download, iOS Share, outlined Info, and Reply icons beside Plan 307's unchanged localized compact-overflow labels. Preserve order, directional layout, capabilities, keys, dispatch, action effects, bottom controls, and every non-compact viewer. |

---

## 3Z. Message context long-press haptics (2026-07-31)

| Plan | Status | Owns / boundary |
|---|---|---|
| [309-message-context-long-press-haptics-tdd-plan.md](309-message-context-long-press-haptics-tdd-plan.md) | **Implemented / host-green (2026-07-31)** — `$tdd-review` tightened isolated per-variant discrimination and exact sentinel paths; causal RED/GREEN plus mutation re-red passed; full 16/80/73 affected suites, 2,524-test `1to1`, 3,266-test `groups`, Go tails, analyzer, diff hygiene, and Graphify refresh are green | FEATURE IMPROVEMENT: one small `lightImpact` when the shared message context overlay opens from text, visual-media, or voice long presses in 1:1 and group conversations. Existing copy `selectionClick`, menus, actions, authorization, and message behavior remain unchanged. |

---

## 3AA. Android notification recovery completion (2026-08-04)

| Plan | Status | Owns / boundary |
|---|---|---|
| [335-signal-style-group-foreground-notification-preview-tdd-plan.md](335-signal-style-group-foreground-notification-preview-tdd-plan.md) | **CLOSED / PLAN-GREEN / ANDROID DEVICE-VERIFIED (2026-08-04).** `$tdd-review` verdict `plan-fixes-required` was applied before RED. Typed anchored foreground group drain-error recovery now resolves recipient-owned group/sender context and the exact local key, then reuses authenticated decrypt/privacy formatting; untrusted resolution suppresses instead of leaking generic/provider copy. Causal RED, mutation re-red, focused 117/117, groups 4024/4024 plus Go tails, full analyzer, hygiene, and current Graphify fingerprint `f2c73d7ccf0c33f3` are green. Android S2 passed every programmatic/OS predicate with one Signal-style card on emulator `emulator-5554` plus USB Pixel `21071FDF600CSC`; it remains preservation rather than drain-error causality. | BUG CLOSED: group recovery shows `group / sender: preview`, promotes authenticated-inner IDs, preserves private media, identity-free wakes, payload-only legacy routes, and single-owned successful canonical presentation. No relay, schema, iOS, notification-ID, tone, or private-media-policy change. |
| [334-android-background-storage-queue-liveness-tdd-plan.md](334-android-background-storage-queue-liveness-tdd-plan.md) | **CLOSED / PLAN-GREEN / ANDROID DEVICE-VERIFIED (2026-08-04).** A real SQLCipher five-phase queue inversion first reproduced on USB Pixel `21071FDF600CSC` and tripped the 4 s causal watchdog at 7384 ms; the immutable RED artifact is retained. Current-source queue and handoff/process-death proof is green on that Pixel and emulator `emulator-5554`, with zero native residue; native contracts, Pixel raw-key 8/8, rollback/restoration, focused 335/335, adversarial notification 41/41, completeness 1414/1414, ST-008, serialized groups 4016/4016 plus four Go tails, core 3100/3100, feature 8777 pass/1 declared skip, full analyzer, diff hygiene, and current Graphify fingerprint `dc4beab216d8c42b` are green. The exact historical lock owner remains unknowable; the source-permitted mechanism is deterministically proven without claiming that missing attribution. | BUG CLOSED: sqflite_sqlcipher Android state is engine-owned with per-engine FIFO/maps/handles; read-only background storage uses one 8 s aggregate / 2 s phase budget and a 1 s busy timeout; Android coordination uses bounded contender acquisition while acquired mutating/native owners remain serialized; event/tone publication is one-way, strict-shape, replay-proof, atomic, and fail-closed with max-preserving silent repair; direct custody stages before eligibility. Group policy, relay custody, exact post-show ownership, same-group order, Plan 331 activation, server/wire/schema, and legacy iOS/macOS lock/parser/in-place-write behavior remain preserved. Dart-timeout-only, FlutterFire-latch forks, and per-group-listener changes remain prohibited. |
| [333-iphone-notification-recovery-parity-tdd-plan.md](333-iphone-notification-recovery-parity-tdd-plan.md) | **IMPLEMENTED / HOST+SIMULATOR VERIFIED / PHYSICAL EVIDENCE-GATED (2026-08-04).** `$tdd-review` corrections and the post-implementation audit leave no blocker/high/medium production-correctness finding. Focused Dart 212/212, pinned-simulator XCTest 94/94, `1to1` 2778/2778, `groups` 4050/4050, core 3131/3131, feature 8800 + 1 intentional skip, adapter contracts, Go preservation, strict analysis, and hygiene are green. Physical closure reuses one existing target-bound capability for two serial fresh-install legs on one discovered iPhone; owner-private APNs/signing/provider/relay inputs are absent, so that receipt remains open. | BUG: adds native account/generation-fenced exact Apple request custody, watermark-safe remote-card retirement, badge-free NSE handoff, and one iOS-only pending-to-canonical absolute direct+group unread reconciler. Cold open uses one atomic native capture/clear/arm operation, conditional Firebase fallback, opaque-scope settlement, and Dart retention until strict routing succeeds, including failed-node release. Preserves NSE privacy/parity, trusted thread grouping, visible alert transport, reaction collapse, direct staged recovery, and Android ownership. No summary metadata, NSE SQLCipher/network/Flutter, provider unread badge, ordinary collapse ID, blanket remote clear, Android scheduler clone, cross-language codec, or new provider stack. |
| [332-android-oem-launcher-badge-completion-tdd-plan.md](332-android-oem-launcher-badge-completion-tdd-plan.md) | **EXECUTION-READY / STANDARD-ANDROID / AVAILABILITY-BOUNDED (2026-08-03).** Current effective behavior is host-green; remaining work pins explicit badge-capable channel/detail metadata, permanent dependency/merged-manifest denial, and independent fresh-identity proof on USB Pixel plus one available emulator. Other OEM hardware is policy-authorized N/A, not a blocker. | VERIFICATION + REGRESSION HARDENING: retain independent per-conversation Android `number`/history and exact cancellation through standard notification APIs only. ShortcutBadger, all 16 vendor permissions, an app-global Android count, native/plugin bridge, SQL/schema work, relay/FCM dependency, and a numeric Pixel/OEM rendering promise are explicitly rejected. Legacy manufacturer-specific numeric badges require a future decision and plan. |
| [331-android-notification-recovery-completion-tdd-plan.md](331-android-notification-recovery-completion-tdd-plan.md) | **PARTIALLY IMPLEMENTED / HOST+H0 DEVICE GREEN / PRODUCTION-ACTIVATION SAFETY-GATED (2026-08-03).** `$tdd-review` returned `not-ready` on the submitted draft and its repairs are applied. Landed slices include the process-global runtime foundation, account-bound WorkManager protocol, DB v107 direct custody/read/reconciliation, authenticated inner-ID defense-in-depth, rich canonical history/count plus encrypted pre-materialization overlay, durable registration health/settings UX, and localized native recovery copy. Exact core `3048/3048`, feature `8754 passed + 1 capability skip`, curated lanes, pinned Pixel baseline/transport, native/Go, analyzer, and both-device real SQLCipher/H0 proofs are green. A second critical audit proved the UI bootstrap cannot safely compose production headless direct/group recovery yet, so `activateRecoveryWork` remains false and the AOT entry point truthfully retries with `headless_composition_unavailable`. The plan remains open for the UI-neutral production graph, invocation/identity/ingress/projection/teardown seams, reboot/full-headless device proof, and TC-331-23; its Sims row is intentionally `automationReady:false`, and relay/FCM credentials are absent. | BUG: closes independently safe Android notification durability/projection/health/localization work after Plan 330 while refusing an unsafe headless acknowledgement path. Defers canonical shared-reaction re-keying, OEM launcher badges, iOS parity, user force-stop, and identity-free exact binding. |
| [331a-android-canonical-runtime-ownership-prerequisite-tdd-plan.md](331a-android-canonical-runtime-ownership-prerequisite-tdd-plan.md) | **H0 COMPLETE / DEVICE-VERIFIED (2026-08-03).** `ACTIVE -> DRAINING -> RELEASED` ownership, single Go initialization, JNI/result/callback fencing, explicit SQLCipher close-before-transfer, account-bound custody, minimal plugins, no Activity/UI root, close-failure retention, FlutterFire read-only coexistence, and process-death handoff pass on USB Pixel `21071FDF600CSC` API36 and emulator `emulator-5554` API37. Final source/APK digests are `9c956aa4...d8a7d` / `26323df5...f4d36`; all four content-addressed artifacts keep recovery activation false and the disposable package is absent afterward. | COMPLETED BUG PREREQUISITE: one process-global Go singleton and one writable SQLCipher owner are safe. This proves ownership only; it does not supply or authorize Plan 331's production inbox/projection graph. |
| [330-android-group-notification-projection-durability-tdd-plan.md](330-android-group-notification-projection-durability-tdd-plan.md) | **CLOSED / PLAN-GREEN / ANDROID-CLOSED (2026-08-03).** The critical-review passes repaired marker-first custody, typed claims, total policy, a shared keyed read/show lane, durable cross-kind reconciliation, exact-event read acknowledgement, foreground/background replay fences, trigger-safe rollback-atomic account import, privacy-bounded reaction copy, identifier normalization, and two final proof defects (unfenced ADB compose focus and a stale kind-wide read-cancel validator). Final receipts: affected 788/788; completeness 1389/1389; final post-harness `groups` 3904/3904 plus all Go/relay legs; runtime-roots 20/20; core 3000/3000 across 379 paths; feature 825/825 paths exit 0 with one known SQLCipher-capability skip; Sims 22/22; analyzer/diff clean; migration chain 17/17; all eight current-source SQLCipher/release-diagnostic legs PASS on USB Pixel `21071FDF600CSC` API36 and emulator `emulator-5554` API37. Formal projection passed 5/5 with APK SHA `2cea4ae25b4add38bb3b37dfbc5e7d1beff2664869e276ede60e329d78b4ab32` and evidence SHA `ea0a898a65d89eedba4ee3fecd70d1ec515e882368e157ffc7b28f411ff2f433`; reaction campaign passed 5/5 (evidence SHA `42326cb83736902588dc83aa6bff72205e750d2478ccddc5ba55666f6603cb57`); S2/S8/S9/S10 text/photo/video/voice each passed programmatic + OS-card checks with one stable card and zero duplicates (summary SHA `cc7c7280ce0431714bdcb4feef70c1cc6830b1d9f97bde08d041c9f333086e5a`). No iOS, manual input, or notification-card taps. Plan 331 has since landed the ownership H0, direct custody, authenticated-inner-ID defense, rich history/Android `number`, registration health, and native localization slices. Remaining residuals are the UI-neutral production headless recovery graph/activation, conservative behavior for truly identity-free pushes, OEM badge behavior, and manual iOS parity. | BUG + RELIABILITY HARDENING: canonical main-runtime OS-card custody for eligible group text/image/video/voice/reaction events across display errors; exact A-only retirement on read/policy/delete/REMOVE while later B and unrelated cards survive. No wire/relay/plaintext-outbox/headless-Go change. |
| [309-group-notification-reliability-tdd-plan.md](309-group-notification-reliability-tdd-plan.md) | **Execution-ready (v2, `$tdd-review` applied 2026-07-31)** — 13-agent verify→refute pass (**5 of 9 candidate findings refuted**, recorded as do-not-re-introduce), then a 4-worker adversarial review that returned `plan-fixes-required` on v1 and found **2 blockers**: C1's surface census was incomplete (six unguarded surfaces, not four — both live in-app paths were missed) and C3's fix was a **no-op** (`saveKey` already publishes the epoch before any appended write). Both fixed in v2 along with 9 plan-fixes. 17-row contract across Dart host, Swift XCTest, and Go host; one manual `GROUP_TESTS` registration; no migration; five user-owned decisions (D1/D1b/D2/D3/D4) open by design | BUG: three source-confirmed notification defects — (C1) the `archived` guard exists on only 2 of 8 notification-decision surfaces, so archived groups still alert for reactions on both platforms and for messages *and* reactions in-app; (C2) group reaction pushes collapse per-group, so concurrent offline reactions overwrite each other (FCM caps a token at 4 distinct collapse keys — review recommends omitting the key rather than making it per-event); (C3) a freshly joined group sits in the iOS NSE projection without a keyEpoch for the whole roster loop, blanking every group push in that window. Preserves push-message archived suppression, mute on both live paths, `apns-collapse-id` semantics, the iOS epoch/role gates, the NSE blanking privacy boundary, and the relay ACL. Defers: drain-notifies-for-reactions (D4) and the later-joined-admin custody drop (F7 — *message loss*, needs its own plan). **2026-07-31 postscript: implemented, deployed as relay v1.7.0, and FULLY ROLLED BACK the same day after the app broke on device; client edits reverted, relay back on v1.6.0. Re-land only under a staged protocol (one device + smoke before fleet/relay), after the plan-315 wave.** |
| [315-group-reaction-wake-nomination-tdd-plan.md](315-group-reaction-wake-nomination-tdd-plan.md) | **Implemented / host-green (2026-08-01)** — all 16 contract rows closed at host tier: causal REDs on the documented mechanisms, TC-02 mutation re-red (`Expected ['peer-2'], Actual []`), 3,281-test `groups` lane + go-mknoon tails + filtered AND unfiltered relay suites green, completeness 1366/1366, analyzer/diff clean, Graphify refreshed. **Open operational boundaries (user-owned): the six-scenario real-device campaign (incl. the new backgrounded-but-connected scenario) has not run, and relay v1.7.1 is built but NOT deployed.** Plan v2 provenance: grounded in live production forensics (relay journal + redis custody: zero reaction pushes in a 24h test window; the only reaction envelopes in custody are self-reactions with signed empty wake lists) plus an 8-agent verify→refute workflow that **refuted H1** (custody is NOT reachability-conditional) and **refuted H3-as-stated** (empty `devices_json` is covered by the account-id legacy fallback). A 3-worker adversarial review then found **2 blockers, both fixed in v2**: the F1 fallback could not distinguish never-arrived from REMOVED authors (removal hard-deletes the roster row ⇒ ACL leak to ex-members; now gated on `getRemovedMemberSnapshot`, TC-15) and the F2 pre-check missed the third store path (`exactRetry` re-tap bypass ⇒ false `stored`; guard moved into `_attemptReactionInboxStore`, TC-16) — plus 8 plan-fixes (retrier now RETIRES unroutable rows; TC-12 gains push-origin + connectivity discriminators; relay counter rows gain negatives; journal lines avoid the validator's `[PUSH] Notification sent to` contract). 16-row contract across Dart host, Go host, and one new device-lane scenario; zero manual gate registrations; no migration; relay version bumps to 1.7.1 (1.7.0 is burned by the rolled-back 309 binary) | BUG: three confirmed reaction-wake defects — (F1) an author missing from the reactor's roster yields a signed, **permanent** empty wake nomination (custody stores, relay silent, frozen-payload retries dedupe as duplicates above the fan-out, so the wake decision is once-per-transitionId); (F2) an empty replay set becomes a silent false `stored` (Go bridge no-ops without calling the relay; the flagship EK004 test is green on exactly this dropped shape); (F4) every relay-side wake decline — empty nomination, capability skip, duplicate, ~15 validation rejections — has no counter and no journal line, and even successful reaction pushes log unattributed. Fixes: author-account fallback into BOTH replay+nomination (subset preserved; self-reaction and all-revoked stay empty by design), loud reaction-scoped unroutable-custody pre-check (row `failed`, never false-`stored`, distinct flow events), relay `relay_group_reaction_wake_total{outcome}` counter + attributed journal lines, stale "offline members" docstring fix, and a 6th device scenario (backgrounded-but-connected recipient — the exact user-test topology no tier covered). Defers: stage-build-throw custody abandonment (F3), wake-rescue-on-duplicate (F5), inviter-side invitee device backfill (multi-device wave), message-lane empty-set observability twin. |
| [329-signal-grade-group-notification-recovery-tdd-plan.md](329-signal-grade-group-notification-recovery-tdd-plan.md) | **IMPLEMENTED / SOURCE-CLOSED (2026-08-02; production rollout not performed)** — Signal-informed P0 reliability slice. `/tdd-review` first returned `plan-fixes-required`, refuted a duplicate foreground presenter, and forced a race-safe redesign: app-owned Android `onDeletedMessages`, durable monotonic generation + compare-and-ack, authoritative cold/resume polling, truthful awaited direct+group convergence, canonical recovered-reaction presentation, and apply-before-delete reaction custody. Also absorbs/implements plan 328's unusable-envelope guards and sender attribution, makes a nil relay provider non-success, removes warm+cold blanket `cancelAll`, and coalesces the generic recovery card with alert-once semantics. Current-source focused/native/groups/feature evidence is green; core/1:1 contain only audited checkpoint debt and zero Plan-329 failures. Fresh closure: the attested real-FCM aggregate passed 5/5 text/reaction scenarios on emulator `emulator-5554` + USB Pixel `21071FDF600CSC`; a separate Android-only `--non-interactive` media run passed S2/S8/S9/S10 text/photo/video/voice OS-card rows with zero duplicates (its summary does not independently embed target/build provenance). iPhone remains manual-only. Plan 324 owns relay rollout only; app-release ownership is not assigned here. |
| [328-group-media-push-notification-integrity-tdd-plan.md](328-group-media-push-notification-integrity-tdd-plan.md) | **SUPERSEDED / ABSORBED AND IMPLEMENTED BY PLAN 329.** Ordinary direct/group encrypted-preview extraction now fails closed to `preview_unavailable`; Android trusted degraded group copy retains sender attribution without adding plaintext media-type hints. Relay rollout remains bundled with plan 324. |
| [320-permanent-push-error-and-wake-measurability-tdd-plan.md](320-permanent-push-error-and-wake-measurability-tdd-plan.md) | **CLOSED (2026-08-01 — relay v1.7.5 deployed stable 20:08Z; all 13 rows closed, six mutation-reds run+restored; groups lane 3342/3342 LANE_EXIT=0 on a quiescent rerun after one documented load-flake; TC-320-13 deterministic dead-token probe PASS: scratch peer + real retired token → one store → same-second `Removed invalid token … reason=typed_unregistered`, zero retry lines, key deleted — live FCM's 404 carries the structured UNREGISTERED errorCode, so the TYPED arm fires in production and the literal arm is belt-and-braces; P2 live-token re-read + cached fallback shipped client-side; P3 transition= on both wake lines + all three silent declines counted with the per-transition accounting identity)** — F5 part 1, scope set by live production evidence + a 10-agent verify→refute pass (`wf_c500330c-980`) that **refuted the planner's own inference** that "production shows zero lost wakes": no wake line carries the transitionId and `attempted` is incremented before the goroutine dispatch, so the journal is structurally incapable of exhibiting a lost wake. Three confirmed defects: (P1) `isInvalidTokenError` is a hand-rolled CASE-SENSITIVE two-literal substring test that misses the SDK's `NotRegistered`, so a PERMANENT dead-token error walks the transient 3-attempt ladder and the token is never evicted (live journal proof); the pinned `firebase.google.com/go/v4@v4.15.1` does expose typed predicates (`IsUnregistered`, `IsSenderIDMismatch`) — confirmed by a host-side module probe, clearing the review-flagged "unspecifiable" blocker; (P2) any eviction is undone because the client re-registers its CACHED token on every relay-health transition; (P3) the wake path is unmeasurable (no transition id on either wake line, two entirely silent decline guards, dispatch counted as if delivery). Harm bound confirmed: custody precedes every wake decision, so a missed wake costs notification latency, not the reaction. 13-row contract; new Go test file name-prefixed to match the lane's only relay filter; relay v1.7.5 ships under standing deploy authorization. **Re-fan-out-on-duplicate DEFERRED by explicit user decision** — recorded honestly as "population unmeasurable until P3 lands + a safe design needs durable per-(transitionId,recipient) state that does not exist", NOT as "production proved it unnecessary" | BUG: dead FCM tokens burn retry ladders and persist forever; declined wakes are invisible to measurement. |
| [327-content-free-push-privacy-tdd-plan.md](327-content-free-push-privacy-tdd-plan.md) | **PLANNED, NOT EXECUTED (user decision 2026-08-02). Classification: evidence-gated — Stage A ready, Stage B needs a threat-model decision, Stage C prerequisite-blocked.** PRIVACY (metadata, NOT content): every push hands APNs/FCM `type` (activity class), `sender_id`, `groupId`, `sender_transport_peer_id`, `message_id`, the reaction correlators (`event_id`/`target_message_id`/`reactor_peer_id`, reaction_push.go:418-432) — plus **display names in clear** (`sender_username` inbox.go:498-500; group names in invite copy :458-459/:1109). **Message content does NOT leak**: visible-alert title/body come from fixed templates (:447-500) and group message pushes carry no title/body at all (:544-551) — frame honestly as a social-graph/activity-timeline exposure. Staged by threat-model delta: **A** strip display names (cheapest real win; reuses the ALREADY-SHIPPING envelope-less path `buildOversizedFallbackPushMessage`, see 173-…:39), **B** opaque per-event routing tokens (largest win — breaks cross-event graph building; note the relay already knows sender/group/message since it routes them, so it may hold the mapping — needs sign-off), **C** full Signal model. **C is prerequisite-blocked**: Signal's presenter is its NSE and Signal's NSE FETCHES; ours has ZERO network I/O (grep URLSession/WebSocket/http in ios/NotificationService/ → nothing) and must never open SQLCipher, inside a ~24MB budget. **REFUTED: a contentless wake does NOT improve reliability** — a lost wake is lost identically; never sell this as a reliability fix. Prior art reconciled: plan 74's *preview-text* concern is CLOSED (group pushes are ciphertext-only); **plan 148 is in DIRECT TENSION** (it wants a RICHER payload spooled for tap latency, and is itself blocked on Phase-0) — one must be chosen. Highest-risk coupling: reaction dedupe identity parity across relay/Android/NSE. 10 contract rows (8 executable at Stage A). |
| [326-group-roster-convergence-tdd-plan.md](326-group-roster-convergence-tdd-plan.md) | **PLANNED — Stage 1 execution-ready, Stage 2 PREREQUISITE-BLOCKED. The headline defect REMAINS OPEN after Stage 1.** Successor to plan 323 deferral C. Headline: `_applyAuthoritativeGroupConfigSnapshot` hard-deletes members omitted from a snapshot (:3703-3708, default `true` at :3607, taken by :2389/:2627/:2706), and a metadata edit builds the snapshot from the acting admin's OWN roster (group_info_wired.dart:1934-1938) — so an admin merely missing a member strips that member's custody everywhere. **Cannot be fixed by removing the prune**: it is the ONLY self-healing path, and the roster is a signed-transition hash input (signed_group_transition_audit.dart:439), so a retained ghost freezes every later audited transition at processor:404-410 — whose rejection event has ZERO consumers in lib/. **All four candidate designs evaluated, none sufficient**: D1 config-request reconciliation **REFUTED** (the response roster is the SAME artifact — on_join_group_config_resync_use_case.dart:90 → buildGroupConfigPayload, identical to the defective snapshot; and normalizeGroupConfigMembers silently drops members failing hasDeliverableGroupMemberIdentity); D2 per-member guard **REFUTED** (no discriminator on the wire — `configVersion` always postdates `joinedAt`); D3 soft-delete **defeated by the hash** (forces a binary answer exactly when D3 defers; costs DB v106 + a tri-state across 66 getMembers sites in 38 files); D4 explicit-removal-only **strongest but silent about repair**. **Stage 1 ships two INDEPENDENT defects found on the same lines**: (S1) `:2706` passes neither `eventAt` nor `msgRepo`, so the removal guard at :3660-3671 is inert and a metadata edit **RESURRECTS explicitly-removed members**; (S2) `:2707` syncs the AUTHOR's truncated config to Go's dial/discovery allow-list — fix already exists at `_buildLocalGroupConfigSnapshot` (:3828-3837, used that way at :1956-1958). 6 rows; TC-326-04 is a deliberate sentinel documenting today's prune so Stage 2 has a pre-agreed RED to flip. **U1 open: no sound reconciliation channel exists.** |
| [325-accept-invite-drain-reaction-custody-tdd-plan.md](325-accept-invite-drain-reaction-custody-tdd-plan.md) | **PLANNED, NOT EXECUTED.** The 5th drain lane dropped from plan 322 by review. BUG: `_drainAcceptedGroupInboxBestEffort` (accept_pending_group_invite_use_case.dart:1217-1254) calls `drainGroupOfflineInboxForGroup` at :1230 with NO `pendingReactionRepo`, so a reaction whose target has not landed is permanently discarded (handle_incoming_group_reaction_use_case.dart:203-214); the cursor commits unconditionally (drain:1015-1026) so the relay never re-serves it. **Design: the 10-file threading review costed is unnecessary** — `GroupMessageListener` already HOLDS the repo (:147) and is ALREADY passed to this exact function (:1224/:1237), so the fix is a public getter + one argument. Verified: exactly one `GroupMessageListener(` exists in lib (bootstrap:3746, always repo-bearing at :3797), and where the listener is null the accept lane is unreachable (orbit_wired.dart:1595-1601). **REFUTED: the review's post-accept-flush alternative** — `_flushStartupDurablePendingReactions` (processor:198-213) replays ALREADY-BUFFERED rows; nothing is buffered, so it cannot fix an ingestion defect. **Reachability is DETERMINISTIC, not a race**: the drain defers unknown-sender MESSAGES to a second pass (:2233-2245, :963-990) but never defers reactions (:959), so a page `[M(unknown_sender), R(on M)]` drops R — and unknown-sender is exactly the freshly-accepted-group condition; plus the membership flush straddles the drain (accept:429/434/447). 6 rows. Two mandatory riders: amend the frozen facade set (decomposition_contract_test:307-327, precedent at :311), and **add the accept file to plan 322's census map (wiring_test:436-445) — its absence there is the mechanical reason 322 missed this lane**. |
| [324-reaction-recipient-binding-fail-open-tdd-plan.md](324-reaction-recipient-binding-fail-open-tdd-plan.md) | **PLANNED, NOT EXECUTED (user decision 2026-08-02 — queued behind 322/323).** BUG (security hardening, latent — **no current user impact**): the relay's group-reaction recipient ACL is not bound to the signed set. `reaction_push.go:192-196` returns `valid=false` when the request recipient set differs from the signed `replayRecipients`, and `inbox.go:1608` gates the signed-set override on that same boolean — so a mismatch **skips** the binding and the client-supplied set reaches `s.store` (`:1616`) and is unioned by `mergePeerIds`. The check meant to reject a mismatch instead disables the constraint. Unreachable from any shipping client (`send_group_reaction_use_case.dart:376` sends exactly the signed set, and both sides canonicalize through `canonicalGroupReactionPeerIDs` — no asymmetry), so this is hardening against a modified sender, not a live defect. **Fix shape is fail-SAFE, not fail-closed:** when the envelope is well-formed but the request set disagrees, use the **signed** set; rejecting the store would destroy reaction custody for any client that ever disagreed. 7 Go host rows + a live post-deploy proof; relay deploy to **v1.7.6**. |
| [323-group-custody-318-deferrals-tdd-plan.md](323-group-custody-318-deferrals-tdd-plan.md) | **PARTIALLY EXECUTED 2026-08-02 — `/tdd-review` (`wf_d8df3c95-e44`) returned not-ready and materially changed two of three items.** Takes up all three plan-318 deferrals. **(A) historical-custody remediation → WON'T-BUILD, source-proven infeasible**: the premise that `inboxRetryPayload` holds the signed envelope is false — every successful-store path nulls it (`send_group_message_use_case.dart:2113/2155/2196/2257/2316`) and successful stores ARE the target population; plus non-reproducible envelopes (fresh nonce), receiver `recipient_not_entitled` fail-close, TTL-expired resurrection, and sender-device-only action. **(B) private-media retry → BUILT as B2 only**: subset containment (`persisted ⊆ current`) + explicit empty-set denial + preserved sender arm, local to `_matchesCurrentPrivateRetryRecipients`; the shared `sameGroupPrivateMediaRecipientPeerIds` is deliberately untouched (3 other callers need exact equality — one is a blob-ACL drift check). **B1 (thread `inviteDeliveryAttemptRepo` into the retry lane) was DROPPED by review**: subset is monotone in `current`, so B1 is redundant, and narrowing `current` can only turn allows into denies — reintroducing the exclusion-driven custody loss 318 closed. 4 tests, 3 mutations verified (exact equality → TC-323-02 red; no empty guard → TC-323-04 red; inverted predicate → TC-323-03 red **by allowing a departed peer**). **(C) U3 config-resync → classified, returned to design**: on-join resync is metadata-only; the real hazard is `_applyAuthoritativeGroupConfigSnapshot`'s omitted-member prune (`:3703-3708`, default `true` at `:3607`, taken by `:2389/:2627/:2706`). NOT fixed — the prune is the **only** downward roster-convergence path (suppressing it leaves ghosts holding relay custody *and* receiving rotated group keys, `rotate_and_distribute_group_key_use_case.dart:296`), and the roster is a signed-transition hash input (`signed_group_transition_audit.dart:439`) so a retained ghost freezes every later audited transition on that device. Also corrected: 318's U1 `unknown`-status rationale is void (structurally unpersistable, `group_invite_delivery_attempt.dart:115-118`). |
| [322-drain-reaction-custody-tdd-plan.md](322-drain-reaction-custody-tdd-plan.md) | **IMPLEMENTED 2026-08-02 (D4 resolution).** BUG: a group reaction recovered by the offline inbox drain was **permanently discarded** when its target message had not landed — `handleIncomingGroupReaction` buffers only when `pendingReactionRepo != null` (`:182-202`), else emits `GROUP_REACTION_RECEIVE_UNKNOWN_MESSAGE` and drops it (`:203-214`), and the relay entry is already consumed. 5 of 8 production drain entry points omitted the argument, including the app-resume drain. **D4 as filed ("should the drain notify for reactions?") is closed WON'T-BUILD**: the drain-persists/push-presents split is architected and pinned (`group_reaction_notification_pipeline_test.dart:25` "display must follow inbox drain", `:100` shared atomic claim); a drain notify would be a third producer arbitrated only by a durable claim that fails OPEN on storage error and evicts past 256 entries/48h. Also refuted: routing through the listener (drops `senderPublicKey`, which fails OPEN on the key dimension of `_isReactionSenderDeviceBound`). **Built:** `pendingReactionRepo` threaded at 4 sites (resume ×2, startup network recovery incl. `_buildRestartedStartupRouter`'s 60-field clone, notif-tap via both callers) + throw isolation in the drain's reaction branch (it runs OUTSIDE the decode try/catch, so a buffer throw would raise `errorCount` and cost the group its recovery ack AND cursor advance). Site 8 (accept-invite) **dropped by review** — 6 private functions + 4 widget public APIs across ~10 files; own plan. Tests: TC-322-02/02b source census (the wiring IS the defect, so a census is the causal pin) + TC-322-03 throw isolation, all three mutation-verified. `/tdd-review` `wf_d8df3c95-e44`. |
| [321-fresh-join-projection-atomicity-tdd-plan.md](321-fresh-join-projection-atomicity-tdd-plan.md) | **IMPLEMENTED, host-green (2026-08-01) — TC-01..11 closed: causal RED with reason verification (join SUCCESS + first doc `keyEpoch: null`), 11 mutations red+restored md5-identical (incl. both TC-01 directions, the unwrapped-publish race, and the Swift `?? 0` parser mutant against the epoch-0-hardened fixture), lane 3351/3351 LANE_EXIT=0 first quiescent try, Swift `NotificationPreviewResolverTests` SUCCEEDED (2 pre-existing `IosReceiverBootstrapHandoffTests` reds = branch baseline from `7f31a155a`, untouched). Queued projection writes per fresh join now 1 (was N+2). TC-07's mutation downgraded to sentinel-only (review option A — no harness stages latest≠pinned at reentry). TC-321-12 ROLLOUT COMPLETE (2026-08-02) — active fleet (Pixel + iPhone 11 + iPhone 13) verified on the new build: Pixel smoke PASS (background→reopen = the 07-31 shape passes); live fresh-join + push-render PASS via the full reaction campaign on the 321 tree vs prod v1.7.5 + real FCM (assertions=5, CAMPAIGN_EXIT=0; the campaign's relay-provenance guard correctly blocked until the staging manifest declared v1.7.5); fleet verified on the Pixel + 2 iPhones; the 17 Pro Max/iPhone 12 UDIDs are absent from the signing profile (proven by decoding the embedded profile; same two failed identically on 2026-07-17) and are not in the active fleet. Plan history: v2 /tdd-review `wf_e8ef0731-598` (core bet CONFIRMED, 7 fixes incl. the fake TC-01 RED, the FIVE-site census with the local-variable `_mirrorGroupContextForPush:1693`, two broken acceptance commands, in-scope barrier staging, TC-04's pre-seeded discriminator, TC-09's observable split); grounding `wf_e74e0e40-017` (naive suppression sketch REFUTED with two kills)** — C3's hardened successor (dropped from the 309 re-land; the seam is the lead suspect for the unrecovered 07-31 "unable to load anything" Android breakage, recorded shape replayed as the TC-321-12 staged-rollout smoke). Design: capability `is`-check on `GroupRepositoryImpl` only (no interface change); refcounted per-groupId MIRROR-UPSERT-ONLY suppression consulted at exactly three sites (`_projectAuthoritativeGroup` — covers saveGroup AND updateGroup, closing KILL 1 — plus saveMember/saveKey mirrors), removals NEVER suppressed (failure-branch + rollback removals pass through); atomic `projectCommittedFreshJoin(groupId)` inside `_runGroupMutation` reading the LATEST committed key (closes KILL 2's epoch downgrade — `replaceAcceptedGroupContextStrict` sets keyEpoch unconditionally, no monotonic guard), qa-type → clean no-op (its ArgumentError would otherwise fire where today is benign); publish failure = log-and-no-op flow event `GROUP_FRESH_JOIN_PROJECTION_DEFERRED` (the join NEVER fails on projection); drains reordered post-publish (reentry precedent, scope shrink). Queued projection writes per join drop N+2 → 1 (strictly less global wipe surface). 12-row contract, host closure (Dart + Swift XCTest locking BOTH NSE parsers' epoch-drop guards — 309's TC-11 never landed); no migration, no relay; staged rollout MANDATORY (Pixel smoke incl. background→reopen before fleet). Refuted-and-recorded: 309 v1's append-write no-op, the network-stretched-window claim, the retrier-re-materializes claim, "already fixed on HEAD", 309's stale NSE guard line refs | BUG: a fresh accepted join publishes the group epoch-less for the whole roster loop; BOTH NSE snapshot parsers drop epoch-less docs, so every push for the new group renders sanitized/blank during the window; the N+2 incremental writes multiply the projection's wipe-on-failure surface; a failed mid-join projection can blank the group until relaunch. |
| [319-reaction-custody-needs-build-rescue-tdd-plan.md](319-reaction-custody-needs-build-rescue-tdd-plan.md) | **CLOSED host-tier (2026-08-01, commit `cc7c16ec1`)** — F3 fixed in BOTH lanes: needs_build-first staging, DB v105 (sentinel-empty payload, rowid+indexes preserved, registry/version/7 chain tests wired), write-guard honesty, remove-lane unroutable pre-check, retrier rebuild with ORIGINAL ids + created_at-anchored plaintext, needs_build threaded through all consumers. REDs first (incl. the ordering leg), mutation re-red, 638 host tests + 184 affected dependents green, completeness PASS, zero relay diffs, curated lane green on a single reaped run (three earlier lane reads were self-inflicted: mid-lane mutation/stash, host load, and a launcher without a reap running three lanes at once — launcher fixed). Plan provenance: **Execution-ready (v2, `/tdd-review` applied 2026-08-01)** — 3-worker audit: 2 blockers fixed (v1's rollback story false both directions — full revert fail-closes at the one-way DB floor, NULL payload crashes old builds' unfiltered probe → sentinel-empty representation + keep-migration rollback semantics; TC-14 was vacuous → real unfiltered-probe leg); exactRetry "untouched" refuted (probe must gain payload-present + PK-reuse on fall-through or re-taps orphan the row); registry/version/chain-test ripple, rowid+index preservation, four cleanup IN-lists + Move manifest + E2E probe lifecycle, plaintext timestamp anchoring (tombstone guard), conflict-matcher const + negative leg all folded. Original grounding: — F3 from 315, grounded by a 10-agent verify→refute workflow (`wf_5c74a876-7b1`) that found **three silent custody-loss shapes per lane** (build-throw returns rowless while the local reaction persists, live publish succeeds, and SEND_QUEUED lies 'pending'; saveEntry-throw leaves nothing durable; NEW: the outbox upsert ignores the write-guard boolean → staged=true with zero rows for self-removed parents) plus BOTH remove-lane gaps (identical abandonment AND the missing 315 unroutable pre-check — remove's exactRetry false-stores empty payloads). **REFUTED the naive rescue**: fresh-transitionId rebuilds double-banner (live publish already notified everyone; ALL notification surfaces dedupe by transitionId) and relay custody identity is the extension TransitionID (inbox.go:1165-1174), not envelope messageId. Design: **needs_build-first staging (DB v105: nullable payload + widened status CHECK, table-rebuild migration with legacy-query rollback leg) + retrier rebuild reusing the ORIGINAL ids with the notification extension attached + mapping the relay's 'conflicting group inbox messageId' hard error to stored** (lost-ack custody already exists; without the mapping such rows fail forever). 14-row contract; client-only, zero relay diffs, no deploy | BUG: reactions/removals whose envelope build throws lose relay custody, offline replay, and recipient wakes permanently and silently. |
| [318-group-custody-local-evidence-recipients-tdd-plan.md](318-group-custody-local-evidence-recipients-tdd-plan.md) | **CLOSED host-tier (2026-08-01, commit `155308edc`)** — 15/15 rows; 4 causal REDs mutation-verified (revert reds all four); full-file sweep broke EXACTLY the two review-predicted renegotiation sites and nothing else; eligibility file passed unchanged (inclusion outcomes structural) and is now GROUP_TESTS-registered; new gate-reachable Go ACL verb test green (focused `-v` PASS + lane tail); completeness 1367/1367; curated groups lane `LANE_EXIT=0`; relay untouched, NO deploy; D1 ADOPTED with the 52-matrix INV-106 row re-scoped. Plan provenance: **Execution-ready (v2, `/tdd-review` applied 2026-08-01)** — 3-worker audit (`wf_93aaa450-407`): no blockers, core bet CONFIRMED flow-scoped; 10 fixes folded in, headline: two smoke tests pinning the deleted contract sat outside v1's edit surface (:13678/:13814 → new TC-318-15), the #1 bet flow-scoped (create flow writes invite rows only at send-result time — crash-window failure direction is include-on-doubt, not loss), Go acceptance commands were unrunnable (bridge denies bare `go`/`bash -c`; own module → repo script), private-media pre-upgrade rows are permanently retry-DENIED not frozen-replayed (accepted), TC-318-14 re-scoped (message-retry re-send CORRECTLY adopts wider sets), TC-318-09 de-theatered (pin `qualification.recipientPeerIds`), D1 sign-off expanded to the three concrete consequences (suppressed/sanitized pushes, retrieval authorization, accept-time backlog — delta = tracker-with-joined-evidence senders only). Original grounding: — F7 from 309 (later-joined-admin custody drop, MESSAGE LOSS), grounded by a 10-agent verify→refute workflow (`wf_69ebcebe-30b`) that CONFIRMED the defect on HEAD with **two independent trigger branches** (admin-arm; any witnessed-join row — which also suppresses the timeline-evidence load) and found it **WIDER than 309 recorded**: `markJoined` fires on any device witnessing a later join, flipping ordinary non-admin joiners into "trackers" too. **REFUTED the naive fix bases**: roster-minus-removals (staged-never-accepted invitees materialize into EVERY roster at staging time — `members_added` broadcast before invites send; joiners inherit them from the invite config), joinedAt (stage-time everywhere in rosters; accept-time only in timeline messages), admin-arm-removal-alone (branch 2 reproduces the identical drop), retry-recompute (envelope byte-identity feeds relay dedup/merge — which is also the future remediation channel: byte-identical re-store MERGES recipient sets). Design (309-mandated step): **exclusion by affirmative local evidence only** — staging always writes a row at stage time, so null-status can never be "my own pending invitee"; delete the null-inference + timeline machinery, keep persisted-status exclusion; include-on-doubt matches the already-accepted REG-119b + reaction-lane precedents (D1). Loss shape pinned: custody+push loss online, TOTAL loss offline (one ACL predicate gates all three retrieval verbs; no group ack/delete verb; push fanout iterates RecipientPeerIds). 14-row contract; registrations fix two discovered gate gaps: the REG-119/119b eligibility file was in NO curated array, and ZERO Go ACL tests matched the groups-lane `^TestRelayNotificationClosure_` filter (new gate-reachable `…_GroupCustodyAclVerbs` test). Client-only; relay untouched; NO deploy. Defers: historical-custody remediation (merge-based re-store), config-resync roster semantics (U3) | BUG: silent permanent group-message loss (chat/media/retry/share/announcements) for members the sender's device holds no local invite evidence for. |
| [317-state-guard-order-insensitive-verify-tdd-plan.md](317-state-guard-order-insensitive-verify-tdd-plan.md) | **CLOSED — implemented, device-closed, campaign-green (2026-08-01, commits `917b19102` + 315-scope `a91f741c4`)**: 15/15 rows; 33/33 guard tests; mutation re-red; ROUNDTRIP PASS on the Pixel in 28s; **first fully-green campaign aggregate on record (PASS assertions=5)** — run 1 red fired the plan's interpretation rule exactly as written (315-scope `--plain-name` substring nesting from the TC-12 test name, fixed with an anchored `--name` regex; guard restore was already green in the failing run). Plan provenance: **Execution-ready (v2, `/tdd-review` applied 2026-08-01)** — 4-worker adversarial audit (`wf_707851cf-825`) returned plan-fixes-required with the core bet CONFIRMED against toybox tar.c; 10 fixes folded into v2, headline: untested path↔digest binding (new TC-317-14), untested verify-stream failure (new TC-317-15), TC-317-08 fake mechanism could never fire (drift-counter redesign), TC-317-13's unconfounded-green premise refuted (latest campaign artifact predates the exit-79 fix and has a failed scenario → interpretation rule), destructive-roundtrip safety contract + archive-not-restore backup hygiene, diag5/6 committed as durable evidence, hardlink/symlink residuals documented. Grounded by a 10-agent verify→refute workflow (`wf_67cae0a2-e35`) whose adversarial pass **refuted the handover's own root cause**: the campaign-red "state-guard verify false negative" is NOT volatile skia/code_cache content churn (new forensics diag5/diag6: the re-cut verify tar is member-for-member identical to the backup — names, sizes, mtimes, uid/gid/mode, payload SHA-256 all equal; the earlier diag4 "size/mtime header diff" was a misparse of a GNU LongLink payload). Real cause: `_privateArchiveMatches` compares a whole-tar SHA-256 byte-exact while restore's clear+re-extract RE-CREATES directories, and the Pixel 6's /data enumerates a re-created many-entry directory in a different readdir order — identical bytes, different arrangement, deterministic mismatch that the repair/replay re-permutes and can never converge (emulator ext4 is name-hash-stable, hence green there; "live-traffic" was correlation, not mechanism). Fix: order-insensitive canonical member-digest comparison (one pure host function, `canonicalPrivateArchiveDigest`, applied symmetrically to the stored backup tar and a verify tar streamed via the capture-identical exec-out invocation + fresh pre-verify force-stop); volatile-path exclusion recorded as REFUTED/do-not-introduce (would mask real divergence and leave the same latent bug for `app_flutter/media`). No manifest/schema change — legacy retained backups (incl. `docker-ws/pixel6-state-guard-backup-20260801`) verify under the new contract for free; staging/APK/permission digests are file-level and untouched. 13-row contract, all in the existing host-all-only test file (zero new registrations); known landmines pre-caught: `loadRecovery` hardcodes null capturer (recovery tests would hit real adb — gains optional seams), fake's abstract `_verify.tar` sha branch goes dead (mismatch injection moves to real tar bytes through the capturer seam), verify stream bypasses `_runner` (ordering pins re-anchored to a shared log). Closure: Pixel 6 round-trip proof + full campaign aggregate PASS | BUG (test-infra): every guard-consuming campaign lane red on the physical phone with content-perfect restores; four ~333 MB false-negative recovery backups stranded in macOS tmp. Defers: `loadRecovery` production wrapper (no caller today), wake-window full closure (impossible; honest fail+retry accepted), retained-backup hygiene owned by this plan's device phase. |
| [316-ordinary-push-platform-projection-tdd-plan.md](316-ordinary-push-platform-projection-tdd-plan.md) | **Implemented / host-green (2026-08-01, v2 `/tdd-review` applied)** — review found 3 blockers, all fixed in execution: (R1) ios projection would have killed the provider-rejection rescue (silent drop instead of generic banner) → CustomData routing-source in `buildStrictMinimalFallbackPushMessage` + TC-09; (R2) the plan's own gate-reachable ios test `push_payload_closure_test.go:277` was the causal RED — reproduced projection-only (`send calls = 1`), greened by the rescue fix with the test UNTOUCHED; (R3) four `inbox_test.go` ios sites relocated to CustomData assertions (the only unfiltered failures — zero collateral). 10-row contract closed: 6 causal REDs + mutation re-red + filtered/unfiltered relay suites green + gofmt/diff clean. Also: 1:1 reaction builder gains the missing per-leg provider-budget gate; stale data-only 4096 comment rewritten; provider error now logged on rescued sends. **v1.7.2 DEPLOYED 2026-08-01T23:18Z (pinned toolchain, 92s stable, baselines captured). TC-08 residual: the observation window + the iOS decrypted-preview device check.** Deploy bonus: first-ever live reaction-wake evidence — `relay_group_reaction_wake_total{attempted}=6, {duplicate_suppressed}=2` within 35 min of plan-315's v1.7.1 | BUG: ordinary chat/group/1:1-reaction push builders ship BOTH the FCM `Data` map and the APNs `CustomData` copy in one request; the per-leg budget check passes while FCM rejects the combined message, downgrading mid-band (media-sized) previews to generic banners after a failed round-trip. Fix: generalize the group-reaction lane's proven per-platform projection (`projectGroupReactionPushMessageForPlatform`) and apply it at the three un-projected send sites — shape-aware on iOS (drop `Data` only when `CustomData` exists; visible-copy tap-routing keys preserved). PROD-CRITICAL leg closes post-deploy via flat `payload_too_large` counters and zero new strict-fallback journal lines. |

---

## 3AB. 1:1 Keep-in-chat received-video overflow actions (2026-07-31)

| Plan | Status | Owns / boundary |
|---|---|---|
| [310-1to1-keep-in-chat-video-viewer-overflow-actions-tdd-plan.md](310-1to1-keep-in-chat-video-viewer-overflow-actions-tdd-plan.md) | **Implemented / host-green (2026-07-31)** — `$tdd-review` initial `plan-fixes-required` was applied and re-audited `ready`; causal RED/GREEN plus three mutation re-reds passed; complete 20/20 shared-viewer and 19/19 direct-actions suites, exact preservation sentinels, the 2,525-test `1to1` lane and Go tails, analyzer, format/diff hygiene, and incremental Graphify refresh are green | MODIFICATION: received ordinary 1:1 videos use one three-dot popup containing every authorized media action (Save/Share/Info/Reply/Forward/Delete) plus conditional PiP, with icons and no duplicate top/bottom controls. Received photos retain their existing popup plus bottom Forward/Delete; outgoing, GIF, Shared Media, group/announcement, private/default, effects, PiP policy, native, schema, wire, and transport remain unchanged. |

---

## 3AC. Direct and group video metadata overlay removal (2026-07-31)

| Plan | Status | Owns / boundary |
|---|---|---|
| [312-direct-group-video-viewer-metadata-removal-tdd-plan.md](312-direct-group-video-viewer-metadata-removal-tdd-plan.md) | **Implemented / host-green (2026-07-31)** — four independent caller RED/GREEN cycles plus restored mutation re-reds; focused suites and Info/action/private/PiP sentinels green; curated `1to1` `+2525` and clean `groups` retry `+3318` with Go tails; analyzer, format, diff, impact query, and incremental Graphify refresh green. No migration, device, native, localization, transport, or gate registration change | MODIFICATION: automatic sender/time/type/size/duration details disappear from ordinary direct/group video pages while captions, typed metadata, eligible existing Info, images/GIFs, actions, playback/resume/PiP, private media, and default/legacy callers retain their current contracts. |

---

## 3AD. Group Keep-in-chat media viewer action layout parity (2026-07-31)

| Plan | Status | Owns / boundary |
|---|---|---|
| [313-group-keep-in-chat-media-viewer-action-layout-tdd-plan.md](313-group-keep-in-chat-media-viewer-action-layout-tdd-plan.md) | **Implemented; host-green (2026-07-31)** — reviewed plan executed tests-first: both causal tests RED/GREEN/mutation-proven, the complete group conversation and group-library/shared/direct preservation suites passed, the curated group gate passed 3,320 Flutter cases plus all Go/relay tails, and analyzer/format/diff/Graphify closure is clean. Full `host-all` remains deferred to the presentation batch/release cadence. | MODIFICATION: incoming ordinary discussion-group static images select the existing 1:1 compact image popup plus bottom Forward/Delete layout, and incoming ordinary discussion-group videos select the existing all-action video overflow. Announcements, outgoing media, GIFs, private/Shared Media viewers, capabilities, dispatch, metadata, PiP, direct chat, native, persistence, and transport remain unchanged. |

---

## 3AE. Group image metadata overlay removal (2026-07-31)

| Plan | Status | Owns / boundary |
|---|---|---|
| [314-group-image-viewer-metadata-removal-tdd-plan.md](314-group-image-viewer-metadata-removal-tdd-plan.md) | **Implemented; host-green (2026-07-31)** — `$tdd-plan` plus reviewed correction executed tests-first: both caller tests RED/GREEN and old-behavior mutation-proven, the announcement boundary mutation re-failed, complete affected suites and direct/default/private sentinels passed, the curated groups gate passed 3,272 Flutter cases plus all Go/relay tails, and analyzer/format/diff/Graphify closure is green. Full `host-all` remains deferred to the presentation batch/release cadence. | MODIFICATION: remove automatic sender/time/type/size/dimensions from ordinary static images opened in discussion-group conversation and Shared Media routes while retaining captions, typed data, eligible incoming Info, compact actions, GIF/video policy, and announcement/default/private behavior. |

---

## 3AF. go-libp2p transport correctness roadmap (2026-08-05)

| Plan | Status | Owns / boundary |
|---|---|---|
| [336-atomic-outgoing-transport-settlement-tdd-plan.md](336-atomic-outgoing-transport-settlement-tdd-plan.md) | **IMPLEMENTED / PLAN-GREEN / ANDROID SQLCIPHER DEVICE-VERIFIED (2026-08-05).** Causal RED/mutation evidence and every named focused/preservation proof are recorded. Curated `1to1` passed all 106 commands, `feed` passed 315/315, four-way `core-host-all` passed 396 paths / 3141 tests plus both contracts, and four-way `feature-host-all` passed 836 paths / 8811 tests with one existing skip. The pinned USB Pixel 6 `21071FDF600CSC` rejected the wrong key and passed exact correct-key close/reopen settlement; analyzer, format, diff, registration, and Graphify closure are green. No migration, Go/relay, iOS, ACK-auth, protocol, queue, or ranking change. | BUG CLOSED: exact text/media attempts stage before network; normal rows and ordinary tombstones settle atomically; live/inbox, receipt, retry, pause, invalidation, and post-send UI/feed writers use guarded capabilities; protected/view-once, verified custody loss, legacy non-replay, and reaction projections remain preserved. |
| [337-authenticated-committed-live-settlement-tdd-plan.md](337-authenticated-committed-live-settlement-tdd-plan.md) | **IMPLEMENTED / AUTHORITY HARNESS CLOSED / CORE-HOST GREEN (2026-08-05).** Semantic native ACK parsing, receiver stage-before-confirm, immediate first-proof settlement, WebSocket de-authority, ordinary/delete/retry bypasses, trusted receipt provenance, and native gate registration are implemented. The four remaining resilience expectations were corrected without production changes: exact files pass `+8`, and `core-host-all` passes 396 Flutter paths / 3144 tests plus both shell contracts. | BUG / R2 CLOSED: only an authenticated target-Peer-ID libp2p stream with explicit `acked == true` may prove ordinary 1:1 text/media or delete delivery. Local WebSocket remains attempted written telemetry; exact transmission/row counters preserve receiver dedup while authenticated direct/relay proof owns final delivery and sticky learning. Mixed-version capability negotiation remains deferred. |
| [338-cross-layer-deadline-contract-tdd-plan.md](338-cross-layer-deadline-contract-tdd-plan.md) | **IMPLEMENTED / R3-OWNED + AFFECTED CORE GATES GREEN / WAVE RECEIPT LATER CLOSED BY PLAN 340 (2026-08-06).** The six-second Dart T0 leg, serialized-millisecond admission, three-second type-scoped committed-ACK reserve, 500 ms bridge watchdog ordering, one absolute Go deadline, ordered half-close, cleanup outcomes, causal tests, and real-gate registration are implemented. Focused R3 tests, 2818-test curated `1to1`, analyzer, hygiene, all eight Go legs, and the post-R2-cleanup 3144-test core gate pass. The historical pre-cleanup Flutter receipt remains `+13571 ~1 -4`; Plan 340 later closed the R4-R5 wave with a green 1326-path `host-all`. | BUG / R3 CLOSED: repairs the 1.5-2 second sender-versus-receiver mismatch and timeout renewal without a combined API, cancellation protocol, coordinator, schema change, presence/inbox change, route retime, or device obligation. |
| [339-attachment-envelope-live-relay-eligibility-tdd-plan.md](339-attachment-envelope-live-relay-eligibility-tdd-plan.md) | **IMPLEMENTED / PLAN-GREEN / R4 PRESERVATION + WAVE RECEIPT CLOSED (2026-08-06).** Four causal REDs, four mutation re-reds, focused `+158`, exact preservation `+11`, curated `1to1` `+2821` plus relay Go contracts, and affected `feature-host-all` `+8832 ~1`; analyzer, formatting, diff hygiene, and refreshed Graphify fingerprint `6b96acb19b528031` are green. Plan 340 subsequently strengthened and re-ran TC-339-05, then the R4-R5 wave `host-all` passed 1326 paths. | FEATURE IMPROVEMENT / R5 CLOSED: uploaded attachment descriptors may use an existing live circuit when the complete encrypted envelope fits the inclusive 96 KiB UTF-8 ceiling; primary media blobs remain separate. The later R4 hedge retains exactly one joined inbox deposit on eligible attachment relay-live failure. |
| [340-presence-independent-durability-hedge-tdd-plan.md](340-presence-independent-durability-hedge-tdd-plan.md) | **IMPLEMENTED / PLAN-GREEN / R4-R5 WAVE HOST-ALL GREEN (2026-08-06).** The T0-anchored typed single-flight hedge is implemented with presence detached from live delivery, authenticated committed proofs as the only cancellation authority, and both terminal non-proof funnels joining the same operation. Focused files passed `+170`; curated `1to1` passed `+2827` plus relay Go contracts; affected `feature-host-all` passed `+8838 ~1` across 837 paths; and the wave `host-all` passed `+13584 ~1` across 1326 paths plus every registered host/Go contract. Mutations, preservation sentinels, analyzer, formatting, diff hygiene, and Graphify fingerprint `c99cfc25938cfbdb` are green. | BUG / R4 CLOSED: eligible live work is independent of relay-presence advice while connected/LAN custody is bounded by one 2.5-second T0 hedge. R1-R5 are complete. FDC-S6 subsequently closed with the conservative keep verdict (retire-chat N / retire-media N / bonsoir always), so there is no WS-removal follow-up or automatic Plan 341. Connection single-flight, relay classification, cross-FFI cancellation, DCUtR, and timing retuning remain telemetry-gated. |
| [341-fresh-group-bootstrap-peer-refresh-lag-tdd-plan.md](341-fresh-group-bootstrap-peer-refresh-lag-tdd-plan.md) | **IMPLEMENTED / DEVICE-ACCEPTED (2026-08-06).** After repairing the MD-004 canonical-runtime lease prerequisite, the same Pixel 6 + Android emulator matrix recorded baseline `create|online-cold` medians of `1523.073/1890.159 ms` and closure medians of `375.439/1083.137 ms` for pre-fanout/caller. All 30 closure samples had exact delivery evidence, the host validator set `productionAuthorized=true`, curated `groups` passed 4,054 Flutter tests plus all Go/relay legs, and analyzer/diff/Graphify closure is green. | BUG CLOSED: only the fresh initial signed `members_added` publish skips foreground peer refresh; authorization, crypto, `topic.Publish`, invite roster authority, established-group refresh/replay, background discovery, timeouts, wire/schema, and non-create paths remain preserved. USB Android + Android emulator proof complete; no iOS used. |

---

## 3AG. Private reliable notification custody (2026-08-06)

| Plan | Status | Owns / boundary |
|---|---|---|
| [342-gap-n01-direct-chat-inbox-custody-outbox-tdd-plan.md](342-gap-n01-direct-chat-inbox-custody-outbox-tdd-plan.md) | **IMPLEMENTED / PLAN-GREEN / ANDROID SQLCIPHER DEVICE-VERIFIED / NOT STANDALONE RELEASE-ELIGIBLE (2026-08-07).** DB v108 immutable exact-envelope custody for fresh ordinary direct text, atomic message/outbox staging, status-independent lifecycle drain, no live-ACK cancellation, canonical composer custody authority, and physical-Android SQLCipher migration/reopen proof are implemented. A reviewed correctness exception fixes the blocking crossed initial/edit collision with authenticated outer/inner edit `eventId`, exact current-edit retry, one-time legacy-edit rebuild, and namespaced direct-inbox dedupe across relay memory/limited/Redis while generic push `message_id` remains the target. Relay-first deployment is required. Final receipts are green: curated `1to1` passed 2,869 Dart tests plus relay/toolchain checks, core passed 401 registered items, feature passed 838 paths, the post-lint exact payload/edit set passed 55 tests, analyzer/diff/discovery and exact relay/native proofs passed, six Pixel device tests passed, and Graphify is current at `fca8cff3a08b2868`; full `host-all` was intentionally not run under per-plan cadence. | MODIFICATION / PARTIAL GAP-N01: the outbox still owns fresh ordinary direct text to the current target peer only. The edit identity/retry correction protects that initial custody and is not edit custody expansion. It ships only with relay-first ordering inside the GAP-N01/N02/N03 dependency wave. Ordinary/private media and disappearing-policy sends, existing attempt custody minting, edit/delete custody, reactions, group truthfulness/order, linked-device fanout, canonical content hash, provider-wake/alert arbitration, full real-relay/device recovery, broader rollout, and consolidated iOS closure remain explicitly deferred. |
| [343-gap-n01-direct-reaction-inbox-custody-outbox-tdd-plan.md](343-gap-n01-direct-reaction-inbox-custody-outbox-tdd-plan.md) | **IMPLEMENTED / PLAN-GREEN / ANDROID SQLCIPHER DEVICE-VERIFIED / NOT STANDALONE RELEASE-ELIGIBLE (2026-08-07).** DB v109 immutable per-event reaction custody, atomic ADD/REMOVE staging, exact typed-outcome drain on the existing lifecycle cadence, generation-safe optimistic UI, the cross-type-safe relay `reaction-event-id:` namespace, and production-v109 transfer are implemented. Focused and preservation suites, relay backend/toolchain tests, curated `1to1` (2,908 Flutter tests), core (403 registered items), feature (839 paths / 8,898 tests at native batch concurrency 2), full analyzer, and diff hygiene are green. Pinned USB Pixel 6 `21071FDF600CSC` passed TC-343-10, five prior-version current-schema proofs, and the PB266 connected release sentinel; Graphify is current at `a6798bd775bd0331`. Full `host-all` was intentionally not run under per-plan cadence; no causal RED or representative mutation re-red receipt is claimed. | MODIFICATION / PARTIAL GAP-N01: bounded direct reaction ADD/REMOVE sender custody only. Current relay overflow still evicts the oldest unacknowledged row and therefore blocks full PRD custody/release; non-destructive capacity and machine-checkable mixed-version activation stay in the dependency wave. No media/edit/delete/group/device-fanout, provider-payload, outcome-ledger, abrupt-kill/real-relay presentation, or iOS closure claim. Relay-first activation and the GAP-N01/N02/N03 wave guard remain. |
| [344-gap-n01-direct-inbox-non-destructive-capacity-mixed-version-activation-tdd-plan.md](344-gap-n01-direct-inbox-non-destructive-capacity-mixed-version-activation-tdd-plan.md) | **IMPLEMENTED / DEFAULT-OFF CODE-CLOSED / HOST + ANDROID-BINDING GREEN / TWO ACCEPTANCE CAVEATS (2026-08-07).** Three additive actions on the frozen protocol now use a Redis-only isolated ACK-or-expiry lane with atomic same-ID legacy shadows, non-destructive capacity, stable cross-lane FIFO, bounded per-relay mixed-version receive/ACK fanout, exact proof-gated v108/v109 ownership, flag-off draining, fixed metrics, rollout docs, and rebuilt Android bindings. Focused Dart passed 494; curated `1to1` passed 2,919 plus registered Go/shell proofs; `feed` passed 323; core passed 401 paths / 3,170 tests; the complete feature family passed 839 paths / 8,904 tests serially; full relay/native/tagged process and framed-handler gates, analyzer, changed-file format, Go/shell/JSON/diff hygiene, and Graphify fingerprint `fa8e958259e251a5` are green. Exact feature concurrency 2 remains a harness caveat: two runs produced 10 then 7 different early-observation failures only in unchanged `group_conversation_wired_test.dart`, while that file passed 231 alone and its three-file neighborhood passed 385 at concurrency 2; no unsafe global timing patch was retained. The current SDK's repository-wide formatter also reports 506 pre-existing unchanged files, while all 40 Plan 344 Dart files are clean. | MODIFICATION / GAP-N01 RELEASE-BOUNDARY SLICE: closes destructive relay capacity and mixed-version code activation for the already-landed direct text/reaction custody owners only. Admission remains off. No historical backfill, production Redis restart/restore or rollback-artifact proof, DB migration, push outbox, payload/event-lane expansion, live deploy, mobile-device leg, or iOS acceptance; full release remains guarded by S2 ops activation plus the GAP-N01/N02/N03 wave. |
| [345-gap-n01-fresh-ordinary-direct-media-voice-inbox-custody-tdd-plan.md](345-gap-n01-fresh-ordinary-direct-media-voice-inbox-custody-tdd-plan.md) | **IMPLEMENTED / PLAN-GREEN / ANDROID SQLCIPHER DEVICE-VERIFIED / NOT STANDALONE RELEASE-ELIGIBLE (2026-08-08).** DB v110 adds one nullable manifest-bound local intent and atomically consumes it into the existing v108 exact-envelope owner with the complete ordinary direct media/voice projection; generic saves, producer/retry crossings, immutable-winner adoption, production bootstrap, and account transfer fail closed or preserve custody. Green receipts are focused 47/47, recovery 441/441, media repository 55/55, media helper 28/28, send-chat 177/177, relay 1/1, core 402 paths / 3,185 tests, serial feature 839 paths / 8,941 passes / 1 declared skip, and curated `1to1` 2,961 Flutter passes plus relay/toolchain checks. Physical Pixel 6 `21071FDF600CSC` (Android 16/API 36) passed TC-345-11 1/1 and all seven TC-345-12 files; direct-inbox was 2/2, and release diagnostics used `connectedReleaseAndroidTest` with `BUILD SUCCESSFUL`. Full analysis reported no issues in 105.6s, all 86 Plan 345 Dart paths were format-clean, and baseline diff hygiene passed. Canonical Graphify refreshed 42 changed / 3,073 unchanged / 0 deleted into 68,941 nodes / 101,467 edges and 3,115 manifest rows; graph SHA-256 `b84726a98bf6c531cc1321cace329457108f6ecf68d8785d765147aee0cd7ad3`, manifest SHA-256 `62e22454df4414fa5e4337c29fd91d427b1ccda1effd9d6815a3cf4d28efeb08`. The exact 95-path changeset passed byte-identity and 20-path Plan-346 exclusion checks with sorted path-set SHA-256 `ed2a743b72f6be1bd8e39f6ffea3ea0fa4d6b136af6429a9dce2fddeed223654`; the normal main Git index remained clean. Implementation and evidence are committed together in this changeset. No retained causal RED or representative mutation re-red is claimed, and full `host-all` was intentionally not run. | MODIFICATION / PARTIAL GAP-N01: fresh ordinary direct media/voice initial encrypted-envelope custody only. No relay-blob durability, private/disappearing media, edit/delete/group/device fanout, historical backfill, production admission, standalone release, or iOS claim. |
| [346-gap-n01-relay-media-blob-non-destructive-ack-or-expiry-durability-tdd-plan.md](346-gap-n01-relay-media-blob-non-destructive-ack-or-expiry-durability-tdd-plan.md) | **IMPLEMENTED / PLAN-GREEN / AGGREGATE `1to1` GREEN / NOT STANDALONE RELEASE-ELIGIBLE (2026-08-08).** The default-off isolated filesystem lane provides exact encrypted-byte identity/proof, sidecar-last commit recovery, non-destructive protected capacity, strict download, exact tombstone ACK/expiry, phase-aware single-relay negotiation, source-pinned ACK, bridge/Dart compatibility, fixed metrics, and non-vacuous process/gate coverage. Focused relay/native/bridge/Dart/process and dedicated gates, full affected relay/native packages, `core-host-all` (402 paths / 3,190 tests), Android bindings, analyzer/hygiene, and the final aggregate `1to1` are green; that rerun passed 2,968 Flutter tests plus every relay notification/toolchain, ACK-custody, media-custody process/bridge/mixed-relay, deterministic gomobile-stamp, and no-adopter tail with wrapper exit 0. Final Graphify closed at 69,148 nodes / 102,080 edges and 3,123 manifest rows, graph SHA-256 `009093649f46fe635e91abde367e4af7252305f1dcd73aa56f2a0810ad5e3b70`. Execution provenance still lacks a clean post-345 baseline and raw first-RED receipt. | MODIFICATION / GAP-N01 ENABLING PRIMITIVE ONLY: admission remains default-off and no conversation caller adopts it. Plan 347 owns durable exact-ciphertext reuse, strict API adoption, persisted relay proof, and v108 blob-commitment/expiry coupling before custody transfer. Actual Android two-peer recovery, production activation/rollout, private/group/historical/device-fanout media, and iOS remain open. Plan 346 does not close the media-blob item or GAP-N01 and is release-ineligible. |
| [347-gap-n01-ordinary-direct-media-blob-client-adoption-tdd-plan.md](347-gap-n01-ordinary-direct-media-blob-client-adoption-tdd-plan.md) | **IMPLEMENTED / DEFAULT-OFF CODE-CLOSED / PLAN-GREEN / NOT STANDALONE RELEASE-ELIGIBLE (2026-08-08).** Plan 347 adopts only Plan 345's durably prepared fresh ordinary-direct composer/voice ciphertext and exact reopen retries. DB v111 preserves the exact encrypted generation before network, strict relay proof is durably bound to the v108 envelope by canonical manifest hash and earliest expiry, and atomic incoming staging owns strict download, byte verification, local custody, then source-pinned ACK or verified source-less LAN expiry. The client selector and both relay admissions remain false in production, so legacy behavior is preserved while disabled. | MODIFICATION / PARTIAL GAP-N01 PREPARED ORDINARY-DIRECT ADOPTION ONLY: marker-free external share, private/disappearing media, edit/delete custody, group media, historical backfill, and linked-device fanout remain legacy or unimplemented. Authenticated production activation, rollout/operations evidence, aggregate quota and UX, the dependency-wave `host-all`, consolidated iOS evidence, broader release evidence, and standalone release eligibility remain open. |
| [348-gap-n01-ordinary-direct-external-share-media-blob-custody-adoption-tdd-plan.md](348-gap-n01-ordinary-direct-external-share-media-blob-custody-adoption-tdd-plan.md) | **IMPLEMENTED / DEFAULT-OFF CODE-CLOSED / PLAN-GREEN / HOST GATES GREEN / NOT STANDALONE RELEASE-ELIGIBLE (2026-08-09).** Marker-free external-OS media sharing to ordinary direct contacts now creates one canonical parent, complete direct attachment set, v110 intent, and v111 exact-ciphertext generation atomically before preview copy or network. The thin adopter reuses Plan 347's lifecycle lock, artifact/network coordinator, foreground lease, retry owner, selector, and v108 completion; exact capability selection, winner adoption/key compensation, post-commit authority truth, truthful multi-target progress, and source-free v111 restart are covered. Focused selector-on closure passed 140 tests; source and 1,437/1,437 completeness checks, curated `1to1`, concurrent feature (841 paths / 8,979 passes / 7 declared skips) and core (404 paths / 3,204 passes), analyzer, exact format, diff hygiene, and the ordering mutation receipt are green. | MODIFICATION / PARTIAL GAP-N01 EXTERNAL-SHARE ADOPTER ONLY: selector and both relay admissions remain default-off. No DB migration, share queue, new retry/transport owner, plaintext reconstruction/scavenger, UI redesign, Go/native/relay change, Android/iOS campaign, production activation, or release claim. Private/disappearing, edit/delete, group/announcement, historical/no-intent, and device-fanout lanes, wave-level full `host-all`, consolidated iOS, activation/operations/quota UX, GAP-N01 closure, and standalone release eligibility remain open. |
| [349-gap-n01-ordinary-direct-text-mutation-custody-tdd-plan.md](349-gap-n01-ordinary-direct-text-mutation-custody-tdd-plan.md) | **IMPLEMENTED / DEFAULT-OFF CODE-CLOSED / PLAN-GREEN / HOST GATES GREEN / NOT STANDALONE RELEASE-ELIGIBLE (2026-08-09).** Newly authored ordinary direct-text EDIT and delete-for-everyone now reuse the unchanged raw-event-ID v109 owner with atomic parent+event staging/completion, one fair reaction/mutation drain, strict `direct_mutation_v109` relay/native selection, deletion identity parity, repository-current edit ordering, failed/unacked bypass guards, transactional ordinary-text receiver convergence, and additive event-aware receipts. Completeness 1,437/1,437, core 404 paths / 3,207 tests, feature 841 paths / 8,992 tests / 7 skips, final `1to1` 3,015 Flutter tests / 6 skips plus every Go tail, full relay/node/bridge/mixed packages, Android bindings, analyzer, format, hygiene and Graphify are green. | MODIFICATION / PARTIAL GAP-N01 DIRECT-MUTATION SLICE ONLY: no DB v112/schema/kind column, local subtype keys, second drain/scheduler, provenance marker, historical scan, media/private/disappearing/group adoption, activation, mobile/iOS campaign, per-plan full `host-all`, or release claim. Ownerless pre-349 edits retain exact legacy fallback; production admission and broader N01/wave closure remain deferred. |
| [350-gap-n01-ordinary-direct-contact-internal-media-forward-blob-custody-adoption-tdd-plan.md](350-gap-n01-ordinary-direct-contact-internal-media-forward-blob-custody-adoption-tdd-plan.md) | **IMPLEMENTED / DEFAULT-OFF CODE-CLOSED / PLAN-GREEN / HOST GATES GREEN / NOT STANDALONE RELEASE-ELIGIBLE (2026-08-09).** The already-authorized received-direct, direct-library, and discussion/announcement-origin ordinary media forwards whose destination is a direct contact now publish one forwarded parent, complete direct attachment set, v110 intent, and v111 exact-ciphertext generation atomically before LAN or relay. One nullable ephemeral `authorizedForwardDedupKey`, read only at a reviewed entry leg after its source gate, threads that authorization through the shared contact sender, Plan 348's fresh coordinator/repository/DB owner, the exact share-level authority re-read, production bootstrap, and the shared real-DB fixture; the fresh predicate now accepts exactly two canonical parent shapes and refuses every crossed one atomically. Progress-observer throws are contained so a durable v111 target can never become picker-owned failure. Focused selector-on closure passed 153 tests; preservation sentinels (52), `TC-347-08c`, the strict-owner shell, completeness 1,437/1,437, host `1to1`, serial feature (841 paths / 9,000 tests / 7 skips), the `core-host-all` Flutter batch (404 paths / 3,209 tests), analyzer, exact format, diff hygiene, Graphify, and five mutation re-reds are green. | MODIFICATION / PARTIAL GAP-N01 INTERNAL-FORWARD ADOPTER ONLY: no migration, persisted forward kind, new owner/queue/scheduler/flag, relay/native/bridge change, source-lock/snapshot-owner expansion, duplicate persistence harness, UI redesign, device/iOS campaign, per-plan full `host-all`, activation, GAP-N01 closure, or release claim. Point-in-time direct-library TOCTOU and exceptional snapshot cleanup retain their Plan 249 owner. Group destinations, private/view-once/disappearing media, caption edit/delete-for-everyone, historical promotion, device fanout, activation, aggregate Android/iOS evidence, the dependency-wave full `host-all`, GAP-N01 closure, and standalone release eligibility remain open. Closure also repaired a container-only harness gap unrelated to the adopter: `core-host-all`'s two Android manifest contracts shell out to path-qualified `./android/gradlew`, which no host shim satisfies, so the gate now routes them through the host bridge and the whole scope passes from the container. |
| [351-gap-n01-ordinary-direct-media-delete-for-everyone-custody-tdd-plan.md](351-gap-n01-ordinary-direct-media-delete-for-everyone-custody-tdd-plan.md) | **IMPLEMENTED / DEFAULT-OFF CODE-CLOSED / PLAN-GREEN / HOST GATES GREEN / NOT STANDALONE RELEASE-ELIGIBLE (2026-08-09).** Newly authored strict ordinary direct-media delete-for-everyone now commits the exact v111 state transition, the visible tombstone, and the raw-event v109 obligation in one transaction under the message-wide media lifecycle lock, selecting its lane from persisted attachment/v111/v108 authority instead of the parent's media snapshot. A live v108 and its bound generation survive the tombstone and still complete once; refusals leave parent, v111, v108, attachments and network untouched. Deletion identity is minted only for a v109-owning lane, cleanup now follows stage authority even when the node is stopped, and protected acceptance settles a media-staged deletion whether or not cleanup already removed its attachments (media EDIT stays refused). On receive, one transactional current-deletion owner tombstones absent/text/strict-media targets, retires the direct display marker in the same commit, and returns durable supersession for exact replay or an already-newer tombstone; strict initial staging returns a typed `supersededByDeletion`, publication returns a non-throwing `durablySuperseded`, and both still emit exactly one initial receipt. Local-path commit, generic incoming save and startup retry refuse a tombstone, a losing promotion deletes only its own canonical plaintext, and v111 still ACKs or expires independently. Four causal REDs, focused GREEN, one stage-order mutation re-red, completeness 1,437/1,437, host `1to1` (120 paths), `core-host-all` (404 paths / 3,214 tests + both manifest contracts), `feature-host-all` (841 paths / 9,006 tests / 7 skips), analyzer, exact format, diff hygiene and one Graphify refresh are green. | MODIFICATION / PARTIAL GAP-N01 MEDIA-DELETE SLICE ONLY: no schema, wire, relay/native, new owner/drain/scheduler/flag, cleanup journal/scanner, caption edit, private/group/history promotion/fanout/activation, device/iOS, per-plan full `host-all`, GAP-N01 closure, or release claim. The strict blob fingerprint is written only on the incoming path, so rows 1-3 select on v111/v108 authority and a strict parent whose v111 has fully drained after v108 completion stays on the unchanged legacy row-5 lane. Crash-complete local artifact erasure remains explicit storage-hygiene follow-up. |
| [352-gap-n01-preserve-direct-media-post-drain-delete-custody-tdd-plan.md](352-gap-n01-preserve-direct-media-post-drain-delete-custody-tdd-plan.md) | **IMPLEMENTED / DEFAULT-OFF CODE-CLOSED / PLAN-GREEN / HOST GATES GREEN / NOT STANDALONE RELEASE-ELIGIBLE (2026-08-09).** Exact accepted v108 completion now conditionally persists the already-proven per-attachment strict lineage before it retires the v108 owner and moves v111 to cleanup, so physical cleanup can no longer make a normally delivered ordinary direct-media parent fall back to the legacy node-dependent delete lane. Authorship is prevalidated over the complete physical projection, CAS-stamps only null values, accepts exact pre-stamped input, re-reads the whole projection, and shares the one existing `dbWriteTransaction` with the message projection, the exact v108 delete and every v111 transition; a crossed, foreign, missing, extra or drifted row returns `stale` before the first write. A terminal, superseded or no-longer-owned parent keeps its current completion outcome, so Plan 351 deletion-first convergence is untouched. Three causal REDs, two mutation re-reds (delivered fast path; prevalidation-before-first-write), focused GREEN 74/74, repository accepted-difference sentinel green UNMODIFIED plus its full file 66/66, host `1to1` 120 paths / 2,286 tests / 2 skips, `core-host-all` 404 paths / 3,217 tests, analyzer, exact format, diff hygiene and one Graphify refresh are green. | BUG / GAP-N01 POST-DRAIN DELETE CUSTODY: one core production helper (`direct_inbox_custody_outbox_db_helpers.dart`) reusing the existing v111 fingerprint column, digest function and unchanged Plan 351 classifier. DB stays v111. No schema, API, feature production, backfill, scanner, new owner, relay/native, completeness, feature/full host sweep, device/iOS, activation, GAP-N01 closure, or release claim. Pre-352 generations whose v108 and v111 already drained stay legacy; media caption EDIT and the other GAP-N01 lanes remain separate. Execution recorded three plan corrections: the repository sentinel needed no narrowing (that scenario stages an unbound v108, so no lineage is authored), the planned CAS matrix needed a trailing-contradiction row to make prevalidation provable, and the shared strict seed fixture needed unique v108 incarnations. |
| [353-gap-n01-ordinary-direct-media-caption-only-edit-custody-tdd-plan.md](353-gap-n01-ordinary-direct-media-caption-only-edit-custody-tdd-plan.md) | **IMPLEMENTED / DEFAULT-OFF CODE-CLOSED / PLAN-GREEN / HOST GATES GREEN / NOT STANDALONE RELEASE-ELIGIBLE (2026-08-09).** A newly authored caption-only EDIT of a strict ordinary direct-media parent now takes the same exact raw-event v109 custody as text: the persisted parent, its deterministic `created_at ASC, id ASC` attachment projection and the independent v108/v111 lineage decide the lane, so the caller's UI media snapshot can no longer add, remove, reorder or re-key a published attachment, and identity drift refuses before key lookup, encryption or network. The caption, the exact edit-attempt transport fields, any provable null-to-exact lineage stamp and one v109 row commit in a single transaction under the media lifecycle lock while every v108/v111/blob/attachment byte stays identical; raw keys are hydrated outside SQLite and compared there, since two attachments can share a storage reference while their keys differ. Exact v108 completion now also recognises the canonical delivered successor a direct/LAN receipt leaves behind, closing the common path where cleanup turned a normally delivered generation legacy. Exact v109 completion is attachment-independent for both mutation kinds. On receive, an event-bearing proof-less media EDIT is routed before every generic branch: a tombstone supersedes first with its exact receipt, an absent or hidden nondeleted original writes nothing and stays retryable, a live all-null/no-v111 legacy parent keeps its unchanged generic apply, and a strict parent gets text plus editedAt only. Six causal REDs, focused GREEN 11/11, nine preservation sentinels, two mutation re-reds, host `1to1` 120 paths, `core-host-all` 404 paths / 3,221 tests, `feature-host-all` 841 paths / 9,014 tests / 7 skips, analyzer, exact format, diff hygiene and one Graphify refresh are green. | MODIFICATION / GAP-N01 CAPTION-ONLY SLICE: the reviewed eight production files only. DB stays v111 and the physical v109/v111 layouts stay byte-compatible. No schema, wire proof, new owner/kind/drain/scheduler/flag, blob reconstruction/upload, media replacement, private/view-once/disappearing, group/announcement, historical promotion, linked-device fanout, relay/native, device/iOS, per-plan full `host-all`, activation, GAP-N01 closure, or release claim. Execution recorded three corrections: a hydrated raw-key barrier the plan did not name (empty projections deliberately defer to the transactional owner so a missing original stays retryable), three path-string guards re-pinned rather than weakened (two DTR-18 digests plus the delivered-status census, 5 -> 6 for a status that is never persisted), and a test-only barrier change because a same-connection insert inside the transaction deadlocks. Ownerless pre-353 media edits remain a documented compatibility ambiguity with no provenance marker. |
| [354-gap-n01-protected-view-once-direct-media-initial-inbox-blob-custody-adoption-tdd-plan.md](354-gap-n01-protected-view-once-direct-media-initial-inbox-blob-custody-adoption-tdd-plan.md) | **IMPLEMENTED / DEFAULT-OFF CODE-CLOSED / PLAN-GREEN / HOST GATES GREEN / NOT STANDALONE RELEASE-ELIGIBLE (2026-08-10).** A newly authored v1 Protected image/video or View-Once image initial now publishes one exact `outgoing_prepared` v111 generation over its durable private parent plus its single convention-owned pending attachment — no v110 intent is minted or required — before the first LAN callback and before the sole strict relay upload owner. The strict result is canonicalized to the private path with no network or re-encryption, keeps its key, nonce, hashes and `blobCustody` byte-for-byte, and rides the existing private completion coordinator including its `deferredActiveLease` and `transportOnlyTerminalCustody` authorizations. Barrier B then commits the exact envelope, one `direct_text_v108` row and the complete v111 binding in ONE transaction before chat egress, returning the exact committed/adopted authority with no post-commit read; exact-winner adoption precedes the capacity check, the proof must expire strictly after `now+3000ms`, and any later refusal rolls the whole transaction back. Both retry lanes reopen byte-identical ciphertext with zero legacy upload, re-encryption, key rotation or envelope invalidation; private local hide is lock-owned; terminal cleanup retains the complete projection while a live prepared/stored v111 exists. On receive, strict parsing admits exactly these initial shapes and re-enforces the producer matrix independently of the wire: one transaction publishes the private parent lifecycle, its secure-key-backed pending attachment and one `incoming_committed` row before any marker, publication, stream, notification or receipt, while a durable same-author terminal parent takes a dedicated zero-effect `durablySuperseded` disposition (matching committed/ACK-pending survivor or reduced no-v111 parent equivalence emits exactly one receipt; absent parent or crossed survivor emits none) that the listener never routes through the global display retry. Explicit-user private download enters the same sole strict owner only after the exact transfer token, the DB `downloading` claim and path authorization, uses one deterministic convention-owned ciphertext/decrypt staging pair that private cleanup and restart recovery enumerate exactly, and the production drain retains private committed rows without network. TC-354-01a/02a/04a/04e/05e GREEN, 15 preservation sentinels GREEN, strict-owner source contract PASS, host `1to1` 121 paths, `core-host-all` 404 paths / 3,225 tests, `feature-host-all` 841 paths / 9,015 tests / 7 skips, analyzer, format, diff hygiene and one Graphify refresh all green; the wired transport census and the DTR-18 digests were re-pinned, never weakened. | **MODIFICATION / PARTIAL GAP-N01 PRIVATE-INITIAL SLICE:** the reviewed Dart seams only. DB stays v111 and the physical v108/v111 layouts stay byte-compatible. No schema, v110 expansion, new outbox/queue/drain/scheduler/lock/flag, relay/native/wire change, second network caller, cleanup scanner, device/iOS or per-plan full `host-all`. Accepted evidence difference: five of the plan's twenty-one named tests were authored (the two durable barriers, the atomic receive plus full terminal replay matrix, the listener disposition and the bootstrap retention/claim ordering); the remaining behavioral cases and all four planned mutation re-reds were not, so those boundaries rest on the fifteen exact sentinels plus the affected-family sweep. Selector-on new-sender to pre-354 receiver compatibility, enabled-to-disabled rollback with extant private v111, disappearing, private edit/delete-event custody, groups/announcements, historical promotion, linked-device fanout, activation/cohort policy, aggregate platform evidence, GAP-N01 closure and release eligibility remain open. |

---

## 4. Dead Code

| Report | Focus |
|--------|-------|
| [311-remove-qa-group-type-tdd-plan.md](311-remove-qa-group-type-tdd-plan.md) | **Execution-ready (2026-07-31)** — `$tdd-plan`; retires the unreachable `GroupType.qa` ("Q&A") third group type: no shipped build can create one (`orbit_screen.dart:946,951` offers only chat/announce) and its sole ingress is a wire string no build emits. Removes 8 `lib/` symbol sites, 21 references across 16 test files, the localized badge in en/ar/de plus the generated API, and `'qa'` from the Android push handler's accepted set — permanently closing the latent Android/iOS notification divergence plan 309 deliberately left out of scope. **The crux is the read path:** `GroupType.fromValue` throws on unknown values and has three unguarded persisted/wire call sites, so deletion without a fallback would turn a dead type into a crash-on-read; the plan adds `tryFromValue` with explicit per-call-site fallbacks rather than a blanket default (which would silently reclassify announcement groups). 13-row contract, host-only, **no migration** (both CHECK constraints stay permissive and are test-locked), one manual `GROUP_TESTS` registration, compile-RED via enum exhaustiveness in four named files |
| [06-dead-code-lib.md](06-dead-code-lib.md) | Conservative dead-code review — only a small subset looks safely removable now |
| [07-dead-code-deps-config.md](07-dead-code-deps-config.md) | Dependency/config cleanup — confirmed unused package plus low-value optional script cleanup |
| [dead-code-and-technical-debt-removal-roadmap.md](dead-code-and-technical-debt-removal-roadmap.md) | Canonical DTR sequencing and acceptance ledger. Waves 4A, 4B, and 4C are accepted. DTR-16 and DTR-17 are Plan-green/Wave-accepted; DTR-18 is terminally complete and Wave-accepted under `DTR18-AUTH-01` plus closed `DTR18-AUTH-02`. The terminal architecture result is 165 dependency / 0 placement / 0 issues, an overall DTR-18 delta of dependency -17 and placement -24. Wave 4C `performance-host` then full `host-all` are green. Final release `host-all` and `performance-host` remain pending. [Stable Wave 4A evidence](evidence/dtr-wave4a/README.md). [Stable Wave 4B evidence](evidence/dtr-wave4b/README.md). [Stable Wave 4C evidence](evidence/dtr-wave4c/README.md). |
| [272-runtime-root-inventory-advisory-reachability-guard-tdd-plan.md](272-runtime-root-inventory-advisory-reachability-guard-tdd-plan.md) | **Plan-green DTR-01 (2026-07-25)** — added the read-only two-axis runtime-root inventory and candidate ratchet distinguishing callbacks, manual/native/generated/tooling/compatibility/resource roots from advisory candidates; no deletion, app behavior, schema, native, or device change; Wave 0 `host-all` is accepted |
| [273-analyzer-baseline-retirement-production-suppression-ratchet-tdd-plan.md](273-analyzer-baseline-retirement-production-suppression-ratchet-tdd-plan.md) | **Plan-green DTR-02 (2026-07-25)** — replaced the stale 1,609-finding analyzer allowance with strict analysis and an exact production `unused_*` suppression ratchet, with analyzer-options bypass guards and Sims registration. It originally landed four identities; after authorized Plan 276 removed the sole handwritten relay-helper identity, the current ratchet is exactly the three generated l10n imports. Wave 0 `host-all` is accepted. |
| [274-dead-core-api-model-removal-tdd-plan.md](274-dead-core-api-model-removal-tdd-plan.md) | **Wave-accepted DTR-03 (2026-07-26; plan green 2026-07-25)** — retired the duplicate core `ChatMessage`, hollow core contact listener plus SUT-only suite, and only the deprecated topic-only wrapper/legacy test fragments (339 dead code/test lines), while preserving live feature models/listener, DTR-10 join/hydrate islands, and the full-config Dart/native/Go group-join path; causal RED/GREEN and mutation re-red, `runtime-roots`, `1to1`, `groups`, `core-host-all`, strict analysis, completeness, Graphify, protected-scope, and diff gates are green; the complete Wave 1 `host-all` retry passed |
| [275-unused-visible-feature-leaves-removal-tdd-plan.md](275-unused-visible-feature-leaves-removal-tdd-plan.md) | **Wave-accepted DTR-04 (2026-07-26; plan green 2026-07-25)** — retired eight visible/UI leaves and four obsolete SUT-only tests while preserving their live replacements; the group cursor was separately retired under DTR-10 Plan 283, while the post follow-on helper is protected by DTR-06 accepted retention (`DTR06-AUTH-01`); the complete Wave 1 `host-all` retry passed |
| [276-dormant-chat-relay-probe-helper-removal-tdd-plan.md](276-dormant-chat-relay-probe-helper-removal-tdd-plan.md) | **Wave-accepted DTR-05 / implementation-complete (2026-07-26)** — atomically removed the zero-call-site chat-only `_tryRelayProbeSend` helper, its `unused_element` suppression, the ratchet's only handwritten entry, and stale serial-probe prose. Causal RED/GREEN and mutation re-reds, exact host/Go preservation, `1to1`, physical Pixel 6 `transport`, the 821-path `feature-host-all` sweep, strict analysis, discovery, Graphify, and scope gates are green. `DTR05-AUTH-01` and its leaf-only two-peer waiver remain recorded; `relayProbeSendAttempts`, the live introduction twin, and the shared `probeRelay`/`relay:probe`/Go `RelayProbe` protocol are unchanged. The complete Wave 1 `host-all` retry passed. |
| [277-old-join-group-removal-tdd-plan.md](277-old-join-group-removal-tdd-plan.md) | **Plan-green DTR-10 / implementation-complete under `DTR10-AUTH-01` (2026-07-26)** — removed the test-only `joinGroup` island, its orphaned `commitFreshDirectJoin` repository seam/test/fake fragments, SUT-only suite, and exact bookkeeping while preserving live invite admission, lifecycle rejoin, accepted-reentry serialization, full-config Dart/native/Go join, stored keys, and epochs |
| [278-hydrate-groups-from-peers-disposition-tdd-plan.md](278-hydrate-groups-from-peers-disposition-tdd-plan.md) | **Plan-green / implementation-complete DTR-10 under `DTR10-AUTH-02` (2026-07-26)** — removed the duplicate test-only wrapper after confirming cold-start, resume, and retrier recovery already perform richer production rejoin/inbox recovery. It could not acquire missing fresh-device group/key state, and any such future feature requires a separately authorized plan |
| [279-dormant-group-leave-path-removal-tdd-plan.md](279-dormant-group-leave-path-removal-tdd-plan.md) | **Plan-green / closed DTR-10 / `DTR10-AUTH-03` (2026-07-26)** — retired the dormant `leaveGroup` source/test and default-false branch, rehomed the byte-identical last-admin message, and migrated valuable host/device callers onto the durable exit path. Causal removal/mutation proof, focused host preservation, `runtime-roots`, `groups`, all 815 `feature-host-all` selectors, strict analysis, and the pinned three-Android H-01/GM-015 closure are green while v103/v104, diagnostics, native leave, and strict dissolved-local cleanup remain preserved |
| [280-superseded-leave-local-history-coordinator-removal-tdd-plan.md](280-superseded-leave-local-history-coordinator-removal-tdd-plan.md) | **Plan-green / implementation-complete DTR-10 under `DTR10-AUTH-04` (2026-07-26)** — migrated 11 live-surface proofs onto the production-shaped durable exit coordinator/runner and removed only the superseded leave/local-history coordinator, its legacy fixture, and SUT-only blocks while preserving v103/v104, native leave, diagnostics, and UI behavior |
| [281-legacy-group-key-rotation-path-removal-tdd-plan.md](281-legacy-group-key-rotation-path-removal-tdd-plan.md) | **Plan-green / implementation-complete DTR-10 under `DTR10-AUTH-05` (2026-07-27)** — retired only the obsolete Dart rotation use case, refusal helper, raw command mapping, SUT-only tests/fragments, and bookkeeping; preserved live generate/distribute/promote/update rotation, native/Go compatibility, and every persisted key generation. Causal RED/mutation re-red, exact preservation, bindings/Go, runtime roots, completeness, stabilized groups, isolated 815-path feature, strict analysis, Graphify, and protected-scope gates are green |
| [282-duplicate-dart-group-message-payload-removal-tdd-plan.md](282-duplicate-dart-group-message-payload-removal-tdd-plan.md) | **Implementation-complete DTR-10 / `DTR10-AUTH-06` (2026-07-27)** — **Plan-green / closed.** Removed only the unused Dart `GroupMessagePayload`, its SUT-only test, and bookkeeping under the recorded copy/paste interpretation while preserving the live Go v3 envelope/parser/publish/receive protocol. Focused Dart/Go preservation, completeness, `groups`, all 815 `feature-host-all` paths, strict analysis, and scoped runtime-root closure are green |
| [283-unused-group-inbox-cursor-model-removal-tdd-plan.md](283-unused-group-inbox-cursor-model-removal-tdd-plan.md) | **Plan-green / closed DTR-10 / `DTR10-AUTH-07` (2026-07-27)** — retired only the 31-line orphan `GroupInboxCursor` model and exact runtime-root declaration while preserving migration 066, cursor/receipt tables, DB helpers, repository/drain transactions, production composition, and the account-migration metadata sibling. Causal/mutation proof, persistence sentinels, isolated `runtime-roots`, `groups`, all 815 `feature-host-all` paths, final strict analysis with no issues, and diff hygiene are green |
| [284-legacy-group-secret-security-scrub-retention-evidence-tdd-plan.md](284-legacy-group-secret-security-scrub-retention-evidence-tdd-plan.md) | **Acceptance-verified retained DTR-10 / `DTR10-AUTH-08` (2026-07-27)** — 17 focused tests, source/evidence audits, the 3,235-test `groups` gate with Go tails, strict analysis, and protected-scope verification are green without a code delta. Keep the foreground startup scrub; its fixed unowned marker, post-startup Move Account database replacement, background-isolate bypass, and absent fleet completion evidence still prohibit an operational-sufficiency or removal claim |
| [286-group-list-ui-island-retirement-tdd-plan.md](286-group-list-ui-island-retirement-tdd-plan.md) | **Wave-accepted DTR-07 (2026-07-27; Plan-green 2026-07-26) under `DTR07-AUTH-01`** — retired the unreachable 2,035-line Group-list island, its five SUT-only suites, three manifest declarations, one group-gate entry, and exactly eight orphan localization keys per locale. Orbit's separate `allChats` replacement, live group navigation, ICU conversation carriers, and every backend/native/schema boundary remain. The `/tdd-review` fixes hardened inventory isolation, current-doc/downstream-plan proof, localization regeneration, and staged/unstaged deletion bounds. Causal RED/GREEN, mutation re-red, focused preservation, `runtime-roots`, `groups`, `feed`, the stabilized 816-path feature-family replay (8,476 pass, 1 skip), completeness, diff hygiene, and Graphify refresh are recorded in the plan. Its unrelated concurrent Plan-285 analyzer warning is historical; Plan 285 later closed cleanly. The Wave 2 aggregate retry passed Flutter `+12,827 ~1`, all eight Go tails, the final scope marker, and exit 0. Final-rollout `host-all` remains separate. |
| [287-remaining-test-only-leaves-disposition-tdd-plan.md](287-remaining-test-only-leaves-disposition-tdd-plan.md) | **Wave-accepted DTR-11 under `DTR11-AUTH-01` (2026-07-27)** — re-derived and closed the historical residual as exactly 11 test-only app leaves / 1,065 LOC: ten sources and ten SUT-only suites retired, one 94-line telemetry calculator relocated unchanged to tooling, live Feed/Orbit/QR/LetterCard replacements preserved, 11 manifest rows removed, only the dead backlog summary/two keys and inert S15 diagnostic retired, and recursive `test/unit/**` registered under `core-host-all`. `_RaceResult.relayProbeEligible` and every live behavior/native/Go/wire/storage boundary remain intact. Causal/focused/curated/family/analyzer/completeness/Graphify gates passed; the separate Wave-3 aggregate then passed 1,250 Flutter paths (`+12,755 ~1`), all eight Go tails, and exit 0. [Stable evidence](evidence/dtr-wave3/README.md). |
| [288-architecture-layer-boundary-guard-tdd-plan.md](288-architecture-layer-boundary-guard-tdd-plan.md) | **Wave-accepted DTR-12 (2026-07-28; Plan-green 2026-07-27)** — pins the corrected current baseline of 182 forbidden layer dependencies plus 24 concrete feature-domain repository placements with an AST/Git-backed read-only checker, strict metadata-complete manifest, first-class `architecture-boundaries` lane, and required Sims-major registration. Review and adversarial implementation audit hardened normalized identity, class-alias/typedef ancestry, duplicate JSON keys, named parts, fixture-capable CLI proof, and DTR-01 JSON-index preservation. No exception was rebaselined: Wave 4A accepted the unchanged 182 forbidden dependencies, 24 placement pins, and 92 debug exceptions after integrated core/analyzer/Graphify receipts and the full aggregate passed. DTR-13 owned this wave's composition-root boundary; DTR-18 owns any future broader reduction. [Stable evidence](evidence/dtr-wave4a/README.md). |
| [289-debug-e2e-composition-root-tdd-plan.md](289-debug-e2e-composition-root-tdd-plan.md) | **Plan-green / Wave-accepted DTR-13 under closed `DTR13-AUTH-01` (2026-07-28)** — moved only concrete harness construction/scheduling from `lib/main.dart` into `lib/debug/debug_e2e_composition_root.dart`; default composition constructs none. All profiles, manual smoke roots, the retained smoke runner, defines, commands, lifecycle/async ordering, and headless/manual/simulation owners remain. Under `W4A-BLOCKERS-AUTH-01`, independently reverified final reports pass wake 2/2, intro 2/2, group/announcement 4/4, and iOS APNs/NSE/offline-tap 7/7 against coherent Sims digest `73f4339412d85b7846f45894a122aa174f87141771fa121666a3ff06a3f0e825`. `core-host-all` passed 362 items / 2,825 tests, strict analysis passed with zero diagnostics, Graphify refreshed, and the 1,265-item aggregate exited 0. [Stable evidence](evidence/dtr-wave4a/README.md). |
| [290-android-wake-token-cold-start-reliability-tdd-plan.md](290-android-wake-token-cold-start-reliability-tdd-plan.md) | **Plan-green / Wave-accepted; implementation-complete (2026-07-28)** — token-bearing stores await true node readiness at the shared `P2PServiceImpl` seam, use one original deadline and forward only remaining time, preserve immediate tokenless dispatch, and fail safely on disposal/stream close. The final coherent physical+emulator wake report passed 2/2: report SHA `0a286828746388173e6c489b864cad57b1defda10f17fffb889b7577556827e2`; proof SHA `f5067faf9b1bdca64480fb39fca44fd4c295f8ff87e848aab55998dcf0771bc5`. |
| [291-intro-notification-campaign-budget-tdd-plan.md](291-intro-notification-campaign-budget-tdd-plan.md) | **Plan-green / Wave-accepted; harness implementation-complete (2026-07-28)** — adapter and child share one absolute 24-minute deadline with phase ceilings, exact acceptance discrimination, bounded SIGTERM/SIGKILL ownership, and idempotent late cleanup. The retained historical live failure exposed the separate exact-anchor product defect owned and corrected by Plan 252; it remains causal history rather than being restated as harness scope. The final coherent three-Android intro report passed 2/2: report SHA `e75c34a00b01c967b4d0ef825ec6a71437a3696b7972e6613080da410c5b1cf6`; proof SHA `1cd87227a523523c9a490787b0a8ab0cad98fb1e942b9bccd2904113ebafcc5b`. |
| [292-group-reaction-notification-fixture-driver-stability-tdd-plan.md](292-group-reaction-notification-fixture-driver-stability-tdd-plan.md) | **Plan-green / Wave-accepted; implementation-complete (2026-07-28)** — exact `orbit_create_group_fab` semantics, one non-force-stop recovery, current-process sendability, pending-invite discrimination, split cleanup, and bounded read-only log retries remain covered by the focused suite. The final coherent physical+emulator group/announcement report passed 4/4: report SHA `c0d14c28193c5f750320c8addf2b22f415ca287dc4e5ca836b37f0f32f9b0672`; proof SHA `76299c7874dd9684df81e8beba88fa3fe14cff2388f6b79c9a6e883ad51b52b1`. |
| [293-main-bootstrap-application-interfaces-tdd-plan.md](293-main-bootstrap-application-interfaces-tdd-plan.md) | **Plan-green / Wave-accepted DTR-14 under `DTR14-AUTH-01` (2026-07-28)** — reduced `main.dart` from 7,262 to 14 lines behind stable `ApplicationBootstrap` / `PreparedApplication` / `ApplicationHost` interfaces, separated the 5,225-line concrete graph from the 2,295-line application root, and preserved startup, lifecycle, push, Move Account, DTR-13, profiles, public facade, and headless FCM behavior. Focused RED/GREEN and five mutation re-reds, exact-owner locks, curated lanes, `move-feature`, core/feature families, policy, strict analysis, and Graphify are green. The Wave 4B aggregate `host-all` and required same-tree `performance-host` replay passed. [Stable evidence](evidence/dtr-wave4b/README.md). |
| [294-shared-conversation-controller-extraction-tdd-plan.md](294-shared-conversation-controller-extraction-tdd-plan.md) | **Plan-green / Wave-accepted / implementation-complete DTR-15 under `DTR15-AUTH-01` (2026-07-28)** — extracted composer snapshot, upload activity, recorder session, and reaction projection into four plain compositional controllers while both private Wired States remain independent roots and public widgets/facades remain frozen. Captured group send-lane dependencies and a monotonic binding generation preserve ordinary unmount completion while fencing retarget/ABA work. Causal/mutation proof, `1to1`, `groups`, 810-path `feature-host-all`, 21-path `performance-host`, policy, strict analysis, diff hygiene, and Graphify are green. The Wave 4B aggregate `host-all` and required same-tree `performance-host` replay passed. [Stable evidence](evidence/dtr-wave4b/README.md). |
| [296-group-message-listener-decomposition-tdd-plan.md](296-group-message-listener-decomposition-tdd-plan.md) | **Plan-green / Wave-accepted DTR-16 under `DTR16-AUTH-01` (2026-07-28)** — the stable public/subclassable `GroupMessageListener` facade delegates signed system transitions, durable membership buffering, incoming reactions, and media receive-task ownership to four constructor-once private collaborators. Causal and mutation proof, the expanded 28-test focused set, 3,265-test `groups` lane with all Go/relay tails, 811-path `feature-host-all` lane with 8,441 pass / 1 declared skip, policy gates, whole-tree analysis, hygiene, and Graphify are green. The later Wave 4C `performance-host` and full `host-all` aggregate passed; final release closure remains separate. [Stable evidence](evidence/dtr-wave4c/README.md). |
| [295-p2p-service-component-decomposition-tdd-plan.md](295-p2p-service-component-decomposition-tdd-plan.md) | **Plan-green / Wave-accepted DTR-17 under `DTR17-AUTH-01` (2026-07-28)** — the complete `P2PServiceImpl` facade composes durable inbox custody/replay and peer/LAN policy through two private typed port-only coordinators. Causal RED/GREEN, strengthened ownership/delegation/callback mutations, exact inbox/callback/peer/LAN/lifecycle preservation, 21-file performance, pinned physical-Android transport, policy, strict analysis, hygiene, and current Graphify proof are green. Final per-plan receipts are `1to1` 2,464 plus relay Go tails and `core-host-all` 2,840 across 365 paths plus its Android manifest tail. The retrier blocker was repaired test-only with explicit completion synchronization; the concurrent DTR-16 suppression was replaced by a live emission-only lifecycle fence and re-proved by `groups` 3,265 plus Go/relay tails and 811-path `feature-host-all` at 8,441 pass / 1 declared skip. The later Wave 4C `performance-host` and full `host-all` aggregate passed; final release closure remains separate. [Stable evidence](evidence/dtr-wave4c/README.md). |
| [297-application-resume-posts-adapter-layer-relocation-tdd-plan.md](297-application-resume-posts-adapter-layer-relocation-tdd-plan.md) | **Plan-green first increment / Wave-accepted DTR-18 under `DTR18-AUTH-01` (2026-07-28)** — moved byte-identical resume orchestration from core to the DTR-14 app owner and the two Posts Phase-3 persistence adapters from domain to data, with no shim or behavior/schema/native change. Exact causal/mutation/preservation proof, pinned Android Posts integration, `1to1`, `groups`, `move-feature`, 366-path `core-host-all` at 2,843 pass, 811-path `feature-host-all` at 8,441 pass / 1 declared skip, policy, zero-issue analysis, hygiene, and refreshed Graphify evidence are green. The architecture baseline decreased by exactly 17 dependency and two placement exceptions from the historical 182/24 baseline to trustworthy 165/22 with zero issues; all three destinations are runtime-root `main-reachable` without a new declaration. Plans 298–300 later closed the remaining placements and the separate Wave 4C aggregate passed. Plan 297 remains DTR-18's first increment, not its terminal scope. [Stable evidence](evidence/dtr-wave4c/README.md). |
| [298-groups-repository-placement-relocation-tdd-plan.md](298-groups-repository-placement-relocation-tdd-plan.md) | **Plan-green / implementation-complete / Wave-accepted DTR-18 placement slice under `DTR18-AUTH-02` (2026-07-28)** — relocated the exact 13 Groups concrete repository implementations from feature `domain/repositories/` to `data/repositories/`, with no compatibility shim or implementation-body, behavior, schema, native, wire, or crypto change. Focused preservation, Groups/feature-family, architecture/policy, whole-tree analyzer, hygiene, and Graphify proof are green; the later Wave 4C aggregate passed. [Stable evidence](evidence/dtr-wave4c/README.md). |
| [299-conversation-repository-placement-relocation-tdd-plan.md](299-conversation-repository-placement-relocation-tdd-plan.md) | **Plan-green / implementation-complete / Wave-accepted DTR-18 placement slice under `DTR18-AUTH-02` (2026-07-28)** — relocated the exact three Conversation concrete repository implementations from feature `domain/repositories/` to `data/repositories/`, with import/source-lock rewrites but no compatibility shim or implementation-body, behavior, schema, native, wire, or crypto change. Focused preservation, Conversation/feature-family, architecture/policy, whole-tree analyzer, hygiene, and Graphify proof are green; the later Wave 4C aggregate passed. [Stable evidence](evidence/dtr-wave4c/README.md). |
| [300-remaining-feature-repository-placement-relocation-tdd-plan.md](300-remaining-feature-repository-placement-relocation-tdd-plan.md) | **Plan-green / implementation-complete / Wave-accepted DTR-18 terminal placement and residual-disposition slice under closed `DTR18-AUTH-02` (2026-07-28)** — relocated the exact six remaining Contact Request, Contacts, Identity, Introduction, and Posts concrete repository implementations and closed placement at zero while retaining the exact 165 dependency identities with explicit owner, reason, and revisit condition. Architecture passed at 165 dependency / 0 placement / 0 issues; full disposition-tuple SHA-256 `5f24faf4c5f693d0f19eb18503e5d37c4db4580c6ccbc91806abc75a78dcf132`. Across DTR-18 the terminal delta is dependency -17 and placement -24. Focused/curated/family/policy/analyzer/Graphify proof and the Wave 4C 21-path `performance-host` (106 pass) then 1,277-item `host-all` (12,845 pass, 1 skip, all eight Go tails) are green. DTR-18 is terminally complete and Wave 4C is accepted; final release closure remains separate. [Stable evidence](evidence/dtr-wave4c/README.md). |

`DTR-10` is terminally complete: Plans 277–283 are Plan-green/closed and Plan
284 is acceptance-verified terminal `Retained`. DTR-11 and Wave 3 are
Wave-accepted. DTR-12 is Wave-accepted; DTR-13 is Plan-green/Wave-accepted,
and `DTR13-AUTH-01` is closed on its authorized scope. DTR-14 and DTR-15 are
Plan-green/Wave-accepted under `DTR14-AUTH-01` and `DTR15-AUTH-01`; Wave 4B is
accepted and its full receipt is preserved in the
[stable evidence archive](evidence/dtr-wave4b/README.md). Plans 295–300 are
Plan-green and Wave-accepted. DTR-16 and DTR-17 are Wave-accepted under
`DTR16-AUTH-01` and `DTR17-AUTH-01`. Plan 297 remains DTR-18's Plan-green first
increment under `DTR18-AUTH-01`; Plans 298–300 closed the bounded placement
family under accepted `DTR18-AUTH-02`. DTR-18 is terminally complete at 165
dependency / 0 placement / 0 issues, an overall delta of dependency -17 and
placement -24. The Wave 4C `performance-host` and full `host-all` aggregate
passed, and its receipt is preserved in the
[stable evidence archive](evidence/dtr-wave4c/README.md). DEP-01's
preceding-wave gate is green, but Wave 5 planning remains blocked on the open
Product + Release supported-platform decision.
The full Wave 4A
receipt remains preserved in its [stable evidence archive](evidence/dtr-wave4a/README.md).
Final release-closure `host-all` and `performance-host` remain separate later
obligations.

**Top finding:** The project is clean, but the earlier “24 files safe to remove” claim is too broad. Removal is path-specific and owner-approved; the remaining runtime-root candidates are advisory and several are still test-, smoke-, or compatibility-backed.

---

## 5. Network Component

| Report | Focus |
|--------|-------|
| [08-network-1to1-messaging.md](08-network-1to1-messaging.md) | 1:1 send/receive/media/voice reliability with corrected durability and read-state findings |
| [09-network-group-messaging.md](09-network-group-messaging.md) | Group/announcement architecture for current scale; stale “missing retry” claims removed |
| [10-network-measurement-strategy.md](10-network-measurement-strategy.md) | Incremental observability plan — local counters/timers first, export stack deferred |

**Top findings:**
- 1:1 durable send-path parity between conversation and feed inline reply is now landed in the current Flutter tree
- Shared 1:1 delivery changes now have a named regression gate plus feed-surface companion direct coverage
- Group media retry/recovery already exists; it is not a missing P0 feature
- Announcement coverage is stronger than earlier reported inside the Flutter tree
- Lean local timing/counter coverage for the highest-value messaging gaps is now landed; exporter/dashboard work remains deferred

---

## 6. Use Case Audits (1:1 / Discussions / Announcements)

| Report | Focus |
|--------|-------|
| [12-1to1-chat-use-case-audit.md](12-1to1-chat-use-case-audit.md) | Core 1:1 flows are tested; remaining gaps are product features and narrow residual edges |
| [11-group-discussion-use-case-audit.md](11-group-discussion-use-case-audit.md) | Core group lifecycle/message/recovery flows are tested; stale gap claims removed |
| [13-announcement-use-case-audit.md](13-announcement-use-case-audit.md) | Announcement enforcement is well covered in Flutter; Go-side writer enforcement remains outside this tree |
| [18-group-discussion-reliability-audit.md](18-group-discussion-reliability-audit.md) | Lean reliability-gap review: what group chat still needs to feel as trustworthy as 1:1 without overengineering |

**Top findings:**
- **1:1 Chat:** Core implemented flows are tested; the earlier feed-inline
  durable-send gap, the sender-visible `reuse` fast-path mismatch, and the
  broader direct-vs-relay transport-truth seam for new Go-backed 1:1 rows are
  now closed. The remaining gaps are mostly product features plus narrower
  residual edge cases such as local-file-missing retry behavior
- **Group Discussions:** Core use cases/listeners are well tested; missing items are mostly intentional product scope, not correctness failures
- **Group Discussion Reliability:** Final acceptance revalidation confirms ordinary-media parent-row durability, ordinary-media failed-send retry parity, and explicit one-thread send serialization are landed; voice publish-failure retry remains only a narrower producer-side residual if reopened later
- **Announcements:** Session `28` revalidated that shared group reliability work did not regress announcement auth/send/recovery/read-only behavior; remaining gaps are now narrower evidence niceties, not a new reliability program
- **QA type:** Still defined in the enum/schema but filtered out of creation UI — placeholder only

---

## 7. Regression Strategy

| Report | Focus |
|--------|-------|
| [14-regression-test-strategy.md](14-regression-test-strategy.md) | Practical regression model: baseline gate, subsystem gates, missing regressions to add, and run rules for new work |
| [258-sims-major-update-gate-and-build-reuse-tdd-plan.md](258-sims-major-update-gate-and-build-reuse-tdd-plan.md) | Implemented contract for a truthful `$sims major` umbrella: the typed manifest, strict verdicts, six-profile build cache, live-device resolver, checkpointing, resource scheduler, and all active campaign drivers have landed. Release closure still requires an authoritative clean run whose available live prerequisites pass; configuration blockers remain blockers. Future Voice/Video Call 1:1 capabilities remain inactive until that feature is implemented. |

**Top finding:** The repo already has most of the needed tests. Plan 258 defines
the release umbrella that was missing above the existing small PR baseline,
change-based subsystem gates, explicit named-gate lists, bulk classification,
and the permanent-regression rule for escaped bugs.

`$sims` now names three distinct contracts: no arguments selects `major`, the
only release-green-eligible critical-feature umbrella; `full` is the cleaned
registered reliability/device inventory (including explicit blockers); and
`smoke` is the fast representative
subset. Filtered, continued, retried, or resumed runs remain diagnostic until a
separate clean unfiltered `major` passes. Major rows use typed
`PASS|FAIL|BLOCKED|SKIP|N/A` verdicts, reuse one attested artifact per compatible
build profile, and permit overlap only through declared resource-safe
`--simultaneous` scheduling. The skill adapters delegate those decisions to the
repo CLI and do not carry a separate iOS-first target policy.

Implementation snapshot (2026-07-15): the logical major plan has 26 active
rows, six declared reusable device build profiles, and 11 device/campaign
consumers. Every active row has an automated driver. Live preflight reports
missing provider/relay/staging/signing or disposable-iOS-receiver prerequisites
as `BLOCKED`, and only genuinely absent policy-bounded topology as `N/A`.
Recorder and performance share the central
`android.e2e.standard` APK; the performance adapter binds the runtime tuple and
APK digest and enforces FEED average `<8 ms`, FEED p99 `<24 ms`, FEED worst
`<100 ms`, and production Go `node:status` MethodChannel p99 `<50 ms`.
Android campaigns restore exact pre-run app/device state before PASS. Preflight
skips profiles with no runnable consumer, and the report's requested, built,
failed, cache-hit, per-profile elapsed, and total build elapsed fields are
authoritative. The
flag `--simultaneous` schedules compatible plan rows (default cap four); it does
not parallelize central artifact preparation or override shared locks. The
Android recorder now stages a profile/scenario/role/run-ID/nonce invocation and
requires a matching app acknowledgement while consuming the prepared APK.
Performance uses the same acknowledged runtime protocol, while intro has its
own prepared production-FCM campaign adapter. The new intro/performance
readiness statements are implementation-state claims; they do not add
fabricated live-campaign or clean-major PASS results.

---

## 8. Session Roadmaps And Closure

| Report | Focus |
|--------|-------|
| [15-session-todo-roadmap.md](15-session-todo-roadmap.md) | Historical execution backlog for Sessions `1` through `11` |
| [16-session-todo-roadmap-2.md](16-session-todo-roadmap-2.md) | Historical follow-on backlog for Sessions `12` through `23`, including profile/evidence-gated and cross-tree work |
| [session-24-plan.md](session-24-plan.md) | Historical residual reliability implementation session |
| [session-25-plan.md](session-25-plan.md) | Historical residual reliability implementation session |
| [session-26-plan.md](session-26-plan.md) | Historical residual reliability implementation session |
| [session-27-plan.md](session-27-plan.md) | Historical residual reliability acceptance session |
| [session-28-plan.md](session-28-plan.md) | Historical announcement acceptance session |
| [session-29-plan.md](session-29-plan.md) | Historical lean local measurement session |
| [session-30-plan.md](session-30-plan.md) | Plan-only narrow residual reopen artifact; not executed closure work |
| [session-31-plan.md](session-31-plan.md) | Historical narrow 1:1 transport-label closure session |
| [session-32-plan.md](session-32-plan.md) | Historical narrow 1:1 transport-truth closure session |
| [session-34-plan.md](session-34-plan.md) | Historical narrow standalone CLI-backed transport residual closure session |
| [session-35-plan.md](session-35-plan.md) | Historical narrow intro-to-Orbit / intro-to-Feed follow-up closure session |
| [session-36-plan.md](session-36-plan.md) | Historical narrow standalone CLI-backed post-verify closure session |
| [session-37-plan.md](session-37-plan.md) | Historical narrow 1:1 failed-send recovery regression-coverage closure session |
| [24-cancel-media-upload-session-breakdown.md](24-cancel-media-upload-session-breakdown.md) | Historical multi-session breakdown and closure-owner artifact for Report `24` |
| [session-44-plan.md](session-44-plan.md) | Historical cancelable-upload retry/delete contract session |
| [session-45-plan.md](session-45-plan.md) | Historical 1:1 cancelable-upload surface session |
| [session-46-plan.md](session-46-plan.md) | Historical group/announcement parity session for cancelable uploads |
| [session-47-plan.md](session-47-plan.md) | Historical acceptance-only closure refresh for cancelable uploads |
| [27-persistent-nav-bar-orbit-session-breakdown.md](27-persistent-nav-bar-orbit-session-breakdown.md) | Historical multi-session breakdown and closure-owner artifact for Report `27` |
| [27-persistent-nav-bar-orbit-session-54-plan.md](27-persistent-nav-bar-orbit-session-54-plan.md) | Historical already-covered in-app Orbit persistent-nav session artifact |
| [27-persistent-nav-bar-orbit-session-55-plan.md](27-persistent-nav-bar-orbit-session-55-plan.md) | Historical intro-notification Orbit parity and closure session |
| [28-orbit-intro-badge-session-breakdown.md](28-orbit-intro-badge-session-breakdown.md) | Historical doc-scoped breakdown and closure-owner artifact for Report `28` |
| [28-orbit-intro-badge-session-56-plan.md](28-orbit-intro-badge-session-56-plan.md) | Historical shared Orbit intro badge and freshness-wiring session artifact |
| [29-batch-parallel-intro-sending-session-breakdown.md](29-batch-parallel-intro-sending-session-breakdown.md) | Historical doc-scoped breakdown and closure-owner artifact for Report `29` |
| [29-batch-parallel-intro-sending-session-58-plan.md](29-batch-parallel-intro-sending-session-58-plan.md) | Historical capped intro batching and progress-contract session artifact |
| [29-batch-parallel-intro-sending-session-59-plan.md](29-batch-parallel-intro-sending-session-59-plan.md) | Historical picker progress UX and closure session artifact |
| [30-swipe-nav-feed-orbit-session-breakdown.md](30-swipe-nav-feed-orbit-session-breakdown.md) | Historical doc-scoped breakdown and closure-owner artifact for Report `30` |
| [30-swipe-nav-feed-orbit-session-59-plan.md](30-swipe-nav-feed-orbit-session-59-plan.md) | Historical shared-host migration and preserved-state session artifact for Report `30` |
| [30-swipe-nav-feed-orbit-session-60-plan.md](30-swipe-nav-feed-orbit-session-60-plan.md) | Historical horizontal swipe / gesture arbitration closure session artifact for Report `30` |
| [34-orbit-intros-swipe-delete-missing-session-breakdown.md](34-orbit-intros-swipe-delete-missing-session-breakdown.md) | Historical doc-scoped breakdown and closure-owner artifact for Report `34` |
| [34-orbit-intros-swipe-delete-missing-session-1-plan.md](34-orbit-intros-swipe-delete-missing-session-1-plan.md) | Historical live Orbit intro swipe-delete / closure session artifact for Report `34` |
| [35-cancelled-video-upload-still-sends-session-breakdown.md](35-cancelled-video-upload-still-sends-session-breakdown.md) | Historical doc-scoped breakdown and closure-owner artifact for Report `35` |
| [35-cancelled-video-upload-still-sends-session-1-plan.md](35-cancelled-video-upload-still-sends-session-1-plan.md) | Historical late-boundary 1:1 video cancel reopen/closure session artifact for Report `35` |
| [41-notification-open-missing-incoming-messages-session-breakdown.md](41-notification-open-missing-incoming-messages-session-breakdown.md) | Historical doc-scoped breakdown and closure-owner artifact for Report `41` |
| [41-notification-open-missing-incoming-messages-session-1-plan.md](41-notification-open-missing-incoming-messages-session-1-plan.md) | Historical two-phase relay inbox retrieve/ack session artifact for Report `41` |
| [41-notification-open-missing-incoming-messages-session-2-plan.md](41-notification-open-missing-incoming-messages-session-2-plan.md) | Historical durable inbox staging/replay and reject-observability session artifact for Report `41` |
| [41-notification-open-missing-incoming-messages-session-3-plan.md](41-notification-open-missing-incoming-messages-session-3-plan.md) | Historical app-root notification parity and closure session artifact for Report `41` |
| [44-feed-orbit-notification-desync-session-breakdown.md](44-feed-orbit-notification-desync-session-breakdown.md) | Historical doc-scoped breakdown and closure-owner artifact for Report `44` |
| [44-feed-orbit-notification-desync-session-1-plan.md](44-feed-orbit-notification-desync-session-1-plan.md) | Historical Feed/Orbit handled-notification sync and closure session artifact for Report `44` |
| [45-feed-stack-card-does-not-reorient-after-inline-reply-session-breakdown.md](45-feed-stack-card-does-not-reorient-after-inline-reply-session-breakdown.md) | Historical doc-scoped breakdown and closure-owner artifact for Report `45` |
| [45-feed-stack-card-does-not-reorient-after-inline-reply-session-1-plan.md](45-feed-stack-card-does-not-reorient-after-inline-reply-session-1-plan.md) | Historical Feed inline-reply viewport reorientation and closure session artifact for Report `45` |
| [48-gap3-remove-destructive-inbox-fallback-plan-session-breakdown.md](48-gap3-remove-destructive-inbox-fallback-plan-session-breakdown.md) | Historical doc-scoped breakdown and closure-owner artifact for Report `48` |
| [48-gap3-remove-destructive-inbox-fallback-plan-session-1-plan.md](48-gap3-remove-destructive-inbox-fallback-plan-session-1-plan.md) | Historical automatic 1:1 inbox-drain fallback removal and closure session artifact for Report `48` |
| [50-two-simulator-user-journey-tests-todo-session-breakdown.md](50-two-simulator-user-journey-tests-todo-session-breakdown.md) | Closed doc-scoped controller for Report `50` manual-journey coverage refresh |
| [50-two-simulator-user-journey-tests-todo-session-10-plan.md](50-two-simulator-user-journey-tests-todo-session-10-plan.md) | Historical closure-only matrix refresh and accepted-difference session artifact for Report `50` |
| [55-external-share-skip-post-and-multi-recipient-plan-session-breakdown.md](55-external-share-skip-post-and-multi-recipient-plan-session-breakdown.md) | Closed doc-scoped controller for Report `55` iOS share handoff plus multi-recipient picker/batch-send rollout |
| [55-external-share-skip-post-and-multi-recipient-plan-session-1-plan.md](55-external-share-skip-post-and-multi-recipient-plan-session-1-plan.md) | Historical iOS auto-redirect handoff proof session artifact for Report `55` |
| [55-external-share-skip-post-and-multi-recipient-plan-session-2-plan.md](55-external-share-skip-post-and-multi-recipient-plan-session-2-plan.md) | Historical picker multi-select batch-send and closure session artifact for Report `55` |
| [76-remove-legacy-1to1-plaintext-path-plan-session-breakdown.md](76-remove-legacy-1to1-plaintext-path-plan-session-breakdown.md) | Closed doc-scoped controller for Report `76` outbound 1:1 v2-only privacy closure |
| [76-remove-legacy-1to1-plaintext-path-plan-session-1-plan.md](76-remove-legacy-1to1-plaintext-path-plan-session-1-plan.md) | Historical outbound/retry legacy plaintext removal and closure session artifact for Report `76` |
| [17-roadmap-closure-audit.md](17-roadmap-closure-audit.md) | Post-execution closure audit and current reading order for the folder |

**Top finding:** The folder has now moved from backlog mode into closure mode.
Roadmaps `15` and `16` served as the main execution backlogs through Session
`23`, Sessions `24` through `29` closed the residual
group/announcement/measurement track, Sessions `31` and `32` closed the narrow
1:1 transport-label and transport-truth seams, Session `35` closed the narrow
intro-to-Orbit / intro-to-Feed stale follow-up seam, Sessions `34` and `36`
closed the reviewed standalone CLI-backed transport and post-verify proof
seams, Session `37` closed the narrow deterministic failed-send recovery
coverage seam, Sessions `44` through `47` closed the Report `24`
cancelable-upload rollout and refreshed the stable messaging closure refs,
Sessions `54` and `55` closed the Report `27` persistent-nav rollout and
refreshed the folder closure docs, Session `56` closed the Report `28` shared
Orbit intro badge truth and freshness wiring seam and refreshed the closure
docs, Sessions `58` and `59` closed the Report `29` ordered intro-batching and
truthful picker-progress rollout, the doc-scoped Sessions `59` and `60`
closed the Report `30` shared-host plus horizontal-swipe Feed/Orbit rollout,
the doc-scoped Session `1` for Report `35` reclosed the narrow late-boundary
1:1 video cancel seam without reopening the broader Report `24`
cancelable-upload program, the doc-scoped Sessions `1` through `3` for Report
`41` closed the staged inbox recovery plus app-root notification-open trust
seam and refreshed the stable 1:1 closure wording, the doc-scoped Session `1`
for Report `44` closed the stale Feed/Orbit handled-notification contradiction
without widening into app-root notification routing or unread architecture
work, the doc-scoped Session `1` for Report `45` closed the stale absolute
scroll-offset seam after successful inline reply without widening into
unread-model redesign or broader Feed/Orbit navigation architecture, the
doc-scoped Session `1` for Report `48` closed the automatic inbox-drain path
from durable `retrieve_pending` back to destructive `inbox:retrieve`,
refreshed the stable 1:1 closure wording without widening into public
inbox-API removal, the doc-scoped Sessions `1` through `10` for Report `50`
closed the manual-journey coverage backlog plus the stale notification-open
assumption, the doc-scoped Sessions `1` and `2` for Report `55` closed the
external-share iOS handoff plus truthful multi-recipient picker/batch-send
rollout without widening into Android entry or composer redesign, the
doc-scoped Session `1` for Report `76` closed ordinary outbound 1:1 legacy
plaintext send/retry by making those paths encrypted v2 or fail-closed, and
Session `30` remains a plan-only residual artifact rather than executed work.
The closure audit now describes what is historical rationale, what remains
open, and what should only be reopened if a real residual gap appears.

---

## 9. Post-Execution State

| Status | Item | Why It Still Matters | Report |
|--------|------|----------------------|--------|
| Maintain | Keep `test-gate-definitions.md` and named gates canonical | Future changes should continue to use the same gate language and command surface | 14 / 15 / 16 |
| Maintain | Keep the lean local messaging measurement events coherent with the current flow-event contract | Future send/retry/media/rejoin work should extend the landed local event layer, not invent a second metrics stack | 10 / 29 |
| Maintain | Use the direct standalone CLI-backed transport command alongside the named transport gate when touching the Sessions `34` / `36` seams | The named transport gate can run `transport_e2e_test.dart` without the CLI fixture/orchestrator path | 19 / 34 / 36 |
| Maintain | Use the direct intro/orbit/feed maintenance suite plus `baseline` when intro follow-up wiring changes | No named gate directly owns this seam; maintenance-time safety sits in `orbit_wired_test.dart`, `orbit_intros_wiring_test.dart`, `feed_wired_test.dart`, and the intro listener/regression/integration suites | 35 / test-gate-definitions |
| Maintain | Use `50-two-simulator-user-journey-tests-todo-session-breakdown.md` plus the refreshed Report `50` audit/journey docs when manual two/three-simulator coverage claims are questioned | Report `50` is now the closure-time controller for this matrix, including the accepted notification-open routing difference and the exact direct suites that closed Sessions `1` through `9` | 50 |
| Maintain | Use the stable 1:1/group closure refs plus `24-cancel-media-upload-session-breakdown.md` and `35-cancelled-video-upload-still-sends-session-breakdown.md` for cancelable-upload maintenance | Session `47` closed the broad rollout and the doc-scoped Session `1` for Report `35` reclosed the narrow late-boundary 1:1 video cancel seam without widening gate definitions or announcement-specific architecture | 19 / 20 / 24 / 35 / 47 |
| Maintain | Use `41-notification-open-missing-incoming-messages-session-breakdown.md`, the direct notification-open suites, `./scripts/run_test_gates.sh 1to1`, and `baseline` when shared 1:1 inbox recovery or app-root notification-open routing changes | The doc-scoped Report `41` sessions closed durable fetched-envelope staging/replay plus prepare-before-route parity for terminated remote, warm remote, terminated local, and warm local notification opens; `transport` becomes required again only if later work broadens into startup/resume/inbox-drain ordering changes | 19 / 41 / test-gate-definitions |
| Maintain | Use `48-gap3-remove-destructive-inbox-fallback-plan-session-breakdown.md`, the direct automatic-drain service/lifecycle suites, `./scripts/run_test_gates.sh 1to1`, `./scripts/run_test_gates.sh baseline`, and `FLUTTER_DEVICE_ID=<device-id> ./scripts/run_test_gates.sh transport` when shared automatic 1:1 inbox drain or `retrieve_pending` fallback wiring changes | The doc-scoped Session `1` for Report `48` closed the production path from durable automatic drain back to destructive `inbox:retrieve` without widening into public destructive-API removal or malformed-row cleanup architecture | 19 / 41 / 48 / test-gate-definitions |
| Maintain | Use `76-remove-legacy-1to1-plaintext-path-plan-session-breakdown.md`, the refreshed 1:1 closure reference, the direct send/delete/retry/voice suites, `./scripts/run_test_gates.sh 1to1`, `FLUTTER_DEVICE_ID=macos ./scripts/run_test_gates.sh baseline`, and `./scripts/run_test_gates.sh completeness-check` when ordinary 1:1 outbound encryption or retry wire-envelope replay changes | The doc-scoped Session `1` for Report `76` closed the outbound plaintext leak by requiring encrypted v2 or fail-closed behavior before send, inbox store, new outbound `wireEnvelope` persistence, or voice upload, while preserving inbound v1 compatibility | 19 / 76 / test-gate-definitions |
| Maintain | Use `27-persistent-nav-bar-orbit-session-breakdown.md`, `intro_notification_orbit_route_test.dart`, the direct Orbit/Feed nav suites, and `baseline` when app-root intro-open wiring changes | Session `55` closed the notification-opened Orbit persistent-nav seam without widening into sibling-tab hosting, keep-alive, or swipe navigation scope | 27 / 54 / 55 |
| Maintain | Use `28-orbit-intro-badge-session-breakdown.md`, the direct Feed/Orbit intro badge suites, `intro_notification_orbit_route_test.dart`, and `baseline` when shared Orbit pending-intro badge truth changes | Session `56` closed shared badge coexistence, expiry-aware load, live refresh, route-return freshness, and persistent-nav Orbit parity without widening into a second unread system or root-owned badge controller | 28 / 56 |
| Maintain | Use `29-batch-parallel-intro-sending-session-breakdown.md`, the direct intro application/picker/integration suites, and rerun `baseline` only when broader conversation or banner entry wiring changes | Sessions `58` and `59` closed capped batch intro sending plus truthful picker progress without widening into Go/bridge batch APIs or new named-gate ownership | 29 / 58 / 59 |
| Maintain | Use `55-external-share-skip-post-and-multi-recipient-plan-session-breakdown.md`, the direct share picker/coordinator/integration suites, `FLUTTER_DEVICE_ID=macos ./scripts/run_test_gates.sh baseline`, `FLUTTER_DEVICE_ID=macos ./scripts/run_test_gates.sh 1to1`, and `FLUTTER_DEVICE_ID=macos ./scripts/run_test_gates.sh groups` when shared external-share picker or share fanout wiring changes | The doc-scoped Sessions `1` and `2` for Report `55` closed the iOS share-entry handoff plus truthful multi-recipient picker/batch-send slice without widening into Android entry, composer redesign, or shared 1:1/group send semantics | 55 / test-gate-definitions |
| Maintain | Use `30-swipe-nav-feed-orbit-session-breakdown.md`, the direct Feed/Orbit swipe and local-gesture suites, and `baseline` when the shared Feed/Orbit navigation seam changes | The doc-scoped Sessions `59` and `60` closed the shared-host plus horizontal-swipe rollout without widening into notification-opened Orbit routing parity, a broader app-root tab shell, or unread/badge architecture work | 30 / 59 / 60 |
| Maintain | Use `44-feed-orbit-notification-desync-session-breakdown.md`, the direct `feed_wired_test.dart` and `orbit_wired_test.dart` suites, plus `./scripts/run_test_gates.sh feed`, `FLUTTER_DEVICE_ID=macos ./scripts/run_test_gates.sh 1to1`, and `FLUTTER_DEVICE_ID=macos ./scripts/run_test_gates.sh baseline` when shared Feed/Orbit handled-notification truth changes | The doc-scoped Session `1` for Report `44` closed the mounted Orbit stale-unread contradiction after Feed collapse or successful inline reply without widening into app-root notification routing, group sync, or unread-architecture redesign | 30 / 40 / 44 / test-gate-definitions |
| Maintain | Use `45-feed-stack-card-does-not-reorient-after-inline-reply-session-breakdown.md`, the direct `feed_wired_test.dart` and `feed_screen_test.dart` suites, plus `./scripts/run_test_gates.sh feed`, `./scripts/run_test_gates.sh 1to1`, and `FLUTTER_DEVICE_ID=macos ./scripts/run_test_gates.sh baseline` when Feed inline-reply viewport continuity changes | The doc-scoped Session `1` for Report `45` closed the stale same-card viewport continuity seam after successful inline reply without widening into unread-truth redesign, group parity, or broader Feed/Orbit navigation architecture | 40 / 44 / 45 / test-gate-definitions |
| Maintain | Use `34-orbit-intros-swipe-delete-missing-session-breakdown.md`, `orbit_wired_test.dart`, `orbit_intros_wiring_test.dart`, `orbit_screen_archived_groups_test.dart`, `swipeable_friend_row_test.dart`, and `baseline` when live Orbit intro delete behavior changes | The doc-scoped Session `1` closed the live Orbit intro swipe-delete seam without widening into intro protocol, loader, or broader Feed/Orbit architecture work | 34 / doc-scoped 1 |
| Follow up only if needed | Complete external CI / release owner wiring for Session `12` if the local handoff artifact is still the final state | This is the only clearly externalized closure item | 16 |
| Follow up only if needed | Revalidate Session `30` before any execution | The plan file alone should not reopen the already-closed broader program | 17 / 30 |
| Residual only | Reopen group reliability only if voice publish-failure retry becomes a real escaped bug or a clearly justified trust gap | The broader discussion reliability program is closed | 18 / 24 / 25 / 26 / 27 |
| Residual only | Reopen intro follow-up work only if a real regression makes `mutualAccepted` contacts reappear under `Intros`, hides the Feed connection card, or breaks the blocked-accept listener contract | Session `35` closed the UI-state race without reopening listener or protocol work | 35 |
| Residual only | Reopen Orbit intro badge work only if pending-intro badge truth regresses on cold load, live intro updates, route return from Orbit accept/pass, or persistent-nav Orbit parity | Session `56` closed the report; later work should reopen only on real badge-truth regressions, not to invent a new root-level badge architecture | 28 / 56 |
| Residual only | Reopen live Orbit intro delete work only if swipe-delete affordance, confirmation, grouped-row cleanup, empty-state truth, or pending-intro badge truth regresses after delete | The doc-scoped Session `1` for Report `34` closed the local Orbit intro delete seam; later work should reopen only on real UI/count regressions, not to invent P2P delete sync or widened intro-status scope | 34 / doc-scoped 1 |
| Residual only | Reopen 1:1 transport-label/truth work only if new outgoing or incoming 1:1 rows regress to misleading transport labels | Sessions `31` and `32` closed new-row reuse fast-path labeling and Go/libp2p direct-vs-relay truth; old `reuse` rows remain legacy-only fallback | 19 / 31 / 32 |
| Residual only | Reopen 1:1 failed-send recovery coverage only if the deterministic foreground online-transition or lock-after-failure resume regressions stop proving exact-once healing | Session `37` closed the missing matrix cell for visible failure during network switch followed by later foreground or resume recovery | 19 / 33 / 37 |
| Residual only | Reopen the late-boundary 1:1 video cancel seam only if accepted cancel again falls through into the ordinary upload-failure UX or the later final-send path for that same attempt | The doc-scoped Session `1` for Report `35` reclosed that narrow seam without reopening group parity, retry ownership, or status-model scope | 19 / 24 / 35 |
| Residual only | Reopen standalone CLI-backed transport work only if a current direct run reintroduces the previously closed transport-truth or post-verify proof seams | Sessions `34` and `36` closed the reviewed `A1` / `A4` / `A2` / `A5` / `D4` / `A7` / `A8` / `A8b` / `C3` / `B8` / `G6` / `E8` / `RECV-A1` / `RECV-A4` / `RECV-A6` seams; the direct command remains required maintenance proof alongside the named gate | 19 / 34 / 36 |
| Residual only | Reopen a roadmap item only if a real regression, failed gate, or newly proven gap appears | Avoids restarting broad cleanup work without evidence | 17 |
| Intentionally deferred | Product-scope items such as read receipts, typing indicators, search, exporter/dashboard work | These were deferred by design, not missed correctness work | 08 / 09 / 10 |
| Do not reopen by default | Broad SQL/index/caching work, mass dead-code cleanup, or exporter architecture | These remain unjustified without new evidence | 05 / 06 / 10 |

---

## 10. Closure References

| Report | Focus |
|--------|-------|
| [19-1to1-message-reliability-closure-reference.md](19-1to1-message-reliability-closure-reference.md) | Stable closure bar for trustworthy 1:1 text/media/voice messaging |
| [20-group-discussion-reliability-closure-reference.md](20-group-discussion-reliability-closure-reference.md) | Stable closure bar for trustworthy group discussions under current receipt-less group architecture |
| [21-announcement-reliability-closure-reference.md](21-announcement-reliability-closure-reference.md) | Stable closure bar for trustworthy announcements on top of shared group reliability and admin-only enforcement |

**Top finding:** These three docs are the canonical "stop here unless a real regression appears" references for messaging reliability after the roadmap work. They are intentionally narrower than full feature backlogs and should be used to avoid reopening product-scope or protocol-scope debates by accident.

---

## 11. Feature Specs (New Work)

| Report | Focus |
|--------|-------|
| [22-media-transfer-size-limit.md](22-media-transfer-size-limit.md) | Raise media transfer cap from 100 MB to 5 GB with attach-time warning, compression enforcement, wake lock, and progress UX |
| [24-cancel-media-upload.md](24-cancel-media-upload.md) | Cancel in-progress uploads, delete/retry failed messages, prevent unwanted auto-retries |
| [25-delete-intro-swipe.md](25-delete-intro-swipe.md) | Swipe-to-delete introductions in Orbit Intros tab with DB removal and re-introduction support |
| [26-long-press-message-context-menu.md](26-long-press-message-context-menu.md) | Signal-like long-press overlay with emoji bar, blurred background, and Reply/Copy context menu on messages |
| [27-persistent-nav-bar-orbit.md](27-persistent-nav-bar-orbit.md) | Bottom nav bar disappears when navigating to Orbit; should persist with active tab indicator |
| [28-orbit-intro-badge.md](28-orbit-intro-badge.md) | Badge/dot on Orbit nav button when pending introductions exist |
| [29-batch-parallel-intro-sending.md](29-batch-parallel-intro-sending.md) | Ordered batched introduction sending with concurrency cap of 10 and truthful picker progress |
| [30-swipe-nav-feed-orbit.md](30-swipe-nav-feed-orbit.md) | Horizontal swipe navigation between Feed and Orbit screens |
| [31-edit-last-sent-message.md](31-edit-last-sent-message.md) | Edit last sent message via long-press context menu with P2P sync |
| [32-notification-card-interactions.md](32-notification-card-interactions.md) | Profile picture tap and collapse bar non-responsive on notification-opened feed cards |
| [33-delete-message-for-me-everyone.md](33-delete-message-for-me-everyone.md) | Delete messages with "Delete for Me" and "Delete for Everyone" via long-press context menu |
| [34-orbit-intros-swipe-delete-missing.md](34-orbit-intros-swipe-delete-missing.md) | Live Orbit intros are missing the existing swipe-to-delete affordance and confirmation flow |
| [35-cancelled-video-upload-still-sends.md](35-cancelled-video-upload-still-sends.md) | Accepted cancel on a 1:1 video upload can still fall through into failure/send; cancel should stop delivery for that attempt |
| [55-external-share-skip-post-and-multi-recipient-plan.md](55-external-share-skip-post-and-multi-recipient-plan.md) | Skip the extra native iOS Post screen on external share and support truthful multi-recipient share delivery from the picker |
| [76-remove-legacy-1to1-plaintext-path-plan.md](76-remove-legacy-1to1-plaintext-path-plan.md) | Remove ordinary outbound 1:1 legacy plaintext send/retry paths while preserving inbound v1 compatibility |

**Context:** These specs address user-facing gaps discovered during TestFlight
usage. Report `24` directly relates to the upload reliability path covered by
reports `08`, `19`, and `22`, and its landed maintenance-time meaning now
lives in `24-cancel-media-upload-session-breakdown.md` plus the refreshed
closure references rather than in the proposal alone. Report `27` directly
relates to the intro/orbit/feed navigation seam first narrowed by Session
`35`, and its landed maintenance-time meaning now lives in
`27-persistent-nav-bar-orbit-session-breakdown.md` plus the refreshed closure
docs rather than in the proposal alone. Report `28` directly relates to that
same intro/orbit/feed seam, and its landed maintenance-time meaning now lives
in `28-orbit-intro-badge-session-breakdown.md` plus the refreshed closure docs
rather than in the proposal alone. Report `29` directly relates to the same
intro sending seam, and its landed maintenance-time meaning now lives in
`29-batch-parallel-intro-sending-session-breakdown.md` plus the refreshed
closure docs rather than in the proposal alone. Report `30` directly relates to
that same Feed/Orbit navigation seam, and its landed maintenance-time meaning
now lives in `30-swipe-nav-feed-orbit-session-breakdown.md` plus the refreshed
closure docs rather than in the proposal alone. Report `34` directly relates to
the same intro/orbit/feed seam, and its landed maintenance-time meaning now
lives in `34-orbit-intros-swipe-delete-missing-session-breakdown.md` plus the
refreshed closure docs rather than in the proposal alone. Report `35` directly
relates to the Report `24` 1:1 cancel seam, and its landed maintenance-time
meaning now lives in
`35-cancelled-video-upload-still-sends-session-breakdown.md` plus the
refreshed 1:1 closure docs rather than in the proposal alone. Report `55`
directly relates to the external-share entry and delivery seam, and its landed
maintenance-time meaning now lives in
`55-external-share-skip-post-and-multi-recipient-plan-session-breakdown.md`
rather than in the proposal alone. Report `76` directly relates to the 1:1
privacy and retry closure bar, and its landed maintenance-time meaning now
lives in
`76-remove-legacy-1to1-plaintext-path-plan-session-breakdown.md` plus the
refreshed 1:1 closure reference rather than in the proposal alone.
