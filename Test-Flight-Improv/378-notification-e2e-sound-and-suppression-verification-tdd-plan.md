# 378 - Notification E2E Sound-Disposition And Suppression Verification Closure

Status: execution-ready
Type: Modification
Spec: free-text intent (no formal spec) — UI-23 assessment gaps G1–G7 (2026-08-17 session review of `UI-23-notification/Mknoon_Private_Reliable_Notifications_PRD_v1.2.md` §6/§9/§13 and the coverage doc §6.1/§6.5/GAP-N12)
Classification: implementation-ready
Closure tier: device

## Planning Progress
| Time | Role | Files inspected | Decision/blocker | Next action |
|---|---|---|---|---|
| 2026-08-17 am | Evidence Collector | PRD v1.2, coverage doc §6/§3, `show_notification_use_case.dart`, `local_notification_support.dart`, `notification_tone_tracker.dart`, `durable_notification_tone_lease.dart`, sound smoke harness+orchestrator, sims registry | Three-disposition model confirmed (audible / silent-visible / suppressed); device tier never asserts disposition | verify→refute agents |
| 2026-08-17 am | Verify→Refute (3 agents) | harness S4/backgrounding mechanics; reaction-campaign drive + mute seams; registration/contract gates; payload campaign | All G1–G6 mechanics confirmed feasible on existing owners; S5–S13 discovery invisibility found (real HEAD defect); mute drivable via real UI switch, zero new production code | emit contract |
| 2026-08-17 am | Planner | tier-matrix, sufficiency checklist, AGENTS.md:41-43, live `adb devices` | Pinned pair live (21071FDF600CSC + emulator-5554); plan emitted | /tdd-review |
| 2026-08-17 pm | /tdd-review (2-worker adversarial audit + lead source verification) | foreground FCM drain path, bg suppressed events, group listener post-371 wiring, mute reconciliation seam, discovery filter, orchestrator arg loop, probe signature, fakes | Verdict plan-fixes-required: 6 plan-fixes + 2 over-engineering cuts (see Reviewer Findings); no refuted core bet | apply deltas |
| 2026-08-17 pm | Planner (revision) | this file | All review deltas applied; scenario register renumbered S1–S16 (announcement-suppression device leg cut; backgrounded leg is S16) | execution |

## Problem And Evidence
- Behavior to improve: the notification **sound contract** (audible high channel vs silent-visible low channel vs fully suppressed) and the policy gates (same-chat suppression for groups, muted-group full effect, dual-path single-alert, backgrounded-but-connected delivery) are host-proven but **never asserted by any device/e2e verification** on the connected Android pair.
- Impact: the exact regression class the audible/silent split can produce (every card silently landing on the wrong channel; a muted group alerting; a dual-path double alert) would pass every existing device gate today.
- Confirmed current gaps (each survived the refute pass and the independent /tdd-review audit):
  - **G1** `buildVerdict` ignores the recorded `silent` flag — `integration_test/notification_sound_smoke_harness.dart:1095-1148` (flag recorded at `:362-378`, captured from the wrapper's own `silent` PARAM at `:330` before delegating to the real service). Orchestrator OS check accepts **either** channel — `integration_test/scripts/run_notification_sound_smoke.dart:505-507`; audible confirmation is a manual prompt (`:665-678`) skipped under `--non-interactive` (`:137-147`).
  - **G2** no scenario exercises the 30s tone window (`lib/core/notifications/notification_tone_tracker.dart:34-43`; durable lease window `lib/core/notifications/durable_notification_tone_lease.dart:349`; background reserve `lib/features/push/application/background_message_handler.dart:955-971`). The harness listener uses the real file-backed lease by default (`lib/features/conversation/application/chat_message_listener.dart:166-168`).
  - **G3** same-chat suppression e2e exists only for 1:1 (S4 sets `chatConversationTracker.setActive` at harness `:1286-1288`). **Post-371 reality (found by review):** `GroupMessageListener`'s `groupConversationTracker` param is a discarded compat input — "Notification presentation never consults it" (`lib/features/groups/application/group_message_listener.dart:313-315`) — and the legacy suppression gate **fails open** when the visibility reader is null (`:688-689`, `_maySuppressGroupNotification` returns false). The harness passes no `appVisibility:` (`notification_sound_smoke_harness.dart:955-964`), so today group suppression is *impossible* in the harness while group notifications still fire (legacy outbox-null show lane). A group suppression scenario therefore requires one wiring edit (TC-378-06).
  - **G4** no full-effect muted-group Android campaign. Only the SQLCipher `is_muted` projection hop is device-proven (`integration_test/group_mute_notification_db_proof_test.dart`). Production seams all exist: policy gate `lib/features/push/application/group_notification_display_policy.dart:109-110`; retire-on-mute IS production behavior — `updateGroup` emits `emitGroupNotificationReconciliationSignal` (`lib/features/groups/data/repositories/group_repository_impl.dart:533`, `:591`, `:618`) and the reconciler cancels the muted generation, host-locked at `test/features/groups/application/group_notification_reconciliation_wiring_test.dart:172-197`; badge exclusion `lib/core/database/helpers/canonical_notification_badge_state_db_helpers.dart:138`; mute switch UI `lib/features/groups/presentation/screens/group_info_screen.dart:495-559`.
  - **G5** payload campaign asserts `a6.no_duplicate_render` but never "at most one audible" nor the losing path's typed suppression (`integration_test/scripts/notification_android_payload_campaign.dart` — zero silent/audible/channel assertions; dumpsys capture exists at `:1147-1150` and targets the **emulator** device).
  - **G6** backgrounded-but-connected delivery is asserted only via a hand-rolled fake service call log (`integration_test/notification_open_during_other_chat_harness.dart:117-187`, `:729-755`) — no OS-level record assertion anywhere.
  - **G-reg (found during grounding, re-confirmed live during review)** the discovery gate registers only S1–S4: the awk at `scripts/check_reliability_simulation_discovery.sh:1019-1039` cannot parse the S5–S13 map-loop emission (`run_notification_sound_smoke.dart:931-956`, `id: entry.key` — no quoted literal). Live `--checks-tsv` run on HEAD → exactly 4 rows; the gate still exits 0 (invisible-but-green).
- Existing coverage: `test/features/push/application/show_notification_use_case_test.dart` (56 tests today, incl. exact direct/group suppression `:1153-1186`), `test/core/notifications/notification_tone_tracker_test.dart`, `test/core/notifications/durable_notification_tone_lease_test.dart`, `test/core/notifications/local_notification_support_test.dart`, channel pins in `test/core/notifications/flutter_notification_service_test.dart` (`:214` audible default channel, `:385-405` silent channel), mute host rows `test/features/groups/application/group_message_listener_test.dart:16483+` (muted suppression persists message), `:17033+` (muted ADD reaction does not notify) and `test/features/push/application/background_message_handler_test.dart:4683-4742`, badge fixture `test/core/database/helpers/canonical_notification_badge_state_db_helpers_test.dart:186-239`; device campaigns `notifications.android_payload_campaign`, `groups.reaction_notification_campaign`, sound smoke S1–S13 (presence/copy only).
- Missing coverage: everything under "Confirmed current gaps" — device-tier disposition, tone window, group same-chat suppression, mute full effect, dual-path audible-once, backgrounded-connected OS record, S5–S13 discovery rows.
- Refuted findings (do NOT re-introduce):
  - "A new production E2E mute action is required for G4" — refuted: the real Group-Info switch is uiautomator-drivable; **tap mechanics nuance (review-verified):** the layout is a plain `Row` (no `SwitchListTile`) — the "Mute Notifications" `Text` (`group_info_screen.dart:528-529`, l10n `app_en.arb:701`) is find-able by text, but the `Switch.adaptive` (`:550-555`) exposes no text/content-desc; the capture stage must tap the checkable/`android.widget.Switch` node in that row's vertical band (or a bounds offset), never the label center.
  - "The reaction campaign parses the notification channel" — refuted: `ActiveNotificationCard` has no channel field (`reaction_notification_proof_support.dart:5714-5729`, `:6338-6364`); muted rows assert **zero cards / card-count**, never channel, in that campaign.
  - "Flow events are unavailable on the production_fcm build" — refuted: `android.production_fcm` is a provider-configured **debug** APK with `E2E_TEST_MODE=true` (`tool/sims/critical_features.json:53-59`), so `flowEventLoggingEnabled` (kDebugMode default) is on.
  - "assertions arrays in `critical_features.json` are machine-enforced against scenario counts" — refuted: `manifest.dart:381-393` checks non-empty only; `executor.dart:651-664` never compares counts.
  - "`open_during_other_chat` proves OS-level backgrounded delivery" — refuted: fake service, call-log only.
  - **(review)** "Mute never retires the current card in the live path" — refuted: signal-driven reconciliation exists (`group_repository_impl.dart:533` → reconciler cancel, wiring test `:172-197`). Kept host-owned in this plan because the UI-driven toggle confounds device attribution (see TC-378-08).
  - **(review)** "The sound smoke's S2/S3/S8–S13 baseline is broken post-371" — refuted: the harness runs the **legacy outbox-null show lane**, whose null-reader gate fails OPEN (`group_message_listener.dart:688-689`), so group notifications still fire; only *suppression* is impossible without the TC-378-06 wiring.
  - Do not re-litigate closed OQ decisions: same-chat cue stays **no cue** (OQ-01); mute scope stays per-group installation-local (OQ-05).
- Unresolved findings:
  - Whether the dumpsys record's `Notification(channel=…)` reflects the **silent** channel after the same-ID in-place update on both API levels (TC-378-05 fallback assertion is mandatory either way — see Test Notes).
- Affected production / test / gate files: `integration_test/notification_sound_smoke_harness.dart`, `integration_test/scripts/run_notification_sound_smoke.dart`, `scripts/test/notification_sound_disposition_contract_test.sh` (new), `scripts/test/notification_tap_campaign_adapter_contract_test.sh`, `integration_test/scripts/run_notification_tap_device_real.dart`, `integration_test/scripts/notification_android_payload_campaign.dart`, `integration_test/scripts/group_reaction_notification_device_criteria.dart`, `integration_test/scripts/capture_group_reaction_notification_device.dart`, `integration_test/scripts/run_group_reaction_notification_sims.dart`, `integration_test/scripts/validate_group_reaction_notification_artifacts.dart`, `integration_test/group_announcement_reaction_notification_proof_test.dart`, `lib/core/debug/group_reaction_e2e_probe.dart` (debug-only, badge observation), `tool/sims/critical_features.json`, plus one new host test `test/core/debug/group_reaction_e2e_probe_badge_observation_test.dart`.

## Graph Grounding Snapshot
- Graph fingerprint / freshness: `c096affcb7765d9f`; current (`.needs_incremental_refresh` absent).
- Query / profile: `python3 graphify-arch/tdd_context.py query "notification_sound_smoke_harness expected channel mknoon_messages_silent showMessageNotification silent verdict dumpsys" --profile tdd --budget 700` (plus the session's general-profile query on `NotificationToneTracker`/`maybeShowNotification` and a review-profile counterexample query).
- Anchors: `maybeShowNotification` → `lib/features/push/application/show_notification_use_case.dart:74`; `notificationToneTracker` → `lib/features/conversation/application/chat_message_listener.dart:107`; `showMessageNotification` → `test/shared/fakes/fake_notification_service.dart:18`.
- Surfaced proof/gate files: `test/features/push/application/show_notification_use_case_test.dart` (feature-host-all + 1to1 + groups lanes), `test/core/notifications/flutter_notification_service_test.dart` (core-host-all + both curated lanes).
- Graph gaps that required raw source search: all `integration_test/scripts/**`, `scripts/test/**`, and `tool/sims/**` mechanics (test/orchestration infrastructure outside the arch graph) — resolved by three verify→refute agents plus a two-worker review audit, all with file:line evidence.
- Reuse rule: anchors may be handed to review/execution as search starting points; every conclusion above carries current-source file:line evidence.

## Scope Contract And Guard
In scope:
- Sound-disposition contract: one machine-readable scenario→disposition table (S1–S16) in the sound-smoke orchestrator, enforced per scenario at the OS-capture verdict (exact expected channel, not either-channel), plus harness-side `silent`-flag assertions.
- New sound-smoke scenarios S14 (tone debounce), S15 (group same-chat suppression, incl. the one-argument harness group-listener visibility wiring it requires), S16 (logically-backgrounded-but-connected 1:1 delivery).
- Literal `_runScenario` blocks for S5–S13 so discovery registers all rows, with S11–S13 descriptions reworded to `Group announcement …` so the category filter admits them (fixes the G-reg HEAD defect at the coordinated count of 16).
- Two muted-group scenarios in `groups.reaction_notification_campaign` (foreground message + background-connected reaction) with unread-preserved, is_muted and badge-exclusion probes; mute toggled through the real Group-Info switch (bounds-tap on the Switch node).
- One dual-path scenario in `notifications.android_payload_campaign` (`tc_b13_dual_path_single_alert`) on a **backgrounded-but-connected emulator recipient**, asserting one card, one audible channel, the losing path's typed suppression via a discriminator **union**, and dual-attempt evidence.
- A badge-observation extension of the debug-only SQLCipher probe (`group_reaction_e2e_probe.dart`) + focused host test (probe injectability review-confirmed: `observeGroupReactionE2EState({required Database database, …})` at `:167-171`; ffi precedent `test/core/debug/group_reaction_e2e_probe_test.dart`).

Must preserve:
- Existing S1–S13 pass semantics (identity/copy/payload checks) → the tightened run itself plus `GREEN sentinel` TC-378-13. (Review re-verified the baseline is intact on HEAD: legacy show lane fires with a null visibility reader.)
- Existing 4 payload-campaign scenarios and 5 reaction-campaign scenarios byte-listed and passing → TC-378-13 (adapter contract sh; campaign runs).
- Host decision-logic guards (56-test use-case file, tone tracker, lease, channels + service channel pins, mute rows, badge fixture) → TC-378-13.
- Sims manifest validity and capture-owned validator bindings → `flutter test test/tool/sims/sims_manifest_test.dart test/tool/sims/sims_proof_binding_registry_test.dart`.

Hard `Do not`:
- Do not change any production notification behavior: no edits to `show_notification_use_case.dart`, channels, tone windows, mute policy, or listeners. The only `lib/` edit permitted is the kDebugMode/E2E-gated probe observation extension (read-only projection). (Mutations named in the contract are executed-and-byte-restored probes, never landed.)
- Do not add a new mute E2E production action/verb; do not invent a same-chat cue, a mention lane, or linked-device clearing (OQ-01/04/05 closed dispositions).
- Do not parse/assert channels in the reaction campaign's shared `ActiveNotificationCard` parser (zero-card assertions suffice there).
- Do not pass `-d` during any HEAD RED probe of the orchestrator — with a device pair attached the orchestrator starts a real two-device run (`run_notification_sound_smoke.dart:747-754` only exits when devices ≠ 2).
- Do not run `graphify label`; do not run graphify build/update from inside `graphify-arch/`.
- Do not treat the S16 logical-lifecycle leg as proof of true process-suspension delivery — that boundary stays owned by the payload campaign's b12 rows.

Deferred / accepted difference:
- **G7** Doze / notification-permission-denied / channel-disabled / token-refresh / OEM matrix → deferred, named owner: GAP-N12 device-evidence program (PRD §13 debt).
- iOS presentation legs → consolidated GAP-N12 iOS closure phase per adopted §9.1 sequencing.
- True-suspension/Doze delivery for S16's behavior → owned by `notifications.android_payload_campaign` b12 rows; S16 proves only the still-live-connection + non-foreground-active seam (PRD G-06 shape).
- **Announcement same-chat suppression device leg** → deliberately NOT duplicated (review over-engineering cut): the device suppression seam is byte-identical to S15's — lane identity is prefix-based `group:` for chat and announcement alike (`show_notification_use_case.dart:105-110`) — so no defect can separate them there; group-type policy divergence is host-owned (`group_notification_display_policy` rows). Announcement lane device coverage = S3 (audible) + S11–S13 (media consistency).
- **Stale-visible-thread freshness device leg** → deliberately NOT added (review over-engineering cut): the harness's visibility reader is the `TrackerBackedAppVisibility` fake, whose `maySuppress` short-circuits to false whenever lifecycle ≠ resumed (`test/shared/fakes/fake_app_visibility.dart:21-27`), so a paused+tracker-set leg cannot fail for the freshness reason; that seam stays host-owned (`test/core/notifications/` app-visibility tests).
- **Device-tier retire-on-mute attribution** → host-owned: the reconciliation semantics are locked by `group_notification_reconciliation_wiring_test.dart:172-197`; the UI-driven device flow cannot attribute card disappearance to mute (opening the group to reach the switch already clears the card via open-cleanup), so TC-378-08 records the post-toggle card state as **non-attributing observation**, not an assertion.
- **Pure-FCM background-isolate muted read on device** (`PUSH_BACKGROUND_NOTIFICATION_SUPPRESSED` reason `muted`, `background_message_handler.dart:694-705`) → asserted opportunistically via cursor-scoped logcat when FCM wins the race in TC-378-09; otherwise remains host-owned (`background_message_handler_test.dart:4683-4742`). The background-connected recipe keeps the live bridge up, so the deterministic device assertion is the ingress-processor path.

Dependencies:
- `android.production_fcm` build profile + staging relay (campaign rows); e2e sound-smoke build (harness rows). No upstream plan dependency; no DB migration; no relay/wire change.

## Test Contract
Zero empty cells. `HEAD state` is one of `causal RED` · `GREEN sentinel` · `manual/device-only proof`.

| Case | Behavior | Named test/proof | Tier / fixture | HEAD state → GREEN | Mutation that re-reds | Gate / registration |
|---|---|---|---|---|---|---|
| TC-378-01 | One pinned scenario→disposition table (S1–S16): S1/S2/S3/S16 audible-strict, S4/S15 suppressed, S5–S13 decision-consistency, S14 tone-debounce pair | `scripts/test/notification_sound_disposition_contract_test.sh::case1_print_disposition_contract` (byte-compares `dart run integration_test/scripts/run_notification_sound_smoke.dart --print-disposition-contract`) | host process-contract / no fixture | causal RED (unknown flags are silently ignored — arg loop `:731-746` has no unknown-flag branch; the deviceless invocation exits **1** via the usage path `:747-754` and emits no table; RED asserts nonzero exit AND absent table, never a specific 64, and never passes `-d`) → byte-exact table emitted | flip S1's table entry to `silent` → cmp red | `bash scripts/test/notification_sound_disposition_contract_test.sh`; AUTO by `*_test.sh` glob (`run_test_gates.sh:1712`) into `./scripts/run_test_gates.sh sims-contracts`; verify `./scripts/run_test_gates.sh sims-contracts --list` lists it (self-discovery pinned by `scripts/test/sims_test_gate_contract_test.sh:94-105`) |
| TC-378-02 | OS-capture validation FAILS when the observed channel contradicts the scenario's disposition — **capped surface**: one pure decision function (scenario disposition + parsed record → verdict) reused by the live path; offline `--verify-os-capture <scenario> <dump> <verdict-json>` mode; ≤3 canned dumps inlined as heredocs in the sh; output = exit code + one reason line (no fixture directory, no schema) | same sh contract, `::case2_verify_os_capture_fixtures` | host process-contract / heredoc dumpsys fixtures | causal RED (offline mode absent on HEAD — flag ignored, deviceless exit 1, no verdict line; and HEAD predicate `:505-507` accepts either channel) → contradiction fixture (audible-expected + silent-channel record) exits nonzero with typed reason; audible fixture passes; suppressed-expected + any record fails | restore the either-channel predicate `channel == 'mknoon_messages' \|\| channel == 'mknoon_messages_silent'` → contradiction fixture passes → case red | same as TC-378-01 |
| TC-378-03 | Harness verdict asserts the recorded `silent` flag sequence per scenario (single-call scenarios: exact expected flag; S14: exactly `[false, true]`) and `programmaticPass` fails on mismatch | `integration_test/notification_sound_smoke_harness.dart::Bob(Notif) — S1..S16` (buildVerdict extension: `silentFlagsMatch`) | device harness / real `FlutterNotificationService` recording wrapper | manual/device-only proof (flag recorded but unasserted on HEAD `:1095-1148`) → verdict JSON `silentFlagsMatch=true` required for pass | representative device mutation (byte-restored): force `silent = true` after the tone decision inside `maybeShowNotification` (`lib/features/push/application/show_notification_use_case.dart:303-326` region) → flips the RECORDED flag AND the posted channel → TC-378-03 and TC-378-04 red together (a service-internal silent hardcode would NOT flip the recorded flag — the wrapper records the upstream param at harness `:330`) | runs inside every orchestrator row; verdict schema pinned by TC-378-01's table |
| TC-378-04 | S1/S2/S3 first-message audible on device: exactly one OS record, channel == `mknoon_messages` (strict), `silent == false`, copy/identity checks preserved | orchestrator OS verdict for rows S1,S2,S3 (`_captureAndroidNotificationState` + tightened `channelMatchesExpected` via the TC-378-02 decision function) | device proof / pinned pair, e2e build, real bridge+relay | manual/device-only proof (HEAD passes even if all cards land silent) → all three rows pass with strict audible channel | same mutation as TC-378-03 (use-case silent decision) → S1/S2/S3 red on channel AND flag | `dart run integration_test/scripts/run_notification_sound_smoke.dart -d 21071FDF600CSC,emulator-5554 --non-interactive` exit 0, summary JSON `programmaticPass=true` per row; rows registered via literal `_runScenario` blocks (discovery awk `check_reliability_simulation_discovery.sh:1019-1039`) |
| TC-378-05 | Tone-window debounce (G2): two 1:1 texts <30s apart in one conversation → first audible; second is a silent in-place update — same stable notification id, channel `mknoon_messages_silent`, `silent=true`, no second record id; orchestrator enforces the <30s send gap else the row fails as inconclusive. **The `[false,true]` flags assertion is non-negotiable in BOTH the primary and the fallback assertion sets** (it is the load-bearing exclusion of the second-message-suppressed degenerate pass) | new scenario S14 (`id: 'S14'`, description contains `1:1`), two-phase OS capture | device proof / pinned pair; real file-backed `DurableNotificationToneLease` (default resolver `chat_message_listener.dart:166-168`) | manual/device-only proof (scenario absent on HEAD) → S14 verdict pass incl. same-id relationship assertion | set `toneWindow` to `Duration.zero` at `durable_notification_tone_lease.dart:349` (byte-restored) → second message audible → S14 red (also re-reds `durable_notification_tone_lease_test.dart`) | orchestrator run (same command as TC-378-04); discovery row S14 present (`--checks-tsv`) |
| TC-378-06 | Group same-chat suppression (G3): **pre-req wiring edit** — construct the harness group listener with `appVisibility: TrackerBackedAppVisibility(tracker: groupConversationTracker, lifecycle: () => currentLifecycle)` (mirror of the chat wiring at `:1047-1050`), because the tracker param alone is discarded (`group_message_listener.dart:313-315`) and the legacy gate fails open on a null reader (`:688-689`) — without the wiring the suppressed leg reds immediately. Then: Bob resumed with `groupConversationTracker.setActive('group:<id>')` → incoming group text yields zero `showMessageNotification` calls AND zero OS records (6s settle, S4 pattern); after `clear()`, a follow-up message notifies (in-scenario control). Announcement device leg deliberately not duplicated (identical prefix-based seam, see Scope) | new scenario S15 (description contains `Group`), harness group-tracker leg + the named wiring edit | device proof / pinned pair | manual/device-only proof (no group suppression scenario exists on HEAD; suppression currently impossible in-harness) → S15 passes, control leg notifies | scenario-side: omit `setActive` → suppression leg red (card appears — legacy gate consults the wired tracker). Production gate causally guarded at host by `show_notification_use_case_test.dart:1153-1186` | orchestrator run; discovery row S15 under `group` family |
| TC-378-07 | Backgrounded-but-connected 1:1 (G6, PRD G-06/AC-04 shape): Bob logical lifecycle → `paused` (mutable `currentLifecycle`), **no active conversation**; Alice sends over the still-live bridge; real service posts exactly one OS record on `mknoon_messages` (31s tone cooldown precedes the leg). Single leg only — the paused+tracker-set freshness variant is cut (fake short-circuits on paused, `fake_app_visibility.dart:21-27`; freshness stays host-owned) | new scenario S16 (description contains `1:1`); harness makes `currentLifecycle` mutable (logical-lifecycle recipe of `notification_open_during_other_chat_harness.dart:729-755`, adopted with its documented pump-deadlock rationale) | device proof / pinned pair; real `FlutterNotificationService` + dumpsys (unlike the fake-service sibling harness) | manual/device-only proof (no OS-level assertion exists on HEAD) → leg passes; explicit label: logical-lifecycle boundary — true suspension stays owned by payload-campaign b12 rows | scenario-side: set lifecycle back to `resumed` with the tracker on Alice's conversation → suppression occurs → expected-shown assertion red | orchestrator run; discovery row S16 under `1to1` |
| TC-378-08 | Muted-group message full effect, foreground/live path (G4), **confound-free sequencing**: (1) recipient at HOME; msg1 → card present (control); (2) open group → Group Info; toggle mute via **bounds-tap on the Switch node** in the label's row band (label text found via "Mute Notifications"; the Switch exposes no text/content-desc — `group_info_screen.dart:528-555`); post-toggle card state recorded as **non-attributing observation** (open-cleanup already clears the card; retire-on-mute semantics are host-owned by `group_notification_reconciliation_wiring_test.dart:172-197`); (3) **navigate back to HOME and settle BEFORE msg2**; (4) msg2 → zero new cards at HOME; SQLCipher probe: msg2 row present, `read_at` NULL for msg2, `groups.is_muted=1`, canonical badge projection excludes the group | new campaign scenario `group_muted_message_suppression_lifecycle` in `group_reaction_notification_device_criteria.dart` + capture stage branch | device campaign / `android.production_fcm` + staging relay, pinned pair | manual/device-only proof (no full-effect mute campaign exists; coverage doc §6.5 names this exact debt) → scenario artifact `status: passed` | production mutation (byte-restored): drop the `isMuted` early return at `group_notification_display_policy.dart:109-110` → msg2 card appears **at HOME** (visibility cannot mask it) → scenario red; host mute rows re-red the same seam (`group_message_listener_test.dart:16483+`) | `.claude/skills/sims/scripts/run_with_devices.sh major --only groups.reaction_notification_campaign` exit 0 + artifact validator PASS; registration = 7-surface checklist (criteria const; capture stage branch; `groupReactionE2EAndroidScenarios` allow-list `group_reaction_e2e_probe.dart:18-30`; named `test('<id>')` block in `group_announcement_reaction_notification_proof_test.dart` — exit-79 landmine; `assertions` array `critical_features.json:751`; staging-manifest path `run_group_reaction_notification_sims.dart:433-442`; adapter prose `:79-81`) |
| TC-378-09 | Muted-group reaction suppression on the background-connected recipient (G4): muted group; recipient in the existing background-connected stage; sender reacts to a recipient-authored target → zero cards; probe: reaction row persisted. **Live-path seam = the reaction ingress processor** (`group_message_listener_reaction_ingress_processor.dart:~458` reads the same policy) — the connection stays live in this recipe, so the FCM/bg-isolate muted read (`background_message_handler.dart:694-705`, message-path resolver gated `type=='group_message'` at `:2995`) is asserted opportunistically via cursor-scoped logcat only when FCM wins the race; otherwise it remains host-owned (`background_message_handler_test.dart:4683-4742`). The existing unmuted scenario `group_reaction_background_connected_recipient` remains the live control | new campaign scenario `group_muted_reaction_background_suppression` (same registration surfaces) | device campaign / `android.production_fcm`, pinned pair | manual/device-only proof → artifact `status: passed` | same policy-gate mutation family as TC-378-08 (ingress processor reads the same policy); host re-red `background_message_handler_test.dart:4683-4742` | same campaign command + validator; same 7-surface checklist |
| TC-378-10 | Badge observation is computed by the production helper (not a hand-rolled query): probe observation gains `canonicalBadgeState` and a muted group is excluded from it. **Injectability resolved-positive by review:** `observeGroupReactionE2EState({required Database database, required SecureKeyStore secureKeyStore, required GroupReactionE2EProbeRequest request})` at `group_reaction_e2e_probe.dart:167-171`; ffi precedent `test/core/debug/group_reaction_e2e_probe_test.dart` (opens `inMemoryDatabasePath` via `databaseFactoryFfi`, calls the probe) | `test/core/debug/group_reaction_e2e_probe_badge_observation_test.dart::muted group excluded from canonical badge observation` | host / `sqflite_common_ffi` (real migrations in setUp; **plain SQLite — cipher hop closes on the device probe inside TC-378-08**) | causal RED (field absent from `observeGroupReactionE2EState` on HEAD) → field present and muted group excluded | replace the helper call with a raw query lacking `AND g.is_muted = 0` (`canonical_notification_badge_state_db_helpers.dart:138`) → red | `flutter test test/core/debug/group_reaction_e2e_probe_badge_observation_test.dart`; AUTO (`test/core/**` glob, core-host-all); `./scripts/run_test_gates.sh completeness-check` must stay 0-unmatched |
| TC-378-11 | Dual-path live+FCM single alert (G5), **review-corrected topology**: recipient = the **emulator** (its dumpsys is what the campaign captures, `notification_android_payload_campaign.dart:1147-1150`), **backgrounded-but-connected** (foreground FCM never reaches a notification decision — `handle_foreground_remote_message_use_case.dart:13-20` drains with `needsNotification:false` and emits no suppression event); sender sends marker; real FCM wake also fires. Checks: `b13.single_card` (exactly one active card bearing the marker), `b13.single_audible_channel` (that card's raw-dump `Notification(channel=` == `mknoon_messages`), `b13.losing_path_typed_suppression` via the **discriminator union** — `NOTIFICATION_SUPPRESSED × {recent_remote_push \| message_event_already_claimed}` when the live path loses (`show_notification_use_case.dart:152-172`, `:209-230`; the LEGACY_*_RECONCILE variants require a durable effect context the 1:1 live listener never passes) OR `PUSH_BACKGROUND_NOTIFICATION_SUPPRESSED × {message_event_already_claimed \| <exact sibling reasons pinned at execution from bmh:694-705/:797/:937-951/:1614>}` when the FCM leg loses — plus `b13.dual_attempt` (cursor-scoped logcat markers proving BOTH legs attempted: the live listener's arrival marker AND the FCM receipt marker, exact strings pinned at execution) with a bounded wait, else the leg fails as inconclusive-red | catalog row `tc_b13_dual_path_single_alert` (TC-B13) + campaign leg; host causal RED via the **pre-updated** adapter contract list | host (adapter contract) + device campaign / `android.production_fcm`, pinned pair | causal RED at host: update `scripts/test/notification_tap_campaign_adapter_contract_test.sh:22-28` expectation FIRST (byte-exact `cmp -s`) → `--list-scenarios` red on HEAD until the catalog row exists → GREEN; device leg manual/device-only proof | remove `tc_b13_dual_path_single_alert` from `_androidPayloadCampaignIds` (`run_notification_tap_device_real.dart:104-109`) → adapter contract red; device sensitivity: flip expected card count to 2 → red. Production dedupe seam host-guarded (use-case dedupe rows, TC-378-13) | `bash scripts/test/notification_tap_campaign_adapter_contract_test.sh` exit 0; `run_with_devices.sh major --only notifications.android_payload_campaign` exit 0 + validator; registration checklist: `_Scenario` catalog `:25-102`; campaign ids `:104-109`; leg + `_writeScenarioArtifact`; `_assertionsAttempted` ladder `notification_android_payload_campaign.dart:437,540,667` + literal 4s `:269,274`; `_removeScenarioArtifacts` `:1489-1495`; `assertions` array `critical_features.json:667` |
| TC-378-12 | Discovery census: ALL sound-smoke rows registered — **16 rows, S1..S16** — in `--checks-tsv` under the correct families (fixes the S5–S13 invisibility defect). S11–S13's literal blocks MUST be reworded `Group announcement image-only/video-only/voice-only message` (current `announcement …` strings match neither filter arm — `check_reliability_simulation_discovery.sh:1010,:1013` — and would yield 13 rows) | sh contract `::case3_discovery_rows` + `scripts/check_reliability_simulation_discovery.sh` itself | host process-contract | **causal RED on HEAD**: `./scripts/check_reliability_simulation_discovery.sh --checks-tsv \| GRAPH_OK=1 grep -cE 'run_notification_sound_smoke\.dart'` returns **4** today (live-verified during review) → 16 rows | author S14 in map-loop style, or keep an `announcement …` description without `Group` → row absent → red | `./scripts/check_reliability_simulation_discovery.sh` exit 0; `flutter test test/tool/sims/sims_proof_binding_registry_test.dart` (shells the checker; `test/tool` is host-all-only → direct command) |
| TC-378-13 | Preservation: existing decision-logic and campaign guards stay green — use-case file (currently 56 tests); tone tracker + durable lease + channel-support tests; service channel pins (`flutter_notification_service_test.dart:214`, `:385-405`); mute host rows (`group_message_listener_test.dart:16483+`, `background_message_handler_test.dart:4683-4742`); badge fixture; existing 4 payload + 5 reaction scenarios; sims manifest + proof-binding registry | `flutter test test/features/push/application/show_notification_use_case_test.dart test/core/notifications/notification_tone_tracker_test.dart test/core/notifications/durable_notification_tone_lease_test.dart test/core/notifications/local_notification_support_test.dart test/core/notifications/flutter_notification_service_test.dart test/tool/sims/sims_manifest_test.dart test/tool/sims/sims_proof_binding_registry_test.dart` + both campaign runs + `bash scripts/test/notification_tap_campaign_adapter_contract_test.sh` | host + device | GREEN sentinel (all pass today) → still pass | guarded by the causal/device mutations above: TC-378-03/04's use-case silent-decision mutation re-reds the use-case tone/silent rows; TC-378-05's toneWindow-zero re-reds the lease test; a channel-mapping mutation (`local_notification_support.dart:85`) re-reds the service pins AND TC-378-04's consistency class; TC-378-08/09's policy-gate mutation re-reds the mute host rows; TC-378-11's id-removal re-reds the adapter contract | commands as named; zero failures |

## Test Notes
- TC-378-01 table semantics: `audibleStrict` = exactly one record on `mknoon_messages` + `silent=false`; `suppressed` = zero calls + zero records; `consistency` (S5–S13) = for each shown call, OS channel must equal `silent ? mknoon_messages_silent : mknoon_messages` (`local_notification_support.dart:85` mapping) — S5–S13 stay timing-free because consecutive same-lane scenarios legitimately fall inside the 30s window; `toneDebounce` (S14) = flags `[false,true]` + same-id relationship.
- S14/S16 determinism: a 31s cooldown precedes each (window anchors to last audible; `notification_tone_tracker.dart:31-33`). S14's orchestrator asserts the msg1→msg2 send gap <30s or fails the row as inconclusive-red (never silently passes).
- S14 fallback if the in-place update's dumpsys channel proves API-inconsistent: the conclusive assertion set degrades to `shownCalls == [false,true]` + record count == 1 + same id — **the flags assertion is mandatory in both modes** (it alone excludes the second-message-suppressed degenerate pass; count+same-id alone would not). Record the deviation in the execution log; the channel assertion stays primary on the emulator leg.
- S15 signals: `bob_viewing_group` + `s15_go`, mirroring `bob_viewing_conversation` (`:1288`, Alice wait `:768-775`). S15's expected flags for the post-clear control call: `[false]` (first tone on that fresh group key).
- New scenario descriptions must satisfy the discovery category filter (`check_reliability_simulation_discovery.sh:1005-1015`): contain `1:1`/`conversation` for 1to1, `group` for group — S14/S16 use "1:1 …", S15 "Group discussion …", and the reworded S11–S13 use "Group announcement …" (orchestrator-side labels only; notification copy is untouched).
- TC-378-08 evidence kinds reuse the existing vocabulary (`sqlcipher_state`, `android_notification_records`, `ui_automation`, `recipient_app`, `sender_app` — `group_reaction_notification_device_criteria.dart:264-305`); artifact key sets are exact-match, so declare only required kinds.
- TC-378-08 switch tap: uiautomator locates the "Mute Notifications" text node, then taps the checkable/`android.widget.Switch` node sharing that row's vertical band (or a right-edge bounds offset within the label's y band) — never the label center (plain `Row`, no row-level gesture handler). Post-toggle truth is read from the SQLCipher `is_muted` probe, not the UI.
- TC-378-11 FLOW capture: the campaign already tails logcat with a cursor (`_deviceLogcatCursor`; `logcat -c` is banned by the adapter contract) — the discriminator-union and dual-attempt greps must be cursor-scoped to the leg. Pin the exact FCM-loses reason strings from `background_message_handler.dart`'s four `PUSH_BACKGROUND_NOTIFICATION_SUPPRESSED` sites (`:697,:797,:940,:1614`) during execution.
- Optional hardening (review note, ~3 lines): at each scenario start assert `recording.shown.length` equals the previous scenario's post-verdict length, closing the inter-scenario blind window that per-scenario `baselineCount` leaves open.
- Sound smoke on the physical device uses `flutter test -d` (debug install): it **destroys a stamped release install**. Run the sound-smoke rows BEFORE the campaigns, or restore via the state-guard backup + `adb install` afterward; the campaigns re-install their own stamped builds through `AndroidAppStateGuard.prepareFreshInstall`.

## Implementation Steps
1. Snapshot `git status --short`. Author TC-378-01/02/12's sh contract and pre-update the tap adapter list (TC-378-11) BEFORE any orchestrator/campaign edits; run both → record the causal REDs (deviceless orchestrator invocation: exit 1 + no table/verdict output — never pass `-d`; adapter `cmp` missing `tc_b13`; discovery grep = 4 rows).
2. Orchestrator (`run_notification_sound_smoke.dart`): add the disposition table + `--print-disposition-contract` + the capped offline `--verify-os-capture` mode (one pure decision function reused by the live path); enforce per-scenario expected channel in `_captureAndroidNotificationState` (replace the either-channel predicate `:505-507`); convert S5–S13 to literal `_runScenario` blocks **with S11–S13 reworded to `Group announcement …`**; add S14–S16 legs (cooldowns, S14 two-phase capture, S16 paused leg); extend `_allScenarioIds` (`:74-88`), `laneFor` (`:995-1000`), prose (`:10`, `:164`, `:765`). Stop-if: any existing S1–S13 semantic other than the verdict tightening would have to change — that means the table is wrong, replan.
3. Harness (`notification_sound_smoke_harness.dart`): **wire the group listener's visibility reader** — add `appVisibility: TrackerBackedAppVisibility(tracker: groupConversationTracker, lifecycle: () => currentLifecycle)` to the `GroupMessageListener` construction at `:955-964` (mirror of the chat wiring at `:1047-1050`; without it S15 cannot suppress — the tracker param is discarded and the legacy gate fails open). Make `currentLifecycle` mutable; add S14–S16 Bob/Alice legs (group tracker setActive/clear; paused leg; dual-call S14 verdict); extend `buildVerdict` with `silentFlagsMatch`; widen `S1..S13` labels to `S1..S16`.
4. Reaction campaign: add both scenario consts + capture stage branches with the **TC-378-08 confound-free sequencing** (HOME control card → open Group Info → bounds-tap the Switch → back to HOME + settle → msg2 → probes); extend the SQLCipher probe observation with `canonicalBadgeState` via the production helper (debug-only file); write TC-378-10's host test first (RED: field absent; injectability already confirmed — `Database` is a probe parameter).
5. Payload campaign: catalog row, campaign-ids set, dual-path leg on the **backgrounded-but-connected emulator recipient** (`_writeScenarioArtifact`; discriminator union + dual-attempt markers + bounded wait/inconclusive-red), `_assertionsAttempted` ladder + literal 4s, `_removeScenarioArtifacts`, `critical_features.json` assertions (+`b13.*`; +2 `groups.*` mute IDs; update adapter prose `run_group_reaction_notification_sims.dart:79-81`).
6. Perform every registration named in the contract rows; verify by grep/discovery/completeness commands (never family-gate `--list`).
7. Run focused GREENs → preservation sentinels → device closure runs in the Test-Notes order (sound smoke first). Byte-restore every device mutation immediately after its re-red is recorded; never mutate mid-lane.

## Risks And Blind Spots
- Tone-window nondeterminism on shared lanes → TC-378-01's `consistency` class + cooldown guards (S14/S16); inconclusive-red rule prevents silent false passes.
- Dual-path race nondeterminism → TC-378-11's dual-attempt evidence + discriminator union + bounded wait (a single-path run can never pass as a dual-path proof).
- Lifecycle / derived-state durability: N/A — no new production state; the probe extension is a read-only projection of persisted rows.
- Sibling-surface consistency: all three lanes (direct/group/announcement) get disposition rows; reaction + mute + background siblings covered by TC-378-08/09/11; the announcement suppression device leg and the freshness leg are deliberate, evidence-backed cuts (see Scope); the iOS sibling surface is deferred to GAP-N12 with an owner.
- Destructive-action side effects: new payload scenario id added to `_removeScenarioArtifacts` (stale-artifact trap, `notification_android_payload_campaign.dart:1489-1495`); `AndroidAppStateGuard` restoration untouched; sound-smoke debug-install destroys stamped builds → sequencing note in Test Notes.
- Invariant re-verification under new transitions: the mid-scenario mute toggle asserts the FULL post-transition state — msg2 zero-cards at HOME AND unread preserved AND `is_muted=1` AND badge excluded (TC-378-08); the post-toggle card state is recorded but non-attributing (retire-on-mute is host-owned).
- Construction/call-site census (re-derived at review, re-run at execution): `GRAPH_OK=1 grep -rn "mknoonConversationNotificationDetails(" lib/` → 4 occurrences / 3 files today (definition + `flutter_notification_service.dart:413,:675` + `background_message_handler.dart:1118`); `GRAPH_OK=1 grep -rn "showMessageNotification(" lib/` → 4 / 4 files (2 declarations + `show_notification_use_case.dart:436` + `background_push_notification_fallback.dart:509`). **Known-and-pinned exception, not an escape:** generic `showNotification` (`flutter_notification_service.dart:506-521`) posts the shared always-audible const for non-message notifications, host-pinned at `flutter_notification_service_test.dart:184`. No message-path site bypasses the helper.
- Build-artifact provenance: no new native artifact; campaign builds already provenance-stamped and verified by the existing state guard (`phone-build-provenance` policy).
- Permission / ACL verb symmetry: N/A — no ACL or permission surface changes (notification runtime-permission scenarios are G7 deferred debt).
- Fake side-effect fidelity: S16 deliberately lives in the sound-smoke harness because its wrapper DELEGATES to the real `FlutterNotificationService`; the `open_during_other_chat` fake (no-op initialize/clear) is not used for any new row. The `TrackerBackedAppVisibility` fake is used ONLY for tracker/lifecycle gating the production reader also performs; the freshness seam it cannot reproduce is explicitly host-owned (Scope).
- Composite-node / relationship assertions: S14 asserts the SAME notification id across the audible→silent update (relationship, not two independent finds); TC-378-11 asserts card-count==1 AND channel AND discriminator AND dual-attempt on the same leg.

## Gate Cadence
- Per-plan closure: the new sh contract cases (TC-378-01/02/12) + `bash scripts/test/notification_tap_campaign_adapter_contract_test.sh` + `./scripts/run_test_gates.sh sims-contracts` + TC-378-10's direct `flutter test` + preservation sentinels (TC-378-13 commands) + the three device closure runs. Curated lane: `./scripts/run_test_gates.sh groups` runs ONLY because one `lib/` file changes (`group_reaction_e2e_probe.dart`); no `core-host-all`/`feature-host-all` sweep is justified (no production behavior change).
- Graph-affected first: after the probe edit and BEFORE the groups lane — `python3 graphify-arch/tdd_context.py affected lib/core/debug/group_reaction_e2e_probe.dart --budget 600`, then run the named test files directly.
- Full `host-all` is **not** a per-plan gate. The next wave-level run is owned by the notification-program aggregate (WP-05–WP-07 / GAP-N12 consolidation wave), and one more at final rollout/release closure.
- Shared tests outside the feature/core globs: `flutter test test/tool/sims/sims_manifest_test.dart test/tool/sims/sims_proof_binding_registry_test.dart` (test/tool is host-all-only) — direct commands in this plan; they stay registered for the later wave `host-all`.
- After the coherent edit set: `/claude-host-bin/host-run bash ./graphify-arch/refresh_arch_graph.sh --incremental` once (graphify CLI is host-only).

## Acceptance Gates  (literal — copy/paste)
```bash
# Dirty-tree snapshot before execution
git status --short

# Causal REDs (BEFORE orchestrator/campaign edits) — each must fail for its documented reason
bash scripts/test/notification_sound_disposition_contract_test.sh   # RED: deviceless orchestrator invocation exits 1 (usage path :747-754), no table/verdict emitted; NEVER pass -d in the RED probe
bash scripts/test/notification_tap_campaign_adapter_contract_test.sh # RED: tc_b13_dual_path_single_alert missing from --list-scenarios (cmp -s)
./scripts/check_reliability_simulation_discovery.sh --checks-tsv | GRAPH_OK=1 grep -cE "run_notification_sound_smoke" # baseline: 4 rows (S1-S4 only; live-verified at review)
flutter test test/core/debug/group_reaction_e2e_probe_badge_observation_test.dart # RED: canonicalBadgeState absent

# Focused GREEN (after the edits) — expect exit 0, zero failures
bash scripts/test/notification_sound_disposition_contract_test.sh
bash scripts/test/notification_tap_campaign_adapter_contract_test.sh
flutter test test/core/debug/group_reaction_e2e_probe_badge_observation_test.dart
./scripts/run_test_gates.sh sims-contracts        # all scripts/test/*_test.sh green, incl. both notification contracts

# Registration verification — grep/discovery/completeness, never family-gate --list
./scripts/check_reliability_simulation_discovery.sh                  # exit 0; zero unclassified
./scripts/check_reliability_simulation_discovery.sh --checks-tsv | GRAPH_OK=1 grep -cE "run_notification_sound_smoke\.dart" # expect 16 rows (S1..S16)
./scripts/run_test_gates.sh completeness-check                       # PASS, 0 unmatched
flutter test test/tool/sims/sims_manifest_test.dart test/tool/sims/sims_proof_binding_registry_test.dart # exit 0
.claude/skills/sims/scripts/run_with_devices.sh major --list         # lists groups.reaction_notification_campaign + notifications.android_payload_campaign

# Preservation sentinels — expect exit 0, zero failures
flutter test test/features/push/application/show_notification_use_case_test.dart \
  test/core/notifications/notification_tone_tracker_test.dart \
  test/core/notifications/durable_notification_tone_lease_test.dart \
  test/core/notifications/local_notification_support_test.dart \
  test/core/notifications/flutter_notification_service_test.dart

# Graph-affected BEFORE the curated lane (the one lib/ edit)
python3 graphify-arch/tdd_context.py affected lib/core/debug/group_reaction_e2e_probe.dart --budget 600
# then: flutter test <the test files it names>
./scripts/run_test_gates.sh groups                                    # affected curated lane, exit 0

# Device closure (pinned pair; sound smoke FIRST — its debug install destroys stamped builds)
adb devices                                                           # both 21071FDF600CSC and emulator-5554 present
dart run integration_test/scripts/run_notification_sound_smoke.dart -d 21071FDF600CSC,emulator-5554 --non-interactive
#   → exit 0; summary JSON: programmaticPass=true for S1..S16; S4/S15 zero records; S14 flags [false,true]
.claude/skills/sims/scripts/run_with_devices.sh major --only groups.reaction_notification_campaign
#   → PASS; both muted scenarios' artifacts validated (status: passed)
.claude/skills/sims/scripts/run_with_devices.sh major --only notifications.android_payload_campaign
#   → PASS; tc_b13 artifact validated (single card, audible channel, discriminator union + dual-attempt evidence); existing 4 scenarios preserved

# Hygiene
flutter analyze            # 0 new issues
git diff --check           # clean
/claude-host-bin/host-run bash ./graphify-arch/refresh_arch_graph.sh --incremental
```

## Execution Interpretation And Done Criteria
- Expected RED: the four causal REDs above, each for its stated mechanism (deviceless exit-1/no-output; missing catalog id; 4 discovery rows; absent probe field).
- GREEN sentinel: TC-378-13 command set, zero failures.
- Pre-existing dirty tree / known failure: the 9 `test/integration/**` notification/relay-degradation host-all failures and 13 perf reds predate this plan (worktree-verified in prior plans) — out of scope, not touched by any gate here.
- Environment blocker (NOT a product blocker): Pixel keyguard locked (uiautomator legs need an unlocked device — check `dumpsys window policy | grep secure` early); emulator absent from `adb devices`; staging-relay credentials unavailable. Hardware outside the live matrix is `N/A (target unavailable by project policy)`.
- Inconclusive-red (retry, not product failure): S14 send-gap ≥30s; TC-378-11 missing dual-attempt evidence within the bounded wait.
- Scope drift (BLOCKING): any production behavior change outside the debug-only probe projection; any red in an unrelated curated family.

- [ ] Every behavior (G1–G6 + G-reg) has a named test or a justified device proof; G7 is named deferred debt with an owner.
- [ ] Causal RED, focused GREEN, and representative mutation re-red are recorded (device mutations byte-restored immediately; never mid-lane).
- [ ] Preservation sentinels and named gates pass with semantic outcomes.
- [ ] Harness registration implemented AND verified (16 discovery rows, sims-contracts list, completeness-check, manifest/proof-binding tests).
- [ ] Device closure runs pass on the pinned pair in the Test-Notes order.
- [ ] `flutter analyze` no new issues; `git diff --check` clean; incremental graph refresh run once.
- [ ] The Scope Contract And Guard is respected (zero production notification-behavior edits).

## Device/Relay Proof Profile
- Profile: paired-device (plus staging relay for the two campaigns).
- Boundary being proven: real Android channel disposition of posted OS notifications (audible vs silent vs absent), real SQLCipher mute/badge/unread state, real FCM+live dual-path collapse, real notification-shade state — none provable at host tier.
- Live availability check: `adb devices` → observed 2026-08-17: `21071FDF600CSC  device`, `emulator-5554  device`. Re-run at execution.
- Pinned targets: physical Android `21071FDF600CSC` (Pixel 6, Android 16) + Android emulator `emulator-5554`. TC-378-11's recipient is the **emulator** (the campaign's dumpsys capture targets it). No iOS leg — nothing here is iOS-specific; iOS presentation is GAP-N12's consolidated phase.
- Automation: every setup, permission grant, mute toggle (Switch-node bounds tap), navigation, send, capture, and assertion is harness/orchestrator-driven — no user taps. Physical device must be UNLOCKED for uiautomator legs (secure-keyguard caveat).
- Closure role: required closure evidence (this plan's whole point is device-tier verification).
- `FLUTTER_DEVICE_ID`: host selector only; both pinned ids are passed explicitly (`-d a,b` / campaign `--sender/--recipient`).
- Registration: sound-smoke rows via literal `_runScenario` blocks (discovery awk) — `classify_path` already records the runner at `check_reliability_simulation_discovery.sh:466-469`; campaign scenarios via the 7-surface (reaction) and catalog/ladder (payload) checklists in the Test Contract.
- Discovery command: `.claude/skills/sims/scripts/run_with_devices.sh major --list` → both campaign capability ids listed; `./scripts/check_reliability_simulation_discovery.sh --checks-tsv` → 16 sound-smoke rows.
- Closure command: the three device commands in Acceptance Gates with their stated semantic outcomes.
- Deferred device work: G7 matrix → GAP-N12 device-evidence program; iOS legs → GAP-N12 consolidated phase; pure-FCM bg-isolate muted read → opportunistic in TC-378-09, else host-owned.

## Handoff
- First causal RED command: `bash scripts/test/notification_sound_disposition_contract_test.sh` (after authoring the sh contract; RED: deviceless orchestrator exit 1 with no table + 4-row discovery baseline — never pass `-d` in the RED probe).
- Preservation command: the TC-378-13 `flutter test` bundle (see Acceptance Gates).
- Manual registration: reaction-campaign 7-surface checklist ×2 scenarios; payload-campaign catalog/ids/ladder/cleanup/assertions; `critical_features.json` assertion appends; everything else is glob/awk-automatic and verified by the registration commands.
- Migration: none (no DB change).
- Boundary closure: the three device runs on the pinned pair (sound smoke S1–S16; reaction campaign incl. 2 muted scenarios; payload campaign incl. `tc_b13`).
- Unresolved evidence: S14 in-place-update dumpsys channel visibility (Test-Notes fallback assertion, flags mandatory).

## Reviewer Findings
/tdd-review 2026-08-17 (two-worker adversarial workflow + lead source verification of every blocker/plan-fix linchpin). Verdict on the original draft: **plan-fixes-required**; disposition **apply-plan-fixes** — all deltas are applied in this revision. Core bet **confirmed** (S14 dumpsys-channel sub-claim stays unresolved with a verified-load-bearing fallback).

Applied deltas:
1. **TC-378-11 topology + discriminators** — foreground FCM drains with `needsNotification:false` and emits no suppression event (`handle_foreground_remote_message_use_case.dart:13-20`); FCM-loses order emits `PUSH_BACKGROUND_NOTIFICATION_SUPPRESSED` (`background_message_handler.dart:937-951`); LEGACY_*_RECONCILE requires a durable effect context the live 1:1 listener never passes. → backgrounded-but-connected emulator recipient, discriminator union, dual-attempt evidence, bounded wait/inconclusive-red.
2. **S15 wiring pre-req** — `groupConversationTracker` is a discarded compat param (`group_message_listener.dart:313-315`) and the legacy gate fails open on a null reader (`:688-689`); the harness passes no `appVisibility:`. → explicit one-argument wiring edit in step 3. (Worker's stronger "S2/S3 baseline broken" claim refuted by lead: legacy outbox-null lane fires fine.)
3. **Census 17→16 + S11–S13 rewording** — `announcement …` descriptions match neither discovery filter arm (`:1010/:1013`); reworded to `Group announcement …`; announcement-suppression scenario cut (see 7).
4. **TC-378-08 confound-free sequencing** — retire-on-mute IS production (signal at `group_repository_impl.dart:533`; wiring test `:172-197` — worker's contrary claim refuted by lead), but the UI-driven toggle confounds device attribution; card-state after toggle is non-attributing; recipient returns HOME before msg2 so the policy-gate mutation genuinely re-reds.
5. **TC-378-01 RED mechanism** — unknown flags silently ignored (`:731-746`); usage exits 1 (`:747-754`); RED probe must never pass `-d`.
6. **TC-378-03 mutation seam** — the recording wrapper captures the upstream `silent` param (harness `:330`); mutation moved to the use-case silent decision so flag AND channel flip together.
7. **Over-engineering cuts (user-directed)** — announcement same-chat suppression device leg cut (identical prefix-based seam, `show_notification_use_case.dart:105-110`); S17 freshness leg cut (fake short-circuits on paused, `fake_app_visibility.dart:21-27`); TC-378-02 capped (one pure decision fn, ≤3 heredoc dumps, exit-code+reason output). Kept: 9 literal blocks (awk is a contract-pinned enforcing gate); both mute scenarios (different boundaries).
8. **Tightenings** — stale cites refreshed (mute host rows `:16483+/:17033+`, bmh `:4683-4742`; adapter ids `:104-109`; 56 tests); TC-378-10 injectability resolved-positive (`group_reaction_e2e_probe.dart:167-171` + ffi precedent test); S14 flags-mandatory sentence; switch bounds-tap mechanics; `showNotification` census exception pinned; optional inter-scenario baseline continuity.

## Execution Progress
| Time | Phase | Files | Last command/result | Current evidence | Decision/blocker | Next |
|---|---|---|---|---|---|---|
| 2026-08-17 pm | causal REDs | new sh contract; tap adapter sh | 4 REDs recorded | (1) disposition contract: deviceless exit 1, no table (never `-d`); (2) adapter `cmp` missing `tc_b13`; (3) discovery census = **4** rows; (4) `canonicalBadgeState` absent from probe | as planned | orchestrator/harness |
| 2026-08-17 pm | TC-378-01/02/04/05/06/07/12 | `run_notification_sound_smoke.dart`, `notification_sound_smoke_harness.dart`, `scripts/test/notification_sound_disposition_contract_test.sh` | contract sh PASS; discovery **16/16**; analyze clean | S1–S16 disposition table; offline `--verify-os-capture` decision fn reused by the live path; strict per-scenario channel; S5–S13 literal blocks; S14/S15/S16 legs; `silentFlagsMatch` | — | mutations |
| 2026-08-17 pm | mutation re-reds | orchestrator (byte-restored, sha-verified) | both RED then restored GREEN | (a) S1 table entry → `mknoon_messages_silent` ⇒ `cmp` red; (b) HEAD either-channel predicate ⇒ "audible-expected accepted a silent-channel record" | — | campaigns |
| 2026-08-17 pm | TC-378-11 (host leg) | `run_notification_tap_device_real.dart`, `notification_android_payload_campaign.dart`, `support/android_notification_payload_campaign.dart`, `tool/sims/device_criteria.dart`, `critical_features.json` | adapter contract PASS | `tc_b13_dual_path_single_alert` leg + 9 registration surfaces. **Plan checklist was incomplete**: `device_criteria.dart` `_notificationRequirements` is a 10th surface whose absence throws at runtime | device leg not run (see blocker below) | — |
| 2026-08-17 pm | TC-378-10 | `lib/core/debug/group_reaction_e2e_probe.dart`, new host test | RED then GREEN; probe byte-restored after RED | `canonicalBadgeState` from the production helper, redacted; 4 host rows; existing probe test still green via the `available:false` branch | — | — |
| 2026-08-17 pm | TC-378-13 + gates | — | 159 preservation tests PASS; discovery PASS; completeness 1466/1466; groups lane 4359 + Go gates PASS; `flutter analyze` clean | — | — | device closure |
| 2026-08-17 pm | **BLOCKER (pre-existing)** | `notification_sound_smoke_harness.dart` | Alice harness fails at ~00:02 during `setupGroupMultiDeviceStack`, right after DB open | **Verified identical at HEAD** (same device, same command, only the harness file swapped) ⇒ NOT caused by this plan. Exception text is swallowed by the integration-test binding and absent from logcat | sound-smoke device closure cannot complete until the harness setup break is fixed | own investigation |
| 2026-08-17 pm | **false-green fix (in scope)** | `run_notification_sound_smoke.dart` | same aborted run: exit **0 → 1** | The orchestrator's `finally { … exit(failed ? 1 : 0) }` swallowed the abort AND its exit code: 0 scenarios, every lane trivially "stable" ⇒ exit 0. Now tracks `runCompleted`/`runError` + `selectedOutcomes.length < expected` and prints `INCOMPLETE: 0/16 … (aborted: …)` | — | — |
| 2026-08-17 pm | scope decision (user) | `Test-Flight-Improv/379-…-carveout.md` | — | TC-378-08/09 carved out: the reaction criteria file dispatches message-vs-reaction on the id suffix at 6 sites and hard-asserts `unreadCount == 0`, contradicting TC-378-08's "unread preserved" | G4 deferred with an owner | plan 379 |
| 2026-08-17 pm | pre-existing red repaired | `scripts/test/group_reaction_notification_device_contract_test.sh` | PASS | pinned 5 rows while the runner emits 6 (plan 315 added one without repinning); blocked the plan's own `sims-contracts` gate | — | — |
