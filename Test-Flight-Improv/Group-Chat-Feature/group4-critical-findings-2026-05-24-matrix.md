# Group4 Critical Findings Matrix - 2026-05-24

Source: user-provided group4 finding list from 2026-05-24.

Status vocabulary: Open, Closed, Covered, Skipped.

| ID | Finding | Decision | Status | Evidence / Closure Bar |
| --- | --- | --- | --- | --- |
| G4-001 | Group `clamp()` values can infer `num` where `int`/`double` is required. | Implement exact casts. | Closed | Group conversation clamp call sites now cast clamp results to the required `int`/`double` types. Evidence: `dart analyze lib/core/media/group_media_mime_policy.dart lib/features/groups/presentation/screens/group_conversation_wired.dart lib/features/groups/application/retry_incomplete_group_uploads_use_case.dart` passed; `dart analyze test/features/groups/presentation/group_conversation_wired_test.dart` passed; `flutter test test/features/groups/presentation/group_conversation_wired_test.dart` passed (`+98`) on 2026-05-24. |
| G4-002 | Pause/resume can duplicate active group media sends. | Modify. Use a stale cutoff for pause recovery and skip fresh in-flight sends in retry paths rather than adding schema-level leases in this pass. | Closed | Group pause now uses `recoverStuckSendingMessages(olderThan: kPausedGroupSendingRecoveryThreshold)` instead of blanket `transitionSendingToFailed()`, and upload retry skips fresh outgoing `sending` parents before upload/publish. Evidence: `dart analyze` on G4-S3 production/test owner files passed; `flutter test test/core/lifecycle/handle_app_paused_group_test.dart` passed (`+5`); `flutter test test/features/groups/application/retry_incomplete_group_uploads_use_case_test.dart` passed (`+17`) on 2026-05-24. |
| G4-003 | `retryIncompleteGroupUploads` lacks a claim/lease. | Modify. Add a process-local retry claim plus fresh-send guard; DB lease is deferred because current repository interfaces have no lease columns. | Closed | `retryIncompleteGroupUploads` now has an app-isolate in-flight guard and returns `0` for overlapping invocations while the first pass owns pending rows. Evidence: `returns 0 for overlapping same-isolate retry while first upload is in flight` and `skips fresh outgoing sending parent before upload or publish` passed in `retry_incomplete_group_uploads_use_case_test.dart` (`+17`) on 2026-05-24. |
| G4-004 | Group recovery ack can happen without group inbox drain. | Implement narrow ack skip in the branch where no `groupMsgRepo` exists. | Closed | `handleAppResumed` now emits `APP_LIFECYCLE_RESUME_GROUP_ACK_SKIPPED` and does not call `group:acknowledgeRecovery` when `groupMsgRepo` is absent; the successful-drain branch remains the only ack path and drain-error ack skip emits the same event with `group_inbox_not_drained`. Evidence: targeted no-`groupMsgRepo` resume test passed; targeted `BB-012 does not acknowledge recovery when inbox drain reports an error` passed on 2026-05-24. |
| G4-005 | Group push payload parsing only accepts `groupId`. | Implement aliases and missing-id event. | Closed | `NotificationRouteTarget` accepts `groupId`/`group_id`/`gid`/`conversation_id`, exact `type`/`payloadType`/`kind` group-message identities, preserves `group_invite` to intros, and emits `PUSH_GROUP_ROUTE_MISSING_GROUP_ID` through foreground and remote-open boundaries. Evidence: `notification_route_target_test.dart`, `notification_route_dispatch_test.dart`, `app_root_notification_open_test.dart`, `handle_foreground_remote_message_use_case_test.dart`, `background_push_notification_fallback_test.dart`, plus `flutter test test/core/notifications` and `flutter test test/features/push/application` passed on 2026-05-24. |
| G4-006 | Notification-open prep does not resolve missing local group state. | Skip. Current route handling resolves group state and pending invites before navigation. | Skipped | `main.dart` resolves via `resolveGroupNotificationRouteTarget` before `GroupConversationWired` navigation. |
| G4-007 | Foreground group notification suppression can miss active group keys. | Modify. Normalize route-target suppression keys while keeping existing caller compatibility. | Closed | `ActiveConversationTracker` normalizes anchored group route keys and `maybeShowNotification` checks both contact key and route payload. Evidence: `show_notification_use_case_test.dart`, `flutter test test/features/push/application`, and G4-S1 direct notification/push tests passed on 2026-05-24. |
| G4-008 | Active conversation clear can clear a newer active screen. | Implement `clearIfActive` and use it on group/conversation disposal. | Closed | `clearIfActive` landed and group/1:1 conversation disposal/removal paths use guarded clearing, so stale screens do not clear newer active keys. Evidence: tracker tests in `show_notification_use_case_test.dart`, `group_conversation_wired_test.dart`, and `conversation_wired_test.dart` passed on 2026-05-24. |
| G4-009 | Notification-open dedupe marks seen before routing succeeds. | Implement in-flight/completed TTL API and route-success marking. | Closed | `NotificationOpenDedupeGate` now has in-flight/completed TTL state, route-identity keys, retryable failed finishes, and remote-open result wiring marks completion only after success. Evidence: `notification_open_dedupe_gate_test.dart`, `app_root_notification_open_test.dart`, `chat_and_group_push_open_flow_test.dart`, and `flutter test test/core/notifications` passed on 2026-05-24. |
| G4-010 | Outgoing/retry media paths do not validate actual file bytes. | Implement file validation before durable upload rows and retry upload. | Closed | Durable media send, voice send, and incomplete-upload retry now validate actual file bytes before upload/retry persistence. Evidence: `durable media send rejects spoofed bytes before saving upload rows`, `terminalizes spoofed retry bytes before upload or resend`, and media MIME policy spoofing tests passed in the focused suites on 2026-05-24. |
| G4-011 | Unknown files are accepted as `application/octet-stream`. | Implement rejection for generic octet-stream in group media. | Closed | `application/octet-stream` is no longer an allowed group media MIME and retry rows using it are terminalized without upload/resend. Evidence: `group_media_mime_policy_test.dart` passed (`+6`) and `retry_incomplete_group_uploads_use_case_test.dart` passed (`+15`) on 2026-05-24. |
| G4-012 | Display integrity policy allows missing encryption metadata in resolver. | Implement resolver alignment with central policy, with local outgoing exception. | Closed | The group media resolver now quarantines incoming done media that lacks required encryption metadata while allowing only local outgoing owned media with valid persisted hashes. Evidence: `incoming done media without encryption metadata is quarantined but local outgoing owned media can display` passed inside `group_conversation_wired_test.dart` (`+98`) on 2026-05-24. |
| G4-013 | Failed durable upload restores composer but leaves retryable old rows. | Implement cleanup of old durable retry state when restoring composer after upload failure. | Closed | Restored composer retry state is cleaned so old upload-pending rows are not left retryable after a durable upload failure. Evidence: `partial durable upload failure keeps successful uploads and clears retryable restored rows` passed in `group_conversation_wired_test.dart` on 2026-05-24. |
| G4-014 | Partial successful uploads are discarded when one attachment fails. | Implement immediate persistence of successful uploads. | Closed | Successful durable uploads are persisted immediately before later attachment failures are handled, preserving uploaded attachments across partial failure. Evidence: `partial durable upload failure keeps successful uploads and clears retryable restored rows` passed in `group_conversation_wired_test.dart` on 2026-05-24. |
| G4-015 | Voice messages can lose local playback after pending dir deletion. | Harden by stabilizing voice attachment local paths from durable storage before deleting pending dir. | Closed | Voice attachments now use stable owned media paths from durable storage before pending cleanup. Evidence: existing voice durable regressions, including `successful voice send uses the durable copy, cleans pending uploads, and survives temp deletion`, passed in `group_conversation_wired_test.dart` (`+98`), and voice background-task coverage passed in `group_conversation_wired_bg_task_test.dart` (`+18`) on 2026-05-24. |
| G4-016 | Background task begin/end can wedge `_isSending`. | Implement guarded begin/end and always end send flow. | Closed | Background begin/end failures are guarded and send flow reset is finalized so `_isSending` does not wedge. Evidence: `send proceeds and resets when bg:begin throws` and `bg:end errors are swallowed so _isSending resets` passed in `group_conversation_wired_bg_task_test.dart` (`+18`) on 2026-05-24. |
| G4-017 | `didUpdateWidget` does not reset state on group changes. | Implement full group-change reset/re-subscribe/reload. | Closed | Group id changes now reset group-bound messages, media, reactions, members, security/history/draft/quote state, subscriptions, and active tracker keys before reloading the new group. Evidence: `reused group conversation widget clears stale messages media and reactions when group id changes` passed in `group_conversation_wired_test.dart` on 2026-05-24. |
| G4-018 | Read receipts can be marked while backgrounded/replaying. | Implement foreground active-key read gating. | Closed | Visible read marking now requires resumed lifecycle and matching active group key. Evidence: `read marking requires foreground lifecycle and matching active group key` passed in `group_conversation_wired_test.dart` on 2026-05-24. |
| G4-019 | Foreground push drain failures have no user-visible fallback. | Implement result enum and caller fallback local notification. | Closed | `handleForegroundRemoteMessage` returns a drain result, group drain failures request notification fallback, and the foreground listener shows a local fallback through the existing notification service contract. Evidence: `handle_foreground_remote_message_use_case_test.dart`, `background_push_notification_fallback_test.dart`, and `flutter test test/features/push/application` passed on 2026-05-24. Broad `groups`, `baseline`, and `completeness-check` gates still have unrelated dirty-worktree residuals outside this row. |
| G4-020 | Optimistic reaction state does not roll back on normal failure/throw. | Implement rollback for non-success and exceptions. | Closed | Optimistic group reaction add/remove state now rolls back on non-success and thrown persistence errors. Evidence: `optimistic reaction add rolls back when the target message is gone`, `optimistic reaction remove rolls back on non-success result`, and `optimistic reaction add rolls back when persistence throws` passed in `group_conversation_wired_test.dart` on 2026-05-24. |
