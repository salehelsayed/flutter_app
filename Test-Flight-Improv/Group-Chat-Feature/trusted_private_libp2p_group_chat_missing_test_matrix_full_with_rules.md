# Trusted Private libp2p Group Chat Missing Test Matrix with Coverage Rules

This file now keeps only the 49 rows considered valuable after the current implementation review on 2026-04-30. Rows moved out by that review were copied to `trusted_private_libp2p_group_chat_missing_test_matrix_full_with_rules_v1.md` so they remain available without driving near-term work.

## Scope

This file is the trusted-private update to `libp2p_group_chat_missing_test_matrix_full_with_rules.md`.

It uses the same matrix style as `Discussion_and_announcement_test_matrix_full_with_rules_COMPLETE.md` and remains a gap-focused addendum to the current group-chat `test-inventory.md`.

The product assumption for this version is:

- groups are **private, invite-only, and intended for trusted users**
- groups are **not public chat rooms**
- public listing, public discoverability, open auto-join, public join requests, and public-room anti-spam/reporting workflows are **not release-scope capabilities** unless explicitly enabled later
- trusted members are still authorized only while their membership, device, role, and encryption epoch are current
- relays, bootstrap/rendezvous infrastructure, mailbox/store-and-forward peers, logs, diagnostics, and push bridges are **not trusted with plaintext**

That scope reduces the priority of public-room abuse and moderation tests, but it does **not** remove the need for authorization, identity binding, key rotation, removed-device isolation, replay protection, offline sync repair, message/media integrity, notification privacy, and truthful UI states.

## Source bundle used

- `test-inventory.md`
- `Discussion_and_announcement_test_matrix_full_with_rules_COMPLETE.md`
- Functional requirements catalog supplied in the prompt, `FR-001` through `FR-380`
- Previous generated matrix: `libp2p_group_chat_missing_test_matrix_full_with_rules.md`

## Actors

- **A** = group creator, owner, admin, or authorized moderator
- **B** = active trusted group member on one device
- **C** = invitee, rejoiner, removed member, banned identity, or sibling device
- **D** = prospective trusted member not yet in the group
- **X** = unauthorized, stale, compromised, buggy, relay, mailbox, storage, or protocol-incompatible peer

## Status legend

- **Covered** = dedicated row-owned coverage is now recorded in this matrix with concrete file, test, command, gate, and caveat evidence.
- **Partial** = adjacent or lower-level coverage exists in the current inventory, but the full user-visible or protocol-visible requirement is not directly proven.
- **Open** = no dedicated coverage was found in the current inventory for the stated behavior.
- **Unsupported** = outside the trusted-private release scope; keep it excluded from release gates unless product scope changes.

## Priority guide

- **P0** = release-blocking trust, privacy, safety, authorization, crypto, admission, or state-convergence contract for trusted private groups.
- **P1** = important before broad trusted-private rollout or before claiming complete group-chat reliability.
- **P2** = roadmap, policy-dependent, or public-room-style capability; add coverage when intentionally enabled.

## Coverage policy used in this matrix

### Coverage legend

- **Required** = should exist before treating the row as production-ready.
- **Recommended** = high-value proof but not always required for the first trusted-private release gate.
- **N/A** = do not force this layer for the row.

### Rules

**Unit**
Use for deterministic logic: validation, permission checks, state conflict resolution, replay protection, epoch decisions, notification eligibility, error classification, metadata minimization, configuration guards, and API payload shape.

**Integration**
Use for most rows where trust depends on local persistence, application services, bridge payloads, and UI-visible state.

**Smoke**
Keep small and release-blocking. Smoke should prove the core trusted-private user journey and the highest-risk failure modes, not every branch.

**Fake Network**
Use when the risk is peer discovery, PubSub duplication, relay fallback, sync gaps, partitions, store-and-forward behavior, stale removed peers, or protocol-incompatible peer traffic.

**3-Party E2E**
Use for flows users perceive across A/B/C devices: create, direct invite, join, removal, key rotation, media, notifications, history sync, and multi-device convergence.

## Current repo execution note (2026-04-30)

The current inventory is strong on discussion and announcement happy paths, direct send/receive, pending invites, admin-only announcement writes, role promotion/demotion, removals, key rotation basics, replay dedupe, inbox drain, retry/resume, media scaffolding, Go-side PubSub validation, and several decryption-failure paths.

For a trusted-private group launch, the remaining release-sensitive gaps are concentrated in: private-only admission guards, invite revocation/replay, device-specific admission, local-and-remote authorization on every mutation, removed-device isolation, membership/key epoch convergence, offline sync repair, message ordering, media safety, notification/privacy correctness, event-log/database integrity, and UI truthfulness.

Public/discoverable group behaviors, open join queues, broad public anti-spam, report workflows, and public-room moderation policies are de-scoped unless the product explicitly enables them later. This matrix keeps only trusted-private guard rows where they protect invite-only admission or authorization.

## Matrix interpretation

- This file contains only the 49 retained rows that map to plausible current-code regressions or missing verification around the implemented trusted-private group chat behavior.
- Rows moved to `trusted_private_libp2p_group_chat_missing_test_matrix_full_with_rules_v1.md` are preserved for traceability, but they are not part of the immediate gap-closure set.
- A retained row does not automatically mean the product should add new behavior. It means the existing behavior is important enough to verify so users are not disappointed by regressions.
- Rows intentionally exclude public group, untrusted membership, admin/moderation, group invite links, server roster authority, and generic P2P transport tests unless they directly protect trusted-private group-chat behavior.

## Group Lifecycle, Metadata, Schema, and Governance Truth

| Test ID | Scenario | Preconditions | Steps | Expected Result | Priority | Current status | Unit | Integration | Smoke | Fake Network | 3-Party E2E | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| GL-005 | Private-only visibility and discoverability are enforced | The release scope is trusted, private, invite-only groups; public listing/discoverability is disabled. | 1. Create a trusted private group. 2. Query every discovery, listing, preview, and join surface as D and X. 3. Attempt public/discoverable creation flags and open join routes. | Only invited/authorized identities can discover usable group metadata or join material; public listing, public previews, and open joins are rejected or hidden. | P0 | Covered | Required | Required | Recommended | Recommended | Recommended | Covered by GL-005 execution on 2026-04-30. Evidence files: `test/features/groups/application/create_group_use_case_test.dart` pins supported private group create payload variants with no public/open route fields; `test/features/groups/application/create_group_with_members_use_case_test.dart` pins selected-member-only config, `members_added`, and invite fanout; `test/features/groups/application/group_invite_listener_test.dart` rejects public-preview-shaped unknown/blocked invites before pending/group/key/join state; `test/features/groups/presentation/group_list_wired_test.dart` renders only persisted active groups and repository-backed pending invites; `go-mknoon/node/pubsub_test.go` proves non-member discovery results are filtered before dial/use; `go-mknoon/bridge/bridge_test.go` proves unsupported public/open `GroupCreate.groupType` values return `INVALID_INPUT`; `go-mknoon/bridge/bridge.go` now rejects unsupported `groupType` before topic join.<br>Passed commands/gates: `flutter test --no-pub test/features/groups/application/create_group_use_case_test.dart`; `flutter test --no-pub test/features/groups/application/create_group_with_members_use_case_test.dart`; `flutter test --no-pub test/features/groups/application/group_invite_listener_test.dart`; `flutter test --no-pub test/features/groups/application/handle_incoming_group_invite_use_case_test.dart`; `flutter test --no-pub test/features/groups/presentation/group_list_wired_test.dart`; `flutter test --no-pub test/features/groups/integration/group_membership_smoke_test.dart`; `cd go-mknoon && go test ./node -run "GL005\|GroupRendezvousNamespace\|GroupTopicAndRendezvousNamespace\|FilterDiscoveredGroupMembers\|DiscoverAndConnectGroupPeers\|GroupDiscovery" -v`; `cd go-mknoon && go test ./bridge -run "GroupCreate\|GroupJoinTopic" -v`; `./scripts/run_test_gates.sh groups`; `git diff --check`.<br>Known unrelated caveat: broad `cd go-mknoon && go test ./node -run "Group\|PubSub\|Rendezvous" -v` failed only the existing LP-006 `TestPublishGroupMessage_ReturnsPeerCountZero_WhenNoPeers` sender/transport mismatch path; direct GL-005 node/bridge tests and the canonical `groups` gate passed. |

## libp2p Topology, Topics, Discovery, and Protocol Negotiation

| Test ID | Scenario | Preconditions | Steps | Expected Result | Priority | Current status | Unit | Integration | Smoke | Fake Network | 3-Party E2E | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| LP-002 | PubSub authorization is enforced before accept or forward | X is not a member, is stale, or has been removed. | 1. X publishes message, reaction, metadata, membership, and key events. 2. Observe local accept, forward, and diagnostics behavior. | Unauthorized traffic is rejected before state mutation or forwarding; diagnostics are privacy-safe and rate-limited. | P0 | Covered | Required | Required | Required | Required | Recommended | Covered by LP-002 execution on 2026-04-30. Evidence files: `go-mknoon/node/pubsub_authorization_forward_test.go` adds `TestLP002UnauthorizedRawPubSubRejectsBeforeAcceptAndForward`, a live X-B-C raw Go PubSub topology where X is stale/removed, X is not directly connected to C, and unauthorized message, reaction, membership (`members_added`), metadata (`group_metadata_updated`), and key-rotation (`key_rotated`) envelopes produce exactly one `non_member` validator reject on B with no accepted/decrypt/parse events on B or C; `TestLP002UnauthorizedRejectDiagnosticsArePrivacySafeAndRateLimited` proves repeated rejects are rate-limited and diagnostics omit plaintext, group id/name, raw sender ids, public/private keys, signatures, ciphertext, nonce, and multiaddrs while retaining hashed reason/group/sender/transport markers; `go-mknoon/node/pubsub.go` and `go-mknoon/node/node.go` add hashed, rate-limited validator reject diagnostics without changing validation accept/reject semantics; `go-mknoon/node/pubsub_test.go` keeps `TestGroupTopicValidator_RejectsUnauthorizedEventFamiliesBeforeForward` for pure validator event-family rejection.<br>Passed commands/gates: `cd go-mknoon && go test ./node -run 'TestLP002UnauthorizedRawPubSubRejectsBeforeAcceptAndForward\|TestLP002UnauthorizedRejectDiagnosticsArePrivacySafeAndRateLimited\|TestGroupTopicValidator_RejectsUnauthorizedEventFamiliesBeforeForward' -v`; `cd go-mknoon && go test ./cmd/testpeer -run 'Group\|PubSub\|Rendezvous\|Protocol\|Inbox' -v`; `git diff --check`.<br>App-owned peer scoring is non-applicable: `go-mknoon/node` does not configure `WithPeerScore` or `PeerScoreParams`, and the live reject-before-forward proof closes the row without enabling production peer scoring. Named Flutter gates were not run because LP-002 changed Go validator diagnostics/logging and tests only; no Dart-visible group behavior or bridge API contract changed.<br>Known unrelated caveat: broad owner command `cd go-mknoon && go test ./node ./bridge ./cmd/testpeer -run 'Group\|PubSub\|Rendezvous\|Protocol\|Inbox' -v` failed only current dirty-worktree sender/transport mismatch tests `TestPublishGroupMessage_ReturnsPeerCountZero_WhenNoPeers` and `TestGroupPublish_ResponseIncludesTopicPeers`, outside LP-002 diagnostics. |
| LP-003 | Unsubscribe after leave, removal, ban, and deletion | B leaves; C is removed or banned; a group is deleted or dissolved. | 1. Trigger each exit path. 2. Inspect active subscriptions and discovery loops. 3. Publish after exit. | Exited devices are unsubscribed and do not receive or process future group topic traffic. | P0 | Covered | Required | Required | Recommended | Required | Recommended | Covered by LP-003 execution on 2026-05-01. Evidence files: `go-mknoon/node/pubsub_unsubscribe_exit_paths_test.go` adds `TestLP003LeaveGroupTopicStopsLiveDeliveryAfterExit`, a live Go/libp2p proof that an exited node receives pre-exit traffic, calls `LeaveGroupTopic`, loses topic/subscription/subscription context/discovery context/config/key state, receives no post-exit normal message, reaction, payload-parse-failure, or decrypt-failure events, and fails closed on post-exit message/reaction publishes with `group not joined`; existing `go-mknoon/node/pubsub_test.go` keeps `TestLeaveGroupTopic_RemovesPubSubStateAndBlocksFuturePublish` and `TestLeaveGroupTopic_CancelsDiscoveryContext`; `test/features/groups/application/leave_group_use_case_test.dart` adds `LP003 normal leave dispatches group leave and clears local state`; `test/features/groups/application/delete_group_and_messages_use_case_test.dart` adds `LP003 active delete dispatches one group leave` and `LP003 dissolved local cleanup does not publish a second group leave`; `test/features/groups/application/group_message_listener_test.dart` adds `LP003 member_removed self-removal is the ban-equivalent leave path` and `LP003 replayed group_dissolved dispatches one group leave`; `test/features/groups/application/drain_group_offline_inbox_use_case_test.dart` keeps replayed self-removal cleanup/cutoff proof; `test/features/groups/integration/group_membership_smoke_test.dart` keeps fake-network self-removal and removed-member send/notification suppression proof.<br>Passed commands/gates: `cd go-mknoon && go test ./node -run 'TestLP003LeaveGroupTopicStopsLiveDeliveryAfterExit\|TestLeaveGroupTopic_RemovesPubSubStateAndBlocksFuturePublish\|TestLeaveGroupTopic_CancelsDiscoveryContext' -v`; `flutter test --no-pub test/features/groups/application/leave_group_use_case_test.dart test/features/groups/application/delete_group_and_messages_use_case_test.dart test/features/groups/application/group_message_listener_test.dart`; `flutter test --no-pub test/features/groups/application/drain_group_offline_inbox_use_case_test.dart --name 'replayed member_removed routes through listener cleanup instead of saving a chat row\|replayed self-removal cuts off later queued inbox traffic for that group'`; `flutter test --no-pub test/features/groups/integration/group_membership_smoke_test.dart`; `git diff --check`.<br>No LP-003 production code changed: proof-first tests passed against existing unsubscribe behavior. Ban is an accepted current-architecture mapping to `member_removed`; execution found no first-class group ban event/model in scoped group/node/bridge code, so LP-003 does not invent new moderation policy. `group-real-network-nightly` was not run because relay env is unset and the closure profile is host-only raw-Go/libp2p plus focused Dart tests. Known unrelated caveats: full `drain_group_offline_inbox_use_case_test.dart` still fails only `MD-011 removed member cannot decode future media replay with only the old epoch`, and the broad Go owner command still fails only `TestGroupPublish_ResponseIncludesTopicPeers` after `peer_mismatch`; neither is caused by LP-003. |
| LP-013 | Duplicate PubSub handling covers hash, ID, and sequence collisions | Live PubSub and sync deliver the same payload or malicious duplicates. | 1. Deliver duplicate messages with same ID, same hash, and conflicting sequence. 2. Compare DB, UI, notifications, and receipts. | Duplicates are ignored or enriched deterministically without double rows, double notifications, or overwritten trusted fields. | P0 | Covered | Required | Required | Required | Required | Recommended | Covered by LP-013 execution on 2026-05-01. Evidence files: `go-mknoon/node/pubsub_delivery_test.go` adds `TestLP013DefaultPubSubMessageIdUsesSourceAndSeqnoNotPayloadHash`, proving default PubSub message IDs use source plus seqno rather than payload hash; `TestLP013DuplicateWireEnvelopeWithDistinctPubSubSeqnosPreservesApplicationMessageId`, proving duplicate identical encrypted wire envelopes preserve the application `messageId` through live/raw PubSub delivery; `TestLP013ConflictingApplicationDuplicatePubSubPayloadsPreserveFirstWriterInputsForDartDedupe`, proving conflicting duplicate application payloads still surface the same application `messageId` for Dart dedupe; and the existing `TestPublishGroupMessage_DuplicateProvidedMessageIdRemainsVisibleAfterDecrypt` anchor remains green. `test/features/groups/application/group_message_listener_test.dart` adds `LP013 duplicate PubSub delivery preserves first row and notification state`, proving one saved row, one UI stream insertion, one local notification, one unread item, and first trusted text/timestamp/status/key/quoted/media preservation. Existing anchors passed: `handle_incoming_group_message_use_case_test.dart` duplicate replay with the same `messageId` ignores conflicting content; `group_resume_recovery_test.dart` same message is not duplicated if both PubSub and group inbox deliver it; `group_edge_cases_smoke_test.dart` duplicate delivery; and `group_notification_dedupe_integration_test.dart` notification dedupe.<br>Passed commands/gates: `cd go-mknoon && go test ./node -run 'TestLP013\|TestPublishGroupMessage_DuplicateProvidedMessageIdRemainsVisibleAfterDecrypt' -v`; `flutter test --no-pub test/features/groups/application/group_message_listener_test.dart --plain-name 'LP013'`; `flutter test --no-pub test/features/groups/application/handle_incoming_group_message_use_case_test.dart --plain-name 'duplicate replay with the same messageId ignores conflicting content'`; `flutter test --no-pub test/features/groups/integration/group_resume_recovery_test.dart --plain-name 'same message is not duplicated if both pubsub and group inbox deliver it'`; `flutter test --no-pub test/features/groups/integration/group_edge_cases_smoke_test.dart --plain-name 'duplicate delivery'`; `flutter test --no-pub test/integration/group_notification_dedupe_integration_test.dart`; `./scripts/run_test_gates.sh groups`; `flutter test --no-pub test/features/groups/integration`; `git diff --check`.<br>No LP-013 production code changed. Broad owner command `cd go-mknoon && go test ./node ./bridge ./cmd/testpeer -run 'Group\|PubSub\|Rendezvous\|Protocol\|Inbox' -v` failed only known unrelated peer-mismatch tests `TestPublishGroupMessage_ReturnsPeerCountZero_WhenNoPeers` and `TestGroupPublish_ResponseIncludesTopicPeers`; all LP-013 tests passed inside that selection. Device/relay proof is supporting only for LP-013 because relay env is unset and the row's closure profile accepts host-only raw-Go/libp2p proof plus focused Flutter host tests. First-class group receipts are an accepted out-of-scope architectural difference because the scoped group implementation has no first-class receipt protocol to test or modify under LP-013. |

## Invitations, Join Flows, and Device Admission

| Test ID | Scenario | Preconditions | Steps | Expected Result | Priority | Current status | Unit | Integration | Smoke | Fake Network | 3-Party E2E | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| IJ-001 | Invite payload completeness and privacy | A sends an invite to C. | 1. Create direct and offline invites. 2. Inspect encrypted and decrypted payloads. 3. Verify preview, permissions, expiration, device allowance, and key-material reference. | Invites include enough data for the intended invitee to evaluate and accept the private group, but never expose member lists, keys, peer addresses, or history beyond policy. | P0 | Covered | Required | Required | Recommended | Recommended | Recommended | Covered by IJ-001 execution on 2026-05-01. `group_invite_policy.dart` adds first-class `GroupInvitePolicy`; `group_invite_payload.dart`, `pending_group_invite.dart`, `send_group_invite_use_case.dart`, `handle_incoming_group_invite_use_case.dart`, and `accept_pending_group_invite_use_case.dart` keep policy only inside encrypted invite plaintext, fail closed on missing or contradictory policy during parse/send/pending-store/direct-handle/materialization/pending-accept repair, derive pending expiry from policy clamped to local TTL, and preserve the cleartext v2 envelope as preview/routing only. Direct evidence: `group_invite_payload_test.dart` tests `IJ001 parses a first-class encrypted invite policy` and `IJ001 rejects missing or contradictory first-class invite policy`; `pending_group_invite_test.dart` tests policy-clamped expiry; `send_group_invite_use_case_test.dart` tests encrypted-only policy placement and `IJ001 returns invalidPayload before encryption or delivery when policy derivation fails`; `store_pending_group_invite_use_case_test.dart`, `handle_incoming_group_invite_use_case_test.dart`, and `accept_pending_group_invite_use_case_test.dart` test missing/contradictory policy fail-closed paths; `invite_round_trip_test.dart` proves direct/listener round trips preserve policy privately; `group_new_member_onboarding_test.dart` remains green for authorized post-join state/history boundaries. Passed gates: focused domain `+21`, focused application `+57`, application invite glob `+72`, `invite_round_trip_test.dart` `+12`, `group_new_member_onboarding_test.dart` `+6`, `./scripts/run_test_gates.sh groups` `+94`, `flutter test --no-pub test/features/groups/integration` `+116`, controller rerun of format/no-change plus focused IJ-001 domain/application suites, and `git diff --check`. Supporting `group-real-network-nightly` was not run because `FLUTTER_DEVICE_ID` and `MKNOON_RELAY_ADDRESSES` are unset; this is non-blocking for IJ-001. Current peer-bound `allowedDevices: [recipientPeerId]` is accepted for IJ-001; separate account/device registry work remains outside the shipped Peer ID invite contract. Signed inviter auth is IJ-002, revocation IJ-003, and replay/reuse IJ-005; auto-join is covered by IJ-009; concurrent joins are covered by IJ-010, and authorized history entitlement is covered by IJ-011. |
| IJ-002 | Invite signature, inviter authorization, and stale inviter rejection | A can invite; B has no invite permission or was removed. | 1. Send signed invites from A, B, and a removed B. 2. Tamper inviter fields. 3. Accept from C. | Only authorized signed invites are stored or accepted; stale or tampered inviter payloads fail closed. | P0 | Covered | Required | Required | Required | Recommended | Recommended | Covered by IJ-002 execution on 2026-05-01. FR-044, FR-045, FR-172, and FR-348 are covered for signed invite attestation, inviter authorization, and stale/removed inviter fail-closed handling. Production files: `group_invite_payload.dart` now requires a canonical `inviteSignature` inside encrypted invite plaintext; new `group_invite_auth.dart` verifies the signature with the trusted contact public key and validates inviter authorization from the signed group snapshot; `send_group_invite_use_case.dart` signs the canonical invite payload before encryption; `handle_incoming_group_invite_use_case.dart` and `accept_pending_group_invite_use_case.dart` reject invalid, unsigned, tampered, non-admin, invite-disabled, and removed-inviter payloads before pending/group/key/join/consumption side effects; `pending_group_invite.dart`, `create_group_with_members_use_case.dart`, `contact_picker_wired.dart`, `group_list_wired.dart`, `orbit_wired.dart`, and `test/core/bridge/fake_bridge.dart` were updated for the signed invite contract. Direct evidence: `group_invite_payload_test.dart` tests `IJ002 requires signed invite attestation and rejects canonical mismatch`; `send_group_invite_use_case_test.dart` tests `IJ002 signs canonical invite payload before encryption and delivery` and signing failure; `handle_incoming_group_invite_use_case_test.dart` tests invalid signature, tampered fields, non-admin, invite-disabled, and removed-inviter rejection before state or join; `group_invite_listener_test.dart` tests no pending storage for signature failure or unauthorized/removed inviters; `accept_pending_group_invite_use_case_test.dart` tests tampered persisted invite deletion and accept-time authorization revalidation; `add_group_member_use_case_test.dart` and `create_group_with_members_use_case_test.dart` keep sender-side add/create authorization and invite fan-out behavior green; `invite_round_trip_test.dart` and `group_new_member_onboarding_test.dart` remain green for signed invite round trips and authorized onboarding. Passed gates: RED evidence was recorded at 2026-05-01 02:07 CEST; exact format check passed with `0 changed`; payload model test passed (`+18`); focused send/handle/accept/listener tests passed (`+69`); application invite wildcard passed (`+81`); add/create member command passed (`+30`); `invite_round_trip_test.dart` passed (`+12`); `group_new_member_onboarding_test.dart` passed (`+6`); `./scripts/run_test_gates.sh groups` passed (`+94`); `flutter test --no-pub test/features/groups/integration` passed (`+116`); `git diff --check` passed. Supporting `group-real-network-nightly` was not run because `FLUTTER_DEVICE_ID` and `MKNOON_RELAY_ADDRESSES` are unset; this is non-blocking for IJ-002. Stale/removed inviter rejection is closed for the self-contained signed snapshot plus accept-time trusted contact revalidation; this row does not claim durable historical membership-index semantics. Revocation delivery is covered by IJ-003; direct invite replay/reuse is covered separately by IJ-005; auto-join is covered by IJ-009, concurrent joins are covered by IJ-010, separate account/device registry remains outside the shipped Peer ID invite contract, and broad event-family signature parity remains EK-004. |
| IJ-003 | Invite revocation and revoked-accept failure | A has a pending invite for C. | 1. Revoke the invite. 2. Deliver delayed direct and mailbox copies. 3. Attempt accept. | Revoked invites are removed from pending surfaces and cannot be accepted even if replayed later. | P0 | Covered | Required | Required | Recommended | Recommended | Recommended | Covered by IJ-003 execution on 2026-05-01. FR-047 and FR-351 are covered for signed encrypted invite revocation delivery and revoked-accept failure. Production files: `group_invite_revocation_payload.dart` adds the canonical signed revocation payload; `group_invite_auth.dart` adds revocation signer/auth checks; `revoke_pending_group_invite_use_case.dart` signs before encryption and direct-sends with inbox fallback; `handle_incoming_group_invite_use_case.dart` validates trusted signer/auth snapshot, recipient binding, expiry, transport sender, and canonical signature before mutation; `group_invite_listener.dart` dispatches revocations with pending refresh; `incoming_message_router.dart` routes `group_invite_revocation` through the invite stream. Direct evidence: `group_invite_revocation_payload_test.dart`, `revoke_pending_group_invite_use_case_test.dart`, `group_invite_listener_test.dart`, `store_pending_group_invite_use_case_test.dart`, `accept_pending_group_invite_use_case_test.dart`, `incoming_message_router_test.dart`, and `invite_round_trip_test.dart` prove valid revocation tombstones delete only the matching pending invite, invalid/tampered/unknown/unauthorized/wrong-recipient/expired revocations fail closed, delayed direct/mailbox invite copies are rejected after tombstone, revoked accept has no pending/group/key/join/consumed side effects, and the cleartext revocation envelope exposes no keys, member lists, addresses, policy, history, or signature. Preservation evidence kept invite payload, send, handle, and onboarding suites green. Gates: RED `flutter test --no-pub test/features/groups/domain/models/group_invite_revocation_payload_test.dart test/features/groups/application/revoke_pending_group_invite_use_case_test.dart test/features/groups/application/group_invite_listener_test.dart test/features/groups/application/store_pending_group_invite_use_case_test.dart test/features/groups/application/accept_pending_group_invite_use_case_test.dart test/core/services/incoming_message_router_test.dart test/features/groups/integration/invite_round_trip_test.dart` failed as expected before production; the same focused command passed after implementation, invite wildcard passed, `group_new_member_onboarding_test.dart` passed, `./scripts/run_test_gates.sh groups` passed, `flutter test --no-pub test/features/groups/integration` passed, QA rerun smoke suites passed, and `git diff --check` passed. Supporting `group-real-network-nightly` was not run because `FLUTTER_DEVICE_ID` and `MKNOON_RELAY_ADDRESSES` are unset; this is non-blocking for IJ-003. IJ-003 alone does not close TP-SMOKE-01, IJ-005 replay/reuse, IJ-009 auto-join, IJ-010 concurrent joins, separate account/device registry, or EK-004 broad event-family signature parity. |
| IJ-005 | Invite reuse policy and replay protection | Single-use, multi-use, and link invites can be configured. | 1. Accept an invite once. 2. Replay the same credential from same and different devices. 3. Try after expiry. | Reuse follows policy exactly; single-use and expired credentials cannot create duplicate or unauthorized membership. | P0 | Covered | Required | Required | Recommended | Required | Recommended | Covered by IJ-005 execution on 2026-05-01 for direct signed invite credentials. Production files: `group_invite_policy.dart` and `group_invite_payload.dart` define explicit signed/encrypted `singleUse` and `multiUse` reuse policy; `send_group_invite_use_case.dart` defaults direct sends to `singleUse` and exposes explicit `multiUse` direct invite creation; `handle_incoming_group_invite_use_case.dart`, `group_invite_listener.dart`, and `accept_pending_group_invite_use_case.dart` reject missing, unknown, or contradictory reuse policy, check expiry and receiver-local consumption tombstones before pending/accept side effects, enforce recipient/allowed-device policy when local peer identity is available, and keep multi-use replay idempotent without duplicate group membership, key, join, or pending rows. Direct evidence: `group_invite_payload_test.dart`, `send_group_invite_use_case_test.dart`, `store_pending_group_invite_use_case_test.dart`, `accept_pending_group_invite_use_case_test.dart`, `group_invite_listener_test.dart`, `handle_incoming_group_invite_use_case_test.dart`, `invite_round_trip_test.dart`, and onboarding/groups integration gates prove policy parsing/privacy, single-use default, explicit multi-use send path, fail-closed bad policy, single-use replay tombstone ordering, expired replay rejection before state mutation, different peer/device rejection when identity is known, duplicate-group protection, and privacy-safe cleartext invite envelopes. Gates: RED command failed as expected before production because `GroupInviteReusePolicy`, `GroupInvitePolicy.reusePolicy`, and `sendGroupInvite(reusePolicy:)` were missing; focused IJ-005 command passed (`+81`); invite wildcard passed (`+96`) after listener compatibility fix; onboarding passed (`+6`); `./scripts/run_test_gates.sh groups` passed (`+96`); `flutter test --no-pub test/features/groups/integration` passed (`+118`); `git diff --check` passed. Supporting `group-real-network-nightly` was unconfigured with `FLUTTER_DEVICE_ID is required for Group Real-Network Nightly Gate.` and is non-blocking. Residual caveats: first-class link invite creation/claim remains prerequisite-owned or product-scope unsupported until a link-token surface exists, and shared account-wide cross-device consumption remains outside the shipped Peer ID invite contract and would require a separate account/device/shared-state model. |
| IJ-009 | Open auto-join is disabled for trusted-private groups | A peer presents a token, link, stale invite, or copied join payload without identity-bound authorization. | 1. Attempt auto-join with valid-looking but unbound tokens. 2. Attempt auto-join with copied or replayed payloads. 3. Inspect membership, key, and notification state. | No member, device, key, or local group state is created unless a current signed invite is bound to the accepting identity/device. | P0 | Covered | Required | Required | Required | Required | Recommended | Covered by IJ-009 execution on 2026-05-01. Production files: `handle_incoming_group_invite_use_case.dart` now requires a non-empty current `ownPeerId` before direct or pending invite resolution, rejects mismatched `recipientPeerId`, and rejects invites whose `invitePolicy.allowedDevices` does not include the current peer before pending/group/key/join side effects; `group_invite_listener.dart` now requires `getOwnPeerId` and passes it into pending storage; `lib/main.dart` wires the persisted identity peer id into `GroupInviteListener`. Direct evidence: `handle_incoming_group_invite_use_case_test.dart` adds IJ-009 coverage rejecting signed invites when local peer identity is unavailable before group/key/join state; `store_pending_group_invite_use_case_test.dart` adds IJ-009 coverage rejecting missing and mismatched local peer identity before pending/group/key/join state; `group_invite_listener_test.dart` adds IJ-009 coverage rejecting copied signed invites and identity-unavailable invites before pending stream, pending row, group/key, notification, or `group:join` state. Preservation coverage in `accept_pending_group_invite_use_case_test.dart`, `join_group_use_case_test.dart`, `invite_round_trip_test.dart`, and `group_membership_smoke_test.dart` remains green for valid signed invite handling and the low-level join helper. Gates: RED focused command failed as expected before production edits (`+54 -3`); the focused direct/listener command passed after implementation (`+57`); format check passed with `Formatted 9 files (0 changed)`; accept/join preservation passed (`+19`); application invite wildcard passed (`+100`); `invite_round_trip_test.dart` passed (`+14`); `group_membership_smoke_test.dart` passed (`+23`); `./scripts/run_test_gates.sh groups` passed (`+96`); `git diff --check` passed. Supporting `group-real-network-nightly` failed only because `FLUTTER_DEVICE_ID` is unset and is non-blocking for IJ-009. First-class link invite creation/claim remains out of scope until a link-token surface exists; first-class shared account/device semantics remain outside the shipped Peer ID invite contract; concurrent joins are covered by IJ-010; broad event-family signature parity remains EK-004; TP-SMOKE-01 remains a supporting scenario, not a source row closed by IJ-009. |
| IJ-010 | Concurrent joins converge membership and epoch state | C and D join at nearly the same time from different peers. | 1. Deliver concurrent join events and welcomes. 2. Sync all peers. 3. Send after convergence. | Concurrent direct invites and accepts converge membership and epoch state without admitting unintended identities or overwriting trusted member roles. | P0 | Covered | Required | Required | Recommended | Required | Required | Covered by IJ-010 execution on 2026-05-01. `test/features/groups/integration/group_membership_smoke_test.dart` adds `IJ010 concurrent direct invite accepts converge membership epoch and delivery`, a fake-network multi-user proof that an admin and existing member converge after a batch `members_added` authoritative config, Charlie and Dave accept separate signed direct pending invites concurrently, both joiners issue `group:join` and subscribe only after acceptance, admin/existing/Charlie/Dave converge on exactly the intended peer IDs, roles, key epoch, and encrypted key material, both pending invites are cleared with consumed tombstones, uninvited Eve has no group/key/subscription/messages, and post-join sends from both joiners are delivered to the existing member and trusted participants. Preservation evidence: existing `invite_round_trip_test.dart` still proves `concurrent pending accepts converge members, key epoch, and sendability`; invite application tests, invite round trip, onboarding, groups gate, and `git diff --check` passed. Commands: focused IJ-010 `flutter test --no-pub test/features/groups/integration/group_membership_smoke_test.dart --plain-name 'IJ010'` passed (`+1`); existing concurrent accept focused command passed (`+1`); `flutter test --no-pub test/features/groups/application/*invite*_test.dart` passed (`+100`); `flutter test --no-pub test/features/groups/integration/invite_round_trip_test.dart` passed (`+14`); `flutter test --no-pub test/features/groups/integration/group_new_member_onboarding_test.dart` passed (`+6`); `./scripts/run_test_gates.sh groups` passed (`+97`); `git diff --check` passed. Supporting `group-real-network-nightly` was not run because `FLUTTER_DEVICE_ID` and `MKNOON_RELAY_ADDRESSES` are unset. The authorized history/state entitlement is covered by IJ-011; separate account/device registry, EK-004 broad event signatures, RP conflict semantics, and first-class real relay/device nightly proof are not closed by IJ-010. |
| IJ-011 | New-member sync includes only authorized state and history | C joins after metadata, role, pin, and history changes. | 1. Send pre-join messages and update metadata/permissions. 2. C joins. 3. Sync and inspect C state. | New trusted members receive only policy-allowed metadata, membership, permissions, pinned items, and history; pre-join history remains inaccessible if policy says so. | P0 | Covered | Required | Required | Recommended | Required | Required | Covered by IJ-011 execution on 2026-05-01. `test/features/groups/integration/group_new_member_onboarding_test.dart` tightens `new member receives current metadata and roles without pre-join history` so a newly added member receives the latest group metadata, creation fields, membership set, current role snapshot, and explicit permission overrides after pre-join metadata and role changes. The test now pins Charlie's reader role plus custom `GroupMemberPermissions` (`inviteMembers: false`, `editMetadata: false`, `pinMessages: true`) in Bob's new-member snapshot, verifies Bob's own member row has no custom overrides, verifies Bob has no pre-join message or timeline rows before post-join traffic, and verifies the post-join message is delivered while pre-join history stays inaccessible. Existing onboarding tests also cover no pre-join text/media backfill, post-join media descriptors/downloads, post-join reactions without pre-join reaction state, quoted pre-join parent fallback, and deterministic add/send boundaries; `invite_round_trip_test.dart` preserves future-only history and post-join replay. Commands: focused IJ-011 onboarding test passed (`+1`); full `group_new_member_onboarding_test.dart` passed (`+6`); `invite_round_trip_test.dart` passed (`+14`); `flutter test --no-pub test/features/groups/application/*invite*_test.dart` passed (`+100`); `./scripts/run_test_gates.sh groups` passed (`+97`); `git diff --check` passed. Supporting `group-real-network-nightly` was not run because `FLUTTER_DEVICE_ID` and `MKNOON_RELAY_ADDRESSES` are unset. First-class group pinned-message state is not a shipped group-chat surface in this repo, so IJ-011 covers the current pin-related group permission state (`pinMessages`) but does not claim product-level group pinned-item sync. IJ-013 now covers shipped Peer ID / `allowedDevices` invite binding and IJ-014 now covers shipped inline group-key repair state; IJ-011 does not close separate account/device registry, RP authorization/conflict rows, EK signatures/key rows, or first-class real relay/device proof. |
| IJ-013 | Invite bound to wrong identity or device fails safely | C has an invite bound to a different user, device, or Peer ID. | 1. Attempt accept from the wrong identity and wrong device. 2. Replay via sync/mailbox. | Accept fails without creating group state or leaking key material. | P0 | Covered | Required | Required | Recommended | Recommended | Recommended | Covered by IJ-013 execution on 2026-05-01. `lib/features/groups/application/accept_pending_group_invite_use_case.dart` now revalidates persisted pending invites against the current local identity (`senderPeerId`) before reuse checks, signature authorization, materialization, group key persistence, inbox drain, or `group:join` side effects: `recipientPeerId` must match and `invitePolicy.allowedDevices` must contain the local peer id, otherwise the pending row is deleted and `invalidPayload` is returned. `test/features/groups/application/accept_pending_group_invite_use_case_test.dart` adds `IJ013 copied pending invite rejects wrong local identity before state or join`, proving a copied pending invite creates no pending row, consumed tombstone, group, key, message, or `group:join` side effect on the wrong local identity. Existing row-adjacent coverage remains green: `handle_incoming_group_invite_use_case_test.dart` proves matching bound invites accept and v1/v2 wrong-recipient invites reject before state; `store_pending_group_invite_use_case_test.dart` proves missing or mismatched local identity is not stored; `group_invite_listener_test.dart` proves copied signed invites do not enter pending state; `send_group_invite_use_case_test.dart` proves encrypted invite payloads bind `recipientPeerId` and `allowedDevices`. Commands: focused IJ-013 accept-pending test passed (`+1`); handle recipient-peer tests passed (`+3`); store local-identity test passed (`+1`); listener copied-invite test passed (`+1`); `flutter test --no-pub test/features/groups/application/*invite*_test.dart` passed (`+101`); `invite_round_trip_test.dart` passed (`+14`); `group_new_member_onboarding_test.dart` passed (`+6`); `./scripts/run_test_gates.sh groups` passed (`+97`); `git diff --check` passed. Supporting `group-real-network-nightly` was not run because `FLUTTER_DEVICE_ID` and `MKNOON_RELAY_ADDRESSES` are unset. Current trusted-private invites bind to the shipped local libp2p Peer ID / `allowedDevices` identity unit; IJ-014 now covers shipped inline group-key repair state. IJ-013 does not claim a separate account/device registry, sibling-device approval, EK/RP signatures/authorization rows, TP-SMOKE real-device proof, or first-class real relay/device proof. |
| IJ-014 | Welcome/key package delivery failure leaves clear pending repair state | C accepts invite but welcome/key material delivery fails or is stale. | 1. Accept with missing, stale, or undecryptable welcome material. 2. Trigger repair sync. | C does not join into an unusable silent state; the UI shows repair or rejoin requirements. | P0 | Covered | Required | Required | Recommended | Recommended | Recommended | Covered by IJ-014 execution on 2026-05-01 for the shipped inline group-key invite contract. `handle_incoming_group_invite_use_case.dart` now classifies explicit invalid, stale, or undecryptable join-material `BridgeCommandException` failures, rolls back partially materialized group, member, and group-key state, emits `GROUP_INVITE_HANDLE_JOIN_MATERIAL_REPAIR_PENDING`, and returns a repairable invalid-payload outcome instead of clearing the pending invite into an unusable joined group. Generic non-key-material `bridgeError` behavior remains preserved. `accept_pending_group_invite_use_case_test.dart` adds IJ-014 tests proving repairable join-material failure keeps the pending invite row, creates no consumed tombstone, group, member, key, message, publish, mailbox drain, or successful join side effect, and can retry successfully after fresh key material. `handle_incoming_group_invite_use_case_test.dart` proves direct materialization rolls back group/member/key state on a repairable welcome decrypt failure. `group_list_wired_test.dart` proves the pending invite remains visible and the UI shows the fresh key-material warning. Commands: focused IJ-014 accept tests passed (`+2`); focused IJ-014 direct handler test passed (`+1`); focused IJ-014 UI test passed (`+1`); adjacent UI preservation tests passed (`+2`); `flutter test --no-pub test/features/groups/application/*invite*_test.dart` passed (`+104`); `invite_round_trip_test.dart` passed (`+14`); `group_new_member_onboarding_test.dart` passed (`+6`); full `group_list_wired_test.dart` passed (`+17`); `./scripts/run_test_gates.sh groups` passed (`+97`); `git diff --check` passed. Supporting `group-real-network-nightly` was not run because `FLUTTER_DEVICE_ID` and `MKNOON_RELAY_ADDRESSES` are unset. IJ-014 does not claim first-class MLS welcome/key-package transport, a separate device identity registry, sibling-device approval, live three-party device proof, or first-class real relay/device proof. |

## Roles, Permissions, Membership Changes, Bans, and Leaving

| Test ID | Scenario | Preconditions | Steps | Expected Result | Priority | Current status | Unit | Integration | Smoke | Fake Network | 3-Party E2E | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| RP-004 | Every mutating action is authorized locally and again on receive | A, B, C, and X attempt every mutating group action. | 1. Exercise create, update, invite, approve, send, react, pin, remove, ban, rotate, close, and metadata edits. 2. Replay as remote events. | Unauthorized actions are blocked before local side effects and revalidated on receive before applying remote state. | P0 | Covered | Required | Required | Required | Required | Recommended | Covered by RP-004 execution on 2026-05-01 for shipped trusted-private group mutating surfaces. `GroupKeyUpdateListener` now re-authorizes direct `group_key_update` receive messages by requiring `message.from` to be a current group member with effective `rotateKeys` permission before `group:updateKey`, event-log append, or key save. `group_key_update_listener_test.dart` adds RP004 tests proving an unauthorized writer direct key update is ignored before bridge update, log, or key save while a writer with explicit `rotateKeys` override is accepted. `member_removal_integration_test.dart` now seeds authorized sender membership for direct key-update receive fixtures and proves receiver key-update sync plus voluntary-leave key-update receipt remain green under the new auth contract. Existing local authorization evidence remains green for add, remove, role update, key rotation, metadata edit, send, send reaction, and remove reaction; existing `group_message_listener_test.dart` proves unauthorized remote `member_added`, `members_added`, `member_removed`, `member_role_updated`, `group_metadata_updated`, and `group_dissolved` events leave state/bridge unchanged. Commands: focused RP004 key-update tests passed (`+2`); full `group_key_update_listener_test.dart` passed (`+18`); generic receive mutation guard passed (`+1`); focused local mutation guard tests each passed (`+1`); `member_removal_integration_test.dart` passed (`+5`); `group_membership_smoke_test.dart` passed (`+24`); `./scripts/run_test_gates.sh groups` passed (`+97`); `flutter test --no-pub test/features/groups/integration` passed (`+119`); `git diff --check` passed. The broad `flutter test --no-pub test/features/groups/application` command still fails unrelated existing MD-011 drain-inbox media replay coverage, which also fails in isolation and is not caused by RP-004. Supporting `group-real-network-nightly` was not run because `FLUTTER_DEVICE_ID` and `MKNOON_RELAY_ADDRESSES` are unset. RP-004 covers shipped role/metadata/key/send/reaction/invite/removal mutation paths; first-class group pin, message edit/delete, and ban product flows are not shipped and are not claimed by this row. |
| RP-005 | Stale permission rejection after demotion, removal, or role race | B queues actions while authorized, then loses permission before publish or receive. | 1. Queue invite, send, metadata, role, and key actions. 2. Demote/remove B. 3. Reconnect and publish queued actions. | Actions signed under stale state are rejected or rechecked before publication and cannot mutate group state. | P0 | Covered | Required | Required | Recommended | Required | Recommended | Covered by RP-005 execution on 2026-05-01. `GroupMessageListener` now rechecks current sender membership/role state for receive-side membership and metadata mutation events instead of trusting stored `createdBy` after the creator is demoted or removed; missing senders are rejected, role updates keep bounded permission/escalation checks, and self-removal still requires current membership. `group_message_listener_test.dart` adds `RP005 demoted creator receive-side mutations are rejected before side effects`, proving demoted creator-originated `member_added`, `member_removed`, `member_role_updated`, and signed `group_metadata_updated` events leave group/member/timeline state unchanged and do not call `payload.verify` or `group:updateConfig`. Existing local stale queued-action guards remain green for add, remove, role update, key rotation, metadata edit, send, and failed-message retry; existing receive stale watermark tests remain green for older metadata and role/member events after newer state. Commands: focused RP005 listener test passed (`+1`); metadata/role watermark tests passed (`+1` each); focused local stale guard tests passed (`+1` each); full `group_message_listener_test.dart` passed (`+81`); `group_membership_smoke_test.dart` passed (`+24`); `./scripts/run_test_gates.sh groups` passed (`+97`); `flutter test --no-pub test/features/groups/integration` passed (`+119`). The broad `flutter test --no-pub test/features/groups/application` command still fails unrelated existing MD-011 drain-inbox media replay coverage, and the MD-011 test fails in isolation. Supporting `group-real-network-nightly` was not run because `FLUTTER_DEVICE_ID` and `MKNOON_RELAY_ADDRESSES` are unset. RP-005 covers shipped local stale-action rechecks and receive-side stale mutation rejection; it does not claim a broader cryptographic actor-signature matrix, first-class real transport/device proof, or unshipped product surfaces. |
| RP-006 | Permission escalation protection | B has limited moderation permission. | 1. B tries to grant themselves or others higher permissions. 2. Replay remote role changes. | Members cannot grant permissions they do not possess, and forged/escalated role changes are rejected. | P0 | Covered | Required | Required | Recommended | Recommended | Recommended | Covered by RP-006 execution on 2026-05-01 for shipped role-update escalation protection. `group_role_update_authorization.dart` now blocks non-admin actors from assigning admin or changing an existing admin role and still rejects explicit permission grants the actor does not hold. `update_group_member_role_use_case.dart` now loads the target member before the local escalation check and passes current target role and permissions into the shared helper before state mutation or bridge sync. `group_message_listener.dart` uses the same helper for receive-side `member_role_updated`, so replayed limited-manager role changes cannot promote a member, demote/touch an existing admin, or grant unheld permissions before member state, timeline rows, or `group:updateConfig` side effects. Direct tests: `update_group_member_role_use_case_test.dart` covers `writer with manage-roles permission cannot promote a member to admin`, `writer with manage-roles permission cannot demote an admin`, and `allows writer with manage-roles permission override to update role`; `group_message_listener_test.dart` covers `limited manager member_role_updated cannot promote a member to admin`, `limited manager member_role_updated cannot demote an admin`, and `limited manager member_role_updated cannot grant unheld permissions`. `drain_group_offline_inbox_use_case_test.dart` self-removal replay fixtures now seed the authorized admin sender required by stricter receive-side authorization. Passed gates: focused RP-006 tests passed, full `update_group_member_role_use_case_test.dart` passed `+11`, full `group_message_listener_test.dart` passed `+82`, focused self-removal replay fixtures passed, `group_membership_smoke_test.dart` passed `+24`, `./scripts/run_test_gates.sh groups` passed `+97`, and `flutter test --no-pub test/features/groups/integration` passed `+119`. Broad `flutter test --no-pub test/features/groups/application --reporter json` still fails only the unrelated existing MD-011 future-media replay test, which remains outside RP-006. Supporting `group-real-network-nightly` was not run because relay/device env is unset. RP-006 does not claim a first-class permission-edit UI/API, broad cryptographic actor-signature matrix, account/device registry, real-device proof, or unshipped pin/delete product surfaces. |
| RP-014 | Voluntary leave key rotation policy is enforced | B leaves a group where post-leave access to future content is blocked by policy. | 1. B leaves. 2. A sends after leave. 3. B replays and attempts decrypt. | If policy requires rotation on leave, B cannot decrypt post-leave content; remaining members converge on the new epoch. | P0 | Covered | Required | Required | Recommended | Recommended | Required | Covered by RP-014 execution on 2026-05-01 for the shipped voluntary-leave policy path. `broadcast_voluntary_leave_use_case.dart` now owns the application-layer leave preparation: it loads local identity, skips sole-admin/no-self cases, publishes a `member_removed` self-removal, stores durable replay only for remaining members, rotates/distributes the next group key to remaining members, and throws `voluntaryLeaveRotationFailedMessage` before local cleanup if rotation cannot complete while remaining members exist. `group_info_wired.dart` now calls this helper before `leaveGroup`, preserving leave ordering while removing the policy from a screen-private helper. `member_removal_integration_test.dart` extends `voluntary leave rotation excludes leaver and remaining members send on rotated epoch` to use the helper, prove key updates go only to admin/Bob and not the leaver, prove admin/Bob save/promote epoch 2, prove `leaveGroup` removes the leaver's group/member/key state, prove the first post-leave send uses epoch 2 and inbox recipients exclude the leaver, prove normal post-cleanup drain does not retrieve a deleted group, and prove a forced post-leave replay attempt with only removed local key state saves an `undecryptable` placeholder instead of future plaintext. Passed gates: focused RP-014 integration test passed (`+1`), focused multi-admin and writer leave UI tests passed (`+1` each), full `group_info_wired_test.dart` passed (`+27`), `group_membership_smoke_test.dart` passed (`+24`), `./scripts/run_test_gates.sh groups` passed (`+97`), and `flutter test --no-pub test/features/groups/integration` passed (`+119`). Supporting `group-real-network-nightly` was not run because `FLUTTER_DEVICE_ID` and `MKNOON_RELAY_ADDRESSES` are unset. RP-014 does not claim a configurable leave-rotation product setting, packet-capture proof, real-device proof, first-class ban/unban policy, or broader removed-peer publish/dial isolation. |
| RP-017 | Removed peer isolation blocks continued publishing and dialing | C is removed but keeps publishing to the old topic and dialing peers. | 1. Remove C. 2. C publishes live messages, sync requests, media, and key events. 3. Observe connections and local rejection behavior. | Peers disconnect, block, or ignore C according to policy; no future plaintext/media rows or keys are accepted. | P0 | Covered | Required | Required | Recommended | Required | Recommended | FR-103, FR-259, FR-281. Covered for the shipped ignore/filter policy: `TestRP017RemovedPeerContinuedPublishesAreRejectedBeforeAcceptAndForward` proves a removed peer's raw live message, reaction, membership, metadata, and key-rotation publishes are rejected as non-member traffic before accepted delivery or forwarding; `TestRP017RemovedPeerExcludedFromKnownAndDiscoveredDialsAfterConfigUpdate` proves `UpdateGroupConfig` removal excludes the removed peer from known-member and rendezvous discovery dials while remaining members stay eligible. Flutter removed-member, media ACL, inbox replay, retry, listener, and key-rotation tests prove removed members cannot send/retry, future sends/keys/inbox/media ACLs exclude them, replayed self-removal cuts off later queued traffic, and future-media replay with only an old epoch saves only an `undecryptable` placeholder with no plaintext, media download, or decrypt. `./scripts/run_test_gates.sh groups`, full groups integration, and `git diff --check` passed. Caveats: this is ignore/filter behavior, not forced transport disconnect or peer downscore; `group-real-network-nightly` was not run. |
| RP-018 | Membership add, remove, and role conflicts converge deterministically | A and B perform conflicting membership actions while partitioned. | 1. Concurrently add/remove/promote/demote C. 2. Heal partition. 3. Sync all peers. | Every peer converges on the same membership and permission state with clear conflict diagnostics if needed. | P0 | Covered | Required | Required | Recommended | Required | Required | Covered by RP-018 execution on 2026-05-01. `group_message_listener.dart` now resolves shipped membership conflict replays deterministically: stale `member_removed` events still apply when the target currently exists from before the removal timestamp, old removals are ignored after explicit re-adds, and `member_role_updated` for a missing target is rejected before a stale snapshot can recreate a removed member. `group_message_listener_test.dart` adds `RP018 stale removal beats role replay and later role cannot resurrect`, proving remove-over-role conflict handling and diagnostics (`GROUP_MESSAGE_LISTENER_STALE_MEMBER_REMOVED_CONFLICT_APPLIED`, `GROUP_MESSAGE_LISTENER_MEMBER_ROLE_UPDATE_MISSING_TARGET_IGNORED`). `group_membership_smoke_test.dart` adds `RP018 partitioned add remove promote demote replay converges membership`, proving add/remove/promote/demote replay after partition heal converges all remaining peers on the same member/role map. Focused RP-018 listener and smoke tests, full listener and smoke files, resume churn, key conflict, `./scripts/run_test_gates.sh groups` (`+98`), full groups integration (`+120`), and `git diff --check` passed. Supporting `group-real-network-nightly` was not run because relay/device env is unset. |

## Messaging, Ordering, Editing, Deletion, Reactions, Mentions, and Drafts

| Test ID | Scenario | Preconditions | Steps | Expected Result | Priority | Current status | Unit | Integration | Smoke | Fake Network | 3-Party E2E | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| MS-002 | Message author, device identity, and Peer ID binding are verified | B sends from a valid device; X spoofs B or uses mismatched Peer ID. | 1. Deliver valid, spoofed, and device-mismatched messages. 2. Inspect validators and stored author fields. | Every stored message has verifiable author and device identity bound to the transport Peer ID. | P0 | Covered | Required | Required | Required | Required | Recommended | Covered by MS-002 execution on 2026-05-01. `go-mknoon/node/pubsub.go` already rejects claimed `senderId` versus libp2p transport Peer ID mismatches before member lookup, authorization, signature verification, decrypt, accept, or forwarding, and now emits `transportPeerId` in `group_message:received` events after that validator binding. Migration 061 adds nullable `group_messages.transport_peer_id`; `GroupMessage`, DB helpers, and `GroupMessageRepositoryImpl` persist and surface it. `handle_incoming_group_message_use_case.dart`, `group_message_listener.dart`, `drain_group_offline_inbox_use_case.dart`, and `send_group_message_use_case.dart` propagate the verified transport Peer ID through live, retry, and offline inbox paths, and reject nonempty transport/sender mismatches before event-log or message persistence side effects. Direct tests cover Go received-event metadata and transport mismatch validators, migration/helper/repository mapping, app-layer live mismatch rejection, offline inbox relay `from` binding, and fake-network live valid-versus-invalid transport binding. Focused MS-002 tests, full group-message application wildcard (`+213`), `./scripts/run_test_gates.sh groups` (`+99`), full groups integration (`+121`), and `git diff --check` passed. Supporting `group-real-network-nightly` was not run because relay/device env is unset. The shipped device identity unit remains the libp2p Peer ID; separate account/device registry and per-device key-package identity are outside MS-002. |
| MS-003 | Clock skew does not corrupt message order | A, B, and C have skewed clocks. | 1. Send messages with past, future, and normal client timestamps. 2. Sync through live and inbox paths. | Receive timestamps and ordering rules prevent impossible timeline jumps or state corruption while preserving useful client-time display. | P0 | Covered | Required | Required | Recommended | Required | Recommended | Covered by MS-003 execution on 2026-05-01. `handle_incoming_group_message_use_case.dart` clamps timestamps beyond the five-minute future-skew window to receive time before event-log append, membership cutoff checks, persistence, and latest-message selection. `group_conversation_wired.dart` and `group_group_messages_into_threads.dart` now order equal timestamp ties by message id for live conversation upserts and group feed projection; DB and fake repositories already use timestamp/id ordering. Focused MS003 tests cover direct handler past/current/near-future/far-future normalization, offline inbox far-future replay/latest selection, fake-network live skew convergence across recipients, wired equal-timestamp order, and feed equal-timestamp order. Commands passed: focused handle-incoming (`+2`), drain-inbox (`+1`), fake-network live (`+1`), wired (`+1`), feed (`+1`), full group-message application wildcard (`+213`), `./scripts/run_test_gates.sh groups` (`+100`), full groups integration (`+122`), and `git diff --check`. Supporting `group-real-network-nightly` was not run because relay/device env is unset. MS-004 still owns causal references and concurrent ordering beyond timestamp/id ordering. |
| MS-004 | Deterministic ordering and causal references under concurrent sends | A, B, and C send concurrently and reply to recent messages. | 1. Send concurrent messages across partitions. 2. Include parent or previous-state references. 3. Heal and sync. | All peers render a deterministic order and replies remain attached to the right causal parent. | P0 | Covered | Required | Required | Recommended | Required | Required | Covered by MS-004 execution on 2026-05-01. `group_message_ordering.dart` defines deterministic timeline ordering: unrelated messages sort by timestamp/id, and present quoted parents are placed before replies with cycle-safe fallback. `GroupMessageRepositoryImpl`, `InMemoryGroupMessageRepository`, `GroupConversationWired`, and `groupGroupMessagesIntoThreads` now use that ordering for loaded pages, fake-network/users, live upserts, and feed projection. Focused tests prove DB/fake repo parent-before-reply even when timestamp/id would invert, feed and wired live parent-before-reply, fake-network A/B/C equal-timestamp concurrent sends plus quoted replies converge on all peers, and partition/offline replay preserves parent/reply order and `quotedMessageId` despite reply id sorting earlier. Commands passed: focused repo (`+2`), feed (`+1`), wired (`+1`), fake-network live (`+1`), resume replay (`+1`), group-message application wildcard (`+213`), `./scripts/run_test_gates.sh groups` (`+100`), full groups integration (`+122`), and `git diff --check`. Supporting `group-real-network-nightly` was not run because relay/device env is unset. MS-004 closes on shipped `quotedMessageId` causal parent reference; no vector clocks, previous-state DAG, account/device registry, or real-device packet proof is claimed. |
| MS-018 | Messages created during key rotation bind to exactly one valid epoch | A rotates key while B sends. | 1. Start rotation. 2. Send before, during, and after epoch commit. 3. Deliver out of order. | Each message is bound to a specific epoch and is sent, queued, or rejected according to current validity. | P0 | Covered | Required | Required | Recommended | Required | Required | Covered by MS-018 execution on 2026-05-01. Existing app-layer tests prove `sendGroupMessage` snapshots the latest committed local key into both `GroupMessage.keyGeneration` and encrypted replay `keyEpoch`, keeps epoch 1 when epoch 2 is saved before publish returns, binds before/during/after rotation sends to epochs 1/1/2, keeps pending key-update sends on the old epoch until local commit, persists mixed epoch inbox replay without rewriting epochs, and stores future-epoch replay as a safe undecryptable placeholder. New fake-network 3-party proof in `group_messaging_smoke_test.dart` models Alice rotating to epoch 2 while Bob sends before, during, and after Bob's local epoch-2 commit; Charlie's live deliveries are held and released in reverse order, and Bob, Alice, and Charlie persist the same message ids under epochs 1/1/2 with no duplicate or rewritten epoch. Commands passed: new fake-network MS018 proof (`+1`), send MS-018 suite (`+2`), pending key-update send proof (`+1`), epoch inbox replay proof (`+3`), group-message application wildcard (`+213`), `./scripts/run_test_gates.sh groups` (`+101`), full groups integration (`+123`), and `git diff --check`. Supporting `group-real-network-nightly` was not run because relay/device env is unset. No packet-capture/device-lab proof, account/device registry, MLS commit semantics, or new transport cryptography is claimed. |

## Encryption, Key Epochs, Identity Verification, and Secret Handling

| Test ID | Scenario | Preconditions | Steps | Expected Result | Priority | Current status | Unit | Integration | Smoke | Fake Network | 3-Party E2E | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| EK-003 | Member identity key, device key, and Peer ID binding are enforced | B rotates devices or Peer IDs; X spoofs identity fields. | 1. Bind valid device to member identity. 2. Attempt spoofed Peer ID and device key mismatch. | Only correctly bound identities can send, receive key updates, or apply membership events. | P0 | Covered | Required | Required | Recommended | Required | Recommended | EK-003 covered by `PREREQ-DEVICE-IDENTITY` on 2026-05-01. `GroupMemberDeviceIdentity` plus migration `062_group_member_device_identities.dart` persists per-member device rosters while `GroupMember.peerId` remains the member/account identity; config snapshots, invites, key distribution, live messages/reactions, offline replay, fakes, and Go envelopes/config validation now carry device id, transport peer id, device signing key, ML-KEM/key-package material, status, and stable key-package id. Valid bound devices are accepted, while unbound sibling devices, signing-key mismatches, transport mismatches, wrong local invite recipients, wrong key-update recipients, and invalid replay senders reject before message, key, event-log, listener, notification, or bridge side effects. Passed evidence: model/invite domain block, migration/helper/full-chain block, invite admission block, key distribution/listener block, message/listener/offline replay block, fake-network integration block, Go envelope/device validator blocks, `./scripts/run_test_gates.sh groups`, `./scripts/run_test_gates.sh completeness-check`, and `git diff --check`. Paired-iOS and real-relay proof remain supporting only; Android paired proof is fixture-blocked by missing `adb` and `emulator-5556`. |
| EK-004 | Signature verification covers every security-relevant event type | Messages, invites, membership, roles, settings, key updates, and audit events are signed. | 1. Tamper each event family. 2. Replay with valid and invalid signatures. | Every invalid signature is rejected before local state mutation and produces privacy-safe diagnostics. | P0 | Covered | Required | Required | Required | Required | Recommended | Covered by EK-004 gap-closure on 2026-05-02. Current `group_offline_replay` envelopes are signed before storage and verified before decrypt/apply in `group_offline_replay_envelope.dart` and `drain_group_offline_inbox_use_case.dart`; unsigned legacy relay payloads, malformed/mismatched/invalid replay signatures, wrong sender/relay bindings, invalid history repair ranges, and pending-key repair replays fail closed before message, reaction, system/member/key/timeline, cursor, notification, receipt/read-state, or event-log mutation. Sender-side signed replay generation is covered for message, reaction add/remove/retry, invite accept/member join, voluntary leave/member removal, metadata/role/admin flows, dissolve, and presentation send hooks, while direct invite, invite revocation, welcome/key-package-in-invite, direct `group_key_update`, signed transition audit, local `group_created` event-log, and Go live PubSub signature validators remain preserved. Evidence passed: focused EK004 Flutter bundle (`+16`), direct key/audit/remote-family selector (`+12`), local create/event-log (`+19`), invite wildcard (`+149`), key wildcard (`+49`), fake-network invite/resume/membership replay bundle (`+80`), Go invalid-signature selector for message/reaction/member_added/members_added/member_removed/member_banned/member_unbanned/member_role_updated/group_message_deleted/group_metadata_updated/group_dissolved/key_rotated, targeted format, scoped analyzer with info-only diagnostics, `groups` (`+103`), `completeness-check` (`712/712`), and `git diff --check`. `group_info_wired.dart` still has the documented pre-existing warning-only analyzer cluster and no analyzer errors. Real device/relay proof was not required because the row closed on deterministic host/fake-network/Go replay-validation seams; full MLS semantics and separate account/device registry remain out of scope. |
| EK-005 | Unknown future epoch messages queue and trigger key sync repair | B receives a message encrypted for an epoch not yet known. | 1. Deliver future-epoch message. 2. Deliver missing epoch transitions. 3. Attempt decrypt. | Future messages are queued or marked pending until key sync completes, then decrypt or become safely undecryptable. | P0 | Covered | Required | Required | Recommended | Required | Recommended | Covered by `PREREQ-FUTURE-EPOCH-KEY-REPAIR` execution on 2026-05-01. Migration `063_group_pending_key_repairs`, DB helpers, and `GroupPendingKeyRepairRepositoryImpl` add a durable deduped pending queue. Offline future/missing-key replay now stores a safe pending placeholder, emits a scoped key-repair request, and preserves the encrypted replay envelope; `GroupKeyUpdateListener` retries pending repairs after a saved key update; `GroupPendingKeyRepairRunner` replays valid queued envelopes through the normal verified handler and replaces the placeholder, while unrecoverable retries finalize idempotently to safe `undecryptable` state without plaintext, media downloads, duplicate rows, or endless retries. Evidence passed: RED compile failure before implementation; migration/helper/repository tests; focused drain/key-listener/message-listener/UI/bridge tests; full migration chain; owner Flutter suites (`+178`, `+36`, `+49`); Go focused and owner regexes; `./scripts/run_test_gates.sh groups` (`+101`); `./scripts/run_test_gates.sh completeness-check` (`703/703`); and `git diff --check`. Unknown future live epochs may still reject before normal Go delivery; repair is provided by the durable offline replay/key-arrival path. |
| EK-006 | Stale epoch messages from invalid senders are rejected | C sends after removal or after stale epoch expiry. | 1. Remove C and rotate keys. 2. Deliver old-epoch and grace-window messages. 3. Expire grace and replay. | Only policy-allowed grace traffic is accepted; stale or unauthorized epoch traffic is rejected. | P0 | Covered | Required | Required | Recommended | Required | Recommended | EK-006 accepted on 2026-05-01. Raw Go proof in `go-mknoon/node/pubsub_key_rotation_grace_test.go` covers authorized previous-epoch acceptance during grace, previous-epoch rejection after grace expiry, current-epoch acceptance during grace, removed/non-member previous-epoch rejection during grace as `reject:non_member`, previous-epoch subscription decrypt during grace, and previous-epoch subscription drop after grace expiry with no `group_message:received`. App-level proof covers removed-sender cutoff behavior: `send_group_message_use_case_test.dart` rejects local stale sends before persistence or bridge publish; `handle_incoming_group_message_use_case_test.dart` accepts only pre-cutoff removed-sender messages and rejects at-cutoff/later replay without overwrite; `drain_group_offline_inbox_use_case_test.dart` carries the removedAt cutoff across replay cursor pages; and `group_membership_smoke_test.dart` proves remaining peers accept only delayed pre-cutoff removed-sender envelopes while no at-cutoff UI rows appear, and a self-removed member cannot send after cleanup. Commands passed: focused Go EK-006 grace/expiry suite, focused Flutter removed-sender handler (`+3`), offline replay cutoff (`+1`), stale local send (`+1`), live fake-network cutoff (`+1`), self-removal send guard (`+1`), and `git diff --check`. Supporting real-network/device-lab/packet-capture proof remains supplemental and was not run. No first-class device identity, MLS commit semantics, or new transport cryptography is claimed. |
| EK-011 | Welcome messages and key packages are validated before admission | C joins with valid, stale, malformed, and weak key packages. | 1. Submit each key package. 2. Attempt welcome decrypt and first send. | Only valid key packages are admitted and welcome material decrypts on the intended device. | P0 | Covered | Required | Required | Recommended | Recommended | Recommended | Covered by `PREREQ-WELCOME-KEY-PACKAGE` Executor and recovery evidence on 2026-05-01. `GroupWelcomeKeyPackage` now models package id/material/hash, recipient member/device/transport/ML-KEM binding, invite id, group id, epoch, issue time, expiry, and schema version; `GroupInvitePayload`/`GroupInvitePolicy` bind that object into the signed canonical encrypted invite payload. Send rejects weak package material before signing/encryption. Store, direct handle, and pending accept reject stale, malformed, tampered, wrong-recipient, wrong-device, wrong-transport, wrong-package, and weak packages before pending, group, member, key, join, mailbox, publish, listener, notification, or bridge side effects. Migration/helper/repository wiring adds durable `group_welcome_key_package_tombstones`, and accept records the tombstone only after successful materialization; replaying the same package under a changed invite id fails before state. File-backed repository tests prove close/reopen tombstone survival. The recovery pass adds `defaultGroupWelcomeKeyPackageIdForDevice`, wires `ownKeyPackageId` through `main.dart`, `GroupListWired`, and `OrbitWired`, and proves valid first-class package invites store/accept through production listener and UI pending-accept paths while a wrong local package id still rejects. Evidence passed: required focused welcome-package command (`120` tests after recovery), recovery listener/UI focused tests (`3` + `1` + `1` tests), invite wildcard (`117` tests), targeted analyzer for edited owner files, `./scripts/run_test_gates.sh groups` (`101` tests), `./scripts/run_test_gates.sh completeness-check` (`706/706`), and `git diff --check`. This closure is scoped to the shipped signed encrypted invite architecture, not full MLS welcome messages or a server-side key-package directory. |
| EK-012 | Replay protection covers messages, commits, invites, and key packages | X replays old encrypted payloads and key artifacts. | 1. Replay each artifact family with same and modified metadata. 2. Observe DB, epoch, and diagnostics. | Replays are rejected or idempotent and cannot roll back membership, permissions, or keys. | P0 | Covered | Required | Required | Required | Required | Recommended | Covered by `PREREQ-REMOTE-EVENT-FAMILIES` final QA on 2026-05-01, combined with prior replay prerequisites. The remaining unmodeled replay-family blocker is closed by trusted-private encrypted system-event support for `member_banned`, `member_unbanned`, and `group_message_deleted`: `trusted_private_group_system_event.dart` parses canonical tombstone fields, `GroupMessageListener` routes those families through existing device binding, authorization, signed transition audit/event-log, deterministic tombstone/timeline rows, duplicate replay idempotency, stale ban-after-unban/rejoin protection, and stale/wrong-group/newer-message delete protection. Go invalid-signature coverage now includes the new event families. Prior prerequisites keep welcome/key-package tombstones, signed system-transition replay, durable receipt replay, future/missing-key repair replay, message/reaction replay, and invite replay covered. Evidence passed: PREREQ listener tests (`+3`), offline replay tests (`+1`), Go invalid-signature regex, `groups`, `completeness-check`, and `git diff --check`; targeted analyzer still reports only the documented pre-existing warning debt in `group_message_listener.dart` and no analyzer errors. EK-004 is covered separately for full offline replay signature-equivalence. |

## Offline Delivery, Sync, Reconnect, and History Repair

| Test ID | Scenario | Preconditions | Steps | Expected Result | Priority | Current status | Unit | Integration | Smoke | Fake Network | 3-Party E2E | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| OS-006 | Partial history, gap detection, and multi-peer gap repair | B only receives some pages or one peer lacks history. | 1. Create missing ranges. 2. Detect gaps. 3. Repair from multiple peers. | The UI marks partial history honestly and repairs gaps without duplicate or out-of-order rows. | P0 | Covered | Required | Required | Recommended | Required | Recommended | Covered by `PREREQ-HISTORY-GAP-REPAIR` executor and fix-pass evidence on 2026-05-01. Production adds durable `GroupHistoryGapRepair` lifecycle storage through migration/helper/repository wiring, relay-authorized cursor gap metadata, relay/Go/Dart `group:historyRepairRange` retrieval, inbox-page history-gap parsing, drain orchestration that persists detected/repairing/failed/repaired state, deterministic range-hash validation, authorized multi-source fallback, and repaired-envelope application through the existing replay/listener validation and dedupe path. UI evidence distinguishes active, failed, and repaired gap state from backlog-retention expiry. Direct evidence passed: migration/helper tests, focused full migration-chain table proof, `drain_group_offline_inbox_use_case_test.dart --plain-name 'PREREQ-HISTORY-GAP-REPAIR'`, fake-network `group_resume_recovery_test.dart --plain-name 'PREREQ-HISTORY-GAP-REPAIR'`, conversation UI `--plain-name 'PREREQ-HISTORY-GAP-REPAIR'`, bridge helper cursor history-gap parsing, Dart bridge `--plain-name 'history repair'`, Go node/bridge `TestGroupHistoryRepair*` / `TestGroupInboxRetrieveWithCursorResult*` / `TestBridgeGroupHistoryRepair*`, `go-relay-server go test ./...`, targeted analyzer over touched Dart files, `./scripts/run_test_gates.sh groups`, `./scripts/run_test_gates.sh completeness-check`, and `git diff --check`. Scope caveat: host/fake-network proof is primary; full MLS history, server archive, packet capture, Android paired-device proof, and broad transport rewrite remain out of scope. |

## Local Database, Migrations, Event Log, Search, and State Recovery

| Test ID | Scenario | Preconditions | Steps | Expected Result | Priority | Current status | Unit | Integration | Smoke | Fake Network | 3-Party E2E | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| DB-002 | Signed append-only or tamper-evident local event log | Remote group events are applied locally. | 1. Persist membership, metadata, messages, roles, and key commits. 2. Tamper and replay. | The local event log detects tampering and can explain applied state without silent mutation. | P0 | Covered | Required | Required | Recommended | N/A | N/A | Covered by DB-002 execution on 2026-05-01. Migration 060 and `group_event_log_db_helpers.dart` provide a tamper-evident per-group hash-chain event log with canonical payloads, source-event uniqueness, idempotent exact replay, conflicting replay rejection through `GroupEventLogTamperException`, and row tamper verification. `main.dart` wires the DB append helper to group message, invite, and now direct key-update listeners; the DB-002 session added the missing production `GroupKeyUpdateListener` wiring. Incoming messages, membership/metadata/role system events, `key_rotated` system events, and direct `group_key_update` commits append before silent local DB mutation; focused tests prove tampered replay blocks incoming messages, `member_added`, signed `group_metadata_updated`, `member_role_updated`, and direct key updates, while exact duplicate direct key-update replay remains idempotent. Passed commands are recorded in `trusted_private_libp2p_group_chat_missing_test_matrix_full_with_rules-session-DB-002-plan.md`. This row does not claim MLS signed commit-transition support, first-class key-package replay protection, external device proof, or a per-actor signed audit model; EK-004, EK-012, and DB-012 remain separate. |
| DB-004 | Transactional update boundaries survive crash | Message insert, receipt update, and sync cursor update happen together. | 1. Crash between each operation boundary. 2. Restart and repair. | No half-applied state causes duplicate messages, lost receipts, or advanced cursors without rows. | P0 | Covered | Required | Required | Recommended | N/A | N/A | Covered by `PREREQ-GROUP-SYNC-RECEIPTS` final QA on 2026-05-01. Migration 066 adds durable `group_inbox_cursors` and `group_message_receipts`; `group_sync_receipts_db_helpers.dart` loads/upserts durable cursors and receipts and applies message insert, receipt/read-state update, and cursor advancement in one SQLite transaction. `GroupMessageRepositoryImpl` and `main.dart` expose transaction-scoped page apply, while `drain_group_offline_inbox_use_case.dart` starts replay from the durable cursor and advances only through `runInboxPageTransaction`. Normal and system replay now use `msgRepoOverride` plus `rethrowOnError: true`; system timeline saves use the transaction-scoped repository, so listener failures roll back message rows, receipts/read-state, timeline rows, emitted events, and cursor advancement. Evidence passed: migration 066 test (`+1`), sync helper tests (`+4`), repository PREREQ tests (`+2`), drain PREREQ tests (`+5`), full-chain v65-to-v66 and fresh-install proofs, `group_message_listener_test.dart` (`+88`), `./scripts/run_test_gates.sh groups` (`+102`), `./scripts/run_test_gates.sh completeness-check` (`710/710`), scoped analyzer with only documented pre-existing listener warning debt outside the clean slice, and `git diff --check`. |
| DB-006 | Plaintext secret keys are excluded from ordinary message tables | Secret keys, media keys, and recovery material exist locally. | 1. Store and rotate keys. 2. Inspect group, message, media, and event tables. | Secrets are stored only in approved secure storage, not ordinary tables. | P0 | Covered | Required | Required | Recommended | N/A | N/A | Covered by `PREREQ-SECRET-STORAGE-WRAPPING` final QA on 2026-05-01. `secret_storage_references.dart` defines deterministic `secure:` references for media attachment keys and group key material, and `legacy_group_secret_storage_scrub.dart` migrates existing plaintext-equivalent `media_attachments.encryption_key_base64` and `group_keys.encrypted_key` SQL rows into `SecureKeyStore` while rewriting SQL values to references. `MediaAttachmentRepositoryImpl` and `GroupRepositoryImpl` now store actual key material in the primary secure store, persist only references in ordinary SQL rows, hydrate models when secure material exists, and fail closed when referenced secure material is missing; `GroupRepositoryImpl.mirrorAllKeysToSecureStore` skips unresolved references and refuses to mirror `secure:` reference text. `main.dart` runs the scrub before group-key mirroring and injects the primary secure store into production media/group repositories. Evidence passed: PREREQ media repository tests (`+5`), group repository tests (`+5`), legacy scrub tests (`+3`), media/group helper tests, fresh full-migration-chain proof, targeted analyzer, `./scripts/run_test_gates.sh groups`, `./scripts/run_test_gates.sh completeness-check` (`711/711`), and `git diff --check`. DB-012 and EK-012 remain separate event-family/replay rows. |
| DB-012 | Idempotent apply covers every remote event type | Remote events can be delivered multiple times. | 1. Apply duplicate messages, metadata, roles, joins, leaves, bans, deletes, receipts, media, and commits. | Applying the same event repeatedly produces the same local state and no duplicate visible timeline spam. | P0 | Covered | Required | Required | Recommended | Recommended | Recommended | Covered by `PREREQ-REMOTE-EVENT-FAMILIES` final QA on 2026-05-01. The last row-named event-family gaps now have production apply models/tests: trusted-private `member_banned`, `member_unbanned`, and `group_message_deleted` route through `GroupMessageListener` with deterministic tombstone/timeline rows, duplicate replay idempotency, stale-state guards, authorization, signed transition audit/event-log wiring, and offline replay through the existing transaction-scoped listener path. Existing prior evidence covers duplicate/idempotent apply for messages, media enrichment, reactions, `member_added`, `members_added`, non-self/self `member_removed`, `member_role_updated`, signed `group_metadata_updated`, `group_dissolved`, `key_rotated`, direct `group_key_update`, welcome/key-package tombstones, durable receipts, and live-plus-inbox duplicate replay. Evidence passed: `group_message_listener_test.dart --plain-name 'PREREQ-REMOTE-EVENT-FAMILIES'` (`+3`), `drain_group_offline_inbox_use_case_test.dart --plain-name 'PREREQ-REMOTE-EVENT-FAMILIES'` (`+1`), Go invalid-signature regex, `./scripts/run_test_gates.sh groups`, `./scripts/run_test_gates.sh completeness-check`, and `git diff --check`; targeted analyzer has only documented pre-existing listener warnings. |

## Error Handling, Retry Behavior, Recovery Prompts, Audit, and Observability

| Test ID | Scenario | Preconditions | Steps | Expected Result | Priority | Current status | Unit | Integration | Smoke | Fake Network | 3-Party E2E | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| ER-001 | Invalid signature diagnostics are privacy-safe and actionable | X sends invalid signatures for every event family. | 1. Inject invalid signatures. 2. Inspect logs, metrics, and UI. | Invalid items are rejected, counted, and logged without secret material or sensitive peer addresses. | P0 | Covered | Required | Required | Recommended | Required | N/A | Covered by ER-001 execution on 2026-05-01. `go-mknoon/node/pubsub.go` now emits rate-limited `group:validation_rejected` diagnostics from validator reject paths using only `reason`, `groupHash`, `senderHash`, `transportPeerHash`, `localPeerHash`, `envelopeType`, and `keyEpoch`; the existing logs use the same hashed fields. `TestER001InvalidSignatureDiagnosticsArePrivacySafeAndActionable` injects invalid signatures for shipped event families including messages, reactions, `member_added`, `members_added`, `member_removed`, `member_role_updated`, `group_metadata_updated`, `group_dissolved`, and `key_rotated`, then proves reject, one actionable diagnostic, and no raw group IDs, peer IDs, group names, keys, signatures, ciphertexts, nonces, plaintext, or sensitive multiaddrs in logs or diagnostic events. `GoBridgeClient` forwards `group:validation_rejected` into `groupDiagnosticEventStream` without invoking the normal group-message callback. Passed commands are recorded in `trusted_private_libp2p_group_chat_missing_test_matrix_full_with_rules-session-ER-001-plan.md`. This row covers shipped validator event families only; unmodeled bans, remote deletes, receipts, and commit/key-package transitions remain owned by DB-012/EK-012-style prerequisite rows. |
| ER-002 | Unknown and removed sender quarantine has no ghost UI rows | X is unknown or removed. | 1. Send valid-looking messages, media, reactions, and receipts. 2. Observe quarantine, notifications, and DB. | Unknown or removed sender traffic is rejected or quarantined without visible ghost messages or notifications. | P0 | Covered | Required | Required | Recommended | Required | Recommended | Covered by ER-002 execution on 2026-05-01. `handleIncomingGroupMessage` now rejects unknown senders with no persisted removal cutoff before event-log append or message persistence, while preserving intentional pre-removal cutoff tolerance and rejecting at-cutoff/later removed-sender replay. `handleIncomingGroupReaction` now returns `unknownSender` before reaction storage when the sender is not a current member. Focused listener proof shows an unknown-sender group message creates no DB row, emits no group-message stream item, and shows no local notification; fake-network membership smoke still proves removed members receive no notifications while removed and only resume after rejoin. Passed commands are recorded in `trusted_private_libp2p_group_chat_missing_test_matrix_full_with_rules-session-ER-002-plan.md`. First-class receipt traffic is not modeled and remains outside ER-002 until it exists. |
| ER-004 | Decryption failure triggers key repair and safe placeholder UI | B receives undecryptable content. | 1. Deliver wrong-key, missing-key, future-epoch, and tampered payloads. 2. Trigger repair. | The app does not crash, does not create plaintext rows, attempts key sync where possible, and shows a safe placeholder when unrecoverable. | P0 | Covered | Required | Required | Recommended | Required | Recommended | Covered by `PREREQ-FUTURE-EPOCH-KEY-REPAIR` execution on 2026-05-01. Go diagnostics remain preserved for live wrong-key, tampered nonce, and tampered ciphertext without normal `group_message:received`, and Flutter bridge diagnostics still do not invoke the normal callback. `GroupMessageListener` now listens to `group:decryption_failed`, creates a truthful pending repair placeholder, queues a durable repair item, and triggers scoped key repair without plaintext delivery. Offline future/missing-key replay uses the same pending/finalized placeholder states, retries after key arrival, replaces placeholders on valid repair, and finalizes invalid or no-envelope repairs idempotently to safe `undecryptable` text. Evidence passed: focused Go decryption/future-epoch diagnostics, Flutter bridge diagnostic preservation, focused live placeholder test, focused offline queue/repair test, focused key-arrival/rejected-key tests, focused UI pending/finalized test, migration/helper/repository/full-chain tests, direct owner suites, `./scripts/run_test_gates.sh groups` (`+101`), `./scripts/run_test_gates.sh completeness-check` (`703/703`), and `git diff --check`. |
| ER-005 | Safe error messages never expose secrets or sensitive peer data | Failures occur across invite, sync, media, key, discovery, and bridge paths. | 1. Force each error. 2. Capture snackbar, logs, diagnostics export, and flow events. | User-visible errors are actionable and never expose keys, raw encrypted blobs, internal dumps, or sensitive multiaddrs. | P0 | Covered | Required | Required | Recommended | N/A | N/A | Covered by ER-005 execution on 2026-05-01. `flow_event_emitter.dart` now sanitizes flow-event details before test sinks and debug `[FLOW]` logs, redacting sensitive keys, raw encrypted blobs, long Peer IDs, and multiaddr strings recursively. `emitGroupDiagnosticEvent` now sanitizes diagnostic payloads before Flutter listeners receive them. `GoBridgeClient.send` now sanitizes native `ok:false` error messages plus `PlatformException` and unexpected exception messages before returning them or emitting flow events, without changing command payloads sent to the native bridge. Focused flow-event, ER005 bridge, PlatformException, full bridge-client, and `git diff --check` evidence is recorded in `trusted_private_libp2p_group_chat_missing_test_matrix_full_with_rules-session-ER-005-plan.md`. This row covers the shared bridge/diagnostics emission boundary; app-specific snackbar copy can stay row-owned if a future row finds a non-bridge surface. |

## Abuse Prevention, Moderation, Limits, and Safety

| Test ID | Scenario | Preconditions | Steps | Expected Result | Priority | Current status | Unit | Integration | Smoke | Fake Network | 3-Party E2E | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| AB-006 | Suspicious or oversized media does not auto-download | X sends suspicious media descriptors. | 1. Deliver large, mismatched, dangerous, and untrusted media. 2. Inspect auto-download behavior. | Suspicious media remains blocked or manual-only until validation passes. | P0 | Covered | Required | Required | Recommended | Recommended | Recommended | Covered by AB-006 evidence audit on 2026-05-01. Existing media policy tests prove dangerous/unsupported MIME, MIME/type mismatch, oversized single attachments, oversized aggregate descriptors, malformed remote sizes, hash/display prerequisites, and tampered content-hash checks. Send-side tests prove dangerous MIME, oversized media, and mediaType mismatch reject before message persistence, media persistence, group publish, or inbox store. Listener tests prove invalid, oversized, and hashless live media reject before message persistence, notification preview, or `media:download`. Offline replay proof rejects dangerous encrypted media before message or attachment storage. Fake-network proof shows oversized recipient media is not stored or downloaded, while tampered downloads fail integrity and are deleted before done/display state. Passed commands are recorded in `trusted_private_libp2p_group_chat_missing_test_matrix_full_with_rules-session-AB-006-plan.md`. |

## UI System State, User-Facing Truth, and Accessibility

| Test ID | Scenario | Preconditions | Steps | Expected Result | Priority | Current status | Unit | Integration | Smoke | Fake Network | 3-Party E2E | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| UI-003 | Key change, encryption status, and verification warnings are visible | A key rotates or identity changes. | 1. Rotate key. 2. Change device identity. 3. Open group security surfaces. | Users see clear encrypted-state, verified-member, identity-change, and key-change warnings. | P0 | Covered | Required | Required | Recommended | N/A | Recommended | Covered by UI-003 execution on 2026-05-01. `group_security_status_view_state.dart` derives a safe presentation status from the latest group key, member count, saved-contact verification matches, unverified members, and identity-change warnings without exposing key material. `GroupInfoScreen` now renders a group security status card with encrypted-state, current/key-changed epoch text, verified-member count, and verification warning copy; `GroupConversationScreen` now renders a compact security strip with encrypted/key-epoch and review state. `GroupInfoWired` and `GroupConversationWired` load the latest group key and existing `GroupMemberIdentitySafety` comparisons from repositories/contacts to feed those surfaces. Focused and full presentation gates passed: pure info screen (`+18`), pure conversation screen (`+35`), full info wired (`+28`), full conversation wired (`+74`), plus focused UI-003 reruns and `git diff --check`. Existing member rows continue to show current/saved safety numbers for changed identities. |
| UI-005 | Undecryptable placeholders are safe and policy-compliant | Messages cannot decrypt or are unavailable because the required key/material is missing. | 1. Deliver undecryptable content. 2. Open all surfaces. | Placeholders do not reveal content, explain repair where possible, and do not break layout. | P0 | Covered | Required | Required | Recommended | N/A | Recommended | Covered by UI-005 evidence audit on 2026-05-01. `drain_group_offline_inbox_use_case.dart` stores missing-future-key replay as one generic `groupUndecryptablePlaceholderText` message with `status: 'undecryptable'`, preserved key epoch, and no `group.decrypt` call or original plaintext exposure. `group_conversation_screen_test.dart` proves the conversation renders the safe placeholder text, does not render the original future-epoch plaintext, and does not show failed-media retry/delete controls for undecryptable rows. Focused application and presentation commands passed, plus `git diff --check`. Live key-repair lifecycle remains ER-004 scope and is not claimed by UI-005. |

## Security, Privacy, Metadata Controls, Backup, and Secret Controls

| Test ID | Scenario | Preconditions | Steps | Expected Result | Priority | Current status | Unit | Integration | Smoke | Fake Network | 3-Party E2E | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| SP-001 | Peer authentication and request authorization cover every protocol | X connects with valid transport but no group authorization. | 1. Attempt invite, sync, media, receipt, key, and diagnostics requests. | Every request authenticates the peer and authorizes against current group state before processing. | P0 | Covered | Required | Required | Required | Required | Recommended | Covered 2026-05-01 by SP-001 session. `go-relay-server/inbox.go` now binds `group_store` `from` to the authenticated `RemotePeer`, rejects empty `recipientPeerIds`, persists per-message recipient ACLs through `backend_memory.go` and `backend_redis.go`, and filters `group_retrieve` / `group_retrieve_cursor` to the sender or stored recipients only. `go-relay-server/inbox_test.go` proves spoofed sender rejection, unauthorized retrieve suppression, and cursor filtering; `go-mknoon/node/protocol_version_test.go`, `pubsub_test.go`, and `pubsub_authorization_forward_test.go` cover secure protocol negotiation plus PubSub sender/member/signature/system-event authorization; `go-relay-server/media_test.go` covers unauthorized media ACLs. Relay group inbox authorization enforces authenticated transport peer plus stored fanout ACL; live authoritative group-state control-plane semantics remain separate scope. |
| SP-002 | Metadata minimization covers topics, discovery, relays, push, and diagnostics | Sensitive group membership and names exist. | 1. Inspect topic names, discovery records, relay addresses, push payloads, logs, and diagnostics. | The app exposes only unavoidable metadata and documents or warns about relay-visible participation metadata. | P0 | Covered | Required | Required | Recommended | Required | Recommended | Covered 2026-05-01 by SP-002 session. `go-mknoon/node/pubsub_test.go` proves group topic/rendezvous identifiers and join logs omit human-readable group metadata, and relay-visible PubSub envelopes remain encrypted. `go-mknoon/node/group_inbox.go` no longer serializes retired `pushTitle` / `pushBody`; `group_inbox_test.go` proves supplied preview fields are omitted from group inbox requests and opaque replay envelopes. `go-mknoon/bridge/bridge.go` ignores legacy preview fields on `group:inboxStore`, and `retry_failed_group_inbox_stores_use_case_test.dart` proves stale persisted retry payloads replay without those fields. Relay/push tests prove encrypted group pushes are generic/data-only and forbidden preview canaries stay out; push fallback/preview and bridge diagnostics tests prove protected message push data and diagnostics avoid plaintext, raw peer IDs, secret fields, and sensitive multiaddrs. Relay-visible group IDs, recipient peer IDs, push tokens, relay addresses, and encrypted replay blobs remain unavoidable metadata and are explicitly not hidden from relay infrastructure. |
| SP-003 | Secure random generation is used for IDs, keys, nonces, tokens, and salts | Random artifacts are generated repeatedly. | 1. Generate many group IDs, keys, nonces, invite tokens, and salts. 2. Check uniqueness and source constraints. | Artifacts are generated from cryptographically secure randomness and collision rates stay within policy. | P0 | Covered | Required | Recommended | N/A | N/A | N/A | Covered 2026-05-01 by SP-003 session. `go-mknoon/crypto/group_test.go` now generates repeated group AES keys and repeated AES-GCM encryptions with stable plaintext, proving 32-byte keys, 12-byte nonces, unique keys, unique nonces, and unique ciphertexts. `go-mknoon/crypto/x25519_test.go` proves repeated contact-request encryption produces unique 32-byte X25519 ephemeral public keys used as HKDF salts, unique 12-byte nonces, and unique ciphertexts. `go-mknoon/identity/identity_test.go` proves repeated BIP39 identity generation produces unique 12-word mnemonics and Peer IDs. `go-mknoon/bridge/bridge_test.go` proves bridge-created group IDs and native publish message IDs are UUID v4 values, group keys decode to 32 bytes, and repeated group creation does not collide; the publish fixture now uses one keypair for the started node and sender material under strict transport-peer binding. Flutter group send and invite tests prove default message IDs and direct invite IDs are unique UUID v4 values. Locked Dart `uuid` v4 defaults use `CryptoRNG` / `Random.secure`, and Go `github.com/google/uuid` v4 uses `crypto/rand`. No separate public/link invite-token generator is shipped; direct invites use UUID v4 invite IDs, push tokens are externally supplied, and contact safety numbers are deterministic rather than random salts. |

## Edge Cases, Invalid Actions, and Fork Handling

| Test ID | Scenario | Preconditions | Steps | Expected Result | Priority | Current status | Unit | Integration | Smoke | Fake Network | 3-Party E2E | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| EC-001 | Invite expiry, revocation, wrong identity, and already-used errors are distinct | C attempts invalid invite accepts. | 1. Accept expired, revoked, wrong-identity, malformed, and already-used invites. 2. Inspect UI and local state. | Each failure is classified safely and no group/key state is created. | P0 | Covered | Required | Required | Recommended | Recommended | Recommended | Covered 2026-05-01 by EC-001 session. `accept_pending_group_invite_use_case.dart` now returns distinct accepted-failure classifications for expired, revoked, already-used, wrong-identity, malformed/tampered, duplicate, repair-pending, and bridge-error paths; copied pending invites whose signed `recipientPeerId` / `allowedDevices` do not match the current local Peer ID now return `wrongIdentity` instead of the generic malformed `invalidPayload`. `group_list_wired.dart` and `orbit_wired.dart` show a distinct wrong-identity snackbar. `accept_pending_group_invite_use_case_test.dart` adds `EC001 invalid invite accepts classify failures without group or key state`, proving expired, revoked, wrong-identity, malformed signed-payload, and already-used accepts delete pending state as appropriate and create no group, key, join, or message side effects. Supporting `store_pending_group_invite_use_case_test.dart` evidence proves delayed revoked/already-used/expired/local-identity-mismatched copies do not create pending or group state. |
| EC-003 | Future-dated or future-epoch events queue, expire, or reject safely | X or delayed peers deliver future events. | 1. Send future timestamp, future epoch, and missing-dependency events. 2. Later deliver dependencies or let them expire. | Future state does not corrupt current state and resolves only when dependencies become valid. | P0 | Covered | Required | Required | Recommended | Required | Recommended | Covered 2026-05-01 by EC-003 session for the shipped reject/placeholder/enrichment contract. `handle_incoming_group_message_use_case_test.dart` proves far-future incoming timestamps are clamped to receive time and past/current/near-future timestamps retain chronological order. `group_messaging_smoke_test.dart` proves fake-network live skewed timestamps keep sane ordering/latest-message state after valid membership hydration; the supporting fixture now broadcasts Charlie's membership before Charlie publishes under strict sender validation. `go-mknoon/node/pubsub_key_rotation_grace_test.go` proves unknown live future epochs reject before delivery. `drain_group_offline_inbox_use_case_test.dart` proves offline future-epoch encrypted replay creates exactly one generic undecryptable placeholder without decrypting or exposing future plaintext, and later valid inbox replay can enrich a sparse prior live row with quote/media data without duplicate state. Durable future-key queue/key-sync repair and live repair lifecycle remain EK-005/ER-004 scope. |
| EC-004 | Old but valid events apply only if they do not conflict with finalized newer state | Delayed old events arrive after newer state is finalized. | 1. Apply newer remove, role, key, and tombstone events. 2. Deliver older valid messages and membership updates. | Old events are accepted only within policy cutoffs and cannot roll back finalized newer state. | P0 | Covered | Required | Required | Recommended | Required | Recommended | Covered 2026-05-01 by EC-004 evidence audit. `group_message_listener_test.dart` proves older metadata, member-added, member-removed, and member-role events cannot roll back newer metadata, membership, or role state across restart, and old system events after `group_dissolved` or local deletion cannot mutate metadata, members, keys, or visible messages. `handle_incoming_group_message_use_case_test.dart` and `handle_incoming_group_reaction_use_case_test.dart` prove pre-dissolve messages/reactions remain valid while at/after-cutoff replay is ignored and cannot overwrite trusted rows. Fake-network membership and resume-recovery smoke prove removed-sender replay is accepted only before the persisted removal cutoff, conflicting promote/remove converges to removal, and offline metadata replay converges to the newer final state. Real-device packet-capture proof is supporting only and was not required for this deterministic host/fake-network conflict policy. |
| EC-006 | Replayed tombstones cannot corrupt current state | Old delete, leave, remove, ban, unban, and dissolve tombstones are replayed. | 1. Replay tombstones before and after rejoin/reinvite. 2. Inspect state. | Tombstone replay is idempotent and cannot delete current valid membership or messages outside its scope. | P0 | Covered | Required | Required | Recommended | Recommended | Recommended | Covered by `PREREQ-REMOTE-EVENT-FAMILIES` final QA on 2026-05-01. `trusted_private_group_system_event.dart` and `GroupMessageListener` now model the missing tombstone families: `member_banned`, `member_unbanned`, and `group_message_deleted`. Duplicate replay creates one deterministic tombstone/timeline row; stale ban replay after a later unban/rejoin does not remove current valid membership; unban is a tombstone/freshness event and does not recreate membership; remote delete removes only the exact same-group target message and ignores wrong-group, missing, stale, newer-message, or unauthorized deletes. Offline replay uses the same listener path through `drainGroupOfflineInbox`, preserving transaction/cursor behavior. Prior evidence still covers removal, voluntary leave, dissolve, local-delete, and re-invite tombstone paths. Evidence passed: PREREQ listener tests (`+3`), offline replay test (`+1`), Go invalid-signature regex, `groups`, `completeness-check`, and `git diff --check`; targeted analyzer caveat remains warning-only pre-existing listener debt. |
| EC-007 | Role change after demotion and invite after removal are rejected across offline replay | B is demoted or removed while queued actions exist. | 1. B queues role changes and invites. 2. A demotes/removes B. 3. B reconnects and publishes. | Queued stale actions fail locally and remotely and do not create invites, role changes, or audit confusion. | P0 | Covered | Required | Required | Recommended | Required | Recommended | Covered by `PREREQ-INVITER-FRESHNESS` final QA on 2026-05-02. Existing evidence still proves queued role updates, queued add/invite-member actions, receive-side stale mutations, and invalid signed-snapshot invites reject before side effects. The remaining stale self-consistent invite gap is closed by first-class `GroupInviteMembershipFreshnessProof` in `group_invite_payload.dart` and `group_invite_auth.dart`: invite payloads bind current group config state, inviter membership/permission snapshot, inviter/recipient/device/key-package identity, invite id, group id, key epoch, issue time, and proof expiry into the canonical signed invite payload. `send_group_invite_use_case.dart` reloads current `GroupRepository` membership/config state before proof/sign/encrypt/delivery, so stale caller configs cannot mint deliverable invites. `handle_incoming_group_invite_use_case.dart`, `group_invite_listener.dart`, and `accept_pending_group_invite_use_case.dart` reject missing, malformed, tampered, mismatched, or stale proofs before pending/group/key/join/mailbox/notification/consumption side effects; accept-time stale proofs delete the pending invite without creating consumed or welcome-package tombstones. The bounded fix pass made listener pending-store validation use local receive time instead of replayed `ChatMessage.timestamp`; listener and fake-network replay tests keep the original queued timestamp while advancing receipt time past `groupInviteMembershipFreshnessTtl`. Evidence passed: focused PREREQ selectors for payload, send, create fanout, contact picker fanout, direct handle, listener, accept, and invite round trip; invite wildcard; full invite round trip; valid fresh and remove-rotate-reinvite preservation selectors; targeted analyzer; `groups`; `completeness-check`; and `git diff --check`. |

## Remaining Partial Row Prerequisite Owner Reconciliation (Closed 2026-05-02)

No source rows remain `Partial`. The former EK-004 prerequisite blocker is closed by the 2026-05-02 signed offline/pending/history replay signature-equivalence proof.

| Source row | Current status | Prerequisite owner session(s) | Missing proof owner | Fixture blocker |
|---|---|---|---|---|
| None | Covered | None | All retained source rows are now `Covered`; EK-004 closed after signed replay-envelope generation/verification and all-family offline/history replay rejection proof passed focused Flutter, Go, named-gate, analyzer, and diff hygiene checks. | None. |

## Recommended trusted-private smoke suite for gap closure

These smoke specs are intentionally scenario-level. Each can assert multiple matrix rows while keeping runtime bounded.

| Smoke ID | Purpose | Covers |
|---|---|---|
| TP-SMOKE-01 | Create a trusted-private group, add trusted contacts, reject non-trusted identities, and keep membership deterministic across restart. | `GL-005`, `IJ-002`, `IJ-003`, `IJ-009`, `IJ-013` |
| TP-SMOKE-02 | Send while one member is offline, restart multiple clients, replay history exactly once, and preserve pinned chat metadata. | `LP-002`, `LP-003`, `RP-017`, `EC-006` |
| TP-SMOKE-03 | Exercise group encryption, sender attribution, corrupted payload rejection, unknown-key handling, and multi-device sender continuity. | `EK-003`, `EK-004`, `EK-005`, `EK-012`, `SP-001` |
| TP-SMOKE-04 | Force relay churn and sync gaps, then verify group history and unread state recover without duplicates. | `OS-006` |
| TP-SMOKE-05 | Verify suspicious or oversized group media stays blocked before persistence and delivery side effects. | `AB-006` |
| TP-SMOKE-06 | Render mixed group history, unread separators, and message actions after restart and replay. | `UI-003`, `UI-005` |
| TP-SMOKE-07 | Preserve lifecycle, profile, and device-level state across leave, rejoin, rename, restart, and relay recovery. | `LP-013`, `SP-002` |

## Removed non-fitting rows from earlier broad matrix ideas

The following test ideas were intentionally not included because they require semantics outside trusted-private direct membership:

- Public discoverable groups.
- Open joins by room code or invite link.
- Server-authoritative admin/moderator role management.
- Kicking/banning arbitrary members in a public room.
- Bot accounts and webhooks.
- Federated group search.
- Full generic relay/load testing with no group-chat assertion.
- Cosmetic theme tests with no group-chat correctness risk.
- Payment, entitlement, or subscription behavior.

## Suggested implementation order

1. Membership and trusted-contact gating: `GL-005`, `IJ-002`, `IJ-003`, `IJ-005`, `IJ-009`, `IJ-013`, `RP-004`, `RP-005`, `RP-006`, `SP-001`.
2. Persistence and replay: `LP-003`, `RP-014`, `RP-017`, `EK-003`, `EK-004`, `EK-011`, `EK-012`, `EC-006`.
3. Offline, restart, and relay recovery: `OS-006`, `RP-018`, `DB-012`.
4. Media and metadata: `MS-002`, `MS-003`, `MS-004`, `MS-018`.
5. UI, DB resilience, and error surfaces: `UI-003`, `UI-005`, `DB-002`, `DB-004`, `DB-006`, `ER-005`.
