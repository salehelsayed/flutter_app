# Private Group Chat Reliability Test Matrix with Coverage Rules

## Scope

This matrix is a release-gate test plan for the private group chat feature in this repo. It is intentionally focused on the failure class you described: members being added, removed, and re-added, after which some current members stop seeing incoming messages. The matrix covers the Go transport/crypto/inbox/relay code and the Flutter app-layer integration tests around it.

The matrix cannot prove every possible internet failure away, but it is designed so that passing the P0 rows gives strong evidence that current members reliably receive entitled messages, removed members lose access, re-added members regain access with the right epoch, live PubSub loss is repaired by inbox replay, and all membership/key/device races converge deterministically.

## Actors

- **A** = group creator/admin or current sender.
- **B** = stable current member who must continue receiving messages.
- **C** = member being removed, offline, re-added, or using a stale key/config.
- **D** = late invitee/new member.
- **S1/S2** = sibling devices for the same logical user.
- **X** = unauthorized peer, removed member after cutoff, revoked device, malicious relay/source, or malformed sender.

## Status legend

- **Covered** = a direct automated test already exists in your full repo and proves the user-visible contract.
- **Partial** = adjacent low-level coverage exists, but the full transport + membership + inbox + app outcome is not proven.
- **Open** = add this test or explicitly map it to an equivalent existing test before treating private group chat as reliable.
- **Unsupported** = capability is intentionally not supported and should be blocked or documented.

## Priority guide

- **P0** = release-blocking reliability, privacy, or authorization contract.
- **P1** = important hardening before broad rollout or before claiming reliability.
- **P2** = useful or product-dependent coverage.

## Minimum private group chat invariants

- A current member must eventually receive every message they are entitled to, either live through PubSub or later through inbox replay.
- A removed member must not send accepted post-removal messages and must not decrypt post-removal replay.
- A re-added member must regain access only from the correct rejoin membership/key epoch window.
- Add/remove/re-add must update membership, key epoch, recipient list, PubSub validator, discovery targets, topic rejoin state, and replay cutoffs together.
- Live PubSub delivery and durable inbox replay must dedupe into one visible message, not two.
- Validator behavior for live messages and replay behavior for stored messages must enforce the same sender, device, role, membership, and key rules.
- Multi-device sender identity must be bound to the active device transport peer, not only to a logical member ID.
- Stale local config/key state must produce a repairable diagnostic, not silent message loss.
- Relay refresh or watchdog restart must either preserve joined topics or force explicit app rejoin before clearing recovery state.
- Test artifacts must make every missing-message failure class attributable to one of: config mismatch, key epoch mismatch, device binding, topic peer absence, inbox failure, replay dedupe/cutoff, or relay recovery.

## Code-review risk anchors that shaped this matrix

- `hasKeyRotationGrace` requires `PrevKeyEpoch > 0` (`pubsub.go:414-419`). If initial groups use epoch `0`, the first rotation from epoch `0` to `1` may reject in-flight epoch-0 messages during grace. This is a high-priority suspected reliability bug.
- `UpdateGroupConfig` stores any pointer including `nil` (`pubsub.go:356-360`), while `findMember` dereferences `config.Members`. Nil or malformed config tests are required to prevent panics/black holes.
- `JoinGroupTopic` registers a validator before join/subscribe (`pubsub.go:70-84`). Failure paths need tests proving validators are not leaked and later rejoin works.
- Device-aware validation exists (`activeMemberDeviceForEnvelope`), but known-member dialing and expected-peer counting use `member.PeerId` (`pubsub.go:1202-1242`, `1437-1545`, `1675-1722`). Multi-device or re-added-device cases need direct tests.
- The subscription handler skips messages where `env.SenderId == selfPeerId` (`pubsub.go:702-718`). This must be tested with sibling devices and any account-level sender ID model.
- Signature data covers only `groupId|epoch|ciphertext` (`group.go:107-111`). Tamper tests must define whether type, nonce, sender, and device-field mutations are rejected by validator/decryption or are harmless.
- `GroupInboxStore` trusts caller-provided `recipientPeerIds` and ignores push title/body (`group_inbox.go:136-222`). App-layer tests must enforce current-membership recipients and relay privacy.
- `GroupHistoryRepairRange` passes integrity values through to the app layer (`group_inbox.go:277-323`). App tests must reject range/head mismatches.
- `Stop` clears group runtime state (`node.go:430-463`). Relay watchdog/full restart must be paired with explicit app rejoin coverage.

## Coverage policy used in this matrix

### Coverage legend

- **Required** = should exist before this feature is considered production-ready.
- **Recommended** = high-value additional proof.
- **N/A** = not necessary for that row.

### Rules

**Unit**: use for crypto, signature, envelope parsing, key epoch/grace, authorization helpers, device binding, discovery filters, retry/backoff, request-shape, and relay session state.

**Integration**: use for Node-level publish/receive, join/leave/update, inbox store/retrieve/repair, relay recovery, topic rejoin, app persistence, membership ordering, and replay cutoff behavior.

**Smoke**: keep small but release-blocking: create/join, remove, re-add, send after re-add, offline replay, zero-peer fallback, and relay restart rejoin.

**Fake Network**: use deterministic fake clocks, fake PubSub topic peers, fake relay/inbox/rendezvous, partitions, delayed delivery, duplicated delivery, stale envelopes, and relay restart.

**3-Party E2E**: use A/B/C simulators for every path where the user-visible failure is `some member stopped seeing messages`.

## Matrix

## Group Topic Lifecycle and Local Truth

| Test ID | Scenario | Preconditions | Steps | Expected Result | Priority | Current status | Unit | Integration | Smoke | Fake Network | 3-Party E2E | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| GL-001 | Join rejects when PubSub has not been initialized | Node exists but Start/initPubSub was not called. | 1. Call JoinGroupTopic. 2. Inspect maps and goroutines. | Returns a clear error and does not create topic, subscription, config, key, validator, or discovery loop. | P0 | Covered | Required | Required | N/A | N/A | N/A | Covered by `go-mknoon/node/pubsub_test.go::TestJoinGroupTopic_FailsWithoutPubSub`: exact `pubsub not initialized`, nil PubSub so validator registration is unreachable, and no attempted-group entries in `groupTopics`, `groupSubs`, `groupConfigs`, `groupKeys`, `groupSubCtx`, or `groupDiscoveryCtx`; GL-001 plan records passing Go and Flutter host commands. |
| GL-002 | Join is idempotence-safe for an already joined group | Node has joined group G once. | 1. Call JoinGroupTopic(G) again. 2. Publish and receive after the failed second call. | Second call fails without replacing the existing topic, subscription, config, key, validator, or active handlers; first join remains usable. | P0 | Covered | Required | Required | Recommended | N/A | N/A | Covered by `go-mknoon/node/pubsub_test.go::TestJoinGroupTopic_DuplicateJoinPreservesExistingState` and `go-mknoon/node/pubsub_delivery_test.go::TestJoinGroupTopic_DuplicateJoinPreservesDelivery`: duplicate join returns `already joined`, first topic/subscription/config/key and handler/discovery entries remain present, the duplicate config/key are not stored, and post-failure publish/receive succeeds with the original key epoch. Direct validator registry identity is not introspected; the accepted proof is unchanged validator backing state plus delivery after duplicate rejection. GL-002 plan records passing targeted Go, row Go gate, startup rejoin, and lifecycle recovery commands. |
| GL-003 | Join failure after validator registration unregisters validator | RegisterTopicValidator succeeds but PubSub Join fails. | 1. Inject join failure. 2. Retry JoinGroupTopic. 3. Publish a valid message. | The failed attempt leaves no stale validator; retry succeeds with exactly one validator for the topic. | P0 | Covered | Required | Required | N/A | N/A | N/A | Covered by `go-mknoon/node/pubsub.go::JoinGroupTopic`, which now unregisters the current call's validator when `pubsub.Join(topicName)` fails after validator registration, and `go-mknoon/node/pubsub_test.go::TestJoinGroupTopic_JoinFailureUnregistersValidatorAndAllowsRetry`, which was RED before the fix with duplicate-validator retry failure and GREEN after the fix while proving no stored group state, retry success, and publish success. GL-003 plan records the passing single Go regression, row Go command, and Flutter startup rejoin smoke command. |
| GL-004 | Subscribe failure after topic join unregisters validator and closes topic | Topic Join succeeds but Subscribe fails. | 1. Inject subscribe failure. 2. Verify topic closed and validator unregistered. 3. Retry join. | Retry succeeds; no leaked topic, validator, subscription goroutine, discovery loop, config, or key remains from failed attempt. | P0 | Covered | Required | Required | N/A | N/A | N/A | Covered by `go-mknoon/node/pubsub.go::JoinGroupTopic`, which now closes the joined topic and unregisters the topic validator when subscribe fails after join, plus `go-mknoon/node/node.go`'s private nil-default `joinGroupTopicSubscribeHook` and `go-mknoon/node/pubsub_test.go::TestJoinGroupTopic_SubscribeFailureUnregistersValidatorClosesTopicAndAllowsRetry`, which was RED with duplicate-validator retry failure and GREEN after the fix while proving no stored topic/subscription/config/key/subscription-context/discovery-context state, retry success, and publish-after-retry. GL-004 plan records the passing targeted Go, row Go, startup rejoin, groups gate, and `git diff --check` commands. |
| GL-005 | Successful join stores config and joined key snapshot atomically | Valid config/key are supplied. | 1. Join group G. 2. Immediately call GetGroupKeyInfo and publish. 3. Inspect maps through test seam. | Config, topic, subscription, key, subscription cancel, and discovery cancel all exist or none exist; no partially joined state. | P0 | Covered | Required | Required | Recommended | N/A | N/A | Covered by `go-mknoon/node/pubsub_test.go::TestJoinGroupTopic_SuccessStoresAtomicStateAndSupportsImmediateKeyInfoAndPublish`: successful join stores topic, subscription, config, joined key snapshot, subscription cancel, and discovery cancel; immediate `GetGroupKeyInfo` returns the joined key/epoch clone; immediate `PublishGroupMessage` returns a non-empty message id. GL-005 plan records passing direct Go regression (`ok github.com/mknoon/go-mknoon/node 0.555s`), row Go gate (`ok 4.299s`), Flutter startup rejoin smoke (`+3 All tests passed`), and `git diff --check`; no production code changed. |
| GL-006 | Join with nil key fails or creates explicit non-sendable state | Valid config but keyInfo is nil. | 1. Join with nil keyInfo. 2. Attempt PublishGroupMessage. 3. Attempt inbound validation/decryption. | Behavior is explicit: either join is blocked, or sends fail with missing-key and inbound messages emit diagnostics; no panic or silent black hole. | P0 | Covered | Required | Required | N/A | N/A | N/A | Covered by `go-mknoon/node/pubsub.go::JoinGroupTopic`, which now rejects nil `keyInfo` after nil-PubSub and duplicate-join checks and before topic, validator, subscription, or map writes with `missing group key info for group`; `go-mknoon/node/pubsub_test.go::TestJoinGroupTopic_RejectsNilKeyInfoAndLeavesNoGroupState` proved RED when nil-key join succeeded, then GREEN with no group state, nil `GetGroupKeyInfo`, and publish failing as `group not joined`; `TestGroupTopicValidator_NilKeyRejectsNoKeyAndDecryptReportsMissingKey` pins inbound `reject:no_key` and decrypt `missing group key info`. GL-006 plan records passed direct Go tests, row Go gate (`ok 4.303s`), Flutter startup rejoin smoke (`+3 All tests passed`), and `git diff --check`; no full groups gate was required because scope stayed in Go `JoinGroupTopic` plus required smoke. |
| GL-007 | Join with nil config fails or creates explicit non-sendable state | Valid key but config is nil. | 1. Join with nil config. 2. Call PublishGroupMessage and validator. 3. Run with race/panic detector. | No panic; behavior is a clear group-config-missing error and no message is published or accepted. | P0 | Covered | Required | Required | N/A | N/A | N/A | Covered by `go-mknoon/node/pubsub.go::JoinGroupTopic`, which now rejects nil config after nil-PubSub, duplicate-join, and nil-key checks and before topic name creation, validator registration, join, subscribe, map writes, or goroutines with `missing group config for group`; `go-mknoon/node/pubsub_test.go::TestJoinGroupTopic_RejectsNilConfigAndLeavesNoGroupState` proved RED when nil-config join succeeded, then GREEN with no group state, nil `GetGroupKeyInfo`, and publish failing as `group not joined`; `TestGroupTopicValidator_NilConfigRejectsUnknownGroupForGL007` pins nil-config validator rejection as `reject:unknown_group`. Race row tests, row Go sweep, Flutter startup rejoin smoke, and `git diff --check` passed; full groups gate was not required because scope stayed in Go `JoinGroupTopic` plus required smoke. |
| GL-008 | Leave cancels subscription and discovery before deleting state | Group G is joined and discovery loop is active. | 1. Call LeaveGroupTopic(G). 2. Wait one discovery interval in fake time. 3. Publish/receive attempts. | No further group discovery/register/dial events occur; no inbound messages are emitted; publish fails as not joined. | P0 | Covered | Required | Required | Recommended | Recommended | N/A | Covered by `go-mknoon/node/pubsub_unsubscribe_exit_paths_test.go::TestGL008LeaveGroupTopicStopsDiscoveryAndInboundAfterLeave`: drives an active group discovery loop to `discover_result`, verifies pre-leave live delivery, calls `LeaveGroupTopic`, asserts topic/subscription/subscription-context/discovery-context/config/key state is removed, local message/reaction publishes fail as `group not joined`, and a bounded post-leave window emits no group discovery/register/dial work or inbound message/reaction/parse/decrypt events for the left group. Same file now uses a test-local collector helper that installs callbacks before `Start` for the GL-008 and LP003 race paths. GL-008 plan records passing direct Go proof, row Go sweep, race proof, Flutter startup rejoin smoke, and `git diff --check`; groups gate was not required because scope stayed tests-only with no production or Flutter/Dart changes. Accepted difference: existing hooks plus a bounded wait replaced fake-time discovery interval proof without adding production seams. |
| GL-009 | Leave unregisters the topic validator for G | Group G is joined. | 1. LeaveGroupTopic(G). 2. Rejoin G. 3. Publish valid and invalid envelopes. | Old validator is gone; new validator uses the latest config/key only. | P0 | Covered | Required | Required | N/A | N/A | N/A | Covered by `go-mknoon/node/pubsub_delivery_test.go::TestGL009LeaveGroupTopicUnregistersValidatorAndRejoinUsesLatestConfigKey`: same-group leave then rejoin succeeds, proving the old validator was unregistered; latest config/key raw envelope delivers; stale old config/key raw envelope rejects with `group:validation_rejected`/`bad_signature_or_epoch` and no stale marker delivery; latest-key/old-signer direct validator rejects. GL-009 plan records the initial direct-test failure as a test-shape issue (`topic publish: validation failed` from nodeA's local validator), followed by passing direct GL-009 command, row Go sweep, Flutter startup rejoin smoke, and `git diff --check`; groups gate was not required because scope stayed tests-only with no production or Flutter/Dart changes. |
| GL-010 | Leave unknown group is safe and no-op | Node is running but G is not joined. | 1. Call LeaveGroupTopic(G). 2. Inspect maps and validator calls. | Returns without panic and does not unregister unrelated topics or keys. | P1 | Covered | Required | Recommended | N/A | N/A | N/A | Covered on 2026-05-13 by `go-mknoon/node/pubsub_test.go::TestGL010LeaveUnknownGroupIsNoOpForJoinedGroupState`: a running node joins a known group, snapshots its topic, subscription, config, key, subscription context, and discovery context, calls `LeaveGroupTopic` for an unknown group, proves the known group state is unchanged and no unknown-group state is created, then publishes successfully to the known group. Passed evidence: gofmt, exact focused Go proof (`ok node 0.608s`), adjacent join/leave/update/recovery Go selector (`ok node 17.831s`), Flutter startup rejoin smoke (`+5`), named `./scripts/run_test_gates.sh groups` (`+159`), and diff hygiene. Residual-only none; GL-016 is covered separately; GR-002 is the next unresolved session in ordered ledger order. |
| GL-011 | UpdateGroupConfig replaces membership atomically during active subscription | G is joined; inbound messages are arriving. | 1. Start delivery loop. 2. Update config to add/remove C. 3. Validate messages before and after update. | Messages are authorized using either the old config before update or new config after update; no mixed/panic window. | P0 | Covered | Required | Required | N/A | Recommended | Recommended | Covered by `go-mknoon/node/pubsub.go::JoinGroupTopic` and `UpdateGroupConfig` storing owned `cloneGroupConfig` snapshots, with `cloneGroupConfig` deep-copying `Members` and nested `Devices`; `go-mknoon/node/pubsub_delivery_test.go::TestGL011UpdateGroupConfigReplacesMembershipDuringActiveSubscription` proves an active subscription accepts C before removal, rejects C as `non_member` after `UpdateGroupConfig` removes C, and accepts C again after re-add without rejoining or changing keys; `go-mknoon/node/pubsub_test.go::TestGL011UpdateGroupConfigSnapshotsMembershipForValidatorReads` proves caller-owned config mutations after update do not affect validator reads; `go-mknoon/node/pubsub_test.go::TestGL011UpdateGroupConfigConcurrentValidatorReadsAreRaceFree` passed under the race detector. GL-011 plan records passing focused Go, race, row Go sweep, Flutter startup rejoin smoke, groups gate, and `git diff --check` commands. Accepted scope: nil config remains GL-012, concurrent join/leave/update stress remains GL-019, and bridge spot check was not required because bridge files were untouched. |
| GL-012 | UpdateGroupConfig with nil is rejected or handled without panic | G is joined. | 1. Call UpdateGroupConfig(G, nil). 2. Publish, validate, discover. 3. Run race/panic detector. | No panic in isAllowedWriter, findMember, discovery, or counters; tests define expected error/disabled state. | P0 | Covered | Required | Required | N/A | N/A | N/A | Covered by `go-mknoon/node/pubsub.go::UpdateGroupConfig`, which now treats `UpdateGroupConfig(groupId, nil)` as a fail-closed config delete; publish and reaction return `group not joined: <groupId>`, the real validator rejects unknown/config-disabled groups, `findMember` plus direct discovery/counter paths are nil-safe, and a later valid config update repairs publish/validator behavior without rejoin. `go-mknoon/node/pubsub_test.go::TestGL012UpdateGroupConfigNilDisablesJoinedGroupWithoutPanic` proved the disabled-state behavior after pre-fix RED panic evidence, and `TestGL012UpdateGroupConfigNilConcurrentReadersAreRaceFree` passed under the race detector. GL-012 plan records passing focused Go, focused race, row Go sweep, Flutter startup rejoin smoke, groups gate (`00:05 +103` rerun), and `git diff --check`. Accepted scope: Go-only production change; GL-013 key nil/update semantics and GL-019 join/leave/update stress remain separate rows. |
| GL-013 | UpdateGroupKey(nil) removes the local key and disables decrypt/send honestly | G is joined with key epoch E. | 1. UpdateGroupKey(G, nil). 2. Publish and receive. 3. Inspect emitted events. | Publish fails as not joined/missing key; inbound emits missing-key/decryption diagnostics and no bogus message appears. | P0 | Covered | Required | Required | N/A | N/A | N/A | Covered by `go-mknoon/node/pubsub.go::UpdateGroupKey`, which deletes `groupKeys[groupId]` on nil so `GetGroupKeyInfo` returns nil; `PublishGroupMessage`, `PublishGroupReaction`, and `groupTopicValidator` treat absent or nil key info as fail-closed, with the real validator emitting `group:validation_rejected` reason `missing_key`; `handleGroupSubscription` now emits `group:decryption_failed` through `emitGroupDecryptionFailed` for the accepted-then-key-removed path with `groupId`, `senderId`, `keyEpoch`, `error` containing `missing group key info`, and `decryptMs`, while omitting `localKeyEpoch`. `go-mknoon/node/pubsub_key_rotation_grace_test.go::TestGL013UpdateGroupKeyNilRemovesKeyAndDisablesSendAndValidator` proves nil update removes local key material, message publish returns empty id/zero peers with an honest error, reaction publish fails honestly, and the real validator rejects with `missing_key`; `go-mknoon/node/pubsub_decryption_failure_test.go::TestGL013HandleGroupSubscriptionEmitsDecryptionFailedAfterKeyRemoval` proves missing-key subscription delivery emits decryption diagnostics and no bogus message/reaction receive events. GL-013 plan records passing focused Go, focused race, adjacent key/decrypt sweep, row Go sweep, Flutter startup rejoin smoke, `./scripts/run_test_gates.sh groups`, and `git diff --check`; QA reran the focused normal/race pair plus diff check. Accepted scope: Go-only behavior, no Dart/Flutter or bridge changes, normal missing-key inbound remains validator-rejected, and GL-014/GL-015 key epoch policy rows remain separate. |
| GL-014 | UpdateGroupKey ignores older epoch without losing current key | G has current epoch 3. | 1. UpdateGroupKey with epoch 2. 2. Publish/decrypt epoch 3. 3. Inspect GetGroupKeyInfo. | Epoch 3 remains current and no previous-key window is replaced by stale data. | P0 | Covered | Required | Required | N/A | N/A | N/A | Covered by `go-mknoon/node/pubsub_key_rotation_grace_test.go::TestGL014UpdateGroupKeyIgnoresOlderEpochAndKeepsCurrentEpochDelivery`: sender and receiver join with a real generated epoch-2 key, both advance to a real generated epoch-3 key, `GetGroupKeyInfo` captures current epoch/key, previous epoch/key, and grace deadline, a stale generated epoch-2 key update is applied to both nodes, current epoch-3 state plus previous epoch-2 grace state remain unchanged, epoch-3 `PublishGroupMessage` succeeds, the receiver emits `group_message:received` with expected group/sender/message/text/`keyEpoch` 3, no `group:decryption_failed` appears, and receiver key info remains unchanged after delivery. Existing `TestUpdateGroupKey_IgnoresOlderEpochAfterCurrent` remains adjacent simpler coverage. GL-014 plan records passing focused Go, focused race, adjacent key-rotation/grace sweep, row Go sweep, Flutter startup rejoin smoke, and `git diff --check`; no GL-014 production code changed, and `./scripts/run_test_gates.sh groups` was not required because scope stayed tests-only. GL-015 same-epoch mismatch policy remains separate. |
| GL-015 | UpdateGroupKey same epoch with different material is rejected explicitly | G has epoch 3 key K1; caller supplies epoch 3 key K2. | 1. UpdateGroupKey(G, K2/E3). 2. Publish/decrypt. 3. Inspect diagnostics. | Either same-epoch key mismatch is rejected with an alert or ignored deterministically; local state never becomes split-brain. | P0 | Covered | Required | Required | N/A | N/A | N/A | Covered by `go-mknoon/node/pubsub_key_rotation_grace_test.go::TestGL015UpdateGroupKeyIgnoresSameEpochDifferentMaterialAndKeepsEpoch3Delivery`: sender and receiver join with a real generated epoch-2 key, both advance to generated epoch-3 K1, snapshot `GetGroupKeyInfo`, then apply conflicting generated epoch-3 K2 updates to both nodes; current key/epoch, previous key/epoch, and grace deadline remain unchanged. Epoch-3 `PublishGroupMessage` succeeds and the receiver emits `group_message:received` with expected group/sender/message/text/`keyEpoch` 3, no `group:decryption_failed` appears, and receiver key info remains unchanged after delivery. Existing `TestUpdateGroupKey_IgnoresSameEpochDifferentMaterial` remains adjacent state-only coverage. GL-015 plan records passing focused Go, focused race, adjacent key/decrypt sweep, GL evidence-key Go sweep, Flutter startup rejoin smoke, and `git diff --check`; QA reran the focused normal/race pair plus diff check. Accepted difference: deterministic ignore satisfies the expected-result cell, so no explicit local no-op diagnostic was added. No GL-015 production code changed, and `./scripts/run_test_gates.sh groups` was not required. |
| GL-016 | GetGroupKeyInfo returns a clone and cannot mutate internal state | G is joined with key info. | 1. Call GetGroupKeyInfo. 2. Mutate returned struct. 3. Call again and publish/decrypt. | Internal key state is unchanged. | P1 | Covered | Required | Recommended | N/A | N/A | N/A | Covered on 2026-05-13 by `go-mknoon/node/pubsub_test.go::TestGL016GetGroupKeyInfoReturnsCloneCannotMutateInternalState`: a running node joins a group with a real generated key, rotates to a second real key, retrieves key info with current/previous epoch plus grace deadline, mutates every returned field to invalid values, retrieves key info again, proves the internal current key, previous key, epochs, and grace deadline remain unchanged and a fresh pointer is returned, then publishes successfully with the unmutated internal key. Passed evidence: gofmt, exact focused Go proof (`ok node 0.786s`), adjacent GetGroupKeyInfo/join/update/publish Go selector (`ok node 3.554s`), Flutter startup rejoin smoke (`+5`), named `./scripts/run_test_gates.sh groups` (`+159`), and diff hygiene. Residual-only none; GR-002 is the next unresolved session in ordered ledger order. |
| GL-017 | Stop clears all group topics and subscriptions | Node has several joined groups. | 1. Call Stop. 2. Inspect maps, subscriptions, discovery loops, event dispatcher. 3. Restart. | All group runtime state is cleared; after restart Flutter/app must explicitly rejoin groups before publish/receive. | P0 | Covered | Required | Required | Recommended | Recommended | N/A | Covered by `go-mknoon/node/pubsub_test.go::TestGL017StopClearsGroupRuntimeStateAndRequiresExplicitRejoinAfterRestart`: starts a node with event callback/dispatcher, joins three groups, verifies pre-Stop group topics, subscriptions, configs, keys, subscription contexts, and discovery contexts exist, calls `Stop()`, then verifies the node is stopped, `pubsub`, `groupConfigs`, `groupKeys`, and `eventDispatcher` are nil, group runtime maps are empty, and no joined group ids are retained. The same regression proves repeated `Stop()` is safe, restart with the same config preserves peer id and creates fresh empty pubsub maps, publish before explicit rejoin fails as `group not joined` with empty id/zero peers, explicit rejoin rebuilds runtime state for all groups, and publish after rejoin succeeds. GL-017 plan records passing focused Go, focused race, adjacent lifecycle/pubsub sweep, broader row Go sweep, Flutter startup rejoin smoke with exact `--plain-name`, and `git diff --check`. Accepted scope: tests-only; no GL-017 production code changed, and GL-018 remains responsible for persisted app rejoin restoring all groups. |
| GL-018 | Restart followed by app rejoin restores all groups from persisted app state | Node stopped/restarted after having groups A/B/C. | 1. Restart node. 2. Rejoin persisted groups with config/key. 3. Send messages in each group. | Every group rejoins exactly once with current config/key and can receive; no group is silently missing after restart. | P0 | Covered | Recommended | Required | Required | Recommended | Required | Covered by `test/features/groups/integration/group_startup_rejoin_smoke_test.dart::GL-018 restart rejoin restores all persisted groups exactly once and resumes delivery`: proves persisted groups A/B/C exist before restart; Bob is unsubscribed from all fake topics to model lost Go runtime state; startup `rejoinGroupTopics(reason: RejoinReason.startup)` emits exactly three `group:join` commands with each group id exactly once; every payload carries the current group key, key epoch, and current group config with expected group name/type and member public keys including Charlie in the three-member group; fake-network resubscribe models Go join subscription; Bob receives exactly one post-rejoin message in every group. GL-018 plan records passing direct startup-rejoin regression (`+1`), full startup-rejoin suite (`+4`), rejoin use-case suite (`+18`), adjacent messaging/resume/lifecycle plain-name checks (`+1` each), `./scripts/run_test_gates.sh groups` (`+104`), and `git diff --check`. Accepted scope: tests-only; no GL-018 production code changed, and Go checks were not run because no GL-018 Go production/test files changed and no direct failure pointed into Go runtime explicit rejoin support. |
| GL-019 | Concurrent join/leave/update for same group is race-free | G is being rapidly mutated by app recovery and user actions. | 1. Run join, leave, UpdateGroupConfig, UpdateGroupKey concurrently. 2. Run under race detector. 3. Assert final state. | No data race, panic, leaked goroutine, or impossible final map state occurs. | P0 | Covered | Required | Required | N/A | Recommended | N/A | Covered by `go-mknoon/node/pubsub_test.go::TestGL019ConcurrentJoinLeaveUpdateSameGroupIsRaceFree`: starts a real local node, initially joins one group, concurrently runs `JoinGroupTopic`, `LeaveGroupTopic`, `UpdateGroupConfig`, and `UpdateGroupKey` for the same `groupID`, allows only expected duplicate-join errors, checks the post-stress runtime state is not impossible, deterministically leaves and verifies no retained entries across `groupTopics`, `groupSubs`, `groupSubCtx`, `groupDiscoveryCtx`, `groupConfigs`, and `groupKeys`, then deterministically rejoins with the latest config/key and verifies full runtime state rebuild before final leave cleanup. GL-019 plan records passing focused Go (`ok ... 0.562s`), focused race (`ok ... 1.606s`), row Go sweep (`ok ... 17.764s`), and `git diff --check`; conditional production, delivery-test, and Flutter/app checks were not required because execution changed only `go-mknoon/node/pubsub_test.go` plus the GL-019 plan file. |
| GL-020 | Bulk recovery of many groups respects group recovery concurrency | User has many private groups. | 1. Trigger relay/group recovery. 2. Rejoin/discover many groups. 3. Measure events and sem slots. | Recovery is bounded, eventually attempts every group, and does not starve the affected private group. | P1 | Covered | Required | Required | N/A | Recommended | N/A | Covered on 2026-05-13 by `go-mknoon/node/pubsub_test.go::TestGL020GroupRecoveryLimiterDrainsManyGroupsWithoutStarvingAffectedGroup`: the test saturates `groupRecoverySem` with `GroupDiscoveryConcurrency` active recovery cycles, queues additional groups plus `gl020-affected-group`, proves no queued cycle starts before a slot is released, then releases the slots and proves every group registers/discovers exactly once while `maxActive == GroupDiscoveryConcurrency` and the affected group is not starved. Passed evidence: gofmt, exact focused Go proof (`ok node 0.760s`), adjacent join/leave/update/recovery/discovery Go selector (`ok node 19.205s`), Flutter startup rejoin smoke (`+5`), named `./scripts/run_test_gates.sh groups` (`+159`), and diff hygiene. Residual-only none; GR-002 is the next unresolved session in ordered ledger order. |

## Membership Mutation, Remove, Re-add, and Visibility

| Test ID | Scenario | Preconditions | Steps | Expected Result | Priority | Current status | Unit | Integration | Smoke | Fake Network | 3-Party E2E | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| GM-001 | Create private group with A, B, C and all join same topic/key epoch | A/B/C have valid peer IDs, signing keys, and one initial group key. | 1. A creates G. 2. B and C join topic with same config/key. 3. A sends. | B and C receive exactly one message with same groupId, messageId, sender, epoch, and plaintext. | P0 | Covered | Required | Required | Required | Recommended | Required | Covered by row-specific GM-001 closure evidence. Host proof passed: `flutter test --no-pub test/features/groups/integration/group_messaging_smoke_test.dart --plain-name 'GM-001 creates private A/B/C group with shared epoch and exact fanout tuple'`. Criteria guard passed: `flutter test test/integration/group_multi_party_device_criteria_test.dart`. Exact relay/device proof passed with `--scenario gm001` on Alice `38FECA55-03C1-4907-BD9D-8E64BF8E3469`, Bob `347FB118-10D0-40C8-A05B-B0C3BD6B8CCD`, and Charlie `5BA69F1C-B112-47BE-B1FF-8C1003728C8F`; verdict `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_gm001_kd81Da/gmp_1778405304614_gm001_orchestrator_verdict.json` records `scenario: gm001`, `ok: true`, and `gm001 verdicts valid for alice, bob, charlie`. Bob and Charlie per-role verdicts each persisted exactly one matching incoming Alice message with shared groupId/messageId/sender/keyEpoch/plaintext. Supporting gates passed: `./scripts/run_test_gates.sh groups` (`00:11 +106: All tests passed!`) and `git diff --check`. Prior prerequisite-shaped artifact remains prerequisite-only; this row-specific artifact closes GM-001. |
| GM-002 | Add D while A/B/C are online | G has A/B/C joined and connected. | 1. A updates config to include D. 2. D joins. 3. A and D send. | All current members converge on same member list and all can receive messages allowed by their membership window. | P0 | Covered | Required | Required | Required | Recommended | Required | Covered by row-specific GM-002 closure evidence. Host/config proof passed: `flutter test --no-pub test/features/groups/application/add_group_member_use_case_test.dart --plain-name 'GM-002 addGroupMember syncs updated A/B/C/D config payload'` and `flutter test --no-pub test/features/groups/integration/group_messaging_smoke_test.dart --plain-name 'GM-002 adds D while A/B/C are online and converges post-add delivery'`. Criteria guard passed: `flutter test test/integration/group_multi_party_device_criteria_test.dart`. Exact relay/device proof passed with `--scenario gm002` on Alice `38FECA55-03C1-4907-BD9D-8E64BF8E3469`, Bob `347FB118-10D0-40C8-A05B-B0C3BD6B8CCD`, Charlie `5BA69F1C-B112-47BE-B1FF-8C1003728C8F`, and Dana `1B098DFF-6294-407A-A209-BBF360893485`; verdict `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_gm002_LWsi7q/gmp_1778407359493_gm002_orchestrator_verdict.json` records `scenario: gm002`, `ok: true`, and `gm002 verdicts valid for alice, bob, charlie, dana`. QA verified all four roles converged on the same four-member list with common key epoch `1`; Bob/Charlie/Dana each received Alice's post-add message exactly once with matching tuple, and Alice/Bob/Charlie each received Dana's post-join message exactly once with matching tuple. Supporting gates passed: full `add_group_member_use_case_test.dart` (14), `group_messaging_smoke_test.dart` (15), `group_membership_smoke_test.dart` (25), `group_new_member_onboarding_test.dart` (7), `member_removal_integration_test.dart` (6), `./scripts/run_test_gates.sh groups` (106), and `git diff --check`. Prior prerequisite-shaped GM-002 artifact remains prerequisite-only; this row-specific artifact closes GM-002. GM-013 and later rows remain Open. |
| GM-003 | Add D while D is offline, then D opens app | D is invited but offline. | 1. A sends messages before and after invite. 2. D comes online, joins, retrieves inbox. 3. D sends. | D receives only allowed post-add messages and can send after key/config install. | P0 | Covered | Required | Required | Required | Required | Required | Covered by row-specific GM-003 closure evidence. Criteria guard passed: `flutter test --no-pub test/integration/group_multi_party_device_criteria_test.dart` (17 tests). Host proof passed: `flutter test --no-pub test/features/groups/integration/group_messaging_smoke_test.dart --plain-name 'GM-003 adds D while D is offline, catches up post-add only, and D can send'`; adjacent invite/onboarding proof passed via `group_new_member_onboarding_test.dart` (7 tests) and `accept_pending_group_invite_use_case_test.dart --plain-name 'accepts pending invite, persists group, and drains inbox'`. Fresh `flutter devices --machine` and `xcrun simctl list devices available` found Pixel 6 plus the four required booted iOS 26.1 simulators. Exact relay/device proof passed with `--scenario gm003` on Alice `38FECA55-03C1-4907-BD9D-8E64BF8E3469`, Bob `347FB118-10D0-40C8-A05B-B0C3BD6B8CCD`, Charlie `5BA69F1C-B112-47BE-B1FF-8C1003728C8F`, and Dana `1B098DFF-6294-407A-A209-BBF360893485`; verdict `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_gm003_HQxXEh/gmp_1778410554671_gm003_orchestrator_verdict.json` records `scenario: gm003`, `ok: true`, and `gm003 verdicts valid for alice, bob, charlie, dana`. Role verdicts prove Alice sent `aliceBeforeDanaAdd` and `aliceAfterDanaOfflineAdd` with `keyEpoch` 1; Bob and Charlie each received pre-add, post-add, and `danaAfterOfflineJoin` exactly once; Dana received only `aliceAfterDanaOfflineAdd` exactly once, sent `danaAfterOfflineJoin`, and did not receive `aliceBeforeDanaAdd`; Dana's `gm003OfflineCatchUpProof` and Alice's `gm003OfflineAddProof` fields are true. Supporting gates passed: `./scripts/run_test_gates.sh groups`, `./scripts/run_test_gates.sh completeness-check` (`731/731 test files classified`), and `git diff --check`. Accepted difference: `--scenario all` still expands to GM-001/GM-002 only; GM-003 uses the required direct `--scenario gm003` proof, so this is non-blocking operator convenience follow-up. GM-013 and later rows remain Open. |
| GM-004 | Remove C while C is online | C is a current member and topic peer. | 1. A removes C and rotates key. 2. A/B send. 3. C attempts send/decrypt. | A/B continue receiving; C is rejected for send and cannot decrypt post-removal messages. | P0 | Covered | Required | Required | Required | Required | Required | Covered by row-specific GM-004 closure evidence. Missing proof support was implemented without product behavior changes in `integration_test/group_multi_party_device_real_harness.dart`, `integration_test/scripts/group_multi_party_device_criteria.dart`, `integration_test/scripts/run_group_multi_party_device_real.dart`, `test/integration/group_multi_party_device_criteria_test.dart`, and `test/features/groups/integration/group_membership_smoke_test.dart`. Host/app proof passed: `flutter test --no-pub test/features/groups/application/member_removal_integration_test.dart`; focused `flutter test --no-pub test/features/groups/integration/group_membership_smoke_test.dart --plain-name 'GM-004 removes C while online, rotates key, A/B continue, and C loses access'`; full `flutter test --no-pub test/features/groups/integration/group_membership_smoke_test.dart`; criteria guard `flutter test --no-pub test/integration/group_multi_party_device_criteria_test.dart`. Exact relay/device proof passed with `--scenario gm004` on Alice `38FECA55-03C1-4907-BD9D-8E64BF8E3469`, Bob `347FB118-10D0-40C8-A05B-B0C3BD6B8CCD`, and Charlie `5BA69F1C-B112-47BE-B1FF-8C1003728C8F` using the accepted relay env; verdict `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_gm004_xapxB3/gmp_1778414729800_gm004_orchestrator_verdict.json` records `scenario: gm004`, `ok: true`, and `gm004 verdicts valid for alice, bob, charlie`. Role verdicts prove Alice/Bob are at key epoch `2`, member lists exclude Charlie, Bob receives Alice's post-removal message, Alice receives Bob's post-removal message, Charlie has no post-removal group/key, persists no post-removal plaintext, and Charlie's post-removal send is `groupNotFound` with `accepted: false`. Supporting gates passed: `./scripts/run_test_gates.sh groups`, `./scripts/run_test_gates.sh completeness-check` (`731/731 test files classified`), and `git diff --check`. This closes GM-004; GM-013 and later rows remain Open. |
| GM-005 | Remove C while C is offline | C has old config/key and no live topic. | 1. A removes C and sends N messages. 2. C reconnects with stale state. 3. C retrieves inbox and tries topic publish. | C converges to removed state and cannot access post-removal content; remaining members do not lose delivery. | P0 | Covered | Required | Required | Required | Required | Required | Covered by row-specific GM-005 closure evidence. Missing host/criteria/runner/harness proof support was implemented in `test/features/groups/integration/group_membership_smoke_test.dart`, `integration_test/scripts/group_multi_party_device_criteria.dart`, `test/integration/group_multi_party_device_criteria_test.dart`, `integration_test/scripts/run_group_multi_party_device_real.dart`, and `integration_test/group_multi_party_device_real_harness.dart`; no production behavior change was required. Host/app proof passed: focused `flutter test --no-pub test/features/groups/integration/group_membership_smoke_test.dart --plain-name 'GM-005 removes C while offline, C catches up removed, cannot access post-removal content, and A/B delivery continues'`, full `group_membership_smoke_test.dart`, `member_removal_integration_test.dart`, and `drain_group_offline_inbox_use_case_test.dart`. Criteria guard passed: `flutter test --no-pub test/integration/group_multi_party_device_criteria_test.dart` with 30 tests. Exact relay/device proof passed with `--scenario gm005` on Alice `38FECA55-03C1-4907-BD9D-8E64BF8E3469`, Bob `347FB118-10D0-40C8-A05B-B0C3BD6B8CCD`, and Charlie `5BA69F1C-B112-47BE-B1FF-8C1003728C8F` using the accepted relay env; verdict `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_gm005_J6Cj77/gmp_1778419237387_gm005_orchestrator_verdict.json` records `scenario: gm005`, `ok: true`, and `gm005 verdicts valid for alice, bob, charlie`. Role verdicts prove Bob reached key epoch `2`, excludes Charlie, and received all three Alice post-removal messages exactly once; Charlie had old config/key before offline, was offline during removal, reconnected with stale state, retrieved inbox, converged removed, has no group or rotated epoch, persisted no post-removal plaintext, and post-removal send was `groupNotFound` with `accepted: false`. Accepted harness note: `flutter drive` relaunch resets the app container, so the Charlie proof restores stale group/key from the runner fixture before drain while criteria still require stale-state, inbox retrieval, removal convergence, no post-removal plaintext/rotated key, and send rejection. Supporting gates passed: `./scripts/run_test_gates.sh groups`, `./scripts/run_test_gates.sh completeness-check` (`731/731 test files classified`), and `git diff --check`. GM-013 and later rows remain Open. |
| GM-006 | Remove C and immediately re-add C with new epoch | C was removed, key rotated, and then re-invited quickly. | 1. Remove C. 2. Rotate to E1. 3. Re-add C and distribute E1 or E2 according to product rule. 4. Send from A/B/C. | C can decrypt only the correct rejoin epoch; A/B accept C's messages under current config/device binding. | P0 | Covered | Required | Required | Required | Required | Required | Covered by row-specific GM-006 closure evidence. Missing host/criteria/runner/harness proof support was implemented in `test/features/groups/integration/group_membership_smoke_test.dart`, `integration_test/scripts/group_multi_party_device_criteria.dart`, `test/integration/group_multi_party_device_criteria_test.dart`, `integration_test/scripts/run_group_multi_party_device_real.dart`, and `integration_test/group_multi_party_device_real_harness.dart`; no production behavior change was required. Host proof passed: `flutter test --no-pub test/features/groups/integration/group_membership_smoke_test.dart --plain-name 'GM-006 removes and immediately re-adds C with current epoch and accepts only post-readd traffic'`. Criteria guard passed: `flutter test --no-pub test/integration/group_multi_party_device_criteria_test.dart` with 35 tests, including negative stale-epoch, removed-window plaintext, missing-delivery, and incomplete-proof cases. Targeted analyzer passed for the harness, runner, and criteria files. Fresh `flutter devices --machine` and `xcrun simctl list devices available` found the booted iOS simulator targets. Exact relay/device proof passed with `--scenario gm006` on Alice `38FECA55-03C1-4907-BD9D-8E64BF8E3469`, Bob `347FB118-10D0-40C8-A05B-B0C3BD6B8CCD`, and Charlie `5BA69F1C-B112-47BE-B1FF-8C1003728C8F` using the accepted relay env; verdict `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_gm006_Gjv8kM/gmp_1778421144199_gm006_orchestrator_verdict.json` records `scenario: gm006`, `ok: true`, and `gm006 verdicts valid for alice, bob, charlie`. Role verdicts prove Bob received the removed-window Alice message exactly once, Charlie received zero removed-window plaintext, all three converged on member list A/B/C and final epoch `2`, Charlie's post-readd publish was accepted, Alice/Bob received Charlie's post-readd message exactly once, and Bob/Charlie received Alice's post-readd message exactly once. Supporting gates passed: `./scripts/run_test_gates.sh groups` (`+110`), `./scripts/run_test_gates.sh completeness-check` (`731/731 test files classified`), and `git diff --check`. GM-013 and later rows remain Open. |
| GM-007 | Remove C, send during absence, then re-add C | C leaves access window for several messages. | 1. A sends M0. 2. Remove C. 3. A/B send M1..M3. 4. Re-add C. 5. Send M4. | C sees M0 if allowed by old membership and M4 after rejoin, but not M1..M3. | P0 | Covered | Required | Required | Required | Required | Required | Covered by row-specific GM-007 closure evidence. Missing host/criteria/runner/harness proof support was implemented in `test/features/groups/integration/group_membership_smoke_test.dart`, `integration_test/scripts/group_multi_party_device_criteria.dart`, `test/integration/group_multi_party_device_criteria_test.dart`, `integration_test/scripts/run_group_multi_party_device_real.dart`, and `integration_test/group_multi_party_device_real_harness.dart`; no production behavior change was required. Host proof passed: `flutter test --no-pub test/features/groups/integration/group_membership_smoke_test.dart --plain-name 'GM-007 preserves allowed pre-removal and post-readd messages while excluding removed-window messages'`. Criteria guard passed: `flutter test --no-pub test/integration/group_multi_party_device_criteria_test.dart` with 41 tests, including negative missing-proof, missing-M0, removed-window plaintext, missing Bob removed-window delivery, and missing-M4 cases. Targeted analyzer passed for the harness, runner, and criteria files. Fresh device discovery found the booted iOS simulator targets. Exact relay/device proof passed with `--scenario gm007` on Alice `38FECA55-03C1-4907-BD9D-8E64BF8E3469`, Bob `347FB118-10D0-40C8-A05B-B0C3BD6B8CCD`, and Charlie `5BA69F1C-B112-47BE-B1FF-8C1003728C8F` using the accepted relay env; verdict `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_gm007_Yp12M1/gmp_1778422910998_gm007_orchestrator_verdict.json` records `scenario: gm007`, `ok: true`, and `gm007 verdicts valid for alice, bob, charlie`. Role verdicts prove Alice sent M0 before removal, M1..M3 while Charlie was removed, and M4 after re-add; Bob received M0, M1, M2, M3, and M4 exactly once; Charlie received M0 and M4 exactly once, stored zero removed-window plaintext, had `hasStaleEpochAfterReadd: false`, and all roles converged on member list A/B/C with final epoch `2`. Supporting gates passed: `./scripts/run_test_gates.sh groups` (`+111`), `./scripts/run_test_gates.sh completeness-check` (`731/731 test files classified`), and `git diff --check`. GM-013 and later rows remain Open. |
| GM-008 | Remove C, re-add C after app restart | All peers restart or C restarts between remove and re-add. | 1. Remove C. 2. Restart one or more nodes. 3. Re-add C. 4. Send both directions. | Rejoin does not rely on in-memory PrevKey/grace state; persisted config/key epoch is sufficient. | P0 | Covered | Required | Required | Required | Required | Required | Covered by row-specific GM-008 closure evidence. Missing host/criteria/runner/harness proof support was implemented without production behavior changes in `test/shared/fakes/group_test_user.dart`, `test/features/groups/integration/group_membership_smoke_test.dart`, `integration_test/group_multi_party_device_real_harness.dart`, `integration_test/scripts/run_group_multi_party_device_real.dart`, `integration_test/scripts/group_multi_party_device_criteria.dart`, and `test/integration/group_multi_party_device_criteria_test.dart`. Host proof passed: `flutter test --no-pub test/features/groups/integration/group_membership_smoke_test.dart --plain-name 'GM-008 removes C, restarts C before re-add, and rejoins from current persisted epoch'`. Criteria guard passed: `flutter test --no-pub test/integration/group_multi_party_device_criteria_test.dart`. Adjacent suites and gates passed: full `group_membership_smoke_test.dart`, `member_removal_integration_test.dart`, `group_new_member_onboarding_test.dart`, `group_startup_rejoin_smoke_test.dart`, targeted analyzer, `./scripts/run_test_gates.sh groups`, `./scripts/run_test_gates.sh completeness-check` (`731/731`), and `git diff --check`. Initial exact simulator proof failed due to DerivedData/stale app-session state; this was fixed by clearing Runner/Pods DerivedData and `build/ios`, uninstalling `com.mknoon.app`, `com.mknoon.app.ShareExtension`, and `com.mknoon.app.NotificationService` from the three target iOS simulators, rebooting the simulators, and rerunning the exact command. Exact three-iOS-simulator relay proof then passed with `--scenario gm008` on Alice `38FECA55-03C1-4907-BD9D-8E64BF8E3469`, Bob `347FB118-10D0-40C8-A05B-B0C3BD6B8CCD`, and Charlie `5BA69F1C-B112-47BE-B1FF-8C1003728C8F`; verdict `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_gm008_OwsAQ5/gmp_1778437132542_gm008_orchestrator_verdict.json` records `scenario: gm008`, `ok: true`, and `gm008 verdicts valid for alice, bob, charlie`. Role verdicts prove Charlie was removed, Charlie restarted before re-add, the removed-window send occurred while Charlie was restarted/absent, Charlie had no group/key before re-add, Charlie received no removed-window plaintext, all roles converged on A/B/C and final epoch `2`, Bob received Alice's removed-window and post-readd messages exactly once, Charlie's post-readd publish was accepted, Alice/Bob received Charlie's post-readd message exactly once, and Bob/Charlie received Alice's post-readd message exactly once. GM-013 and later rows remain Open. |
| GM-009 | Remove C twice is idempotent | C is removed once already. | 1. Apply the same remove event twice. 2. Rotate key at most once according to event ID. 3. Send. | No duplicate member state, no duplicate key rotation, and A/B delivery remains reliable. | P0 | Covered | Required | Required | Required | Required | Required | Covered by row-specific GM-009 closure evidence. Product fix in `remove_group_member_use_case.dart` makes duplicate same-event absent-member removal a strict idempotent no-op only when the membership event watermark proves the logical remove was already applied; unknown absent-member removes still throw. Focused host proof passed: `flutter test --no-pub test/features/groups/integration/group_membership_smoke_test.dart --plain-name 'GM-009 removes C twice idempotently, rotates at most once, and preserves A/B delivery'`. Adjacent/direct gates passed: `member_removal_integration_test.dart`, full `group_membership_smoke_test.dart`, `group_new_member_onboarding_test.dart`, focused GM-009 criteria test, targeted analyzer, `./scripts/run_test_gates.sh groups`, `./scripts/run_test_gates.sh completeness-check` (`731/731`), and `git diff --check`. Exact three-iOS-simulator relay proof passed with `--scenario gm009` on Alice `38FECA55-03C1-4907-BD9D-8E64BF8E3469`, Bob `347FB118-10D0-40C8-A05B-B0C3BD6B8CCD`, and Charlie `5BA69F1C-B112-47BE-B1FF-8C1003728C8F`; verdict `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_gm009_BVfZlR/gmp_1778439691828_gm009_orchestrator_verdict.json` records `scenario: gm009`, `ok: true`, and `gm009 verdicts valid for alice, bob, charlie`. Role verdicts prove Alice removed Charlie once, ignored the duplicate remove, recorded one removal timeline row, rotated and distributed once, did not distribute the key to Charlie, and ended at epoch `2`; Bob excludes Charlie and receives Alice's post-duplicate-remove message exactly once; Charlie has no group after duplicate removal, no rotated epoch, zero post-removal plaintext, and rejected post-removal publish. GM-013 and later rows remain Open. |
| GM-010 | Re-add C twice is idempotent | C receives duplicate invite/add events. | 1. Apply add event twice. 2. C accepts or joins twice. 3. Send. | C has one active membership/device binding; no duplicate topic joins or duplicate inbox recipients. | P0 | Covered | Required | Required | Required | Recommended | Recommended | Covered by row-specific GM-010 closure evidence. Product fix in `add_group_member_use_case.dart` makes a strict identical duplicate member/device add or re-add a no-op while preserving conflicting duplicate-add rejection. Focused GM-010 host regression, full `group_membership_smoke_test.dart`, `add_group_member_use_case_test.dart`, duplicate `member_added` and `members_added` listener checks, `group_new_member_onboarding_test.dart`, GM-010 criteria tests, targeted analyzer, conditional GM-006 through GM-009 focused host checks, `./scripts/run_test_gates.sh groups`, `./scripts/run_test_gates.sh completeness-check`, and `git diff --check` passed. Exact simulator-only proof passed with `--scenario gm010` on Alice `38FECA55-03C1-4907-BD9D-8E64BF8E3469`, Bob `347FB118-10D0-40C8-A05B-B0C3BD6B8CCD`, and Charlie `5BA69F1C-B112-47BE-B1FF-8C1003728C8F`; verdict `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_gm010_8wG9iD/gmp_1778443518461_gm010_orchestrator_verdict.json` records `scenario: gm010`, `ok: true`, and `gm010 verdicts valid for alice, bob, charlie`. Role verdicts prove one Charlie membership row, one active Charlie device binding, one measured Charlie group-config join after re-add, duplicate re-add did not trigger another join, durable inbox recipients are unique, Alice/Bob/Charlie delivery is reliable and exact-once, and all roles converge. Accepted QA fix: final proof uses harness-measured `charlieGroupConfigJoinCountAfterReadd`, `duplicateReaddTriggeredCharlieGroupConfigJoin`, and `charlieJoinMeasurementSource`; earlier `mT2xfk` proof is historical only. GM-013 and later rows remain Open. |
| GM-011 | Remove then stale add arrives out of order | Network delivers older add after newer remove. | 1. Create add event at version 2. 2. Remove at version 3. 3. Deliver version 2 after version 3. 4. Send. | Final membership remains removed; stale add does not resurrect C or old keys. | P0 | Covered | Required | Required | Required | Required | Required | Covered by row-specific GM-011 closure evidence. Existing timestamp watermark handling in `group_message_listener.dart` was proven sufficient for stale add-after-remove; no GM-011 product behavior fix was required beyond mechanical analyzer cleanup in owner/helper files. Focused GM-011 listener regression, focused GM-011 host integration, focused GM-011 criteria, full listener suite, full `group_membership_smoke_test.dart`, `member_removal_integration_test.dart`, `group_new_member_onboarding_test.dart`, full `group_multi_party_device_criteria_test.dart`, conditional dirty-owner `add_group_member_use_case_test.dart`, targeted analyzer, exact three-iOS-simulator `--scenario gm011`, `./scripts/run_test_gates.sh groups`, `./scripts/run_test_gates.sh completeness-check`, and `git diff --check` passed. Exact simulator-only proof used Alice `38FECA55-03C1-4907-BD9D-8E64BF8E3469`, Bob `347FB118-10D0-40C8-A05B-B0C3BD6B8CCD`, and Charlie `5BA69F1C-B112-47BE-B1FF-8C1003728C8F`; verdict `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_gm011_9PPfPc/gmp_1778446826367_gm011_orchestrator_verdict.json` records `scenario: gm011`, `ok: true`, and detail `gm011 verdicts valid for alice, bob, charlie`. Role/criteria proof showed the older `member_added` stale add delivered after newer `member_removed` was ignored; final Alice/Bob member/config state excludes Charlie; Charlie was not resurrected, could not publish/decrypt/read post-removal plaintext, and durable recipients exclude Charlie; Alice/Bob delivery remained exact/reliable. GM-013 and later rows remain Open. |
| GM-012 | Add then stale remove arrives out of order | Network delivers older remove after newer re-add. | 1. Remove C version 2. 2. Re-add C version 3. 3. Deliver version 2 late. 4. Send. | Final membership remains re-added only if version 3 wins; old remove does not strand C. | P0 | Covered | Required | Required | Required | Required | Required | Covered by row-owned GM-012 listener, host, criteria, runner, and exact three-iOS-simulator proof. Existing timestamp watermark behavior was sufficient; no GM-012 product behavior fix was required. Verdict `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_gm012_KrNL8h/gmp_1778450066004_gm012_orchestrator_verdict.json` records `scenario: gm012`, `ok: true`, and `gm012 verdicts valid for alice, bob, charlie`; role proof keeps Charlie re-added exactly once with one active device binding, final epoch 2/current config, unique durable recipients where applicable, and exact A/B/C delivery including Bob's send leg. |
| GM-013 | Simultaneous admin remove and member send | C sends while A removes C. | 1. Start C publish under old epoch. 2. Apply remove+rotation. 3. Deliver C envelope at A/B. | A deterministic cutoff is enforced: message before remove accepted, after remove rejected, with clear event. | P0 | Covered | Required | Required | Required | Required | Required | Covered by row-owned GM-013 code/test/device evidence. `remove_group_member_use_case.dart` now preserves/persists the removal cutoff before local member deletion, with caller/fake/harness wiring, so valid before-cutoff Charlie traffic is not downgraded to unknown sender and after-cutoff Charlie traffic rejects with `GROUP_HANDLE_INCOMING_MSG_REMOVED_AFTER_CUTOFF`. Focused handle-incoming, listener, host membership smoke, criteria, full six-file direct `flutter test --no-pub` sweep (`272` tests), targeted GM-013 analyzer on owned files, `./scripts/run_test_gates.sh groups`, completeness-check (`731/731`), and diff checks passed. Exact three-iOS-simulator proof passed with `--scenario gm013` on Alice `38FECA55-03C1-4907-BD9D-8E64BF8E3469`, Bob `347FB118-10D0-40C8-A05B-B0C3BD6B8CCD`, and Charlie `5BA69F1C-B112-47BE-B1FF-8C1003728C8F`; verdict `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_gm013_KQsxqK/gmp_1778453850242_gm013_orchestrator_verdict.json` records `scenario: gm013`, `ok: true`, and `gm013 verdicts valid for alice, bob, charlie`. Residual note: broader touched-file analyzer still reports 26 pre-existing warning-level `group_info_wired.dart` findings outside the GM-013 callsite; GM-014 and later rows remain Open. |
| GM-014 | Simultaneous re-add and sender send | C is re-added while A sends under new epoch. | 1. A updates config/key. 2. C receives key slightly later. 3. A sends. 4. C joins/retrieves. | C obtains/decrypts all messages from its membership start or receives explicit key-missing repair, not silent loss. | P0 | Covered | Required | Required | Required | Required | Required | Covered by row-owned GM-014 code/test/device evidence. `handle_incoming_group_message_use_case.dart` and `group_message_listener.dart` now preserve one shared re-add membership-start timestamp from `member_added`/`members_added` `eventAt` across live and replay paths and reject removed-window messages after the persisted removal cutoff. Focused GM-014 listener, offline-inbox, key-update, host membership smoke, and criteria tests passed; adjacent `PREREQ-FUTURE-EPOCH-KEY-REPAIR`, `GEK002`, `GEK003`, GM-006, GM-007, GM-010, GM-012, full `member_removal_integration_test.dart`, full `group_new_member_onboarding_test.dart`, targeted analyzer, `./scripts/run_test_gates.sh groups`, completeness-check (`731/731`), and diff hygiene passed. Exact three-iOS-simulator proof passed with `--scenario gm014` on Alice `38FECA55-03C1-4907-BD9D-8E64BF8E3469`, Bob `347FB118-10D0-40C8-A05B-B0C3BD6B8CCD`, and Charlie `5BA69F1C-B112-47BE-B1FF-8C1003728C8F`; verdict `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_gm014_RnOLc5/gmp_1778458583240_gm014_orchestrator_verdict.json` records `scenario: gm014`, `ok: true`, and `gm014 verdicts valid for alice, bob, charlie`. QA accepted after one fix pass: criteria reject cross-role re-add timestamp drift, and the harness measures removed-window plaintext, re-add topic joins, and duplicate durable recipients from actual state. GM-017 and later rows remain Open. |
| GM-015 | Admin removes self/creator policy is explicit | A is admin/creator. | 1. Attempt to remove A or have A leave. 2. Send from remaining members. 3. Inspect roles. | Current policy is explicit `block_last_admin/no_op`: self-remove and leave are blocked, not transfer/dissolve. | P0 | Covered | Required | Required | Required | Recommended | Required | Covered by direct remove/leave/member-removal/smoke/criteria tests and exact three-iOS-simulator `gm015` proof `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_gm015_gOh1Lx/gmp_1778461641801_gm015_orchestrator_verdict.json` (`ok: true`; Alice `38FECA55-03C1-4907-BD9D-8E64BF8E3469`, Bob `347FB118-10D0-40C8-A05B-B0C3BD6B8CCD`, Charlie `5BA69F1C-B112-47BE-B1FF-8C1003728C8F`). `broadcast_voluntary_leave_use_case.dart` exposes `VoluntaryLeaveBroadcastSkipReason.lastAdmin`; role proof keeps Alice creator/admin, proves Bob/Charlie post-attempt delivery, and prevents writerless zombie state. |
| GM-016 | Removed member remains unsubscribed from topic | C was removed and app called LeaveGroupTopic. | 1. C receives old discovery/register state. 2. A sends post-removal. 3. Inspect C events. | C has no active discovery loop/subscription and emits no post-removal message. | P0 | Covered | Required | Required | Required | Required | Required | Covered by row-owned GM-016 tests/harness/criteria support with no production source changes. Focused host tests prove `group:leave`/`LeaveGroupTopic`, local group/member/key cleanup, no stale post-leave persistence/emission, no rejoin from deleted state, and healthy A/B delivery while Charlie remains unsubscribed. Exact three-iOS-simulator proof `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_gm016_pfv9ja/gmp_1778464599719_gm016_orchestrator_verdict.json` records `scenario: gm016`, `ok: true`, and `gm016 verdicts valid for alice, bob, charlie` on Alice `38FECA55-03C1-4907-BD9D-8E64BF8E3469`, Bob `347FB118-10D0-40C8-A05B-B0C3BD6B8CCD`, and Charlie `5BA69F1C-B112-47BE-B1FF-8C1003728C8F`. Role proof shows Charlie `leaveRequested`/`leaveResponseOk`, no group recreation after a 5002ms quiet window, zero member/key rows, zero post-leave join/inbound/reaction/discovery/parse/decrypt/plaintext counts, and no Alice post-removal plaintext; Alice's recipient list is Bob only and Bob persists Alice's post-removal message once. Targeted analyzer, `./scripts/run_test_gates.sh groups`, `./scripts/run_test_gates.sh completeness-check` (`731/731`), and `git diff --check` passed. GM-017 stale publish validator backstop remains Open. |
| GM-017 | Removed member with stale subscription cannot publish accepted messages | C did not call LeaveGroupTopic after removal. | 1. A/B update config without C. 2. C publishes with old key. 3. Validators on A/B run. | A/B reject C as non_member or bad epoch and log validation_rejected. | P0 | Covered | Required | Required | Required | Required | Required | Covered by row-owned GM-017 validator, criteria, runner, harness, and host-support evidence. Go production code did not require changes; `go-mknoon/node/pubsub_delivery_test.go::TestGM017RemovedMemberWithStaleSubscriptionRejectedByRemainingValidators` was added and passed with `(cd go-mknoon && go test ./node -run '^TestGM017RemovedMemberWithStaleSubscriptionRejectedByRemainingValidators$' -count=1)`. GM-017 criteria/runner/harness support landed in `integration_test/scripts/group_multi_party_device_criteria.dart`, `test/integration/group_multi_party_device_criteria_test.dart`, `integration_test/scripts/run_group_multi_party_device_real.dart`, and `integration_test/group_multi_party_device_real_harness.dart`; focused support landed in `test/features/groups/application/member_removal_integration_test.dart` and `test/features/groups/integration/group_membership_smoke_test.dart`. Focused GM-017 criteria/member-removal/membership selectors passed; direct `member_removal_integration_test.dart` plus `group_membership_smoke_test.dart` passed 47 tests after `flutter pub get`; `flutter test test/features/groups/integration/group_new_member_onboarding_test.dart` passed 7 tests. Targeted analyzer exited 0 with only unrelated info-level `use_null_aware_elements` lints in pre-existing production files. Exact simulator-only `--scenario gm017` proof passed on Alice `38FECA55-03C1-4907-BD9D-8E64BF8E3469`, Bob `347FB118-10D0-40C8-A05B-B0C3BD6B8CCD`, and Charlie `5BA69F1C-B112-47BE-B1FF-8C1003728C8F`; verdict `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_gm017_dVjLD0/gmp_1778467478117_gm017_orchestrator_verdict.json` records `scenario: gm017`, `ok: true`, and `gm017 verdicts valid for alice, bob, charlie`. Role proof: Alice and Bob each have `validationRejected: true`, `validationRejectCount: 1`, `validationRejectReason: non_member`, `stalePlaintextCount: 0`, and final member lists exclude Charlie; Charlie has `groupPresentAfterRemoval: true`, `keyPresentAfterRemoval: true`, `memberListStillIncludesCharlie: true`, `staleSubscriptionPresent: true`, `sentStaleMarker: true`, `leaveRequested: false`, and `leaveResponseOk: false`; Bob received Alice's healthy post-rejection message once. `./scripts/run_test_gates.sh groups` passed 122 tests, `./scripts/run_test_gates.sh completeness-check` passed `731/731`, and `git diff --check` passed. Conditional `integration_test/group_real_crypto_onboarding_test.dart` was not run because GM-017 did not touch real-crypto onboarding, bridge encryption, or onboarding harness behavior beyond scenario proof wiring. No simulator/Xcode build-state blocker occurred for GM-017. Execution used local sequential fallback because nested Executor/QA child tools were unavailable; this is accepted execution mode, not a product residual. Checkpoint skipped because a clean scoped checkpoint remains unsafe due overlapping aggregate rollout docs and existing dirty product/test edits. GM-018 and later rows remain Open. |
| GM-018 | Remaining members keep delivery after C removal | A/B remove C. | 1. A sends multiple messages after C removal. 2. B receives live and via inbox. 3. Repeat with C online/offline. | B's delivery is unaffected by C's stale state or rejection spam. | P0 | Covered | Required | Required | Required | Required | Required | Covered by row-owned GM-018 application/member-removal proof, host membership smoke proof, Go stale-pressure delivery proof, criteria/runner/harness support, and exact simulator-only `--scenario gm018` proof. No production code changes were required. Accepted verdict `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_gm018_YlHgXb/gmp_1778472193920_gm018_orchestrator_verdict.json` records `scenario: gm018`, `ok: true`, and `gm018 verdicts valid for alice, bob, charlie` on Alice `38FECA55-03C1-4907-BD9D-8E64BF8E3469`, Bob `347FB118-10D0-40C8-A05B-B0C3BD6B8CCD`, and Charlie `5BA69F1C-B112-47BE-B1FF-8C1003728C8F`. QA rejected the first green simulator verdict because Bob was not offline and replay count was hardcoded; fix loop 1 now proves Bob stopped before Alice inbox sends, Bob restarted before explicit durable inbox drain, replay fields use actual IDs/counts, Charlie stale online pressure is live-only, and Bob proof includes member list excluding Charlie, stale online/offline pressure handling, exact-once delivery, zero live inbox leak before replay, `liveBobReceiptCount: 3`, `inboxReplayReceiptCount: 3`, replay keys `aliceGm018Inbox1..3`, and persisted count `1` for each Alice live/inbox/after-offline message. GM-019 and later rows remain Open. |
| GM-019 | Re-added member is in recipient list for durable inbox only after re-add | C is removed then re-added. | 1. Store messages during removed interval. 2. Store messages after re-add. 3. Inspect recipientPeerIds. | C is excluded from removed-window recipient sets and included after re-add. | P0 | Covered | Required | Required | Required | Required | Required | Covered by row-owned GM-019 code/test/harness evidence. `send_group_message_use_case.dart` now applies a membership cutoff for explicit in-group-lifetime send timestamps when selecting durable `recipientPeerIds`, while Go `GroupInboxStore` remains the accepted caller-provided-recipient boundary. Focused GM-019 host selectors, adjacent group host suites, targeted analyzer over touched Dart files, exact simulator-only `--scenario gm019`, `./scripts/run_test_gates.sh groups` after adjacent MS004 fix (`+124`), `./scripts/run_test_gates.sh completeness-check` (`731/731`), and `git diff --check` passed. Accepted verdict `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_gm019_a1v7yz/gmp_1778475225394_gm019_orchestrator_verdict.json` records `scenario: gm019`, `ok: true`, and `gm019 verdicts valid for alice, bob, charlie` on Alice `38FECA55-03C1-4907-BD9D-8E64BF8E3469`, Bob `347FB118-10D0-40C8-A05B-B0C3BD6B8CCD`, and Charlie `5BA69F1C-B112-47BE-B1FF-8C1003728C8F`. Actual durable payload proof, not recomputed membership: Alice removed-window recipients are Bob-only; Alice post-readd recipients are Bob+Charlie; Bob post-readd recipients are Alice+Charlie; Charlie receives no removed-window plaintext and receives Alice/Bob post-readd messages. Go tests were skipped because no Go files changed; real-crypto onboarding was skipped because no onboarding/crypto surfaces changed; simulator cleanup/rerun was skipped because simulator/Xcode/Flutter build state did not fail. GM-020 and later rows remain Open. |
| GM-020 | Removed member is excluded from recipient list immediately after removal | C was active and may be offline. | 1. Remove C. 2. A sends immediately. 3. Inspect GroupInboxStore request. | recipientPeerIds omit C for every post-removal message. | P0 | Covered | Required | Required | Required | Required | Required | Covered by row-owned GM-020 tests/harness/criteria/runner proof with no production code changes. Focused GM-020 send-use-case, member-removal, membership-smoke, and criteria selectors passed; adjacent member-removal plus membership suites, onboarding suite, targeted analyzer over touched Dart files, exact simulator-only `--scenario gm020`, `./scripts/run_test_gates.sh groups`, `./scripts/run_test_gates.sh completeness-check` (`731/731`), and `git diff --check` passed. Accepted verdict `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_gm020_UyQtpw/gmp_1778478097803_gm020_orchestrator_verdict.json` records `scenario: gm020`, `ok: true`, and `gm020 verdicts valid for alice, bob, charlie` on Alice `38FECA55-03C1-4907-BD9D-8E64BF8E3469`, Bob `347FB118-10D0-40C8-A05B-B0C3BD6B8CCD`, and Charlie `5BA69F1C-B112-47BE-B1FF-8C1003728C8F`. Alice proof shows immediate and offline post-removal sends `aliceGm020ImmediatePostRemoval` and `aliceGm020OfflinePostRemoval` both have `actualDurablePayloadProof: true`, Bob-only `recipientPeerIds`, ordered timestamps `removedAt < firstPostRemovalSentAt < offlinePostRemovalSentAt`, `postRemovalMessageCount: 2`, `everyPostRemovalExcludedCharlie: true`, and `charlieUnavailableBeforeOfflinePostRemoval: true`; Bob receives both exactly once; Charlie has no group/member/key after removal, receives zero post-removal plaintext, is unavailable before the offline send, and both post-removal keys are checked. Go tests were skipped because no Go files changed, real-crypto onboarding was skipped because no onboarding/crypto surfaces changed, and simulator cleanup/rerun was skipped because simulator/Xcode/Flutter build state did not fail. GM-021 is covered separately; GM-022 and later rows remain Open. |
| GM-021 | Re-add uses fresh invite/key package, not cached removed package | C was previously removed. | 1. Re-add C. 2. Compare key package/device identity with old one. 3. Send from C. | Validator binds C to the active device/key package only; old removed package cannot send. | P0 | Covered | Required | Required | Required | Recommended | Required | Covered by row-owned GM-021 code/test/harness proof. Production changes: `lib/features/groups/application/send_group_message_use_case.dart` propagates the resolved fresh sender key package id into live `group:publish` while the replay path was already aligned; `lib/core/bridge/go_bridge_client.dart` passes `group:validation_rejected` through to raw diagnostic flow/event handling with no claimed message callback behavior change. Focused Go validator, member-removal RED-then-GREEN, membership-smoke, criteria, adjacent member-removal/membership, onboarding, exact simulator-only `--scenario gm021`, `./scripts/run_test_gates.sh groups`, `./scripts/run_test_gates.sh completeness-check`, `git diff --check`, and QA spot-reruns passed. Accepted final verdict `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_gm021_Vf3gW7/gmp_1778482464428_gm021_orchestrator_verdict.json` records `scenario: gm021`, `ok: true`, and `gm021 verdicts valid for alice, bob, charlie` on Alice `38FECA55-03C1-4907-BD9D-8E64BF8E3469`, Bob `347FB118-10D0-40C8-A05B-B0C3BD6B8CCD`, and Charlie `5BA69F1C-B112-47BE-B1FF-8C1003728C8F`. Proof shows Charlie's simulator old/fresh device id is the same while old/fresh key package ids are distinct, the active config contains the fresh package and not the old removed package, Charlie's fresh send uses the fresh `senderKeyPackageId` and reaches Alice/Bob exactly once, the same-active-device stale old `SenderKeyPackageId` attempt is rejected as `unbound_device`, and Alice/Bob stale plaintext count is 0. Residual-only: simulator proof does not assert full old-device rejection because old/fresh device IDs are the same; the conditional different-device path is covered by the Go validator selector. Conditional real-crypto onboarding and physical/external devices were not required. GM-022 and later rows remain Open. |
| GM-022 | Member list contains no duplicate peer IDs after re-add | C is removed and re-added many times. | 1. Run 20 remove/re-add cycles. 2. Inspect config members on A/B/C. 3. Send. | Exactly one active C entry exists; findMember returns the active entry; delivery remains stable. | P0 | Covered | Required | Required | Required | Recommended | Required | Covered by row-owned GM-022 code/test/harness proof. Production changes in `lib/features/groups/application/group_config_payload.dart`, `lib/features/groups/application/group_message_listener.dart`, and `go-mknoon/node/pubsub.go` normalize duplicate member/config peer IDs so active/fresher entries win and Go validator lookup cannot bind to a stale duplicate. Proof changes added GM-022 regressions and criteria/runner/harness support. Passed: `(cd go-mknoon && go test ./node -run 'TestGM022|TestFindMember_DuplicatePeerId|TestGroupTopicValidator')`, five focused GM-022 Flutter selectors, adjacent member-removal/membership and onboarding suites, exact simulator-only `--scenario gm022`, `./scripts/run_test_gates.sh groups`, `./scripts/run_test_gates.sh completeness-check`, `git diff --check`, and independent QA reruns of the Go selector, focused GM-022 Flutter/criteria selectors, and `git diff --check`. Accepted verdict `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_gm022_lTUTTe/gmp_1778486096373_gm022_orchestrator_verdict.json` records `scenario: gm022`, `ok: true`, and `gm022 verdicts valid for alice, bob, charlie` on Alice `38FECA55-03C1-4907-BD9D-8E64BF8E3469`, Bob `347FB118-10D0-40C8-A05B-B0C3BD6B8CCD`, and Charlie `5BA69F1C-B112-47BE-B1FF-8C1003728C8F`. All roles record `removeReaddCycleCount: 20`, no duplicate raw/config member peer IDs, `duplicateMemberPeerIds: []`, `charlieMemberEntryCount: 1`, `activeCharlieEntryCount: 1`, `activeCharlieDeviceCount: 1`, `validatorUsedActiveEntry: true`, `freshCharlieSendAccepted: true`, `staleShadowSendAccepted: false`, `postCycleDeliveryStable: true`, and `durableRecipientsUnique: true`; Alice/Bob/Charlie post-cycle messages deliver exactly once with unique durable recipients. Conditional real-crypto onboarding was not run because no onboarding/crypto surfaces changed, physical/external devices were not used, and simulator-only proof was accepted. GM-023 and later rows remain Open. |
| GM-023 | Member list contains no stale inactive entry that shadows active C | Implementation keeps tombstones/inactive rows. | 1. Keep inactive C before active C in list. 2. Validate C message. 3. Discover/dial. | Authorization/discovery uses the active C record or rejects duplicate config; inactive shadow cannot break delivery. | P0 | Covered | Required | Required | Required | Recommended | Required | Covered by row-owned GM-023 code/test/harness proof. Production change: `go-mknoon/node/pubsub.go` active discovery/dial/count target selection now uses the active device transport identity, so an inactive/tombstoned Charlie cannot shadow count or dial as active. Flutter product code did not change; existing config/listener/send surfaces passed focused GM-023 selectors. RED observed: Go `TestGM023GroupPeerDiscoveryUsesActiveDeviceAfterInactiveShadow` failed because `countRemoteGroupMembers` returned `2` instead of `1`, and criteria/runner plus the exact runner failed because `gm023` was unsupported; direct Flutter GM-023 product selectors already passed. Passed: `(cd go-mknoon && go test ./node -run 'TestGM023|TestFindMember_DuplicatePeerId|TestGroupTopicValidator|TestGroupPeerDiscovery')`, all required focused GM-023 Flutter selectors, exact simulator-only `--scenario gm023`, `./scripts/run_test_gates.sh groups`, `./scripts/run_test_gates.sh completeness-check`, `flutter test test/features/groups/integration/group_new_member_onboarding_test.dart`, and `git diff --check`. Accepted verdict `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_gm023_Ct6xrV/gmp_1778490465751_gm023_orchestrator_verdict.json` records `scenario: gm023`, `ok: true`, and `gm023 verdicts valid for alice, bob, charlie` on Alice `38FECA55-03C1-4907-BD9D-8E64BF8E3469`, Bob `347FB118-10D0-40C8-A05B-B0C3BD6B8CCD`, and Charlie `5BA69F1C-B112-47BE-B1FF-8C1003728C8F`. All roles record `gm023InactiveShadowProof` with `inactiveShadowBeforeActive: true`, `duplicateConfigRejected: false`, `activeEntrySelected: true`, `freshCharlieSendAccepted: true`, `staleInactiveShadowSendAccepted: false`, `discoveryUsedActiveEntry: true`, `inactiveShadowDialedOrCounted: false`, `postShadowDeliveryStable: true`, `durableRecipientsUnique: true`, and one active Charlie entry/device. Initial exact simulator proof hit Charlie install-state `IXErrorDomain code=19` twice and passed after simulator/build cleanup; no physical external devices were used. Residual-only: an adjacent combined host command still has non-GM023 GM-019 failure `member_removal_integration_test.dart:1235`, but GM-023 selectors/gates passed and QA classified it as non-blocking. GM-024 and later rows remain Open. |
| GM-024 | Member display/state converges for A, B, and re-added C | A removes/re-adds C. | 1. Inspect member list, role, topic joined state, key epoch, compose permission. 2. Send. | All surfaces agree on C's current status and the transport state matches UI state. | P0 | Covered | Required | Required | Recommended | Recommended | Required | Covered by GM-024 recovery evidence after the 2026-05-11 gap-closure reconciliation reopened the stale accepted row. Product fix: `lib/features/groups/application/group_message_listener.dart` now resolves authoritative snapshot `joinedAt` so `member_added` / `members_added` event timestamps apply only to actual added peer IDs; incumbent members without local rows fall back to group `createdAt`; `preserveMissingPermissions: false` remains unchanged for authoritative snapshots. Test fix: `test/features/groups/integration/group_membership_smoke_test.dart` GM-024 fixture controls the re-add and later snapshot timestamps and proves Alice's incumbent post-readd message is accepted even when the authoritative snapshot timestamp is later, while Charlie/Bob post-readd delivery and unique durable recipients remain covered. Fresh controller reruns passed: `flutter test --no-pub test/features/groups/integration/group_membership_smoke_test.dart --plain-name 'GM-024'` (`00:00 +1: All tests passed!`), overlapping `--plain-name 'GM-025'` (`00:00 +1: All tests passed!`), and `git diff --check`. The prior accepted `gm024_ybrOjp` simulator proof and the 15:55 stale accepted state are historical only and are superseded by this recovery evidence. GM-025, GM-027, and GM-028 remain covered separately; GM-029 and later rows remain Open. |
| GM-025 | Role change during re-add is honored | C is re-added with a current private-chat role/permission set. | 1. Re-add C with role/permissions R. 2. Inspect member/config state on A/B/C. 3. Exercise the private-chat action controlled by that role or permission. | Action policy matches latest role/permissions, not cached old state. | P0 | Covered | Required | Required | Required | Recommended | Required | Covered by row-owned GM-025 code/test/proof evidence. Production change: `lib/features/groups/domain/models/group_member.dart` adds explicit authoritative snapshot parsing control for missing permission overrides, and `lib/features/groups/application/group_message_listener.dart` applies authoritative group config snapshots with `preserveMissingPermissions: false` so a re-added member cannot keep stale permission overrides when the current config omits them; partial member parsing keeps the previous preserve behavior. Proof/support changed in `test/features/groups/application/member_removal_integration_test.dart`, `test/features/groups/integration/group_membership_smoke_test.dart`, `test/integration/group_multi_party_device_criteria_test.dart`, `test/shared/fakes/group_test_user.dart`, `integration_test/group_multi_party_device_real_harness.dart`, `integration_test/scripts/group_multi_party_device_criteria.dart`, and `integration_test/scripts/run_group_multi_party_device_real.dart`. RED baseline: focused GM-025 member-removal, membership-smoke, and criteria selectors initially exited 79/no tests matched; the fake-network selector then exposed stale `removeMembers` on Bob after Charlie re-add before the scoped fix. Passed: focused GM-025 member-removal, membership-smoke, and criteria selectors; direct add-member/remove-member/group-message-listener and new-member-onboarding suites; exact simulator-only `--scenario gm025` on Alice `38FECA55-03C1-4907-BD9D-8E64BF8E3469`, Bob `347FB118-10D0-40C8-A05B-B0C3BD6B8CCD`, and Charlie `5BA69F1C-B112-47BE-B1FF-8C1003728C8F`; `./scripts/run_test_gates.sh completeness-check` (`731/731`); and `git diff --check`. Accepted verdict `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_gm025_1M8qKB/gmp_1778505949522_gm025_orchestrator_verdict.json` records `scenario: gm025`, `ok: true`, and `gm025 verdicts valid for alice, bob, charlie`. Role proof shows exactly one current Charlie entry, old `removeMembers` permission was true, re-added `removeMembers` permission is false, stale remove-members authorization remains false after re-add, bridge current role and permission proofs are true, Charlie's stale action is attempted and denied, Bob remains a member, Alice still sees Bob, no action tombstone persists, live topic peer count is 2, and Alice/Bob/Charlie post-readd sends deliver exactly once with actual durable and topic-peer proof. Accepted difference: `./scripts/run_test_gates.sh groups` is red due the existing non-GM-025 GM-024 selector failure at `group_membership_smoke_test.dart:5796`; the same JSON reproduction shows GM-025 passed, and no GM-024 patch was made in this GM-025 session. GM-027 and GM-028 remain covered separately; GM-029 and later rows remain Open. |
| GM-027 | Unknown peer ID cannot be added as ghost member | Invite target lacks valid peer/device info. | 1. Add unknown/invalid peer ID. 2. Start discovery and send. 3. Inspect recipient list. | Invalid member is rejected or marked failed; it does not inflate expected peer counts or block reliability. | P0 | Covered | Required | Required | Required | Recommended | Recommended | Covered by row-owned GM-027 code/test evidence. Production changes in `lib/features/groups/application/group_config_payload.dart`, `lib/features/groups/application/add_group_member_use_case.dart`, `lib/features/groups/application/group_message_listener.dart`, `lib/features/groups/application/send_group_message_use_case.dart`, and `go-mknoon/node/pubsub.go` reject no-delivery local add targets, normalize/filter invalid inbound member entries before config sync, exclude pre-existing ghosts from durable recipients/retry payloads, and ignore malformed device-less peer IDs in Go dial/expected-target counts. Proof changes added exact GM-027 regressions in add-member, listener, send, membership smoke, and Go pubsub tests; `test/features/groups/application/member_removal_integration_test.dart` was repaired for a valid-member fixture exposed by adjacent validation. RED showed the ghost add/persist/recipient/count failures before the fix. Passed evidence includes focused GM-027 Flutter selectors, `(cd go-mknoon && go test ./node -run 'TestGM027|TestGM023|TestFindMember_DuplicatePeerId')`, adjacent group application/integration suites, targeted analyzer, `./scripts/run_test_gates.sh groups`, `./scripts/run_test_gates.sh completeness-check`, QA reruns of the combined GM-027 Flutter selector and Go selector, and `git diff --check`. Accepted difference: legacy publicKey-only members remain a future hardening topic outside this row because GM-027's invalid no-key/no-device invite target is rejected and does not inflate recipients or reliability counts. GM-028 remains covered separately; GM-029 and later rows remain Open. |
| GM-028 | Member with empty PeerId is rejected before config install | A receives malformed add event. | 1. Update config with empty PeerId. 2. Publish/discover. 3. Run panic/race detectors. | Malformed member cannot break discovery counters or publish preflight. | P0 | Covered | Required | Required | Required | Recommended | N/A | Covered by row-owned GM-028 code/test evidence. Existing Dart guards now have exact GM-028 tests proving whitespace top-level `peerId` is rejected before save/config sync, malformed `member_added` / `members_added` and snapshot members are not persisted or installed, stale blank local members are omitted from removal configs and durable `recipientPeerIds` / retry payloads, and valid Alice/Bob delivery continues. Go product change: `go-mknoon/node/group_inbox.go` trims/drops blank inbox recipient peer IDs before marshaling while preserving valid recipients. Controller race recovery: `go-mknoon/node/node.go` passes the startup `event.Subscription` into `watchConnectionEvents`, avoiding the `Stop` race that cleared `n.eventSub` while the watcher read it. Go proof covers `UpdateGroupConfig`, `findMember`, active dial targets, remote-member counts, expected connected counts, and group-inbox blank-recipient filtering. Controller reruns passed: `flutter test --no-pub test/features/groups/application/add_group_member_use_case_test.dart test/features/groups/application/group_message_listener_test.dart test/features/groups/application/member_removal_integration_test.dart test/features/groups/application/send_group_message_use_case_test.dart test/features/groups/integration/group_membership_smoke_test.dart --plain-name 'GM-028'` (`00:01 +5: All tests passed!`), `(cd go-mknoon && go test ./node -run 'TestGM028|TestGM027|TestGM023|TestFindMember_DuplicatePeerId' -count=1)` (`ok github.com/mknoon/go-mknoon/node 0.619s`), `(cd go-mknoon && go test -race ./node -run 'TestGM028|TestGM027|TestGM023|TestFindMember_DuplicatePeerId' -count=1)` (`ok github.com/mknoon/go-mknoon/node 1.888s`), and `git diff --check`; execution also recorded `./scripts/run_test_gates.sh groups` and `./scripts/run_test_gates.sh completeness-check` (`731/731`) passing. Residual-only: none for GM-028; GM-029 and later rows remain Open. |
| GM-029 | Config version monotonicity is enforced across devices | A/B/C receive membership events in different orders. | 1. Randomize event delivery. 2. Apply versioned events. 3. Compare final configs. | Final active membership, roles, key epoch, and topic state converge across all peers. | P0 | Covered | Required | Required | Required | Required | Required | Covered by accepted GM-029 Dart code/test evidence. Product changes in `lib/features/groups/application/group_config_payload.dart`, `group_message_listener.dart`, `add_group_member_use_case.dart`, `remove_group_member_use_case.dart`, and `update_group_member_role_use_case.dart` parse and compare `configVersion` monotonically, ignore older versioned config snapshots/removes, preserve same-version idempotence, and align add/remove/role-update membership producers' emitted `groupConfig.configVersion` with the persisted membership watermark via an explicit override. Row-owned tests cover producer watermark/version alignment, stale-version listener application, and A/B/C shuffled convergence with final active membership, role/permission, key epoch, topic/subscription, bridge config, stale-after-latest, and exact-once delivery assertions. Controller reruns on 2026-05-11 passed `flutter test --no-pub test/features/groups/application/group_membership_config_version_producers_test.dart test/features/groups/application/group_message_listener_test.dart test/features/groups/integration/group_membership_smoke_test.dart --plain-name 'GM-029'` (`00:01 +6: All tests passed!`), `./scripts/run_test_gates.sh groups` (`00:15 +133: All tests passed!`), and `git diff --check` with no output. Go product was not touched by GM-029, so Go selectors were not required. GM-030 and later rows remain Open. |
| GM-030 | Membership mutation updates group discovery allowed-member filter | D added; C removed. | 1. Run RendezvousDiscover returning B/C/D/X. 2. DiscoverAndConnect. 3. Inspect ignoredNonMembers. | Only current active member transport peers are dialed; removed C and X are ignored. | P0 | Covered | Required | Required | Required | Required | Recommended | Covered by tests-only Go proof in `go-mknoon/node/pubsub_test.go`. `TestGM030MembershipMutationUpdatesDiscoveryAllowedMemberFilter` and `assertGM030DiscoveryEventTotals` prove the old admin/B/C config is replaced by current admin/B/D, rendezvous returns B/C/D/X, only B and D are dialed/connected, C and X remain disconnected/ignored, and discovery event totals report `totalFound == 4`, `newPeers == 2`, and `ignoredNonMembers == 2`. Controller rerun passed `cd go-mknoon && go test ./node -run 'TestGM030|TestRP017RemovedPeerExcludedFromKnownAndDiscoveredDialsAfterConfigUpdate|TestFilterDiscoveredGroupMembers|TestGL005PrivateGroupDiscoveryFiltersNonMembersBeforeDialUse|TestGroupDiscovery_UsesDiscoveredAddressesBeforeRelayFallback|TestGM023|TestGM027|TestGM028' -count=1` with `ok github.com/mknoon/go-mknoon/node 0.754s`; execution also recorded the same selector passing at `0.973s`, QA rerun at `0.761s`, exact GM-030 selector at `0.433s`, clean `gofmt -l go-mknoon/node/pubsub_test.go`, and `git diff --check` with no output. Go race selector was not required because no GM-030 production Go discovery/config code changed. Flutter gates were not required because no Flutter code changed. GM-031 and later rows remain Open. |
| GM-031 | Membership mutation updates known-member dial targets | D added with active transport device; C removed. | 1. Call dialKnownGroupMembers. 2. Capture peer IDs dialed. 3. Compare with active devices. | Dial targets match active devices/current members, not removed users or stale PeerId fields. | P0 | Covered | Required | Required | Required | Required | Recommended | Covered by tests-only Go proof in `go-mknoon/node/pubsub_test.go::TestGM031MembershipMutationUpdatesKnownMemberDialTargets`. The test replaces old admin/B/C config with current admin/B/D, gives B/D active device `TransportPeerId` values that differ from valid addressable stale top-level peer IDs, keeps removed C with prior device/top-level addresses, then proves `dialKnownGroupMembers(groupId, true)` targets/connects only B/D active device transports. Removed C and stale top-level B/C/D peers are not connected or named by `known_member_*` events; `direct_dial.totalMembers == 2`; remote/expected target counts stay at 2. Controller rerun passed `cd go-mknoon && go test ./node -run 'TestGM031|TestGM030|TestGM023|TestGM027|TestGM028|TestRP017RemovedPeerExcludedFromKnownAndDiscoveredDialsAfterConfigUpdate' -count=1` with `ok github.com/mknoon/go-mknoon/node 0.922s`; execution and QA also recorded exact GM-031 selector passes at `0.608s` and `0.507s`, required adjacent selector pass at `0.697s`, clean `gofmt -l go-mknoon/node/pubsub.go go-mknoon/node/pubsub_test.go`, and `git diff --check` with no output. Go race and Flutter gates were not required because GM-031 changed no production Go code and no Flutter product/config code. GM-032 is covered separately; GK-028 is covered separately; GK-031 is covered separately; GK-032 and later rows remain Open. |
| GM-032 | Group dissolve/removal of all members stops delivery | Group is dissolved or membership becomes empty. | 1. Apply dissolve/all-removed event. 2. Try publish/receive/inbox store. 3. Reopen history. | Live send is disabled, topic left, and historical messages remain according to product rules. | P1 | Covered | Required | Required | Required | Recommended | Recommended | Covered on 2026-05-13 by private-chat dissolve and all-members-removed handling. `send_group_message_use_case.dart` now treats an empty private-chat membership list as `groupDissolved` before `group:publish` or `group:inboxStore`, while preserving announcement-channel send semantics. `group_message_listener.dart` now treats authoritative raw `members: []` member-removal snapshots as terminal local closure, marks the group dissolved, records the membership watermark, leaves the native topic, and preserves existing history. Exact tests added/updated: `dissolve_group_use_case_test.dart::GM-032 dissolved group disables publish and inbox while preserving history`, `send_group_message_use_case_test.dart::GM-032 empty active membership disables publish and inbox`, `group_message_listener_test.dart::GM-032 all-members-removed snapshot dissolves, leaves, and preserves history`, and `group_membership_smoke_test.dart::GM-032 offline member converges to dissolved state through replay, cannot send afterwards, and can delete locally without affecting others`. Passed evidence: Dart format, focused GM-032 selectors (`+1` each), full send suite (`+86`), full listener suite (`+107`), combined owner suite (`+253`), and named `./scripts/run_test_gates.sh groups` (`+159`). Residual-only none; GR-002 is the next unresolved session in ordered ledger order. |
| GM-033 | Membership update while inbox replay is running | C is retrieving messages while A removes/re-adds C. | 1. Start cursor replay. 2. Apply remove/re-add. 3. Continue replay. 4. Send live. | Replay applies membership cutoffs and does not install stale removed-window messages. | P0 | Covered | Required | Required | Required | Required | Required | Covered by row-owned code/test/criteria evidence plus exact relay-backed three-party simulator proof. Product/repo changes persist hidden self-removal cutoff markers so replay rejects removed-window messages after re-add without visible system rows, and the real multi-device harness repository now loads removal cutoffs. Passed support evidence includes `flutter test --no-pub test/features/groups/application/drain_group_offline_inbox_use_case_test.dart --plain-name "GM-033"`, criteria support, direct member-removal plus membership-smoke suite, onboarding suite, targeted analyzer, and targeted format checks. The required proof `MKNOON_RELAY_ADDRESSES=... dart run integration_test/scripts/run_group_multi_party_device_real.dart --scenario gm033 -d 560D3E2D-78F8-4D28-A010-16B399581C99,511B36DA-7113-41A7-A718-4450C87C0E62,DE36DBBE-64FC-4652-AAD9-17329A1BA245` exited 0; evidence dir `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_gm033_5Bm9Ho`; orchestrator verdict `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_gm033_5Bm9Ho/gmp_1778541849052_gm033_orchestrator_verdict.json` records `scenario: gm033`, `ok: true`, detail `gm033 verdicts valid for alice, bob, charlie`, and role devices Alice `560D3E2D-78F8-4D28-A010-16B399581C99`, Bob `511B36DA-7113-41A7-A718-4450C87C0E62`, Charlie `DE36DBBE-64FC-4652-AAD9-17329A1BA245`. Alice proves replay started before removal, stale removed-window stored for Charlie, before-removal included Charlie, removed-window recipients excluded Charlie, and post-readd included Charlie. Bob proves receipt before removal, removed-window receipt, Alice post-readd receipt, and Bob post-readd send. Charlie proves replay started/resumed, receives before-removal and Alice/Bob post-readd exactly once, and records `receivedRemovedWindowMessage:false`, `removedWindowPlaintextCount:0`, `removedWindowMessageIdPersisted:false`, `postReaddExactOnce:true`. Residual-only: none for GM-033; earlier external fixture blocker is superseded by this successful rerun. |
| GM-034 | Membership update while direct PubSub receive is running | B receives A messages while config update arrives. | 1. Deliver message and config update in both orders. 2. Decrypt/validate. 3. Inspect timeline. | No valid message is lost due solely to local config update timing; boundary semantics are deterministic. | P0 | Covered | Required | Required | Required | Required | Required | Covered by row-owned GM-034 host, Go, criteria, and exact device proof plus recovered listener-gate evidence. Host fake-network `flutter test test/features/groups/integration/group_membership_smoke_test.dart --plain-name "GM-034"` exited 0. Go direct `(cd go-mknoon && go test ./node -run 'TestGM034|TestGL011UpdateGroupConfigReplacesMembershipDuringActiveSubscription|TestGL011UpdateGroupConfigConcurrentValidatorReadsAreRaceFree' -count=1)` exited 0; Go race `(cd go-mknoon && go test -race ./node -run 'TestGM034|TestGL011UpdateGroupConfigConcurrentValidatorReadsAreRaceFree' -count=1)` exited 0. Criteria `flutter test test/integration/group_multi_party_device_criteria_test.dart --plain-name "GM-034"` exited 0. Exact device proof `MKNOON_RELAY_ADDRESSES=... dart run integration_test/scripts/run_group_multi_party_device_real.dart --scenario gm034 -d 560D3E2D-78F8-4D28-A010-16B399581C99,511B36DA-7113-41A7-A718-4450C87C0E62,DE36DBBE-64FC-4652-AAD9-17329A1BA245` exited 0; evidence dir `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_gm034_xoW6M1`; orchestrator verdict `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_gm034_xoW6M1/gmp_1778534173894_gm034_orchestrator_verdict.json` records `scenario: gm034`, `ok: true`, and all roles valid. Bob proves both `message_then_config` and `config_then_message` exact-once survival with no duplicate IDs; Charlie is removed, Alice/Bob are retained, and `validAliceMessagesSurvived=true`. Recovery/adjacent gates are green: listener suite `105` passed, GM-029 selector passed, member-removal plus membership smoke `68` passed, onboarding `7` passed, groups gate `134` passed, and completeness-check `732/732` classified. Residual-only: none for GM-034; GM-033 and GM-035+ remain separate. |
| GM-035 | Re-added member sends first message before others finish discovery | C has latest key but not all peers have mesh link. | 1. C joins and immediately sends. 2. A/B use pubsub and inbox fallback. 3. Inspect events. | A/B eventually see C's message once, even if live topic peer count is initially zero. | P0 | Covered | Required | Required | Required | Required | Required | Covered by row-owned GM-035 host, criteria, runner, harness, and exact three-party simulator proof. Host `flutter test --no-pub test/features/groups/integration/group_membership_smoke_test.dart --plain-name "GM-035"` passed. Criteria `flutter test test/integration/group_multi_party_device_criteria_test.dart --plain-name "GM-035"` passed in execution and QA. Harness hygiene `dart format --set-exit-if-changed integration_test/group_multi_party_device_real_harness.dart`, `flutter analyze integration_test/group_multi_party_device_real_harness.dart`, and `git diff --check` passed. Exact iOS 26.1 proof `MKNOON_RELAY_ADDRESSES=... dart run integration_test/scripts/run_group_multi_party_device_real.dart --scenario gm035 -d 38FECA55-03C1-4907-BD9D-8E64BF8E3469,347FB118-10D0-40C8-A05B-B0C3BD6B8CCD,5BA69F1C-B112-47BE-B1FF-8C1003728C8F` exited 0; evidence dir `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_gm035_ISnTSP`; orchestrator verdict `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_gm035_ISnTSP/gmp_1778540620083_gm035_orchestrator_verdict.json` records `scenario: gm035`, `ok: true`, and all roles valid. Charlie's first post-readd send is `outcome:"successNoPeers"`, `topicPeers:0`, `initialTopicPeers:0`, `actualDurablePayloadProof:true`, with durable recipients Alice+Bob only. Alice/Bob live-topic-unavailable proofs retain group/key/member state; Alice and Bob each persist Charlie's message once and both keep `postLiveDuplicatePersistedCount:1` after the optional live duplicate. Residual-only: none for GM-035; exact proof is simulator/relay-backed. |
| GM-036 | Admin feedback distinguishes local membership update from invite/key delivery | A batch re-adds C/D with mixed failures. | 1. Force C key delivery success, D failure. 2. Inspect UI/API outcome. 3. Send. | App does not claim all re-added members can receive; failed members are not silently stranded. | P0 | Covered | Required | Required | Recommended | Recommended | Recommended | Covered by tests-only row-owned evidence; no GM-036 production code changed. Added exact tests `GM-036 batch invite reports mixed delivery after local re-add`, `GM-036 mixed invite statuses remain visible after reload`, and `GM-036 send after mixed re-add does not clear failed invite status` in contact-picker, group-info, and membership-smoke coverage. The tests prove C/D local membership succeeds, C invite/key delivery succeeds while D persists as `needsResend` with `lastError: send_failed`, completion/API/UI avoids a clean all-invited claim, Group Info reload keeps D actionable and C not failed, and a later send does not clear D's failed invite status. Passed evidence: `flutter test --no-pub test/features/groups/presentation/contact_picker_wired_test.dart --plain-name "GM-036"`, sequential rerun of `flutter test --no-pub test/features/groups/presentation/group_info_wired_test.dart --plain-name "GM-036"` after initial native-asset/lipo contention, `flutter test --no-pub test/features/groups/integration/group_membership_smoke_test.dart --plain-name "GM-036"`, `flutter test test/features/groups/application/member_removal_integration_test.dart test/features/groups/integration/group_membership_smoke_test.dart`, `flutter test test/features/groups/integration/group_new_member_onboarding_test.dart`, `./scripts/run_test_gates.sh groups`, QA reruns of all three exact GM-036 selectors plus groups gate, and `git diff --check`. Residual-only: none for GM-036; 3-party E2E is Recommended and was not required because the row closed through host/widget/API proof and no real invite/onboarding payload behavior changed. |

## Key Epoch, Encryption, Signature, and Envelope Integrity

| Test ID | Scenario | Preconditions | Steps | Expected Result | Priority | Current status | Unit | Integration | Smoke | Fake Network | 3-Party E2E | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| GK-001 | GenerateGroupKey returns 32-byte base64 AES key | None. | 1. Generate many keys. 2. Decode and inspect length/uniqueness. | Every key decodes to 32 bytes and repeated calls are unique. | P0 | Covered | Required | Recommended | N/A | N/A | N/A | Covered by exact row-owned Go crypto proof. Added `go-mknoon/crypto/group_test.go::TestGK001GenerateGroupKeyReturns32ByteBase64AESKey`, which generates 128 keys, decodes each with `base64.StdEncoding.Strict()`, asserts every decoded key is exactly 32 bytes, and rejects duplicate key strings across the sample. Execution passed `(cd go-mknoon && go test ./crypto -run 'TestGK001\|TestGenerateGroupKey\|TestSP003' -count=1)`, the broader row-relevant sweep `(cd go-mknoon && go test ./node ./internal ./crypto -run 'GroupTopicValidator\|UpdateGroupKey\|GroupEnvelope\|GroupMessage\|DecryptionFailed\|TestGK001\|TestGenerateGroupKey\|TestSP003' -count=1)`, QA reruns of both commands, and `git diff --check`. No production code changed; integration evidence is Recommended and not required for this direct key-generation invariant. |
| GK-002 | Encrypt/decrypt round-trip for text payload | Valid group key. | 1. Encrypt JSON payload. 2. Decrypt. 3. Compare exact bytes/string. | Plaintext matches exactly. | P0 | Covered | Required | Recommended | N/A | N/A | N/A | Covered by exact row-owned Go crypto proof. Added `go-mknoon/crypto/group_test.go::TestGK002EncryptDecryptRoundTripForTextPayload`, which generates a valid group key, encrypts a stable JSON/text payload, verifies non-empty strict-standard-base64 ciphertext and nonce artifacts, verifies the decoded nonce is 12 bytes, decrypts with `DecryptGroupMessage`, and compares exact plaintext bytes with `bytes.Equal`. Execution passed `(cd go-mknoon && go test ./crypto -run 'TestGK002\|TestGroupEncryptDecrypt\|TestGK001' -count=1)`, the broader row-relevant sweep `(cd go-mknoon && go test ./node ./internal ./crypto -run 'GroupTopicValidator\|UpdateGroupKey\|GroupEnvelope\|GroupMessage\|DecryptionFailed\|TestGK002\|TestGroupEncryptDecrypt\|TestGK001' -count=1)`, QA reruns of both commands, and `git diff --check`. No production code changed; nonce/ciphertext uniqueness remains owned by GK-003. |
| GK-003 | Encryption produces unique nonces/ciphertexts for same plaintext | Valid group key. | 1. Encrypt same payload many times. 2. Compare nonce/ciphertext. | Nonces are 12 bytes and no duplicate nonce/ciphertext occurs in sample. | P0 | Covered | Required | Recommended | N/A | N/A | N/A | Covered by exact row-owned Go crypto proof. Added `go-mknoon/crypto/group_test.go::TestGK003EncryptionProducesUniqueNoncesAndCiphertextsForSamePlaintext`, which uses one generated group key, one fixed plaintext, and 128 same-plaintext encryptions; strict-decodes ciphertext and nonce with standard base64; rejects empty ciphertext/nonce artifacts and empty decoded ciphertext; asserts every decoded nonce is exactly 12 bytes; and rejects duplicate nonce and ciphertext strings. Execution passed the focused command `(cd go-mknoon && go test ./crypto -run 'TestGK003EncryptionProducesUniqueNoncesAndCiphertextsForSamePlaintext\|TestSP003GroupKeysAndNoncesUseFreshRandomness\|TestGroupEncryptDecrypt_UniqueNonces\|TestGK002EncryptDecryptRoundTripForTextPayload\|TestGK001GenerateGroupKeyReturns32ByteBase64AESKey' -count=1)`, the broader row-relevant sweep `(cd go-mknoon && go test ./node ./internal ./crypto -run 'GroupTopicValidator\|UpdateGroupKey\|GroupEnvelope\|GroupMessage\|DecryptionFailed\|TestGK003\|TestGK002\|TestGK001\|TestSP003\|TestGroupEncryptDecrypt' -count=1)`, QA reruns of both commands, and `git diff --check`. No production code changed; `go-mknoon/crypto/group.go` has no diff. Integration evidence is Recommended, not Required. |
| GK-004 | Invalid key base64 is rejected | Malformed key string. | 1. Encrypt/decrypt with malformed key. 2. Inspect errors. | Clear decode error and no panic. | P0 | Covered | Required | Recommended | N/A | N/A | N/A | Covered by exact row-owned Go crypto proof in `go-mknoon/crypto/group_test.go::TestGK004InvalidKeyBase64IsRejected`. The test uses a malformed key, proves encrypt returns a non-nil error containing `decode group key`, leaves ciphertext and nonce empty, then decrypts with the same malformed key plus valid-looking base64 ciphertext/nonce placeholders and proves a non-nil `decode group key` error with empty plaintext; normal test completion is the no-panic proof. Executor focused command passed: `(cd go-mknoon && go test ./crypto -run '^(TestGK004InvalidKeyBase64IsRejected\|TestEncryptGroupMessage_InvalidKey\|TestDecryptGroupMessage_InvalidBase64\|TestGK001GenerateGroupKeyReturns32ByteBase64AESKey\|TestGK002EncryptDecryptRoundTripForTextPayload\|TestGK003EncryptionProducesUniqueNoncesAndCiphertextsForSamePlaintext)$' -count=1)`. Executor broader command passed: `(cd go-mknoon && go test ./node ./internal ./crypto -run 'GroupTopicValidator\|UpdateGroupKey\|GroupEnvelope\|GroupMessage\|DecryptionFailed\|TestGK004\|TestGK003\|TestGK002\|TestGK001\|InvalidBase64\|InvalidKey' -count=1)`. QA reran both commands successfully and `git diff --check` passed. No production code changed; `go-mknoon/crypto/group.go` has no diff. Integration evidence is Recommended, not Required. |
| GK-005 | Wrong key length is rejected | Base64 decodes to not 32 bytes. | 1. Encrypt/decrypt. 2. Inspect errors. | Clear key length error. | P0 | Covered | Required | Recommended | N/A | N/A | N/A | Covered by exact row-owned Go crypto proof in `go-mknoon/crypto/group_test.go::TestGK005WrongKeyLengthIsRejected`. The test uses base64 key `AAAAAAAAAAAAAAAAAAAAAA==`, which decodes to 16 bytes, and proves both `EncryptGroupMessage` and `DecryptGroupMessage` reject with `invalid group key length` plus `got 16, want 32`. Encrypt returns empty ciphertext and nonce; decrypt returns empty plaintext. Decrypt uses valid-looking base64 ciphertext and 12-byte nonce placeholders so the wrong decoded key length is the rejecting condition, and normal test completion proves no panic. Executor focused command passed: `(cd go-mknoon && go test ./crypto -run '^(TestGK005WrongKeyLengthIsRejected\|TestEncryptGroupMessage_WrongKeyLength\|TestGK004InvalidKeyBase64IsRejected\|TestDecryptGroupMessage_InvalidBase64\|TestGroupEncryptDecrypt_WrongKey)$' -count=1)`. Executor broader command passed: `(cd go-mknoon && go test ./node ./internal ./crypto -run 'GroupTopicValidator\|UpdateGroupKey\|GroupEnvelope\|GroupMessage\|DecryptionFailed\|TestGK005\|TestGK004\|TestGK003\|TestGK002\|TestGK001\|TestGenerateGroupKey\|TestGroupEncryptDecrypt\|WrongKey\|WrongKeyLength\|InvalidBase64\|InvalidKey' -count=1)`. QA reran both selectors successfully and `git diff --check` passed. No production code changed; `go-mknoon/crypto/group.go` has no diff and existing guards reject non-32-byte decoded keys. Integration evidence is Recommended, not Required. |
| GK-006 | Ciphertext tamper fails decrypt and emits no message | Valid envelope then one ciphertext byte changed. | 1. Deliver tampered envelope. 2. Validator/decrypt. 3. Inspect events. | Signature or AES-GCM fails; no group_message:received event. | P0 | Covered | Required | Required | N/A | Recommended | N/A | Covered by `go-mknoon/node/pubsub_decryption_failure_test.go::TestGK006TamperedCiphertextAfterSigningRejectedByValidatorAndEmitsNoMessage`: builds a valid signed group envelope, mutates exactly one decoded ciphertext byte via `mutateGroupEnvelope` without re-signing, rejects pure validation as `reject:bad_signature`, unregisters only node A's local validator, observes node B `group:validation_rejected` reason `bad_signature_or_epoch`, and asserts no `group_message:received`, no `group_reaction:received`, and no `group:decryption_failed` after baseline. Executor and QA focused/race/broader Go sweeps plus `git diff --check` passed; no production code changed. GK-007 nonce/AES-GCM, GK-008 wrong-public-key signature, GK-009 signature-data binding, and GK-010 missing-`groupId` parser branches are covered separately; GK-011 remains the next unresolved P0 row. |
| GK-007 | Nonce tamper fails decrypt and emits no message | Valid envelope then nonce byte changed. | 1. Deliver tampered envelope. 2. Decrypt. 3. Inspect events. | AES-GCM fails and emits group:decryption_failed, not plaintext. | P0 | Covered | Required | Required | N/A | Recommended | N/A | Covered by `go-mknoon/node/pubsub_decryption_failure_test.go::TestGK007NonceByteTamperFailsDecryptAndEmitsNoMessage`: flips one decoded 12-byte nonce byte in a valid signed group envelope, keeps valid base64, preserves ciphertext and signature, does not re-sign, proves `validateGroupEnvelope(...) == "accept"`, publishes through the real two-node PubSub path, observes `group:decryption_failed` with `aes-gcm decrypt`, and asserts no post-baseline `group_message:received`, `group_reaction:received`, or plaintext marker. Executor focused selector passed (`ok github.com/mknoon/go-mknoon/node 5.832s`), race selector passed (`ok github.com/mknoon/go-mknoon/node 3.620s`), broader Go sweep passed (`ok node 213.353s`, `ok internal 0.267s`, `ok crypto 0.606s`), and `git diff --check` passed. Independent QA reran the focused selector (`ok node 5.839s`), race selector (`ok node 3.652s`), broader Go sweep (`ok node 210.756s`, `ok internal 0.640s`, `ok crypto 0.384s`), and `git diff --check`; all passed. No production code changed; GK-008, GK-009, and GK-010 are covered separately, and GK-011 remains the next unresolved P0 row. |
| GK-008 | Signature rejects wrong public key | Envelope signed by X but claims B. | 1. Build envelope with X signature. 2. Validate under B member public key. | Validator rejects bad_signature_or_epoch. | P0 | Covered | Required | Required | N/A | Recommended | N/A | Covered by `go-mknoon/node/pubsub_decryption_failure_test.go::TestGK008SignatureRejectsWrongPublicKeyAndEmitsNoMessage`: builds an envelope for member B/node A with member B's configured public key while signing with attacker key X, proves signature verification succeeds with X and fails with B for the same `BuildGroupSignatureData` payload, proves pure `validateGroupEnvelope(...) == "reject:bad_signature"`, unregisters only node A local validator for raw fanout, publishes through the real two-node PubSub path, observes node B `group:validation_rejected` reason `bad_signature_or_epoch` at epoch 8, and asserts no post-baseline `group_message:received`, `group_reaction:received`, `group:decryption_failed`, or plaintext marker. Executor focused selector passed (`ok github.com/mknoon/go-mknoon/node 5.173s`), race selector passed (`ok github.com/mknoon/go-mknoon/node 4.138s`), broader Go sweep passed (`ok node 214.614s`, `ok internal 0.343s`, `ok crypto 0.701s`), and `git diff --check` passed. Independent QA reran the focused selector (`ok node 5.241s`), race selector (`ok node 4.172s`), broader Go sweep (`ok node 215.286s`, `ok internal 1.142s`, `ok crypto 0.724s`), and `git diff --check`; all passed. No production code changed; residual-only none; GK-009 and GK-010 are covered separately, and GK-011 remains the next unresolved P0 row. |
| GK-009 | Signature data is deterministic and epoch-bound | Known groupId, epoch, ciphertext. | 1. Build signature data twice. 2. Change epoch/group/ciphertext. 3. Verify signatures. | Same inputs produce same data; any changed signed field invalidates signature. | P0 | Covered | Required | Recommended | N/A | N/A | N/A | Covered by `go-mknoon/crypto/group_test.go::TestGK009SignatureDataIsDeterministicAndEpochBound`: builds identical `BuildGroupSignatureData(groupID, epoch, ciphertext)` twice, asserts exact stable data `group-gk-009\|7\|Y2lwaGVydGV4dC1nazAwOQ==`, signs the base data once, verifies the base data, and proves changed epoch, changed group id, and changed ciphertext each produce different data and fail `VerifyPayload` with the original signature/public key. Executor passed focused crypto selector (`ok github.com/mknoon/go-mknoon/crypto 0.390s`), broader Go sweep (`ok node 213.685s`, `ok internal 0.694s`, `ok crypto 0.928s`), and `git diff --check`; independent QA passed focused crypto selector (`ok github.com/mknoon/go-mknoon/crypto 0.428s`), broader Go sweep (`ok node 214.969s`, `ok internal 0.447s`, `ok crypto 0.258s`), and `git diff --check`. Production code unchanged; scoped crypto diff is tests-only in `go-mknoon/crypto/group_test.go`; residual-only none. GK-010 is covered separately; GK-011 remains the next unresolved P0 row. |
| GK-010 | Envelope parser rejects missing groupId | JSON has version/type but no groupId. | 1. Parse and validate. 2. Inspect error. | ParseGroupEnvelope returns missing groupId error. | P0 | Covered | Required | Recommended | N/A | N/A | N/A | Covered by `go-mknoon/internal/group_envelope_test.go::TestGK010ParseGroupEnvelopeRejectsMissingGroupID`: builds v3-looking JSON with `version` and `type` but no `groupId`, asserts `ParseGroupEnvelope` returns `env == nil`, asserts a non-nil parser error, and requires error text containing both `parse group envelope` and `missing groupId`. Executor passed focused internal selector (`ok internal 0.404s`), broader Go parser/validator sweep (`ok node 9.606s`, `ok internal 0.441s`, `ok crypto 0.308s`), and `git diff --check`. Independent QA passed focused internal selector (`ok internal 0.302s`), broader Go sweep (`ok node 9.055s`, `ok internal 0.171s`, `ok crypto 0.634s`), and `git diff --check`. Fresh audit reruns passed focused internal selector (`ok internal 0.505s`), broader Go sweep (`ok node 9.051s`, `ok internal 0.573s`, `ok crypto 0.847s`), and `git diff --check`. `go-mknoon/internal/group_envelope.go` has no production diff. Residual-only: none; GK-011 remains the next unresolved P0 row. |
| GK-011 | Envelope parser/validator rejects missing senderId | Envelope has no senderId. | 1. Deliver envelope. 2. Validator runs. 3. Inspect reason. | Rejected as non_member or invalid_envelope; no panic. | P0 | Covered | Required | Required | N/A | Recommended | N/A | Covered by GK-011 node validator proof. Regression-first focused run first failed for the expected current gap: omitted `senderId` rejected as `peer_mismatch` instead of `invalid_envelope`. Production guard in `go-mknoon/node/pubsub.go` now rejects omitted, blank, or whitespace-only `SenderId` as `invalid_envelope` immediately after `ParseGroupEnvelope`; `validateGroupEnvelopeForTransportPeer` mirrors the guard. Exact tests in `go-mknoon/node/pubsub_test.go` are `TestGK011ValidateGroupEnvelopeRejectsMissingSenderIDAsInvalidEnvelope` and `TestGK011GroupTopicValidatorRejectsMissingSenderIDAsInvalidEnvelopeNoPanic`; they delete the JSON `senderId` key through structured JSON mutation, use a non-empty transport peer, exercise the real `groupTopicValidator`, include a panic guard, and assert `group:validation_rejected` reason `invalid_envelope`. Executor passed focused `ok node 0.517s`, adjacent `ok node 0.344s` / `ok internal 0.580s`, broader `ok node 4.347s` / `ok internal 0.756s` / `ok crypto 0.623s`, race `ok node 1.523s`, and `git diff --check`. QA passed focused `ok node 0.427s`, adjacent `ok node 0.334s` / `ok internal 0.470s`, broader `ok node 4.455s` / `ok internal 0.176s` / `ok crypto 0.249s`, race `ok node 1.429s`, and `git diff --check`. Fresh audit reruns passed focused `ok node 1.228s`, adjacent `ok node 1.625s` / `ok internal 1.322s`, broader `ok node 4.432s` / `ok internal 0.979s` / `ok crypto 0.732s`, race `ok node 3.037s`, and `git diff --check`. Accepted difference: row closes on `invalid_envelope` rather than `non_member`; residual-only none. GK-012 remains the next unresolved P0 row. |
| GK-012 | Envelope parser/validator rejects missing signature | Envelope has required structural fields but no signature. | 1. Deliver envelope. 2. Validator runs. | Rejected as bad_signature_or_epoch; no decrypt/event. | P0 | Covered | Required | Required | N/A | Recommended | N/A | Covered by GK-012 tests-only Go node proof; no production code changed. `go-mknoon/node/pubsub_test.go::TestGK012ValidateGroupEnvelopeRejectsMissingSignatureAsBadSignature` deletes the top-level JSON `signature` key from an otherwise valid v3 envelope, proves the envelope remains parseable with `Signature == ""`, and asserts pure `validateGroupEnvelope(...) == "reject:bad_signature"`. `go-mknoon/node/pubsub_decryption_failure_test.go::TestGK012MissingSignatureRejectedByValidatorAndEmitsNoMessage` publishes the missing-signature envelope through a two-node PubSub validator path, observes `group:validation_rejected` reason `bad_signature_or_epoch`, and asserts no `group_message:received`, no `group_reaction:received`, and no `group:decryption_failed` after baseline. Executor and QA passed focused `ok node 2.604s`, adjacent `ok node 7.167s`, broader `ok node 10.293s` / `ok internal 0.290s` / `ok crypto 0.885s`, race `ok node 3.673s`, and `git diff --check`. Accepted difference: missing signature closes through existing `VerifyPayload` / bad-signature validation rather than parser-level structural validation. Residual-only none; GK-013 is covered separately; GK-014 remains the next unresolved P0 row. |
| GK-013 | Envelope parser/validator rejects missing encrypted fields | Envelope lacks ciphertext or nonce. | 1. Deliver envelope. 2. Validator/decrypt. 3. Inspect events. | Rejected before or during decrypt with diagnostic; no payload event. | P0 | Covered | Required | Required | N/A | Recommended | N/A | Covered by code change plus row-owned Go crypto/node proof. `go-mknoon/node/pubsub.go` now rejects missing or blank `encrypted.ciphertext` or `encrypted.nonce` as `invalid_envelope` before group mismatch, signature verification, or decrypt; the pure validator helper in `go-mknoon/node/pubsub_test.go` mirrors the same encrypted-field helper. `go-mknoon/crypto/group.go::DecryptGroupMessage` now returns diagnostic errors for missing ciphertext, missing nonce, empty decoded ciphertext, and wrong decoded nonce length instead of panicking. Tests added: `go-mknoon/crypto/group_test.go::TestGK013DecryptGroupMessageRejectsMissingCiphertextOrNonceWithoutPanic`, `go-mknoon/node/pubsub_test.go::TestGK013ValidateGroupEnvelopeRejectsMissingEncryptedFieldsAsInvalidEnvelope`, and `go-mknoon/node/pubsub_decryption_failure_test.go::TestGK013MissingEncryptedFieldsRejectedByValidatorAndEmitsNoPayloadEvent`. Regression-first failures recorded the prior gaps: crypto missing ciphertext returned `aes-gcm decrypt`, crypto missing/short nonce panicked, node missing ciphertext returned `bad_signature`, and node missing nonce returned `accept`. QA passed focused `ok crypto 0.424s`, focused `ok node 5.701s`, crypto regression `ok crypto 0.203s`, adjacent node `ok node 11.955s`, broader `ok node 16.370s` / `ok internal 0.368s` / `ok crypto 0.545s`, race `ok node 13.338s`, and `git diff --check`. Accepted differences: missing encrypted fields close as `invalid_envelope` before signature/decrypt, real-network proof is Recommended-only, and Flutter/Dart replay was not required because landed scope stayed in Go validator/decrypt paths. Residual-only none; GK-014 remains the next unresolved P0 row. |
| GK-014 | Only version 3 group_message/group_reaction is accepted | Various version/type combinations. | 1. Call IsGroupEnvelope. 2. Run validator. 3. Inspect rejection reason. | Only v3 group_message and group_reaction are accepted. | P0 | Covered | Required | Recommended | N/A | N/A | N/A | Covered by GK-014 tests-only Go internal/node proof; no production, Dart, Flutter, crypto, wire-format, membership, authorization, or transport changes were required. Exact tests added: `go-mknoon/internal/group_envelope_test.go::TestGK014IsGroupEnvelopeAcceptsOnlyV3GroupMessageAndReaction`, `go-mknoon/node/pubsub_test.go::TestGK014ValidateGroupEnvelopeAcceptsOnlyV3GroupMessageAndReaction`, and `go-mknoon/node/pubsub_test.go::TestGK014GroupTopicValidatorRejectsUnsupportedVersionsAndTypesAsNotV3Envelope`. Behavior proven: only v3 `group_message` and v3 `group_reaction` pass `IsGroupEnvelope` and the first validator gate; unsupported versions/types, missing version/type, numeric version, and malformed JSON reject as `reject:not_v3` in the pure helper or `not_v3_envelope` in the live validator before parse/config/key/signature/decrypt paths. Controller verification passed focused internal `ok internal 0.171s`, focused node `ok node 0.362s`, adjacent internal `ok internal 0.234s`, adjacent node `ok node 7.688s`, broader `ok node 10.801s` / `ok internal 0.258s` / `ok crypto 0.727s`, and `git diff --check`; fresh QA reruns passed focused internal `ok internal 0.384s`, focused node `ok node 0.455s`, adjacent internal `ok internal 0.178s`, adjacent node `ok node 7.680s`, broader `ok node 11.047s` / `ok internal 0.183s` / `ok crypto 0.477s`, and `git diff --check`; Completion Auditor reruns passed focused internal `ok internal 0.366s`, focused node `ok node 0.490s`, and `git diff --check`. Accepted differences: direct pure validator and direct `groupTopicValidator` host proof are sufficient, full libp2p fanout/real network remains Recommended-only, and race/Flutter commands were not required because GK-014 did not change production validator or Dart replay code. Residual-only none; GK-015 is the next unresolved P0 row. |
| GK-015 | Envelope groupId mismatch is rejected | Message is delivered on topic G but envelope says H. | 1. Publish/mutate envelope. 2. Validator runs. | Rejected as group_mismatch. | P0 | Covered | Required | Required | N/A | Recommended | N/A | Covered by tests-only Go node proof; no GK-015 production, Dart, Flutter, crypto, wire-format, membership, authorization, or transport behavior changes were required. Existing `go-mknoon/node/pubsub.go` rejects `env.GroupId != groupId` as `group_mismatch` before transport peer, config, key, signature, or decrypt paths. Exact tests added: `go-mknoon/node/pubsub_test.go::TestGK015ValidateGroupEnvelopeRejectsGroupIDMismatchBeforeTransportPeerAndSignature`, `go-mknoon/node/pubsub_test.go::TestGK015GroupTopicValidatorRejectsGroupIDMismatchAndEmitsReason`, and `go-mknoon/node/pubsub_decryption_failure_test.go::TestGK015GroupIDMismatchRejectedByValidatorAndEmitsNoPayloadEvent`. These prove pure `reject:group_mismatch` for topic G/envelope H before downstream validator branches, live `ValidationReject` plus `group:validation_rejected` reason `group_mismatch` with envelope metadata, and raw-publish/no-payload behavior with no `group_message:received`, no `group_reaction:received`, no `group:decryption_failed`, and no plaintext marker after rejection. Executor evidence passed focused GK-015, adjacent GK-011..GK-015, broader node/internal/crypto selectors, and `git diff --check`; Completion Auditor reran the focused GK-015 selector (`ok github.com/mknoon/go-mknoon/node 3.124s`) and `git diff --check`. Accepted differences: full real-network proof remains Recommended-only, and race/Flutter/groups gates were not required because no production validator, Dart replay, or Flutter group code changed. Residual-only none; GK-016 remains the next unresolved P0 row. |
| GK-016 | First key rotation preserves epoch 0 during grace | Current epoch is 0 and update to 1 occurs. | 1. Set current key E0. 2. Update to E1. 3. Verify/decrypt a valid E0 envelope before grace deadline. | E0 message is accepted during grace if product allows in-flight messages. | P0 | Covered | Required | Required | Required | Recommended | Required | Covered by GK-016 code change plus row-owned Go node proof. `go-mknoon/node/pubsub.go::hasKeyRotationGrace` no longer excludes `PrevKeyEpoch == 0`; it still requires non-empty `PrevKey`, non-zero `GraceDeadline`, and a live deadline, while signature/decrypt still require `env.KeyEpoch == keyInfo.PrevKeyEpoch`, so initial join does not gain grace. Exact tests added in `go-mknoon/node/pubsub_key_rotation_grace_test.go`: `TestGK016GroupTopicValidatorAcceptsEpoch0PreviousKeyDuringFirstRotationGrace` proves epoch-0 envelope acceptance during 0->1 grace and negative controls for missing previous key, zero deadline, and expired deadline; `TestGK016HandleGroupSubscriptionDecryptsEpoch0PreviousKeyDuringFirstRotationGrace` proves live raw-publish delivery/decrypt of epoch-0 message with keyEpoch 0 and no decryption failure. Adjacent harness cleanup installs collectors before join in existing live grace/expiry tests to remove event callback race without weakening assertions. Pre-fix RED focused selector failed with `reject:bad_signature` and `bad_signature_or_epoch`; final focused, adjacent, broader node/internal/crypto, race, QA/audit reruns, and `git diff --check` passed. Flutter/offline replay not required because no Dart/Flutter touched and evidence stayed in Go validator/decrypt behavior. Residual-only none; GK-017 remains the next unresolved P0 row. |
| GK-017 | Previous epoch is rejected after grace deadline | Current E2 with Prev E1 and grace expired. | 1. Deliver valid E1 envelope after deadline. 2. Validator/decrypt. | Rejected as bad_signature_or_epoch or no key available. | P0 | Covered | Required | Required | N/A | Recommended | N/A | Covered by tests-only row-owned Go node proof in `go-mknoon/node/pubsub_key_rotation_grace_test.go` with no GK-017 production code change. `TestGK017GroupTopicValidatorRejectsPreviousEpochAfterGraceDeadline` proves a valid E1 envelope is accepted during live grace but rejects as pure `reject:bad_signature` after the E2/Prev E1 grace deadline, and also rejects without previous key material or without a grace deadline. `TestGK017DecryptGroupEnvelopePayloadRejectsPreviousEpochAfterGraceDeadline` proves direct decrypt succeeds during live grace but returns `no group key available for epoch 1` after the expired deadline. `TestGK017GroupTopicValidatorEmitsBadSignatureOrEpochAfterGraceDeadline` proves the live raw-publish path emits `group:validation_rejected` reason `bad_signature_or_epoch` with `keyEpoch == 1` and emits no `group_message:received`, no `group_reaction:received`, and no `group:decryption_failed`. Executor passed focused, adjacent GK-016/GK-017 grace/expiry, broader node/internal/crypto selectors, and `git diff --check`; fresh Completion Auditor rerun passed `(cd go-mknoon && go test ./node -run 'TestGK017' -count=1)` with `ok github.com/mknoon/go-mknoon/node 2.667s`, and `git diff --check` passed with no output. Race, Flutter/offline, and real-device/relay gates were not required because GK-017 landed tests-only in Go node validator/decrypt coverage and device/relay proof is Recommended-only. Residual-only none; GK-018 remains the next unresolved P0 row. |
| GK-018 | Current epoch remains accepted after grace deadline | Current E2 with Prev E1 expired. | 1. Deliver valid E2 envelope. 2. Validator/decrypt. | Accepted and received. | P0 | Covered | Required | Required | N/A | Recommended | N/A | Covered by tests-only row-owned Go node proof in `go-mknoon/node/pubsub_key_rotation_grace_test.go` with no GK-018 production code change. `TestGK018GroupTopicValidatorAcceptsCurrentEpochAfterGraceDeadline` proves a valid E2 envelope signed/encrypted with the current E2 key validates as `accept` after the E2/Prev E1 grace deadline has expired. `TestGK018DecryptGroupEnvelopePayloadAcceptsCurrentEpochAfterGraceDeadline` proves direct decrypt after the expired previous-key grace window returns current E2 plaintext. `TestGK018HandleGroupSubscriptionReceivesCurrentEpochAfterGraceDeadline` proves live raw-publish delivery emits `group_message:received` with `keyEpoch == 2` and no `group:validation_rejected` or `group:decryption_failed` after baseline. Executor passed focused GK-018 (`ok node 2.142s`), adjacent GK-016/GK-017/GK-018 grace/expiry (`ok node 6.129s`), broader node/internal/crypto (`ok node 11.175s`, `ok internal 0.894s`, `ok crypto 0.690s`), and `git diff --check`; QA reran focused (`ok node 2.025s`), adjacent (`ok node 6.087s`), broader (`ok node 11.088s`, `ok internal 0.577s`, `ok crypto 0.894s`), and `git diff --check`; Completion Auditor reran focused GK-018 (`ok github.com/mknoon/go-mknoon/node 2.077s`) and `git diff --check`. Race was not required because GK-018 made no production Go concurrency change; Flutter/offline and device/relay gates remain Recommended-only because no Dart/Flutter/transport files changed. Residual-only none; GK-019 remains the next unresolved P0 row. |
| GK-019 | Jump from epoch 0 to 2 preserves only allowed prior epoch | Current E0 updates to E2. | 1. Update key to E2. 2. Deliver E0/E1/E2 envelopes. 3. Inspect acceptance. | Only epochs allowed by the app's rotation contract are accepted; unexpected gaps are explicit. | P0 | Covered | Required | Required | N/A | Recommended | N/A | Covered by tests-only row-owned Go node proof in `go-mknoon/node/pubsub_key_rotation_grace_test.go`; no GK-019 production code changed. `TestGK019UpdateGroupKeyJumpFromEpoch0To2PreservesOnlyEpoch0AsPrevious` proves direct E0->E2 `UpdateGroupKey` stores current E2, previous E0, and a live grace deadline, without storing/synthesizing E1. `TestGK019GroupTopicValidatorAcceptsOnlyEpoch0GraceAndCurrentEpoch2ForDirectJump` proves pure validation accepts E0 only during live grace, rejects expired E0 and skipped E1 as `reject:bad_signature`, and accepts current E2. `TestGK019DecryptGroupEnvelopePayloadAcceptsOnlyEpoch0GraceAndCurrentEpoch2ForDirectJump` proves direct decrypt succeeds for live E0 and current E2, rejects skipped E1, and rejects E0 after grace expiry. `TestGK019HandleGroupSubscriptionDirectJumpReceivesAllowedEpochsOnly` proves live raw-publish receives E0 and E2 while rejected E1 emits `bad_signature_or_epoch` with no message/reaction/decrypt-failure side effects. Executor passed focused GK-019 `ok github.com/mknoon/go-mknoon/node 3.724s`, adjacent grace selector `ok node 9.374s`, broader Go selector `ok node 14.561s` / `ok internal 0.283s` / `ok crypto 0.894s`, and `git diff --check`; Completion Auditor reran focused GK-019 `ok github.com/mknoon/go-mknoon/node 3.771s` and `git diff --check`. Race, Flutter/offline, and device/relay gates were not required because GK-019 landed tests-only without production Go concurrency, Dart/Flutter, or transport changes. Residual-only none; GK-020 remains the next unresolved P0 row. |
| GK-020 | Multiple rotations keep exactly one previous key or documented window | E0->E1->E2 quickly. | 1. Rotate twice. 2. Deliver E0, E1, E2. 3. Inspect results. | Acceptance matches documented replay/grace policy; no accidental decrypt of too-old epochs. | P0 | Covered | Required | Required | N/A | Recommended | N/A | Covered by tests-only row-owned Go node proof in `go-mknoon/node/pubsub_key_rotation_grace_test.go`; no GK-020 production code changed. `TestGK020UpdateGroupKeySequentialRotationsKeepsOnlyEpoch1AsPrevious` proves actual sequential E0->E1->E2 `UpdateGroupKey` state stores current E2 plus previous E1 with a live second-rotation grace deadline and no retained E0 previous key. `TestGK020GroupTopicValidatorAcceptsOnlyEpoch1GraceAndCurrentEpoch2AfterSequentialRotations` proves pure validation rejects too-old E0, accepts E1 only during live grace, rejects E1 after grace expiry, and accepts current E2 both during and after grace. `TestGK020DecryptGroupEnvelopePayloadAcceptsOnlyEpoch1GraceAndCurrentEpoch2AfterSequentialRotations` proves the same epoch matrix through direct decrypt, including no plaintext for E0 and no previous-key decrypt after expiry. `TestGK020HandleGroupSubscriptionSequentialRotationsReceivesAllowedEpochsOnly` proves live raw-publish rejects E0 as `bad_signature_or_epoch` with no message/reaction/decrypt-failure side effects, then receives allowed E1 and E2 payloads. Executor passed focused GK-020 `ok github.com/mknoon/go-mknoon/node 4.731s`, adjacent GK-016..GK-020 grace selector `ok github.com/mknoon/go-mknoon/node 13.438s`, broader Go selector `ok node 17.121s` / `ok internal 0.725s` / `ok crypto 0.446s`, and `git diff --check`; Completion Auditor reran focused GK-020 `ok github.com/mknoon/go-mknoon/node 4.735s` and `git diff --check`. Race, Flutter/offline, and device/relay gates were not required because GK-020 landed tests-only without production Go concurrency, Dart/Flutter, or transport changes. Residual-only none; GK-021 remains the next unresolved P0 row. |
| GK-021 | Re-add key epoch differs from removal epoch | C removed at E1 and re-added at E2. | 1. Give C old E1 and then new E2. 2. Send with both. 3. Validate on A/B. | Old E1 post-removal messages rejected; E2 after re-add accepted. | P0 | Covered | Required | Required | Required | Recommended | Required | Covered by tests-only row-owned Go node proof in `go-mknoon/node/pubsub_key_rotation_grace_test.go`; no GK-021 production code changed. `TestGK021GroupTopicValidatorRejectsRemovalEpochPackageAndAcceptsReaddEpoch` proves a post-re-add config containing only C's fresh E2 device/key package rejects stale E1 removed-package traffic as `reject:unbound_device` even while E1 is still configured as a live previous epoch, rejects stale E1 traffic that uses the current re-add device/transport/signing key but the removed E1 key package, and accepts fresh E2 traffic. `TestGK021HandleGroupSubscriptionRejectsRemovalEpochPackageAndReceivesReaddEpoch` proves the live two-node raw-publish path rejects C's stale E1 removed package on the current re-add transport/signing key with `group:validation_rejected` reason `unbound_device` and `keyEpoch == 1`, emits no message/reaction/decrypt-failure side effects, then receives C's fresh E2 message with `keyEpoch == 2`. Controller verification passed focused GK-021 `ok github.com/mknoon/go-mknoon/node 3.758s`, adjacent grace/key-package selector `ok node 16.728s`, broader node/internal/crypto selector `ok node 19.283s` / `ok internal 0.175s` / `ok crypto 0.211s`, and `git diff --check`; Completion Auditor reran focused GK-021 `ok github.com/mknoon/go-mknoon/node 3.717s` and `git diff --check`. Accepted differences: direct Go validator/live raw-publish host proof is sufficient for this row; Flutter/offline, device/relay, and removed-window inbox backlog proof remain separate GK-022/GK-023 work because no Dart/Flutter/transport code changed. Residual-only none; GK-022 remains the next unresolved P0 row. |
| GK-022 | Removed member cannot decrypt inbox messages encrypted after removal | C holds old key. | 1. A/B rotate key and store encrypted messages. 2. C retrieves relay messages. 3. Attempt decrypt. | C cannot decrypt post-removal envelopes and app does not render plaintext. | P0 | Covered | Required | Required | Required | Required | Required | Covered by tests-only row-owned Flutter offline replay proof in `test/features/groups/application/drain_group_offline_inbox_use_case_test.dart`; no GK-022 production code changed. `GK-022 removed member with old key cannot decrypt post-removal inbox replay` proves a stale removed client with only E1 stored locally and no E2 key can retrieve an E2 post-removal signed replay envelope without decrypting or rendering the protected plaintext: `payload.verify` runs, `group.decrypt` does not run, the E2 key remains absent, the protected text is not present in visible messages, and the only persisted row is an `undecryptable` placeholder at `keyGeneration == 2` with `groupUndecryptablePlaceholderText`. Focused GK-022 passed `00:00 +1: All tests passed!`; adjacent GK-022/future-epoch/MD-011 replay selector passed `00:00 +3: All tests passed!`; replay signature support passed `00:00 +2: All tests passed!`; Go inbox request support passed `ok github.com/mknoon/go-mknoon/node 0.518s`; `./scripts/run_test_gates.sh groups` passed `00:17 +136: All tests passed!`; `dart format --set-exit-if-changed` and `git diff --check` passed. Accepted differences: direct host replay/decrypt proof is sufficient for this row; real relay/device and relay ACL proof are supplemental because no relay/transport code changed, re-added-member backlog is covered by GK-023, and late-join entitlement remains GK-024. Residual-only none; GK-024 remains the next unresolved P0 row. |
| GK-023 | Re-added member cannot decrypt removed-window backlog | C is re-added with new key. | 1. C retrieves all relay records. 2. Apply membership windows. 3. Attempt decrypt removed-window records. | Removed-window records stay hidden/skipped; post-readd records render. | P0 | Covered | Required | Required | Required | Required | Required | Covered by tests-only row-owned Flutter offline replay proof in `test/features/groups/application/drain_group_offline_inbox_use_case_test.dart`; no GK-023 production code changed. `GK-023 re-added member skips removed-window replay and renders post-readd replay` proves Charlie drains an allowed pre-removal page, persists the cursor, receives a trusted self-removal cutoff, is re-added at a later membership timestamp with E2, then retrieves all relay records including duplicate E1 removed-window backlog plus an E2 post-readd record. Removed-window records are not persisted or rendered by id or plaintext, no pending key repair is queued for the removed-window message, the cursor completes, the self-removed-window-after-rejoin flow event is emitted, and the post-readd E2 message renders exactly once. Focused GK-023 passed `00:00 +1: All tests passed!`; adjacent GK-023/GM-033 replay selector passed `00:00 +2: All tests passed!`; replay signature support passed `00:00 +2: All tests passed!`; `./scripts/run_test_gates.sh groups` passed `00:16 +136: All tests passed!`; `dart format --set-exit-if-changed` passed with `0 changed`; `git diff --check` passed. Accepted differences: direct host replay/membership-window proof is sufficient because no relay, transport, device harness, or production code changed. Residual-only none; GK-024 remains the next unresolved P0 row. |
| GK-024 | Late-joining member cannot decrypt pre-join history unless explicitly allowed | D joins after old messages. | 1. A/B send before D. 2. Add D with current key. 3. D retrieves. | D sees only allowed messages for its membership epoch/window. | P0 | Covered | Required | Required | Recommended | Required | Required | Covered by app-layer replay entitlement guards in `lib/features/groups/application/drain_group_offline_inbox_use_case.dart` and `lib/features/groups/application/handle_incoming_group_message_use_case.dart` plus exact Flutter offline replay proof in `test/features/groups/application/drain_group_offline_inbox_use_case_test.dart`. `GK-024 late-joining member skips pre-join replay and renders post-join replay` proves Dana joins after old messages, skips both old-key and current-key pre-join replay records without rendering or queuing key repair, emits two pre-join skip events, and renders the post-join replay exactly once. Focused GK-024, adjacent GK-023/GM-033 replay selector, replay envelope support, IJ010 smoke repro, `./scripts/run_test_gates.sh groups` (`00:27 +136`), format, and `git diff --check` passed. Residual-only none; GK-025, GK-026, and GK-027 are covered separately; GK-029 is covered separately and GK-030 is covered separately and GK-033 is covered separately and GA-001 is covered separately and GA-002 is covered separately and GA-003 is covered separately and GE-005 is the next unresolved P0 session. |
| GK-025 | Tampering envelope type from group_message to group_reaction is rejected or harmless | Valid signed message envelope is modified only in type. | 1. Change type. 2. Validator/decrypt. 3. Inspect emitted event. | Either signature policy rejects tampered type or app safely ignores mismatched inner schema; no fake reaction appears. | P0 | Covered | Required | Required | N/A | Recommended | N/A | Covered by Go reaction dispatch guard in `go-mknoon/node/pubsub.go` plus exact row-owned tests in `go-mknoon/node/pubsub_decryption_failure_test.go`. `TestGK025EnvelopeTypeTamperToReactionEmitsPayloadParseFailedWithoutFakeReaction` proves a valid signed `group_message` envelope whose outer type is changed to `group_reaction` still preserves validator semantics but emits `group:payload_parse_failed` and no fake `group_reaction:received`, `group_message:received`, decrypt failure, or plaintext marker. `TestGK025ValidGroupReactionDeliveryStillEmitsReaction` proves valid full-schema reactions still emit, and `TestLP003|TestGL008` proves existing minimal reaction payloads remain valid. Focused GK-025, LP003/GL008 compatibility, adjacent tamper/security, broader node/internal/crypto, race, gofmt, and `git diff --check` passed. Residual-only none; GK-026 and GK-027 are covered separately; GK-029 is covered separately and GK-030 is covered separately and GK-033 is covered separately and GA-001 is covered separately and GA-002 is covered separately and GA-003 is covered separately and GE-005 is the next unresolved P0 session. |
| GK-026 | Tampering senderId is rejected | Envelope signed by B is modified to claim C. | 1. Change SenderId. 2. Validate under C's member key. 3. Inspect reason. | Rejected unless C has same signing key, which should be impossible; no attribution corruption. | P0 | Covered | Required | Required | N/A | Recommended | N/A | Covered by existing Go validator claimed-sender key lookup plus exact row-owned tests in `go-mknoon/node/pubsub_test.go` and `go-mknoon/node/pubsub_decryption_failure_test.go`. `TestGK026ValidateGroupEnvelopeRejectsSenderIDTamperUnderClaimedMemberKey` proves a valid B-signed envelope mutated only at top-level `senderId` to C preserves signature, ciphertext, nonce, group, type, epoch, and sender public-key fields, still verifies with B's key, fails under C's configured key, and rejects pure validation as `reject:bad_signature`. `TestGK026SenderIDTamperLiveRawPublishRejectsWithoutAttributionCorruption` proves live raw publish from C's transport emits `group:validation_rejected` reason `bad_signature_or_epoch` and no message, reaction, decrypt failure, plaintext marker, or raw C attribution event. Focused GK-026, adjacent sender/signature tamper, broader node/internal/crypto, race, gofmt, and `git diff --check` passed. Residual-only none; GK-027 is covered separately and GK-029 is covered separately and GK-030 is covered separately and GK-033 is covered separately and GA-001 is covered separately and GA-002 is covered separately and GA-003 is covered separately and GE-005 is the next unresolved P0 session. |
| GK-027 | Tampering senderDeviceId/transport binding is rejected | Valid envelope modified to revoked/other device. | 1. Change senderDeviceId or senderTransportPeerId. 2. Validate. | Rejected as peer_mismatch or unbound_device. | P0 | Covered | Required | Required | N/A | Recommended | N/A | Covered by tests-only Go node proof in `go-mknoon/node/pubsub_test.go` and `go-mknoon/node/pubsub_decryption_failure_test.go`; no GK-027 production code changed. `TestGK027ValidateGroupEnvelopeRejectsDeviceAndTransportBindingTamper` proves a valid active device-bound envelope accepts, mutating only `senderDeviceId` to a revoked device preserves signature/ciphertext/nonce/group/type/epoch/public-key/key-package fields and rejects pure validation as `reject:unbound_device`, and mutating only `senderTransportPeerId` to another transport preserves the same artifacts and rejects as `reject:peer_mismatch`. `TestGK027DeviceAndTransportBindingTamperLiveRawPublishRejectsWithoutPayload` proves live raw publishes emit `group:validation_rejected` reasons `unbound_device` and `peer_mismatch` with no `group_message:received`, `group_reaction:received`, `group:decryption_failed`, plaintext marker, or tampered binding attribution side effects. Focused GK-027 passed (`ok node 6.335s`), adjacent device/transport selector passed (`ok node 12.538s`), broader node/internal/crypto passed (`ok node 9.470s`, `ok internal 0.585s`, `ok crypto 0.326s`), race passed (`ok node 7.355s`), gofmt and `git diff --check` passed. Residual-only none; GK-029 is covered separately and GK-030 is covered separately and GK-033 is covered separately and GA-001 is covered separately and GA-002 is covered separately and GA-003 is covered separately and GE-005 is the next unresolved P0 session. |
| GK-028 | Tampering SenderPublicKey does not bypass config public key | Envelope includes wrong SenderPublicKey field. | 1. Build envelope signed by member key but SenderPublicKey field changed. 2. Validate. | Validation uses configured active device key; field mismatch is either ignored safely or rejected explicitly. | P1 | Covered | Required | Recommended | N/A | N/A | N/A | Covered on 2026-05-13 by exact native pure-validator, live raw-publish validator, and Dart offline replay proofs. `go-mknoon/node/pubsub.go` validates with the configured active device key (`sourceDevice.DeviceSigningPublicKey`) rather than trusting legacy `SenderPublicKey`. `go-mknoon/node/pubsub_test.go::TestGK028ValidateGroupEnvelopeRejectsSenderPublicKeyBypass` proves a configured-member envelope signed with an attacker key and carrying attacker `SenderPublicKey` still rejects under the configured key. `go-mknoon/node/pubsub_decryption_failure_test.go::TestGK028SenderPublicKeyTamperLiveRawPublishRejectsWithoutPayload` proves the same live raw publish emits validation rejection without payload, decrypt, parse, plaintext, or attacker-key attribution side effects. `test/features/groups/application/group_offline_replay_envelope_test.dart::GK-028 decode rejects senderPublicKey tamper before decrypt` proves offline replay rejects `sender_key_mismatch` before decrypt. Passed evidence: gofmt, focused Go GK-028 (`ok node 4.071s`), focused Dart GK-028 (`+1`), full offline replay envelope suite (`+3`), scoped analyzer clean, adjacent Go selector (`ok node 22.222s`, `ok internal 0.275s`, `ok crypto 0.891s`), and named `./scripts/run_test_gates.sh groups` (`+159`). Residual-only none; GR-002 is the next unresolved session in ordered ledger order. |
| GK-029 | Malformed payload JSON emits payload parse failure only | Envelope decrypts to non-JSON or wrong schema. | 1. Encrypt bad plaintext with valid signature. 2. Deliver. 3. Inspect events. | No group_message:received; group:payload_parse_failed emitted with group/sender/type. | P0 | Covered | Required | Required | N/A | Recommended | N/A | Covered by parser guard in `go-mknoon/internal/group_envelope.go` plus exact Go node/internal tests in `go-mknoon/internal/group_envelope_test.go` and `go-mknoon/node/pubsub_decryption_failure_test.go`. `ParseGroupPayload` now rejects non-object JSON, missing/non-string `text`, missing/non-string/blank `timestamp`, while preserving valid payloads with present empty text and extras. `TestGK029ParseGroupPayloadRejectsWrongSchema` proves wrong-schema parser rejection, and `TestGK029ParseGroupPayloadAcceptsPresentEmptyTextWithTimestamp` proves attachment-friendly empty text remains valid. `TestGK029MalformedPayloadJSONEmitsPayloadParseFailedOnly` proves live raw-publish non-JSON and wrong-schema plaintext both emit `group:payload_parse_failed` with group/sender/type and no `group_message:received`, `group_reaction:received`, `group:decryption_failed`, or plaintext marker. Focused GK-029 passed (`ok internal`, `ok node 5.755s`), adjacent parser/payload selector passed (`ok internal`, `ok node 9.835s`), broader node/internal/crypto passed (`ok node 13.741s`, `ok internal`, `ok crypto`), race passed (`ok node 10.148s`) after replacing post-start test `eventCallback` assignment with collector-backed node construction, gofmt and `git diff --check` passed. Residual-only none; GK-030 is covered separately and GK-033 is covered separately and GA-001 is covered separately and GA-002 is covered separately and GA-003 is covered separately and GE-005 is the next unresolved P0 session. |
| GK-030 | Extra fields required by app are preserved or explicitly dropped | Payload contains media, quote, attachments, delivery IDs, unknown fields. | 1. Publish with full opts. 2. Receive event. 3. Compare required fields. | All product-required fields are emitted; any dropped fields are documented and not needed for rendering/dedupe. | P0 | Covered | Required | Required | Recommended | Recommended | Required | Covered by receive-event extra preservation in `go-mknoon/node/pubsub.go` plus exact Go node tests in `go-mknoon/node/pubsub_test.go` and `go-mknoon/node/pubsub_delivery_test.go`. `buildGroupMessageReceivedEvent` now forwards every non-canonical `GroupMessagePayload.Extra` key into `group_message:received`, preserving media, quote, attachments, delivery IDs, client IDs, `publishedAtNano`, and unknown fields while protecting envelope/payload-owned fields and receive-path metrics. `TestGK030BuildGroupMessageReceivedEventPreservesExtrasAndProtectsCanonicalFields` proves the pure mapping contract, including spoofed canonical field protection. `TestGK030PublishGroupMessagePreservesExtraFieldsInReceivedEvent` proves live encrypted publish delivery preserves full payload-only `opts`, does not mutate the input opts map, and keeps `deliveryMs` receive-path owned. Focused GK-030 passed (`ok node 0.613s`), adjacent publish/event selector passed (`ok node 1.109s`), broader node/internal/crypto passed (`ok node 24.226s`, `ok internal 0.629s`, `ok crypto 0.904s`), race passed (`ok node 2.598s`), gofmt and `git diff --check` passed. Residual-only none; GK-033 is covered separately and GA-001 is covered separately and GA-002 is covered separately and GA-003 is covered separately and GE-005 is the next unresolved P0 session. |
| GK-031 | messageId cannot be overridden through opts | Sender supplies opts.messageId different from explicit messageId. | 1. Publish with conflicting values. 2. Decrypt payload. 3. Inspect received event. | Explicit messageId wins and is stable for dedupe/inbox. | P1 | Covered | Required | Recommended | N/A | N/A | N/A | Covered on 2026-05-13 by exact Go unit and live publish/receive proofs. `go-mknoon/node/pubsub.go::buildGroupMessageExtra` copies opts and then writes the explicit `messageId`, and `PublishGroupMessage` returns the explicit id when provided. `go-mknoon/node/pubsub_test.go::TestGK031BuildGroupMessageExtraExplicitMessageIDWins` proves conflicting `opts.messageId` cannot override the explicit id and input opts are not mutated. `go-mknoon/node/pubsub_delivery_test.go::TestGK031PublishGroupMessageExplicitMessageIDWinsOverOptsMessageID` proves live publish returns and delivers the explicit id while preserving unrelated opts. Passed evidence: gofmt, focused GK-031 (`ok node 0.683s`), adjacent publish/message-id selector (`ok node 3.389s`), broader node/internal/crypto selector (`ok node 2.590s`, `ok internal 1.252s`, `ok crypto 0.935s`), selected race selector (`ok node 4.093s`), and named `./scripts/run_test_gates.sh groups` (`+159`). Residual-only none; GR-002 is the next unresolved session in ordered ledger order. |
| GK-032 | publishedAtNano is parse-safe and cannot crash receive | Payload has missing, malformed, or huge publishedAtNano. | 1. Deliver variants. 2. Receive. 3. Inspect deliveryMs. | Message still emits; deliveryMs omitted or bounded when invalid. | P1 | Covered | Required | Recommended | N/A | N/A | N/A | Covered on 2026-05-13 by exact live encrypted receive proof. `go-mknoon/node/pubsub_delivery_test.go::TestGK032PublishedAtNanoInvalidValuesStillEmitMessageWithoutDeliveryMs` publishes raw valid encrypted group-message envelopes with missing, malformed, overflow-sized, and non-string `publishedAtNano` extras, proves every variant emits `group_message:received` with expected message id/text, and proves `deliveryMs` is omitted for invalid/missing values. Passed evidence: gofmt, focused GK-032 (`ok node 0.619s`), adjacent publish/receive selector (`ok node 1.349s`), broader node/internal/crypto selector (`ok node 1.705s`, `ok internal 0.289s`, `ok crypto 0.922s`), selected race selector (`ok node 3.056s`), named `./scripts/run_test_gates.sh groups` (`+159`), and diff hygiene. Residual-only none; GR-002 is the next unresolved session in ordered ledger order. |
| GK-033 | Group reaction uses same epoch/device validation as message | Valid and invalid reaction envelopes. | 1. Publish reactions from member, removed member, revoked device. 2. Validate/receive. | Only active members' reactions are accepted; reactions do not bypass removal or key epoch. | P0 | Covered | Required | Required | Recommended | Recommended | Required | Covered by tests-only Go node proof in `go-mknoon/node/pubsub_test.go` and `go-mknoon/node/pubsub_decryption_failure_test.go`; no GK-033 production code changed. Existing `groupTopicValidator` validates `group_reaction` envelopes through the same group, member, active-device, transport-peer, signature, and key-epoch path as messages. `TestGK033ValidateGroupReactionUsesMessageEpochAndDeviceValidation` proves pure validation accepts a current active-device reaction, rejects an expired previous-epoch reaction as `reject:bad_signature`, and rejects a revoked-device reaction as `reject:unbound_device`. `TestGK033GroupReactionRejectsStaleEpochAndRevokedDeviceWithoutReceive` proves live current reactions emit `group_reaction:received`, while stale-epoch and revoked-device raw publishes emit `group:validation_rejected` (`bad_signature_or_epoch`, then `unbound_device`) with no `group_reaction:received`, `group_message:received`, `group:decryption_failed`, or plaintext marker. Focused GK-033 passed (`ok node 5.778s`), adjacent reaction/security selector passed (`ok node 11.415s`), broader node/internal/crypto passed (`ok node 10.159s`, `ok internal 0.572s`, `ok crypto 0.309s`), race passed (`ok node 7.872s`), gofmt and `git diff --check` passed. Residual-only none; GA-001 is covered separately and GA-002 is covered separately and GA-003 is covered separately and GE-005 is the next unresolved P0 session. |

## Authorization, Roles, and Device Binding

| Test ID | Scenario | Preconditions | Steps | Expected Result | Priority | Current status | Unit | Integration | Smoke | Fake Network | 3-Party E2E | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| GA-001 | Current member can publish message in private chat group | A/B are active members; group type chat/private. | 1. B publishes. 2. A validates and receives. | Message accepted and emitted once. | P0 | Covered | Required | Required | Required | Recommended | Required | Covered by tests-only Go node proof in `go-mknoon/node/pubsub_delivery_test.go`; no GA-001 production code changed. `TestGA001CurrentMemberPublishesPrivateChatMessageExactlyOnce` uses a two-node `GroupTypeChat` private group with active members, publishes with an explicit message ID from the current member, verifies publish success and positive topic peer count, and proves the receiving member emits exactly one `group_message:received` event for that message with expected `groupId`, `senderId`, `senderUsername`, `messageId`, `text`, and `keyEpoch`. Focused GA-001 passed (`ok node 1.161s`), adjacent publish/authorization selector passed (`ok node 3.797s`), broader node/internal/crypto passed (`ok node 4.109s`, `ok internal 0.898s`, `ok crypto 0.325s`), race passed (`ok node 3.088s`), gofmt and `git diff --check` passed. Residual-only none; GA-002 is covered separately and GA-003 is covered separately and GE-005 is the next unresolved P0 session. |
| GA-002 | Non-member cannot publish | X is not in config. | 1. X publishes validly signed envelope with own key. 2. A/B validators run. | Rejected as non_member; no decrypt or render. | P0 | Covered | Required | Required | Required | Recommended | Required | Covered by tests-only Go node proof in `go-mknoon/node/pubsub_authorization_forward_test.go`; no GA-002 production code changed. `TestGA002NonMemberCannotPublishValidEnvelope` builds a `GroupTypeChat` private group where X can publish from a stale local config with a valid signature/key while the current A/B configs exclude X, proves pure validation returns `reject:non_member`, proves both current members emit `group:validation_rejected` reason `non_member` at the expected `keyEpoch`, and asserts no `group_message:received`, `group_reaction:received`, `group:decryption_failed`, `group:payload_parse_failed`, or plaintext marker appears. Focused GA-002 passed (`ok node 7.152s`), adjacent authorization selector passed (`ok node 13.496s`), broader node/internal/crypto passed (`ok node 10.692s`, `ok internal 0.607s`, `ok crypto 0.911s`), race passed (`ok node 8.314s`), gofmt and `git diff --check` passed. Residual-only none; GA-003 is covered separately and GE-005 is the next unresolved P0 session. |
| GA-003 | Removed member cannot publish with old config/key | C removed but has old key. | 1. C publishes after A/B update config/key. 2. A/B validators run. | Rejected as non_member or bad epoch; rejection diagnostic emitted. | P0 | Covered | Required | Required | Required | Recommended | Required | Covered by tests-only Go node proof in `go-mknoon/node/pubsub_delivery_test.go`; no GA-003 production code changed. `TestGA003RemovedMemberCannotPublishWithOldConfigKey` builds a three-node `GroupTypeChat` private group with A/B/C initially active, then updates A/B to remove C and rotate to a newer key while C remains locally joined with its old config/key. The test proves pure validation against the current A/B config returns `reject:non_member`, C can still publish locally with the old key/config, both A and B emit `group:validation_rejected` with an allowed row reason (`non_member` or `bad_signature_or_epoch`) for old epoch traffic, and no `group_message:received`, `group_reaction:received`, `group:decryption_failed`, `group:payload_parse_failed`, or plaintext marker appears. Focused GA-003 passed (`ok node 7.578s`), adjacent authorization selector passed (`ok node 25.471s`), broader node/internal/crypto passed (`ok node 16.729s`, `ok internal 0.282s`, `ok crypto 0.882s`), race passed (`ok node 13.776s`), gofmt and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GA-005 | Role update takes effect without topic rejoin | B changes from member to admin or admin to member. | 1. UpdateGroupConfig. 2. B sends before/after. 3. Validate. | Write permission follows latest role immediately. | P0 | Covered | Required | Required | N/A | Recommended | Required | Covered by tests-only Go node proof in `go-mknoon/node/pubsub_delivery_test.go`; no GA-005 production code changed. `TestGA005RoleUpdateTakesEffectWithoutTopicRejoin` uses the repo role-sensitive `GroupTypeAnnouncement` path because chat/QA groups intentionally allow any member to write. It proves B as writer is blocked locally and pure validation rejects as `reject:unauthorized`, `UpdateGroupConfig` promotion to admin keeps both nodes on the same joined topic and allows B to publish without rejoin, A receives the promoted message, receiver-side demotion keeps the topic while A rejects stale-local-admin traffic as `unauthorized_writer` with no message/reaction/decrypt/payload-parse/plaintext side effects, and B's local demotion immediately blocks `PublishGroupMessage` without rejoin. Focused GA-005 passed (`ok node 4.706s`), adjacent role/update selector passed (`ok node 12.148s`), broader node/internal/crypto passed (`ok node 7.329s`, `ok internal 0.714s`, `ok crypto 1.039s`), race passed (`ok node 10.055s`), gofmt and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GA-006 | Sender transport peer must match envelope | B envelope claims B but transport peer is X. | 1. Deliver from pid X. 2. Validator checks. | Rejected as peer_mismatch. | P0 | Covered | Required | Required | N/A | Recommended | N/A | Covered by tests-only Go node proof in `go-mknoon/node/pubsub_authorization_forward_test.go`; no GA-006 production code changed. `TestGA006SenderTransportPeerMismatchRejects` builds a current config where B is a valid member, constructs a valid B-signed envelope that omits `senderTransportPeerId` so expected transport defaults to B, proves pure validation accepts from B and rejects from X as `reject:peer_mismatch`, bypasses only X's local validator for raw fanout, publishes from transport peer X, and proves A emits `group:validation_rejected` reason `peer_mismatch` with no `group_message:received`, `group_reaction:received`, `group:decryption_failed`, `group:payload_parse_failed`, or plaintext marker. Focused GA-006 passed (`ok node 3.554s`), adjacent transport selector passed (`ok node 10.009s`), broader node/internal/crypto passed (`ok node 6.401s`, `ok internal 0.946s`, `ok crypto 0.696s`), race passed (`ok node 4.768s`), gofmt and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GA-007 | Legacy single-device member accepts default device binding | Member has no Devices list. | 1. Envelope senderDeviceId/transport omitted or equals PeerId. 2. Validate. | Accepted when public key matches member.PublicKey. | P0 | Covered | Required | Required | N/A | Recommended | N/A | Covered by tests-only Go node proof in `go-mknoon/node/pubsub_delivery_test.go`; no GA-007 production code changed. `TestGA007LegacySingleDeviceMemberAcceptsDefaultDeviceBinding` builds a two-node private chat group where B is a current legacy member with no `Devices` list and `PublicKey == pubB`, proves pure validation accepts both an omitted-binding B envelope and an explicit default-binding envelope with `senderDeviceId == B`, `senderTransportPeerId == B`, and `senderDevicePublicKey == pubB`, publishes both through the live group topic, and proves A emits `group_message:received` with expected group, sender, text, and key epoch without validation/decrypt/payload-parse failures. Focused GA-007 passed (`ok node 4.141s`), adjacent device fallback selector passed (`ok node 7.045s`), broader node/internal/crypto passed (`ok node 7.330s`, `ok internal 0.552s`, `ok crypto 0.394s`), race passed (`ok node 5.198s`), gofmt and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GA-008 | Legacy single-device member rejects wrong senderDeviceId | Member has no Devices list. | 1. Envelope SenderDeviceId != member.PeerId. 2. Validate. | Rejected as unbound_device. | P0 | Covered | Required | Required | N/A | Recommended | N/A | Covered by tests-only Go node proof in `go-mknoon/node/pubsub_delivery_test.go`; no GA-008 production code changed. `TestGA008LegacySingleDeviceMemberRejectsWrongSenderDeviceID` builds a two-node private chat group where B is a current legacy member with no `Devices` list, constructs a valid B-signed envelope with `senderDeviceId != B` while transport remains B, proves pure validation rejects as `reject:unbound_device`, bypasses only B's local validator for raw fanout, publishes from B, and proves A emits `group:validation_rejected` reason `unbound_device` with no `group_message:received`, `group_reaction:received`, `group:decryption_failed`, `group:payload_parse_failed`, or plaintext marker. Focused GA-008 passed (`ok node 3.590s`), adjacent fallback/unbound selector passed (`ok node 11.035s`), broader node/internal/crypto passed (`ok node 10.489s`, `ok internal 0.668s`, `ok crypto 0.346s`), race passed (`ok node 8.323s`), gofmt and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GA-009 | Legacy single-device member rejects wrong SenderDevicePublicKey | Member has no Devices list. | 1. Envelope signed or field uses wrong public key. 2. Validate. | Rejected or fails signature; no render. | P0 | Covered | Required | Required | N/A | Recommended | N/A | Covered by tests-only Go node proof in `go-mknoon/node/pubsub_delivery_test.go`; no GA-009 production code changed. `TestGA009LegacySingleDeviceMemberRejectsWrongSenderDevicePublicKey` builds a two-node private chat group where B is a current legacy member with no `Devices` list and `PublicKey == pubB`, constructs a valid B-signed envelope with `senderDevicePublicKey != pubB`, proves pure validation rejects as `reject:unbound_device`, bypasses only B's local validator for raw fanout, publishes from B, and proves A emits `group:validation_rejected` reason `unbound_device` with no `group_message:received`, `group_reaction:received`, `group:decryption_failed`, `group:payload_parse_failed`, or plaintext marker. Focused GA-009 passed (`ok node 3.677s`), adjacent public-key/unbound selector passed (`ok node 12.692s`), broader node/internal/crypto passed (`ok node 6.710s`, `ok internal 0.487s`, `ok crypto 0.980s`), race passed (`ok node 4.656s`), gofmt and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GA-010 | Multi-device active device can publish | Member B has active device S with transportPeerId P. | 1. Publish with SenderId=B, SenderDeviceId=S, SenderTransportPeerId=P. 2. Validate from pid P. | Accepted using S.DeviceSigningPublicKey. | P0 | Covered | Required | Required | Required | Recommended | Required | Covered by tests-only Go node proof in `go-mknoon/node/pubsub_delivery_test.go`; no GA-010 production code changed. `TestGA010MultiDeviceActiveDeviceCanPublish` builds a private chat group where logical member B has active device S bound to transport P, device signing public key, and key package K, constructs a valid device-signed envelope with `SenderId == B`, `SenderDeviceId == S`, `SenderTransportPeerId == P`, `SenderDevicePublicKey == devicePub`, and `SenderKeyPackageId == K`, proves pure validation from P returns `accept`, raw-publishes from P, and proves A emits `group_message:received` with expected group, sender, device, transport, text, and key epoch with no validation/decrypt side effects. Focused GA-010 passed (`ok node 2.145s`), adjacent device selector passed (`ok node 12.081s`), broader node/internal/crypto passed (`ok node 4.836s`, `ok internal 0.854s`, `ok crypto 0.605s`), race passed (`ok node 3.410s`), gofmt and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GA-011 | Multi-device missing SenderDeviceId is rejected or explicitly unsupported | Member has Devices list; envelope omits device ID. | 1. Publish without opts. 2. Validate. | Rejected with unbound_device unless app guarantees opts are always set. | P0 | Covered | Required | Required | N/A | Recommended | Required | Covered by tests-only Go node proof in `go-mknoon/node/pubsub_delivery_test.go`; no GA-011 production code changed. `TestGA011MultiDeviceMissingSenderDeviceIDRejects` builds a private chat group where logical member B has active device S bound to transport P, constructs a valid device-bound envelope, deletes only top-level `senderDeviceId` while preserving sender, transport, device public key, key package, group, type, epoch, encrypted payload, and signature, proves pure validation from P returns `reject:unbound_device`, raw-publishes from P after bypassing only P's local validator, and proves A emits `group:validation_rejected` reason `unbound_device` with no message/reaction/decrypt/payload-parse/plaintext side effects. Focused GA-011 passed (`ok node 3.684s`), adjacent device selector passed (`ok node 11.147s`), broader node/internal/crypto passed (`ok node 6.565s`, `ok internal 0.889s`, `ok crypto 1.179s`), race passed (`ok node 4.728s`), gofmt and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GA-012 | Multi-device missing SenderTransportPeerId is rejected or repaired consistently | Member has Devices list; envelope omits transport peer. | 1. Deliver from active transport pid. 2. Validate. | Accepted only if deterministic fallback matches active device; otherwise rejected with diagnostics. | P0 | Covered | Required | Required | N/A | Recommended | N/A | Covered by tests-only Go node proof in `go-mknoon/node/pubsub_delivery_test.go`; no GA-012 production code changed. `TestGA012MultiDeviceMissingSenderTransportPeerIDRejects` builds a private chat group where logical member B has active device S bound to transport P, constructs a valid device-bound envelope, deletes only top-level `senderTransportPeerId` while preserving sender, device ID, device public key, key package, group, type, epoch, encrypted payload, and signature, proves pure validation from P returns diagnostic `reject:peer_mismatch`, raw-publishes from P after bypassing only P's local validator, and proves A emits `group:validation_rejected` reason `peer_mismatch` with no message/reaction/decrypt/payload-parse/plaintext side effects. Focused GA-012 passed (`ok node 3.570s`), adjacent device/transport selector passed (`ok node 11.430s`), broader node/internal/crypto passed (`ok node 9.366s`, `ok internal 0.854s`, `ok crypto 0.691s`), race passed (`ok node 7.822s`), gofmt and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GA-013 | Revoked device cannot publish | B has device S revoked. | 1. Deliver signed envelope from S. 2. Validate. | Rejected as unbound_device. | P0 | Covered | Required | Required | Required | Recommended | Required | Covered by tests-only Go node proof in `go-mknoon/node/pubsub_delivery_test.go`; no GA-013 production code changed. `TestGA013RevokedDeviceCannotPublish` builds a private chat group where logical member B has revoked device S bound to transport P with S's own device signing key and key package, constructs a revoked-device-signed envelope with matching sender/device/transport/public-key/key-package fields, proves pure validation from P returns `reject:unbound_device`, raw-publishes from P after bypassing only P's local validator, and proves A emits `group:validation_rejected` reason `unbound_device` with no message/reaction/decrypt/payload-parse/plaintext side effects. Focused GA-013 passed (`ok node 3.554s`), adjacent revoked/unbound selector passed (`ok node 13.436s`), broader node/internal/crypto passed (`ok node 12.048s`, `ok internal 0.576s`, `ok crypto 1.070s`), race passed (`ok node 9.880s`), gofmt and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GA-014 | Inactive device cannot publish | B has device S status inactive. | 1. Deliver signed envelope. 2. Validate. | Rejected as unbound_device. | P0 | Covered | Required | Required | N/A | Recommended | N/A | Covered by tests-only Go node proof in `go-mknoon/node/pubsub_delivery_test.go`; no GA-014 production code changed. `TestGA014InactiveDeviceCannotPublish` builds a private chat group where logical member B has inactive device S bound to transport P with S's own device signing key and key package, constructs an inactive-device-signed envelope with matching sender/device/transport/public-key/key-package fields, proves pure validation from P returns `reject:unbound_device`, raw-publishes from P after bypassing only P's local validator, and proves A emits `group:validation_rejected` reason `unbound_device` with no message/reaction/decrypt/payload-parse/plaintext side effects. Focused GA-014 passed (`ok node 3.662s`), adjacent inactive/unbound selector passed (`ok node 7.117s`), broader node/internal/crypto passed (`ok node 7.010s`, `ok internal 0.344s`, `ok crypto 0.622s`), race passed (`ok node 4.826s`), gofmt and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GA-015 | KeyPackageId mismatch is rejected | B active device has keyPackageId K1; envelope says K2. | 1. Deliver envelope. 2. Validate. | Rejected as unbound_device. | P0 | Covered | Required | Required | N/A | Recommended | N/A | Covered by tests-only Go node proof in `go-mknoon/node/pubsub_delivery_test.go`; no GA-015 production code changed. `TestGA015KeyPackageIDMismatchRejects` builds a private chat group where logical member B has active device S bound to transport P with device signing key and active key package K1, constructs a device-signed envelope from S with matching sender/device/transport/public-key fields but mismatched `senderKeyPackageId == K2`, proves pure validation from P returns `reject:unbound_device`, raw-publishes from P after bypassing only P's local validator, and proves A emits `group:validation_rejected` reason `unbound_device` with no message/reaction/decrypt/payload-parse/plaintext side effects. Focused GA-015 passed (`ok node 3.599s`), adjacent key-package/unbound selector passed (`ok node 6.995s`), broader node/internal/crypto passed (`ok node 6.240s`, `ok internal 0.912s`, `ok crypto 0.659s`), race passed (`ok node 4.697s`), gofmt and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GA-016 | Device public key mismatch is rejected even if device ID matches | B active device has signing key PK1; envelope says PK2/signs with PK2. | 1. Deliver envelope. 2. Validate. | Rejected as unbound_device or bad_signature. | P0 | Covered | Required | Required | N/A | Recommended | N/A | Covered by tests-only Go node proof in `go-mknoon/node/pubsub_delivery_test.go`; no GA-016 production code changed. `TestGA016DevicePublicKeyMismatchRejects` builds a private chat group where logical member B has active device S bound to transport P with configured signing key PK1 and key package K, constructs an envelope with matching sender/device/transport/key-package fields but `senderDevicePublicKey == PK2` and a PK2 signature, proves the signature verifies with PK2 and not PK1, proves pure validation from P returns `reject:unbound_device`, raw-publishes from P after bypassing only P's local validator, and proves A emits `group:validation_rejected` reason `unbound_device` with no message/reaction/decrypt/payload-parse/plaintext side effects. Focused GA-016 passed (`ok node 3.903s`) after fixing an unused test variable, adjacent public-key/unbound selector passed (`ok node 11.833s`), broader node/internal/crypto passed (`ok node 9.376s`, `ok internal 0.691s`, `ok crypto 0.551s`), race passed (`ok node 10.048s`), gofmt and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GA-017 | Sibling device messages are not skipped as self | Same account has devices S1 and S2. | 1. Local node S1 receives envelope from S2 with SenderId account-level B. 2. Subscription handler runs. | S1 emits message from sibling or app-specific sync event; it is not dropped by env.SenderId == selfPeerId unless selfPeerId is truly same transport. | P0 | Covered | Required | Required | Required | Recommended | Required | Covered by transport-aware self-echo handling in `go-mknoon/node/pubsub.go` plus exact live Go node proof in `go-mknoon/node/pubsub_delivery_test.go`. `groupEnvelopeOriginatesFromLocalTransport` now skips only same local transport echoes by honoring explicit `senderTransportPeerId` first and falling back to `senderId` only for legacy envelopes without transport binding. `TestGA017SiblingDeviceMessageIsNotSkippedAsSelf` builds one logical member whose S1 local peer id equals the account-level `SenderId`, sends a valid S2 active-device envelope from S2 transport, proves pure validation accepts, proves S2 is not classified as local echo while S1 same-transport remains local echo, and proves live raw publish emits `group_message:received` on S1 with expected group/sender/device/transport/text/key epoch and no validation/decrypt/payload-parse side effects. Focused GA-017 passed (`ok node 2.602s`), adjacent sibling/self/transport/device selector passed (`ok node 11.759s`), broader node/internal/crypto passed (`ok node 11.933s`, `ok internal 0.287s`, `ok crypto 0.568s`), race passed (`ok node 10.081s`), gofmt and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GA-018 | Self echo from same transport is skipped once | Local node receives its own pubsub echo. | 1. Publish from local transport. 2. Subscription receives echo. 3. Inspect events. | No duplicate incoming message for same local send; local sender status remains separate. | P1 | Covered | Required | Required | N/A | Recommended | N/A | Covered on 2026-05-14 by exact live Go subscription-path proof. `go-mknoon/node/pubsub_delivery_test.go::TestGA018SameTransportSelfEchoIsSkippedOnce` proves the local sender path emits a separate `group:publish_debug` event for the explicit message id, proves a non-local raw control envelope reaches the local subscription and emits `group_message:received`, then raw-publishes a valid same-transport self-echo envelope with local `senderTransportPeerId` and proves no duplicate `group_message:received`, decrypt, payload-parse, or validation side effect is emitted. Passed evidence: gofmt, focused GA-018 (`ok node 4.714s`), adjacent transport/device selector (`ok node 55.671s`), broader node/internal/crypto selector (`ok node 60.677s`, `ok internal 1.280s`, `ok crypto 1.024s`), selected race selector (`ok node 59.023s`), named `./scripts/run_test_gates.sh groups` (`+159`), and diff hygiene. Residual-only none; GR-002 is the next unresolved session in ordered ledger order. |
| GA-019 | Public key reuse across member IDs is rejected by policy | Misconfigured B and C share device signing key. | 1. B signs envelope claiming C. 2. Validate. 3. Inspect config validation. | Config is rejected or message attribution remains safe; no impersonation. | P0 | Covered | Required | Required | N/A | N/A | N/A | Covered by signing-key uniqueness policy in `go-mknoon/node/pubsub.go` plus exact live Go node proof in `go-mknoon/node/pubsub_delivery_test.go`. `validateGroupConfigSigningKeyUniqueness` rejects active signing keys reused across different member IDs; `JoinGroupTopic`, local message/reaction publish paths, production topic validation, and pure validator tests all fail closed as `ambiguous_signing_key` or an invalid-config error. `TestGA019PublicKeyReuseAcrossMemberIDsRejectsPolicy` proves a safe config with C's shared key accepts the C envelope, an ambiguous B/C shared-device-key config is rejected by config policy, initial join stores no topic/config state, pure validation rejects as `reject:ambiguous_signing_key`, live raw publish emits `group:validation_rejected` reason `ambiguous_signing_key` with no message/reaction/decrypt/payload-parse/plaintext side effects, and local publish is blocked. Focused GA-019 passed (`ok node 3.591s`), adjacent signing-key/device selector passed (`ok node 12.514s`), broader node/internal/crypto passed (`ok node 6.290s`, `ok internal 0.601s`, `ok crypto 0.892s`), race passed (`ok node 4.688s`), gofmt and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GA-020 | Duplicate device IDs within a member are rejected | Config has two active devices with same DeviceId. | 1. Update config. 2. Deliver envelope for that DeviceId. 3. Validate. | Config validation rejects duplicates or deterministic active binding is safe. | P0 | Covered | Required | Required | N/A | N/A | N/A | Covered by existing deterministic device normalization in `go-mknoon/node/pubsub.go` plus exact Go node proof in `go-mknoon/node/pubsub_delivery_test.go`. `normalizeGroupMemberDevices` deduplicates devices by `DeviceId` before group configs are stored, preferring the active/key-package-bearing candidate so validators never first-match across duplicate entries. `TestGA020DuplicateDeviceIDsWithinMemberUseDeterministicBinding` proves a config with two active devices sharing the same `DeviceId` stores exactly one selected device, accepts and live-delivers only the selected transport/signing-key/key-package binding, rejects the shadow duplicate as pure/live `unbound_device`, and emits no message/reaction/decrypt/payload-parse/plaintext side effects for the shadow duplicate. Focused GA-020 passed (`ok node 5.168s`), adjacent duplicate-device/device-binding selector passed (`ok node 11.983s`), broader node/internal/crypto passed (`ok node 8.416s`, `ok internal 0.313s`, `ok crypto 0.606s`), race passed (`ok node 6.241s`), gofmt and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GA-021 | Duplicate transportPeerId across members is rejected | B and C active devices share transport peer. | 1. Update config. 2. Deliver from shared pid. 3. Validate. | Config rejected; no message can be attributed ambiguously. | P0 | Covered | Required | Required | N/A | N/A | N/A | Covered by transport-peer identity uniqueness policy in `go-mknoon/node/pubsub.go` plus exact live Go node proof in `go-mknoon/node/pubsub_delivery_test.go`. `validateGroupConfigTransportPeerUniqueness` rejects active transport peer IDs reused across different member IDs; `validateGroupConfigIdentityUniqueness` wires that policy through `JoinGroupTopic`, local message/reaction publish, production topic validation, and the pure test validator with distinct `ambiguous_transport_peer` diagnostics. `TestGA021DuplicateTransportPeerAcrossMembersRejectsPolicy` proves a safe C-owned shared transport config accepts C traffic, an ambiguous B/C duplicate active transport config is rejected by policy, initial join stores no topic/config state, pure validation rejects as `reject:ambiguous_transport_peer`, live raw publish emits `group:validation_rejected` reason `ambiguous_transport_peer` with no message/reaction/decrypt/payload-parse/plaintext side effects, and local publish is blocked. Focused GA-021 passed (`ok node 3.611s`), adjacent transport identity selector passed (`ok node 14.722s`), broader node/internal/crypto passed (`ok node 13.119s`, `ok internal 0.480s`, `ok crypto 0.755s`), race passed (`ok node 10.984s`), gofmt and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GA-022 | Malformed peer IDs in config do not crash discovery | Config contains invalid PeerId/TransportPeerId. | 1. Run discovery/dial/counters. 2. Run race/panic detector. | Invalid IDs are ignored with diagnostics; valid members still connect. | P0 | Covered | Required | Required | N/A | Recommended | N/A | Covered by malformed target diagnostics in `go-mknoon/node/pubsub.go` plus exact Go node proof in `go-mknoon/node/pubsub_test.go`. `activeGroupMemberDialTargetSummary` now preserves existing valid-target filtering while counting malformed active config peer IDs ignored during target construction, and discovery/known-member dial summary events expose `ignoredInvalidConfigPeers`. `TestGA022MalformedPeerIDsInConfigDoNotCrashDiscoveryDialAndCounters` proves invalid legacy member peer IDs and invalid active device `TransportPeerId` values are excluded from target and expected-peer counters, known-member direct dial emits the diagnostic count and still connects the valid member, rendezvous discovery emits the diagnostic count, ignores a non-member discovery result, and still connects the valid member without panic. Focused GA-022 passed (`ok node 0.871s`), adjacent malformed discovery selector passed (`ok node 16.388s`), broader node/internal/crypto passed (`ok node 9.021s`, `ok internal 0.437s`, `ok crypto 0.977s`), race passed (`ok node 8.078s`), gofmt and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GA-023 | Config update removes revoked device from discovery allowed set | B device S is revoked. | 1. Rendezvous returns S transport ID. 2. Discover. 3. Inspect ignoredNonMembers. | S is not dialed or counted as expected. | P0 | Covered | Required | Required | N/A | Recommended | N/A | Covered by discovery allowed-set filtering in `go-mknoon/node/pubsub.go` plus exact Go node proof in `go-mknoon/node/pubsub_test.go`. `filterDiscoveredGroupMembers` now treats nil allowed-member sets as unfiltered discovery but treats empty non-nil allowed-member sets as no active members allowed, so a current config with no active remote transports ignores all discovered peers instead of dialing revoked transports. `TestGA023ConfigUpdateRemovesRevokedDeviceFromDiscoveryAllowedSet` proves initial active device S is counted as a remote target, `UpdateGroupConfig` revokes S, stored config active targets, `activeGroupMemberDialTargetSet`, `countRemoteGroupMembers`, and `expectedConnectedGroupMembers` no longer include S, known-member direct dial emits zero total members and does not connect S even with peerstore addresses, and rendezvous discovery returning S emits `ignoredNonMembers == 1` / `newPeers == 0` and does not connect S. Regression-first focused GA-023 exposed the empty-allowed-set bug, then focused GA-023 passed (`ok node 0.786s`), adjacent revoked discovery selector passed (`ok node 45.145s`), broader node/internal/crypto passed (`ok node 12.606s`, `ok internal 0.545s`, `ok crypto 0.830s`), race passed (`ok node 10.444s`), gofmt and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GA-024 | Known-member dialing uses active device transports | Member has Devices list with transportPeerIds. | 1. Call dialKnownGroupMembers. 2. Capture dials. 3. Compare to devices. | Dials each active device transport, not member.PeerId, or explicitly documents single-device-only behavior. | P0 | Covered | Required | Required | Required | Recommended | Required | Covered by active-device known-member dial target selection in `go-mknoon/node/pubsub.go` plus exact Go node proof in `go-mknoon/node/pubsub_test.go`. `activeGroupMemberDialTargets`, `countRemoteGroupMembers`, and `expectedConnectedGroupMembers` use active device `TransportPeerId` values when a member has a `Devices` list. `TestGA024KnownMemberDialingUsesActiveDeviceTransports` builds a logical member with a valid logical `PeerId` plus two active device transports, proves target/counter APIs include device S1/S2 and exclude the logical member peer, seeds peerstore addresses for all three peers, runs `dialKnownGroupMembersDirectOnly`, observes both active devices connected and known-member direct success/discovery summary events, and proves the logical member peer remains undialed. Focused GA-024 passed (`ok node 0.532s`); adjacent active-device dialing selector passed (`ok node 3.163s`); broader node/internal/crypto selector passed (`ok node 51.544s`, `ok internal 0.276s`, `ok crypto 0.898s`); race selector passed (`ok node 3.632s`); gofmt and `git diff --check` passed. No production code changed for GA-024 because existing active-device target selection satisfies the row. Residual-only none; GE-005 is the next unresolved P0 session. |
| GA-025 | Expected peer count uses active devices/current recipients | Group has multi-device members and one removed member. | 1. Call ensureGroupTopicPeersBeforePublish. 2. Inspect expectedPeers. 3. Send. | Expected peer count reflects active reachable transports and does not wait on removed/stale peers. | P0 | Covered | Required | Required | N/A | Recommended | N/A | Covered by publish preflight target-count behavior in `go-mknoon/node/pubsub.go` plus exact Go node proof in `go-mknoon/node/pubsub_test.go`. `ensureGroupTopicPeersBeforePublish` gets `expectedPeers` from `countRemoteGroupMembers`, which uses active device transport targets from `activeGroupMemberDialTargets`. `TestGA025ExpectedPeerCountUsesActiveDevicesCurrentRecipients` builds an admin sender, a logical multi-device member with two active device transports plus a revoked device, and a stale removed-member entry with only a revoked transport. It proves target/counter APIs count exactly the two active device transports, seeds peerstore addresses for active, logical, revoked, and removed transports, sends through `PublishGroupMessage`, verifies `publish_peer_refresh_begin`/`done.expectedPeers == 2`, `direct_dial.totalMembers == 2`, publish returns exactly two topic peers, and logical/revoked/removed peers are not connected. Focused GA-025 passed (`ok node 0.674s`); adjacent peer-count selector passed (`ok node 2.933s`); broader node/internal/crypto selector passed (`ok node 52.248s`, `ok internal 0.591s`, `ok crypto 1.178s`); race selector passed (`ok node 2.235s`); gofmt and `git diff --check` passed. No production code changed for GA-025 because existing active-device target selection satisfies the row. Residual-only none; GE-005 is the next unresolved P0 session. |
| GA-026 | Validation rejection diagnostics are privacy-safe | Invalid X/C messages are rejected. | 1. Cause every reject reason. 2. Inspect emitted data/logs. | Only hashes/truncated fields appear; no group plaintext, full IDs beyond policy, or keys. | P1 | Covered | Required | Recommended | N/A | N/A | N/A | Covered by exact native all-reject-reason diagnostic privacy proof in `go-mknoon/node/pubsub_authorization_forward_test.go`; no production code changed because existing `logPubSubValidationReject` already hashes identifiers. `TestGA026ValidationRejectDiagnosticsArePrivacySafeForAllReasons` emits diagnostics for `not_v3_envelope`, `invalid_envelope`, `group_mismatch`, `peer_mismatch`, `unknown_group`, `ambiguous_signing_key`, `ambiguous_transport_peer`, `non_member`, `unbound_device`, `unauthorized_writer`, `missing_key`, and `bad_signature_or_epoch`, then proves every `group:validation_rejected` event contains only `reason`, `groupHash`, `senderHash`, `transportPeerHash`, `localPeerHash`, `envelopeType`, and `keyEpoch`, every hash is the expected 12-character truncated value or `none`, logs use the same hashed identifiers, and logs/events omit raw group IDs, sender IDs, transport/local peer IDs, message IDs, device IDs, key-package IDs, public keys, signatures, ciphertext, nonce, and plaintext markers. Focused GA-026 passed (`ok node 0.541s`), adjacent validator/auth selector passed (`ok node 54.626s`), broader node/internal/crypto selector passed (`ok node 54.930s`, `ok internal 0.366s`, `ok crypto 0.975s`), selected race selector passed (`ok node 1.643s`), `./scripts/run_test_gates.sh groups` passed (`+159`), gofmt and diff hygiene passed. Residual-only none; GR-002 is the next unresolved session in ordered ledger order. |

## Live PubSub Discovery, Mesh Formation, and Send Semantics

| Test ID | Scenario | Preconditions | Steps | Expected Result | Priority | Current status | Unit | Integration | Smoke | Fake Network | 3-Party E2E | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| GP-001 | Publish fails clearly when group is not joined | Node running but no topic/config/key for G. | 1. Call PublishGroupMessage. 2. Inspect return. | Returns group not joined with msgID empty and peerCount 0. | P0 | Covered | Required | Required | N/A | N/A | N/A | Covered by `go-mknoon/node/pubsub.go::PublishGroupMessage` plus exact Go node proof in `go-mknoon/node/pubsub_delivery_test.go::TestGP001PublishGroupMessageFailsClearlyWhenGroupNotJoined`. The test starts a node without joining the target group, calls `PublishGroupMessage` with valid sender key material and an explicit client message id, proves the error contains `group not joined: <groupId>`, proves the returned message id is empty and peer count is `0`, and verifies no topic/config/key/subscription state is created for the unjoined group. Focused GP-001 passed (`ok node 0.573s`), adjacent publish/unjoined selector passed (`ok node 10.164s`), broader node/internal/crypto selector passed (`ok node 1.743s`, `ok internal 0.273s`, `ok crypto 0.564s`), race passed (`ok node 1.587s`), gofmt and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GP-002 | Publish blocks unauthorized writer before encryption/signing | Sender not allowed to write. | 1. Attempt publish. 2. Instrument encrypt/sign not called. | Returns not allowed; no encrypted payload, pubsub publish, or inbox side effect. | P0 | Covered | Required | Required | N/A | N/A | N/A | Covered by `go-mknoon/node/pubsub.go::PublishGroupMessage` plus exact Go node proof in `go-mknoon/node/pubsub_delivery_test.go::TestGP002PublishBlocksUnauthorizedWriterBeforeEncryptSignAndPublish`. The test joins announcement-group writer scenarios, uses an invalid group-key sentinel to prove unauthorized publish returns `not allowed to write` before encryption while an authorized control reaches `encrypt group message`, uses a valid group key plus invalid private-key sentinel to prove unauthorized publish returns `not allowed to write` before signing while an authorized control reaches `sign group message`, and proves unauthorized attempts return empty message id, peer count `0`, no `group:publish_debug`, and no supplied message-id/text marker event. Focused GP-002 passed (`ok node 1.856s`), adjacent publish/authorization selector passed (`ok node 9.670s`), broader node/internal/crypto selector passed (`ok node 7.616s`, `ok internal 0.576s`, `ok crypto 0.862s`), race passed (`ok node 7.986s`), gofmt and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GP-003 | Publish creates stable caller-provided messageId | Caller supplies messageId. | 1. Publish. 2. Decrypt received payload. 3. Compare event. | Same messageId appears in payload and received event; no new ID generated. | P0 | Covered | Required | Required | Recommended | Recommended | Required | Covered by `go-mknoon/node/pubsub.go::PublishGroupMessage` plus exact Go node proof in `go-mknoon/node/pubsub_delivery_test.go::TestGP003PublishUsesCallerProvidedMessageIDInPayloadAndReceivedEvent`. The test joins a two-node chat group, publishes with caller-provided `messageId`, proves `PublishGroupMessage` returns that exact id with a positive topic peer count, proves sender `group:publish_debug.messageId` equals the provided id, and proves the receiver's decrypted `group_message:received` event carries the same id with expected group, sender, username, text, and key epoch. Focused GP-003 passed (`ok node 1.060s`), adjacent message-id selector passed (`ok node 1.728s`), broader node/internal/crypto selector passed (`ok node 1.708s`, `ok internal 0.523s`, `ok crypto 0.708s`), race passed (`ok node 3.106s`), gofmt and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GP-004 | Publish generates UUID when messageId is empty | Caller supplies empty messageId. | 1. Publish. 2. Validate ID format and received event. | Generated ID is non-empty/stable for that send. | P1 | Covered | Required | Recommended | N/A | N/A | N/A | Covered by `go-mknoon/node/pubsub.go::PublishGroupMessage` plus exact Go node proof in `go-mknoon/node/pubsub_delivery_test.go::TestGP004PublishGeneratesUUIDWhenMessageIDEmpty`. The test joins a two-node chat group, publishes with empty `messageId`, proves `PublishGroupMessage` returns a UUID v4 id with a positive topic peer count, proves sender `group:publish_debug.messageId` equals the generated id, and proves the receiver's decrypted `group_message:received` event carries the same generated id with expected group, sender, username, text, and key epoch. Focused GP-004 passed (`ok node 1.084s`), adjacent generated-id selector passed (`ok node 1.707s`), broader node/internal/crypto selector passed (`ok node 1.710s`, `ok internal 0.868s`, `ok crypto 0.620s`), race passed (`ok node 3.122s`), gofmt and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GP-005 | Publish with zero topic peers still succeeds and reports zero | No live peers in topic. | 1. Publish. 2. Inspect peerCount and events. 3. Verify inbox fallback in app layer. | Go publish returns success if PubSub publish succeeds; app marks sender status honestly and stores durable fallback. | P0 | Covered | Required | Required | Required | Recommended | Required | Covered by tests-only Go and Flutter proof; no production code changed. `go-mknoon/node/pubsub_delivery_test.go::TestGP005PublishWithZeroTopicPeersStillSucceedsAndReportsZero` joins a single-node group, publishes with no other topic peers, and proves `PublishGroupMessage` succeeds with the caller-provided id and `peerCount == 0`. `test/features/groups/application/send_group_message_use_case_test.dart` test `GP-005 zero topic peers records durable fallback custody` forces bridge `group:publish` to return `topicPeers: 0` and proves the use case returns `successNoPeers`, persists sender status `sent`, records `inboxStored == true`, clears retry/wire payloads, stores `group:inboxStore` for the recipient, and emits `success_no_peers` timing with `topicPeers: 0`. `test/features/groups/integration/group_resume_recovery_test.dart` test `publish with zero peers falls back to inbox` proves the inbox fallback drains to the offline recipient. Focused GP-005 Go, adjacent Go publish selector, focused GP-005 Flutter, app zero-peer selector, focused integration fallback proof, gofmt, dart format, and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GP-006 | Publish with partial peers refreshes before send | Only one of several recipients is in topic. | 1. Set topic peers partial. 2. Publish. 3. Capture discovery events/dials. | Known members are dialed, peerCount may increase, and missing peers receive via inbox fallback. | P0 | Covered | Required | Required | Recommended | Recommended | Required | Covered by tests-only Go and Flutter proof; no production code changed. `go-mknoon/node/pubsub_delivery_test.go::TestGP006PublishWithPartialPeersRefreshesKnownMembersBeforeSend` builds A/B/C with A initially seeing only B as a live topic peer, adds C's direct addresses immediately before publish, and proves publish preflight emits `publish_peer_refresh_begin` with `topicPeers == 1` / `expectedPeers == 2`, emits C-specific `known_member_dial_success`, emits `direct_dial`, emits `publish_peer_refresh_done` with promoted topic peers, returns `peerCount >= 2`, and both B and C receive the message. `test/features/groups/application/send_group_message_use_case_test.dart` test `GP-006 partial peers keep durable fallback for recipients` forces bridge `topicPeers: 1` with two recipients and proves normal `success`, sender status `sent`, `inboxStored == true`, cleared retry/wire payloads, `group:inboxStore` for both recipient peer ids, and success timing with `topicPeers: 1`. `test/features/groups/integration/group_resume_recovery_test.dart` test `partial delivery with inbox drain completion` proves one online reader receives before drain while offline readers receive once after inbox drain. Focused GP-006 Go, adjacent Go publish-refresh selector, focused GP-006 Flutter, focused partial-delivery integration proof, gofmt, dart format, and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GP-007 | Publish does not wait too long for zero-peer group | No live peers; user taps send. | 1. Measure publish preflight duration. 2. Compare to GroupPublishZeroPeerSettleWait. | Send path uses bounded wait and delegates reliability to inbox/retry; no UI hang. | P1 | Covered | Required | Required | N/A | Recommended | N/A | Covered on 2026-05-14 by exact native, app, and integration zero-peer bounded-send proofs. `go-mknoon/node/pubsub_delivery_test.go::TestGP007ZeroPeerPublishUsesBoundedSettleWait` joins a sender plus one valid offline member so expected peers are non-zero while live topic peers are zero, publishes with a caller id, proves `peerCount == 0`, bounds elapsed publish time near `GroupPublishZeroPeerSettleWait`, proves `publish_peer_refresh_begin` saw `topicPeers == 0` / `expectedPeers == 1`, proves `publish_peer_refresh_done.settleWaitMs == GroupPublishZeroPeerSettleWait.Milliseconds()`, and asserts the zero-peer settle wait remains below the partial-peer wait. `test/features/groups/application/send_group_message_use_case_test.dart::GP-007 zero topic peers complete without retry staging and use inbox` proves the Flutter send path returns `successNoPeers` within one second, persists `sent`, records durable inbox custody, clears retry/wire payloads, stores the recipient inbox payload, and emits success-no-peers/timing details with `topicPeers == 0`, `inboxStored == true`, `inboxPending == false`, and `elapsedMs < 1000`. `test/features/groups/integration/group_resume_recovery_test.dart::GP-007 zero-peer send delegates to inbox without visible delay` proves the GroupTestUser bridge path completes under one second, does not stage retry, writes the sender row as sent/inbox-stored, and drains the inbox to the offline recipient exactly once. Passed evidence: gofmt, Dart format, exact Go GP-007 (`ok node 1.111s`), adjacent Go publish/discovery selector (`ok node 17.397s`), broader node/internal/crypto selector (`ok node 15.297s`, `ok internal 0.880s`, `ok crypto 1.152s`), selected race selector (`ok node 3.336s`), exact app proof (`+1`), app zero-peer matrix selector (`+24`), exact integration proof (`+1`), adjacent integration zero-peer selector (`+2`), named `./scripts/run_test_gates.sh groups` (`+160`), and diff hygiene. Residual-only none; GR-002 is the next unresolved session in ordered ledger order. |
| GP-008 | Publish peer refresh uses latest config after add/remove | C removed and D added just before publish. | 1. Update config. 2. Publish. 3. Inspect dials/expectedPeers. | Dial/expected recipients use latest active membership. | P0 | Covered | Required | Required | Recommended | Recommended | Required | Covered by code-plus-tests evidence in `go-mknoon/node/pubsub.go`, `go-mknoon/node/pubsub_delivery_test.go::TestGP008PublishPeerRefreshUsesLatestConfigAfterAddRemove`, and `test/features/groups/application/send_group_message_use_case_test.dart::GP-008 latest membership drives durable fallback after remove add`. The Go regression starts A with old A/B/C config and one live peer B, seeds removed C and newly added D addresses, updates A to current A/B/D immediately before publish, and proves publish refresh expects two current peers, dials D, emits no known-member dial event for removed C after the update, returns promoted peer count, and delivers to B/D without delivering to C. `pubsub.go::isCurrentActiveGroupDialTarget` now revalidates discovered, known-member, and pre-relay direct dial targets against the latest stored config before dialing, closing the stale background discovery snapshot gap exposed by the first focused run. The Flutter app test mutates membership from B/C to B/D before send and proves durable `group:inboxStore` recipients are B/D and exclude removed C. Focused GP-008 Go passed (`ok node 2.643s`), adjacent `GP008\|GM030\|GM031\|UpdateGroupConfig\|KnownMemberDial\|PublishGroupMessage` selector passed (`ok node 6.902s`), focused GP-008 Flutter passed (`+1`), gofmt, dart format, and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GP-009 | Discovery loop registers and discovers after relay readiness | Group joined before relayReady closes. | 1. Join group. 2. Delay relayReady. 3. Close relayReady. 4. Inspect events. | Direct pre-relay recovery happens first, then relay-assisted register/discover starts. | P1 | Covered | Required | Required | N/A | Recommended | N/A | Covered on 2026-05-14 by exact native relay-readiness discovery proof. `go-mknoon/node/pubsub_delivery_test.go::TestGP009GroupDiscoveryRegistersAndDiscoversAfterRelayReady` joins a private group while node A's `relayReady` channel is still open, waits for `pre_relay_direct_dial` before readiness with `membersDialed == 0` and `noDirectAddr == 1`, proves relay dial, rendezvous register, and rendezvous discover hooks have zero calls before `relayReady` closes, then closes `relayReady` and proves known-member relay dial success, `registered`, `discover_result`, and hook sequence `register:<group namespace>` followed by `discover:<group namespace>`. Existing GR-014 remains adjacent late-relay delivery proof. Passed evidence: gofmt, exact GP-009 Go (`ok node 2.901s`), adjacent discovery selector (`ok node 16.204s`), broader node/internal/crypto selector (`ok node 13.876s`, `ok internal 0.937s`, `ok crypto 0.683s`), selected race selector (`ok node 14.430s`), named `./scripts/run_test_gates.sh groups` (`+160`), and diff hygiene. Residual-only none; GR-002 is the next unresolved session in ordered ledger order. |
| GP-010 | Discovery loop exits and unregisters on leave | Group discovery was registered. | 1. Leave group. 2. Cancel context. 3. Inspect RendezvousUnregister. | Unregister called once if registered; no further discovery events. | P0 | Covered | Required | Required | N/A | Recommended | N/A | Covered by code-plus-test Go node proof. `go-mknoon/node/rendezvous.go::RendezvousUnregister` now has a narrow `rendezvousUnregisterHook` test seam matching the existing register/discover hooks in `go-mknoon/node/node.go`. `go-mknoon/node/pubsub_unsubscribe_exit_paths_test.go::TestGP010DiscoveryLoopUnregistersOnceAndStopsAfterLeave` joins a group, waits for group rendezvous registration, leaves via `LeaveGroupTopic`, proves exactly one unregister call for `groupRendezvousNamespace(groupId)`, verifies group topic/subscription/config/key/discovery state is removed, and asserts no further discovery events or discover hook calls after leave. Focused GP-010 passed (`ok node 4.480s`), adjacent GP010/GL008/LeaveGroupTopic/GroupPeerDiscoveryLoop/RendezvousUnregister selector passed (`ok node 26.448s`), gofmt and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GP-011 | Rendezvous discovery filters non-members | Rendezvous returns B, C removed, X unknown. | 1. Run discoverAndConnectGroupPeers. 2. Inspect dial attempts. | Only current active member peers are dialed; ignoredNonMembers count is accurate. | P0 | Covered | Required | Required | N/A | Recommended | N/A | Covered by tests-only Go node proof in `go-mknoon/node/pubsub_test.go::TestGP011RendezvousDiscoveryFiltersNonMembers`; no production code changed. The test configures current group membership as admin plus active B, makes rendezvous return current B, removed C, and unknown X with direct addresses, runs `discoverAndConnectGroupPeers`, proves only B connects, proves C and X remain disconnected and are not imported into the peerstore before filtering, and verifies `discover_result` reports `totalFound == 3`, `newPeers == 1`, and `ignoredNonMembers == 2`. Focused GP-011 passed (`ok node 0.679s`), adjacent GP011/GM030/RP017/GL005/filter selector passed (`ok node 3.835s`), gofmt and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GP-012 | Rendezvous discovery handles invalid peer IDs safely | Rendezvous/config includes invalid peer IDs. | 1. Run discover. 2. Inspect events and no panic. | Invalid entries skipped; valid members still attempted. | P1 | Covered | Required | Required | N/A | Recommended | N/A | Covered on 2026-05-14 by exact native invalid-peer discovery proof. `go-mknoon/node/pubsub_test.go::TestGP012RendezvousDiscoverySkipsInvalidPeerIDsAndDialsValidMember` seeds a group config with one valid member plus invalid legacy and active-device transport peer IDs, makes rendezvous return one invalid discovered peer ID plus one valid discovered member, runs `discoverAndConnectGroupPeers`, proves `discover_result` reports `totalFound == 2`, `newPeers == 1`, `ignoredNonMembers == 1`, and `ignoredInvalidConfigPeers == 2`, proves the valid member connects, and proves the invalid rendezvous peer is not imported into the peerstore. Passed evidence: gofmt, exact GP-012 Go (`ok node 0.583s`), adjacent discovery/filter selector (`ok node 3.938s`), broader node/internal/crypto selector (`ok node 3.620s`, `ok internal 1.193s`, `ok crypto 0.934s`), selected race selector (`ok node 3.546s`), named `./scripts/run_test_gates.sh groups` (`+160`), and diff hygiene. Residual-only none; GR-002 is the next unresolved session in ordered ledger order. |
| GP-013 | Direct address preference excludes relay circuit addrs | Peerstore has direct and relay-circuit addresses. | 1. collectDirectMultiaddrs. 2. connectGroupPeerPreferDirect. 3. Inspect attempted path. | Direct attempt only uses non-circuit addresses; relay fallback separate. | P1 | Covered | Required | Recommended | N/A | N/A | N/A | Covered on 2026-05-14 by exact native direct-address preference proof. `go-mknoon/node/pubsub_test.go::TestGP013DirectAddressPreferenceExcludesRelayCircuitAddrs` builds a mixed peerstore/candidate set containing a real direct address and a synthetic `/p2p-circuit` relay address for the same peer, proves `collectDirectMultiaddrs` returns only deduped non-circuit addresses, then proves `connectGroupPeerPreferDirect` records `AttemptedDirect`, reports the direct-only address count, succeeds with `Path == direct`, leaves `UsedRelayFallback == false`, and establishes a direct libp2p connection. Passed evidence: gofmt, exact GP-013 Go (`ok node 0.572s`), adjacent direct/relay selector (`ok node 0.418s`), broader node/internal/crypto selector (`ok node 0.435s`, `ok internal 0.894s`, `ok crypto 0.649s`), selected race selector (`ok node 1.792s`), named `./scripts/run_test_gates.sh groups` (`+160`), and diff hygiene. Residual-only none; GR-002 is the next unresolved session in ordered ledger order. |
| GP-014 | Relay fallback occurs when direct connect succeeds but topic peer missing | Direct connection exists but not a live pubsub peer. | 1. connect direct. 2. waitForLiveGroupTopicPeer false. 3. Verify relay fallback. | DialPeerViaRelay is attempted and event includes relay_fallback or topic_missing. | P0 | Covered | Required | Required | N/A | Recommended | N/A | Covered by code-plus-test Go node proof. `go-mknoon/node/node.go::DialPeerViaRelay` now has a narrow `dialPeerViaRelayHook` test seam. `go-mknoon/node/pubsub_test.go::TestGP014RelayFallbackAfterDirectConnectTopicMissing` configures a known group member with direct addresses but no live topic subscription, runs `dialKnownGroupMembers`, proves the direct host connection succeeds, proves `DialPeerViaRelay` is attempted exactly once for that peer after the topic wait misses, and verifies the `known_member_topic_missing` event reports `path == relay_fallback`, `attemptedDirect == true`, `usedRelayFallback == true`, and positive direct address count. Focused GP-014 passed (`ok node 0.505s`), adjacent GP014/topic_missing/relay_fallback/KnownMemberDial/DialPeerViaRelay/connectGroupPeerPreferDirect selector passed (`ok node 5.567s`), gofmt and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GP-015 | Connected host peer is not treated as live topic peer | Peer is connected at libp2p level but has not subscribed. | 1. Connect B without topic. 2. Publish. 3. Inspect peer count and inbox fallback. | PeerCount reflects topic.ListPeers, not host connections; durable fallback still used. | P0 | Covered | Required | Required | Recommended | Recommended | Required | Covered by tests-only Go and Flutter proof; no production code changed. `go-mknoon/node/pubsub_delivery_test.go::TestGP015ConnectedHostPeerDoesNotCountAsLiveTopicPeer` connects B at the libp2p host level without B joining the group topic, proves A sees B as `network.Connected`, proves `countConnectedGroupMembers`/`liveGroupTopicPeerSet` remain zero, publishes from A, verifies `PublishGroupMessage` returns `peerCount == 0`, verifies sender `group:publish_debug.topicPeers == 0`, and proves B receives no group event. Flutter test `GP-015 zero live topic peers keep durable fallback` forces bridge `topicPeers: 0` and proves `successNoPeers`, persisted sender status `sent`, `inboxStored == true`, cleared retry/wire payloads, and `group:inboxStore` custody for the recipient. Focused GP-015 Go passed (`ok node 2.745s`), adjacent GP015/GP014/GP005/topic-peer selector passed (`ok node 3.323s`), focused GP-015 Flutter passed (`+1`), gofmt, dart format, and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GP-016 | Dial cooldown prevents storms but not permanent starvation | Peer dial fails repeatedly then recovers. | 1. Force failures. 2. Advance fake time. 3. Allow success. 4. Send. | Backoff increases then clears on success; peer eventually reconnects and receives. | P0 | Covered | Required | Required | N/A | Recommended | N/A | Covered by tests-only Go node proof in `go-mknoon/node/pubsub_delivery_test.go::TestGP016DialCooldownBacksOffThenClearsOnRecoveredDelivery`; no production code changed. The test joins A/B to the same private chat group while A has no B direct addresses and no relay fixture, forces known-member dial failure, proves an immediate retry emits `direct_dial_skipped_cooldown` without increasing failure count, advances the test-owned cooldown state by moving `nextAllowed` to the past, repeats failures until `groupPeerDialBackoff` increases, then adds B direct addresses, advances cooldown again, proves `known_member_dial_success` via direct path with positive direct address count, verifies `groupDialBackoff` clears on success, publishes a row-owned message, and proves B receives it. Focused GP-016 passed (`ok node 1.124s`), adjacent cooldown/discovery/publish selector passed (`ok node 1.786s`), gofmt and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GP-017 | In-flight dial gate blocks duplicate dials only while active | Two discovery cycles target same peer. | 1. Start dial and hold. 2. Start second cycle. 3. Finish first. 4. Third cycle. | Second is skipped as inflight; third can retry according to success/failure state. | P1 | Covered | Required | Required | N/A | Recommended | N/A | Covered on 2026-05-14 by exact native in-flight dial gate proof. `go-mknoon/node/pubsub_test.go::TestGP017InFlightDialGateBlocksOnlyWhileActive` proves the first `beginGroupPeerDialWithMode` call is allowed, a second discovery cycle for the same peer while the dial is active is blocked with `retryIn == 0` and `blockedByInFlight == true`, a successful `finishGroupPeerDial` clears the active gate so the third cycle can run, and a failed `finishGroupPeerDial` clears the in-flight gate while leaving normal cooldown policy in force until `groupPeerDialBackoff(1)` expires. Passed evidence: gofmt, exact GP-017 Go (`ok node 0.499s`), adjacent in-flight/backoff selector (`ok node 0.359s`), broader node/internal/crypto selector (`ok node 0.390s`, `ok internal 0.623s`, `ok crypto 0.939s`), selected race selector (`ok node 1.688s`), named `./scripts/run_test_gates.sh groups` (`+160`), and diff hygiene. Residual-only none; GR-002 is the next unresolved session in ordered ledger order. |
| GP-018 | Warm retry cadence keeps active group responsive | Some expected members missing after initial discovery. | 1. Simulate missing members. 2. Advance loop. 3. Inspect intervals. | Loop uses warm interval for bounded retries, then backs off; no 30s avoidable gap during foreground catch-up. | P1 | Covered | Required | Required | N/A | Recommended | N/A | Covered on 2026-05-14 by exact native cadence proof plus narrow helper extraction. `go-mknoon/node/pubsub.go` now exposes `initialGroupDiscoveryCadence` and `nextGroupDiscoveryCadence` from the existing discovery-loop cadence policy, and `go-mknoon/node/pubsub_test.go::TestGP018WarmRetryCadenceKeepsActiveGroupResponsive` proves a partially connected active group starts at `GroupDiscoveryWarmInterval`, stays on the warm interval for the bounded `GroupDiscoveryWarmRetries` no-progress window, avoids an immediate 30s maintenance gap, then begins backoff from `GroupDiscoveryWarmInterval * 2`; partial progress resets the failure streak and replenishes warm retries. Passed evidence: gofmt, exact GP-018 Go (`ok node 0.493s`), adjacent warm/backoff discovery selector (`ok node 11.407s`), broader node/internal/crypto selector (`ok node 13.306s`, `ok internal 1.570s`, `ok crypto 1.038s`), selected race selector (`ok node 14.474s`), named `./scripts/run_test_gates.sh groups` (`+160`), and diff hygiene. Residual-only none; GR-002 is the next unresolved session in ordered ledger order. |
| GP-019 | Discovery backoff resets after partial progress | Several members missing, one connects. | 1. BeforeConnected low. 2. AfterConnected higher but below expected. 3. Inspect next interval. | Interval remains warm and failure streak resets. | P1 | Covered | Required | Required | N/A | Recommended | N/A | Covered on 2026-05-14 by exact native partial-progress cadence proof. `go-mknoon/node/pubsub_test.go::TestGP019DiscoveryBackoffResetsAfterPartialProgress` seeds an already-backed-off discovery cadence with missing expected members, advances connected members from `1` to `2` while expected remains `4`, and proves the next interval resets to `GroupDiscoveryWarmInterval`, `backingOff == false`, consecutive failures reset to zero, and `GroupDiscoveryWarmRetries` is replenished. The same test then proves the next no-progress cycle consumes the refreshed warm retry budget instead of immediately backing off again. Passed evidence: gofmt, exact GP-019 Go (`ok node 0.663s`), adjacent warm/backoff discovery selector (`ok node 13.378s`), broader node/internal/crypto selector (`ok node 13.525s`, `ok internal 1.114s`, `ok crypto 0.548s`), selected race selector (`ok node 12.665s`), named `./scripts/run_test_gates.sh groups` (`+160`), and diff hygiene. Residual-only none; GR-002 is the next unresolved session in ordered ledger order. |
| GP-020 | All expected connected returns to maintenance cadence | All current members are live topic peers. | 1. Simulate all connected. 2. Inspect interval. | Loop returns to GroupDiscoveryInterval and resets warm retry count. | P1 | Covered | Required | Recommended | N/A | N/A | N/A | Covered on 2026-05-14 by exact native maintenance-cadence proof. `go-mknoon/node/pubsub_test.go::TestGP020AllExpectedConnectedReturnsToMaintenanceCadence` seeds an already-backed-off discovery cadence, advances connected members to all expected peers (`afterConnected == expectedConnected`), and proves the next interval returns to `GroupDiscoveryInterval`, `backingOff == false`, consecutive failures reset to zero, and `GroupDiscoveryWarmRetries` is replenished. The same test proves the zero-expected-member path also uses maintenance cadence without backoff and resets warm retry state. Passed evidence: gofmt, exact GP-020 Go (`ok node 0.616s`), adjacent warm/backoff discovery selector (`ok node 11.056s`), broader node/internal/crypto selector (`ok node 12.188s`, `ok internal 0.300s`, `ok crypto 0.899s`), selected race selector (`ok node 12.271s`), named `./scripts/run_test_gates.sh groups` (`+160`), and diff hygiene. Residual-only none; GR-002 is the next unresolved session in ordered ledger order. |
| GP-021 | Message receive emits sender/device/transport fields correctly | Valid incoming group_message. | 1. Deliver envelope with explicit device fields. 2. Inspect event. | Event includes groupId, senderId, senderDeviceId, transportPeerId, username, keyEpoch, text, timestamp, messageId. | P0 | Covered | Required | Required | Recommended | Recommended | Required | Covered by tests-only Go node proof in `go-mknoon/node/pubsub_delivery_test.go::TestGP021MessageReceiveEmitsIdentityTransportAndMessageFields`; no production code changed. The test builds a valid encrypted `group_message` envelope from an active member device with explicit `senderDeviceId`, `senderTransportPeerId`, device public key, and key package id, includes payload `username`, `text`, `timestamp`, and `extra.messageId`, validates the envelope for the sender transport, publishes it through a live local PubSub connection, and proves the receiver emits `group_message:received` with `groupId`, `senderId`, `senderDeviceId`, `transportPeerId`, `senderUsername`, `keyEpoch`, `text`, `timestamp`, and `messageId`. It also asserts no validation, decryption, or payload-parse diagnostic for the valid row-owned envelope. Focused GP-021 passed (`ok node 2.582s`), adjacent receive-event selector passed (`ok node 4.555s`), gofmt and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GP-022 | Receive path emits decryption_failed with enough diagnostics | Local key is wrong or stale. | 1. Deliver valid envelope under another key. 2. Inspect event. | group:decryption_failed includes groupId, senderId, keyEpoch, localKeyEpoch, error, decryptMs. | P0 | Covered | Required | Required | N/A | Recommended | N/A | Covered by tests-only Go node proof in `go-mknoon/node/pubsub_decryption_failure_test.go::TestGP022ReceivePathEmitsDecryptionFailedDiagnosticsForWrongLocalKey`; no production code changed. The test publishes a valid signed/encrypted group envelope under the sender's group key while the receiver has a different local key at the same epoch, proves the live receive path emits `group:decryption_failed` with `groupId`, `senderId`, envelope `keyEpoch`, receiver `localKeyEpoch`, an AES-GCM decrypt error, and non-negative numeric `decryptMs`, and asserts no `group_message:received`, `group_reaction:received`, or plaintext leak is emitted after the failure. Focused GP-022 passed (`ok node 2.638s`), adjacent decryption diagnostics selector passed (`ok node 4.601s`), gofmt and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GP-023 | Receive path continues after malformed message | Subscription receives malformed then valid message. | 1. Deliver bad JSON. 2. Deliver valid. 3. Inspect events. | Bad message is skipped; valid later message still emits; handler does not exit. | P0 | Covered | Required | Required | N/A | Recommended | N/A | Covered by tests-only Go node proof in `go-mknoon/node/pubsub_decryption_failure_test.go::TestGP023ReceivePathContinuesAfterMalformedPayload`; no production code changed. The test publishes a valid signed/encrypted `group_message` envelope whose decrypted plaintext is malformed/non-JSON, observes `group:payload_parse_failed`, proves no malformed `group_message:received` or plaintext marker is emitted, then publishes a valid signed/encrypted message on the same live subscription and proves the receiver emits `group_message:received` with expected group, sender, text, timestamp, and message id. Focused GP-023 passed (`ok node 2.159s`), adjacent malformed receive selector passed (`ok node 26.459s`), gofmt and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GP-024 | Subscription error exits only on real subscription failure | sub.Next returns context cancellation vs transient error. | 1. Simulate ctx canceled. 2. Simulate error. 3. Inspect goroutine. | Context cancellation exits quietly; non-context error logs and returns according to contract. | P1 | Covered | Required | Recommended | N/A | N/A | N/A | Covered on 2026-05-14 by a narrow native subscription-error classifier and exact row-owned proof. `go-mknoon/node/pubsub.go::handleGroupSubscription` now delegates error classification to `shouldLogGroupSubscriptionError`, which exits quietly for a canceled loop context, `context.Canceled`, and `context.DeadlineExceeded`, while still logging and returning for real non-context subscription failures. `go-mknoon/node/pubsub_test.go::TestGP024SubscriptionErrorLogsOnlyRealFailures` proves canceled context errors, direct canceled/deadline errors, nil errors, and non-context failures are classified correctly. Passed evidence: gofmt, exact GP-024 Go (`ok node 0.538s`), adjacent subscription-handler selector (`ok node 17.200s`), broader node/internal/crypto selector (`ok node 17.484s`, `ok internal 0.340s`, `ok crypto 0.926s`), selected race selector (`ok node 18.948s`), named `./scripts/run_test_gates.sh groups` (`+160`), and diff hygiene. Residual-only none; GR-002 is the next unresolved session in ordered ledger order. |
| GP-025 | Duplicate live deliveries are deduped by app layer | Same envelope delivered twice by PubSub. | 1. Deliver duplicate same messageId. 2. Inspect timeline/status. | Only one visible message exists; duplicate may update metadata but not duplicate bubble. | P0 | Covered | Recommended | Required | Required | Recommended | Required | Covered by tests-only Flutter app proof in `test/features/groups/application/group_message_listener_test.dart::GP-025 LP013 duplicate PubSub delivery preserves first row and notification state` plus adjacent lower-layer proof in `test/features/groups/application/handle_incoming_group_message_use_case_test.dart::deduplicates by messageId when pubsub and group inbox deliver same message`; no production code changed. The listener test sends two live `group_message:received`-shaped events with the same `messageId`, conflicting duplicate text/timestamp/key/quote/media metadata, and paused-app notification settings; it proves one repository row, one `groupMessageStream` emission, one local notification, one unread count, and first trusted row/attachment metadata preserved. Focused GP-025 listener passed (`+1`), adjacent message-id dedupe passed (`+1`), dart format and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GP-026 | Live plus inbox duplicate delivery is deduped | Message arrives live and later from inbox. | 1. Deliver live. 2. Retrieve same envelope. 3. Inspect timeline. | One visible message with merged delivery state. | P0 | Covered | Recommended | Required | Required | Required | Required | Covered by tests-only Flutter app proof in `test/features/groups/integration/group_resume_recovery_test.dart::GP-026 same message is not duplicated if both pubsub and group inbox deliver it` and `test/features/groups/application/drain_group_offline_inbox_use_case_test.dart::GP-026 GMAR-004 duplicate live plus inbox replay enriches video and voice media once`; no production code changed. The resume-recovery test persists the live copy, drains a signed inbox copy with the same `messageId` and tampered content, and proves one incoming message remains with original trusted text. The offline-drain test persists a sparse live copy, drains a signed inbox replay with quote/video/voice metadata, proves one stored row, enriches `quotedMessageId` and two media attachments exactly once, and re-drains without duplication. Focused GP-026 resume-recovery passed (`+1`), focused GP-026 offline-drain passed (`+1`), dart format and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GP-027 | Out-of-order live messages render in deterministic order | A/B send with timestamps that arrive out of order. | 1. Deliver M2 then M1. 2. Persist/render. 3. Restart. | Timeline order follows product rule and is stable after restart. | P1 | Covered | Recommended | Required | N/A | Recommended | Required | Covered on 2026-05-14 by exact wired conversation live-upsert and restart proof. Existing timeline ordering code already routes both live and persisted views through `lib/features/groups/domain/utils/group_message_ordering.dart::orderGroupMessagesForTimeline`, `lib/features/groups/domain/repositories/group_message_repository_impl.dart::getMessagesPage`, and `lib/features/groups/presentation/screens/group_conversation_wired.dart::_upsertMessage`; no production runtime change was required. `test/features/groups/presentation/group_conversation_wired_test.dart::GP-027 out-of-order live messages keep deterministic order after restart` persists and streams later timestamp `M2` before earlier timestamp `M1`, proves the live `GroupConversationScreen` renders `M1` then `M2`, disposes/rebuilds the screen against the same repository, and proves the restarted load keeps `M1` then `M2`. Passed evidence: Dart format, exact GP-027 widget proof (`+1`), adjacent live-upsert widget selector (`+3`), adjacent repository ordering selector (`+3`), named `./scripts/run_test_gates.sh groups` (`+160`), and diff hygiene. Residual-only none; GR-002 is the next unresolved session in ordered ledger order. |
| GP-028 | High-volume burst does not lose messages | A sends many messages while adding/removing D. | 1. Send 100+ messages. 2. Mutate membership at fixed offsets. 3. Verify per-window receipt. | All entitled recipients receive exactly once; excluded members receive none after cutoff. | P0 | Covered | Recommended | Required | Required | Required | Required | Covered by Flutter fake-network integration proof in `test/features/groups/integration/group_edge_cases_smoke_test.dart`. `GP-028 high-volume burst keeps entitlement windows exact while adding and removing Diana` sends 120 explicit-id messages from Alice while adding Diana at offset 30 and removing her at offset 90; it reads an expanded repository page so the default 50-message page cannot mask delivery, then proves Alice stores all 120 outgoing messages once, Bob and Charlie receive all 120 once, Diana receives exactly ids 030-089 once, Diana receives no id at or after 090, and Diana is unsubscribed after removal. Focused GP-028 passed (`+1`), adjacent rapid-burst selector passed (`+1`), `dart format --set-exit-if-changed` passed, and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |

## Group Inbox Store, Offline Replay, Deduplication, and Repair

| Test ID | Scenario | Preconditions | Steps | Expected Result | Priority | Current status | Unit | Integration | Smoke | Fake Network | 3-Party E2E | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| GI-001 | GroupInboxStore fails clearly when node not started | Node host is nil. | 1. Call GroupInboxStore. 2. Inspect error. | Returns node not started; no stream attempt. | P0 | Covered | Required | Recommended | N/A | N/A | N/A | Covered by tests-only Go node proof in `go-mknoon/node/group_inbox_test.go::TestGI001GroupInboxStoreRequiresStartedNodeBeforeRelayStream`; no production code changed. The test starts a reachable local libp2p relay with an `InboxProtocol` stream handler counter, configures an unstarted `NewNode()` with that relay address, calls `GroupInboxStore`, asserts exact error `node not started`, and asserts `streamAttempts == 0`, proving no relay stream is attempted before node start. Focused GI-001 passed (`ok node 0.520s`), adjacent GroupInbox selector passed (`ok node 0.355s`), gofmt and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GI-002 | GroupInboxStore sends group_store request shape | Node started with fake relay. | 1. Call store. 2. Capture framed request JSON. | Action is group_store with groupId, from, encrypted message, and recipientPeerIds. | P0 | Covered | Required | Required | N/A | N/A | N/A | Covered by tests-only Go node proof in `go-mknoon/node/group_inbox_test.go::TestGI002GroupInboxStoreSendsGroupStoreRequestShape`; no production code changed. The test starts a local libp2p fake relay, captures the framed request from a started node's `GroupInboxStore`, returns `{"status":"OK"}`, and proves the JSON request carries `action == group_store`, requested `groupId`, node peer id in `from`, exact opaque encrypted `message`, and ordered `recipientPeerIds`. Focused GI-002 passed (`ok node 0.526s`), adjacent GroupInbox/protocol selector passed (`ok node 0.384s`), gofmt and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GI-003 | GroupInboxStore does not include plaintext push title/body unless explicitly allowed | pushTitle/body provided. | 1. Call store. 2. Capture request. 3. Search plaintext. | Request does not leak plaintext title/body; notification behavior is separately covered if needed. | P0 | Covered | Required | Required | N/A | N/A | N/A | Covered by tests-only Go node proof in `go-mknoon/node/group_inbox_test.go::TestGI003GroupInboxStoreOmitsPlaintextPushPreviewFields`; no production code changed. The test starts a local libp2p fake relay, captures the raw relay-visible request frame from a started node's `GroupInboxStore`, passes unique plaintext `pushTitle` and `pushBody`, asserts the JSON contains neither retired key names nor either plaintext string, and confirms the normal `group_store` metadata plus exact opaque message remain intact. Focused GI-003 passed (`ok node 0.569s`), adjacent GroupInbox push/privacy selector passed (`ok node 0.421s`), gofmt and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GI-004 | GroupInboxStore includes only current recipients | Group membership changed. | 1. Send after remove/re-add. 2. Capture recipientPeerIds. | Recipients exactly match current entitlement window; removed members excluded, re-added included only after re-add. | P0 | Covered | Recommended | Required | Required | Required | Required | Covered by tests-only Flutter application proof in `test/features/groups/application/send_group_message_use_case_test.dart::GI-004 group inbox recipients follow current remove and re-add entitlement windows`; no production code changed. The test seeds Alice/Bob/Charlie, removes Charlie, sends during the removed window, proves `group:inboxStore.recipientPeerIds == ['peer-2']` while excluding Alice/self and Charlie/removed, then re-adds Charlie and proves the post-readd inbox recipients are exactly Bob and Charlie with no sender or duplicates. Focused GI-004 passed (`+1`), adjacent GM-019 removed/readd recipient window passed (`+1`), adjacent GP-008 latest membership remove/add recipient proof passed (`+1`), dart format and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GI-005 | GroupInboxStore retries relays in order | Multiple relays; first fails, second succeeds. | 1. Inject connect/open/write/read failure on first. 2. Second returns OK. | Overall store succeeds and does not retry successful relay unnecessarily. | P0 | Covered | Required | Required | N/A | Recommended | N/A | Covered by tests-only Go node proof in `go-mknoon/node/group_inbox_test.go::TestGI005GroupInboxStoreRetriesRelaysInOrderAndStopsOnSuccess`; no production code changed. The test starts two local fake relays, configures the node relay list as first then second, makes the first relay return non-OK after reading the request, makes the second relay return OK, proves `GroupInboxStore` succeeds, and asserts the attempt sequence is exactly `first`, `second` with no extra attempt after success. Focused GI-005 passed (`ok node 0.458s`), adjacent relay/group-inbox selector passed (`ok node 0.448s`), gofmt and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GI-006 | GroupInboxStore returns error after all relays fail | All relays fail. | 1. Inject failures. 2. Inspect error/status. | Caller receives actionable error and can stage retry; no message marked durable. | P0 | Covered | Required | Required | Recommended | Recommended | N/A | Covered by tests-only Go and Flutter proof. `go-mknoon/node/group_inbox_test.go::TestGI006GroupInboxStoreReturnsErrorAfterAllRelaysFail` configures two fake relays that both read the request and return non-OK, proves `GroupInboxStore` returns an error containing `all 2 relays failed` plus the last actionable relay error, and proves both relays are attempted in order. `test/features/groups/application/send_group_message_use_case_test.dart::GI-006 inbox failure leaves message pending with retry payload and no durable mark` forces inbox store failure while publish succeeds, then proves returned/saved message status `pending`, `inboxStored == false`, retry payload retained for Bob, wire envelope cleared, and failure diagnostics emitted. Focused GI-006 Go passed (`ok node 0.489s`), focused GI-006 Flutter passed (`+1`), adjacent Go all-relays selector passed (`ok node 0.597s`), adjacent Flutter inbox-fail selector passed (`+1`), gofmt, dart format, and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GI-007 | GroupInboxStore handles relay non-OK status | Relay returns ERROR with message. | 1. Store. 2. Relay responds non-OK. 3. Inspect error. | Error includes relay reason and no success status emitted. | P0 | Covered | Required | Required | N/A | N/A | N/A | Covered by tests-only Go and Flutter proof. `go-mknoon/node/group_inbox_test.go::TestGI007GroupInboxStoreReturnsRelayNonOKError` starts one fake relay returning `{"status":"ERROR","error":"quota exceeded"}`, proves `GroupInboxStore` returns the selector wrapper plus `group inbox store failed: quota exceeded`, and verifies exactly one relay attempt. `test/features/groups/application/send_group_message_use_case_test.dart::GI-007 relay non-OK status leaves message pending with retry payload` uses `_InboxStoreOkFalseBridge` with publish success, then proves returned/saved messages remain `pending`, `inboxStored == false`, and retain retry payload for Bob. Focused GI-007 Go passed (`ok node 0.526s`), focused GI-007 Flutter passed (`+1`), adjacent Go group inbox selector passed (`ok node 0.422s`), adjacent Flutter ok:false selector passed (`+1`), gofmt, dart format, and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GI-008 | GroupInboxStore resets failed stream and closes successful stream | Fake stream records Reset/Close. | 1. Run success and failure cases. 2. Inspect stream calls. | Failed stream reset; successful stream closed. | P1 | Covered | Required | Recommended | N/A | N/A | N/A | Covered on 2026-05-14 by exact native relay-retry stream cleanup proof. `go-mknoon/node/group_inbox_test.go::TestGI008GroupInboxStoreResetsFailedStreamAndClosesSuccessfulStream` forces the first relay to return non-OK, observes the relay-side stream read fail with a reset rather than clean EOF, then forces the second relay to return OK and observes clean EOF after client close. Production inspected only: `go-mknoon/node/group_inbox.go::GroupInboxStore` keeps `streamOK` false until a relay OK response and `go-mknoon/node/node.go::finishStream` resets failed streams and closes successful streams. Passed evidence: gofmt, exact GI-008 Go (`ok node 0.588s`), adjacent GroupInboxStore selector (`ok node 0.464s`), broader node/internal/crypto inbox selector (`ok node 1.124s`, `ok internal 0.273s`, `ok crypto 0.557s`), selected race selector (`ok node 1.770s`), relay-server inbox selector (`ok relay-server 0.797s`), Flutter drain/retry inbox suites (`+92`), named `./scripts/run_test_gates.sh groups` (`+160`), and diff hygiene. Residual-only none; GR-002 is the next unresolved session in ordered ledger order. |
| GI-009 | GroupInboxRetrieve since timestamp sends correct action/limit | Node started with fake relay. | 1. Call GroupInboxRetrieve(G, ts). 2. Capture request. | Action group_retrieve, groupId G, sinceTimestamp ts, limit 50. | P0 | Covered | Required | Required | N/A | N/A | N/A | Covered by tests-only Go node proof in `go-mknoon/node/group_inbox_test.go::TestGI009GroupInboxRetrieveSendsSinceTimestampRequestShape`; no production code changed. The test starts a local fake relay, captures the framed request from `GroupInboxRetrieve`, returns an OK empty page, and proves the request carries `action == group_retrieve`, expected `groupId`, exact caller `sinceTimestamp`, and default `limit == 50`. Focused GI-009 passed (`ok node 0.581s`), adjacent group inbox retrieve selector passed (`ok node 0.404s`), gofmt and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GI-010 | GroupInboxRetrieveWithCursor defaults non-positive limit to 50 | limit <= 0. | 1. Call retrieve cursor with 0/-1. 2. Capture request. | Limit is 50 and cursor is preserved. | P1 | Covered | Required | Recommended | N/A | N/A | N/A | Covered on 2026-05-14 by exact native cursor retrieve request proof. `go-mknoon/node/group_inbox_test.go::TestGI010GroupInboxRetrieveWithCursorDefaultsNonPositiveLimitAndPreservesCursor` starts a local fake relay, captures two framed `group_retrieve_cursor` requests, and proves `limit == 0` and `limit == -1` both serialize as relay-visible `limit == 50` while preserving distinct opaque cursors `cursor-gi-010-zero` and `cursor-gi-010-negative`. Production inspected only: `go-mknoon/node/group_inbox.go::GroupInboxRetrieveWithCursorResult` already normalizes non-positive limits. Passed evidence: gofmt, exact GI-010 Go (`ok node 0.485s`), adjacent cursor retrieve selector (`ok node 0.477s`), broader node/internal/crypto inbox/history selector (`ok node 0.812s`, `ok internal 1.167s`, `ok crypto 0.901s`), selected race selector (`ok node 1.866s`), relay-server inbox selector (`ok relay-server 0.816s`), Flutter drain/retry inbox suites (`+92`), named `./scripts/run_test_gates.sh groups` (`+160`), and diff hygiene. Residual-only none; GR-002 is the next unresolved session in ordered ledger order. |
| GI-011 | Cursor retrieve returns messages, nextCursor, and history gaps | Relay returns OK page. | 1. Call GroupInboxRetrieveWithCursorResult. 2. Inspect result. | Messages, nextCursor, and HistoryGaps are preserved. | P0 | Covered | Required | Required | N/A | N/A | N/A | Covered by tests-only Go node proof in `go-mknoon/node/group_inbox_test.go::TestGI011GroupInboxRetrieveWithCursorResultPreservesMessagesCursorAndHistoryGaps`; no production code changed. The test starts a local fake relay, captures the cursor retrieve request, returns an OK page with two `groupMessages`, `nextCursor`, and one complete `historyGaps` entry, then proves message order/fields, exact next cursor, and all gap metadata including candidate source peers are preserved. Focused GI-011 passed (`ok node 0.541s`), adjacent cursor/history retrieve selector passed (`ok node 0.383s`), gofmt, and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GI-012 | NO_MESSAGES returns empty successful result | Relay status NO_MESSAGES. | 1. Retrieve. 2. Inspect result and stream closure. | Returns empty messages/no error and stream closes OK. | P0 | Covered | Required | Required | N/A | N/A | N/A | Covered by tests-only Go node proof in `go-mknoon/node/group_inbox_test.go::TestGI012GroupInboxRetrieveNoMessagesReturnsEmptyAndClosesStream`; no production code changed. The test starts a local fake relay, captures the timestamp retrieve request, returns `{"status":"NO_MESSAGES"}`, proves `GroupInboxRetrieve` returns no error and an empty message slice, and verifies the relay observes clean `io.EOF` from the client's success-path stream close. Focused GI-012 passed (`ok node 0.464s`), adjacent group inbox retrieve selector passed (`ok node 0.417s`), gofmt, and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GI-013 | Retrieve retries relays in order | First relay fails, second returns messages. | 1. Inject first failure. 2. Second success. 3. Inspect returned data. | Overall retrieve succeeds with second relay data. | P0 | Covered | Required | Required | N/A | Recommended | N/A | Covered by tests-only Go node proof in `go-mknoon/node/group_inbox_test.go::TestGI013GroupInboxRetrieveRetriesRelaysInOrderAndReturnsSecondData`; no production code changed. The test starts two local fake relays, makes the first return non-OK after reading the retrieve request, makes the second return OK with a message, proves `GroupInboxRetrieve` succeeds with the second relay data intact, asserts attempts are `[first, second]`, and proves no extra relay is attempted after success. Focused GI-013 passed (`ok node 0.670s`), adjacent retrieve failover selector passed (`ok node 0.419s`), gofmt, and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GI-014 | Retrieve non-OK status returns error | Relay status ERROR. | 1. Retrieve. 2. Inspect error. | Error includes group inbox retrieve failed reason. | P0 | Covered | Required | Required | N/A | N/A | N/A | Covered by tests-only Go node proof in `go-mknoon/node/group_inbox_test.go::TestGI014GroupInboxRetrieveReturnsRelayNonOKError`; no production code changed. The test starts a local fake relay, captures the timestamp retrieve request, returns `{"status":"ERROR","error":"retrieve quota exceeded for group"}`, and proves `GroupInboxRetrieve` returns no messages plus an error containing both the relay selector wrapper `all 1 relays failed` and `group inbox retrieve failed: retrieve quota exceeded for group`. It also proves exactly one relay attempt and request shape `action == group_retrieve`, expected `groupId`, exact `sinceTimestamp`, and default `limit == 50`. Focused GI-014 passed (`ok node 0.570s`), adjacent retrieve/error selector passed (`ok node 0.466s`), gofmt, and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GI-015 | Retrieve malformed JSON returns error | Relay returns invalid JSON frame. | 1. Retrieve. 2. Inspect error. | Unmarshal error returned; no messages rendered. | P0 | Covered | Required | Required | N/A | N/A | N/A | Covered by tests-only Go node proof in `go-mknoon/node/group_inbox_test.go::TestGI015GroupInboxRetrieveMalformedJSONReturnsError`; no production code changed. The test starts a local fake relay, captures the timestamp retrieve request, returns a framed invalid JSON payload, and proves `GroupInboxRetrieve` returns no messages plus an error containing both `all 1 relays failed` and `unmarshal response`. It also proves exactly one relay attempt and request shape `action == group_retrieve`, expected `groupId`, exact `sinceTimestamp`, and default `limit == 50`. Focused GI-015 passed (`ok node 0.586s`), adjacent retrieve/error selector passed (`ok node 0.436s`), gofmt, and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GI-016 | Retrieve frame too large is rejected | Relay sends frame length > MaxFrameLen. | 1. Retrieve. 2. Inspect error. | readFrame rejects frame too large; no memory blowup. | P0 | Covered | Required | Required | N/A | N/A | N/A | Covered by tests-only Go node proof in `go-mknoon/node/group_inbox_test.go::TestGI016GroupInboxRetrieveRejectsOversizedFrame`; no production code changed. The test starts a local fake relay, captures the timestamp retrieve request, writes only a 4-byte big-endian response length prefix equal to `MaxFrameLen + 1`, and proves `GroupInboxRetrieve` returns no messages plus an error containing both `all 1 relays failed` and `frame too large` without allocating or sending an oversized payload. It also proves exactly one relay attempt and request shape `action == group_retrieve`, expected `groupId`, exact `sinceTimestamp`, and default `limit == 50`. Focused GI-016 passed (`ok node 0.591s`), adjacent retrieve/error selector passed (`ok node 0.451s`), gofmt, and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GI-017 | Offline member retrieves all entitled messages across pages | B offline while A sends > page limit. | 1. A stores 120 messages. 2. B retrieves cursors until done. 3. Verify order/dedupe. | B receives all entitled messages exactly once in chronological or documented order. | P0 | Covered | Recommended | Required | Required | Required | Required | Covered by relay and Flutter app pagination proof. `go-relay-server/group_inbox_test.go::TestGI017GroupInboxStoreAuthorizedCursorPaginationReturns120MessagesExactlyOnce` stores 120 messages for recipient `peer-b`, retrieves as `peer-b` with cursor page size 50, proves 3 pages, all 120 messages in order, no duplicate IDs, empty unauthorized result for `peer-c`, and recipient authorization on every returned row. `test/features/groups/application/drain_group_offline_inbox_use_case_test.dart::GI-017 offline member drains 120 entitled messages across pages exactly once` seeds three signed replay pages (50/50/20), proves app requests cursors `''`, `cursor-050`, `cursor-100` with limit 50 and no `sinceTimestamp`, saves all 120 messages in chronological/id order, clears the final cursor, and a repeat drain leaves count at 120. Focused relay GI-017 passed (`ok relay-server 0.601s`), focused Flutter GI-017 passed (`+1`), adjacent relay cursor selector passed (`ok relay-server 0.441s`), adjacent app cursor-continuation selector passed (`+1`), gofmt, dart format, and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GI-018 | Removed member offline replay stops at removal cutoff | C offline when removed. | 1. Store before and after remove. 2. C retrieves. 3. Apply decrypt/cutoff. | C receives/decrypts only pre-removal allowed messages. | P0 | Covered | Required | Required | Required | Required | Required | Covered by exact Flutter app replay-cutoff proof in `test/features/groups/application/drain_group_offline_inbox_use_case_test.dart::GI-018 removed member offline replay keeps pre-removal message and stops at removal cutoff`; no production code changed. The test seeds a signed pre-removal inbox page, then a self-removal page with same-page post-removal traffic plus a further next-page post-removal message, drains as the removed local member through `GroupMessageListener`, proves the pre-removal plaintext is persisted/decrypted, proves both post-removal message ids are absent, proves `group:leave` and removal-stream cleanup fire, proves cursor retrieval stops before the after-removal page, and proves unique decrypted plaintexts contain only the pre-removal user message plus the self-removal control envelope. Focused GI-018 passed (`+1`), adjacent self-removal cutoff passed (`+1`), adjacent removed-sender cutoff passed (`+1`), dart format passed, and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GI-019 | Re-added member replay respects gap between memberships | C removed then re-added while offline. | 1. Store before, during, after. 2. C retrieves all pages. 3. Verify timeline. | Pre-remove and post-readd windows obey policy; removed-window messages omitted/unreadable. | P0 | Covered | Required | Required | Required | Required | Required | Covered by exact Flutter app replay proof in `test/features/groups/application/drain_group_offline_inbox_use_case_test.dart::GI-019 re-added member replay keeps pre-remove skips removed-window and renders post-readd`; no production code changed. The test drains a signed pre-removal page for Charlie, persists the cursor, applies a replayed self-removal cutoff, re-adds Charlie with a later membership timestamp and a new key epoch, then drains remaining cursor pages containing removed-window traffic, post-readd traffic, and a duplicate removed-window record. It proves the pre-removal and post-readd messages render exactly once with expected plaintext, the removed-window id/plaintext is absent, no pending key repair is queued for the removed-window message, the cursor clears after `''`, `gi019-page-2`, and `gi019-page-3`, and the self-removed-window-after-rejoin diagnostic fires. Focused GI-019 passed (`+1`), adjacent GK-023 re-add replay proof passed (`+1`), adjacent GM-033 replay-resume proof passed (`+1`), `dart format --set-exit-if-changed` passed, and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GI-020 | Inbox replay repairs messages missed by zero-peer publish | No live peers at send time. | 1. A publishes/stores. 2. B later retrieves. 3. Inspect timeline. | B eventually receives message once and sender status transitions to durable/sent per app rule. | P0 | Covered | Recommended | Required | Required | Required | Required | Covered by exact integration proof in `test/features/groups/integration/group_resume_recovery_test.dart::GI-020 zero-peer publish is repaired by later inbox replay exactly once` plus adjacent send-state proof; no production code changed. The test creates Alice with `ZeroPeerPublishBridge` and Bob with cursor inbox replay, unsubscribes Bob to force zero live topic peers, sends deterministic message `gi020-zero-peer-replay`, proves Alice returns `successNoPeers`, persists sender status `sent`, marks `inboxStored == true`, clears retry payload, and emits both publish and inbox-store commands. It injects Alice's durable replay envelope into Bob's inbox, drains Bob, proves Bob receives exactly one incoming row with the deterministic id/text, repeats the drain to prove dedupe stays exact-once, and verifies Alice's sender row remains durable/sent. Focused GI-020 passed (`+1`), adjacent Section 11 zero-peer fallback passed (`+1`), adjacent GP-005 sender durable fallback passed (`+1`), `dart format --set-exit-if-changed` passed, and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GI-021 | Inbox replay does not resurrect messages from non-member sender | Relay stores X envelope for G. | 1. B retrieves. 2. Validator/decrypt/app replay checks. 3. Inspect timeline. | X message rejected and not rendered. | P0 | Covered | Required | Required | N/A | Required | Required | Covered by exact Flutter app offline replay proof in `test/features/groups/application/drain_group_offline_inbox_use_case_test.dart::GI-021 inbox replay rejects non-member sender without rendering`; no production code changed. The test leaves `peer-non-member` absent from group membership, stores a signed replay envelope claiming that sender in the cursor inbox page, drains the inbox, proves the retrieve command runs, proves the message id/text is absent from repository and timeline, proves replay rejects with `GROUP_DRAIN_OFFLINE_INBOX_REPLAY_SIGNATURE_REJECTED` / `unknown_sender`, and proves neither `payload.verify` nor `group.decrypt` is called with untrusted non-member material. Focused GI-021 passed (`+1`), adjacent handle-incoming unknown-sender proof passed (`+1`), adjacent live-listener unknown-sender proof passed (`+1`), delayed membership catch-up guard passed (`+1`), dart format passed, and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GI-022 | Replay path applies same device revocation as live path | Relay has message from revoked device. | 1. Retrieve revoked-device envelope. 2. Process replay. 3. Inspect timeline. | Rejected or hidden; no stale device message appears. | P0 | Covered | Required | Required | N/A | Required | Required | Covered by replay signature code plus exact Flutter app proof. `lib/features/groups/application/group_offline_replay_envelope.dart` now distinguishes matched inactive/revoked member devices as `GroupOfflineReplaySignatureException('revoked_device')`, and `lib/features/groups/application/drain_group_offline_inbox_use_case.dart` records that replay signature rejection and skips the stale envelope without decrypting/rendering or aborting the page. `test/features/groups/application/drain_group_offline_inbox_use_case_test.dart::GI-022 revoked-device replay is rejected while active-device replay continues` seeds a current member with active and revoked devices, stores revoked-device replay followed by active-device replay in one inbox page, proves the revoked id/text is absent, the active message renders once with active transport attribution, the cursor completes, only one unique active `payload.verify`/`group.decrypt` occurs, and flow evidence reports `revoked_device`. Focused GI-022 passed (`+1`), adjacent GI-021 passed (`+1`), replay envelope binding proof passed (`+1`), invalid-signature abort guard passed (`+1`), delayed membership catch-up guard passed (`+1`), dart format passed, and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GI-023 | Replay path applies same key epoch grace rules as live path | Relay has old/current epoch messages. | 1. Retrieve during and after grace. 2. Process. 3. Compare live behavior. | Covered 2026-05-13: `lib/features/groups/application/drain_group_offline_inbox_use_case.dart` now skips signed replay envelopes whose missing key epoch is older than the retained previous generation (`keyEpoch < latestKeyGeneration - 1`) before placeholder/repair/decrypt, while preserving future/current/previous missing-key repair. `test/features/groups/application/drain_group_offline_inbox_use_case_test.dart::GI-023 replay uses previous-epoch grace but skips expired replay epoch` proves epoch 1 and 2 replay render while previous key is retained, epoch 1 is pruned after epoch 3 arrives, stale epoch-1 replay is absent with no pending repair, epoch-3 replay renders, and only one unique decrypt occurs after grace. Gates: focused GI-023, future-key repair, future missing-key placeholder, retained mixed-epoch replay, GI-022, GI-021, `dart format --set-exit-if-changed`, and `git diff --check` all passed. | P0 | Covered | Required | Required | N/A | Required | Required | Prevents live/replay divergence. |
| GI-024 | Replay attack duplicate old envelope is deduped and cannot alter timeline | Relay redelivers an old valid envelope. | 1. Retrieve same old message repeatedly. 2. Inspect persistence. | Covered 2026-05-13: tests-only Flutter app proof in `test/features/groups/application/drain_group_offline_inbox_use_case_test.dart::GI-024 duplicate replay is idempotent without status rollback or notification spam` proves the same signed old replay envelope delivered twice in one page stores one incoming row, emits one listener event, and shows one notification; after the row is marked read, a later duplicate replay page leaves storage count, text, timestamp, sender, status, incoming flag, and `readAt` unchanged, emits no additional stream event or notification, and records two `GROUP_HANDLE_INCOMING_MSG_DUPLICATE` events with `dedupeBy: messageId`. Production inspected: `lib/features/groups/application/handle_incoming_group_message_use_case.dart`, `lib/features/groups/application/group_message_listener.dart`, and `lib/features/groups/application/drain_group_offline_inbox_use_case.dart` already enforce messageId idempotence and notification suppression for duplicate replay. Gates: focused GI-024, listener replay emission guard, GP-026/GMAR-004 live-plus-inbox duplicate enrichment, history-gap repair duplicate/order guard, GI-023, `dart format --set-exit-if-changed`, and `git diff --check` all passed. | P0 | Covered | Required | Required | N/A | Required | Required | Dedupe + replay attack. |
| GI-025 | Replay attack valid pre-removal envelope after removal is bounded | C/relay redelivers old envelope after C removed. | 1. Deliver old valid envelope to A/B after removal. 2. Process. | Covered 2026-05-13: code-plus-test Flutter app/repository proof. `lib/features/groups/application/group_message_listener.dart` now snapshots removed-member signing identity before active membership deletion through `RemovedGroupMemberSnapshotRepository`; `lib/features/groups/application/group_offline_replay_envelope.dart` uses that snapshot only as a replay signature fallback when the active member row is absent; `lib/core/database/migrations/068_removed_group_member_snapshots.dart`, `lib/core/database/helpers/group_members_db_helpers.dart`, `lib/features/groups/domain/repositories/group_repository_impl.dart`, `lib/features/groups/domain/repositories/group_repository.dart`, `test/shared/fakes/in_memory_group_repository.dart`, and `lib/main.dart` persist/wire the bounded snapshot. `test/features/groups/application/drain_group_offline_inbox_use_case_test.dart::GI-025 valid pre-removal replay after removal is accepted only as history` proves a signed `member_removed` page removes the sender, a later duplicate signed pre-removal replay from that removed sender persists exactly once as history, and a signed post-removal replay from the same sender is absent/rejected with `GROUP_HANDLE_INCOMING_MSG_REMOVED_AFTER_CUTOFF`. `test/features/groups/domain/repositories/group_repository_impl_test.dart::removed member snapshot survives active member deletion` proves active deletion does not lose snapshot signing material. Gates: focused GI-025, adjacent removed-member replay cutoff, handler before/at-cutoff proofs, GM-013 handler aggregate, listener remove/cutoff proofs, GI-021 non-member replay regression, GI-024 duplicate replay regression, snapshot repository test, `dart format --set-exit-if-changed`, and `git diff --check` all passed. | P0 | Covered | Required | Required | N/A | Required | Required | Causal boundaries. |
| GI-026 | History gap metadata is preserved to app repair layer | Relay returns historyGaps. | 1. Cursor retrieve. 2. Inspect GroupInboxCursorResult. 3. Trigger repair orchestration. | Covered 2026-05-13: tests-only Flutter app and bridge proof. `lib/core/bridge/bridge_group_helpers.dart` parses `historyGaps` into `GroupInboxHistoryGap`, and `lib/features/groups/application/drain_group_offline_inbox_use_case.dart` persists detected gap stubs before cursor commit and passes the same gap object into history repair orchestration. `test/features/groups/application/drain_group_offline_inbox_use_case_test.dart::GI-026 history gap metadata is preserved to app repair layer` returns a cursor page with a complete non-default history gap, captures the repair callback's `GroupInboxHistoryGap`, and proves `groupId`, `gapId`, missing-before/after boundaries, `expectedRangeHash`, `expectedHeadMessageId`, and ordered `candidateSourcePeerIds` are preserved into both the app repair request and persisted repair record. Gates: focused GI-026, bridge parser proof, first-authorized repair proof, unauthorized/hash fallback proof, repaired replay application proof, cursor-commit invariant proof, `dart format --set-exit-if-changed`, and `git diff --check` all passed. | P0 | Covered | Required | Required | N/A | N/A | N/A | group_inbox.go:50-64. |
| GI-027 | GroupHistoryRepairRange validates required fields | Request has missing/blank fields. | 1. Call NormalizeGroupHistoryRepairRangeRequest variants. 2. Inspect errors. | Covered 2026-05-13: tests-only Go node proof in `go-mknoon/node/group_inbox_test.go::TestGI027GroupHistoryRepairRangeValidatesRequiredFieldsAndTrimsWhitespace`; no production code change required. Existing `go-mknoon/node/group_inbox.go::NormalizeGroupHistoryRepairRangeRequest` trims required fields and `Validate` rejects missing values before node/relay work. The exact test proves all required fields are trimmed exactly, caller-provided positive limit remains unchanged, and whitespace-only `groupId`, `gapId`, `sourcePeerId`, `missingAfterMessageId`, `missingBeforeMessageId`, `expectedRangeHash`, and `expectedHeadMessageId` each fail with field-specific missing errors. Gates: focused GI-027, `GI027|GroupHistoryRepairRange`, adjacent `GI011|GI012|GI027|GroupInboxRetrieveWithCursor|GroupHistoryRepairRange`, gofmt, and `git diff --check` all passed. | P0 | Covered | Required | Recommended | N/A | N/A | N/A | group_inbox.go:91-133. |
| GI-028 | GroupHistoryRepairRange defaults limit to 50 | Request limit <= 0. | 1. Normalize request. 2. Inspect limit. | Limit becomes 50. | P1 | Covered | Required | Recommended | N/A | N/A | N/A | Covered on 2026-05-14 by exact native repair-range normalization proof. `go-mknoon/node/group_inbox_test.go::TestGI028GroupHistoryRepairRangeDefaultsNonPositiveLimitTo50` normalizes valid repair-range requests with `Limit == 0` and `Limit == -1`, proves both become `50`, and proves non-limit group/gap/source/boundary/hash/head fields are unchanged. Production inspected only: `go-mknoon/node/group_inbox.go::NormalizeGroupHistoryRepairRangeRequest` already defaults `req.Limit <= 0` to `50`. Passed evidence: gofmt, exact GI-028 Go (`ok node 0.570s`), adjacent repair-range selector (`ok node 0.470s`), broader node/internal/crypto inbox/history selector (`ok node 0.922s`, `ok internal 1.029s`, `ok crypto 0.418s`), selected race selector (`ok node 1.931s`), relay-server inbox selector (`ok relay-server 0.782s`), Flutter drain/retry inbox suites (`+92`), named `./scripts/run_test_gates.sh groups` (`+160`), and diff hygiene. Residual-only none; GR-002 is the next unresolved session in ordered ledger order. |
| GI-029 | GroupHistoryRepairRange request shape is correct | Valid repair request. | 1. Call GroupHistoryRepairRange with fake relay. 2. Capture JSON. | Action group_history_repair_range with expected boundaries/hash/head/source and limit. | P0 | Covered | Required | Required | N/A | N/A | N/A | Covered by tests-only Go node proof in `go-mknoon/node/group_inbox_test.go::TestGI029GroupHistoryRepairRangeSendsExpectedRequestShape`; no production code changed. The test starts a local libp2p fake relay, captures the framed JSON request from `GroupHistoryRepairRange`, passes whitespace-padded group/gap/source/boundary/hash/head fields and limit `37`, and proves the outbound request carries action `group_history_repair_range`, normalized `groupId`, `gapId`, `sourcePeerId`, `missingAfterMessageId`, `missingBeforeMessageId`, `expectedRangeHash`, `expectedHeadMessageId`, and caller limit `37`. Focused GI-029 passed (`ok node 0.472s`), adjacent `GI027|GI029|GroupHistoryRepairRange` passed (`ok node 0.365s`), adjacent `GI011|GI012|GI027|GI029|GroupInboxRetrieveWithCursor|GroupHistoryRepairRange` passed (`ok node 0.389s`), gofmt and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GI-030 | Repair response fallback fills missing IDs from request | Relay omits groupId/gapId/sourcePeerId. | 1. Return response missing fields. 2. Inspect result. | Response uses normalized request values for missing fields. | P1 | Covered | Required | Recommended | N/A | N/A | N/A | Covered on 2026-05-14 by exact native repair-response fallback proof. `go-mknoon/node/group_inbox_test.go::TestGI030GroupHistoryRepairRangeResponseFallsBackToRequestIDs` starts a local fake relay, captures the normalized `group_history_repair_range` request, returns an OK response that omits `groupId`, `gapId`, and `sourcePeerId`, then proves the node response fills those IDs from normalized request values while preserving relay `rangeHash`, `headMessageId`, and the replay message. Production inspected only: `go-mknoon/node/group_inbox.go::GroupHistoryRepairRange` already fills missing response IDs from the normalized request. Passed evidence: gofmt, exact GI-030 Go (`ok node 0.540s`), adjacent repair-range selector (`ok node 0.517s`), broader node/internal/crypto inbox/history selector (`ok node 0.596s`, `ok internal 0.681s`, `ok crypto 0.999s`), selected race selector (`ok node 1.720s`), relay-server inbox selector (`ok relay-server 0.828s`), Flutter drain/retry inbox suites (`+92`), named `./scripts/run_test_gates.sh groups` (`+160`), and diff hygiene. Residual-only none; GR-002 is the next unresolved session in ordered ledger order. |
| GI-031 | Repair range hash mismatch is rejected by app layer | Relay/source returns messages with wrong rangeHash. | 1. Request repair. 2. Return mismatched rangeHash. 3. Process. | App rejects repair data and does not render untrusted messages. | P0 | Covered | Required | Required | N/A | Required | Required | Covered by app-layer range-hash validation in `lib/features/groups/application/drain_group_offline_inbox_use_case.dart::_validateHistoryRepairResult` plus exact Flutter app proof in `test/features/groups/application/drain_group_offline_inbox_use_case_test.dart::GI-031 repair range hash mismatch is rejected without rendering`; no production code changed. The test seeds a detected history gap with an expected range hash, returns a valid signed repair message from an authorized source with a mismatched `rangeHash`, drains the inbox, proves `gi031-rejected` is not persisted/rendered, proves the repair fails with `range_hash_mismatch`, and proves no repaired message ids are recorded. Focused GI-031 passed (`+1`), adjacent hash-fallback proof passed (`+1`), adjacent repair-success proof passed (`+1`), GI-026 metadata proof passed (`+1`), dart format passed, and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GI-032 | Repair headMessageId mismatch is rejected by app layer | Relay/source returns wrong head. | 1. Request repair. 2. Return mismatched headMessageId. 3. Process. | Repair rejected or flagged; no silent wrong history. | P0 | Covered | Required | Required | N/A | Required | Required | Covered by app-layer head validation in `lib/features/groups/application/drain_group_offline_inbox_use_case.dart::_validateHistoryRepairResult` plus exact Flutter app proof in `test/features/groups/application/drain_group_offline_inbox_use_case_test.dart::GI-032 repair head mismatch is rejected without rendering`; no production code changed. The test seeds a detected history gap whose returned repair messages have the expected `rangeHash` but a wrong `headMessageId`, drains the inbox from an authorized source, proves `gi032-rejected` is not persisted/rendered, proves the repair fails with `head_mismatch`, and proves no repaired message ids are recorded. Focused GI-032 passed (`+1`), adjacent GI-031 range-hash proof passed (`+1`), adjacent repair-success proof passed (`+1`), GI-026 metadata proof passed (`+1`), dart format passed, and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GI-033 | Repair source must be current authorized member | Gap candidates include removed or unknown peer. | 1. Try repair from C removed and B active. 2. Process. | Covered 2026-05-13: tests-only Flutter app proof in `test/features/groups/application/drain_group_offline_inbox_use_case_test.dart::GI-033 repair source must be a current authorized member`; no production code changed. Existing `lib/features/groups/application/drain_group_offline_inbox_use_case.dart::_repairHistoryGapsFromPage` builds authorized repair sources from current `groupRepo.getMembers(groupId)` and records unauthorized candidates without requesting repair data. The test removes a previously saved member, leaves an unknown peer absent, keeps `peer-good` active, seeds repair candidates `[peer-removed, peer-rogue, peer-good]`, asserts `requestHistoryRepairRange` is called only for `peer-good`, proves the removed/unknown candidates are recorded in `attemptedSourcePeerIds`, and proves the active source repairs `gi033-repaired` successfully. Focused GI-033 passed (`+1`), adjacent unauthorized/hash fallback passed (`+1`), GI-031 passed (`+1`), GI-032 passed (`+1`), GI-026 passed (`+1`), dart format passed, and `git diff --check` passed. | P0 | Covered | Required | Required | N/A | Required | Required | Prevents malicious repair. |
| GI-034 | Offline replay notification suppression is correct | B retrieves messages after being offline. | 1. Retrieve N messages. 2. Inspect notifications/badges. | Replay does not create duplicate push notifications; unread state follows product rule. | P1 | Covered | Recommended | Required | N/A | Required | Required | Covered on 2026-05-14 by exact Flutter app offline-replay notification and unread-count proof. `test/features/groups/application/drain_group_offline_inbox_use_case_test.dart::GI-034 offline replay suppresses duplicate notifications and preserves unread state` drains signed replay envelopes through the real `GroupMessageListener`, seeds a recent remote-push announcement for one replay id, includes an in-page duplicate for that replay plus a distinct message, and proves storage contains two incoming rows, the remote-pushed replay shows no local notification, the distinct replay shows exactly one local notification, and unread totals are exactly two. After `markAsRead`, the same replays are delivered again and the test proves storage count, listener emissions, notification count, and total/group unread counts remain unchanged at read/zero. Production inspected only: `lib/features/groups/application/group_message_listener.dart`, `lib/features/groups/application/drain_group_offline_inbox_use_case.dart`, `lib/features/groups/application/handle_incoming_group_message_use_case.dart`, and `lib/features/push/application/show_notification_use_case.dart` already provide replay routing, messageId dedupe, remote-push suppression, and read-state preservation. Passed evidence: Dart format, exact GI-034 Flutter proof (`+1`), Go inbox/history owner selector (`ok node 0.813s`), relay inbox/dedup owner selector (`ok relay-server 0.781s`), Flutter drain/retry inbox suites (`+93`), named `./scripts/run_test_gates.sh groups` (`+160`), and diff hygiene passed. Residual-only none; GR-002 is the next unresolved session in ordered ledger order. |
| GI-035 | Inbox relay never stores plaintext group message | Store request is captured at fake relay. | 1. A sends known plaintext. 2. Inspect relay stored message. 3. Search plaintext. | Covered 2026-05-13: code unchanged; row-owned Go node, Go relay, and Flutter app tests prove relay-visible and relay-persisted group inbox data carries only encrypted/opaque envelopes. `go-mknoon/node/group_inbox_test.go::TestGI035GroupInboxStoreSendsEncryptedEnvelopeWithoutPlaintextToRelay` builds a valid encrypted group envelope from known plaintext/media/delivery fragments, sends it through `GroupInboxStore` to a fake relay, captures the raw `group_store` frame, proves no plaintext fragments or `pushTitle`/`pushBody`/plaintext keys appear, and proves the relay-visible `message` equals the encrypted envelope. `go-relay-server/group_inbox_test.go::TestGI035GroupInboxStorePersistsEncryptedEnvelopeWithoutPlaintext` stores/retrieves an opaque group replay envelope and proves stored relay JSON has no protected plaintext fragments or plaintext fields. `test/features/groups/application/send_group_message_use_case_test.dart::EK-002 GI-035 pending inbox retry stores encrypted replay without protected plaintext` proves Flutter `group:inboxStore` command and retry payload contain only the encrypted replay envelope and no protected text/media/private-state fragments. Focused GI-035 Go node, relay, and Flutter gates passed; adjacent GI003/GI035/GroupInboxStore, relay GroupInbox/InboxDedup, EK-002, gofmt, dart format, and `git diff --check` passed. | P0 | Covered | Required | Required | Required | Required | N/A | Privacy gate. |

## Relay Recovery, Restart, and Group Rejoin

| Test ID | Scenario | Preconditions | Steps | Expected Result | Priority | Current status | Unit | Integration | Smoke | Fake Network | 3-Party E2E | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| GR-001 | RefreshRelaySession fails clearly when node is not started | Node not started. | 1. Call RefreshRelaySession. 2. Inspect result. | Returns NOT_STARTED with reusedHost true and no panic. | P1 | Covered | Required | Recommended | N/A | N/A | N/A | Covered on 2026-05-14 by exact native not-started refresh proof. `go-mknoon/node/node_test.go::TestGR001RefreshRelaySessionNotStartedReturnsStructuredFailure` calls `RefreshRelaySession` on a fresh unstarted node, proves the result is non-nil, unsuccessful, `RecoveryMode == "in_place"`, `ErrorCode == "NOT_STARTED"`, `Reason == "node not started"`, and `ReusedHost == true`, proves no host is created and the node is not marked started, then proves the shared recovery gate clears by making a second call that returns the same structured failure. Production inspected only: `go-mknoon/node/node.go::RefreshRelaySession` and `refreshRelaySessionOwned` already provide the not-started branch. Passed evidence: gofmt, exact GR-001 Go (`ok node 0.534s`), adjacent relay recovery selector (`ok node 21.543s`), Flutter lifecycle/resume recovery pair (`+68`), and diff hygiene passed. Residual-only none; GR-002 is the next unresolved session in ordered ledger order. |
| GR-002 | Concurrent RefreshRelaySession calls coalesce | Node started; recovery hook blocks. | 1. Start several refresh calls. 2. Complete hook. 3. Inspect results. | One owner runs recovery; waiters receive same result with coalesced count. | P1 | Covered | Required | Required | N/A | Recommended | N/A | Covered on 2026-05-14 by exact native public refresh coalescing proof. `go-mknoon/node/node_test.go::TestGR002ConcurrentRefreshRelaySessionCallsCoalesce` starts a node with fake relay hooks, blocks the first public `RefreshRelaySession` owner in `refreshRelaySessionHook`, starts four concurrent public `RefreshRelaySession` callers, proves the shared recovery promise records three coalesced waiters before release, then proves all callers receive the same successful `in_place` result with `ReusedHost == true` and `CoalescedRecoveryRequests == 3`. The test also proves exactly one refresh hook invocation ran and the shared recovery gate clears after completion. Production inspected only: `go-mknoon/node/node.go::RefreshRelaySession` and `go-mknoon/node/relay_session.go::BeginRecovery`/`CompleteRecovery` already provide the singleflight contract. Passed evidence: gofmt, exact GR-002 Go (`ok node 0.582s`), adjacent relay recovery selector (`ok node 21.607s`), Flutter lifecycle/resume recovery pair (`+68`), and diff hygiene passed. Residual-only none; GR-003 is the next unresolved session in ordered ledger order. |
| GR-003 | Stalled recovery clears gate after timeout | Recovery promise never completes. | 1. Begin recovery. 2. Wait using fake shortened timeout or seam. 3. Begin another. | First waiter gets RECOVERY_TIMEOUT and next recovery can start. | P1 | Covered | Required | Recommended | N/A | N/A | N/A | Covered on 2026-05-14 by exact native stalled recovery timeout proof. `go-mknoon/node/relay_session.go` now gives each `RelaySessionManager` an unexported `recoveryWaitTimeout` that defaults to existing `RecoveryWaitTimeout` and is copied into new recovery promises so tests can shorten only the timeout wait. `go-mknoon/node/relay_session_test.go::TestGR003RelaySessionStalledRecoveryClearsGateAfterTimeout` starts a recovery that never completes, joins as a waiter, proves `RECOVERY_TIMEOUT` plus `RecoveryMode == "timeout"`, proves `IsRecovering()` is false after timeout, then starts and successfully completes a fresh recovery on a new promise. Passed evidence: gofmt, exact GR-003 Go (`ok node 0.627s`), adjacent relay recovery selector (`ok node 21.944s`), Flutter lifecycle/resume recovery pair (`+68`), and diff hygiene passed. Residual-only none; GR-007 is the next unresolved session in ordered ledger order. |
| GR-004 | In-place relay recovery preserves group topics | Node has joined groups; relay refresh succeeds. | 1. Join groups. 2. RefreshRelaySession success. 3. Send/receive without rejoin. | Existing PubSub topics/subscriptions remain active and delivery resumes. | P0 | Covered | Required | Required | Required | Required | Required | Covered 2026-05-13: tests-only Go node proof in `go-mknoon/node/pubsub_test.go::TestGR004InPlaceRelayRecoveryPreservesGroupTopicsAndDeliveryWithoutRejoin`; no production code changed. Existing `go-mknoon/node/node.go::RefreshRelaySession` runs in-place recovery and only calls `Stop()` on failed `ReconnectRelays` fallback, while `Stop()` is the path that clears `groupTopics`/`groupSubs`. The exact test starts two real local libp2p nodes joined to the same private group, captures host/topic/subscription/config/key pointers, forces successful `RefreshRelaySession` through the in-place hook, proves the host and group topic/subscription/config/key are the same objects afterward, then publishes `gr004-post-refresh-message` without any rejoin and proves the already-joined peer receives the decrypted `group_message:received` event. Focused GR-004 Go, adjacent relay recovery selectors, race selector, app in-place/no-ack resume proof, app node-requested recovery proof, handleAppResumed ordering proof, fake-network TTL persistence proof, watchdog rejoin adjacent proof, gofmt, and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GR-005 | Watchdog/full restart clears runtime groups and signals rejoin needed | Node has joined groups; reconnect falls back to restart. | 1. Trigger ReconnectRelays restart. 2. Inspect status needsGroupRecovery. 3. Attempt send before rejoin. | Runtime topics are gone, needsGroupRecovery true, sends fail until app rejoins. | P0 | Covered | Required | Required | Required | Required | Required | Covered by tests-only Go node proof in `go-mknoon/node/pubsub_test.go::TestGR005WatchdogRestartClearsGroupTopicsAndRequiresExplicitRejoin`; no production code changed. The test joins a private group, records native host/topic/subscription/config/key state, forces `ReconnectRelays` through failed in-place recovery into full watchdog restart, proves `RecoveryMode == watchdog_restart`, `ReusedHost == false`, host replacement, fresh PubSub, empty `groupTopics`/`groupSubs`/`groupConfigs`/`groupKeys`, `needsGroupRecovery == true`, and `watchdogRestartCount == 1`. It proves `PublishGroupMessage` before explicit app rejoin fails with `group not joined`, empty message id, and peer count zero, then rejoins with the saved app-owned config/key and publishes successfully. Focused GR-005 passed (`ok node 1.158s`), adjacent relay recovery passed (`ok node 21.636s`), combined GR-004/GR-005 recovery passed (`ok node 21.838s`), race selector passed (`ok node 2.151s`), app lifecycle node-requested recovery passed (`+1`), handleAppResumed ordering passed (`+1`), fake-network watchdog live-delivery selector passed (`+1`), fake-network watchdog drain/live selector passed (`+1`), gofmt and `git diff --check` passed. Existing `node.go::Stop`/`ReconnectRelays` and `relay_session.go::CompleteRecovery` behavior already satisfied the row once exact proof was added. Residual-only none; GE-005 is the next unresolved P0 session. |
| GR-006 | App acknowledges group recovery only after all topics rejoined | needsGroupRecovery true. | 1. Rejoin subset of groups. 2. Try acknowledge early. 3. Rejoin all. 4. Acknowledge. | Flag is not cleared until app has restored all private groups; no group left deaf. | P0 | Covered | Recommended | Required | Required | Required | Required | Covered by code change plus row-owned Flutter lifecycle proof. `lib/features/groups/application/rejoin_group_topics_use_case.dart::RejoinGroupTopicsResult.canAcknowledgeGroupRecovery` now requires no skipped recovery run, `skippedNoKeyCount == 0`, and `errorCount == 0`; `lib/core/lifecycle/handle_app_resumed.dart` uses that predicate in both recovery acknowledgment branches, and `lib/main.dart` uses it for the `PendingMessageRetrier` recovery-ack callback. `test/core/lifecycle/app_lifecycle_recovery_test.dart::GR-006 recovery ack waits until every active group topic rejoins` seeds two active groups with one missing key, proves the first resume rejoins only one group and does not call `group:acknowledgeRecovery`, then saves the missing key, reruns resume while `needsGroupRecovery` remains true, proves both topics rejoin, and only then acknowledges recovery. Focused GR-006 passed (`+1`), existing node-requested ack and in-place no-ack lifecycle selectors passed (`+1` each), handleAppResumed ordering passed (`+1`), pending-retrier ack/no-ack selectors passed (`+1` each), main wiring proof passed (`+1`), native Go `AcknowledgeGroupRecovery|GroupRecovery|RelaySession` passed (`ok github.com/mknoon/go-mknoon/node 21.379s`), fake-network watchdog selectors passed (`+1` each), dart format and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session in ledger order. |
| GR-007 | AcknowledgeGroupRecovery fails when node stopped | Node stopped. | 1. Call AcknowledgeGroupRecovery. 2. Inspect error. | Returns node not started and does not mutate state. | P1 | Covered | Required | Recommended | N/A | N/A | N/A | Covered on 2026-05-14 by exact native stopped-node acknowledgement proof. Existing `go-mknoon/node/node.go::AcknowledgeGroupRecovery` already returns `node not started` before mutating relay-session manager state or emitting acknowledgement events when the node is stopped. `go-mknoon/node/node_test.go::TestGR007AcknowledgeGroupRecoveryStoppedNodeFailsWithoutMutatingState` seeds pending recovery with `RecordWatchdogRestart`, stops the node, calls public `AcknowledgeGroupRecovery`, proves the error is `node not started`, proves `needsGroupRecovery` remains true, proves `watchdogRestartCount` remains 1, and proves no relay recovery acknowledgement event is emitted. Passed evidence: gofmt, exact GR-007 Go (`ok node 0.595s`), adjacent acknowledgement/recovery selector (`ok node 21.520s`), Flutter lifecycle/resume recovery pair (`+68`), and diff hygiene passed. Residual-only none; GR-009 is the next unresolved session in ordered ledger order. |
| GR-008 | Relay session RecordWatchdogRestart preserves recovery signal | Manager reset/restart flow. | 1. Call RecordWatchdogRestart. 2. Reset. 3. Inspect status. | watchdogRestartCount increments and needsGroupRecovery remains or is restored per contract. | P0 | Covered | Required | Recommended | N/A | N/A | N/A | Covered by tests-only Go relay-session proof in `go-mknoon/node/relay_session_test.go::TestGR008RecordWatchdogRestartPreservesRecoverySignalAcrossReset`; no production code changed. The test opens a relay reservation, calls `RecordWatchdogRestart`, proves `watchdogRestartCount == 1` and `needsGroupRecovery == true`, calls `Reset`, proves relay sessions and healthy count are cleared while aggregate state returns to `starting`, then proves direct methods and `StatusFields` still expose `watchdogRestartCount == 1` and `needsGroupRecovery == true` after reset. It finally calls `AcknowledgeGroupRecovery` and proves the recovery signal clears only on explicit acknowledgment. Focused GR-008 passed (`ok github.com/mknoon/go-mknoon/node 0.515s`), adjacent reset/watchdog selector passed (`ok github.com/mknoon/go-mknoon/node 2.878s`), adjacent GR-005/GR-008 relay recovery selector passed (`ok github.com/mknoon/go-mknoon/node 21.707s`), race selector passed (`ok github.com/mknoon/go-mknoon/node 4.176s`), gofmt and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session in ledger order. |
| GR-009 | Refresh failures trigger watchdog only after threshold/all relays | Multiple relay sessions with mixed health. | 1. Fail one relay below threshold. 2. Keep another reserved. 3. Inspect state. 4. Fail all beyond threshold. | needsGroupRecovery only true when policy threshold is reached for all relevant relays. | P1 | Covered | Required | Recommended | N/A | N/A | N/A | Covered on 2026-05-14 by exact native multi-relay watchdog threshold proof. Existing `go-mknoon/node/relay_session.go::OnRefreshFailed` already withholds watchdog transition while any reserved relay remains below `WatchdogMaxConsecutiveFailures`. `go-mknoon/node/relay_session_test.go::TestGR009RefreshFailuresTriggerWatchdogOnlyAfterAllRelaysReachThreshold` opens two relay reservations, fails relay A below and through threshold while relay B remains reserved with zero failures, proves no `needsGroupRecovery` and no `watchdog_restart`, then fails relay B below threshold and proves no watchdog signal, finally fails relay B through threshold and proves `needsGroupRecovery == true`, aggregate `watchdog_restart`, and matching status fields. Passed evidence: gofmt, exact GR-009 Go (`ok node 0.569s`), adjacent relay recovery selector (`ok node 21.640s`), Flutter lifecycle/resume recovery pair (`+68`), and diff hygiene passed. Residual-only none; GR-010 is the next unresolved session in ordered ledger order. |
| GR-010 | Refresh success resets consecutive failure counter | Relay failed several times then succeeds. | 1. Call OnRefreshFailed N. 2. OnRefreshSucceeded. 3. Inspect session. | ConsecutiveRefreshFailures resets to 0, state reserved, no stale error. | P1 | Covered | Required | Recommended | N/A | N/A | N/A | Covered on 2026-05-14 by exact native refresh-success reset proof. Existing `go-mknoon/node/relay_session.go::OnRefreshSucceeded` already resets consecutive failures, restores reserved state, updates reservation time, clears `LastError`, and recomputes aggregate state. `go-mknoon/node/relay_session_test.go::TestGR010RefreshSuccessResetsFailureCounterAndStaleError` records refresh failures below threshold, proves a non-zero failure counter and stored last error before success, calls `OnRefreshSucceeded`, then proves `ConsecutiveRefreshFailures == 0`, state is `reserved`, stale `LastError` is empty, `LastReservedAt` is set, `needsGroupRecovery` is false, aggregate/status state is online, healthy count is 1, and status relay state omits stale `lastError`. Passed evidence: gofmt, exact GR-010 Go (`ok node 0.469s`), adjacent relay recovery selector (`ok node 21.766s`), Flutter lifecycle/resume recovery pair (`+68`), and diff hygiene passed. Residual-only none; GR-011 is the next unresolved session in ordered ledger order. |
| GR-011 | Relay connectedness events update relay state only for relay peers | Connect/disconnect non-relay and relay peers. | 1. Emit events. 2. Inspect session manager/status. | Only configured relay peer changes relay state; non-relay peers do not. | P1 | Covered | Required | Recommended | N/A | N/A | N/A | Covered on 2026-05-14 by exact native relay-connectedness filter proof. Existing `go-mknoon/node/node.go::handleRelayConnectednessChanged` already returns unless `isRelayPeer(pid)`, and `go-mknoon/node/autorelay_metrics.go::syncRelaySessionFromRuntime` syncs only configured relay peers from `relayPeerOrder`. `go-mknoon/node/node_test.go::TestGR011RelayConnectednessUpdatesOnlyConfiguredRelayPeers` configures one relay peer, initializes only that relay session, simulates non-relay connectedness and proves no non-relay relay session, no configured relay-state mutation, and no relay-state event, then simulates configured relay connect/disconnect and proves relay state/status update only for that configured relay while healthy relay count drops to zero after disconnect. Passed evidence: gofmt, exact GR-011 Go (`ok node 0.545s`), adjacent relay recovery selector (`ok node 21.613s`), Flutter lifecycle/resume recovery pair (`+68`), and diff hygiene passed. Residual-only none; GR-012 is the next unresolved session in ordered ledger order. |
| GR-012 | Stale circuit addresses are detected and not trusted | Host reports circuit address but no reservation. | 1. Manager with no reserved sessions. 2. Call CircuitAddressesAreStale/Status. | Stale address detected; health is not reported as online. | P1 | Covered | Required | Recommended | N/A | N/A | N/A | Covered on 2026-05-14 by exact native stale-circuit health proof. Existing `go-mknoon/node/relay_session.go::CircuitAddressesAreStale`, `ReportsHealthyWithReservation`, `IgnoresStaleCircuitAddresses`, and `StatusFields` already make reservations the source of relay health truth. `go-mknoon/node/relay_session_test.go::TestGR012StaleCircuitAddressesDoNotReportHealthyWithoutReservation` initializes a relay without reservation, supplies a host-reported `/p2p-circuit` address, and proves stale detection is true, healthy-with-reservation is false, trusted circuit filtering returns empty, status is not online, `healthyRelayCount == 0`, and `lastReservationAt` is absent. It opens a reservation as a positive control, then ends it and proves stale detection/trust filtering/non-online status return. Passed evidence: gofmt, exact GR-012 Go (`ok node 0.967s`), adjacent relay recovery selector (`ok node 21.974s`), Flutter lifecycle/resume recovery pair (`+68`), and diff hygiene passed. Residual-only none; GR-013 is the next unresolved session in ordered ledger order. |
| GR-013 | Foreground relay recovery completes within configured budget | Relay warm/circuit wait hooks. | 1. Trigger refresh. 2. Measure result fields. 3. Compare configured timeouts. | Result includes relayWarmMs, circuitAddressWaitMs, foreground path, and respects budgets. | P1 | Covered | Required | Required | N/A | Recommended | N/A | Covered on 2026-05-14 by exact native foreground recovery budget proof. Existing `go-mknoon/node/node.go::refreshRelaySessionOwned` already warms relays with `ForegroundRelayDialTimeout`, waits first with `ForegroundCircuitAddressWaitTimeout`, reports `foreground_success`, and stamps foreground timeout/cadence plus timing fields. `go-mknoon/node/node_test.go::TestGR013ForegroundRelayRecoveryCompletesWithinConfiguredBudget` starts a node with a fake relay, captures the warm/wait hook budgets, returns immediate foreground circuit success, and proves successful in-place recovery with `ReusedHost == true`, `ForegroundRecoveryPath == "foreground_success"`, configured foreground timeout/cadence fields, non-negative `RelayWarmMs`, `CircuitAddressWaitMs`, and `RelayRefreshMs`, and warm/wait timings within the configured foreground budgets. Passed evidence: gofmt, exact GR-013 Go (`ok node 0.624s`), adjacent relay recovery selector (`ok node 22.169s`), Flutter lifecycle/resume recovery pair (`+68`), and diff hygiene passed. Residual-only none; GR-018, GR-019, GR-020, GE-016, and GE-021 are now covered separately, with GE-023 now covered separately and GE-024 is now covered separately; GO-005 is now covered separately; GO-006 is now covered separately; GO-007 is now covered separately; GO-010 is now covered separately. |
| GR-014 | Group discovery resumes after relayReady closes late | Group joined while relay unavailable. | 1. Join group. 2. Keep relayReady closed later. 3. Send after ready. | Discovery loop starts relay-assisted dialing after readiness and messages eventually deliver. | P0 | Covered | Required | Required | Recommended | Required | Required | Covered by tests-only Go node proof in `go-mknoon/node/pubsub_delivery_test.go::TestGR014GroupDiscoveryResumesAfterRelayReadyClosesLate`; no production code changed. The test joins a private group while node A's `relayReady` channel remains open, proves relay-assisted dial, rendezvous register, and rendezvous discover hooks are not called before readiness, then closes `relayReady` later and proves group discovery resumes with `group:discovery` step `known_member_dial_success` for node B using `path == relay` and `attemptedDirect == false`. It also proves rendezvous discovery resumes, `PublishGroupMessage` returns explicit id `gr014-late-relay-ready-message` with at least one live topic peer, and node B receives the exact message text. Focused GR-014 passed (`ok github.com/mknoon/go-mknoon/node 0.746s`), adjacent discovery selector passed (`ok github.com/mknoon/go-mknoon/node 13.502s`), adjacent relay recovery selector passed (`ok github.com/mknoon/go-mknoon/node 21.383s`), race selector passed (`ok github.com/mknoon/go-mknoon/node 13.228s`), fake-network TTL persistence selector passed (`+1`), watchdog resume live-delivery selector passed (`+1`), app lifecycle node-requested recovery selector passed (`+1`), gofmt and `git diff --check` passed. Residual-only none for GR-014; GE-005 is the next unresolved P0 session. |
| GR-015 | Private group receive survives relay disconnect/reconnect without app restart | A/B/C online through relay. | 1. Disconnect relay path. 2. Refresh in place. 3. Send before/during/after. | Messages during outage arrive through inbox after recovery; messages after recovery arrive live/inbox once. | P0 | Covered | Recommended | Required | Required | Required | Required | Covered by tests-only Flutter fake-network integration proof in `test/features/groups/integration/group_resume_recovery_test.dart::GR-015 relay reconnect replays outage messages and resumes live delivery without restart`; no production code changed. The test starts Alice, Bob, and Carol in one private group, proves baseline live delivery, unsubscribes Carol from the fake group network without disposing or recreating her app/user instance, sends two outage messages, proves Bob receives them live while Carol does not, and proves Alice staged durable inbox stores for Carol. It then resubscribes the same Carol instance, drains two cursor pages from the relay inbox, proves Carol receives `During relay drop 1` and `During relay drop 2` in order, sends `After relay reconnect`, and proves Bob and Carol both have exactly the baseline, two outage, and post-reconnect messages. Focused GR-015 passed (`+1`), adjacent partition replay/live-delivery selector passed (`+1`), native in-place relay recovery selector passed (`ok github.com/mknoon/go-mknoon/node 22.023s`), dart format and `git diff --check` passed. Residual-only none for GR-015; GE-005 is the next unresolved P0 session. |
| GR-016 | Private group receive survives watchdog restart with app topic rejoin | A/B/C online; restart occurs. | 1. Trigger restart. 2. App rejoins groups. 3. Send/retrieve. | All groups resume delivery; no group remains missing because runtime maps were cleared. | P0 | Covered | Recommended | Required | Required | Required | Required | Covered by tests-only Flutter fake-network proof in `test/features/groups/integration/group_resume_recovery_test.dart::GR-016 watchdog restart rejoins every private group and resumes delivery`, backed by native watchdog recovery and app recovery-ack selectors. The row-owned test starts Alice/Bob/Carol in two private groups, drops Bob from both fake topics to model watchdog runtime topic-map loss, sends one bridge-backed outage message per group, proves Carol receives both live while Bob misses them, proves Alice stages durable inbox custody for Bob in both groups, then calls `rejoinGroupTopics(reason: watchdogRestart)` and proves both groups rejoin with no skips/errors and `canAcknowledgeGroupRecovery == true`. It verifies one `group:join` per group with expected key material, drains Bob's inbox, proves the missed message renders in each group, sends one post-watchdog live message per group, and proves Bob and Carol both have exactly the baseline, outage, and post-watchdog messages in every group. Focused GR-016 passed (`+1`); adjacent watchdog live, watchdog drain/live, recovery-ack, native watchdog recovery, format, and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GR-017 | Recovery does not lose pending direct/inbox retry state | Messages staged for retry during recovery. | 1. Stage group inbox stores. 2. Trigger relay recovery. 3. Resume retry. | Pending durable sends are retried or surfaced honestly, not dropped. | P0 | Covered | Recommended | Required | Recommended | Required | Required | Covered by tests-only Flutter fake-network proof in `test/features/groups/integration/group_resume_recovery_test.dart::GR-017 recovery preserves failed direct and pending inbox retry state`, backed by pending retrier ordering and native recovery selectors. The row-owned test stages one failed outgoing group message by failing both publish and fallback inbox-store, stages one pending inbox-store retry by allowing live publish but failing durable store, triggers `handleAppResumed` with group repositories and real `retryFailedGroupMessages` / `retryFailedGroupInboxStores` callbacks, proves failed-message retry runs before inbox-store retry, proves both sender rows become `sent` with `inboxStored == true` and cleared retry payloads, injects the successful durable replay payloads into the offline reader cursor inbox, and proves the live and offline readers each receive exactly one copy of both deterministic messages. Focused GR-017, adjacent failed-message retry, adjacent pending inbox-store retry, pending retrier ordering, native relay/watchdog recovery selector, dart format, and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GR-018 | Recovery events are emitted and privacy-safe | Relay recovery success/failure. | 1. Trigger paths. 2. Inspect events/logs. | Events include enough diagnostics without leaking group content/keys. | P1 | Covered | Required | Recommended | N/A | N/A | N/A | Covered on 2026-05-14 by exact native relay recovery event privacy proof. Existing `go-mknoon/node/node.go::emitRelayStateEvent`, `go-mknoon/node/autorelay_metrics.go::syncRelaySessionFromRuntime`, and `go-mknoon/node/relay_session.go::StatusFields` already emit bounded relay diagnostics without reading private group content or key state. `go-mknoon/node/node_test.go::TestGR018RecoveryEventsAreDiagnosticAndPrivacySafe` seeds private group plaintext/key sentinels, triggers relay sync success and watchdog failure event paths, proves `relay:state` success diagnostics include reason, online aggregate state, healthy relay count, relay peer state, and reservation timestamp, proves failure diagnostics include reason, watchdog aggregate state, group recovery signal, watchdog count, relay peer state, and last error, and recursively proves both event payloads omit sensitive content/key fields and sentinel values. Passed evidence: gofmt, exact GR-018 Go (`ok node 0.542s`), adjacent relay recovery selector (`ok node 21.646s`), Flutter lifecycle/resume recovery pair (`+68`), and diff hygiene passed. Residual-only none; GR-019, GR-020, GE-016, and GE-021 are now covered separately, with GE-023 now covered separately and GE-024 is now covered separately; GO-005 is now covered separately; GO-006 is now covered separately; GO-007 is now covered separately; GO-010 is now covered separately. |
| GR-019 | Group recovery slot limit does not deadlock on canceled context | Many groups recover and context cancels. | 1. Saturate groupRecoverySem. 2. Cancel context. 3. Inspect goroutines. | Slots release and no group loop hangs forever. | P1 | Covered | Required | Recommended | N/A | Recommended | N/A | Covered on 2026-05-14 by exact native group recovery limiter cancellation proof. Existing `go-mknoon/node/pubsub.go::acquireGroupRecoverySlot` and `runGroupDiscoveryCycle` already use context-aware slot acquisition plus deferred release, so no production runtime change was required. `go-mknoon/node/pubsub_test.go::TestGR019GroupRecoveryLimiterReleasesSlotsOnCanceledContext` saturates all `GroupDiscoveryConcurrency` recovery slots with discovery hooks blocked on a shared context, proves a queued group cannot acquire a slot before cancellation, cancels the context, proves every active cycle and the queued cycle exit within bounded time, verifies `groupRecoverySem` has no held slots, then runs a fresh recovery with a fresh context and proves it can register/discover and return the slot to zero. Passed evidence: gofmt, exact GR-019 Go (`ok node 0.741s`), adjacent relay/group recovery selector (`ok node 21.636s`), Flutter lifecycle/resume recovery pair (`+68`), and diff hygiene passed. Residual-only none; GR-020, GE-016, and GE-021 are now covered separately, with GE-023 now covered separately and GE-024 is now covered separately; GO-005 is now covered separately; GO-006 is now covered separately; GO-007 is now covered separately; GO-010 is now covered separately. |
| GR-020 | ReconnectRelays preserves original AutoRegister setting | Node config AutoRegister true/false. | 1. Trigger restart path. 2. Inspect lastConfig.AutoRegister. 3. Personal registration behavior. | Original setting restored after restart. | P2 | Covered | Required | Recommended | N/A | N/A | N/A | Covered on 2026-05-14 by exact native AutoRegister preservation proof. Existing `go-mknoon/node/node.go::reconnectRelaysOwned` suppresses startup auto-registration for the internal restart, restores the caller's original `AutoRegister` value into `lastConfig`, and `go-mknoon/node/personal_rendezvous_refresh.go::recoverPersonalRendezvousRegistration` only re-registers when the restored config has `AutoRegister=true`. `go-mknoon/node/node_test.go::TestGR020ReconnectRelaysPreservesOriginalAutoRegisterSetting` forces in-place recovery failure so `ReconnectRelays` takes the watchdog full-restart path, covers both `AutoRegister=true` and `AutoRegister=false`, proves `lastConfig.AutoRegister` and namespace are restored in both cases, proves the `true` case performs initial and watchdog personal registration and restores the refresh loop, and proves the `false` case performs no personal registration and starts no refresh loop. Passed evidence: gofmt, exact GR-020 Go (`ok node 0.751s`), adjacent relay/group recovery selector (`ok node 21.908s`), Flutter lifecycle/resume recovery pair (`+68`), and diff hygiene passed. Residual-only none; GE-016 and GE-021 are now covered separately, and GE-023 is now covered separately; GE-024 is now covered separately; GO-005 is now covered separately; GO-006 is now covered separately; GO-007 is now covered separately; GO-010 is now covered separately. |

## End-to-End Reliability Journeys and Stateful Fuzzing

| Test ID | Scenario | Preconditions | Steps | Expected Result | Priority | Current status | Unit | Integration | Smoke | Fake Network | 3-Party E2E | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| GE-001 | A/B/C happy-path private chat smoke | Three devices with local relay/fake rendezvous. | 1. Create G. 2. All join. 3. Each sends one message. | Every recipient sees the two messages from others exactly once and no validation errors occur. | P0 | Covered | Recommended | Required | Required | Required | Required | Covered by exact fake-network and three-device relay proof. `test/features/groups/integration/group_messaging_smoke_test.dart::GE-001 A/B/C happy-path private chat smoke delivers every sender exactly once` creates A/B/C, starts all subscribers, sends one bridge-backed message from Alice, Bob, and Charlie, proves three publishes and six deliveries, and verifies each participant has exactly one outgoing self row plus exactly two incoming rows from the other participants with no failed/pending rows. Repo-owned device harness support now includes `ge001` in `integration_test/group_multi_party_device_real_harness.dart`, `integration_test/scripts/group_multi_party_device_criteria.dart`, and `integration_test/scripts/run_group_multi_party_device_real.dart`; `test/integration/group_multi_party_device_criteria_test.dart` validates accepted all-send verdicts and duplicate receiver-persistence rejection. Focused GE-001 host proof passed (`+1`), criteria focused/full passed (`+2`, `+140`), static analysis passed, GM-001 and four-user adjacent smoke selectors passed, broader group smoke gate passed (`+114`), and the required three-simulator relay command passed with orchestrator verdict `ge001 proof passed: ge001 verdicts valid for alice, bob, charlie` using devices `38FECA55-03C1-4907-BD9D-8E64BF8E3469`, `347FB118-10D0-40C8-A05B-B0C3BD6B8CCD`, and `5BA69F1C-B112-47BE-B1FF-8C1003728C8F`. Residual-only none; GE-005 is the next unresolved P0 session. |
| GE-002 | A removes C, B keeps receiving A | A/B/C joined. | 1. A removes C. 2. A sends 10 messages. 3. B receives live/inbox. | B sees all 10; C sees none after cutoff. | P0 | Covered | Recommended | Required | Required | Required | Required | Covered by exact fake-network and three-device relay proof. `test/features/groups/integration/group_messaging_smoke_test.dart::GE-002 A removes C and B receives all ten A messages` creates joined Alice/Bob/Charlie state, has Alice remove Charlie, waits until Bob excludes Charlie and Charlie self-removes, sends ten bridge-backed post-removal messages from Alice, proves every send succeeds with durable Bob-only inbox recipients and no retry payload, proves Bob persists all ten Alice messages exactly once, and proves Charlie persists none after the removal cutoff. Repo-owned `ge002` harness/criteria support now exists in `integration_test/group_multi_party_device_real_harness.dart`, `integration_test/scripts/group_multi_party_device_criteria.dart`, `integration_test/scripts/run_group_multi_party_device_real.dart`, and `test/integration/group_multi_party_device_criteria_test.dart`, including acceptance of valid removal-continuity verdicts and rejection of Charlie post-removal leakage. Focused GE-002 host proof passed (`+1`), focused criteria passed (`+2`), full criteria regression passed (`+142`), static analysis passed, broader group smoke gate passed (`+115`), and the required three-simulator relay command passed with orchestrator verdict `ge002 proof passed: ge002 verdicts valid for alice, bob, charlie` using Alice `38FECA55-03C1-4907-BD9D-8E64BF8E3469`, Bob `347FB118-10D0-40C8-A05B-B0C3BD6B8CCD`, and Charlie `5BA69F1C-B112-47BE-B1FF-8C1003728C8F`; relay proof run id `1778649273954`, shared dir `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_ge002_Bl4Jlh`, with Alice proof `removedCharlie == true`, `actualDurablePayloadProof == true`, `postRemovalMessageCount == 10`, Bob proof `receivedEveryPostRemovalMessage == true`, `postRemovalReceiptCount == 10`, and Charlie proof `selfRemoved == true`, `groupPresentAfterRemoval == false`, `postRemovalPlaintextCount == 0`. Dart format and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GE-003 | A removes C, then B sends to A | A/B remain after C removed. | 1. Remove C. 2. B sends 10 messages. 3. A receives. | A receives all; removed C's stale state does not disrupt remaining pair. | P0 | Covered | Recommended | Required | Required | Required | Required | Covered by exact fake-network and three-device relay proof. `test/features/groups/integration/group_messaging_smoke_test.dart::GE-003 A removes C and A receives all ten B messages` creates joined Alice/Bob/Charlie state, has Alice remove Charlie, waits until Bob excludes Charlie and Charlie self-removes, sends ten bridge-backed post-removal messages from Bob, proves every send succeeds with durable Alice-only inbox recipients and no retry payload, proves Alice persists all ten Bob messages exactly once, and proves Charlie persists none of those post-removal messages. Repo-owned `ge003` harness/criteria support now exists in `integration_test/group_multi_party_device_real_harness.dart`, `integration_test/scripts/group_multi_party_device_criteria.dart`, `integration_test/scripts/run_group_multi_party_device_real.dart`, and `test/integration/group_multi_party_device_criteria_test.dart`, including acceptance of valid remaining-pair verdicts and rejection of Charlie post-removal leakage. Focused GE-003 host proof passed (`+1`), focused criteria passed (`+2`), full criteria regression passed (`+144`), static analysis passed, broader group smoke gate passed (`+116`), and the required three-simulator relay command passed with orchestrator verdict `ge003 proof passed: ge003 verdicts valid for alice, bob, charlie` using Alice `38FECA55-03C1-4907-BD9D-8E64BF8E3469`, Bob `347FB118-10D0-40C8-A05B-B0C3BD6B8CCD`, and Charlie `5BA69F1C-B112-47BE-B1FF-8C1003728C8F`; relay proof run id `1778650374836`, shared dir `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_ge003_HDMm20`, with Alice proof `removedCharlie == true`, `receivedEveryPostRemovalMessage == true`, `postRemovalReceiptCount == 10`, Bob proof `actualDurablePayloadProof == true`, `everyPostRemovalExcludedCharlie == true`, `postRemovalMessageCount == 10`, and Charlie proof `selfRemoved == true`, `groupPresentAfterRemoval == false`, `postRemovalPlaintextCount == 0`. Dart format and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GE-004 | A re-adds C, all three exchange messages | C was removed. | 1. Re-add C. 2. A/B/C each send. 3. Inspect all timelines. | All active members receive all post-readd messages exactly once. | P0 | Covered | Recommended | Required | Required | Required | Required | Covered by exact fake-network and three-device relay proof. `test/features/groups/integration/group_messaging_smoke_test.dart::GE-004 A re-adds C and all three exchange messages` creates joined Alice/Bob/Charlie private group state, has Alice remove Charlie, waits for remaining-member convergence and Charlie self-removal, re-adds Charlie with deterministic membership event time, then has Alice, Bob, and Charlie each send one bridge-backed post-readd message. The test proves each send targets exactly the two non-sender active members as durable inbox recipients, excludes the sender, has no failed/pending retry state, and every participant persists its own outgoing row plus the two incoming post-readd rows exactly once. Repo-owned `ge004` harness/criteria support now exists in `integration_test/group_multi_party_device_real_harness.dart`, `integration_test/scripts/group_multi_party_device_criteria.dart`, `integration_test/scripts/run_group_multi_party_device_real.dart`, and `test/integration/group_multi_party_device_criteria_test.dart`, including acceptance of valid re-add exchange verdicts and rejection of missing Charlie receipt. Focused GE-004 host proof passed (`+1`) after fixing fake helper `broadcastMemberAdded(eventAt:)` for deterministic membership timestamps; focused criteria passed (`+2`), full criteria regression passed (`+146`), static analysis passed, broader group smoke gate passed (`+117`), and the required three-simulator relay command passed with orchestrator verdict `ge004 proof passed: ge004 verdicts valid for alice, bob, charlie` using Alice `38FECA55-03C1-4907-BD9D-8E64BF8E3469`, Bob `347FB118-10D0-40C8-A05B-B0C3BD6B8CCD`, and Charlie `5BA69F1C-B112-47BE-B1FF-8C1003728C8F`; relay proof run id `1778651826540`, shared dir `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_ge004_c4neXY`, with per-role proof for `readdedCharlie`, all-member final membership, one post-readd send per role, two received post-readd messages per role, and actual durable recipient sets. Dart format and `git diff --check` passed. Residual-only none; GE-005 is the next unresolved P0 session. |
| GE-005 | Remove/re-add loop 20 times with sends between transitions | A/B/C joined. | 1. For 20 cycles remove C, send, re-add C, send. 2. Verify after each cycle. | No cycle leaves A/B/C unable to receive entitled messages; no leaked access in removed windows. | P0 | Covered | Recommended | Required | Required | Required | Required | Covered by code changes plus exact host, criteria, and three-device relay proof. Product fixes: `lib/features/groups/application/rotate_and_distribute_group_key_use_case.dart` now fail-closes key promotion until required recipient key updates are delivered, and `lib/features/groups/application/group_membership_update_listener.dart`, `lib/core/services/incoming_message_router.dart`, `lib/features/groups/presentation/screens/group_info_wired.dart`, `integration_test/group_multi_device_real_harness.dart`, `integration_test/group_multi_party_device_real_harness.dart`, and `lib/main.dart` add a direct signed/encrypted membership-update replay fallback for removed peers. Exact host proof `test/features/groups/integration/group_messaging_smoke_test.dart::GE-005 remove/re-add loop preserves entitlement windows` passed (`+1`), criteria focused proof passed (`+2`), full criteria regression passed (`+148`), member-removal suite passed (`+18`), router direct-message proof passed (`+1`), group-info removal proof passed (`+1`) and full group-info suite passed (`+41`), broader group smoke/membership/resume gate passed (`+118`), and targeted analyzer passed with no issues. Required three-device relay proof passed: `MKNOON_RELAY_ADDRESSES=... dart integration_test/scripts/run_group_multi_party_device_real.dart --scenario ge005 -d 38FECA55-03C1-4907-BD9D-8E64BF8E3469,347FB118-10D0-40C8-A05B-B0C3BD6B8CCD,5BA69F1C-B112-47BE-B1FF-8C1003728C8F`; shared dir `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_ge005_bVllxB`, run id `1778657094153`, verdict `ge005 proof passed: ge005 verdicts valid for alice, bob, charlie`. Final verdicts show 20 `charlie_ge005_self_removed_XX`, 20 `charlie_ge005_rejoined_XX`, 20 `bob_ge005_rotated_key_XX`, 20 Alice removed-window sends with Bob-only durable recipients, 20 Bob re-add sends received by Charlie, final epoch 21, all three final members, and no Charlie removed-window plaintext. Residual-only: none; GE-006, GE-007, GE-008, and GE-009 are now covered separately and GE-010 is now covered separately and GE-012 is now covered separately and GE-017 is the next unresolved P0 session. |
| GE-006 | Remove/re-add while C offline | C goes offline before removal and returns after re-add. | 1. C offline. 2. Remove/send/re-add/send. 3. C online retrieves. | C receives exactly post-readd messages and can send; A/B receive C. | P0 | Covered | Recommended | Required | Required | Required | Required | Covered by exact host, criteria, and required three-device relay proof. `test/features/groups/integration/group_messaging_smoke_test.dart::GE-006 remove/re-add while C offline catches up post-readd only` proves Charlie is offline through removal/re-add, Alice's removed-window message targets Bob only, Charlie drains exactly Alice/Bob post-readd messages after reconnect, renders no removed-window plaintext, and Charlie's post-catch-up send reaches Alice/Bob. Repo-owned `ge006` criteria/runner/harness support exists in `integration_test/scripts/group_multi_party_device_criteria.dart`, `integration_test/scripts/run_group_multi_party_device_real.dart`, `integration_test/group_multi_party_device_real_harness.dart`, and `test/integration/group_multi_party_device_criteria_test.dart`. Focused GE-006 host proof passed (`+1`), focused criteria passed (`+3`), full criteria regression passed (`+151`), scoped analyzer passed with no issues, broader group smoke/membership/resume gate passed (`+119`), format and `git diff --check` passed. Required three-device relay proof passed using Alice `38FECA55-03C1-4907-BD9D-8E64BF8E3469`, Bob `347FB118-10D0-40C8-A05B-B0C3BD6B8CCD`, Charlie `5BA69F1C-B112-47BE-B1FF-8C1003728C8F`; shared dir `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_ge006_yCcSao`, run id `1778663062209`, verdict `ge006 proof passed: ge006 verdicts valid for alice, bob, charlie`. Final role verdicts show epoch 2, Alice removed/re-added Charlie, Bob received removed-window and post-readd traffic, Charlie `removedWindowPlaintextCount: 0`, `postReaddReceivedKeys: [aliceGe006PostReadd,bobGe006PostReadd]`, and Alice/Bob received `charlieGe006PostCatchUp`. Residual-only none; GE-007, GE-008, and GE-009 are now covered separately and GE-010 is now covered separately and GE-012 is now covered separately and GE-017 is the next unresolved P0 session. |
| GE-007 | Remove/re-add while B offline observer catches up | B is remaining member but offline during mutation. | 1. B offline. 2. A removes/re-adds C and sends. 3. B returns. | B converges on final membership and receives all messages it was entitled to. | P0 | Covered | Recommended | Required | Required | Required | Required | Covered by exact host, criteria, broader host, analyzer, and required three-device relay proof. `test/features/groups/integration/group_messaging_smoke_test.dart::GE-007 remove/re-add while B offline observer catches up entitled messages` creates joined Alice/Bob/Charlie state, takes Bob offline before Alice removes Charlie, keeps Bob offline through Charlie re-add plus Alice/Charlie post-readd sends, proves Alice's removed-window durable recipients include Bob, proves Alice and Charlie post-readd messages include Bob, reconnects Bob, drains the three entitled messages exactly once, verifies final A/B/C membership, and proves Bob's post-catch-up send reaches Alice and Charlie. Repo-owned `ge007` criteria/runner/harness support now lives in `integration_test/scripts/group_multi_party_device_criteria.dart`, `integration_test/scripts/run_group_multi_party_device_real.dart`, `integration_test/group_multi_party_device_real_harness.dart`, and `test/integration/group_multi_party_device_criteria_test.dart`. Focused GE-007 host proof passed (`+1`), focused criteria passed (`+3`), full criteria regression passed (`+154`), scoped analyzer passed with no issues, broader group smoke/membership/resume passed (`+120`), format and `git diff --check` passed. Required three-device relay proof passed using Alice `38FECA55-03C1-4907-BD9D-8E64BF8E3469`, Bob `347FB118-10D0-40C8-A05B-B0C3BD6B8CCD`, and Charlie `5BA69F1C-B112-47BE-B1FF-8C1003728C8F`; shared dir `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_ge007_ayKyo8`, run id `1778666286428`, verdict `ge007 proof passed: ge007 verdicts valid for alice, bob, charlie`. Final Bob role verdict records `offlineDuringMutation`, `retrievedInboxAfterReconnect`, A/B/C final membership, entitled keys `[aliceGe007RemovedWindow,aliceGe007PostReadd,charlieGe007PostReadd]`, and post-catch-up publish accepted. Residual-only none; GE-008 and GE-009 are now covered separately and GE-010 is now covered separately and GE-012 is now covered separately and GE-017 is the next unresolved P0 session. |
| GE-008 | Simultaneous send storm during remove/re-add | A/B/C send continuously. | 1. Start sends every small interval. 2. Remove/re-add C. 3. Stop and verify windows. | No entitled message loss; cutoff behavior deterministic; no duplicate timeline spam. | P0 | Covered | Recommended | Required | Recommended | Required | Required | Covered by exact host, criteria, broader host, analyzer, and required three-device relay proof. `test/features/groups/integration/group_messaging_smoke_test.dart::GE-008 simultaneous send storm during remove/re-add keeps entitlement windows exact` creates joined Alice/Bob/Charlie state with duplicate live delivery enabled, runs an A/B/C pre-removal storm, removes Charlie, runs Alice/Bob removed-window sends while Charlie stale sends are rejected without publish, re-adds Charlie, then runs an A/B/C post-readd storm. The proof verifies exact recipient windows, no Charlie removed-window plaintext, no stale Charlie publish, no duplicate timeline spam, no failed/pending sender state, and final A/B/C membership. Repo-owned `ge008` criteria/runner/harness support now lives in `integration_test/scripts/group_multi_party_device_criteria.dart`, `integration_test/scripts/run_group_multi_party_device_real.dart`, `integration_test/group_multi_party_device_real_harness.dart`, and `test/integration/group_multi_party_device_criteria_test.dart`. Focused GE-008 host proof passed (`+1`), focused criteria passed (`+3`), full criteria regression passed (`+157`), scoped analyzer passed with no issues, broader group smoke/membership/resume passed (`+121`), and required three-device relay proof passed using Alice `38FECA55-03C1-4907-BD9D-8E64BF8E3469`, Bob `347FB118-10D0-40C8-A05B-B0C3BD6B8CCD`, and Charlie `5BA69F1C-B112-47BE-B1FF-8C1003728C8F`; shared dir `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_ge008_8DkrLF`, run id `1778668448619`, verdict `ge008 proof passed: ge008 verdicts valid for alice, bob, charlie`. Residual-only none; GE-009 is now covered separately and GE-010 is now covered separately and GE-012 is now covered separately and GE-017 is the next unresolved P0 session. |
| GE-009 | Network partition heals after membership mutation | A/B in partition 1; C in partition 2. | 1. Partition. 2. Remove/re-add C. 3. Heal. 4. Replay/inbox. | All peers converge to same membership/key and message set. | P0 | Covered | Recommended | Required | N/A | Required | Required | Covered by exact fake-network host, criteria, broader host, analyzer, format, diff hygiene, and required three-device relay proof. `test/features/groups/integration/group_messaging_smoke_test.dart::GE-009 network partition heals after membership mutation and replay converges` proves Charlie is unsubscribed/partitioned while Alice removes and re-adds him, Alice/Bob post-readd sends are durable for Charlie but not delivered live, Charlie heals by topic resubscribe plus inbox replay, Charlie receives exactly Alice/Bob post-readd messages, Charlie's post-heal send reaches Alice/Bob, and all roles converge to the same GE-009 message set, A/B/C membership, and key epoch. Repo-owned `ge009` criteria/runner/harness support lives in `integration_test/scripts/group_multi_party_device_criteria.dart`, `integration_test/scripts/run_group_multi_party_device_real.dart`, `integration_test/group_multi_party_device_real_harness.dart`, and `test/integration/group_multi_party_device_criteria_test.dart`. Focused GE-009 host passed (`+1`), focused criteria passed (`+3`), full criteria regression passed (`+160`), scoped analyzer passed with no issues, broader group smoke/membership/resume passed (`+122`), format and `git diff --check` passed, and required three-device relay proof passed using Alice `38FECA55-03C1-4907-BD9D-8E64BF8E3469`, Bob `347FB118-10D0-40C8-A05B-B0C3BD6B8CCD`, Charlie `5BA69F1C-B112-47BE-B1FF-8C1003728C8F`; shared dir `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_ge009_HzR9rw`, run id `1778670455217`, verdict `ge009 proof passed: ge009 verdicts valid for alice, bob, charlie`. Residual-only none; GE-010 is now covered separately and GE-012 is now covered separately and GE-017 is the next unresolved P0 session. |
| GE-010 | Zero live topic peers plus inbox fallback | A sends when B/C not live topic peers. | 1. Disconnect topic mesh. 2. A sends/stores. 3. B/C return. | B/C retrieve entitled messages; sender status honest about fallback. | P0 | Covered | Recommended | Required | Required | Required | Required | Covered by exact fake-network host, criteria, broader host, scoped analyzer, formatting, diff hygiene, and required three-device relay proof. `test/features/groups/integration/group_messaging_smoke_test.dart::GE-010 zero live topic peers use durable inbox fallback and receivers recover` creates joined Alice/Bob/Charlie private group state, removes Bob and Charlie from live topic peer delivery without removing group/member/key state, has Alice send with `publishTopicPeersOverride: 0`, proves Alice reports `successNoPeers`, `topicPeers == 0`, sender status `sent`, `inboxStored == true`, no retry payload, durable recipients Bob and Charlie, no live publish/delivery, and then proves Bob and Charlie recover the Alice message exactly once through durable replay after returning. Repo-owned `ge010` criteria/runner/harness support lives in `integration_test/scripts/group_multi_party_device_criteria.dart`, `integration_test/scripts/run_group_multi_party_device_real.dart`, `integration_test/group_multi_party_device_real_harness.dart`, and `test/integration/group_multi_party_device_criteria_test.dart`. Focused GE-010 host passed (`+1`), focused criteria passed (`+3`), full criteria regression passed (`+163`), scoped analyzer passed with no issues, broader group smoke/membership/resume passed (`+123`), and required three-device relay proof passed using Alice `38FECA55-03C1-4907-BD9D-8E64BF8E3469`, Bob `347FB118-10D0-40C8-A05B-B0C3BD6B8CCD`, Charlie `5BA69F1C-B112-47BE-B1FF-8C1003728C8F`; shared dir `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_ge010_28Xi9i`, run id `1778672213356`, verdict `ge010 proof passed: ge010 verdicts valid for alice, bob, charlie`. Residual-only none; GE-012 is now covered separately and GE-017 is the next unresolved P0 session. |
| GE-011 | Partial live topic peers plus inbox fallback | B is live; C is offline. | 1. A sends. 2. B live receives. 3. C retrieves later. | B gets live; C gets inbox; both dedupe if duplicated. | P0 | Covered | Recommended | Required | Required | Required | Required | Covered by exact fake-network host, criteria, broader host, scoped analyzer, formatting, diff hygiene, and required three-device relay proof. `test/features/groups/integration/group_messaging_smoke_test.dart::GE-011 partial live topic peers use live plus inbox fallback and dedupe` creates joined Alice/Bob/Charlie private group state, keeps Bob as a live topic peer while Charlie is off the live topic, has Alice send with `publishTopicPeersOverride: 1`, proves Alice reports `success`, `topicPeers == 1`, sender status `sent`, durable inbox custody for Bob and Charlie, and live delivery only to Bob, then injects duplicate durable replay and proves Bob still persists exactly one copy after live receipt while Charlie receives exactly one copy after rejoining/draining. Repo-owned `ge011` criteria/runner/harness support lives in `integration_test/scripts/group_multi_party_device_criteria.dart`, `integration_test/scripts/run_group_multi_party_device_real.dart`, `integration_test/group_multi_party_device_real_harness.dart`, and `test/integration/group_multi_party_device_criteria_test.dart`. Focused GE-011 host passed (`+1`), focused criteria passed (`+3`), full criteria regression passed (`+166`), scoped analyzer passed with no issues, broader group smoke/membership/resume passed (`+124`), and required three-device relay proof passed using Alice `38FECA55-03C1-4907-BD9D-8E64BF8E3469`, Bob `347FB118-10D0-40C8-A05B-B0C3BD6B8CCD`, Charlie `5BA69F1C-B112-47BE-B1FF-8C1003728C8F`; shared dir `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_ge011_gfMpyN`, run id `1778673583563`, verdict `ge011 proof passed: ge011 verdicts valid for alice, bob, charlie`. Residual-only none; GE-012 is now covered separately and GE-017 is the next unresolved P0 session. |
| GE-012 | Multi-device same user in group | B has devices B1/B2. | 1. A sends. 2. B1 sends. 3. B2 sends. 4. Inspect all devices. | Sibling devices receive or sync according to product rule and do not get rejected/skipped incorrectly. | P0 | Covered | Recommended | Required | Required | Required | Required | Covered by product/harness fixes plus exact host, criteria, broader host, analyzer, formatting, diff hygiene, and required three-device relay proof. Product group send call sites now pass the current P2P transport peer as `senderDeviceId` and `senderTransportPeerId` so same-account devices that share a logical signing key bind outgoing envelopes to the active transport; GE-012 harness sends do the same, and restored Bob sibling starts with a fresh transport identity while preserving logical account identity. Exact send-use-case proof `test/features/groups/application/send_group_message_use_case_test.dart::production send honors requested transport for same-key sibling devices` passed (`+1`). Exact fake-network proof `test/features/groups/integration/group_messaging_smoke_test.dart::GE-012 same-user Bob devices exchange without sibling rejection` passed (`+1`), proving Alice sends to both Bob devices, Bob primary sends to Alice and mirrored sibling, Bob sibling sends to Alice and mirrored primary, no duplicate logical Bob membership row appears, and no sibling rejection occurs. Supporting `ge012` criteria/runner/harness coverage lives in `integration_test/scripts/group_multi_party_device_criteria.dart`, `integration_test/scripts/run_group_multi_party_device_real.dart`, `integration_test/group_multi_party_device_real_harness.dart`, `integration_test/group_multi_device_real_harness.dart`, and `test/integration/group_multi_party_device_criteria_test.dart`. Focused criteria passed (`+3`), full criteria regression passed (`+169`), scoped analyzer passed with no issues, broader group smoke/membership/resume passed (`+125`), and required relay proof passed using Alice `38FECA55-03C1-4907-BD9D-8E64BF8E3469`, Bob primary `347FB118-10D0-40C8-A05B-B0C3BD6B8CCD`, Bob sibling `5BA69F1C-B112-47BE-B1FF-8C1003728C8F`; shared dir `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_ge012_j0W7hh`, run id `1778676353899`, verdict `ge012 proof passed: ge012 verdicts valid for alice, bob, charlie`. Residual-only none; GE-017 is the next unresolved P0 session. |
| GE-013 | Device revocation during active group | B2 revoked while B1 remains. | 1. B2 sends before/after revoke. 2. A/B1 receive. 3. A sends. | B2 post-revoke messages rejected; B1 remains functional. | P0 | Covered | Recommended | Required | N/A | Required | Required | Covered by exact host, criteria, broader host, analyzer, formatting, diff hygiene, and required three-device relay proof. `test/features/groups/integration/group_messaging_smoke_test.dart::GE-013 revoked Bob sibling device cannot send while B1 remains functional` creates Alice plus logical Bob with B1/B2 active, proves B2 can send before revocation and Alice/B1 persist it exactly once, revokes only B2's device identity while B1 remains active, proves B2 post-revoke send returns `unauthorized` without a bridge publish/inbox store or any post-revoke B2 plaintext, then proves B1 sends to Alice and Alice sends to B1 after the revoke. Supporting `ge013` criteria/runner/harness coverage lives in `integration_test/scripts/group_multi_party_device_criteria.dart`, `integration_test/scripts/run_group_multi_party_device_real.dart`, `integration_test/group_multi_party_device_real_harness.dart`, `integration_test/group_multi_device_real_harness.dart`, and `test/integration/group_multi_party_device_criteria_test.dart`; criteria accepts valid device-revocation verdicts and rejects post-revoke B2 plaintext or accepted B2 post-revoke sends. Focused GE-013 host passed (`+1`), focused criteria passed (`+3`), full criteria regression passed (`+172`), scoped analyzer passed with no issues, broader group smoke/membership/resume passed (`+126`), and required relay proof passed using Alice `38FECA55-03C1-4907-BD9D-8E64BF8E3469`, Bob primary `347FB118-10D0-40C8-A05B-B0C3BD6B8CCD`, Bob sibling `5BA69F1C-B112-47BE-B1FF-8C1003728C8F`; shared dir `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_ge013_ypH6Cb`, run id `1778677974482`, verdict `ge013 proof passed: ge013 verdicts valid for alice, bob, charlie`. Residual-only none; GE-017 is the next unresolved P0 session. |
| GE-014 | App restart on one member during re-add | C restarts after receiving invite but before joining topic. | 1. Re-add C. 2. Restart C node/app. 3. Join/retrieve/send. | C recovers persisted invite/key and receives post-readd messages. | P0 | Covered | Recommended | Required | Required | Required | Required | Covered by test/harness-only GE-014 proof with no production changes. Exact host proof `test/features/groups/integration/group_messaging_smoke_test.dart::GE-014 re-added Charlie recovers persisted invite key after restart before topic join` passed (`+1`), proving Charlie persists the re-add invite/key, restarts before joining the topic, recovers the persisted group/member/key state, receives Alice/Bob post-readd messages, and can send after recovery. Supporting `ge014` criteria, runner, and three-device harness coverage lives in `integration_test/scripts/group_multi_party_device_criteria.dart`, `integration_test/scripts/run_group_multi_party_device_real.dart`, `integration_test/group_multi_party_device_real_harness.dart`, and `test/integration/group_multi_party_device_criteria_test.dart`. Passed evidence: format on the five owned files, focused criteria (`+6`), full criteria regression (`+178`), scoped analyzer clean, exact three-device relay command with Alice `38FECA55-03C1-4907-BD9D-8E64BF8E3469`, Bob `347FB118-10D0-40C8-A05B-B0C3BD6B8CCD`, Charlie `5BA69F1C-B112-47BE-B1FF-8C1003728C8F`, shared dir `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_ge014_E6ghJG`, run id `1778682492377`, orchestrator verdict `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_ge014_E6ghJG/gmp_1778682492377_ge014_orchestrator_verdict.json`, result `ge014 proof passed: ge014 verdicts valid for alice, bob, charlie`, role verdicts for Alice/Bob/Charlie in the same dir, and `git diff --check` on owned files plus plan. Residual-only none; GE-017 is the next unresolved P0 session. |
| GE-015 | App restart on admin during mutation | A restarts after local config update before key fanout completes. | 1. Start add/remove. 2. Restart A. 3. Resume/repair. 4. Verify all peers. | No partial mutation leaves members stranded; retry/fanout status is honest. | P0 | Covered | Recommended | Required | N/A | Required | Required | Covered by code changes plus exact host, criteria, full criteria, broader host, analyzer, formatting, diff hygiene, and required three-device relay proof. `lib/features/groups/application/record_group_invite_delivery_attempts.dart` now records pending add/re-add invite fanout as `needsResend` before invite delivery completes, and `lib/features/groups/presentation/screens/contact_picker_wired.dart` records that pending status after local member/config mutation while rolling it back if bridge config update fails. `test/features/groups/integration/group_messaging_smoke_test.dart::GE-015 admin restart during add/remove repairs fanout honestly` proves remove-fanout interruption before admin restart, fail-closed key promotion, post-restart repair, no removed-window Charlie plaintext, durable add/re-add invite `needsResend` status before repair, final sent status, and active peer convergence. Supporting `ge015` criteria/runner/harness coverage lives in `integration_test/scripts/group_multi_party_device_criteria.dart`, `integration_test/scripts/run_group_multi_party_device_real.dart`, `integration_test/group_multi_party_device_real_harness.dart`, and `test/integration/group_multi_party_device_criteria_test.dart`; `test/features/groups/presentation/contact_picker_wired_test.dart` covers invite-status hardening. Passed evidence: focused GE-015 host (`+1`), focused GE-015 criteria (`+4`), full criteria (`+182`), scoped analyzer clean, Dart-only format, full contact-picker suite (`+21`), full group messaging smoke (`+31`), `git diff --check`, and required relay proof `--scenario ge015` with shared dir `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_ge015_zhM5bw`, run id `1778685690376`, orchestrator verdict `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_ge015_zhM5bw/gmp_1778685690376_ge015_orchestrator_verdict.json`, and verdict `ge015 verdicts valid for alice, bob, charlie`. Residual-only none; GE-017 is the next unresolved P0 session. |
| GE-016 | Two admins mutate membership concurrently if supported | A and B admins. | 1. A removes C while B re-adds/adds D. 2. Deliver events in random order. 3. Verify convergence. | Conflict resolution is deterministic and all peers converge. | P1 | Covered | Recommended | Required | N/A | Required | Required | Covered on 2026-05-14 by exact host concurrent-admin mutation proof plus required relay-backed three-party `ge016` device proof. `test/features/groups/integration/group_membership_smoke_test.dart::GE-016 two admins mutate membership concurrently and converge` holds deliveries while two admins mutate membership concurrently, releases them in varied order, and proves active-peer convergence plus removed-member cleanup. Repo-owned `ge016` support in `integration_test/group_multi_party_device_real_harness.dart`, `integration_test/scripts/run_group_multi_party_device_real.dart`, `integration_test/scripts/group_multi_party_device_criteria.dart`, and `test/integration/group_multi_party_device_criteria_test.dart` proves Alice prepares stale Charlie removal, Bob adds synthetic Dana as a newer admin mutation, the delayed stale removal is rejected by version ordering, deterministic winner is `bob_add_dana`, A/B/C converge, Charlie and Dana remain present, and roles converge. Passed evidence: exact GE-016 host (`+1`), focused GE-016 criteria (`+2`), full criteria (`+191`), scoped analyzer clean, adjacent group smoke/membership/resume (`+133`), required device command `MKNOON_RELAY_ADDRESSES=/dns/mknoun.xyz/tcp/4001/wss/p2p/12D3KooWGMYMmN1RGUYjWaSV6P3XtnBjwnosnJGNMnttfVCRnd6g,/dns/mknoun.xyz/udp/4002/quic-v1/p2p/12D3KooWGMYMmN1RGUYjWaSV6P3XtnBjwnosnJGNMnttfVCRnd6g dart run integration_test/scripts/run_group_multi_party_device_real.dart --scenario ge016 -d 38FECA55-03C1-4907-BD9D-8E64BF8E3469,347FB118-10D0-40C8-A05B-B0C3BD6B8CCD,5BA69F1C-B112-47BE-B1FF-8C1003728C8F`, shared dir `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_ge016_6ktSvg`, run id `1778727856388`, final result `ge016 proof passed: ge016 verdicts valid for alice, bob, charlie`. Residual-only none; GE-021 is now covered separately, and GE-023 is now covered separately; GE-024 is now covered separately; GO-005 is now covered separately; GO-006 is now covered separately; GO-007 is now covered separately; GO-010 is now covered separately. |
| GE-017 | Property test random membership operations preserve invariants | Model with A/B/C/D and random operations. | 1. Generate sequences add/remove/readd/send/offline/online/rotate. 2. Run fake network. 3. Check invariants after each step. | No unauthorized decrypt, no entitled message loss after recovery, no duplicate active membership, no panics. | P0 | Covered | Recommended | Required | N/A | Required | Required | Covered by exact deterministic host fake-network property proof. `test/features/groups/integration/group_messaging_smoke_test.dart::GE-017 seeded random membership operations preserve invariants` runs seeds `17017`, `17018`, and `17019` with 30 operations per seed over A/B/C/D, including add, remove, re-add, send, inactive send, offline, online, restart, duplicate live delivery, and key rotation. The oracle records seed/step/operation/active/online context and proves no non-entitled plaintext, no entitled message loss after recovery, no duplicate active membership or device rows, inactive sends do not publish or mutate local messages, active peers converge on membership/key epoch, and no operation panics. Passed evidence: final `dart format --set-exit-if-changed test/features/groups/integration/group_messaging_smoke_test.dart`, final `dart analyze test/features/groups/integration/group_messaging_smoke_test.dart`, focused GE-017 host (`+1`), and final named `./scripts/run_test_gates.sh groups` (`+157`). No production, Go, simulator, runner, or device-harness changes were required; residual-only none. GE-018, GE-019, GE-020, and GO-001 are covered separately; GO-002 is covered separately and GO-003 is covered separately and GO-012 is covered separately; GL-010 is covered separately; GL-016 is covered separately; GR-002 is the next unresolved session in ordered ledger order. |
| GE-018 | Property test random envelope tampering never renders plaintext | Fuzzer mutates envelope fields. | 1. Generate valid envelopes. 2. Mutate fields. 3. Run validator/replay. | Only exactly valid authorized envelopes render; all others reject with safe diagnostics. | P0 | Covered | Required | Required | N/A | Recommended | N/A | Covered by exact deterministic Go validator/live-path and Dart durable-replay tamper proofs. `go-mknoon/node/pubsub_test.go::TestGE018SeededEnvelopeFieldTamperingValidatorClassifiesFailClosed` runs fixed seeds `18018`, `18019`, and `18020` over malformed JSON, version/type/group/sender/device/transport/public-key/key-package, ciphertext, nonce, key-epoch, signature, missing-field, and legacy public-key forgery mutations and proves only valid controls accept. `go-mknoon/node/pubsub_decryption_failure_test.go::TestGE018SeededEnvelopeTamperingLivePathNeverRendersPlaintext` proves valid live control render and representative tampered raw-publish no-render/no-plaintext behavior. `test/features/groups/application/drain_group_offline_inbox_use_case_test.dart::GE-018 seeded offline replay envelope tampering rejects before plaintext render` proves valid replay persists once while replay mutations leave no message, cursor, receipt, or decrypt side effects. Supporting stale GEK003 replay proof was updated to match the current GE-005 fail-closed rotation contract. Passed evidence: focused GE-018 Go and Dart tests, adjacent Go tamper selector, Dart format, scoped Dart analyzer with only existing info-level notes, focused GEK003, full offline replay suite (`+79`), named `./scripts/run_test_gates.sh groups` (`+157`), and full `cd go-mknoon && go test ./...`. No production, simulator, runner, or device-harness changes were required; residual-only none. GE-019, GE-020, and GO-001 are covered separately; GO-002 is covered separately and GO-003 is covered separately and GO-012 is covered separately; GL-010 is covered separately; GL-016 is covered separately; GR-002 is the next unresolved session in ordered ledger order. |
| GE-019 | Property test random key rotations preserve access windows | Model key epochs and membership windows. | 1. Random rotate/remove/add/send. 2. Try decrypt per peer. 3. Compare model. | Peers decrypt exactly model-authorized messages. | P0 | Covered | Required | Required | N/A | Required | N/A | Covered by exact deterministic host fake-network property proof. `test/features/groups/integration/group_messaging_smoke_test.dart::GE-019 seeded random key rotations preserve access windows` runs seeds `19019`, `19020`, and `19021` with 36 operations per seed over an A/B/C/D model, including guaranteed and random sends, key rotations, removals, re-adds, and inactive sends. The oracle records seed/step/operation/active-set/current-epoch context and proves senders and active-at-send recipients persist each modeled plaintext exactly once at the modeled key epoch, peers removed or not yet added at send time never render that plaintext even after later re-add, inactive sends do not publish or mutate local messages, active peers converge on the modeled member set and latest key epoch after every operation, and no duplicate active member/device rows appear. Passed evidence: RED baseline selector exited 79/no tests matched; `dart format --set-exit-if-changed test/features/groups/integration/group_messaging_smoke_test.dart`; `dart analyze test/features/groups/integration/group_messaging_smoke_test.dart` with no issues; focused GE-019 host (`+1`); and named `./scripts/run_test_gates.sh groups` (`+158`). No production, Go, simulator, runner, or device-harness changes were required because the row's required proof is fake-network integration and Smoke/3-Party E2E are N/A. Residual-only none; GE-020 and GO-001 are covered separately and GO-002 is covered separately and GO-003 is covered separately and GO-012 is covered separately; GL-010 is covered separately; GL-016 is covered separately; GR-002 is the next unresolved session in ordered ledger order. |
| GE-020 | Long soak private group with churn | Three or more devices over extended fake time. | 1. Run sends, disconnects, relay refreshes, add/remove/readd. 2. Periodically assert convergence. | No permanent deaf member, goroutine leak, or retry queue loss. | P0 | Covered | Recommended | Required | N/A | Required | Required | Covered by exact deterministic host fake-network soak proof plus exact `ge020` three-party relay proof. `test/features/groups/integration/group_messaging_smoke_test.dart::GE-020 long soak private group with churn preserves convergence` runs seeds `20020`, `20021`, and `20022` with 44 operations per seed across A/B/C/D, covering sends, offline held delivery, online recovery, relay refresh/rejoin, remove, re-add, inactive sends, key rotation, restart, and duplicate send delivery. The oracle records seed/step/operation/active/online/current-epoch context and proves no permanent deaf active member, no entitled message loss after recovery, no removed-window or not-yet-added plaintext, no inactive-send side effects, active membership/key convergence, no duplicate active member/device rows, drained held-delivery queues, and no stranded retry/held queues. Repo-owned `ge020` criteria/runner/harness support lives in `integration_test/scripts/group_multi_party_device_criteria.dart`, `integration_test/scripts/run_group_multi_party_device_real.dart`, `integration_test/group_multi_party_device_real_harness.dart`, and `test/integration/group_multi_party_device_criteria_test.dart`; criteria accepts valid `ge020LongSoakChurnProof` role verdicts and rejects deaf-member, stranded-queue, and convergence failures. Passed evidence: RED baseline selector exited 79/no tests matched; final `dart format --set-exit-if-changed` and `dart analyze` on the five GE-020 owner Dart files passed; focused GE-020 host passed (`+1`); focused GE-020 criteria passed (`+2`); named `./scripts/run_test_gates.sh groups` passed (`+159`); required relay-backed device proof passed with Alice `347FB118-10D0-40C8-A05B-B0C3BD6B8CCD`, Bob `5BA69F1C-B112-47BE-B1FF-8C1003728C8F`, Charlie `1B098DFF-6294-407A-A209-BBF360893485`, shared dir `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_ge020_9KMoiD`, run id `1778694622398`, role verdicts in that dir, and final result `ge020 proof passed: ge020 verdicts valid for alice, bob, charlie`; `git diff --check` on owned files plus closure docs passed. Residual-only none; GO-001 is covered separately and GO-002 is covered separately and GO-003 is covered separately and GO-012 is covered separately; GL-010 is covered separately; GL-016 is covered separately; GR-002 is the next unresolved session in ordered ledger order. |
| GE-021 | Large group with one flaky member | 10+ members; one repeatedly offline/online and removed/readded. | 1. Send from several members. 2. Churn flaky member. 3. Verify all stable members. | Stable members never miss entitled messages because one member is flaky. | P1 | Covered | Recommended | Required | N/A | Required | Required | Covered on 2026-05-14 by exact host large-group flaky-member proof plus required relay-backed three-party `ge021` device proof. `test/features/groups/integration/group_messaging_smoke_test.dart::GE-021 large group with one flaky member preserves stable delivery` uses an 11-member private group, sends from several stable members, repeatedly holds/releases flaky-member delivery, removes and readds the flaky member, and proves all stable entitled members retain expected messages with no stable delivery loss caused by the flaky peer. Repo-owned `ge021` support in `integration_test/group_multi_party_device_real_harness.dart`, `integration_test/scripts/run_group_multi_party_device_real.dart`, `integration_test/scripts/group_multi_party_device_criteria.dart`, and `test/integration/group_multi_party_device_criteria_test.dart` proves an 11-member roster with Alice/Bob stable real devices, Charlie as the flaky real device, eight synthetic stable members with real generated signing and ML-KEM public material, flaky live leave/rejoin, removal/readd, removed-window exclusion, no stable-member miss, no stranded delivery, no removed-window leak, and final roster/epoch convergence. Passed evidence: Dart format, exact GE-021 host (`+1`), full criteria (`+193`), scoped analyzer clean, adjacent group smoke/membership/resume (`+134`), required device command `MKNOON_RELAY_ADDRESSES=/dns/mknoun.xyz/tcp/4001/wss/p2p/12D3KooWGMYMmN1RGUYjWaSV6P3XtnBjwnosnJGNMnttfVCRnd6g,/dns/mknoun.xyz/udp/4002/quic-v1/p2p/12D3KooWGMYMmN1RGUYjWaSV6P3XtnBjwnosnJGNMnttfVCRnd6g dart run integration_test/scripts/run_group_multi_party_device_real.dart --scenario ge021 -d 38FECA55-03C1-4907-BD9D-8E64BF8E3469,347FB118-10D0-40C8-A05B-B0C3BD6B8CCD,5BA69F1C-B112-47BE-B1FF-8C1003728C8F`, shared dir `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_ge021_npLDuk`, run id `1778730737810`, final result `ge021 proof passed: ge021 verdicts valid for alice, bob, charlie`. Residual-only none; GE-023 is now covered separately; GE-024 is now covered separately; GO-005 is now covered separately; GO-006 is now covered separately; GO-007 is now covered separately; GO-010 is now covered separately. |
| GE-023 | Media/attachments in private group through remove/re-add | Message Extra includes media. | 1. Send media before/remove/readd/after. 2. Retrieve. 3. Render. | Entitled members receive media metadata/content; removed-window media inaccessible to C. | P1 | Covered | Recommended | Required | N/A | Required | Required | Covered on 2026-05-14 by exact host media entitlement proof plus required relay-backed three-party `ge023` device proof. `test/features/groups/integration/group_messaging_smoke_test.dart::GE-023 media attachments in private group through remove/re-add respect entitlement` sends encrypted image media before removal, during Charlie's removed window, and after re-add; proves Alice/Bob/Charlie receive entitled media metadata/content, Charlie never receives or persists removed-window media, sender media stays local/done, and attachment metadata carries content hash and encryption metadata. Repo-owned `ge023` harness/criteria support lives in `integration_test/group_multi_party_device_real_harness.dart`, `integration_test/group_multi_device_real_harness.dart`, `integration_test/scripts/run_group_multi_party_device_real.dart`, `integration_test/scripts/group_multi_party_device_criteria.dart`, and `test/integration/group_multi_party_device_criteria_test.dart`; the harness wires SQL-backed `MediaAttachmentRepositoryImpl` and decrypts recorded `group:inboxStore` replay payloads through `decodeInboxMessage` for durable media proof. Passed evidence: exact GE-023 host (`+1`), full criteria (`+195`), scoped analyzer clean, adjacent group smoke/membership/resume (`+135`), exact device command `MKNOON_RELAY_ADDRESSES=/dns/mknoun.xyz/tcp/4001/wss/p2p/12D3KooWGMYMmN1RGUYjWaSV6P3XtnBjwnosnJGNMnttfVCRnd6g,/dns/mknoun.xyz/udp/4002/quic-v1/p2p/12D3KooWGMYMmN1RGUYjWaSV6P3XtnBjwnosnJGNMnttfVCRnd6g dart run integration_test/scripts/run_group_multi_party_device_real.dart --scenario ge023 -d 38FECA55-03C1-4907-BD9D-8E64BF8E3469,347FB118-10D0-40C8-A05B-B0C3BD6B8CCD,5BA69F1C-B112-47BE-B1FF-8C1003728C8F`, shared dir `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_ge023_7Y5Mx0`, run id `1778733356540`, final result `ge023 proof passed: ge023 verdicts valid for alice, bob, charlie`, and diff hygiene. |
| GE-024 | Quoted replies across membership boundary | C is re-added after original quoted message. | 1. A sends M1. 2. Remove/readd C. 3. Reply quoting M1. 4. C receives. | Quote rendering follows history entitlement; no crash if quoted message unavailable. | P1 | Covered | Recommended | Required | N/A | Required | Required | Covered on 2026-05-14 by exact host quoted-reply entitlement proof, exact widget unavailable-parent fallback proof, and required relay-backed three-party `ge024` device proof. `test/features/groups/integration/group_messaging_smoke_test.dart::GE-024 quoted replies across membership boundary preserve entitlement fallback` sends a before-removal parent, removes Charlie, sends a removed-window parent, re-adds Charlie, then has Bob send replies quoting both parents; it proves `quotedMessageId` propagation, Charlie can resolve the entitled parent, Charlie lacks removed-window parent plaintext, and unavailable quote fallback stays safe. `test/features/groups/presentation/group_conversation_screen_test.dart::GE-024 renders available and unavailable quote parents without crashing` proves available and missing quote parents render without crash. Repo-owned `ge024` harness/criteria support lives in `integration_test/group_multi_party_device_real_harness.dart`, `integration_test/scripts/run_group_multi_party_device_real.dart`, `integration_test/scripts/group_multi_party_device_criteria.dart`, and `test/integration/group_multi_party_device_criteria_test.dart`; criteria validates quote IDs, final membership, Charlie's missing removed-window parent, no removed-window plaintext, and `noCrashRenderingUnavailableQuote`. Passed evidence: exact GE-024 host (`+1`), exact GE-024 widget (`+1`), full criteria (`+197`), scoped analyzer clean, adjacent group smoke/membership/resume (`+136`), exact device command `MKNOON_RELAY_ADDRESSES=/dns/mknoun.xyz/tcp/4001/wss/p2p/12D3KooWGMYMmN1RGUYjWaSV6P3XtnBjwnosnJGNMnttfVCRnd6g,/dns/mknoun.xyz/udp/4002/quic-v1/p2p/12D3KooWGMYMmN1RGUYjWaSV6P3XtnBjwnosnJGNMnttfVCRnd6g dart run integration_test/scripts/run_group_multi_party_device_real.dart --scenario ge024 -d 38FECA55-03C1-4907-BD9D-8E64BF8E3469,347FB118-10D0-40C8-A05B-B0C3BD6B8CCD,5BA69F1C-B112-47BE-B1FF-8C1003728C8F`, shared dir `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_ge024_vdTEXj`, run id `1778735185620`, final result `ge024 proof passed: ge024 verdicts valid for alice, bob, charlie`, and diff hygiene. |

## Observability, Failure Honesty, and Release Gates

| Test ID | Scenario | Preconditions | Steps | Expected Result | Priority | Current status | Unit | Integration | Smoke | Fake Network | 3-Party E2E | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| GO-001 | Sender status distinguishes pubsub success with zero peers | Publish succeeds but topicPeerCount=0. | 1. Send with zero peers. 2. App observes return and inbox result. 3. Inspect status. | Sender UI/API shows not-live/fallback-pending or durable status, not misleading delivered-to-all. | P0 | Covered | Recommended | Required | Required | Recommended | Required | Covered on 2026-05-13 by exact app, Go, gate, and required three-party device proof. `send_group_message_use_case.dart` now emits `status`, `topicPeers: 0`, `inboxStored: true`, and `inboxPending: false` for the success-no-peers flow/timing path while preserving durable `sent` status. `send_group_message_use_case_test.dart::GO-001 zero topic peers exposes durable fallback sender status` proves the result, saved row, durable inbox recipient set, no retry payload, flow event, and timing event all distinguish zero-live-peer success. `go-mknoon/node/pubsub_delivery_test.go::TestGO001PublishGroupMessageReportsZeroTopicPeers` proves native publish succeeds with caller id and peer count 0. `go001` criteria/runner/harness support reuses the zero-live-topic proof and the relay-backed command passed with shared dir `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_go001_2Xf4af`, run id `1778696136962`, verdict `go001 proof passed: go001 verdicts valid for alice, bob, charlie`. Format, scoped analyzer, focused GO-001 Dart/criteria tests, focused Go proof, `./scripts/run_test_gates.sh groups` (`+159`), live device availability check, and diff hygiene passed. |
| GO-002 | Sender status distinguishes inbox store failure | PubSub publish succeeds but GroupInboxStore fails. | 1. Force inbox failure. 2. Inspect sender status/retry queue. | Message is staged for retry or marked partial; not silently considered reliable. | P0 | Covered | Recommended | Required | Required | Recommended | Required | Covered on 2026-05-13 by exact send-use-case proof, retry proof, criteria proof, named gate, and required three-party device proof. Existing production behavior already stages live-publish/inbox-store-failure rows honestly; `test/features/groups/application/send_group_message_use_case_test.dart::GO-002 publish success with inbox failure stays pending and retryable` proves PubSub success with `topicPeers == 2` plus forced `group:inboxStore` failure saves/returns a `pending` row with `inboxStored == false`, a retry payload, no wire envelope, durable recipients preserved, and flow/timing details that expose pending/inbox failure state. `test/features/groups/application/retry_failed_group_inbox_stores_use_case_test.dart::GO-002 retry promotes pending inbox store failure to sent` proves one durable retry stores payload id `go002-pending`, promotes status to `sent`, sets `inboxStored == true`, clears the retry payload, and emits retry success/timing events. `go002` criteria/runner/harness support in `integration_test/scripts/group_multi_party_device_criteria.dart`, `integration_test/scripts/run_group_multi_party_device_real.dart`, `integration_test/group_multi_party_device_real_harness.dart`, and `test/integration/group_multi_party_device_criteria_test.dart` forces Alice's inbox-store call to fail, rejects silently reliable sender verdicts before retry, and proves retry promotion plus Bob/Charlie convergence. Format, scoped analyzer, focused GO-002 send/retry/criteria tests, `./scripts/run_test_gates.sh groups` (`+159`), live device availability check, required relay-backed `--scenario go002` proof (`go002 proof passed: go002 verdicts valid for alice, bob, charlie`; shared dir `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_go002_4BmY76`; run id `1778697742706`), and diff hygiene passed. |
| GO-003 | Sender status distinguishes validator rejection by recipients | Sender has stale config/key. | 1. Sender publishes stale envelope. 2. Recipients reject. 3. Sender receives/observes no ack. | System detects or repairs stale sender state; user gets honest failure/retry, not permanent phantom sent. | P0 | Covered | Recommended | Required | N/A | Required | Required | Covered on 2026-05-13 by native recipient-to-publisher validator feedback, Flutter diagnostic handling, exact host tests, named groups gate, iOS binding rebuild, and required relay-backed three-party proof. `go-mknoon/node/group_validation_feedback.go` adds bounded `group:publish_validation_rejected` feedback over `GroupValidationFeedbackProtocol`, `go-mknoon/internal/group_envelope.go` carries top-level `messageId`, and `go-mknoon/node/pubsub.go` sends feedback only for rejected `group_message` envelopes with message ids. `go-mknoon/node/pubsub_delivery_test.go::TestGO003StaleSenderValidationFeedbackReturnsToPublisher` proves stale Charlie traffic is rejected by Alice/Bob, Charlie receives exact publisher feedback, and no stale plaintext renders. `lib/core/bridge/go_bridge_client.dart`, `lib/features/groups/application/group_pending_key_repair_service.dart`, and `lib/features/groups/application/group_message_listener.dart` forward the diagnostic and mark the exact outgoing row `failed` while preserving retry wire-envelope metadata; exact GO-003 bridge and listener tests passed. `go003` criteria/runner/harness support proves sender feedback, failed sender status, retryable envelope, and recipient no-plaintext behavior. Passed evidence: scoped analyzer clean, focused bridge (`+1`), listener (`+1`), criteria (`+1`), Go proof (`ok node 5.349s`), `./scripts/ensure_go_ios_bindings.sh`, `./scripts/run_test_gates.sh groups` (`+159`), required device proof `go003 proof passed: go003 verdicts valid for alice, bob, charlie` with shared dir `/var/folders/nd/_55d26s936d0fb_5l9s00t980000gn/T/group_multi_party_go003_71Ln1O`, run id `1778700408366`, role verdicts in that dir, and `git diff --check` on owned files plus closure docs passed. Residual-only none; GO-012 is covered separately; GL-010 is covered separately; GL-016 is covered separately; GR-002 is the next unresolved session in ordered ledger order. |
| GO-004 | Decryption failures are surfaced for repair without exposing plaintext | Wrong epoch/key on receiver. | 1. Deliver wrong-key message. 2. Inspect events/logs. 3. Trigger repair. | Diagnostic includes epochs/group/sender but no plaintext/key; app can request key repair or resync. | P0 | Covered | Required | Required | N/A | Recommended | N/A | Covered on 2026-05-13 by exact native wrong-local-key diagnostic/no-secret proof, exact Flutter bridge diagnostic/redaction proof, exact listener live key-repair proof, and named groups gate evidence. `go-mknoon/node/pubsub_decryption_failure_test.go::TestGO004DecryptionFailureDiagnosticContainsRepairMetadataOnly` publishes a valid signed/encrypted group envelope while the receiver has a wrong local key at the same epoch, observes `group:decryption_failed` with `groupId`, `senderId`, `keyEpoch`, `localKeyEpoch`, AES-GCM error text, and non-negative `decryptMs`, and proves no plaintext marker, group keys, ciphertext, nonce, signature, sender private key, normal message event, or reaction event leaks. `test/core/bridge/go_bridge_client_test.dart` GO-004 selectors prove diagnostic routing to `groupDiagnosticEventStream`, no group-message callback invocation, metadata preservation, and redaction of plaintext/key/ciphertext/nonce/peer/address/secret fields. `test/features/groups/application/group_message_listener_test.dart::GO-004 live decryption failure creates repair placeholder and trigger without plaintext delivery` proves a safe pending placeholder, pending repair row, and `groupKeyRepairReasonLiveDiagnostic` request without plaintext/key/ciphertext persistence. Passed evidence: gofmt, Dart format, scoped analyzer exit 0 with one pre-existing info-level lint at `group_message_listener_test.dart:4516:13`, focused Go proof (`ok node 5.845s`), focused bridge (`+2`), focused listener (`+1`), and `./scripts/run_test_gates.sh groups` (`+159`). Residual-only none; GO-012 is covered separately; GL-010 is covered separately; GL-016 is covered separately; GR-002 is the next unresolved session in ordered ledger order. |
| GO-005 | Validation rejection rate is bounded/deduped | Malicious/stale C spams invalid envelopes. | 1. Send many invalid messages. 2. Inspect emitted validation_rejected events. | Diagnostics are rate-limited by reason/group/sender/transport; app not flooded. | P1 | Covered | Required | Recommended | N/A | Recommended | N/A | Covered on 2026-05-14 by exact native validation-rejection diagnostic rate-limit proof. Existing `go-mknoon/node/pubsub.go::logPubSubValidationReject` already keys the rate limit by reason, group id, sender id, and transport peer id and returns before log/event/feedback emission during `pubsubAuthorizationRejectDiagnosticWindow`. `go-mknoon/node/pubsub_authorization_forward_test.go::TestGO005ValidationRejectDiagnosticsAreRateLimitedByReasonGroupSenderTransport` drives the diagnostic emitter with a fixed clock, proves 50 repeated rejects for the same reason/group/sender/transport emit one log and one `group:validation_rejected` event, proves distinct transport/sender/group/reason dimensions emit separate first diagnostics, proves repeated `missing_key` rejects are deduped within the window, and proves the original key emits again after the window. Passed evidence: gofmt, exact GO-005 Go (`ok node 0.581s`), adjacent LP-002/GA-026 validator diagnostics selector (`ok node 0.389s`), selected native race selector (`ok node 97.138s`), Flutter send/drain app-facing gate (`+168`), and diff hygiene passed. Residual-only none; GO-006 is now covered separately; GO-007 is now covered separately; GO-010 is now covered separately. |
| GO-006 | Discovery events expose missing peer condition | Group has missing D. | 1. Run discovery. 2. Inspect group:discovery events. | Events show expectedPeers/topicPeers/missing/backoff enough to diagnose delivery gaps. | P1 | Covered | Required | Recommended | N/A | Recommended | N/A | Covered on 2026-05-14 by native discovery diagnostic fields plus exact missing-peer/backoff proof. `go-mknoon/node/pubsub.go` now emits `topicPeers`, `expectedPeers`, `missingPeers`, and `backingOff` on `discover_result`, emits `missingPeers` and `backingOff` on publish peer refresh begin/done diagnostics, and routes discovery backoff through `emitGroupDiscoveryBackoff` with connected/expected/topic/missing counts, `backingOff`, consecutive failure count, next interval fields, and warm retry state. Existing `lib/core/bridge/go_bridge_client.dart` already forwards `group:discovery` into app diagnostics. `go-mknoon/node/pubsub_test.go::TestGO006DiscoveryEventsExposeMissingPeerCondition` proves a group with one missing expected peer emits `discover_result` with `topicPeers == 0`, `expectedPeers == 1`, `missingPeers == 1`, `backingOff == false`, then proves the backoff event exposes the same missing-peer condition plus cadence fields and `backingOff == true`. Passed evidence: gofmt, exact GO-006 Go (`ok node 0.554s`), adjacent discovery/backoff selector (`ok node 0.469s`), selected native race selector (`ok node 99.377s`), Flutter send/drain app-facing gate (`+168`), and diff hygiene passed. Residual-only none; GO-007 is now covered separately; GO-010 is now covered separately. |
| GO-007 | Metrics distinguish host connection from live topic peer | Peer connected but not in ListPeers. | 1. Connect peer without topic. 2. Emit status/debug. 3. Send. | Logs/status make topic-peer absence visible, not just connectedness. | P1 | Covered | Recommended | Required | N/A | Recommended | N/A | Covered on 2026-05-14 by exact native host-connected versus live-topic-peer proof. Existing `go-mknoon/node/pubsub.go` relies on `topic.ListPeers` through `liveGroupTopicPeerSet` and `countConnectedGroupMembers`; publish refresh diagnostics expose `topicPeers`, `expectedPeers`, `missingPeers`, and `backingOff`; and `PublishGroupMessage` emits `group:publish_debug.topicPeers`. `go-mknoon/node/pubsub_delivery_test.go::TestGO007MetricsDistinguishHostConnectionFromLiveTopicPeer` connects the expected peer at the libp2p host layer without joining it to the group topic, proves host `Connectedness` is connected while live topic peer count remains zero, publishes successfully with `peerCount == 0`, observes publish-refresh `topicPeers == 0`, `expectedPeers == 1`, `missingPeers == 1`, `backingOff == false`, observes `group:publish_debug.topicPeers == 0`, and proves the non-topic peer receives no group message event. Passed evidence: gofmt, exact GO-007 Go (`ok node 2.773s`), adjacent topic-peer/zero-peer/discovery selector (`ok node 5.810s`), selected native race selector (`ok node 98.541s`), Flutter send/drain app-facing gate (`+168`), and diff hygiene passed. Residual-only none; GO-010 is now covered separately. |
| GO-008 | Logs never include group keys, plaintext, or full sensitive payloads | Run all failure cases. | 1. Capture logs/events. 2. Search for plaintext/key/ciphertext policy violations. | No secret material or plaintext leaks. | P0 | Covered | Required | Required | N/A | N/A | N/A | Covered on 2026-05-13 by runtime privacy hardening, exact native no-secret diagnostic proof, exact Flutter bridge FLOW redaction proof, exact send/drain privacy proofs, analyzer, selected race gate, and named groups gate. `lib/core/bridge/go_bridge_client.dart` now logs `group_message:received` FLOW details as bounded metadata only while preserving full app callback delivery; `lib/core/utils/flow_event_emitter.dart` redacts broader direct and JSON/key-value encoded sensitive fields. `go-mknoon/node/pubsub_decryption_failure_test.go::TestGO008FailureDiagnosticsDoNotLeakSensitiveLogsOrEvents` proves decrypt, parse, and validation diagnostics/logs omit plaintext, group keys, ciphertext, nonce, signatures, and sender private keys. `test/core/bridge/go_bridge_client_test.dart` GO-008 selectors prove raw incoming group-message FLOW logs omit plaintext/media/ciphertext/group-key fields and diagnostic strings redact embedded JSON secrets. `send_group_message_use_case_test.dart::GO-008 EK-002 GI-035 pending inbox retry and flow logs omit protected plaintext` and `drain_group_offline_inbox_use_case_test.dart::GO-008 cursor error flow logs redact JSON payload plaintext and keys` prove app send/drain logs omit protected fragments. Passed evidence: gofmt, Dart format, scoped analyzer with no issues, focused Go (`ok node 1.171s`), focused bridge (`+2`), focused send (`+1`), focused drain (`+1`), full bridge (`+73`), full send+drain (`+165`), selected Go race gate (`ok node 94.663s`), and `./scripts/run_test_gates.sh groups` (`+159`). Residual-only none; GO-009 is covered separately and GO-012 is covered separately; GL-010 is covered separately; GL-016 is covered separately; GR-002 is the next unresolved session in ordered ledger order. |
| GO-009 | Test suite runs with race detector for group paths | Go tests available. | 1. Run go test -race for pubsub/inbox/relay. 2. Include concurrent join/update/send tests. | No races in maps, callbacks, recovery manager, or subscription handlers. | P0 | Covered | Required | Required | N/A | N/A | N/A | Covered on 2026-05-13 by a repo-owned lifecycle race fix plus clean selected race detector evidence. The required `go test -race ./node -run 'Group|PubSub|Relay' -count=1` initially failed with races in `TestRefreshRelaySession_ReRegistersPersonalNamespaceOnSuccess`, `TestReconnectRelays_WatchdogRestart_ReRegistersPersonalNamespace`, and `TestGL017StopClearsGroupRuntimeStateAndRequiresExplicitRejoinAfterRestart`: old startup/discovery goroutines raced with later resets of `relayReadyOnce` and `relayReady`. `go-mknoon/node/node.go` now allocates/captures a per-start `*sync.Once` and relay-ready channel for warm-relay goroutines, and `go-mknoon/node/pubsub.go` passes the current relay-ready channel into `groupPeerDiscoveryLoop`. Passed evidence: gofmt, focused failing race selector (`ok node 1.695s`), selected group/pubsub/relay race gate (`ok node 94.663s`), and `./scripts/run_test_gates.sh groups` (`+159`). Residual-only none; GO-012 is covered separately; GL-010 is covered separately; GL-016 is covered separately; GR-002 is the next unresolved session in ordered ledger order. |
| GO-010 | Goroutine leak check after join/leave/recovery cycles | Test harness can count goroutines or use leak checker. | 1. Repeat join/leave/recovery. 2. Wait. 3. Assert no leaked loops. | Subscription/discovery/recovery goroutines exit. | P1 | Covered | Required | Required | N/A | Recommended | N/A | Covered on 2026-05-14 by exact native group goroutine leak proof. `go-mknoon/node/pubsub_unsubscribe_exit_paths_test.go::TestGO010JoinLeaveRecoveryCyclesDoNotLeakGroupGoroutines` captures a baseline goroutine profile for group runtime stack frames (`handleGroupSubscription`, `groupPeerDiscoveryLoop`, `runGroupDiscoveryCycle`, and `discoverAndConnectGroupPeers`), repeats four group join/recovery/publish/leave cycles, proves group runtime state is removed after every leave, polls filtered group goroutine stacks back to baseline after every cycle, asserts `groupRecoverySem` has no held slots, verifies four register/discover recovery cycles ran, calls `Stop`, and proves group runtime goroutines remain at or below baseline. Existing `LeaveGroupTopic`, `Stop`, and `runGroupDiscoveryCycle` cleanup behavior was sufficient; no production runtime change was required. Passed evidence: gofmt, exact GO-010 Go (`ok node 0.578s`), adjacent lifecycle/recovery selector (`ok node 10.316s`), selected native race selector (`ok node 96.500s`), Flutter send/drain app-facing gate (`+168`), and diff hygiene passed. Residual-only none; all row-owned source rows in this breakdown are now Covered. |
| GO-012 | Flake budget for reliability tests is zero for deterministic fakes | Fake network tests. | 1. Run core fake tests many times. 2. Track flakes. | No nondeterministic misses; fake clock controls waits/backoff. | P0 | Covered | Recommended | Required | Required | Required | N/A | Covered on 2026-05-13 by deterministic fake-network seed/scheduler hardening, exact fake tests, five-iteration flake-budget runner, and named groups gate evidence. `test/shared/fakes/fake_group_pubsub_network.dart` now accepts a deterministic `randomSeed`, resets the seeded drop sequence in `resetCounters()`, and accepts an injected delay scheduler so tests can control delivery waits instead of sleeping on wall-clock time. `test/shared/fakes/fake_group_pubsub_network_test.dart::GO-012 seeded drops and scheduled delays are repeatable` proves seeded drop/delivery-delay behavior repeats exactly and records scheduled delays without wall-clock waiting; `::GO-012 resetCounters restores seeded drop sequence` proves reset restores the deterministic drop sequence. `scripts/run_group_fake_flake_budget.sh` runs the exact fake test plus selected GE-017/GE-019/GE-020/restart and resume-recovery fake-network reliability selectors repeatedly; final `GO012_REPEAT_COUNT=5 ./scripts/run_group_fake_flake_budget.sh` passed with `GO-012 fake flake budget passed: 5 iterations, 0 failures`. Passed evidence: Dart format, scoped analyzer with no issues, exact fake test (`+2`), five-iteration runner, `./scripts/run_test_gates.sh groups` (`+159`), and diff hygiene. Initial runner validation exposed a repo-owned script selector bug (`--plain-name` with a regex pipe matched no tests); the script now uses `--name` and the rerun passed. Residual-only none; GL-010 is covered separately; GL-016 is covered separately; GR-002 is the next unresolved session in ordered ledger order. |

## P0 smoke subset

Run these as the smallest release-blocking private-group reliability suite: `GE-001`, `GE-002`, `GE-004`, `GE-006`, `GE-010`, `GI-017`, `GI-019`, `GR-005`, `GR-016`, `GK-016`, `GM-006`, `GM-012`, `GA-024`, and `GO-001`.
