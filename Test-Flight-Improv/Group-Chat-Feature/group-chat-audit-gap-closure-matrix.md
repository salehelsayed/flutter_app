# Group Chat Audit Gap Closure Matrix

Source audit date: 2026-05-23

Scope: non-polish group chat audit findings from the May 23, 2026 read-only audit. Each row is row-owned by `Test-Flight-Improv/Group-Chat-Feature/group-chat-audit-gap-closure-session-breakdown.md`.

Status vocabulary:

- `Open`: code and tests still need to be changed.
- `Closed`: implementation and focused tests landed with evidence.
- `Blocked`: implementation cannot proceed under the current hard constraints.

## Matrix

| Row ID | Audit Finding | Severity | Status | Owner Scope | Closure Evidence |
|---|---|---|---|---|---|
| `GCA-001` | Group list load failures are logged only and can show an empty state with no retry/error. | GAP | Closed | `GroupListWired`, `GroupListScreen`, focused widget/unit tests | Closed 2026-05-23: `group_list_wired.dart` and `group_list_screen.dart` now surface `Couldn't load groups` with `Retry` instead of `No groups yet` when there is no displayable content; `group_list_wired_test.dart` covers the retryable failure path. Passed: `flutter test --no-pub test/features/groups/presentation/group_list_wired_test.dart --plain-name "load failure shows retryable error instead of empty state"`, `flutter test --no-pub test/features/groups/presentation/group_list_wired_test.dart test/features/groups/presentation/group_list_screen_test.dart`, and `git diff --check`. `./scripts/run_test_gates.sh groups` was run but remains red on unrelated isolated `group_membership_smoke_test.dart` `GM-028`. |
| `GCA-002A` | Create-group contact loading failures can show `No contacts available` instead of loading/error state. | GAP | Closed | `CreateGroupPickerWired`, `CreateGroupPickerScreen`, focused tests | Closed 2026-05-23: `create_group_picker_wired.dart` and `create_group_picker_screen.dart` now distinguish pending contact load, retryable load failure, true empty contact success, and loaded contacts; `create_group_picker_wired_test.dart` covers slow initial load and retryable load failure. Passed: `flutter test --no-pub test/features/groups/presentation/create_group_picker_wired_test.dart --plain-name "shows contact-loading state instead of empty state while contacts are loading"`, `flutter test --no-pub test/features/groups/presentation/create_group_picker_wired_test.dart --plain-name "contact load failure shows retryable error instead of empty state"`, `flutter test --no-pub test/features/groups/presentation/create_group_picker_wired_test.dart test/features/groups/presentation/create_group_picker_screen_test.dart`, and `git diff --check`. |
| `GCA-002B` | Add-member contact loading failures can show `No contacts available` instead of loading/error state. | GAP | Closed | `ContactPickerWired`, `ContactPickerScreen`, focused tests | Closed 2026-05-23: `contact_picker_wired.dart` and `contact_picker_screen.dart` now distinguish pending contact load, retryable load failure, true empty contact success, and loaded contacts; `contact_picker_wired_test.dart` covers slow initial load and retryable load failure with retry recovery. Passed: `flutter test --no-pub test/features/groups/presentation/contact_picker_wired_test.dart --plain-name "shows loading state instead of empty state while contacts load"`, `flutter test --no-pub test/features/groups/presentation/contact_picker_wired_test.dart --plain-name "contact load failure shows retryable error instead of empty state"`, `flutter test --no-pub test/features/groups/presentation/contact_picker_wired_test.dart test/features/groups/presentation/contact_picker_screen_test.dart`, and `git diff --check`. `./scripts/run_test_gates.sh groups` was run but remains red on known unrelated isolated `group_membership_smoke_test.dart` `GM-028`. |
| `GCA-003` | Group creation can silently skip selected members when `addGroupMember` fails. | GAP | Closed | `create_group_with_members_use_case.dart`, existing create picker warning path, focused tests | Closed 2026-05-23: `create_group_with_members_use_case.dart` now records failed selected add-member attempts in `addMemberFailures`, sets `hasWarnings`, and includes failed recipients in `buildCreateWarningMessage()` while continuing to exclude failed members from persisted members, group config, members-added publish payload, and invite fan-out. Evidence landed in `create_group_with_members_use_case_test.dart`. Passed: `flutter test --no-pub test/features/groups/application/create_group_with_members_use_case_test.dart --plain-name "excludes failed add-member recipients from persisted members, config, publish payload, and invite fan-out"`, `flutter test --no-pub test/features/groups/application/create_group_with_members_use_case_test.dart`, and `git diff --check`. Residual-only note: `./scripts/run_test_gates.sh groups` was run but remains red on invite/recovery/membership integration failures (`BB-007`, `IJ005`, startup/rejoin replay cases, `GE-017`/`GE-019`/`GE-020`, `GM-029`, and known `GM-028`) outside `GCA-003`; no direct row-owned use-case suite failure occurred. |
| `GCA-004` | Invite accept bridge errors consume/delete the pending invite and tombstone key material, removing the obvious retry affordance. | GAP | Closed | `accept_pending_group_invite_use_case.dart`, focused accept-pending use-case and invite round-trip tests | Closed 2026-05-23: `accept_pending_group_invite_use_case.dart` now commits consumed-invite state, welcome-key-package tombstones, and pending-invite deletion only after bridge join plus accepted-inbox drain succeeds; bridge join and accepted-inbox bridge errors leave the same pending invite retryable, and compatible already-materialized local group/key state retries join/drain while non-compatible duplicate/security behavior is preserved. Evidence landed in `accept_pending_group_invite_use_case_test.dart` and `invite_round_trip_test.dart`. Passed: `flutter test --no-pub test/features/groups/application/accept_pending_group_invite_use_case_test.dart --plain-name "GCA-004"`, duplicate-group focused check `returns duplicateGroup and removes pending row when group already exists`, stale newer-key security focused check `ML-019 KE-016 rejects stale invite against newer local removal and key state`, `flutter test --no-pub test/features/groups/integration/invite_round_trip_test.dart --plain-name "GCA-004"`, `flutter test --no-pub test/features/contact_request/integration/contact_request_flow_test.dart`, and `git diff --check`. Residual-only note: the full `accept_pending_group_invite_use_case_test.dart` suite still fails only on `accept replays backlog reactions when reactionRepo is provided` with `GROUP_HANDLE_INCOMING_MSG_LOCAL_MEMBERSHIP_MISSING` for `selfPeerId` `peer-self`, outside GCA-004. `./scripts/run_test_gates.sh groups` was not run in this bounded session. |
| `GCA-005` | Conversation message load failures are swallowed into an empty conversation state. | GAP | Closed | `GroupConversationWired`, `GroupConversationScreen`, focused tests | Closed 2026-05-23: `group_conversation_wired.dart` now records initial message-load failures as `Couldn't load messages`, `group_conversation_screen.dart` renders a retryable error state with `group-conversation-load-retry` instead of the normal `No messages yet` fallback when no messages are displayable, and `group_conversation_wired_test.dart` covers failure plus retry recovery. Evidence: the focused selector `flutter test --no-pub test/features/groups/presentation/group_conversation_wired_test.dart --plain-name "message load failure shows retryable error instead of empty conversation"` failed RED before implementation because `Couldn't load messages` was absent, then passed after implementation and again in QA; `dart format lib/features/groups/presentation/screens/group_conversation_wired.dart lib/features/groups/presentation/screens/group_conversation_screen.dart test/features/groups/presentation/group_conversation_wired_test.dart` passed; `git diff --check` passed. Residual-only note: `flutter test --no-pub test/features/groups/presentation/group_conversation_wired_test.dart test/features/groups/presentation/group_conversation_screen_test.dart` remains red at `+112 -17` on unrelated send/media/voice selectors around `GROUP_SEND_MSG_USE_CASE_EMPTY_MEMBERSHIP_DISSOLVED`, while the `GCA-005` selector passes inside that run. `./scripts/run_test_gates.sh groups` and manual debug verification were not run. |
| `GCA-006` | Composer can be enabled while identity is missing or late, but send silently returns. | GAP | Closed | `GroupConversationWired`, focused tests | Closed 2026-05-23: `group_conversation_wired.dart` now treats incomplete sender identity fields as not writable, shows `Waiting for your identity before you can send.`, rechecks complete identity before sending, suppresses stale missing-identity sends without publishing or persisting a message, and restores writable send after late identity load using the loaded peer id and username. Evidence landed in `group_conversation_wired_test.dart` as `GCA-006 missing identity keeps composer read-only until late identity load`. RED: `flutter test --no-pub test/features/groups/presentation/group_conversation_wired_test.dart --plain-name "GCA-006"` failed before implementation at `screen.canWrite`, actual `true`. Passed after implementation: `dart format --set-exit-if-changed lib/features/groups/presentation/screens/group_conversation_wired.dart test/features/groups/presentation/group_conversation_wired_test.dart`, `flutter analyze --no-pub lib/features/groups/presentation/screens/group_conversation_wired.dart test/features/groups/presentation/group_conversation_wired_test.dart`, `flutter test --no-pub test/features/groups/presentation/group_conversation_wired_test.dart --plain-name "GCA-006"`, `flutter test --no-pub test/features/groups/presentation/group_conversation_wired_test.dart --plain-name "UP-003 composer enables only for an active member with current key"`, and `git diff --check`. Residual-only note: the full `group_conversation_wired_test.dart` suite and `./scripts/run_test_gates.sh groups` remain red only on pre-existing empty-membership send/media/voice failures around `GROUP_SEND_MSG_USE_CASE_EMPTY_MEMBERSHIP_DISSOLVED` plus known `GM-028`; manual debug verification was not run. |
| `GCA-007` | Unauthorized or missing-group send results remove the optimistic message silently. | GAP | Closed | `GroupConversationWired`, focused tests | Closed 2026-05-23: `group_conversation_wired.dart` now shows concrete snackbars after terminal text sends return `unauthorized` (`You no longer have permission to send messages in this group.`) or `groupNotFound` (`This group is no longer available.`), while removing the optimistic row and keeping those outcomes non-retryable; the same-file voice terminal branch mirrors snackbar copy only, and dissolved-group handling remains distinct. Evidence landed in `group_conversation_wired_test.dart`. RED: the two focused selectors failed before implementation because the expected snackbar copy was missing. Passed after implementation/QA: `flutter test --no-pub test/features/groups/presentation/group_conversation_wired_test.dart --plain-name "unauthorized text send shows a concrete error instead of disappearing silently"`, `flutter test --no-pub test/features/groups/presentation/group_conversation_wired_test.dart --plain-name "missing-group text send shows a concrete error instead of disappearing silently"`, preservation selector `flutter test --no-pub test/features/groups/presentation/group_conversation_wired_test.dart --plain-name "failed publish shows message with failed status"`, full row-owned widget file `flutter test --no-pub test/features/groups/presentation/group_conversation_wired_test.dart` (`+88`), and `git diff --check`. Residual-only note: `./scripts/run_test_gates.sh groups` was run but remains red `+288 -13` on integration membership/replay/startup selectors (`BB-007`, `IJ005`, `BB-012`, `NW-004`, `IR-018`, `PL-004`, `IR-003`, `ST-004`, `GE-017`, `GE-019`, `GE-020`, `GM-029`, `GM-028`) outside `GCA-007`; manual debug verification was not run. |
| `GCA-008` | Member removal mutates local membership/config before publish/inbox/key rotation finishes; failure can leave partial state. | CRITICAL | Closed | `remove_group_member_use_case.dart`, `GroupInfoWired`, focused tests | Closed 2026-05-23: `group_info_wired.dart` now captures pre-removal group/member state before `removeGroupMember`, treats `group:publish` `ok:false` as removal failure, and rolls back failed post-remove publish/inbox/key-rotation paths by restoring the removed member, `lastMembershipEventAt`, failed local timeline message, and admin bridge config through `group:updateConfig`; `group_info_wired_test.dart` covers publish, inbox-store, and key-rotation failure rollback. RED: `flutter test --no-pub test/features/groups/presentation/group_info_wired_test.dart --plain-name "GCA-008"` failed before implementation because Alice stayed removed after `group:publish ok:false` and the flow continued to inbox/key rotation. Passed after implementation: the same focused `GCA-008` selector (`+1`), `stale non-member removal shows error and emits no removal side effects`, lower-level rollback selectors `restores removed member when group:updateConfig fails` and `ML-005 config sync failure restores removed online member`, full `flutter test --no-pub test/features/groups/application/remove_group_member_use_case_test.dart` (`+19`), and `git diff --check`. Residual-only note: `flutter test --no-pub test/features/groups/presentation/group_info_wired_test.dart` remains red `+44 -3` only on the existing `rotateAndDistributeGroupKey` pre-generation `group:updateKey` command-order mismatch, writer-leave rotation permission/generate-next-key residual, and `EK004` replay-envelope `messageId` residual; `./scripts/run_test_gates.sh groups` remains red `+288 -13` on known broad group residuals outside `GCA-008`. Manual verification was not run. |
| `GCA-009` | `leaveGroup` promises all local data removal but does not delete group messages when Group Info leave uses it directly. | GAP | Closed | `GroupInfoWired`, existing `deleteGroupAndMessages` path, focused tests | Closed 2026-05-23: `group_info_wired.dart` now keeps the active Group Info leave ordering by broadcasting self-removal first, then calls `deleteGroupAndMessages(...)` when `msgRepo` is available so the group row and local group messages are purged after successful `group:leave`; the existing direct `leaveGroup(...)` fallback remains for callers constructed without `msgRepo`. Evidence landed in `group_info_wired_test.dart` as `GCA-009 leave group deletes local messages and pops to first route`, which seeds target-group and other-group messages, verifies one `group:leave`, verifies the target-group message and group are deleted, verifies the other-group message remains, and verifies route pop. RED: `flutter test --no-pub test/features/groups/presentation/group_info_wired_test.dart --plain-name "GCA-009"` failed before implementation because `msg-left-group` remained. Passed after implementation: the same focused `GCA-009` command, `flutter test --no-pub test/features/groups/application/delete_group_and_messages_use_case_test.dart`, `flutter test --no-pub test/features/groups/application/leave_group_use_case_test.dart`, `dart format` and `dart format --set-exit-if-changed` on touched files, and `git diff --check`. Residual-only note: full `flutter test --no-pub test/features/groups/presentation/group_info_wired_test.dart` remains red only on a triaged unrelated writer-leave fixture conflict from dirty rotation permission work plus two GCA-008 remove-member failures; `FLUTTER_DEVICE_ID=macos ./scripts/run_test_gates.sh groups` remains red on known broader residuals (`BB-007`, `IJ005`, `BB-012`, `NW-004`, `IR-018`, `PL-004`, `IR-003`, `ST-004`, `GE-017`, `GE-019`, `GE-020`, `GM-029`, `GM-028`) outside `GCA-009`. Manual app verification was not run. |
| `GCA-010` | Voluntary leave broadcasts/saves self-removal and rotates before final bridge leave/local delete; later leave failure creates partial leave state. | GAP | Closed | `GroupInfoWired`, focused tests, leave-use-case contract inspection | Closed 2026-05-23 after same-session recovery: `group_info_wired.dart` now preserves pre-native voluntary self-removal publish, inbox-store replay, and key rotation for successful leave; if final native `group:leave` fails, Group Info rolls back local self-left timeline/key artifacts, preserves the group and member rows, stays on Group Info, and shows the failed-leave snackbar. `leave_group_use_case.dart` was inspected/restored to its narrow contract and has no final diff. RED: `flutter test --no-pub test/features/groups/presentation/group_info_wired_test.dart --plain-name "GCA-010 native leave failure rolls back local artifacts after pre-leave broadcast"` failed before recovery because the command log reached `group:leave` first with no `group:publish`, `group:inboxStore`, or `group:generateNextKey` before the forced native leave failure. Passed after recovery/QA: the same `GCA-010` selector, `BB-010 native leave failure stays on info screen and shows failed leave`, `sole admin leave stays on screen and shows an error`, `multi-admin leave broadcasts self-removal, rotates key, and pops to first route`, `writer leave broadcasts a durable left-the-group event before local cleanup`, `flutter test --no-pub test/features/groups/application/leave_group_use_case_test.dart`, `dart analyze lib/features/groups/application/leave_group_use_case.dart lib/features/groups/presentation/screens/group_info_wired.dart test/features/groups/presentation/group_info_wired_test.dart`, and `git diff --check`. Residual-only note: `./scripts/run_test_gates.sh groups` was run by Executor and QA but remains red only on broad group residuals outside `GCA-010` (`BB-007`, `IJ005`, `BB-012`, `NW-004`, `IR-018`, `PL-004`, `IR-003`, `ST-004`, `GE-017`, `GE-019`, `GE-020`, `GM-029`, `GM-028`). Manual verification was not run. |

Out of scope unless explicitly requested: audit finding 11, the no-confirmation leave UX polish item.
