# Private Group Chat Reliability Findings Matrix

Source audit date: 2026-05-23

Scope: critical/high private group chat reliability findings supplied on 2026-05-23. This matrix is row-owned by `Test-Flight-Improv/Group-Chat-Feature/private-group-chat-reliability-findings-2026-05-23-session-breakdown.md`.

Status vocabulary:

- `Open`: code and tests still need to be changed.
- `Closed`: implementation and focused tests landed with evidence.
- `Skipped`: current repo evidence makes the finding obsolete, non-applicable, or unsafe to fold into this rollout.
- `Blocked`: implementation cannot proceed under the current constraints.

## Matrix

| Row ID | Finding | Severity | Status | Critical Assessment | Owner Scope | Closure Evidence |
|---|---|---|---|---|---|---|
| `PGC-001` | Offline system replay can use transport peer as logical sender. | Critical | Closed | Meaningful and fixed. Offline system replay and history-repair replay now preserve logical `senderId` while passing transport identity separately. | `drain_group_offline_inbox_use_case.dart`, focused drain/listener replay tests | Closed by `PGC-DRAIN-1`: focused `PGC-001` system replay, history repair replay, and unbound-transport rejection selectors passed after listener compile was restored. |
| `PGC-002` | Offline payload can forge read/delivered receipts for other members. | Critical | Closed | Meaningful and fixed. Payload receipts are now accepted only when bound to the signed replay sender/device; forged third-party read/delivered claims are dropped. | `drain_group_offline_inbox_use_case.dart`, focused receipt replay tests | Closed by `PGC-DRAIN-1`: `PGC-002 replay receipts are dropped when memberPeerId differs from signed sender` and `PGC-002 sender-bound replay receipt remains idempotent` passed. |
| `PGC-003` | Uploaded migrations do not match helper schema. | Critical/High | Skipped | The uploaded bundle is incomplete. Current app is DB version 71 and has later migrations for group quoted IDs, reliability columns, transport peer IDs, device identities, cursors, receipts, local deletions, and key rotation drafts. Existing full migration-chain coverage exercises current schema. | None | Skipped with evidence: `lib/main.dart` runs migrations 026, 041, 061, 062, 066, 069, 070, 071; `test/core/database/integration/full_migration_chain_test.dart` covers fresh and upgrade paths. |
| `PGC-004` | Offline inbox page transaction silently drops cursor/receipt persistence when helper is missing. | High | Closed | Meaningful and fixed. Missing transaction helper now fails fast before `apply` runs. | `group_message_repository_impl.dart`, focused repository/drain transaction tests | Closed by `PGC-DB-1`: focused transaction-helper selector, owner repository suite, and DB helper suites passed. Broad groups gate remains unrelated dirty-tree follow-up. |
| `PGC-005` | DB read errors are treated as message not found. | High | Closed | Meaningful and fixed. `dbLoadGroupMessage` now returns `null` only for successful empty queries and rethrows DB/query errors. | `group_messages_db_helpers.dart`, focused helper/repository/incoming tests | Closed by `PGC-DB-1`: DB helper rethrow and repository propagation selectors passed; scoped owner suites passed. |
| `PGC-006` | Message save uses replace and can clobber operational state. | High | Closed | Meaningful and fixed. Message insert no longer uses destructive replace; duplicate handling preserves incoming operational fields and allows intentional outgoing transitions. | `group_messages_db_helpers.dart`, focused DB helper tests and self-echo/duplicate incoming tests | Closed by `PGC-DB-1`: duplicate incoming preservation, outgoing transition preservation, preservation selectors, and owner suites passed. |
| `PGC-007` | Content-based dedupe can drop legitimate distinct messages. | High | Closed | Meaningful and fixed. Content dedupe now runs only for legacy no-ID messages; stable message IDs remain the dedupe key. | `handle_incoming_group_message_use_case.dart`, focused incoming handler tests | Closed by `PGC-INCOMING-1`: `PGC-007` normal and event-log path selectors passed, preservation selectors passed, and the full focused handler file passed. Broad groups gate remains unrelated dirty-tree follow-up. |
| `PGC-008` | Listener stop/dispose races with in-flight async handlers. | High | Closed | Meaningful and fixed. Listener shutdown is awaitable where used and stream controller emissions are guarded against late writes after stop/dispose. | `group_message_listener.dart`, focused listener disposal tests | Closed by `PGC-LISTENER-1`: `PGC-008` focused listener tests and full `group_message_listener_test.dart` passed. |
| `PGC-009` | Membership-dependent message buffer is RAM-only. | High | Closed | Meaningful and fixed. Durable pending membership-dependent message storage was coherent in this repo and was implemented with migration/helper/model/repository/fake wiring and restart/startup flush tests. | listener, pending-membership repository/helper/migration or explicit blocker | Closed by `PGC-LISTENER-1`: `PGC-009` focused listener tests, new migration/helper tests, and `full_migration_chain_test.dart` passed. |
| `PGC-010` | Successful live publish can leave local message pending. | High | Closed | Meaningful and fixed. Live publish success with explicit topic peers now surfaces visible `sent` while failed/unknown inbox custody remains retryable through `inboxStored=false` and `inboxRetryPayload`. | `send_group_message_use_case.dart`, focused send matrix tests | Closed by `PGC-SEND-1`: `PGC-010` selector, preservation selectors, retry selector, scoped analyze/format, and diff checks passed. Full send suite has two unrelated dirty-tree failures recorded in the plan. |
| `PGC-011` | Go publish path can report success for messages peers reject. | High | Closed | Meaningful and fixed. Outbound message, reliable-send, and reaction publish paths now preflight active author/device binding before local publish or inbox store. | `go-mknoon/node/pubsub.go`, focused Go outbound validation tests | Closed by `PGC-GO-NODE-1`: direct `TestPGC011...` selectors and focused Go safety selectors passed. Broad `go test ./node` remains red on unrelated `TestGroupInboxRetrieveCursor_DefaultsLimitWhenZero`. |
| `PGC-012` | Go join/leave holds node mutex across blocking pubsub operations. | High | Closed | Meaningful and fixed. `JoinGroupTopic` and `LeaveGroupTopic` now run pubsub subscribe/cancel/close/unregister work outside `n.mu`. | `go-mknoon/node/pubsub.go`, focused Go deadlock/safety tests | Closed by `PGC-GO-NODE-1`: direct `TestPGC012...` mutex selectors and existing join/leave safety selectors passed. |
| `PGC-013` | Group key retention keeps only latest and previous key. | High | Closed | Meaningful and fixed. Dart key retention now uses a shared bounded 8-generation replay window, and stale replay classification uses the same minimum retained generation. | group repository key pruning, stale replay policy, focused retention/replay tests | Closed by `PGC-KEYS-1`: PGC-013 repository/drain selectors, future-key repair, key update safety selectors, full `group_repository_impl_test.dart`, format, and diff checks passed. Full drain file remains red from unrelated drain-suite residuals. |
| `PGC-014` | Offline drain starts unbounded parallel drains for all groups. | High | Closed | Meaningful and fixed. Group offline drain now uses bounded group-drain concurrency. | `drain_group_offline_inbox_use_case.dart`, focused concurrency test | Closed by `PGC-DRAIN-1`: `PGC-014 drainGroupOfflineInbox limits concurrent group drains` passed. Full drain file remains red from unrelated drain-suite residuals. |
| `PGC-015` | Relay recipient ACL is optional in relay core. | High | Closed | Meaningful and fixed. Relay `GroupInboxBackend` now requires recipient-scoped storage and the optional non-ACL fallback path is removed. | `go-relay-server/group_inbox_store.go`, `go-relay-server/inbox.go`, relay tests | Closed by `PGC-RELAY-1`: backend-contract RED proof, focused group inbox ACL selectors, broader group inbox slice, `cd go-relay-server && go test ./... -count=1`, and diff checks passed. |
| `PGC-016` | Reaction replay can fall through into message persistence when reaction repository is missing. | High | Closed | Meaningful and fixed. Reaction replay is terminal even when `reactionRepo` is absent, so missing reaction wiring cannot create malformed timeline messages. | `drain_group_offline_inbox_use_case.dart`, focused drain test | Closed by `PGC-DRAIN-1`: `PGC-016 group_reaction without reactionRepo does not persist a message` passed. |
| `PGC-017` | Signature and encryption AAD do not bind envelope headers. | High hardening | Skipped | The hardening is conceptually valid, but it is a protocol-version migration with cross-language compatibility risk. Current validators and existing tamper tests already fail closed semantically. It should be split into a dedicated versioned-envelope rollout, not folded into this reliability patch batch. | None in this rollout | Skipped with evidence: Go crypto still uses nil AAD and `groupId|epoch|ciphertext` signatures, but current validator-side field tamper protections are already covered by existing tests. |
| `PGC-018` | System messages can be persisted as visible chat if listener has no bridge. | High | Closed | Meaningful and fixed. System payload detection now happens before bridge-dependent handling; no-bridge listeners reject/defer system JSON without visible chat persistence. | `group_message_listener.dart`, focused listener no-bridge system tests | Closed by `PGC-LISTENER-1`: `PGC-018` focused listener selector and full `group_message_listener_test.dart` passed. |
