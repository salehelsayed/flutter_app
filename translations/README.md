# Translation Artifacts

The active localization source for the Flutter app is `lib/l10n/*.arb`.

The JSON, CSV, and markdown files in this directory are review artifacts from an
earlier translation pass. They are useful as historical notes, but they are not
loaded by the app and should not be treated as the source of truth for runtime
copy.
