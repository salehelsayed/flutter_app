# Task Prompt: FL_XS_02 - IdentityRepository Interface

## Instructions for AI Agent
You are implementing a specific task for a Flutter application. Follow the task specification exactly.

---

## Task Definition
```
[TASK FL_XS_02 – IdentityRepository interface]

Goal: Define repository interface for identity persistence.

What to implement:
  - abstract class IdentityRepository with methods:
      Future<IdentityModel?> loadIdentity();
      Future<void> saveIdentity(IdentityModel identity);
  - Doc comments describing behavior

Inputs:
  - None (interface only)

Outputs:
  - Abstract class definition

Flow_events:
  - None (interface only)

Constraints:
  - No implementation
  - Import IdentityModel

Deliverable:
  - File: lib/features/identity/domain/repositories/identity_repository.dart
```

## Begin Implementation
Output the complete Dart file.
