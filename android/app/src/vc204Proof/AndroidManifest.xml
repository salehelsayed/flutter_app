<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!-- This Activity exists only in the disposable VC2-04 proof APK. The
             DUMP permission limits its explicit host-driven entry point to adb
             shell while keeping every production call component non-exported. -->
        <activity
            android:name=".call.Vc204ProcessDeathSeedActivity"
            android:excludeFromRecents="true"
            android:exported="true"
            android:permission="android.permission.DUMP"
            android:theme="@android:style/Theme.Material.Light.NoActionBar" />
    </application>
</manifest>
