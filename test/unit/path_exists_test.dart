import 'package:flutter_test/flutter_test.dart';

void main() {
  test(
    'unit test path exists for CI matrix; unit coverage lives in feature/core suites',
    () {
      expect(true, isTrue);
    },
  );
}
