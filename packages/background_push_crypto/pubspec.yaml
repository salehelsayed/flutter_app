name: background_push_crypto
description: Background-engine-safe ML-KEM preview decryption for Mknoon pushes.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.9.0
  flutter: ">=3.35.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  plugin:
    platforms:
      android:
        package: com.mknoon.background_push_crypto
        pluginClass: BackgroundPushCryptoPlugin
